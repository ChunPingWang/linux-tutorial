# 06 - 檔案系統與儲存

涵蓋分割區、LVM（含線上擴充、快照與還原）、ext4 / XFS / Btrfs / ZFS、軟體 RAID、LUKS 加密、配額、網路儲存（NFS / Samba / iSCSI）、磁碟健康。本章全部流程已由 [`scripts/lab-storage-test.sh`](scripts/lab-storage-test.sh) 在兩個發行版上以 loop 裝置實測（LVM 擴充 / 快照合併還原、ext4 縮放、mdadm RAID1、LUKS2、Btrfs 子卷快照、ext4 與 XFS 配額）。

---

## 1. 觀察儲存狀態

```bash
lsblk -f                                 # 樹狀：裝置、檔案系統、UUID、掛載點
lsblk -o NAME,SIZE,TYPE,FSTYPE,MOUNTPOINT,MODEL,SERIAL,ROTA,TRAN
sudo blkid                               # UUID / LABEL
df -hT                                   # 使用率含類型
df -i                                    # inode 使用率（小檔案塞爆時 df -h 看不出來）
sudo du -xsh /var/* | sort -h            # 誰佔空間（-x 不跨檔案系統）
sudo ncdu -x /                           # 互動式
findmnt; findmnt /var                    # 掛載細節與選項
cat /proc/mounts | grep " / "
sudo fdisk -l; sudo parted -l            # 分割表
sudo pvs; sudo vgs; sudo lvs             # LVM 摘要
sudo smartctl -a /dev/sda                # 磁碟健康（smartmontools）
sudo hdparm -I /dev/sda | head           # 🟠 hdparm；🔵 hdparm
cat /sys/block/sda/queue/rotational      # 0 = SSD
```

---

## 2. 分割區

```bash
# GPT（UEFI、>2TB 必須）
sudo parted /dev/sdb mklabel gpt
sudo parted -a optimal /dev/sdb mkpart primary 1MiB 100%
sudo parted /dev/sdb set 1 lvm on
sudo partprobe /dev/sdb                  # 讓核心重讀分割表（不必重開機）
# 互動式：sudo gdisk /dev/sdb（GPT）、sudo fdisk /dev/sdb（也支援 GPT）、sudo cfdisk /dev/sdb（選單式）

# 常用 partition type
#   EFI System    → ef00 (gdisk) / esp on (parted)
#   Linux LVM     → 8e00 / lvm on
#   Linux RAID    → fd00 / raid on
#   Linux swap    → 8200
sudo sgdisk --zap-all /dev/sdb           # ⚠️ 清除分割表
sudo wipefs -a /dev/sdb1                 # ⚠️ 清除檔案系統簽章
```

---

## 3. LVM（Logical Volume Manager）

```
實體磁碟/分割區 (PV)  →  卷組 (VG)  →  邏輯卷 (LV)  →  檔案系統
/dev/sdb1, /dev/sdc   →  vg_data     →  lv_db (200G)  →  xfs 掛在 /var/lib/pgsql
```

### 3.1 建立

```bash
sudo pvcreate /dev/sdb1 /dev/sdc
sudo vgcreate vg_data /dev/sdb1 /dev/sdc
sudo lvcreate -L 200G -n lv_db vg_data
sudo lvcreate -l 100%FREE -n lv_backup vg_data        # 用光剩餘
sudo lvcreate -l 50%VG -n lv_half vg_data
sudo mkfs.xfs /dev/vg_data/lv_db                       # 🔵 慣例 xfs；🟠 mkfs.ext4
sudo mkdir -p /var/lib/pgsql && sudo mount /dev/vg_data/lv_db /var/lib/pgsql
# 裝置路徑兩種寫法等價：/dev/vg_data/lv_db == /dev/mapper/vg_data-lv_db
```

### 3.2 線上擴充（最常用；已實測）

```bash
# 情境 1：VG 還有空間
sudo lvextend -L +50G -r /dev/vg_data/lv_db     # -r = 順便 resize 檔案系統（ext4/xfs 皆可線上放大）
sudo lvextend -l +100%FREE -r /dev/vg_data/lv_db

# 情境 2：加新磁碟
sudo pvcreate /dev/sdd && sudo vgextend vg_data /dev/sdd
sudo lvextend -l +100%FREE -r /dev/vg_data/lv_db

# 情境 3：VM 磁碟被放大（同一顆磁碟變大）
sudo growpart /dev/sda 3                        # 🟠 cloud-guest-utils；🔵 cloud-utils-growpart
sudo pvresize /dev/sda3
sudo lvextend -l +100%FREE -r /dev/ubuntu-vg/ubuntu-lv      # 🟠 Ubuntu Server 預設 VG/LV 名稱
sudo lvextend -l +100%FREE -r /dev/fedora/root              # 🔵 Fedora Server 預設（也可能是 fedora_<hostname>）

# 沒用 -r 時手動：
sudo resize2fs /dev/vg_data/lv_db               # ext4
sudo xfs_growfs /var/lib/pgsql                  # xfs（給掛載點）
```

🟠 **Ubuntu Server 安裝後必做**：預設只把約一半 VG 給 `/`，`sudo lvextend -l +100%FREE -r /dev/ubuntu-vg/ubuntu-lv`。

### 3.3 縮小（只有 ext4 可以；XFS 不能縮）

```bash
sudo umount /data                               # ⚠️ 必須離線
sudo e2fsck -f /dev/vg_data/lv_x
sudo lvreduce -L 100G -r /dev/vg_data/lv_x      # -r 會先縮檔案系統再縮 LV（安全順序）
sudo mount /data
```

### 3.4 快照（備份前必做；已實測含合併還原）

```bash
# 建立快照（COW，只記錄差異；大小 = 預估快照存活期間的寫入量）
sudo lvcreate -s -L 10G -n lv_db_snap /dev/vg_data/lv_db
sudo lvs -o lv_name,lv_size,snap_percent vg_data     # Snap% 到 100% 快照就失效
# 掛載快照做備份（xfs 需 -o nouuid）
sudo mount -o ro,nouuid /dev/vg_data/lv_db_snap /mnt/snap
sudo tar czf /backup/db.tgz -C /mnt/snap .
sudo umount /mnt/snap && sudo lvremove -y /dev/vg_data/lv_db_snap
# 還原：把快照合併回原 LV（升級失敗回滾用）
sudo lvconvert --merge /dev/vg_data/lv_db_snap       # 若原 LV 使用中，會在下次啟用/重開機時合併
```

⚠️ 快照存在時原 LV 寫入效能下降（每次寫入要 COW），**不要長期保留**。

### 3.5 Thin Provisioning

```bash
sudo lvcreate -L 500G -T vg_data/thinpool
sudo lvcreate -V 1T -T vg_data/thinpool -n lv_thin1     # 可超配（1T 虛擬，實際用多少算多少）
sudo lvs -a                                             # 監看 Data% 不能到 100%
```

### 3.6 其他

```bash
sudo pvmove /dev/sdb1                     # 把資料搬離某 PV（換磁碟）
sudo vgreduce vg_data /dev/sdb1 && sudo pvremove /dev/sdb1
sudo lvrename vg_data lv_old lv_new
sudo vgrename vg_old vg_new               # ⚠️ 根 VG 改名要同步改 /etc/fstab、grub、initramfs
sudo vgchange -ay                         # 啟用所有 VG（救援模式常用）
sudo lvdisplay -m /dev/vg_data/lv_db      # 看 LV 在哪些 PV 上
cat /etc/lvm/lvm.conf; ls /etc/lvm/archive/    # 設定與 metadata 備份（vgcfgrestore 可還原）
```

---

## 4. 檔案系統

### 4.1 選擇

| | ext4 | XFS | Btrfs | ZFS |
|---|---|---|---|---|
| 預設於 | 🟠 Ubuntu | 🔵 Fedora Server | 🔵 Fedora Workstation | — |
| 線上放大 | ✅ | ✅ | ✅ | ✅（pool） |
| 縮小 | ✅（離線） | ❌ | ✅（線上） | ❌ |
| 快照 | ❌（靠 LVM） | ❌（靠 LVM） | ✅ 原生 | ✅ 原生 |
| 壓縮 | ❌ | ❌ | ✅ zstd/lzo | ✅ |
| 資料 checksum | ❌（僅 metadata） | ❌（僅 metadata） | ✅ | ✅ |
| 大檔 / 高並行 | 普通 | 極佳 | 普通 | 佳 |
| 最小大小 | 極小 | **300 MB**（xfsprogs 6+，實測） | 約 100 MB | — |
| 修復工具 | `e2fsck`（成熟） | `xfs_repair` | `btrfs check`（謹慎） | `zpool scrub` |
| 適用 | 通用、根目錄 | 資料庫、大檔、伺服器資料 | 桌面、需快照回滾 | NAS、大量資料完整性 |

### 4.2 建立與掛載

```bash
sudo mkfs.ext4 -L data /dev/vg_data/lv_x
sudo mkfs.ext4 -m 0 -T largefile4 /dev/...            # -m 0 不保留 5% root 空間（純資料碟）
sudo mkfs.xfs -L data /dev/vg_data/lv_x
sudo mkfs.btrfs -L data /dev/sdb1
sudo tune2fs -l /dev/... | grep -i "mount count"       # ext4 參數
sudo xfs_info /data
sudo e2label /dev/sdb1 data; sudo xfs_admin -L data /dev/sdb1

# /etc/fstab（用 UUID 不用 /dev/sdX，裝置順序會變）
sudo blkid /dev/vg_data/lv_x
```

```fstab
# <device>                                  <mount>      <type>  <options>                          <dump> <pass>
UUID=1234-...                               /data        xfs     defaults,noatime                   0 2
/dev/vg_data/lv_db                          /var/lib/pgsql xfs   defaults,noatime,nofail            0 2
UUID=abcd-...                               /home        ext4    defaults,noatime,usrquota          0 2
tmpfs                                       /tmp         tmpfs   defaults,noexec,nosuid,nodev,size=2G 0 0
UUID=5678-...                               none         swap    sw                                 0 0
//nas/share  /mnt/nas  cifs  credentials=/root/.smbcred,uid=1000,_netdev,nofail,x-systemd.automount 0 0
nas:/export/data  /mnt/nfs  nfs  defaults,_netdev,nofail,x-systemd.automount,x-systemd.idle-timeout=600 0 0
```

- `nofail`：裝置不存在也照常開機（外接／網路碟必加，否則開機卡在 emergency）。
- `_netdev`：網路就緒後才掛。
- `x-systemd.automount`：第一次存取時才掛（懶掛載）。
- `noatime`：不更新存取時間，減少寫入。
- `pass`：`/` 為 1、其他 2、不檢查 0（xfs / btrfs 一律 0）。

```bash
sudo mount -a                            # 測試 fstab（⚠️ 改 fstab 後務必執行，錯誤會導致開不了機）
sudo findmnt --verify                    # 檢查 fstab 語法
sudo systemctl daemon-reload             # systemd 會把 fstab 轉成 .mount 單元
sudo umount -l /data                     # lazy unmount（有程序佔用時）
sudo fuser -vm /data; sudo lsof +D /data # 誰在用
```

### 4.3 檢查與修復

```bash
# ⚠️ 一律在未掛載或唯讀狀態下執行
sudo e2fsck -f -y /dev/sdb1              # ext4
sudo xfs_repair /dev/sdb1                # xfs（先 xfs_repair -n 預覽；有 log 問題時 -L 清 log，會丟最近寫入）
sudo btrfs check /dev/sdb1               # btrfs（--repair 是最後手段）
sudo btrfs scrub start /data             # btrfs 線上校驗（可排程每月）
# 開機自動 fsck：ext4 依 tune2fs -c（最大掛載次數）；強制：sudo touch /forcefsck（🟠）或核心參數 fsck.mode=force
```

### 4.4 Btrfs（🔵 Fedora Workstation 預設）

Fedora 的預設佈局：一個 Btrfs 檔案系統、兩個子卷 `root`（掛 `/`）與 `home`（掛 `/home`），掛載選項 `compress=zstd:1`。

```bash
sudo btrfs subvolume list /
sudo btrfs filesystem usage /            # 真實使用量（df 對 Btrfs 不準）
sudo btrfs filesystem df /
sudo btrfs subvolume create /data/projects
sudo btrfs subvolume snapshot -r /home /.snapshots/home-$(date +%F)   # 唯讀快照（瞬間完成）
sudo btrfs subvolume delete /.snapshots/home-2026-08-01
sudo btrfs property set /path ro false
# 還原：把快照 snapshot 回來或直接把 fstab 的 subvol= 指向快照
sudo btrfs send /.snapshots/home-A | sudo btrfs receive /mnt/backup/    # 增量備份到另一顆碟
sudo btrfs send -p /.snapshots/home-A /.snapshots/home-B | sudo btrfs receive /mnt/backup/
sudo btrfs filesystem defragment -r -czstd /data       # 壓縮既有檔案
sudo btrfs balance start -dusage=50 /                  # 重新平衡（空間碎片）
sudo btrfs device add /dev/sdc /data && sudo btrfs balance start /data   # 線上加碟
sudo btrfs filesystem resize -50G /data                # 線上縮小
# 快照管理工具：snapper（sudo dnf install snapper；🟠 apt install snapper），可配 dnf/apt 自動在更新前拍快照
```

💡 Btrfs 的 `nodatacow`（`chattr +C`）：資料庫、VM 映像目錄建議設定，避免 COW 碎片化。

### 4.5 ZFS（🟠 Ubuntu 原生支援）

```bash
sudo apt install -y zfsutils-linux       # 🟠 核心內建模組
# 🔵 Fedora 需 OpenZFS 官方 repo + DKMS（每次核心升級重編，正式主機不建議）
sudo zpool create -o ashift=12 tank mirror /dev/sdb /dev/sdc
sudo zfs create -o compression=zstd -o mountpoint=/data tank/data
sudo zfs set quota=500G tank/data
sudo zfs snapshot tank/data@2026-08-28
sudo zfs rollback tank/data@2026-08-28
sudo zfs send tank/data@a | ssh backup zfs receive pool/data
sudo zpool status; sudo zpool scrub tank
sudo zfs list -t snapshot
```

---

## 5. 軟體 RAID（mdadm，已實測 RAID1）

硬體 RAID 卡 / 雲端磁碟已有冗餘時不需要；本機多顆碟建議 **RAID1（2 顆）/ RAID10（4+ 顆）**；RAID5 在大容量碟重建風險高。

```bash
sudo apt install -y mdadm    # 🟠      sudo dnf install -y mdadm    # 🔵
sudo mdadm --create /dev/md0 --level=1 --raid-devices=2 --metadata=1.2 /dev/sdb /dev/sdc
sudo mdadm --create /dev/md1 --level=10 --raid-devices=4 /dev/sd[d-g]
cat /proc/mdstat                          # 實測：md0 : active raid1 ... [2/2] [UU]
sudo mdadm --detail /dev/md0
# 永久化（開機自動組裝）
sudo mdadm --detail --scan | sudo tee -a /etc/mdadm/mdadm.conf     # 🟠 路徑 /etc/mdadm/mdadm.conf
sudo mdadm --detail --scan | sudo tee -a /etc/mdadm.conf           # 🔵 路徑 /etc/mdadm.conf
sudo update-initramfs -u     # 🟠      sudo dracut --force     # 🔵
# 之後把 /dev/md0 當一般裝置用（pvcreate、mkfs…）

# 維護
sudo mdadm /dev/md0 --fail /dev/sdc --remove /dev/sdc        # 標記壞碟並移除
sudo mdadm /dev/md0 --add /dev/sdh                           # 加新碟，自動重建
sudo mdadm --grow /dev/md0 --raid-devices=3                  # 擴充
echo check | sudo tee /sys/block/md0/md/sync_action           # 手動一致性檢查（🟠 mdcheck_start.timer 每月；🔵 raid-check.timer 每週）
sudo mdadm --monitor --scan --daemonise --mail=root           # 監控通知（兩者 mdmonitor.service 皆隨套件啟用）
sudo mdadm --stop /dev/md0; sudo mdadm --zero-superblock /dev/sdb   # ⚠️ 拆除
```

---

## 6. LUKS 磁碟加密（已實測 LUKS2）

```bash
sudo cryptsetup luksFormat --type luks2 /dev/sdb1          # ⚠️ 清除資料；會要求輸入 YES 與密碼
sudo cryptsetup open /dev/sdb1 cryptdata                   # 解鎖 → /dev/mapper/cryptdata
sudo mkfs.xfs /dev/mapper/cryptdata && sudo mount /dev/mapper/cryptdata /data
sudo cryptsetup close cryptdata
sudo cryptsetup luksDump /dev/sdb1                          # 實測：Version 2、cipher aes-xts-plain64
sudo cryptsetup luksAddKey /dev/sdb1                        # 加第二組密碼（最多 32 個 slot）
sudo cryptsetup luksRemoveKey /dev/sdb1
sudo cryptsetup luksHeaderBackup /dev/sdb1 --header-backup-file /root/sdb1-luks-header.img   # 💡 header 壞了資料全毀，務必備份到別處
sudo cryptsetup luksChangeKey /dev/sdb1

# 開機自動解鎖（金鑰檔）/etc/crypttab：
#   cryptdata  UUID=<luks uuid>  /root/keyfile  luks
sudo dd if=/dev/urandom of=/root/keyfile bs=512 count=8 && sudo chmod 400 /root/keyfile
sudo cryptsetup luksAddKey /dev/sdb1 /root/keyfile
# 然後 /etc/fstab 用 /dev/mapper/cryptdata；根目錄加密需重建 initramfs

# TPM2 自動解鎖（免密碼開機，兩者共通；需 systemd ≥ 248）
sudo apt install -y tpm2-tools    # 🟠   /   🔵 預設有
sudo systemd-cryptenroll --tpm2-device=auto --tpm2-pcrs=7 /dev/sdb1
# /etc/crypttab 加 tpm2-device=auto 選項；🟠 update-initramfs -u；🔵 dracut -f
sudo systemd-cryptenroll --recovery-key /dev/sdb1          # 產生救援金鑰
```

---

## 7. Swap

```bash
free -h; swapon --show; cat /proc/swaps
# 交換檔（🟠 Ubuntu Server 預設 /swap.img）
sudo fallocate -l 4G /swapfile && sudo chmod 600 /swapfile
sudo mkswap /swapfile && sudo swapon /swapfile
echo "/swapfile none swap sw 0 0" | sudo tee -a /etc/fstab
# ⚠️ Btrfs 上的交換檔：sudo btrfs filesystem mkswapfile --size 4g /swapfile（需 nodatacow，Btrfs 5.x+ 指令自動處理）
# 🔵 Fedora 預設 zram（壓縮記憶體）：
cat /etc/systemd/zram-generator.conf; zramctl
# 🟠 Ubuntu 要 zram：sudo apt install zram-tools（/etc/default/zramswap）或同樣用 systemd-zram-generator
sudo sysctl vm.swappiness=10                          # 伺服器建議 10（預設 60；🔵 zram 環境可留高）
```

---

## 8. 磁碟配額（已實測 ext4 與 XFS）

```bash
sudo apt install -y quota    # 🟠      sudo dnf install -y quota    # 🔵
# ext4：掛載選項 usrquota,grpquota（或 mkfs 時 -O quota 內建 journaled quota）
sudo mount -o remount,usrquota,grpquota /home
sudo quotacheck -cugm /home && sudo quotaon /home       # -O quota 的檔案系統不需 quotacheck/quotaon
sudo setquota -u alice 10G 12G 0 0 /home                # soft hard inode-soft inode-hard
sudo edquota -u alice; sudo quota -u alice; sudo repquota -a
# XFS：掛載選項 uquota / gquota / pquota（專案配額，目錄層級）
sudo xfs_quota -x -c 'limit -u bsoft=10g bhard=12g alice' /home
sudo xfs_quota -x -c 'report -h' /home
# XFS 專案配額（限制某目錄大小）：
echo "1:/data/projects/a" | sudo tee -a /etc/projects; echo "proja:1" | sudo tee -a /etc/projid
sudo xfs_quota -x -c 'project -s proja' -c 'limit -p bhard=50g proja' /data
```

---

## 9. 網路儲存

### 9.1 NFS

```bash
# 伺服器
sudo apt install -y nfs-kernel-server   # 🟠     sudo dnf install -y nfs-utils   # 🔵
sudo mkdir -p /export/data && sudo chown nobody:nogroup /export/data   # 🟠 nogroup；🔵 nobody:nobody
echo "/export/data 192.168.1.0/24(rw,sync,no_subtree_check,root_squash)" | sudo tee -a /etc/exports
sudo exportfs -ra && sudo exportfs -v
sudo systemctl enable --now nfs-server          # 🟠 nfs-kernel-server / 🔵 nfs-server（兩者都接受 nfs-server 別名）
# 🟠 ufw allow from 192.168.1.0/24 to any port nfs
# 🔵 firewall-cmd --permanent --add-service={nfs,rpc-bind,mountd} && firewall-cmd --reload
# 🔵 SELinux：分享家目錄要 setsebool -P use_nfs_home_dirs 1；分享其他目錄標 public_content_rw_t
# 客戶端
sudo apt install -y nfs-common   # 🟠     sudo dnf install -y nfs-utils   # 🔵
showmount -e nas
sudo mount -t nfs -o vers=4.2 nas:/export/data /mnt/nfs
# fstab 見 §4.2；NFSv4 只需 2049 埠；nconnect=4 可提升吞吐
```

### 9.2 Samba（SMB / CIFS）

```bash
sudo apt install -y samba    # 🟠     sudo dnf install -y samba    # 🔵
sudo tee -a /etc/samba/smb.conf <<'EOF'
[share]
   path = /srv/share
   browseable = yes
   read only = no
   valid users = @smbusers
   create mask = 0660
   directory mask = 2770
EOF
sudo groupadd smbusers && sudo usermod -aG smbusers alice
sudo smbpasswd -a alice                        # Samba 密碼獨立於系統密碼
testparm                                       # 檢查語法
sudo systemctl enable --now smbd nmbd          # 🟠；🔵 為 smb nmb
# 🟠 ufw allow Samba    🔵 firewall-cmd --permanent --add-service=samba && firewall-cmd --reload
# 🔵 SELinux：sudo semanage fcontext -a -t samba_share_t "/srv/share(/.*)?" && sudo restorecon -R /srv/share
#             家目錄：setsebool -P samba_enable_home_dirs 1
# 客戶端
sudo apt install -y cifs-utils   # 🟠     sudo dnf install -y cifs-utils   # 🔵
sudo mount -t cifs //nas/share /mnt/nas -o username=alice,uid=1000,vers=3.1.1
# 憑證檔（fstab 用）：/root/.smbcred 內容 username=alice\npassword=xxx，chmod 600
```

### 9.3 iSCSI

```bash
# Target（伺服器）：targetcli（兩者同名）
sudo apt install -y targetcli-fb   # 🟠     sudo dnf install -y targetcli   # 🔵
sudo targetcli
#  /backstores/block create disk1 /dev/vg_data/lv_iscsi
#  /iscsi create iqn.2026-08.com.example:storage.disk1
#  /iscsi/iqn.../tpg1/luns create /backstores/block/disk1
#  /iscsi/iqn.../tpg1/acls create iqn.2026-08.com.example:client1
#  saveconfig ; exit
sudo systemctl enable --now target       # 🔵；🟠 為 rtslib-fb-targetctl
# Initiator（客戶端）
sudo apt install -y open-iscsi   # 🟠     sudo dnf install -y iscsi-initiator-utils   # 🔵
cat /etc/iscsi/initiatorname.iscsi
sudo iscsiadm -m discovery -t st -p 192.168.1.50
sudo iscsiadm -m node -T iqn.2026-08.com.example:storage.disk1 -p 192.168.1.50 --login
sudo iscsiadm -m node -T iqn... -p 192.168.1.50 --op update -n node.startup -v automatic
lsblk   # 出現新的 sdX；fstab 需 _netdev
```

---

## 10. 磁碟健康與效能測試

```bash
sudo apt install -y smartmontools   # 🟠     sudo dnf install -y smartmontools   # 🔵
sudo smartctl -H /dev/sda                     # 健康總結
sudo smartctl -a /dev/sda | grep -E "Reallocated|Pending|Uncorrect|Power_On|Temperature"
sudo smartctl -t short /dev/sda               # 自我測試
sudo nvme smart-log /dev/nvme0                # NVMe（nvme-cli）
sudo systemctl enable --now smartd            # 🟠 smartmontools 服務名 smartd；🔵 smartd；設定 /etc/smartd.conf（🟠）/ /etc/smartmontools/smartd.conf（🔵）

# 效能
sudo apt install -y fio   # 🟠     sudo dnf install -y fio   # 🔵
fio --name=randread --rw=randread --bs=4k --size=1G --numjobs=4 --iodepth=32 --runtime=30 --time_based --group_reporting --directory=/data
sudo hdparm -tT /dev/sda                      # 簡易循序讀
dd if=/dev/zero of=/data/test bs=1M count=1024 oflag=direct status=progress   # 簡易循序寫
iostat -xz 1                                  # 🟠 sysstat；🔵 sysstat
sudo fstrim -av                               # SSD TRIM（兩者都有 fstrim.timer 每週執行）
```

---

## 11. 本章差異總結

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 預設 FS | ext4（Server 可選 LVM） | Server：xfs on LVM；Workstation：Btrfs 子卷 |
| 預設 VG / LV 名稱 | `ubuntu-vg/ubuntu-lv` | `fedora/root`（或 `fedora_<host>/root`） |
| 安裝後 LVM | 只用一半，需 `lvextend` | 已配置完 |
| growpart 套件 | `cloud-guest-utils` | `cloud-utils-growpart` |
| mdadm 設定檔 | `/etc/mdadm/mdadm.conf` | `/etc/mdadm.conf` |
| RAID 定期檢查 | `mdcheck_start.timer` + `mdmonitor-oneshot.timer` | `raid-check.timer`（`/etc/sysconfig/raid-check`） |
| initramfs 重建 | `update-initramfs -u` | `dracut --force` |
| swap 預設 | `/swap.img` 交換檔 | zram（Workstation）；Server 無或 LV |
| ZFS | 原生（`zfsutils-linux`） | 第三方 DKMS |
| NFS 伺服器套件 / 服務 | `nfs-kernel-server` / `nfs-kernel-server` | `nfs-utils` / `nfs-server` |
| NFS nobody 群組 | `nogroup` | `nobody` |
| Samba 服務名 | `smbd`、`nmbd` | `smb`、`nmb` |
| iSCSI initiator 套件 | `open-iscsi` | `iscsi-initiator-utils` |
| iSCSI target 套件 / 服務 | `targetcli-fb` / `rtslib-fb-targetctl`（預設啟用） | `targetcli` / `target` |
| smartd 設定檔 | `/etc/smartd.conf` | `/etc/smartmontools/smartd.conf` |
| 分享目錄額外步驟 | 無 | SELinux fcontext / boolean |
| 快照工具 | LVM 快照 | Btrfs 快照 + snapper（Workstation）；LVM（Server） |
