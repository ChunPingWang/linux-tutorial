# 05 - 網路與防火牆

**本章解決什麼問題**：網路是「一改錯就連不上機器、連修都沒辦法修」的領域——遠端把 IP 改錯、`ufw enable` 前忘了放行 SSH、手改的 `/etc/resolv.conf` 過幾分鐘又被改回去，都是常見的自我封鎖事故。本章的取捨原則：先確認「這台機器的網路是誰在管」（Netplan／networkd／NetworkManager），再用各發行版的正規工具改設定，並優先採用有防斷線機制的流程（`netplan try`、firewalld 的 runtime 先試再存）；兩者共通的底層工具（`ip`、`ss`、`nft`）則當作觀察與除錯的共同語言。

本章是兩個發行版差異最大的領域之一：Ubuntu 用 **Netplan**（YAML）與 **ufw**，Fedora 用 **NetworkManager**（nmcli）與 **firewalld**。底層兩者都是 Linux 核心的 netlink + nftables，所以 `ip`、`ss`、`nft` 等工具完全共通。

本章的 Netplan 設定經 `netplan generate` 驗證、nmcli 設定經 `nmcli --offline` 驗證、防火牆規則於 systemd 容器內實際套用驗證。

---

## 1. 網路堆疊總覽

**名詞與關係**：本章出現的工具其實疊成四層——「設定入口 → 後端 daemon → netlink → 核心」，防火牆與 DNS 再各自掛在核心旁邊。兩個發行版只是在最上面的「設定入口」與「防火牆前端」做了不同選擇，越往下越一致：

```
                     🟠 Ubuntu                                   🔵 Fedora
設定入口      /etc/netplan/*.yaml（Netplan）                     nmcli / nmtui / GUI / Cockpit
                  │ renderer: networkd 或 NetworkManager               │
                  ▼                                                    ▼
後端 daemon   systemd-networkd（Server）或 NetworkManager（Desktop）   NetworkManager（Server 與 Workstation）
              產物：/run/systemd/network/*.network                   設定：/etc/NetworkManager/system-connections/
                    /run/NetworkManager/system-connections/                *.nmconnection（keyfile）
                  │                                                    │
                  └───────────────────── netlink ──────────────────────┘ ◀── iproute2（ip / ss）也走這條
                                            ▼
核心          網卡（predictable 命名 enp0s3…）、位址、路由表、netfilter（封包過濾 / NAT 框架）
                                            ▲
防火牆        ufw ─▶ iptables-nft ─▶ nftables ─┘                     firewalld ─▶ nftables ─┘
DNS           systemd-resolved（兩者共通：/etc/resolv.conf → 127.0.0.53 stub resolver）
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Netplan** | Canonical 開發的網路設定抽象層：讀 `/etc/netplan/*.yaml`，轉譯成後端 daemon 的設定檔 | 只在 Ubuntu 有；它自己不碰網卡，全靠 `renderer` 指定的後端執行 |
| **renderer** | Netplan YAML 裡的欄位，指定把設定交給 `networkd` 還是 `NetworkManager` | 決定 Netplan 的產物寫到 `/run/systemd/network/` 還是 `/run/NetworkManager/`；Server 預設前者、Desktop 後者 |
| **systemd-networkd** | systemd 內建的輕量網路 daemon，讀 `.network` 檔設定網卡；無 GUI、不處理 Wi-Fi 漫遊 | Ubuntu Server 的預設後端；Fedora 有裝但預設不啟用，兩邊都不建議與 NM 同時管同一張卡 |
| **NetworkManager（NM）** | Red Hat 主導的網路 daemon，處理有線／Wi-Fi／VPN 與動態切換，提供 nmcli／nmtui／GUI／D-Bus 介面 | Fedora 全場景的唯一後端；Ubuntu Desktop 的後端（由 Netplan 以 `renderer: NetworkManager` 餵設定） |
| **keyfile** | NM 的連線設定檔格式（ini 語法，副檔名 `.nmconnection`），一個檔案就是一個 connection profile | `nmcli` 改的就是它；Netplan 交給 NM 的產物也是 keyfile，只是放在 `/run` 下 |
| **cloud-init** | 雲端映像第一次開機時讀雲平台 metadata、設定主機名／使用者／網路的工具 | Ubuntu 上它把網路寫成 `/etc/netplan/50-cloud-init.yaml`；Fedora 雲映像則寫成 NM keyfile |
| **netlink** | 核心與使用者空間交換網路設定（位址、路由、鏈路狀態）的 socket 介面 | networkd、NM、iproute2 全部經此對核心下命令，所以不論哪個前端，`ip` 看到的都是真實狀態 |
| **iproute2** | `ip`、`ss`、`bridge`、`tc` 等指令所屬的套件，透過 netlink 觀察或直接修改核心狀態 | 取代 net-tools（`ifconfig`／`netstat`／`route`）；兩發行版共通，是 §2 與 §10 的除錯語言 |
| **predictable interface names** | systemd 依 PCI／韌體位置命名網卡（`enp0s3`、`ens18`、`eno1`）的規則 | 取代會隨偵測順序變動的 `eth0`；Netplan 與 nmcli 的設定都要引用這種名字，本章範例中的 `eth0` 只是示意 |
| **systemd-resolved** | systemd 的本機 DNS 解析 daemon，在 `127.0.0.53` 提供 stub resolver（本機代理） | 由 networkd 或 NM 把每張卡的 DNS 交給它；`/etc/resolv.conf` 只是指向它的符號連結，見 §5 |
| **netfilter** | 核心內建的封包過濾與 NAT 框架，在封包路徑上提供 hook 點 | nftables 與 iptables 都是它的使用者介面；ufw／firewalld 的規則最終都在這裡生效 |
| **nftables** | 取代 iptables 的規則語言與工具（`nft`），核心 3.13 起內建 | firewalld 原生使用；ufw 透過 iptables-nft 間接使用；直接手寫見 §8 |
| **iptables-nft** | 以 iptables 語法為介面、但把規則寫進 nftables 的相容層 | 兩發行版的 `iptables` 指令實為它；ufw 產生的 iptables 規則靠它落地，所以 `nft list ruleset` 看得到 ufw 的規則 |
| **ufw** | Ubuntu 的簡易防火牆前端（Uncomplicated Firewall），一行人話等於一條規則 | 產生 iptables 語法 → iptables-nft → nftables；預設裝了但沒開，見 §6 |
| **firewalld** | Red Hat 的動態防火牆 daemon，以 zone（信任等級）為單位管理規則 | 直接寫 nftables；Fedora 預設啟用，見 §7 |
| **ifupdown ／ network-scripts** | 舊世代設定方式：Debian 的 `/etc/network/interfaces`、Red Hat 的 `ifcfg-*` 腳本 | 分別被 Netplan 與 NM keyfile 取代；老文件教的做法在兩者現行版本都不再適用 |

**為什麼**：排網路問題的第一件事是確認「誰在管這張網卡」。在 Ubuntu Server 上用 `nmcli` 改設定，只會看到 unmanaged 而毫無作用；在 Fedora 上找 `/etc/netplan/` 則根本不存在。搞錯管理者，改了設定檔卻「怎麼都不生效」，是最浪費時間的狀況。

兩者之所以不同：Canonical 想讓 Server（systemd-networkd）與 Desktop（NetworkManager）共用同一份宣告式設定，也方便雲端的 cloud-init 寫入網路設定，所以在後端之上加了 Netplan 這層抽象；Red Hat 則選擇讓 NetworkManager 成為所有場景的單一入口，不另加轉譯層。

| 層級 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 設定抽象層 | **Netplan**（`/etc/netplan/*.yaml`） | 無（直接設定 NetworkManager） |
| 後端 daemon | Server：**systemd-networkd**；Desktop：**NetworkManager** | **NetworkManager**（Server 與 Workstation 皆是） |
| 設定檔實體位置 | networkd：`/run/systemd/network/10-netplan-*.network`（由 netplan 產生，勿手改）<br>NM：`/run/NetworkManager/system-connections/netplan-*` | `/etc/NetworkManager/system-connections/*.nmconnection`（keyfile 格式） |
| CLI | `netplan`、`networkctl`（networkd）、`nmcli`（若用 NM 後端） | `nmcli`、`nmtui` |
| DNS 解析 | systemd-resolved（`/etc/resolv.conf` → `127.0.0.53`） | systemd-resolved（Fedora 33+；`/etc/resolv.conf` → `127.0.0.53`） |
| 網卡命名 | Predictable（`enp0s3`、`ens18`、`eno1`） | Predictable（同左） |
| 防火牆前端 | **ufw** | **firewalld** |
| 防火牆後端 | iptables-nft（ufw 仍以 iptables 語法寫規則，由 nft 執行） | nftables（原生） |
| 舊工具 | `ifupdown`（`/etc/network/interfaces`）已不用 | `network-scripts`（`/etc/sysconfig/network-scripts/ifcfg-*`）已在 Fedora 36 移除 |

**怎麼做**：用下列指令確認目前的後端，再決定往 §3 或 §4 走：

```bash
# 查目前是哪個後端在管
networkctl status                       # 🟠 networkd 有輸出即是它在管
nmcli device status                     # 🔵 / 🟠 Desktop；unmanaged 代表不歸 NM 管
```

---

## 2. 共通的觀察指令（iproute2）

**名詞與關係**：本節的指令分成「看核心狀態」與「主動探測」兩類，前者透過 netlink 讀核心，後者則是真的把封包送出去：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **net-tools** | `ifconfig`、`netstat`、`route`、`arp` 所屬的舊套件，讀 `/proc/net` 取得狀態 | 2001 年後停止維護，看不到多個位址、policy routing 等新功能；由 iproute2（§1）取代，兩發行版預設不裝 |
| **ethtool** | 查詢與設定網卡驅動層資訊（速率、雙工、link 偵測、offload、統計）的工具 | 比 `ip link` 更底層，是 §10 第一步「線到底有沒有接上」的判斷依據 |
| **tcpdump** | 把經過網卡的封包抓下來印出的工具 | 當 `ip`／`ss` 看起來都正常、但就是不通時，用它證明封包到底有沒有進來或出去 |
| **dig ／ nc ／ curl ／ mtr** | 分別針對 DNS、TCP 埠、HTTP、逐跳延遲的探測工具 | 對應 §10 除錯流程的第 4～8 步；各在不同套件裡（🟠 `dnsutils`、`mtr-tiny`；🔵 `bind-utils`、`mtr`） |

**為什麼**：不管設定層是 Netplan 還是 nmcli，最後都是核心在跑，所以看「實際生效的狀態」要用 iproute2，而不是看設定檔——設定檔寫了 `192.168.1.10` 不代表核心真的有這個位址。`ifconfig`、`netstat`、`route` 這些 net-tools 已停止維護，兩個發行版預設都不裝，習慣要換過來。

**怎麼做**：

```bash
ip -br link                              # 網卡與狀態（UP/DOWN）
ip -br addr                              # IP 摘要
ip addr show dev eth0
ip route; ip -6 route                    # 路由表
ip route get 8.8.8.8                     # 到某目的地會走哪條路
ip neigh                                 # ARP 表
ip -s link show eth0                     # 流量統計 / 錯誤計數
ss -ltnp                                 # 監聽的 TCP 埠與程序（取代 netstat -tlnp）
ss -s                                    # 連線統計
ss -tan state established '( dport = :443 )'
ethtool eth0; ethtool -S eth0            # 網卡速率 / 統計（🟠 需 apt install ethtool；🔵 預設有）
resolvectl status; resolvectl query example.com
ping -c 3 1.1.1.1; ping6 -c 3 2606:4700:4700::1111
traceroute 8.8.8.8 / tracepath 8.8.8.8 / mtr -rw 8.8.8.8   # 🟠 traceroute、mtr-tiny；🔵 traceroute、mtr
dig +short example.com A; dig @1.1.1.1 example.com MX     # 🟠 dnsutils；🔵 bind-utils
nc -zv host 443; curl -vI https://host   # 埠與 HTTP 測試
sudo tcpdump -i eth0 -nn port 80 -c 20   # 抓包
sudo nmap -sT -p 1-1024 host             # 掃埠（自己主機或有授權的）

# 臨時修改（重開機失效；正式設定請用 §3 / §4）
sudo ip addr add 192.168.1.20/24 dev eth0
sudo ip link set eth0 up
sudo ip route add 10.0.0.0/8 via 192.168.1.254
```

**注意**：最後三行的 `ip addr add` / `ip route add` 是直接改核心狀態，重開機或後端重新套用（`netplan apply`、`nmcli con up`）就會消失，只適合救急或先驗證想法。

---

## 3. 🟠 Ubuntu：Netplan

**名詞與關係**：Netplan 的三個子命令 `generate`／`try`／`apply` 對應設定流動的三個階段，看懂這條路徑就知道為什麼「改 `/run` 底下的檔案沒用」：

```
/etc/netplan/*.yaml（唯一該編輯的地方；依檔名字母序合併，後蓋前）
        │  netplan generate：只轉譯成後端設定檔，不通知 daemon（拿來驗證語法）
        ▼
renderer: networkd ───────▶ /run/systemd/network/10-netplan-*.network ──▶ systemd-networkd ─┐
renderer: NetworkManager ─▶ /run/NetworkManager/system-connections/netplan-* ──▶ NM ─────────┤
        ▲                                                                                     ▼
        │  netplan apply = generate + 通知 daemon 重讀                                    netlink → 核心
        │  netplan try   = apply + 倒數 120 秒，沒按 Enter 就自動還原
        └─ /run 是 tmpfs，每次 apply 都重新產生，所以手改產物必定在下次 apply 或重開機時丟失
```

**為什麼**：Canonical 設計 Netplan 的動機，是讓雲端映像、Server 與 Desktop 共用一份 YAML，由 Netplan 依 `renderer` 轉譯給 systemd-networkd 或 NetworkManager；cloud-init 也是透過這層把雲平台配發的網路設定寫進來。代價是多了一層：直接改 `/run/systemd/network/` 底下的產物，下次 `netplan apply` 就會被覆蓋，一切修改都要回到 `/etc/netplan/`。

### 3.1 檔案與流程

**為什麼**：遠端改網路最怕「apply 下去就斷線」。Netplan 提供 `generate`（只檢查語法、不套用）與 `try`（套用後倒數，沒確認就自動還原）兩道保險；養成「generate → try → apply」的順序，可以避免絕大多數要跑機房接螢幕的狀況。

**怎麼做**：

```bash
ls /etc/netplan/                        # 安裝時產生 50-cloud-init.yaml 或 00-installer-config.yaml
sudo netplan get                        # 顯示合併後的設定
sudo netplan generate                   # 只產生後端設定檔（可用來驗證語法）
sudo netplan try                        # 套用並倒數 120 秒，沒按 Enter 就自動還原（遠端操作救命用）
sudo netplan apply                      # 套用
sudo netplan status                     # 24.04 新增：整合顯示狀態
sudo netplan --debug apply              # 除錯
```

**注意**：
- 檔名按字母序合併，後面的覆蓋前面的；自訂檔建議 `01-xxx.yaml`～`99-xxx.yaml`。
- 檔案權限必須 `600`（否則 warning：`Permissions for /etc/netplan/x.yaml are too open`）。
- 雲端主機的 `50-cloud-init.yaml` 會被 cloud-init 重寫；要自訂請建 `/etc/cloud/cloud.cfg.d/99-disable-network-config.cfg` 內容 `network: {config: disabled}`。

### 3.2 靜態 IP / DHCP

**名詞與關係**：這一節的 YAML 同時處理「位址從哪來」與「兩張卡誰負責出去」兩件事，牽涉的名詞如下：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **DHCP** | 網卡開機時向網段上的 DHCP 伺服器租用 IP、閘道、DNS 的協定（`dhcp4: true`） | 租約內容會覆蓋靜態設定，所以要用 `dhcp4-overrides` 明說「租約裡的 DNS／路由我不要」 |
| **靜態 IP** | 手動寫死的位址（`dhcp4: false` + `addresses:`），重開機不變 | DNS 紀錄、防火牆規則、監控都指著它，伺服器主網卡幾乎一定用靜態 |
| **預設路由（default route）** | 目的地不在任何已知網段時走的出口（`to: default, via: 閘道`） | 兩張卡都有預設路由時，核心只會挑一條，靠 metric 決定 |
| **metric** | 路由的優先權數字，越小越優先 | `route-metric: 200` 讓 DHCP 那張卡的預設路由退居備援，出去的流量才會走主網卡 |
| **`optional: true` ／ systemd-networkd-wait-online** | wait-online 是開機時等待「所有受管網卡就緒」的 systemd 服務；`optional` 把某張卡排除在等待名單外 | 沒接線的卡若不標 `optional`，wait-online 會等到 2 分鐘逾時，開機期間所有需要網路的服務跟著卡住 |

**為什麼**：伺服器通常需要固定 IP（DNS、防火牆規則、監控都指著它），但同一台機器的第二張卡（儲存網、管理網）往往仍用 DHCP。兩張卡都拿到預設路由時，出去的流量會走錯卡；沒接線的網卡沒標 `optional` 則會讓開機卡在 wait-online 兩分鐘。下面的範例把這幾個常見狀況一次寫齊。

**怎麼做**：新增一個獨立檔案，不要動安裝程式產生的檔：

```yaml
# /etc/netplan/01-static.yaml
network:
  version: 2
  renderer: networkd            # Server 預設；Desktop 改 NetworkManager
  ethernets:
    ens18:
      dhcp4: false
      dhcp6: false
      addresses:
        - 192.168.1.10/24
        - "2001:db8::10/64"
      routes:
        - to: default
          via: 192.168.1.1
        - to: 10.0.0.0/8          # 額外靜態路由
          via: 192.168.1.254
          metric: 100
      nameservers:
        addresses: [1.1.1.1, 8.8.8.8]
        search: [example.com]
    ens19:
      dhcp4: true
      dhcp4-overrides:
        use-dns: false            # 這張卡的 DHCP DNS 不要用
        route-metric: 200         # 兩張卡都有預設路由時的優先權
      optional: true              # 開機不等這張卡（避免 systemd-networkd-wait-online 卡 2 分鐘）
```

**注意**：舊寫法 `gateway4:` 已棄用，請用 `routes: [{to: default, via: ...}]`；Desktop 上 `renderer` 要改成 `NetworkManager`，否則 GUI 看不到也管不到這張卡。

### 3.3 Bonding、VLAN、Bridge（已用 `netplan generate` 驗證）

**名詞與關係**：三種虛擬介面各解決一個問題，而且可以疊起來——下面的範例就是「實體卡 → bond → vlan／bridge → 虛擬機」四層：

```
實體卡 eth1 ─┐
             ├─ bond0（Bonding：多條實體線合成一條邏輯鏈路，斷一條不斷網）
實體卡 eth2 ─┘     │
                   ├─ bond0.100（VLAN：在 bond0 上打 802.1Q 標籤 100，成為另一個網段的邏輯介面）
                   │
                   └─ br0（Bridge：軟體交換器，把 bond0 與 VM 的虛擬網卡接進同一個 L2 網段）
                         └─ vnet0…（KVM / LXD 虛擬機的網卡，由虛擬化平台自動掛進 br0）
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Bonding** | 核心 bonding 模組：把多張網卡合成一個 `bondX` 介面，提供備援或頻寬疊加 | 上層的 VLAN、Bridge、IP 都設在 bond 上，不再直接碰實體卡（實體卡在 `ethernets:` 留空 `{}`） |
| **802.3ad ／ LACP** | IEEE 標準的鏈路聚合協定：兩端協商後同時使用所有成員線，頻寬相加 | 是 bonding 的一種 `mode`；交換器那端必須設好 LACP 群組，否則所有成員線都不通 |
| **active-backup** | bonding 的另一種 `mode`：同一時間只用一條線，斷線才切換 | 不需要交換器配合，是 LACP 談不攏時的保底選擇，但頻寬不疊加 |
| **mii-monitor-interval** | bonding 每隔多少毫秒檢查一次成員線的 link 狀態（核心參數名 `miimon`） | 決定斷線後多快切換；nmcli 那邊要用核心參數名寫（§4.3） |
| **transmit-hash-policy** | LACP 模式下決定「某條連線固定走哪條成員線」的雜湊依據（`layer3+4` = 看 IP + 埠） | 只對 802.3ad／balance-xor 有意義；同一條 TCP 連線永遠走同一條線，避免亂序 |
| **VLAN** | IEEE 802.1Q：在乙太網框加標籤，讓一條實體線承載多個互相隔離的網段 | `link:` 指向承載它的介面（這裡是 bond0）；交換器對應埠要設成 trunk 並允許該 VLAN id |
| **Bridge** | 核心的軟體交換器（L2 轉發），把多個介面接成同一個廣播網域 | KVM／LXD 要讓 VM 拿到與宿主同網段的 IP，就必須把實體（或 bond）介面掛進 bridge，IP 也改設在 `br0` 上 |
| **STP** | Spanning Tree Protocol：交換器之間偵測並阻斷迴圈的協定 | 單一宿主的 bridge 不會形成迴圈，關掉（`stp: false`）可省下 30 秒的 forward-delay 等待 |

**為什麼**：Bonding 讓一條線斷了不斷網、LACP 模式下還能疊加頻寬；VLAN 讓一張實體卡承載多個網段；Bridge 則是 KVM／LXD 讓虛擬機直接掛在實體網路上的必要條件。三者在機房常疊在一起用（bond → vlan → bridge），範例照這個層次寫。

**怎麼做**：

```yaml
network:
  version: 2
  renderer: networkd
  ethernets:
    eth1: {}
    eth2: {}
  bonds:
    bond0:
      interfaces: [eth1, eth2]
      parameters:
        mode: 802.3ad             # LACP；交換器不支援時用 active-backup
        lacp-rate: fast
        mii-monitor-interval: 100
        transmit-hash-policy: layer3+4
  vlans:
    bond0.100:
      id: 100
      link: bond0
      addresses: [10.100.0.10/24]
  bridges:
    br0:                          # 給 KVM / LXD 用的橋接
      interfaces: [bond0]
      addresses: [10.0.0.10/24]
      routes:
        - to: default
          via: 10.0.0.1
      nameservers:
        addresses: [10.0.0.1]
      parameters:
        stp: false
        forward-delay: 0
```

套用後確認 bond 真的組起來、成員都在線：

```bash
# 驗證 bond 狀態
cat /proc/net/bonding/bond0
networkctl status bond0
```

**注意**：`802.3ad` 需要交換器那端也設好 LACP，否則兩條線都不通；不確定交換器設定時先用 `active-backup`，它不需要交換器配合。

### 3.4 Wi-Fi（Desktop 通常用 NM GUI；Server 也可）

**名詞與關係**：無線連線多了一層「認證」，所以 Netplan 之下還要一個專門的 daemon：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **wpa_supplicant** | 負責與無線基地台完成 WPA／WPA2／WPA3 認證與加密握手的 daemon | networkd 自己不會講 Wi-Fi，Netplan 把 `wifis:` 轉成 wpa_supplicant 的設定並拉起它；沒裝 `wpasupplicant` 套件 `netplan apply` 會失敗 |
| **SSID** | 無線網路的名稱（`access-points:` 底下的鍵） | 一個 `wifis:` 介面可列多個 SSID，wpa_supplicant 依訊號選一個連 |
| **PSK** | Pre-Shared Key：家用／小型網路常見的「一組密碼大家共用」認證方式（`key-management: psk`） | `password:` 就是 PSK；企業網路的 802.1X／EAP 要改用 `auth:` 區塊指定帳號憑證 |

**為什麼**：Server 版沒有 GUI，要在筆電或小型設備上跑 Ubuntu Server 接 Wi-Fi 時，只能靠 Netplan 把 SSID 與密碼交給 wpa_supplicant。

**怎麼做**：

```yaml
network:
  version: 2
  renderer: networkd
  wifis:
    wlp3s0:
      dhcp4: true
      access-points:
        "MySSID":
          password: "supersecret"      # 或 auth: {key-management: psk, password: ...}
```
（需要 `wpasupplicant` 套件。）

**注意**：密碼是明文存在 YAML 裡，這也是檔案權限必須 `600` 的原因之一。

---

## 4. 🔵 Fedora：NetworkManager（nmcli）

**名詞與關係**：NM 的所有介面（CLI、TUI、GUI、Web、自動化工具）都只是同一個 daemon 的不同客戶端，改的是同一批檔案，這是它與「Netplan 多一層轉譯」最大的不同：

```
nmcli / nmtui / GNOME 設定 / Cockpit（Web 管理介面）/ Ansible 的 nmcli 模組
        │ 全部經 D-Bus（系統匯流排）向同一個 daemon 下指令
        ▼
NetworkManager daemon ──寫入──▶ /etc/NetworkManager/system-connections/*.nmconnection（keyfile）
        │                        每個檔案 = 一個 connection profile；`con mod` 改的是這裡（只改檔、不動網卡）
        │ `con up <profile>`：把該 profile 套到它綁定的裝置（device）上
        ▼
netlink → 核心（網卡拿到位址、路由、DNS 交給 systemd-resolved）
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **connection profile** | 一組完整的連線設定（IP、DNS、路由、Wi-Fi 密碼…），存成一個 keyfile，有自己的名字與 UUID | NM 的操作單位；一張網卡可綁多個 profile，同時只啟用一個（§4.1） |
| **device** | NM 眼中的網卡或虛擬介面（`nmcli device`） | profile 用 `ifname` 綁到某個 device；device 沒有 profile 或標 unmanaged 時 NM 不會動它 |
| **nmcli ／ nmtui** | NM 的命令列工具／文字選單工具 | 兩者功能等價，nmtui 適合互動、nmcli 適合腳本；本章以 nmcli 為主 |
| **Cockpit** | Red Hat 的瀏覽器管理介面（9090 埠），網路頁面就是 NM 的前端 | 與 nmcli 改同一份 keyfile，兩邊操作不會互相覆蓋 |
| **`con mod` ／ `con up`** | 修改 profile 內容／把 profile 套用到裝置 | `con mod` 只寫 keyfile，不 `con up` 網卡不會變；這是 §4.1 注意事項的根源 |

**為什麼**：Red Hat 從 RHEL 8 起淘汰 `network-scripts`，讓 NetworkManager 成為 Server、Workstation、容器主機唯一的網路入口；不管用 nmcli、nmtui、GUI、Cockpit 還是 Ansible，改的都是同一份 keyfile，不會有「兩套設定互相覆蓋」的問題。代價是設定不是一份宣告式檔案，而是一連串 `con mod` 指令，且要自己記得 `con up` 才生效。

### 4.1 觀察

**為什麼**：NM 的操作單位是 **connection profile** 而不是網卡：一張網卡可有多個 profile、同時只啟用一個。很多「改了沒效」的案例，其實是改到沒啟用的那個 profile（例如安裝時留下的 `Wired connection 1`），所以動手前先看清楚 profile 與裝置的對應。

**怎麼做**：

```bash
nmcli                                   # 總覽
nmcli device status                     # 裝置
nmcli connection show                   # 連線設定檔（profile）
nmcli connection show "ens18"           # 某 profile 全部屬性
nmcli device show ens18                 # 裝置目前狀態
nmcli general hostname
nmtui                                   # 文字 UI（新手友善）
ls /etc/NetworkManager/system-connections/     # keyfile 存放處（權限 600）
```

**注意**：修改 profile（`con mod`）只會寫入 keyfile，要 `con up` 重新啟用才會套到網卡上。

### 4.2 靜態 IP / DHCP

**名詞與關係**：nmcli 用「屬性 = 值」表達 §3.2 那份 YAML 裡的每個概念，對照如下（DHCP、預設路由、metric 的定義見 §3.2）：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **`ipv4.method`** | 位址從哪來：`manual`（靜態）、`auto`（DHCP）、`disabled`（不設 IPv4） | 等於 Netplan 的 `dhcp4: false/true`；設 `manual` 時必須同時給 `ipv4.addresses` |
| **`ignore-auto-dns` ／ `never-default` ／ `route-metric`** | 分別是「不採用 DHCP 給的 DNS」、「這張卡不要預設路由」、「預設路由的 metric」 | 對應 Netplan 的 `use-dns: false`、（沒有 default route）、`route-metric`；用來讓第二張卡不搶主網卡的出口 |
| **kickstart** | Red Hat 系的無人值守安裝腳本格式（`ks.cfg`），可在安裝時寫入網路設定 | 與 Ansible 一樣屬於批次佈署工具；批次時直接放 keyfile 比串 nmcli 指令好維護 |
| **`nmcli --offline`** | 不連 daemon、只把指令轉成 keyfile 印到標準輸出 | 拿來預覽或驗證語法，等於 Netplan 的 `generate`；本節的 keyfile 範例就是這樣產生的 |
| **`con reload`** | 讓 NM 重新掃描 `system-connections/` 目錄，認出手動放進去的 keyfile | 手放檔案後不 reload NM 不知道有它；權限不是 600 則直接被忽略且不報錯 |

**為什麼**：需求同 §3.2（伺服器固定 IP、第二張卡 DHCP 但不要它的 DNS 與預設路由），只是改用 nmcli 表達。`con mod` 可以把所有參數一次下完再 `con up`，中途不會斷線；但 NM 沒有 `netplan try` 這種自動還原，遠端操作前務必再看一次 IP 與閘道。

**怎麼做**：

```bash
# 修改既有 profile（安裝時通常已有一個與網卡同名或叫 "Wired connection 1"）
sudo nmcli con mod "ens18" \
  ipv4.method manual \
  ipv4.addresses "192.168.1.10/24" \
  ipv4.gateway "192.168.1.1" \
  ipv4.dns "1.1.1.1 8.8.8.8" \
  ipv4.dns-search "example.com" \
  ipv6.method disabled
sudo nmcli con up "ens18"

# 改回 DHCP
sudo nmcli con mod "ens18" ipv4.method auto ipv4.addresses "" ipv4.gateway "" ipv4.dns ""
sudo nmcli con up "ens18"

# 新建 profile
sudo nmcli con add type ethernet con-name static-ens19 ifname ens19 \
  ipv4.method manual ipv4.addresses 10.0.0.10/24 ipv4.gateway 10.0.0.1 ipv4.dns 10.0.0.1
sudo nmcli con add type ethernet con-name dhcp-ens19 ifname ens19 ipv4.method auto \
  ipv4.ignore-auto-dns yes ipv4.route-metric 200 connection.autoconnect yes

# 額外靜態路由
sudo nmcli con mod "ens18" +ipv4.routes "10.0.0.0/8 192.168.1.254 100"
sudo nmcli con mod "ens18" ipv4.never-default yes     # 這張卡不要預設路由

# 刪除
sudo nmcli con delete static-ens19
```

nmcli 產生的 keyfile（實測 `nmcli --offline` 輸出）：

```ini
[connection]
id=eth0
uuid=6e6c1f71-0d38-495d-846c-ec070e03564d
type=ethernet
interface-name=eth0

[ipv4]
address1=192.168.1.10/24
dns=1.1.1.1;8.8.8.8;
dns-search=example.com;
gateway=192.168.1.1
method=manual

[ipv6]
addr-gen-mode=default
method=disabled
```

**注意**：要用 Ansible 或 kickstart 批次佈署時，直接寫 keyfile 比串 nmcli 指令好維護：放到 `/etc/NetworkManager/system-connections/eth0.nmconnection`（`chmod 600`）再 `sudo nmcli con reload && sudo nmcli con up eth0`。權限不是 600 時 NM 會直接忽略該檔，且不會報錯。

### 4.3 Bonding、VLAN、Bridge（已用 `nmcli --offline` 驗證）

**為什麼**：需求與 §3.3 相同；差別在 NM 把每個成員網卡也當成一個 profile（`master bond0`；NM 用語裡 master／controller 指 bond 或 bridge 本體，slave／port 指被納入的成員網卡），所以一個 bond 至少三個 profile，且要先建 bond 再建成員，順序反了成員會找不到 controller。

**怎麼做**：

```bash
# Bond
sudo nmcli con add type bond con-name bond0 ifname bond0 \
  bond.options "mode=802.3ad,miimon=100,lacp_rate=fast,xmit_hash_policy=layer3+4" \
  ipv4.method disabled ipv6.method disabled
sudo nmcli con add type ethernet con-name bond0-eth1 ifname eth1 master bond0 slave-type bond
sudo nmcli con add type ethernet con-name bond0-eth2 ifname eth2 master bond0 slave-type bond
# （新版 NM 用語：controller / port-type，舊的 master / slave-type 仍相容）

# VLAN 100 在 bond0 上
sudo nmcli con add type vlan con-name bond0.100 ifname bond0.100 dev bond0 id 100 \
  ipv4.method manual ipv4.addresses 10.100.0.10/24

# Bridge（KVM 用）
sudo nmcli con add type bridge con-name br0 ifname br0 bridge.stp no \
  ipv4.method manual ipv4.addresses 10.0.0.10/24 ipv4.gateway 10.0.0.1 ipv4.dns 10.0.0.1
sudo nmcli con add type ethernet con-name br0-bond0 ifname bond0 master br0 slave-type bridge   # 把 bond0 掛進橋

sudo nmcli con up bond0; sudo nmcli con up br0
cat /proc/net/bonding/bond0
```

**注意**：`bond.options` 的鍵名是核心 bonding 模組的參數名（`miimon`、`xmit_hash_policy`），與 Netplan 的 `mii-monitor-interval`、`transmit-hash-policy` 拼法不同，跨發行版照抄會被拒。

### 4.4 Wi-Fi

**為什麼**：NM 直接與 wpa_supplicant 溝通，連 Wi-Fi 一行指令即可、不用寫檔；筆電臨時當 AP（Access Point，無線基地台；nmcli 稱為 hotspot）分享網路給隔壁的設備，也是同一套工具。

**怎麼做**：

```bash
nmcli device wifi list
sudo nmcli device wifi connect "MySSID" password "supersecret"
sudo nmcli con mod "MySSID" connection.autoconnect yes
nmcli device wifi hotspot ssid MyAP password 12345678     # 開熱點
```

**注意**：`device wifi connect` 會自動建立一個與 SSID 同名的 profile，密碼存在 keyfile（600 權限），之後要改設定用 `con mod "MySSID"`。

### 4.5 NetworkManager 全域設定

**名詞與關係**：全域設定是「NM daemon 自己的行為」而不是「某條連線的參數」，所以有另一套檔案與名詞：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **drop-in（`conf.d/*.conf`）** | 放在 `/etc/NetworkManager/conf.d/` 的片段設定檔，與套件自帶的 `NetworkManager.conf` 合併 | 套件升級只會覆蓋主檔，不會動 drop-in；與 sshd、systemd 的 `.d` 目錄是同一種慣例 |
| **veth** | virtual ethernet：成對出現的虛擬網卡，一端在容器內、一端在宿主，Docker／Podman／LXD 用它把容器接到網路 | 宿主端的 veth 會被 NM 看到；不列入 unmanaged NM 就會去嘗試 DHCP |
| **unmanaged-devices** | 告訴 NM「這些介面不歸你管」的清單，可用介面名樣式或 MAC | 標了之後 `nmcli device status` 顯示 unmanaged，NM 不再對它做任何設定；§1 提到的 Ubuntu Server 網卡 unmanaged 就是同一機制 |
| **`rc-manager`** | NM 產生 `/etc/resolv.conf` 的方式（`symlink`、`file`、`resolvconf`…） | 搭配 `dns=systemd-resolved` 時應為 `symlink`，讓 resolv.conf 指向 resolved（§5）；改成自管 DNS 時要一起調整 |
| **IPv6 隱私位址（`ip6-privacy`）** | RFC 4941 的暫時位址：不用 MAC 推導的固定尾碼，而是定期換一組亂數尾碼對外連線 | 對桌機／筆電有意義（避免被追蹤）；伺服器通常要固定位址，不要開 |

**為什麼**：有些行為不屬於任何一個 profile：Docker／libvirt 建的 veth 不希望 NM 去碰（否則它會嘗試 DHCP，拖慢容器啟動、也讓 `nmcli` 輸出一堆雜訊）、DNS 要不要交給 resolved、IPv6 是否用隱私位址。這些放在 conf.d 的 drop-in，套件升級時不會被覆蓋。

**怎麼做**：`/etc/NetworkManager/conf.d/*.conf`：

```ini
[main]
dns=systemd-resolved            # 預設；可改 none 自己管 resolv.conf
rc-manager=symlink

[keyfile]
unmanaged-devices=interface-name:veth*;interface-name:docker0   # 不要管容器網卡

[connection]
ipv6.ip6-privacy=2              # IPv6 隱私位址
```

**注意**：🟠 Ubuntu Desktop 也用 NM，上述 conf.d 設定同樣適用；但 IP 設定請仍透過 Netplan（`renderer: NetworkManager`），否則 `netplan apply` 會覆蓋。

---

## 5. DNS：systemd-resolved（兩者共通）

**名詞與關係**：一個網域名稱從應用程式到真正的 DNS 伺服器要經過三站，resolved 站在中間，所以「DNS 設定」其實分散在好幾個地方：

```
應用程式（curl、ssh、瀏覽器…）
   │ glibc 讀 /etc/resolv.conf → 只有一行 nameserver 127.0.0.53
   ▼
systemd-resolved（stub resolver：本機 DNS 代理，含快取、負責 DoT / DNSSEC）
   │ 依「查詢的網域」對照每張網卡的搜尋／路由網域，決定送給哪一組上游
   ├─ ens18 的上游 DNS：1.1.1.1、8.8.8.8   ◀── 由 networkd / NM 從 DHCP 租約或 Netplan / nmcli 設定交來
   └─ tun0  的上游 DNS：10.8.0.1，Domains=~corp.example.com   ◀── split DNS：只有這個網域走 VPN
（resolved.conf.d 的 [Resolve] 是全域層：所有網卡都沒設定時的預設值，以及 DoT / DNSSEC 開關）
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **stub resolver** | resolved 在 `127.0.0.53:53` 開的極簡 DNS 服務：只接本機查詢、轉給上游、快取結果 | `/etc/resolv.conf` → `stub-resolv.conf` 指向它；應用程式以為在問 DNS 伺服器，其實在問 resolved |
| **上游 DNS（per-link DNS）** | resolved 真正去問的外部 DNS 伺服器，逐張網卡記錄 | 來源是 DHCP、Netplan `nameservers:`、nmcli `ipv4.dns` 或 `resolvectl dns`；`resolvectl status` 逐卡列出 |
| **搜尋網域（search domain）** | 查短主機名時自動補上的尾碼（`search: [example.com]` → `db` 變 `db.example.com`） | 同一個欄位加 `~` 就變成路由網域，語意不同 |
| **路由網域（`~domain`）** | 告訴 resolved「這個網域的查詢只送到這張網卡的 DNS」，不補尾碼 | 是 split DNS 的實作方式；`~.` 表示「所有網域」，等於把該組 DNS 設為全域優先 |
| **split DNS** | 不同網域走不同 DNS 伺服器（公司網域走 VPN 內部 DNS，其他走公網） | 靠路由網域 + 每張網卡各自的上游 DNS 組成；以前要靠 dnsmasq，現在 resolved 內建 |
| **DoT（DNSOverTLS）** | 與上游 DNS 改走 853 埠的 TLS 加密通道；`opportunistic` = 上游支援才用、不支援就退回明文 | 由 resolved 對上游執行，應用程式不用改；`strict` 才有防竄改效果 |
| **DNSSEC** | 驗證 DNS 回應簽章、防止被偽造的機制；`allow-downgrade` = 上游不支援就略過 | 與 DoT 互補：DoT 保護傳輸、DNSSEC 保護資料真偽 |
| **FallbackDNS** | 所有網卡與全域 `DNS=` 都沒設定時才用的最後手段 | 平常不會被用到，設它是防「DHCP 沒給 DNS」時整台機器不能解析 |
| **dnsmasq ／ unbound** | 本機自架的快取或遞迴 DNS 伺服器 | 與 resolved 都想佔 53 埠、都想管 resolv.conf，所以要用其中一個就得停掉另一個（見本節注意） |

**為什麼**：兩個發行版的 `/etc/resolv.conf` 都只是指向 `127.0.0.53` 的符號連結，真正的上游 DNS 由 resolved 依網卡管理。直接編輯 resolv.conf，下次網路事件（DHCP 續約、VPN 上線）就會被改回去——這是 §10 常見問題的常客。好處是 split DNS（VPN 只解析公司網域）與 DNS over TLS 都能在這一層完成，不必再裝 dnsmasq。

**怎麼做**：

```bash
resolvectl status                       # 每張網卡的 DNS 與搜尋網域
resolvectl query www.example.com
resolvectl flush-caches
resolvectl statistics
ls -l /etc/resolv.conf                  # → ../run/systemd/resolve/stub-resolv.conf（127.0.0.53）
```

全域覆寫 `/etc/systemd/resolved.conf.d/dns.conf`：

```ini
[Resolve]
DNS=1.1.1.1 9.9.9.9
FallbackDNS=8.8.8.8
Domains=~.                              # ~. 表示所有網域都優先用這裡的 DNS
DNSOverTLS=opportunistic
DNSSEC=allow-downgrade
Cache=yes
```
```bash
sudo systemctl restart systemd-resolved
```

特定網域走特定 DNS（split DNS，VPN 常用）：
- 🟠 Netplan：`nameservers: {addresses: [10.8.0.1], search: [corp.example.com]}` 加上 `routes` 只走該網卡。
- 🔵 nmcli：`nmcli con mod vpn0 ipv4.dns 10.8.0.1 ipv4.dns-search "~corp.example.com"`。
- 臨時：`resolvectl dns tun0 10.8.0.1; resolvectl domain tun0 ~corp.example.com`。

**注意**：不想用 resolved（例如自己跑 dnsmasq / unbound）時，要整個停掉並換掉符號連結，只改 resolv.conf 內容沒有用：
```bash
sudo systemctl disable --now systemd-resolved
sudo rm /etc/resolv.conf && echo "nameserver 1.1.1.1" | sudo tee /etc/resolv.conf
# 🔵 另需 /etc/NetworkManager/conf.d/dns.conf 設 [main] dns=none，🟠 networkd 不會動 resolv.conf
```

---

## 6. 🟠 Ubuntu：ufw

**名詞與關係**：ufw 是四層架構最上面的一層（見 §1），它的規則要經過 iptables 語法才落到 nftables；理解這條路徑就知道為什麼 NAT 要「退回 iptables 語法」、Docker 又為什麼能繞過它：

```
ufw allow 80/tcp（人話規則；CLI 只產生 filter 表的規則）
   │ 寫入 /etc/ufw/user.rules（iptables-restore 格式）
   │ before.rules / after.rules：框架規則，在 user.rules 之前 / 之後載入 → NAT 要手寫在這裡
   ▼
iptables 指令（實為 iptables-nft：舊語法、新後端）
   ▼
nftables ──▶ 核心 netfilter ┬─ filter 表：accept / deny / reject / limit（單機防火牆）
                            └─ nat 表：MASQUERADE（出去換來源 IP）、REDIRECT / DNAT（進來改目的埠或位址）
                                ▲
   Docker 直接對 iptables 加自己的 DOCKER 鏈，排在 ufw 規則之前 → 發布的埠繞過 ufw
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **application profile** | `/etc/ufw/applications.d/` 內的服務定義（名稱 → 埠與協定），如 `OpenSSH` | 套件安裝時附帶；`ufw allow OpenSSH` 比記埠號可讀，`ufw app list` 查得到 |
| **filter 表 ／ nat 表** | netfilter 裡的兩類規則表：filter 決定封包放不放行，nat 決定要不要改寫位址或埠 | ufw CLI 只管 filter；nat 規則要以 iptables 語法手寫進 `before.rules` |
| **`before.rules` ／ `user.rules` ／ `after.rules`** | ufw 的三份 iptables-restore 格式檔，依序載入 | CLI 下的規則進 `user.rules`；`before.rules` 放 NAT 與框架規則；直接編輯後要 `ufw reload` |
| **deny vs reject** | 都是擋，deny 靜默丟棄、reject 回 ICMP／TCP RST 告知對方 | 對外（WAN）用 deny 不洩漏主機存在；內網用 reject 讓應用程式立刻失敗而不是逾時 |
| **limit** | 同一來源 30 秒內超過 6 次新連線就拒絕的速率限制規則 | 針對 SSH 暴力嘗試的第一道防線，與 Fail2ban 互補（08 章） |
| **NAT ／ masquerade** | NAT 是改寫封包位址；masquerade 是「來源位址改成出口網卡目前的 IP」的動態 SNAT | 當路由器讓內網上網的必要規則；要與 `ip_forward=1`、`DEFAULT_FORWARD_POLICY=ACCEPT` 一起開，見 §9.1 |
| **port forward（REDIRECT ／ DNAT）** | 把進來的封包改送到別的埠（REDIRECT，本機）或別的主機（DNAT） | 屬於 nat 表 PREROUTING 鏈，在路由判斷前改寫，所以也要在 `before.rules` 寫 |
| **`DEFAULT_FORWARD_POLICY`** | `/etc/default/ufw` 內，決定「經過本機轉送」的封包預設放行還是丟棄 | ufw 預設 DROP；只開 masquerade 而沒改它，內網封包會在 forward 鏈被擋 |

**為什麼**：Ubuntu 預設**沒有**啟用任何防火牆，裝完機所有監聽埠都對外開放，一台忘了設定的資料庫主機就是這樣暴露在網路上的。ufw 的設計哲學是「一行人話描述一條規則」——`allow 80/tcp` 就是它的全部語法，適合單機、規則不超過幾十條的情境；它沒有 zone 概念，網卡與來源條件要寫在每條規則裡，規則一多就難維護。

**怎麼做**：

```bash
sudo ufw status verbose                 # 預設：inactive
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow OpenSSH                  # 應用程式 profile（ufw app list；/etc/ufw/applications.d/）
sudo ufw allow 22/tcp
sudo ufw allow 80,443/tcp
sudo ufw allow 5000:5010/tcp            # 範圍
sudo ufw allow from 192.168.1.0/24 to any port 22 proto tcp
sudo ufw allow in on eth1 to any port 3306   # 只允許某網卡
sudo ufw limit ssh                      # 速率限制（30 秒內 6 次連線就擋）；防暴力破解
sudo ufw deny from 10.0.0.5
sudo ufw reject out to 10.66.0.0/16     # reject 會回 ICMP，deny 靜默丟棄
sudo ufw enable                         # ⚠️ 遠端操作前先確定 SSH 規則已加
sudo ufw status numbered
sudo ufw delete 5                       # 依編號刪
sudo ufw delete allow 80/tcp            # 依規則刪
sudo ufw insert 1 deny from 1.2.3.4     # 插到最前面（順序有影響）
sudo ufw logging on|medium|off          # 日誌到 /var/log/ufw.log（實測 rsyslog 有 20-ufw.conf）
sudo ufw reset                          # 全清
sudo ufw show raw / added / listening
```

ufw 的 CLI 只涵蓋 filter 規則；NAT 或 port forward 這類 `*nat` 表的需求，要退回 iptables 語法寫進 `/etc/ufw/before.rules`（iptables-restore 格式）：

```bash
# NAT / masquerade（當路由器）：在 /etc/ufw/before.rules 最上方 *filter 之前加
# *nat
# :POSTROUTING ACCEPT [0:0]
# -A POSTROUTING -s 10.0.0.0/24 -o eth0 -j MASQUERADE
# COMMIT
# 並把 /etc/default/ufw 的 DEFAULT_FORWARD_POLICY 改成 "ACCEPT"，/etc/ufw/sysctl.conf 開 net/ipv4/ip_forward=1
# Port forward：在 *nat 加 -A PREROUTING -p tcp --dport 8080 -j REDIRECT --to-port 80
sudo ufw reload
```

**注意**：
- 順序永遠是「先 `allow OpenSSH`（或 `limit ssh`），再 `enable`」；反過來做，遠端 session 會在 enable 的瞬間斷掉。
- ⚠️ **Docker 與 ufw**：Docker 直接寫 iptables，`-p 8080:80` 發布的埠**會繞過 ufw**。解法：Docker daemon 設 `{"iptables": false}`（自己管 NAT）、或用 `127.0.0.1:8080:80` 只綁 localhost、或用 ufw-docker 專案。Fedora 的 firewalld 也有類似問題（Docker 會加 `DOCKER` zone / policy），Podman 走 netavark 較無此問題。

---

## 7. 🔵 Fedora：firewalld

**名詞與關係**：firewalld 的規則不是一條條平鋪，而是「先把封包歸到 zone，再看 zone 允許什麼」，另外每條規則都有 runtime 與 permanent 兩個版本；把這個模型看懂，`firewall-cmd` 的幾十個選項就只是在填模型裡的格子：

```
封包從 ens18 進來 ──▶ 來源 IP 有沒有被 --add-source 歸到某個 zone？（來源優先）
                         └─ 沒有 ──▶ ens18 被 --change-interface 綁到哪個 zone？（網卡其次）
                                        └─ 都沒有 ──▶ default zone（public）
        ▼
zone（public / internal / trusted / external…）＝一個信任等級：
   允許的 service（http、ssh…）+ port（8080/tcp）+ rich rule（細條件）+ masquerade / forward-port
   其餘封包依 zone 的 target 處理（public 預設 reject；trusted 全放；drop 全丟）
        │ zone 之間的轉送（LAN → WAN）由 policy 定義
        ▼
firewalld daemon ──▶ nftables（FirewallBackend=nftables）──▶ 核心 netfilter
   ├─ runtime：目前生效中的規則；--reload 或重開機即消失
   └─ permanent：/etc/firewalld/zones/*.xml；要 --reload 才變成 runtime
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **zone** | 一組信任等級與允許清單；每張網卡或來源網段恰好屬於一個 zone | 所有 `--add-*` 沒指定 `--zone` 時都作用在 default zone；多網卡主機每張卡各歸一個 zone |
| **service** | 預先定義的「埠 + 協定（+ 模組）」組合，以 XML 描述（`/usr/lib/firewalld/services/`） | `--add-service=http` 等於 `--add-port=80/tcp` 但可讀；自訂放 `/etc/firewalld/services/` |
| **port** | 直接以埠號／協定放行 | 沒有對應 service 時的直接寫法；🔵 服務綁非標準埠還要 SELinux `semanage port`（§10） |
| **rich rule** | 一句話語法的細緻規則：可組合來源、service、動作、log、limit | 當「對某來源開某服務」或速率限制這種 zone 粒度做不到的需求；等於 ufw 的 `from … to any port …` |
| **runtime ／ permanent** | runtime 是核心裡現在生效的規則，permanent 是存在 XML 裡的規則 | 不加 `--permanent` 只改 runtime（試驗用）；加了只改 XML，要 `--reload`；`--runtime-to-permanent` 把試好的存下來 |
| **`--reload`** | 丟棄 runtime、從 permanent 重新載入 | 因此未存的 runtime 規則會在此刻消失，「加了埠卻不通」與「試的規則突然不見」都源於此 |
| **target** | zone 對「沒有任何規則符合」的封包採取的預設動作（ACCEPT／REJECT／DROP／default） | 決定 zone 的基本性格：trusted 是 ACCEPT、drop 是 DROP、public 是 default（reject 進、放行出） |
| **masquerade ／ forward-port** | zone 層級的 NAT 開關／port forward 規則（定義見 §6） | external zone 預設已開 masquerade；與 §9.1 的 `ip_forward` 一起才能當路由器 |
| **policy** | firewalld 1.0 新增：定義「從 zone A 到 zone B」轉送流量的規則 | zone 只管進出本機的流量，跨 zone 轉送要靠 policy；取代舊的 direct rules |
| **panic mode** | 一鍵丟棄所有進出封包 | 遭入侵時斷網用；⚠️ 會連 SSH 一起斷 |

**為什麼**：Fedora 預設就啟用 firewalld，只放行 SSH 等少數服務，新裝的服務不開埠就對外不通——這是 Fedora 上「服務起來了但連不到」的頭號嫌疑。firewalld 的設計哲學與 ufw 相反：先把網卡或來源網段歸到 **zone**，再對 zone 決定放行哪些 service，適合多網卡、多信任等級（WAN／LAN／VPN）的主機；另外它區分 runtime 與 permanent 兩層，可以「先試、確定沒把自己鎖在外面再存檔」。

**怎麼做**：每張網卡／來源網段屬於一個 zone，zone 定義允許的 service / port。預設 zone 為 `public`（實測預設放行 `dhcpv6-client mdns ssh`；Fedora Server 另有 `FedoraServer` zone 加 cockpit）：

```bash
sudo firewall-cmd --state
sudo firewall-cmd --get-default-zone
sudo firewall-cmd --get-active-zones
sudo firewall-cmd --get-zones           # 實測：FedoraServer FedoraWorkstation block dmz drop external home internal nm-shared public trusted work
sudo firewall-cmd --list-all            # 預設 zone 的全部規則
sudo firewall-cmd --list-all-zones
sudo firewall-cmd --get-services        # 263 個預先定義的 service
sudo firewall-cmd --info-service=cockpit

# 規則：不加 --permanent 只有 runtime（重載即失效）；加了要 --reload 才生效
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service={http,https}
sudo firewall-cmd --permanent --add-port=8080/tcp
sudo firewall-cmd --permanent --add-port=5000-5010/tcp
sudo firewall-cmd --permanent --remove-service=mdns
sudo firewall-cmd --reload
# 先用 runtime 試，確定沒問題再存：
sudo firewall-cmd --add-port=9999/tcp && sudo firewall-cmd --runtime-to-permanent

# Zone 操作
sudo firewall-cmd --set-default-zone=public
sudo firewall-cmd --permanent --zone=internal --add-source=192.168.1.0/24    # 來源網段歸 internal
sudo firewall-cmd --permanent --zone=internal --add-service=ssh
sudo firewall-cmd --permanent --zone=public --change-interface=ens18         # 網卡換 zone
sudo firewall-cmd --permanent --zone=trusted --add-interface=virbr0
sudo firewall-cmd --permanent --new-zone=dmz2

# Rich rules（細緻條件）
sudo firewall-cmd --permanent --add-rich-rule='rule family=ipv4 source address=10.0.0.5 reject'
sudo firewall-cmd --permanent --add-rich-rule='rule family=ipv4 source address=192.168.1.0/24 service name=mysql accept'
sudo firewall-cmd --permanent --add-rich-rule='rule service name=ssh limit value=3/m accept'   # 速率限制
sudo firewall-cmd --permanent --add-rich-rule='rule family=ipv4 source address=1.2.3.4 log prefix="BLOCK " level=info drop'
sudo firewall-cmd --list-rich-rules

# NAT / port forward
sudo firewall-cmd --permanent --add-masquerade
sudo firewall-cmd --permanent --add-forward-port=port=8080:proto=tcp:toport=80
sudo firewall-cmd --permanent --add-forward-port=port=2222:proto=tcp:toport=22:toaddr=10.0.0.5
sudo firewall-cmd --reload

# 自訂 service（實測產生 /etc/firewalld/services/myapp.xml）
sudo firewall-cmd --permanent --new-service=myapp
sudo firewall-cmd --permanent --service=myapp --add-port=8085/tcp
sudo firewall-cmd --permanent --service=myapp --set-description="My app"
sudo firewall-cmd --reload && sudo firewall-cmd --permanent --add-service=myapp && sudo firewall-cmd --reload

# 日誌與緊急
sudo firewall-cmd --set-log-denied=unicast     # 記錄被擋的封包（journalctl -k -g REJECT）
sudo firewall-cmd --panic-on                    # ⚠️ 擋掉所有流量（含 SSH）
sudo firewall-cmd --panic-off

# Policies（zone 之間的轉送規則，firewalld 1.0+）
sudo firewall-cmd --get-policies
sudo firewall-cmd --permanent --new-policy lan-to-wan
sudo firewall-cmd --permanent --policy lan-to-wan --add-ingress-zone internal --add-egress-zone external --set-target ACCEPT
```

設定檔位置：`/etc/firewalld/firewalld.conf`（`DefaultZone`、`FirewallBackend=nftables`）、`/etc/firewalld/zones/*.xml`、`/usr/lib/firewalld/services/*.xml`（預設定義，勿改；自訂放 `/etc/firewalld/services/`）。

**注意**：
- `--permanent` 與 runtime 是兩個獨立世界：加了 `--permanent` 沒 `--reload` 不會生效，沒加 `--permanent` 的規則在 `--reload` 或重開機後消失。「明明加了埠卻不通」九成是這個原因。
- 🟠 Ubuntu 上也可以用 firewalld：`sudo apt install firewalld`（需先 `ufw disable`）。

---

## 8. nftables 原生（進階、兩者共通）

**名詞與關係**：nft 規則檔的每個關鍵字都對應到核心 netfilter 的一個概念；先看封包在核心裡經過哪些 hook，再看 nftables 怎麼把規則掛上去：

```
                  ┌───────────── 核心 netfilter 的 5 個 hook ─────────────┐
封包進入網卡 ─▶ prerouting ─▶ 路由判斷 ─┬─▶ input ─▶ 本機程序 ─▶ output ─┐
               （nat：DNAT / redirect）  │   （filter：單機防火牆主戰場）   ├─▶ postrouting ─▶ 封包離開
                                         └─▶ forward（當路由器轉送時）────┘  （nat：masquerade / SNAT）

nftables 物件層次：table（family + 名稱）⊃ chain（掛在某 hook 上，帶 priority 與 policy）⊃ rule（比對條件 + 動作）
                  table 內另可放 set / map，讓一條 rule 對照上萬筆資料；ct 讓 rule 依連線狀態判斷
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **table** | 規則的最外層容器，以 family 區分處理哪種封包：`inet`（IPv4+IPv6 共用）、`ip`、`ip6`、`arp`、`bridge` | 範例用 `inet filter` 一份規則管雙協定；nat 表用 `ip` 是因為 masquerade 在 inet 家族支援較晚 |
| **chain** | table 內的規則序列；base chain 以 `type`＋`hook`＋`priority` 掛到核心，並有 `policy`（沒規則符合時的預設） | 一個 hook 可掛多條 chain，依 priority 由小到大執行——ufw／firewalld 的 chain 也掛在同樣的 hook 上，這就是並用會互相干擾的原因 |
| **hook** | 核心 netfilter 在封包路徑上提供的五個掛載點（prerouting／input／forward／output／postrouting） | `type filter hook input` 管進到本機的封包；`type nat hook postrouting` 才能做 masquerade |
| **priority** | 同一 hook 上多條 chain 的執行順序 | nat 的 prerouting 慣用 -100（在 filter 之前改目的地）、postrouting 用 100（在 filter 之後改來源） |
| **policy** | base chain 的預設動作（`accept`／`drop`） | `input policy drop` 就是白名單模式，所以一定要先放行 `lo`、established 與自己的 SSH |
| **set ／ map** | 具名集合（IP、埠、介面…）／鍵值對照表，可含區間，查詢是 O(1) | `ip saddr @blocklist drop` 一條規則對照整份封鎖名單；這是 ufw／firewalld 前端做不到的 |
| **ct（conntrack）** | 核心的連線追蹤表，記錄每條連線的狀態：`new`、`established`、`related`、`invalid` | `ct state established,related accept` 讓回應封包不用逐條放行；masquerade 也靠它記住「回來的封包要還原給誰」 |
| **counter ／ log** | 規則上的計數器／把符合的封包寫進核心日誌（`journalctl -k`） | 放在最後的 drop 之前，才知道被擋的是什麼 |
| **handle** | 每條 rule 在核心裡的流水號（`nft -a` 顯示） | 動態刪除規則要指定 handle，因為 rule 沒有名字 |
| **`flush ruleset`** | 清空核心內所有 table／chain／rule | 讓規則檔可重複載入，但也會清掉 ufw／firewalld 的規則——擇一使用的理由 |
| **iptables-translate** | 把一條 iptables 指令翻成等價 nft 語法的工具 | 遷移舊腳本用；兩發行版的 `iptables` 本身已是 iptables-nft（§1） |

**為什麼**：ufw 與 firewalld 最終都是在 nftables 上生規則；當你需要 set／map（幾萬筆封鎖名單一條規則搞定）、多 WAN 路由器、或想精確控制規則順序與計數器時，中間層反而礙事。直接寫 nft 的代價是失去前端的防呆，且 ⚠️ 與 ufw / firewalld 並用會互相干擾，擇一。

**怎麼做**：

```bash
sudo apt install -y nftables   # 🟠      sudo dnf install -y nftables   # 🔵（預設已裝）
sudo nft list ruleset           # 看目前所有規則（ufw / firewalld 產生的也看得到）
sudo nft list tables
```

規則寫在 `/etc/nftables.conf`（🟠）/ `/etc/sysconfig/nftables.conf`（🔵，會 include `/etc/nftables/*.nft`）；下面是一份可直接當起點的單機防火牆，input 預設 drop、只放行必要的服務：

```nft
#!/usr/sbin/nft -f
flush ruleset

table inet filter {
    set blocklist { type ipv4_addr; flags interval; elements = { 10.0.0.5, 192.0.2.0/24 } }

    chain input {
        type filter hook input priority 0; policy drop;
        iif lo accept
        ct state established,related accept
        ct state invalid drop
        ip saddr @blocklist drop
        ip protocol icmp accept
        ip6 nexthdr icmpv6 accept
        tcp dport 22 ct state new limit rate 6/minute accept
        tcp dport { 80, 443 } accept
        ip saddr 192.168.1.0/24 tcp dport 3306 accept
        log prefix "nft-drop: " counter drop
    }
    chain forward { type filter hook forward priority 0; policy drop; }
    chain output  { type filter hook output  priority 0; policy accept; }
}

table ip nat {
    chain prerouting  { type nat hook prerouting  priority -100; tcp dport 8080 redirect to :80 }
    chain postrouting { type nat hook postrouting priority 100;  oif "eth0" masquerade }
}
```

```bash
sudo nft -c -f /etc/nftables.conf       # -c 只檢查語法
sudo nft -f /etc/nftables.conf
sudo systemctl enable --now nftables    # 開機載入
sudo nft add rule inet filter input tcp dport 8443 accept    # 動態加
sudo nft -a list chain inet filter input                     # 顯示 handle
sudo nft delete rule inet filter input handle 12
```

**注意**：
- 檔案開頭的 `flush ruleset` 會清掉所有既有規則（包含 ufw / firewalld 的），載入前先 `nft -c` 檢查語法，並確認 input chain 有放行自己的 SSH 來源。
- iptables 語法對照：兩者仍提供 `iptables`（實為 `iptables-nft`），舊腳本可繼續用，`iptables-translate` 可轉成 nft 語法。

---

## 9. 路由、NAT 與 VPN

**為什麼**：前面幾節都是「這台機器自己」的網路；本節處理的是「幫別人轉封包」——當 VM／容器／內網的出口閘道、多條 WAN、跨站點 VPN。這三件事都牽涉核心轉送與防火牆前端的配合，只做一半是最常見的失敗原因。

### 9.1 當路由器 / NAT 閘道

**名詞與關係**：`ip_forward` 是核心的 sysctl 參數（sysctl 是執行期讀寫核心參數的介面，`/etc/sysctl.d/` 讓設定開機重套），控制「收到目的地不是自己的封包時，是丟棄還是轉送」；它與 §6／§7 定義過的 masquerade、§8 的 forward hook 是同一條封包路徑上的三道關卡，缺一不可：

```
內網主機 10.0.0.5 ──▶ eth1（LAN）──▶ ① ip_forward=1？否則核心直接丟棄
                                         ▼
                                   ② forward 鏈放行？（🟠 DEFAULT_FORWARD_POLICY / ufw route；🔵 zone 或 policy）
                                         ▼
                                   ③ postrouting masquerade：來源 10.0.0.5 改成 eth0 的公網 IP
                                         ▼
                                   eth0（WAN）──▶ Internet ──回應──▶ conntrack 還原目的地為 10.0.0.5 ──▶ eth1
```

**為什麼**：Linux 預設不轉送封包（`ip_forward=0`），要當內網的出口閘道，必須同時開轉送與 masquerade：只開轉送，內網封包出得去但回應回不來（來源是私有 IP）；只設 masquerade，封包根本不會被轉送。轉送開關是核心 sysctl，NAT 規則則要交給各自的防火牆前端。

**怎麼做**：

```bash
# 開啟轉送（兩者共通）
echo "net.ipv4.ip_forward = 1" | sudo tee /etc/sysctl.d/99-forward.conf && sudo sysctl --system
# 🟠 ufw：見 §6 的 before.rules；🔵 firewalld：--add-masquerade（external zone 預設有）
# 🔵 一行版：
sudo firewall-cmd --permanent --zone=external --change-interface=eth0     # WAN
sudo firewall-cmd --permanent --zone=internal --change-interface=eth1     # LAN
sudo firewall-cmd --permanent --zone=external --add-masquerade
sudo firewall-cmd --reload
```

### 9.2 Policy routing（多 WAN / 來源路由）

**名詞與關係**：Linux 的路由其實是「先選路由表，再在表裡查目的地」兩步，平常只用到一張表所以感覺不到第一步；policy routing 就是把第一步用起來：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **路由表（routing table）** | 一組「目的地 → 下一跳／出口網卡」的對照；核心可有多張，`main` 是預設那張 | `ip route` 不加 `table` 只看 `main`；每條 WAN 各給一張表，就能各自有不同的 default route |
| **`rt_tables`** | 把路由表的數字編號對應到可讀名稱的檔案（`100 wan2`） | 純方便閱讀，`table 100` 與 `table wan2` 等價；🔵 主檔在 `/usr/share/iproute2/`，自訂放 `rt_tables.d/` |
| **policy routing ／ `ip rule`** | 核心的 RPDB（routing policy database）：依來源位址、標記、入口網卡等條件決定「查哪張表」的規則清單 | `ip rule add from 10.2.0.0/24 table wan2` 就是「來源是這段的，去查 wan2 表」；規則依 priority 依序比對 |
| **非對稱路由** | 去程與回程走不同路徑 | 沒做 policy routing 時，從 WAN2 進來的連線會從 WAN1（main 表的 default）回去，ISP 或對方防火牆（rp_filter）就會把它丟掉 |
| **`routing-policy` ／ `ipv4.routing-rules`** | Netplan／nmcli 裡對應 `ip rule` 的永久化欄位 | 與 `routes: … table:`／`+ipv4.routes "… table=100"` 成對出現：一個放路由進表、一個決定誰查那張表 |

**為什麼**：一般路由只看目的地；兩條 WAN、或「從哪張卡進來就從哪張卡回」的需求，要依**來源**選路由表，否則回應封包走另一條線出去，被對方或 ISP 當成非對稱流量丟掉，症狀是「第二條線 ping 得到卻連不進來」。

**怎麼做**：

```bash
echo "100 wan2" | sudo tee -a /etc/iproute2/rt_tables       # 🟠 /etc/iproute2/；🔵 /usr/share/iproute2/（自訂放 /etc/iproute2/rt_tables.d/）
sudo ip route add default via 10.2.0.1 dev eth2 table wan2
sudo ip rule add from 10.2.0.0/24 table wan2
ip rule show; ip route show table wan2
# 永久化：🟠 Netplan 的 routes: [{to: default, via: ..., table: 100}] + routing-policy: [{from: 10.2.0.0/24, table: 100}]
#         🔵 nmcli con mod eth2 +ipv4.routes "0.0.0.0/0 10.2.0.1 table=100" +ipv4.routing-rules "priority 100 from 10.2.0.0/24 table 100"
```

**注意**：上面的 `ip rule` / `ip route ... table` 是臨時的，重開機即消失；先用臨時指令驗證行為正確，再照區塊內註解寫進 Netplan / nmcli 永久化。

### 9.3 WireGuard（兩者核心內建）

**名詞與關係**：WireGuard 把 VPN 簡化成「一個介面 + 一份金鑰對 + 每個 peer 一段」，而且用 `AllowedIPs` 一個欄位同時當路由表與存取控制：

```
本機 wg0（Address 10.8.0.1/24，ListenPort 51820/udp，PrivateKey）
   │ 送往 10.8.0.2 的封包 ──▶ 查各 [Peer] 的 AllowedIPs，找到含 10.8.0.2/32 的 peer ──▶ 用該 peer 的 PublicKey 加密 ──▶ 送到其 Endpoint
   │ 收到的封包 ──▶ 解密後檢查來源 IP 是否在該 peer 的 AllowedIPs 內，不在就丟（cryptokey routing）
   ▼
wg-quick@wg0：讀 /etc/wireguard/wg0.conf，建介面、設位址與路由、執行 PostUp（開防火牆 / masquerade）
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **WireGuard** | 核心 5.6 起內建的 VPN 協定與介面類型（`wg0`），以 UDP 傳輸、Curve25519 金鑰認證 | 沒有帳號密碼或憑證，身份就是公鑰；`wireguard-tools` 套件只提供 `wg`／`wg-quick` 使用者工具 |
| **金鑰對（PrivateKey ／ PublicKey）** | 每一端各產生一對：私鑰留在自己的 `[Interface]`，公鑰給對方寫進 `[Peer]` | 雙方互相持有對方公鑰才能握手；私鑰外洩等於任何人都能冒充這一端 |
| **peer** | 隧道另一端的一個節點，以 `[Peer]` 區段描述 | 一個 wg0 可有多個 peer（多個客戶端），彼此以 AllowedIPs 區分 |
| **AllowedIPs** | 該 peer 「允許」的 IP 範圍：出站時當路由表（去這些位址就走這個 peer），入站時當過濾器（來源不在範圍就丟） | 客戶端寫 `0.0.0.0/0` = 全部流量走 VPN；伺服器端寫 `/32` = 只接受該客戶端的隧道 IP；寫錯就是「有握手沒流量」 |
| **Endpoint ／ ListenPort** | 對方的公網位址：埠／自己監聽的 UDP 埠 | 至少一端要有固定可達的 Endpoint（伺服器）；ListenPort 要在防火牆放行 UDP |
| **wg-quick ／ PostUp** | 讀 conf 檔建立整個介面的腳本（systemd 單元 `wg-quick@`）／介面起來後執行的指令 | 防火牆放行與 masquerade 放在 PostUp，隨隧道一起開關；純 `wg` 指令只設金鑰與 peer，不管位址與路由 |
| **handshake** | 兩端每 2 分鐘左右重新協商一次工作階段金鑰 | `wg show` 的 `latest handshake` 有時間代表金鑰與 Endpoint 正確；有握手但 transfer 不增加，問題就在 AllowedIPs 或路由 |

**為什麼**：WireGuard 已在核心內建，設定只有一對金鑰與幾行 conf，比 OpenVPN 少掉大半設定量、效能也更好，是跨站點連線與遠端管理的首選。常見的失敗點不在 WireGuard 本身，而是忘了開 UDP 埠、忘了 masquerade，或 `AllowedIPs` 寫錯。

**怎麼做**：

```bash
sudo apt install -y wireguard        # 🟠      sudo dnf install -y wireguard-tools   # 🔵
wg genkey | tee server.key | wg pubkey > server.pub
sudo tee /etc/wireguard/wg0.conf <<'EOF'
[Interface]
Address = 10.8.0.1/24
ListenPort = 51820
PrivateKey = <server.key 內容>
# 🟠 PostUp = ufw route allow in on wg0 out on eth0; iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
# 🔵 PostUp = firewall-cmd --zone=trusted --add-interface=wg0 && firewall-cmd --add-masquerade

[Peer]
PublicKey = <client.pub>
AllowedIPs = 10.8.0.2/32
EOF
sudo chmod 600 /etc/wireguard/wg0.conf
sudo systemctl enable --now wg-quick@wg0
sudo wg show
# 🟠 ufw allow 51820/udp    🔵 firewall-cmd --permanent --add-port=51820/udp && firewall-cmd --reload
# 也可用 nmcli 管：sudo nmcli con import type wireguard file /etc/wireguard/wg0.conf
```

**注意**：`PrivateKey` 明文寫在 conf 裡，所以一定要 `chmod 600`；`AllowedIPs` 同時決定「接受哪個 peer 的來源」與「哪些目的地走隧道」，寫錯的症狀是 `wg show` 有握手但沒有流量。

---

## 10. 網路除錯流程

**名詞與關係**：除錯流程照 OSI 分層由下往上走，下表是流程與症狀表裡幾個尚未在前面出現的名詞：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **OSI 分層** | 把網路拆成實體、鏈路、網路、傳輸…等層次的參考模型 | 除錯依此順序：下層不通上層必不通，所以先驗 link 再驗 IP、路由、DNS、防火牆、服務 |
| **169.254.x.x（link-local）** | DHCP 拿不到位址時系統自動配的保底位址（APIPA） | 看到它就代表 DHCP 失敗，問題在鏈路或 DHCP 伺服器，不用再往 DNS 找 |
| **`rp_filter`** | reverse path filtering：核心檢查「這個來源 IP 的封包，回去時是否會從同一張卡出去」，不是就丟 | 嚴格模式（1）會把多網卡／policy routing 的合法流量當成偽造來源丟掉；設 2（loose）放寬成「只要有任何路由能到該來源即可」 |
| **MTU ／ PMTU** | 一次能送的最大封包大小（乙太網 1500）／路徑上最小的 MTU | VPN 或隧道要加表頭，實際可用 MTU 變小；若中間又擋掉 ICMP，PMTU 探測失敗，症狀是小封包通、大封包卡住，`ping -M do` 指定不分片來測 |
| **AVC ／ `semanage port`** | AVC 是 SELinux 的拒絕紀錄（`ausearch -m avc`）；`semanage port` 把某個埠標記給某類服務使用 | 🔵 服務綁非標準埠時，防火牆開了還是「起不來」或「連不到」，就要查 AVC 並用 `semanage port` 補標記（詳見 16 章） |

**為什麼**：網路問題最忌亂槍打鳥——先改防火牆、再改 DNS，最後發現是線沒插或網卡沒 UP。照 OSI 由下往上一層一層排除，每一步都有明確的指令與判斷依據，通常十分鐘內就能定位到是哪一層出問題，也避免在排錯過程中改壞原本正常的設定。

**怎麼做**：

```
1. 實體層   ip -br link          → 是否 UP？ethtool eth0 → Link detected: yes？
2. IP 層    ip -br addr          → 有沒有拿到 IP？（169.254.x.x = DHCP 失敗）
3. 路由     ip route             → 有 default？ip route get 8.8.8.8
4. 閘道     ping <gateway>       → 不通：VLAN / 線路 / 交換器
5. 外網 IP  ping 1.1.1.1         → 通但下一步不通：DNS 問題
6. DNS      resolvectl query example.com / dig
7. 防火牆   ufw status / firewall-cmd --list-all / nft list ruleset；對方：nc -zv host port
8. 服務     ss -ltnp 有沒有在 0.0.0.0 監聽（只綁 127.0.0.1 就對外不通）
9. 🔵 SELinux  ausearch -m avc -ts recent（服務綁非標準埠被擋）
10. 封包    tcpdump -i eth0 -nn host <ip>
```

流程走完還沒答案，再對照下面這張表；這些都是實際踩過、而且症狀與原因看起來不相干的案例：

| 症狀 | 原因與解法 |
|---|---|
| 開機卡 2 分鐘 "Wait for Network to be Configured" | 🟠 沒接線的網卡沒設 `optional: true`；或 `systemctl disable systemd-networkd-wait-online` |
| 🔵 開機後網卡沒 IP | 安裝時網卡預設關閉：`nmcli con mod ens18 connection.autoconnect yes && nmcli con up ens18` |
| 改了 `/etc/resolv.conf` 又被改回 | 它是 resolved 的符號連結；改 `resolved.conf.d` 或 Netplan / nmcli 的 DNS 設定 |
| `netplan apply` 後 IP 沒變 | 舊 IP 殘留：`sudo ip addr flush dev ens18 && sudo netplan apply`；或檔名排序被其他檔覆蓋（`netplan get` 看合併結果） |
| 🔵 服務綁 8080 被 SELinux 擋 | `semanage port -a -t http_port_t -p tcp 8080` |
| Docker 容器對外被擋 / 或反而穿透防火牆 | 見 §6 Docker 與 ufw 說明 |
| 兩張網卡各有 default route 導致回應走錯卡 | 設 metric、或 policy routing（§9.2）、或 `net.ipv4.conf.all.rp_filter=2` |
| MTU 問題（VPN / 隧道內大封包卡住） | `ping -M do -s 1472 host` 測；`ip link set dev wg0 mtu 1380` |

---

## 11. 本章差異總結

**為什麼**：本章的差異幾乎都源自同一個根本選擇：Canonical 在後端之上加了 Netplan 這層宣告式抽象、並以「裝了但沒開」的簡單 ufw 當前端；Red Hat 則讓 NetworkManager 與 firewalld 直接當唯一入口，並預設啟用防火牆與 SELinux。跨發行版操作時，最容易踩雷的是「防斷線機制」（Fedora 沒有 `netplan try` 的等價物）與「非標準埠」（Fedora 要多做一步 `semanage port`）這兩列。

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 設定方式 | Netplan YAML（`/etc/netplan/`） | nmcli / nmtui / keyfile（`/etc/NetworkManager/system-connections/`） |
| Server 後端 | systemd-networkd | NetworkManager |
| 套用 | `netplan try` / `netplan apply` | `nmcli con up <name>` |
| 驗證語法 | `netplan generate` | `nmcli --offline con add ...`（輸出 keyfile 不寫入） |
| 防斷線機制 | `netplan try`（120 秒自動還原） | 無內建（可先 `nmcli con clone` 備份；或 `systemd-run --on-active=5m nmcli con up old`） |
| 防火牆 | ufw（需手動 enable） | firewalld（預設啟用，zone 模型） |
| 防火牆設定檔 | `/etc/ufw/user.rules`、`before.rules` | `/etc/firewalld/zones/*.xml` |
| 開埠 | `ufw allow 80/tcp` | `firewall-cmd --permanent --add-service=http && firewall-cmd --reload` |
| 速率限制 | `ufw limit ssh` | rich rule `limit value=3/m` |
| NAT | 編輯 `before.rules` | `--add-masquerade` |
| 網卡分區 | 無（用 `in on eth1` 條件） | zone（public / internal / trusted…） |
| nftables 設定檔 | `/etc/nftables.conf` | `/etc/sysconfig/nftables.conf` + `/etc/nftables/*.nft` |
| 路由表名稱檔 | `/etc/iproute2/rt_tables` | `/etc/iproute2/rt_tables.d/*.conf`（主檔在 `/usr/share/iproute2/`） |
| DNS 工具套件 | `dnsutils` | `bind-utils` |
| traceroute / mtr | `traceroute`、`mtr-tiny` | `traceroute`、`mtr` |
| WireGuard 套件 | `wireguard` | `wireguard-tools` |
| 非標準埠 | 直接開 ufw | 開 firewalld **且** `semanage port` |
