# 07 - 日誌管理

兩者都以 **systemd-journald** 為核心。差別在：🟠 Ubuntu Server 預設另裝 **rsyslog** 把日誌落成傳統文字檔（`/var/log/syslog`、`auth.log`）；🔵 Fedora 預設**只有 journald**，要傳統檔案（`/var/log/messages`、`secure`）需自行安裝 rsyslog。以下資訊皆於實驗機驗證。

**本章解決什麼問題**：日誌平時沒人看，出事時卻是唯一證據：服務半夜掛掉、SSH 被暴力嘗試、`/var/log` 塞滿磁碟讓整台機器寫不進東西。兩個發行版都用 journald，但 Ubuntu 另外把日誌落成傳統文字檔而 Fedora 沒有，所以同一句 `grep /var/log/auth.log` 在 Fedora 上直接找不到檔案。本章的取捨原則：查詢一律先用 `journalctl`（兩者相同、有索引、有 metadata），只有需要傳統檔案（相容舊工具、集中傳送）時才處理 rsyslog；並且一定要設保留上限，因為日誌無限成長是最常見的磁碟爆滿原因。

---

## 1. 日誌架構

**為什麼**：排查「日誌到底去哪了」之前要先知道流向：所有來源（服務 stdout、syslog(3)、核心）先進 journald，rsyslog 與集中式 agent 都是從 journald 或 `/dev/log` 再往下接。看懂這張圖就能回答「為什麼 Fedora 沒有 `/var/log/messages`」與「為什麼 rsyslog 停了 journalctl 還有資料」。

**怎麼做**：

```
應用程式 ──┬─ stdout/stderr（systemd 服務）──┐
           ├─ syslog(3) / logger ──────────────┤→ journald（二進位、索引、/run 或 /var/log/journal）
核心 dmesg ─┴──────────────────────────────────┘        │
                                                        ├─→ rsyslog（imjournal / imuxsock）→ /var/log/*.log、遠端
                                                        └─→ journal-remote / Promtail / Vector / Filebeat → 集中式
```

兩者差異源於 Fedora 完全採用 systemd 的設計哲學——journald 本身已能持久化、索引、輪替，rsyslog 被視為選配；Ubuntu Server 為了相容大量假設 `/var/log/syslog` 存在的腳本與監控工具，預設仍裝 rsyslog 落地成文字檔。

| | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| journald 持久化 | ✅ `/var/log/journal/`（實測存在） | ✅ `/var/log/journal/`（實測存在） |
| journald 設定主檔 | `/etc/systemd/journald.conf` | `/usr/lib/systemd/journald.conf`（範本，勿改） |
| journald 覆寫目錄 | `/etc/systemd/journald.conf.d/`（自行建立） | `/etc/systemd/journald.conf.d/`（自行建立） |
| rsyslog | 預設安裝、啟用 | 預設不安裝 |
| rsyslog 輸出（實測） | `/var/log/syslog`、`auth.log`、`kern.log`、`mail.log`、`user.log`、`cron.log` | `/var/log/messages`、`secure`、`maillog`、`cron`、`spooler` |
| rsyslog 輸入來源 | imuxsock + imklog（`/dev/log`、核心） | **imjournal**（從 journald 讀，實測 `/etc/rsyslog.conf`） |
| rsyslog 執行身分 | `syslog` 使用者（PrivDrop；自訂日誌目錄要 `chown syslog:adm`） | root |
| 套件日誌 | `/var/log/apt/history.log`、`term.log`、`/var/log/dpkg.log` | `/var/log/dnf5.log`（+ `dnf history`） |
| 稽核 | `auditd` 需安裝 | `audit` 預設安裝 → `/var/log/audit/audit.log` |
| 防火牆日誌 | `/var/log/ufw.log`（rsyslog 20-ufw.conf） | `journalctl -u firewalld`；被拒封包在 `journalctl -k` |
| SELinux / AppArmor | `journalctl -k -g apparmor`、`/var/log/kern.log` | `/var/log/audit/audit.log`（type=AVC）、`journalctl -t setroubleshoot` |
| 登入紀錄 | `/var/log/wtmp`、`btmp`、`lastlog`（`last`、`lastb`、`lastlog`） | 同左 |
| logrotate 預設 | weekly、rotate 4、`su root adm`、無 dateext | weekly、rotate 4、**dateext** |
| logrotate 觸發 | `logrotate.timer`（每日） | `logrotate.timer`（每日） |

---

## 2. journalctl（兩者完全相同）

**為什麼**：journald 為每筆日誌附上 `_PID`、`_UID`、`_SYSTEMD_UNIT` 等結構化欄位並建索引，所以「這個服務在上次開機、警告等級以上的訊息」一行就能篩出來，不必 `grep` 一堆檔案再靠時間戳對齊。兩個發行版指令完全相同，是本章唯一不需分開記的部分。

**怎麼做**：

```bash
journalctl                               # 全部（最舊在前；按 End 到底）
journalctl -e                            # 直接跳到最後
journalctl -f                            # 即時跟隨（tail -f）
journalctl -n 100                        # 最後 100 行
journalctl -r                            # 反向（最新在前）
journalctl -b                            # 本次開機；-b -1 上次開機；--list-boots
journalctl -k                            # 核心訊息（= dmesg）
journalctl -p err                        # 優先權 ≥ err（emerg alert crit err warning notice info debug）
journalctl -p warning..err
journalctl -u nginx -u php-fpm           # 依服務
journalctl --user -u syncthing           # 使用者服務
journalctl _PID=1234; journalctl _UID=1000; journalctl _COMM=sshd
journalctl -t sshd                       # 依 syslog identifier
journalctl -g "Failed password"          # grep（PCRE）
journalctl --since "2026-08-28 09:00" --until "2026-08-28 10:00"
journalctl --since today; --since yesterday; --since "1 hour ago"
journalctl -o json-pretty -n 1           # 輸出格式：short / short-iso / verbose / json / cat
journalctl -o verbose -u nginx | head    # 看所有 metadata 欄位（用來組合過濾條件）
journalctl -F _SYSTEMD_UNIT              # 列出某欄位所有值
journalctl --no-pager -u nginx > nginx.log
journalctl -u sshd -b -p warning --no-pager -o short-iso   # 常用組合
journalctl _TRANSPORT=kernel; _TRANSPORT=stdout           # 依來源
journalctl --disk-usage                  # 佔用空間（實測約 20 MB 全新系統）
journalctl --verify                      # 檢查日誌檔完整性
```

腳本與自訂程式也應該寫進 journal 而不是自己開檔案，才能沿用同一套查詢與輪替：

```bash
logger -t myscript -p user.err "something failed"   # 從腳本寫
echo "msg" | systemd-cat -t myapp -p info
```

### 2.1 journald 設定

**為什麼**：journald 預設最多用檔案系統的 10%（上限 4 GB），但沒有時間上限，長期營運的機器 journal 只會越長越大；反過來，某個服務狂噴日誌時若沒有速率限制，會把其他服務的記錄擠掉。主檔會隨套件更新被覆寫，所以設定一律放 drop-in 目錄。

**怎麼做**：建立 `/etc/systemd/journald.conf.d/00-custom.conf`（兩者共通；不要改主檔）：

```ini
[Journal]
Storage=persistent          # auto（預設，/var/log/journal 存在即持久）/ persistent / volatile / none
Compress=yes
SystemMaxUse=2G             # /var/log/journal 上限（預設檔案系統 10%，最多 4G）
SystemKeepFree=1G
SystemMaxFileSize=128M
MaxRetentionSec=3month      # 保留期限
MaxFileSec=1week            # 每週切檔（方便按時間清理）
RateLimitIntervalSec=30s    # 速率限制（同一服務 30 秒內超過 RateLimitBurst 則丟棄）
RateLimitBurst=10000
ForwardToSyslog=no          # 🟠 Ubuntu 用 rsyslog imuxsock 時此值不需開；🔵 用 imjournal 也不需要
ForwardToWall=yes
MaxLevelStore=debug
```

重啟後生效；磁碟已經滿了時用 `--vacuum-*` 立即清理，不必等輪替：

```bash
sudo systemctl restart systemd-journald
sudo journalctl --vacuum-size=500M       # 立即清到 500M 以下
sudo journalctl --vacuum-time=30d        # 清 30 天前
sudo journalctl --vacuum-files=5         # 只留 5 個檔
sudo journalctl --rotate                 # 強制切檔
```

**注意**：非 root 讀 journal：把使用者加入 `systemd-journal` 群組（兩者相同）；🟠 Ubuntu 的 `adm` 群組也可讀。

---

## 3. rsyslog

**為什麼**：journald 是二進位格式，舊監控工具、部分 Fail2ban jail、稽核要求「日誌是純文字可長期保存」的環境，都仍需要傳統 `/var/log/*.log`；rsyslog 也是把日誌送到遠端最成熟、最省資源的做法。兩者差異：Ubuntu 預設裝好並用 imuxsock 直接聽 `/dev/log`；Fedora 沒裝，裝了之後預設用 imjournal 從 journald 讀，所以 journald 的 `ForwardToSyslog` 在兩邊都不必開。

### 3.1 安裝與預設

**為什麼**：自訂規則前得先知道預設規則寫在哪、已經把哪些 facility 導去哪個檔案，否則會出現同一筆訊息寫進兩個檔案、或被上游規則的 `stop` 吃掉的情況。Ubuntu 把規則拆在 `/etc/rsyslog.d/50-default.conf`，Fedora 直接寫在主檔。

**怎麼做**：

```bash
# 🟠 預設已安裝
systemctl status rsyslog
cat /etc/rsyslog.conf; ls /etc/rsyslog.d/     # 實測：20-ufw.conf、50-default.conf
# 🔵 需安裝
sudo dnf install -y rsyslog && sudo systemctl enable --now rsyslog
cat /etc/rsyslog.conf                         # 規則直接寫在主檔；/etc/rsyslog.d/ 供擴充
```

🟠 `/etc/rsyslog.d/50-default.conf`（實測）：

```
auth,authpriv.*                 /var/log/auth.log
*.*;auth,authpriv.none          -/var/log/syslog
kern.*                          -/var/log/kern.log
mail.*                          -/var/log/mail.log
mail.err                        /var/log/mail.err
*.emerg                         :omusrmsg:*
```

🔵 `/etc/rsyslog.conf`（實測）：

```
*.info;mail.none;authpriv.none;cron.none    /var/log/messages
authpriv.*                                  /var/log/secure
mail.*                                      -/var/log/maillog
cron.*                                      /var/log/cron
*.emerg                                     :omusrmsg:*
uucp,news.crit                              /var/log/spooler
local7.*                                    /var/log/boot.log
```

（`-` 前綴 = 非同步寫入，效能較好但當機可能遺失最後幾行。）

### 3.2 自訂規則範例

**為什麼**：應用程式的訊息混在 `syslog` / `messages` 裡難找也難輪替，分成獨立檔案後才能針對它設 logrotate、給開發者讀取權限、或送到不同的集中式目的地。`stop` 是關鍵：沒有它同一筆訊息會同時進 `syslog` 與自訂檔，磁碟用量加倍。

**怎麼做**：`/etc/rsyslog.d/30-myapp.conf`：

```
# 依程式名稱分檔
if $programname == 'myapp' then {
    action(type="omfile" file="/var/log/myapp/app.log")
    stop                              # 不再寫入 syslog / messages
}
# 依 facility：應用程式用 logger -p local3.info
local3.*    /var/log/local3.log
# 依內容
:msg, contains, "CRON" stop           # 丟掉 cron 雜訊
```

```bash
sudo mkdir -p /var/log/myapp
sudo chown syslog:adm /var/log/myapp  # 🟠 實測必要：Ubuntu 的 rsyslogd 以 syslog 使用者執行（$PrivDropToUser syslog），目錄 root 擁有時寫不進去且不報錯
                                      # 🔵 Fedora 的 rsyslogd 以 root 執行，不需要
sudo rsyslogd -N1                     # 檢查語法（實測輸出 "End of config validation run. Bye."）
sudo systemctl restart rsyslog
logger -t myapp "test message" && tail -1 /var/log/myapp/app.log
```

**注意**：🟠 Ubuntu 的 rsyslogd 以 `syslog` 使用者執行，自訂目錄若是 root 擁有會**靜默**寫入失敗——沒有錯誤訊息、檔案就是不出現；排查時先看目錄擁有者，再用 `rsyslogd -N1` 驗語法。

### 3.3 集中式：rsyslog 遠端傳送 / 接收

**為什麼**：主機被入侵後攻擊者第一件事就是清本機日誌，磁碟壞了日誌也一起消失，把日誌即時送到另一台機器是唯一可靠的保存方式。TCP + 磁碟佇列是必要的：UDP 在網路抖動時直接丟包，沒有佇列則集中伺服器重啟期間的日誌全部遺失。

**怎麼做**：

```
# 客戶端 /etc/rsyslog.d/90-remote.conf（TCP + 佇列，斷線不丟）
*.* action(type="omfwd" target="log.example.com" port="6514" protocol="tcp"
           queue.type="LinkedList" queue.size="10000" queue.saveOnShutdown="on"
           action.resumeRetryCount="-1"
           StreamDriver="gtls" StreamDriverMode="1" StreamDriverAuthMode="x509/name")   # TLS；不加密改 port 514 並去掉 StreamDriver*

# 伺服器 /etc/rsyslog.d/10-server.conf
module(load="imtcp")            # module(load="imudp") + input(type="imudp" port="514") 若要 UDP
input(type="imtcp" port="514")
template(name="PerHost" type="string" string="/var/log/remote/%HOSTNAME%/%PROGRAMNAME%.log")
if $fromhost-ip != '127.0.0.1' then { action(type="omfile" dynaFile="PerHost"); stop }
```

```bash
# 🟠 ufw allow 514/tcp     🔵 firewall-cmd --permanent --add-port=514/tcp && firewall-cmd --reload
# 🔵 SELinux：非標準埠需 semanage port -a -t syslogd_port_t -p tcp 6514；接收目錄需 var_log_t 標籤
# TLS 需要 rsyslog-gnutls（🟠）/ rsyslog-gnutls（🔵）
```

替代方案：journald 原生的 `systemd-journal-remote` / `systemd-journal-upload`（🟠 `apt install systemd-journal-remote`、🔵 `dnf install systemd-journal-remote`），或現代的 Loki + Promtail / Grafana Alloy、Vector、Elastic Filebeat。

---

## 4. logrotate

**為什麼**：rsyslog 與應用程式自己寫的檔案不會自動輪替，一個 nginx access.log 幾個月就能長到數十 GB。logrotate 每日由 timer 觸發，兩者預設都是 weekly、留 4 份，差別在 Fedora 全域開了 `dateext`（檔名帶日期），Ubuntu 用 `.1`、`.2` 序號並以 `su root adm` 處理目錄權限。

**怎麼做**：

```bash
cat /etc/logrotate.conf; ls /etc/logrotate.d/
systemctl list-timers logrotate.timer         # 每日執行
sudo logrotate -d /etc/logrotate.conf         # -d 模擬（debug，不實際做）
sudo logrotate -f /etc/logrotate.d/myapp      # 強制立即輪替
cat /var/lib/logrotate/status                 # 🟠 狀態檔；🔵 /var/lib/logrotate/logrotate.status
```

`/etc/logrotate.d/myapp`：

```
/var/log/myapp/*.log {
    daily
    rotate 14
    size 100M                 # 或 maxsize（daily 且超過大小）/ minsize
    missingok
    notifempty
    compress
    delaycompress             # 最新一份先不壓（程式可能還在寫）
    dateext                   # 檔名用日期（🔵 全域預設）
    dateformat -%Y%m%d
    create 0640 myapp adm     # 🟠 群組慣例 adm；🔵 慣例 root
    su myapp myapp            # 目錄擁有者非 root 時必加（否則報 "insecure permissions"）
    sharedscripts
    postrotate
        systemctl kill -s HUP myapp.service 2>/dev/null || true   # 通知程式重開檔案
    endscript
}
# copytruncate：程式不支援重開檔案時用（複製後清空原檔；極少量遺失風險）
```

**注意**：輪替後程式若仍握著舊檔案的 fd，會繼續寫進已改名的檔案、新檔永遠是空的；`postrotate` 送 HUP 讓程式重開檔案，程式不支援時才退而用 `copytruncate`。

🟠 Ubuntu 實測 `/etc/logrotate.d/rsyslog`：weekly、rotate 4、compress、postrotate 呼叫 `/usr/lib/rsyslog/rsyslog-rotate`。
🔵 Fedora 實測：只有 `missingok / sharedscripts / postrotate systemctl reload rsyslog`，週期沿用全域（weekly、rotate 4、dateext）。

---

## 5. 稽核日誌（auditd）

**為什麼**：一般日誌只記錄程式「願意說」的事；auditd 掛在核心的系統呼叫層，能回答「誰在幾點改了 `/etc/sudoers`」「哪個帳號刪了那個檔案」，且記錄由核心產生、程式無法繞過，合規（PCI-DSS、STIG）幾乎都強制要求。Fedora 因 SELinux 拒絕訊息（AVC）也走 audit，所以預設就裝好；Ubuntu 要自行安裝。

**怎麼做**：

```bash
sudo apt install -y auditd audispd-plugins   # 🟠
# 🔵 預設安裝（audit 套件）
sudo systemctl enable --now auditd
sudo auditctl -l                              # 目前規則
sudo auditctl -s                              # 狀態
ls /etc/audit/rules.d/                        # 持久規則（audit.rules 由此產生）
ls /usr/share/audit-rules/                    # 🔵 範例規則（10-base-config、30-stig、30-pci-dss-v31…）
ls /usr/share/doc/auditd/examples/rules/      # 🟠 範例規則
```

常用規則 `/etc/audit/rules.d/50-custom.rules`：

```
-D                                    # 清除舊規則
-b 8192                               # buffer
-f 1                                  # 失敗時 printk（2 = panic，高安全環境）
# 監看檔案
-w /etc/passwd -p wa -k identity
-w /etc/shadow -p wa -k identity
-w /etc/sudoers -p wa -k sudoers
-w /etc/sudoers.d/ -p wa -k sudoers
-w /etc/ssh/sshd_config -p wa -k sshd
-w /var/log/audit/ -k auditlog
# 監看系統呼叫（誰刪了檔案）
-a always,exit -F arch=b64 -S unlink,unlinkat,rename,renameat -F auid>=1000 -F auid!=unset -k delete
# 特權指令
-a always,exit -F path=/usr/bin/sudo -F perm=x -F auid>=1000 -F auid!=unset -k priv
-e 2                                  # 鎖定規則（改規則需重開機）；開發時用 -e 1
```

載入規則並查詢：

```bash
sudo augenrules --load && sudo auditctl -l
sudo ausearch -k identity -ts today            # 依 key 查
sudo ausearch -m USER_LOGIN -sv no             # 登入失敗
sudo ausearch -m avc -ts recent                # 🔵 SELinux 拒絕
sudo ausearch -f /etc/passwd -i                # -i 把 uid / syscall 轉成可讀
sudo aureport --summary; sudo aureport -au     # 報表；-au 認證
sudo aureport -x --summary                     # 執行檔統計
# 輪替：/etc/audit/auditd.conf 的 max_log_file、num_logs、max_log_file_action=ROTATE
```

**注意**：`-e 2` 鎖定後連 root 都不能改規則，只能重開機，開發規則階段一定先用 `-e 1`；規則太多會拖慢系統呼叫，`-w` 監看整個目錄時尤其明顯。

---

## 6. 登入與安全相關日誌

**為什麼**：SSH 被暴力嘗試是每台公網主機每天都在發生的事，看登入日誌是判斷「只是掃描」還是「已經有人進來」的第一步；`sudo` 日誌則是事後追究誰動了什麼的依據。服務名稱 `ssh` vs `sshd`、檔案 `auth.log` vs `secure` 是兩者僅有的差異。

**怎麼做**：

```bash
last -n 20                      # 成功登入（wtmp）
lastb -n 20                     # 失敗登入（btmp）；需 root
lastlog                         # 每個帳號最後登入
who -a; w
# SSH
journalctl -u ssh -g "Accepted|Failed" --since today      # 🟠
journalctl -u sshd -g "Accepted|Failed" --since today     # 🔵
grep -E "Accepted|Failed" /var/log/auth.log               # 🟠（rsyslog）
grep -E "Accepted|Failed" /var/log/secure                 # 🔵（rsyslog）
# sudo
journalctl -t sudo --since today                          # 兩者
# 統計暴力破解來源
journalctl -u sshd -g "Failed password" -o cat | awk '{print $(NF-3)}' | sort | uniq -c | sort -rn | head   # 🔵（🟠 改 -u ssh）
```

---

## 7. 應用程式日誌慣例

**為什麼**：出問題時最浪費時間的是找日誌在哪——同一套 Apache 在 Ubuntu 叫 `apache2/access.log`、在 Fedora 叫 `httpd/access_log`；PostgreSQL 與 MariaDB 在 Fedora 上預設只進 journal。差異來自套件維護者的慣例：Debian 傾向讓每個服務有自己的 `/var/log/<svc>/` 文字檔，Fedora 傾向交給 journald。

**怎麼做**：查表定位；找不到時 `journalctl -u <服務>` 幾乎都能命中：

| 應用 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| Nginx | `/var/log/nginx/{access,error}.log` | 同左 |
| Apache | `/var/log/apache2/{access,error}.log` | `/var/log/httpd/{access_log,error_log}` |
| PostgreSQL | `/var/log/postgresql/postgresql-16-main.log` | `journalctl -u postgresql`（或 `/var/lib/pgsql/data/log/`） |
| MariaDB | `/var/log/mysql/error.log`（或 journal） | `journalctl -u mariadb`（或 `/var/log/mariadb/mariadb.log`） |
| 套件管理 | `/var/log/apt/history.log`、`term.log`、`/var/log/dpkg.log` | `/var/log/dnf5.log`、`dnf history` |
| 自動更新 | `/var/log/unattended-upgrades/unattended-upgrades.log` | `journalctl -u dnf5-automatic` |
| 安裝程式 | `/var/log/installer/`（含 autoinstall-user-data） | `/var/log/anaconda/`（含 `anaconda-ks.cfg` 在 /root） |
| cloud-init | `/var/log/cloud-init.log`、`cloud-init-output.log` | 同左 |
| 防火牆 | `/var/log/ufw.log` | `journalctl -u firewalld`、`journalctl -k -g "REJECT\|DROP"` |
| Cockpit | `journalctl -u cockpit` | 同左 |
| 開機 | `/var/log/boot.log`（🟠 需 plymouth）、`journalctl -b` | `/var/log/boot.log`、`journalctl -b` |

💡 容器化服務（Docker / Podman）：`docker logs`、`podman logs`；Docker 預設 json-file driver 要設 `max-size` 否則塞爆磁碟（`/etc/docker/daemon.json`：`{"log-driver":"json-file","log-opts":{"max-size":"50m","max-file":"5"}}`），或改用 `journald` driver。

---

## 8. 集中式日誌方案比較

**為什麼**：機器超過三、五台後，逐台 SSH 進去 `journalctl` 不可行；告警、跨主機關聯、保留期限也都需要集中。選型看規模與查詢方式：小規模、只要 grep 用 rsyslog 就夠；要用 label 查詢並與 Prometheus 共用儀表板用 Loki；需要全文檢索才值得付出 Elasticsearch 的資源成本。

**怎麼做**：

| 方案 | 適合 | 備註 |
|---|---|---|
| rsyslog → rsyslog | 傳統、小型、低資源 | 上面 §3.3；查詢靠 grep |
| journal-upload → journal-remote | 純 systemd 環境 | `journalctl -D /var/log/journal/remote/` 查詢 |
| Promtail / Alloy → **Loki** + Grafana | 中小型、與 Prometheus 整合 | 索引只做 label，成本低 |
| Filebeat → **Elasticsearch** + Kibana | 全文檢索、大量 | 資源需求高 |
| Vector | 高效能的收集 / 轉換 / 路由 | 可取代 Promtail / Filebeat |
| 雲端 | CloudWatch Logs、GCP Logging、Azure Monitor | agent 皆有 deb / rpm |

Promtail 安裝範例（兩者共通，Grafana 官方 repo）：

```bash
# 🟠
sudo mkdir -p /etc/apt/keyrings && wget -q -O - https://apt.grafana.com/gpg.key | gpg --dearmor | sudo tee /etc/apt/keyrings/grafana.gpg >/dev/null
echo "deb [signed-by=/etc/apt/keyrings/grafana.gpg] https://apt.grafana.com stable main" | sudo tee /etc/apt/sources.list.d/grafana.list
sudo apt update && sudo apt install -y promtail
# 🔵
sudo tee /etc/yum.repos.d/grafana.repo <<'EOF'
[grafana]
name=grafana
baseurl=https://rpm.grafana.com
repo_gpgcheck=1
enabled=1
gpgcheck=1
gpgkey=https://rpm.grafana.com/gpg.key
EOF
sudo dnf install -y promtail
# /etc/promtail/config.yml 加 journal scrape：
#   scrape_configs:
#     - job_name: journal
#       journal: {max_age: 12h, labels: {job: systemd-journal}}
#       relabel_configs: [{source_labels: ['__journal__systemd_unit'], target_label: 'unit'}]
```

---

## 9. 日誌相關故障排除

**為什麼**：日誌相關故障的共通點是「症狀出現在別處」——磁碟滿導致資料庫寫入失敗、時間不同步讓集中查詢對不上、程序握著已刪檔案的 fd 讓 `df` 與 `du` 對不起來。先對症狀查表，再回到對應章節處理。

**怎麼做**：

| 症狀 | 處理 |
|---|---|
| `/var/log` 爆滿 | `sudo du -xsh /var/log/* \| sort -h`；`journalctl --vacuum-size`；檢查 logrotate 有沒有跑（`journalctl -u logrotate`）；某服務狂寫 → 用 `RateLimit*` 或修程式 |
| journal 佔用太大 | 設 `SystemMaxUse`；`journalctl --vacuum-*` |
| 刪了大日誌檔空間沒回來 | 程序還握著 fd：`sudo lsof +L1`；重啟該服務或 `: > file` 清空而非 rm |
| 🔵 `/var/log/messages` 不存在 | 沒裝 rsyslog；用 `journalctl` 或 `dnf install rsyslog` |
| 🟠 `/var/log/syslog` 停止更新 | `systemctl status rsyslog`；journald 是否 `ForwardToSyslog`/imuxsock 正常；磁碟滿 |
| journalctl 顯示 "No journal files were found" | `/var/log/journal` 不存在：`sudo mkdir -p /var/log/journal && sudo systemd-tmpfiles --create --prefix /var/log/journal && sudo systemctl restart systemd-journald` |
| 日誌時間對不上 | `timedatectl`；`journalctl --utc`；rsyslog 用 `RSYSLOG_FileFormat` 模板取得高精度 ISO 時間 |
| logrotate 報 "insecure permissions" | 加 `su <user> <group>` |
| auditd 報 "backlog limit exceeded" | 加大 `-b`，或在核心參數加 `audit_backlog_limit=8192` |
