# 12 - 監控與故障排除

**本章解決什麼問題**：沒有監控的系統只會在使用者抱怨時才知道出事，而且出事後沒有「正常時長什麼樣」的對照可比，根因分析只能猜；故障排除若沒有固定流程，則常常在還沒看日誌前就重開機，把證據一起清掉。本章前半建立監控體系（指標、日誌、告警）與兩個發行版的安裝方式，後半是常見故障的系統化排除流程：開不了機、救援模式、磁碟爆滿、記憶體、網路、服務、核心當機分析，以及向支援單位提交資訊的工具。取捨原則：小環境用零設定工具先有數據，正式環境用 Prometheus 這類可告警、可回溯的體系；排障永遠先保留證據、一次只改一件事。

---

## 1. 監控體系

**名詞與關係**：監控體系的名詞可以分成「三種資料」（指標、日誌、追蹤）、「兩種視角」（白箱、黑箱）、以及各自負責的工具；告警是建立在指標之上的另一層：

```
                    ┌─ 指標 metrics：數字的時間序列（CPU 80%、磁碟剩 5%）── Prometheus 定期向 exporter 抓（pull）── Grafana 畫圖
被監控主機 ── 白箱 ──┼─ 日誌 logs：逐筆事件文字（誰在幾點做了什麼）── Loki / ELK 集中收集（07 章）
                    └─ 追蹤 traces：一個請求穿過多個服務的路徑與耗時 ── 應用層才有，本章不談
                    黑箱：從外部用 HTTP / TCP / ICMP 探測「使用者連不連得到」── Blackbox exporter、Uptime Kuma
告警：Prometheus 依規則判斷指標 → Alertmanager 去重、分組、路由 → Email / Slack / PagerDuty
單機零設定：Netdata / Cockpit / glances 自己收自己畫，不需要 Prometheus 那一整套
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **指標（metrics）** | 帶時間戳的數值序列（如每 15 秒一筆 CPU 使用率） | 便宜、可算趨勢、可告警；但只知道「數字多少」不知道「為什麼」 |
| **日誌（logs）** | 逐筆事件的文字記錄 | 回答「當時發生了什麼」；07 章的 journald / rsyslog / Loki / ELK |
| **追蹤（traces）** | 單一請求經過各服務的時間分解 | 微服務除錯用，需要應用埋點；本章不涉及 |
| **白箱 / 黑箱** | 白箱＝從系統內部讀指標（node_exporter）；黑箱＝從外部模擬使用者探測（連不連得到、回應多久） | 白箱知道原因、黑箱知道使用者感受；兩者互補而非二選一 |
| **Prometheus** | 拉取式（pull）時序資料庫兼告警規則引擎 | 向各 exporter 的 `/metrics` 抓數據；規則觸發後交給 Alertmanager |
| **exporter** | 把某個系統的狀態轉成 Prometheus 格式並以 HTTP 提供的小程式 | `node_exporter` 管主機、`blackbox_exporter` 管外部探測，還有 mysqld / nginx 等各自的 exporter |
| **Grafana** | 儀表板工具，向 Prometheus / Loki 查詢並畫圖 | 只負責顯示，本身不存資料 |
| **Alertmanager** | 接收 Prometheus 送來的告警，做去重、分組、靜音與路由到通知管道 | 沒有它，Prometheus 只會「判斷有告警」但不會通知任何人 |
| **Loki / ELK** | 兩套日誌集中系統：Loki 只索引標籤（輕量、與 Grafana 同家）；ELK（Elasticsearch + Logstash + Kibana）全文索引（重量） | 與 Prometheus 並列，各管指標 / 日誌；Grafana 可同時接兩者 |
| **Blackbox exporter / Uptime Kuma** | 黑箱探測工具：前者是 Prometheus 生態的 exporter，後者是獨立、自帶介面與通知的工具 | 前者要配 Prometheus 才會告警；後者自己有通知功能 |
| **Netdata / Cockpit / glances** | 單機即時監控：Netdata 秒級圖表、Cockpit 網頁管理介面、glances 終端機儀表板 | 零設定、不需伺服器端；但沒有跨機器的長期回溯與告警體系 |
| **Zabbix / Nagios / Icinga / Checkmk** | 傳統 agent + server 式監控 | 功能完整但設定重；既有環境常見，新建環境多改用 Prometheus |

**為什麼**：監控要回答三種不同的問題——「現在的數字是多少」（指標）、「當時發生了什麼」（日誌）、「該叫誰起床」（告警），沒有一個工具能同時做好三件事，所以要分層組合；黑箱探測則補上「從使用者角度看服務到底通不通」這個內部指標看不到的視角。

**怎麼做**：依環境規模選一層或多層，下表是各層的主流選項。

| 層 | 工具 | 說明 |
|---|---|---|
| 指標（metrics） | **Prometheus + node_exporter + Grafana** | 事實標準；pull 模式 |
| 日誌 | Loki / ELK（07 章） | |
| 告警 | Alertmanager → Email / Slack / PagerDuty | |
| 可用性（黑箱） | Blackbox exporter、Uptime Kuma | HTTP / TCP / ICMP 探測 |
| 簡易單機 | **Netdata**、Cockpit、`glances` | 秒級即時圖表，零設定 |
| 傳統 | Zabbix、Nagios / Icinga、Checkmk | agent 兩者皆有套件或官方 repo |
| 雲端 | CloudWatch agent、Google Ops Agent、Azure Monitor agent | |

### 1.1 node_exporter（主機指標）

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **node_exporter** | Prometheus 官方的主機 exporter，以 HTTP 在 9100 埠提供 `/metrics` | 由 Prometheus 定期抓取；是所有主機層告警的資料來源 |
| **collector** | node_exporter 內部的模組，每個負責一類指標（cpu、filesystem、systemd、apt…） | 預設啟用一組，`--collector.<name>` 開關；🟠 apt collector 提供 `node_reboot_required` |
| **textfile collector** | 讀取指定目錄內 `*.prom` 檔案並原樣輸出的 collector | 讓 cron / 備份腳本把「最後成功時間」寫成檔案變成指標，供 §1.2 的 BackupStale 告警使用 |
| **/metrics 與 scrape** | `/metrics` 是 exporter 的純文字輸出端點；scrape 是 Prometheus 依 `scrape_interval` 抓一次的動作 | `curl :9100/metrics` 看得到就代表 exporter 正常，剩下是 Prometheus 端設定的問題 |

**為什麼**：node_exporter 是把 CPU、記憶體、磁碟、網路、systemd 狀態暴露成 Prometheus 指標的標準 agent，所有主機告警（磁碟將滿、服務失敗、負載過高）都建立在它之上；沒有它 Prometheus 就沒有主機層的數據可抓。

兩個發行版的套件名不同：Ubuntu 沿用 Debian 的 `prometheus-` 前綴命名；Fedora 套件取名 `node-exporter`，但 unit 與環境檔沿用了 Debian 的命名，容易讓人以為裝錯。

**怎麼做**：套件庫版本較舊但有 unit 與防火牆整合；需要最新功能時用官方二進位。

```bash
# 🟠 Ubuntu 套件庫（1.7）
sudo apt install -y prometheus-node-exporter
sudo systemctl enable --now prometheus-node-exporter        # 服務名帶 prometheus- 前綴；預設 :9100
# 🔵 Fedora 套件庫（實測套件名 node-exporter 1.12，但 unit 與環境檔沿用 Debian 命名）
sudo dnf install -y node-exporter
sudo systemctl enable --now prometheus-node-exporter         # 實測 unit 為 prometheus-node-exporter.service（別名 node_exporter.service）；參數在 /etc/default/prometheus-node-exporter
# ⚪ 或官方二進位（版本最新）
curl -sSLO https://github.com/prometheus/node_exporter/releases/latest/download/node_exporter-*.linux-amd64.tar.gz
curl -s http://localhost:9100/metrics | head
# 🟠 ufw allow from <prometheus-ip> to any port 9100      🔵 firewall-cmd --permanent --add-port=9100/tcp --zone=internal
# textfile collector（自訂指標，如備份成功時間）：--collector.textfile.directory=/var/lib/node_exporter/textfile
```

**注意**：9100 埠只開給 Prometheus 主機，指標內容（核心版本、掛載點、程序數）對攻擊者是很好的偵察資料。

### 1.2 Prometheus + Alertmanager + Grafana

**名詞與關係**：三個元件之間的資料流是單向的，設定檔裡的每個區段都對應下圖的一段：

```
exporter（:9100 / :9115）──scrape（每 scrape_interval）──▶ Prometheus 時序資料庫
                                                                 │ 每 evaluation_interval 用 PromQL 評估 rule_files
                                                                 ▼
                                      alert 條件持續超過 for ──▶ Alertmanager（依 severity 等標籤路由）──▶ Email / Slack / PagerDuty
Grafana ──PromQL 查詢──▶ Prometheus（畫圖）
blackbox exporter：Prometheus 抓 :9115/probe?target=URL；relabel 把設定裡的「目標 URL」改寫成「exporter 位址＋參數」
promtool：改設定 / 規則後先檢查語法，再 reload
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **scrape / scrape_interval** | Prometheus 主動去 exporter 抓一次數據的動作與週期（15 秒） | pull 模式：目標列在 `scrape_configs`；抓不到就 `up == 0` |
| **時序（time series）** | 一個指標名稱＋一組標籤（instance、job…）對應的時間數列 | 標籤是 PromQL 篩選與 `by(instance)` 聚合的依據 |
| **PromQL** | Prometheus 的查詢語言，用於儀表板與告警規則的 `expr` | `rate()` 算變化率、`predict_linear()` 線性預測、`up` 是抓取是否成功 |
| **告警規則（rule_files / for / severity）** | `rule_files` 指向規則檔；`for` 是條件要持續多久才觸發；`severity` 是自訂的嚴重度標籤 | `for` 過濾瞬間尖峰；`severity` 給 Alertmanager 決定 critical 才呼叫人 |
| **Alertmanager** | 接收 Prometheus 的告警、去重分組、依標籤路由到通知管道（:9093） | Prometheus 的 `alerting:` 區段指向它；沒有它告警只會停在 Prometheus 網頁上 |
| **Grafana** | 儀表板；用 PromQL 向 Prometheus 查詢畫圖（:3000） | 儀表板 ID 1860 是社群做好的 node_exporter 全覽 |
| **Blackbox exporter / probe / relabel** | 黑箱探測 exporter（:9115）；`/probe?target=URL&module=http_2xx` 才是它的抓取端點；relabel 是把 Prometheus 設定裡的目標 URL 改寫成「抓 exporter、URL 當參數」的固定寫法 | 沒有 relabel，Prometheus 會直接去抓 example.com 而不是 exporter |
| **promtool** | Prometheus 附的檢查工具（`check config` / `check rules`） | reload 前跑，語法錯誤才不會讓整個 Prometheus 拒絕載入 |
| **dead-man switch** | 「應該定期出現的訊號沒出現就告警」的反向告警模式 | `BackupStale`（超過 36 小時沒有成功時間戳）與 `HostDown`（`up == 0`）都是這種模式；監控系統本身也該有一個送到外部服務的 dead-man 告警 |

**為什麼**：Prometheus 負責定期抓取與儲存時序、評估告警規則，Alertmanager 負責去重、分組與通知路由，Grafana 負責畫圖；三者分工明確，缺一角就會變成「有圖沒告警」或「告警風暴沒人理」。

Ubuntu 套件庫的 Prometheus 停在 2.x LTS、Grafana 完全不收（授權與更新頻率考量），要加官方 repo；Fedora 更新較快，套件庫直接有 Prometheus 3.x 與 Grafana，但反而沒有 Alertmanager。

**怎麼做**：

```bash
# 🟠 套件庫：prometheus 2.45、prometheus-alertmanager；Grafana 需官方 repo（見 07 章 §8 的 apt.grafana.com）
sudo apt install -y prometheus prometheus-alertmanager && sudo apt install -y grafana
# 🔵 套件庫：prometheus 3.13、grafana 10.2（實測皆在官方庫）；alertmanager 需官方二進位或 Grafana repo
sudo dnf install -y prometheus grafana
# 設定：🟠 /etc/prometheus/prometheus.yml；🔵 /etc/prometheus/prometheus.yml（同）
# Grafana：http://host:3000（admin/admin）；🟠🔵 皆為 grafana-server 服務、/etc/grafana/grafana.ini
# 🔵 SELinux：grafana / prometheus 套件自帶政策；自行下載二進位放 /opt 時需 chcon -t bin_t
```

Prometheus 是 pull 模式，所以設定檔的核心是「去哪裡抓、多久抓一次、告警規則在哪、告警送給誰」；blackbox 那段 relabel 是把探測目標從 URL 改寫成 exporter 位址的固定寫法。`/etc/prometheus/prometheus.yml` 最小範例：

```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s
alerting:
  alertmanagers:
    - static_configs: [{targets: ['localhost:9093']}]
rule_files: ['/etc/prometheus/rules/*.yml']
scrape_configs:
  - job_name: node
    static_configs:
      - targets: ['web01:9100', 'web02:9100', 'db01:9100']
  - job_name: blackbox
    metrics_path: /probe
    params: {module: [http_2xx]}
    static_configs: [{targets: ['https://www.example.com']}]
    relabel_configs:
      - {source_labels: [__address__], target_label: __param_target}
      - {source_labels: [__param_target], target_label: instance}
      - {target_label: __address__, replacement: 'localhost:9115'}
```

告警規則的重點是 `for`：瞬間尖峰不該叫人，持續一段時間才算問題；`DiskWillFillIn24h` 用線性預測提前一天通知，比「已經滿了」有用得多。`/etc/prometheus/rules/node.yml` 常用告警：

```yaml
groups:
  - name: node
    rules:
      - alert: HostDown
        expr: up == 0
        for: 2m
        labels: {severity: critical}
        annotations: {summary: "{{ $labels.instance }} 無法抓取"}
      - alert: HighCPU
        expr: 100 - (avg by(instance)(rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100) > 90
        for: 10m
        labels: {severity: warning}
      - alert: LowMemory
        expr: node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes < 0.1
        for: 5m
        labels: {severity: warning}
      - alert: DiskWillFillIn24h
        expr: predict_linear(node_filesystem_avail_bytes{fstype!~"tmpfs|overlay"}[6h], 24*3600) < 0
        for: 30m
        labels: {severity: warning}
      - alert: DiskFull
        expr: node_filesystem_avail_bytes{fstype!~"tmpfs|overlay"} / node_filesystem_size_bytes < 0.05
        for: 5m
        labels: {severity: critical}
      - alert: HighLoad
        expr: node_load5 / count by(instance)(node_cpu_seconds_total{mode="idle"}) > 2
        for: 15m
        labels: {severity: warning}
      - alert: SystemdUnitFailed
        expr: node_systemd_unit_state{state="failed"} == 1
        for: 5m
        labels: {severity: warning}
      - alert: RebootRequired
        expr: node_reboot_required > 0            # 🟠 Ubuntu 的 apt collector 提供；🔵 用 textfile 寫 dnf needs-restarting 結果
        for: 24h
        labels: {severity: info}
      - alert: BackupStale
        expr: time() - backup_last_success_timestamp > 36*3600
        labels: {severity: critical}
```

設定檔或規則有語法錯誤時 Prometheus 會拒絕載入，reload 前先檢查，才不會在改告警時把整個監控弄停：

```bash
promtool check config /etc/prometheus/prometheus.yml
promtool check rules /etc/prometheus/rules/*.yml
sudo systemctl reload prometheus
```

**注意**：告警要有人負責收，沒人看的告警等於沒有告警；`severity` 標籤是 Alertmanager 路由的依據，critical 才進呼叫。Grafana 儀表板：匯入 ID **1860**（Node Exporter Full）。

### 1.3 Netdata（單機 / 小型環境，零設定）

**為什麼**：Prometheus 體系要三個元件加設定檔，對一兩台機器來說太重；Netdata（單機即時監控 agent：自己收集、儲存並用網頁畫出秒級圖表，不需要外部伺服器）裝完就有秒級圖表與內建異常偵測（以機器學習比對每個指標的歷史模式，偏離時在圖上標記），適合「先有數據再說」，或當 Prometheus 之外的即時排障視角（15 秒抓一次的 Prometheus 看不到秒級尖峰）。

**怎麼做**：🟠 套件庫有；🔵 用官方 kickstart 腳本（Netdata 提供的一鍵安裝腳本，會偵測發行版並裝最新版）。

```bash
sudo apt install -y netdata            # 🟠 1.43（套件庫較舊；官方 kickstart 腳本較新）
# 🔵 套件庫無；用官方安裝腳本：bash <(curl -Ss https://get.netdata.cloud/kickstart.sh)
# http://host:19999；預設只綁 localhost，對外需改 /etc/netdata/netdata.conf 的 bind to
```

**注意**：Netdata 預設沒有認證，開放 `bind to` 之前先用防火牆限來源或放在反向代理後面。

### 1.4 簡易腳本監控 + 通知

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **systemd timer** | systemd 的排程 unit（`.timer`），到時間就啟動同名 `.service` | 取代 cron；失敗會留在 `systemctl --failed` 與 journal，天然可被 `OnFailure=` 接住 |
| **OnFailure=** | unit 的 `[Unit]` 選項：本 unit 失敗時啟動指定的另一個 unit | 由 systemd 自己觸發，不需要外部輪詢；配合模板 unit 就能一份通知服務共用 |
| **模板 unit（`@`）與 `%i` / `%n` / `%H`** | 檔名帶 `@` 的 unit 可用 `name@instance` 實例化；`%i` 展開為實例名、`%n` 為完整 unit 名、`%H` 為主機名 | `OnFailure=notify-failed@%n.service` 把失敗的 unit 名當實例傳給通知服務 |
| **msmtp / MTA** | MTA 是負責把信送出去的程式；msmtp 是極簡 MTA，只把信轉給外部 SMTP 伺服器 | 伺服器不需要完整 postfix，msmtp 設好帳號就能用 `mail` 寄信 |
| **webhook** | 用 HTTP POST 把訊息推到 Slack / Teams / 自家服務的 URL | 比 email 即時；通知腳本用 `curl` 打即可 |

**為什麼**：有些檢查（備份是否完成、憑證幾天到期、某個 cron 是否跑過）用 exporter 反而麻煩，一個 timer 加腳本就夠；而 systemd 的 `OnFailure=` 讓任何服務失敗都能觸發通知，不需要外部監控輪詢。

**怎麼做**：先確定機器能寄信或打 webhook，再用通知 unit 模板掛到要監看的服務上。

```bash
# 磁碟 / 服務健康檢查腳本，配 systemd timer 每 5 分鐘；失敗時寄信 / webhook
# 郵件：🟠 apt install msmtp-mta（或 postfix）；🔵 dnf install msmtp（設 /etc/msmtprc）→ mail -s "subj" ops@example.com
# systemd 失敗通知：
sudo tee /etc/systemd/system/notify-failed@.service <<'EOF'
[Unit]
Description=Notify on failure of %i
[Service]
Type=oneshot
ExecStart=/usr/local/bin/notify.sh "%i failed on %H"
EOF
# 在要監看的服務加 drop-in：[Unit] OnFailure=notify-failed@%n.service
```

---

## 2. 故障排除方法論

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **journal / journalctl** | systemd 的結構化日誌與其查詢工具 | `-p err -b` 看本次開機的錯誤；`--since` 依時間篩；07 章 |
| **dmesg** | 核心環形緩衝區（ring buffer）的訊息：硬體、驅動、OOM、I/O 錯誤 | 只在記憶體裡，重開機就沒了（journal 的 `-k` 保存一份）；蒐證要先做 |
| **套件歷史** | 套件管理器記錄的安裝 / 升級 / 移除紀錄（🟠 `/var/log/apt/history.log`、🔵 `dnf history`） | 回答「最近改了什麼」最可靠的來源之一 |
| **etckeeper** | 把 `/etc` 放進 git、每次套件操作後自動 commit 的工具 | 有它就能 `git diff` 看設定差異；沒有就只能靠 `find -mmin` 猜 |
| **last** | 讀 `/var/log/wtmp` 列出登入 / 重開機紀錄 | 確認「誰在那個時間登入過」 |

**為什麼**：排障最常見的失敗模式是跳過蒐證直接動手：重開機清掉了 dmesg 與 journal 的線索、同時改三個設定不知道哪個有效、修好了卻沒人知道為什麼。有固定流程才能在壓力下不漏步驟，也才能把根因變成監控與告警，避免同樣的事再發生。

**怎麼做**：

1. **確認症狀**：誰、什麼時候、什麼操作、錯誤訊息全文。
2. **最近改了什麼**：`journalctl --since "-1h"`、`last`、套件歷史（🟠 `/var/log/apt/history.log`、🔵 `dnf history`）、`/etc` 的變更（`find /etc -mmin -60`；etckeeper）。
3. **分層檢查**：硬體 → 核心 → 儲存 → 網路 → 服務 → 應用。
4. **先看日誌再猜**：`journalctl -p err -b`、`dmesg -T`、服務日誌。
5. **一次改一件事**並記錄。
6. **修復後寫下根因與預防**（監控 / 告警 / 自動化）。

登入後第一分鐘先跑這一行，把系統狀態、失敗的服務、核心錯誤、監聽埠與網路一次抓齊，之後再決定往哪個方向深入。第一分鐘指令組（兩者共通）：

```bash
uptime; hostnamectl; df -h; free -h; systemctl --failed; journalctl -p err -b --no-pager | tail -30; dmesg -T | tail -30; ss -ltnp; ip -br a; ip r
```

---

## 3. 開機故障

**名詞與關係**：開機是一條接力鏈，每一棒壞掉的症狀與救法都不同，先對照鏈上的位置：

```
韌體（UEFI / BIOS）──▶ 開機載入器 GRUB（讀 grub.cfg，載入核心）──▶ 核心 + initramfs（載驅動、找到根檔案系統）
    ──▶ systemd（依 target 啟動 unit）──▶ 各服務 ──▶ 登入
壞在哪一棒 → 症狀：
  韌體找不到開機項 ── 直接進韌體設定或別的 OS（§3.3 efibootmgr）
  GRUB 壞 ── grub rescue> 提示（§3.2、§3.3）
  核心 / initramfs 壞 ── kernel panic、找不到 root（選舊核心、重建 initramfs）
  systemd 階段卡住 ── "A start job is running"、emergency mode（§3.1 救援 target）
  服務失敗 ── 開機完成但 systemctl --failed 有項目（§7）
救援手段依深度：Live 媒體 chroot（連本機核心都不用）＞ rd.break / init=/bin/bash（只用核心）＞ emergency.target ＞ rescue.target
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **韌體（UEFI / BIOS）** | 主機板上第一個執行的程式，負責找到並載入開機載入器 | UEFI 從 ESP 的開機項載 shim / GRUB，BIOS 從 MBR；詳見 01 章 §1.5 |
| **GRUB** | 兩者共用的開機載入器，顯示選單、載入核心與 initramfs | 設定由 `update-grub` / `grub2-mkconfig` 產生；選單能改核心參數是救援的入口 |
| **initramfs** | 隨核心一起載入的小型記憶體檔案系統，含掛載真正根檔案系統所需的驅動與工具（🟠 `update-initramfs`、🔵 `dracut` 產生） | 缺模組就找不到 root；`rd.break` 就是停在這一層 |
| **systemd target** | systemd 的啟動里程碑（`rescue` / `emergency` / `multi-user` / `graphical`），決定要啟動哪些 unit | 核心參數 `systemd.unit=` 可指定停在哪個 target |
| **主控台 / Live 媒體** | 主控台是本機螢幕鍵盤或 BMC / 雲端序列埠；Live 媒體是可開機的安裝 USB | 開機故障沒有 SSH，只能靠這兩條路 |

**為什麼**：開機故障是唯一「連 SSH 都進不去」的一類問題，只能靠主控台或 Live 媒體，且不同階段（韌體 → GRUB → 核心 / initramfs → systemd → 服務）壞掉的表現與修法完全不同；先判斷卡在哪個階段，才知道該用哪種救援方式。

### 3.1 進入救援環境

**名詞與關係**：救援方式從淺到深排成一列，越往右跳過的東西越多：

```
淺 ────────────────────────────────────────────────────────────────────────────────▶ 深
rescue.target            emergency.target          init=/bin/bash（🟠）          Live 媒體 chroot
單人模式：掛好所有檔案系統、  只掛根（唯讀）、不啟動        不執行 systemd，bash 當 PID 1     用 USB 的核心開機，把本機根
啟動基本服務              其他任何 unit              rd.break（🔵）                掛到 /mnt 再 chroot 進去
需要 root 密碼            需要 root 密碼             停在 initramfs，根在 /sysroot   什麼都不需要
                                                    不需要密碼
能救的情況越來越多、能用的工具越來越少；🟠 recovery mode 是 GRUB 選單裡包裝好的救援入口
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **rescue.target** | systemd 的單人模式：掛載所有檔案系統、啟動基本服務，但不起網路與多人服務 | 最淺的救援；需要 root 密碼（🟠 root 預設鎖定，用不了） |
| **emergency.target** | 只掛根檔案系統（唯讀）、不啟動任何其他 unit 的最小 systemd 環境 | fstab 出錯時 systemd 會自動掉進來；仍需 root 密碼 |
| **init=/bin/bash** | 核心參數：不執行 systemd，直接把 bash 當 PID 1 | 🟠 改密碼的標準做法；根是唯讀，要先 `remount,rw` |
| **rd.break** | dracut 的核心參數：在 initramfs 把根掛到 `/sysroot` 之後、切換過去之前停下來給 shell | 🔵 的對應做法；改完要 `chroot /sysroot`，且 SELinux 需 `/.autorelabel` |
| **initramfs shell / dracut** | dracut 是 🔵 產生 initramfs 的工具，其 shell 只有最基本的指令 | `rd.break` 進的就是它；🟠 用 `update-initramfs`，沒有 `rd.break` |
| **chroot** | 把某個目錄當成根目錄執行指令 | Live 媒體救援與 `rd.break` 都靠它「進到」本機系統執行 `grub-install`、`passwd`、套件指令 |
| **recovery mode** | 🟠 GRUB「Advanced options」裡的選單式救援，提供 fsck / network / root shell 等選項 | 本質是帶 `recovery nomodeset` 參數開機後的救援環境 |
| **/.autorelabel** | 根目錄下的旗標檔，🔵 下次開機時讓 SELinux 重新標記全部檔案 | 在 `rd.break` / chroot 內改檔會產生錯誤標籤，不放這個檔就登入失敗 |
| **LUKS / LVM** | 全碟加密 / 邏輯卷管理 | Live 媒體要先 `cryptsetup open`、`vgchange -ay` 才看得到根，否則 chroot 步驟從第一步就失敗 |

**為什麼**：救援方式有深淺之分：`rescue.target` 保留大部分服務、最方便，但需要 root 密碼；`emergency.target` 與 `init=/bin/bash` / `rd.break` 越過越多東西，能救的情況越多、能用的工具越少；Live 媒體 chroot 是連核心都不用本機的最後手段。依故障深度選最淺可用的一種。

兩者做法不同的根源：Ubuntu 的 GRUB 預設隱藏選單並提供 recovery mode 的互動式選單；Fedora 沒有 recovery 項目，而是用 dracut 的 `rd.break` 進 initramfs shell，且因 SELinux 在 shell 內改檔後必須重新標記。

**怎麼做**：

| 方式 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| GRUB 選單 | 開機時按住 `Shift`（BIOS）或 `Esc`（UEFI） | 開機時按 `Esc` / 方向鍵（預設顯示選單 5 秒） |
| 內建救援項目 | Advanced options → recovery mode（有選單：fsck、network、root shell） | 無專用項目；編輯開機項 |
| 編輯核心參數 | 選單按 `e`，在 `linux` 行尾加參數，`Ctrl-X` 開機 | 同左 |
| 救援 target | `systemd.unit=rescue.target`（需 root 密碼；root 鎖定時用下面方法） | 同左 |
| 緊急 target | `systemd.unit=emergency.target` | 同左 |
| 跳過 init | `init=/bin/bash`（root 唯讀；`mount -o remount,rw /`） | `rd.break`（進 initramfs shell；`mount -o remount,rw /sysroot; chroot /sysroot`；改完 `touch /.autorelabel`） |
| Live 媒體 chroot | 安裝 USB → Try Ubuntu → chroot | 安裝 USB → Troubleshooting → Rescue a Fedora system（自動掛到 `/mnt/sysroot` 並提示 chroot） |

Live 媒體 chroot 的關鍵是順序：先解鎖 LUKS、啟用 LVM，再掛根與 `/boot`、`/boot/efi`，最後 bind 掛 `dev/proc/sys/run`，否則 chroot 內的 `grub-install`、`dracut`、套件管理都會失敗。Live 媒體 chroot 通用步驟：

```bash
lsblk -f; sudo cryptsetup open /dev/sda3 crypt   # 若有 LUKS
sudo vgchange -ay                                # 若有 LVM
sudo mount /dev/ubuntu-vg/ubuntu-lv /mnt         # 🟠 / 🔵 /dev/fedora/root（Btrfs：-o subvol=root）
sudo mount /dev/sda2 /mnt/boot; sudo mount /dev/sda1 /mnt/boot/efi
for d in dev proc sys run; do sudo mount --bind /$d /mnt/$d; done
sudo chroot /mnt
# 修復後：exit; umount -R /mnt; reboot
```

**注意**：🔵 在 `rd.break` 或 chroot 內改過任何檔案（尤其 `/etc/shadow`）都要 `touch /.autorelabel`，否則 SELinux 標籤不對，下次開機會登入失敗。

### 3.1.1 在實驗 VM 上練習：序列主控台、GRUB 選單、rescue / rd.break（已實測）

**為什麼**：開機救援是「平常用不到、出事時手忙腳亂」的典型技能，而且錯一步可能讓系統更難救；在可以隨時還原快照的 VM 上先走一遍，真正出事時才不會在主控台前猜。實驗 VM 由 [`scripts/lab/setup-vm.sh`](scripts/lab/setup-vm.sh) 建立（README「建立實驗環境」方式 B），它已把 GRUB 選單設成在序列主控台停 5 秒——雲端映像預設 `GRUB_TIMEOUT=0`，🟠 Ubuntu 還把 GRUB 只輸出到 VGA（`/etc/default/grub.d/50-cloudimg-settings.cfg` 的 `GRUB_TERMINAL=console`），不改就永遠看不到選單。

**怎麼做**：

```bash
# 0) 先快照（練壞了可以回來）
bash scripts/lab/setup-vm.sh snapshot fedora before-rescue

# 1) 終端 A：接上序列主控台（按 Enter 出現 login；帳號 sysop 密碼 lab；Ctrl+] 離開）
bash scripts/lab/setup-vm.sh console fedora        # 或 ubuntu

# 2) 終端 B：重開機，回到終端 A 看開機訊息
virsh --connect qemu:///system reboot lab-vm-fedora
#    終端 A 會先看到 systemd 關機訊息，接著（Fedora 實測）：
#      Fedora Linux (6.19.10-300.fc44.x86_64) 44 (Cloud Edition)
#      Use the ^ and v keys to change the selection.
#      Press 'e' to edit the selected item, or 'c' for a command prompt.
#    （Ubuntu 實測）GNU GRUB version 2.12 → *Ubuntu / Advanced options for Ubuntu

# 3) 在 5 秒內按方向鍵停住選單 → 選核心 → 按 e 編輯
#    找到 linux 開頭那一行（🔵 linux /boot/vmlinuz-... ；🟠 linux /boot/vmlinuz-... root=...）
#    在行尾加上要練的參數（擇一），然後 Ctrl+X 開機：
#      systemd.unit=rescue.target     # 單人維護模式（需要 root 密碼；雲端映像 root 鎖定，會要求密碼而失敗 → 改用下一個）
#      systemd.unit=emergency.target  # 最小環境，/ 唯讀
#      init=/bin/bash                 # 🟠 跳過 systemd，直接 root shell
#      rd.break                       # 🔵 在 initramfs 階段中斷（dracut）
#      enforcing=0                    # 🔵 SELinux permissive 開機（標籤壞掉時用）
```

各模式進去之後要做的事（承 §3.1 表）：

```bash
# 🟠 init=/bin/bash 進來後：根目錄是唯讀，先重新掛成可寫
mount -o remount,rw /
passwd sysop                                   # 例：重設密碼
sync; exec /sbin/init                          # 或 reboot -f；不要直接關 VM

# 🔵 rd.break 進來後：真正的根目錄掛在 /sysroot
mount -o remount,rw /sysroot
chroot /sysroot
passwd sysop
touch /.autorelabel                            # 在 initramfs 動過檔案後，SELinux 需要重標籤，否則下次開機登不進去
exit; exit                                     # 離開 chroot 與 initramfs shell，系統繼續開機（重標籤會多花 1～2 分鐘）

# 兩者：練完確認
journalctl -b -p err --no-pager | tail       # 開機有沒有新錯誤
# 還原快照（不想留下練習痕跡）
virsh --connect qemu:///system snapshot-revert lab-vm-fedora before-rescue
```

**注意**：
- 序列主控台上的 GRUB 用 `^`/`v` 提示上下鍵；有些終端方向鍵不會被 GRUB 認得，改用 `Ctrl+P` / `Ctrl+N`。
- 編輯畫面的核心參數行很長會自動換行，`Ctrl+E` 跳到行尾再加參數。
- 只要在 GRUB 編輯畫面改的參數**只影響這一次開機**，不會寫回 `grub.cfg`——所以練習不會留下副作用。
- 自己的 VM 若看不到選單：🟠 建 `/etc/default/grub.d/99-lab.cfg`（`GRUB_TIMEOUT=5`、`GRUB_TIMEOUT_STYLE=menu`、`GRUB_TERMINAL="serial console"`、`GRUB_SERIAL_COMMAND="serial --speed=115200"`）後 `update-grub`；🔵 `/etc/default/grub` 設 `GRUB_TIMEOUT=5`、`grub2-editenv - unset menu_auto_hide`、`grub2-mkconfig -o /boot/grub2/grub.cfg`（實測兩者皆可在序列主控台看到選單）。
- `virsh console` 出現 `Active console session exists` 表示另一個終端還連著：到那邊 `Ctrl+]`，或加 `--force`。

### 3.2 常見開機問題

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **"A start job is running"** | systemd 等某個 unit（多半是掛載或 wait-online）完成的提示，預設等 90 秒 | 卡在這裡代表 systemd 階段；fstab 的 `nofail`（裝置不存在不擋開機）/ `_netdev`（等網路再掛）可避免 |
| **grub rescue>** | GRUB 找不到自己的模組目錄或 `/boot` 時退化成的極簡提示 | 表示 GRUB 階段壞；用 `set root` / `set prefix` 手動指路開進去後再重裝 GRUB（§3.3） |
| **Secure Boot / shim / MOK** | UEFI 只執行有簽章檔案的機制 / 被 Microsoft 簽過的第一階段載入器 / 使用者自己登錄的簽章金鑰（01 章 §1.5） | 第三方模組（NVIDIA、ZFS）沒有 MOK 簽章就被核心拒載，出現 "Secure Boot violation" |
| **akmods / DKMS** | 🔵 / 🟠 在核心更新後自動重編第三方模組的機制 | 新核心開不了圖形，常是它沒重編成功或簽章沒登錄 |
| **nomodeset** | 核心參數：不載入顯示驅動的 kernel mode setting，用基本 VGA 顯示 | 黑畫面時的臨時進入手段，進去後再修驅動 |
| **grubby** | 🔵 修改 GRUB 開機項與核心參數的工具（`--set-default`、`--args`） | 🟠 沒有，改 `/etc/default/grub` 再 `update-grub` |
| **apt-mark hold / dnf versionlock** | 鎖住套件版本不讓它升級 | 新核心有問題時先鎖舊版等修復 |
| **enforcing=0** | 核心參數：讓 SELinux 以 permissive 模式開機 | 標籤壞掉登入不了時，先用它進系統再 `/.autorelabel` |

**為什麼會發生這類故障**：絕大多數開機故障來自「上一次成功開機之後改了什麼」——新核心 / initramfs 缺模組、fstab 加了不存在或需要網路的裝置、顯示驅動或第三方模組與 Secure Boot 衝突、SELinux 標籤被 chroot 內的操作弄壞。先想最近的變更，再對照下表的症狀。

**怎麼做**：

| 症狀 | 原因 / 處理 |
|---|---|
| 卡在 "A start job is running for ... (1min 30s)" | fstab 有不存在的裝置：加 `nofail`；網路碟加 `_netdev`；網卡等待：`optional: true`（🟠）/ disable wait-online |
| 進 emergency mode，"Failed to mount /data" | 同上；或檔案系統損壞：`journalctl -xb`、`fsck` |
| GRUB rescue> 提示 | GRUB 找不到 `/boot`：`ls`、`set root=(hd0,gpt2)`、`set prefix=...`、`insmod normal`、`normal`；之後重裝 GRUB（§3.3） |
| 核心 panic / 找不到 root | initramfs 壞或缺模組：選舊核心開機 → 🟠 `update-initramfs -u -k all`；🔵 `dracut -f --regenerate-all` |
| 新核心開不了、舊核心可以 | 選舊核心 → 🟠 `apt-mark hold` 舊版 + 回報；🔵 `grubby --set-default` 舊版、`dnf downgrade kernel-core` 或 `dnf versionlock` |
| Secure Boot violation | 第三方模組未簽署或 shim 問題：關 Secure Boot 測試；🟠 `mokutil --import`；🔵 akmods 金鑰登錄 |
| 黑畫面（顯示驅動） | 核心參數加 `nomodeset`（暫時）→ 修 NVIDIA 驅動（🟠 `ubuntu-drivers`、🔵 akmod-nvidia 重建 `akmods --force`） |
| 忘記 root / 管理員密碼 | 🟠 `init=/bin/bash` → `mount -o remount,rw /` → `passwd alice`；🔵 `rd.break` → `chroot /sysroot` → `passwd` → `touch /.autorelabel` |
| 🔵 SELinux 標籤損壞（服務全掛、登入失敗） | 核心參數 `enforcing=0` 開機 → `touch /.autorelabel && reboot` |
| 開機後 `systemctl --failed` 有項目 | `systemctl status <unit>`、`journalctl -u <unit> -b` |

**注意**：兩者都預設保留舊核心，「選舊核心開機」永遠是第一個嘗試；不要急著在 chroot 內重裝 GRUB，那是 GRUB 本身壞掉才需要的步驟。

### 3.3 重裝開機載入器

**名詞與關係**：UEFI 開機牽涉「韌體 NVRAM 的開機項」與「ESP 內的檔案」兩個獨立的東西，任一邊壞掉症狀類似但修法不同：

```
UEFI 韌體 NVRAM 開機項（efibootmgr 管理）──指向──▶ ESP（/boot/efi）內的 \EFI\<distro>\shimx64.efi ──▶ grubx64.efi ──讀──▶ /boot/grub(2)/grub.cfg ──▶ 核心
BIOS 模式：MBR 內的 GRUB 第一階段（grub-install /dev/sda 寫入）──▶ /boot/grub ──▶ 核心
🟠 grub-install 自己產生 ESP 內的 EFI 檔      🔵 EFI 檔來自已簽署的 grub2-efi-x64 / shim-x64 套件，重裝套件即可
grub.cfg 由 update-grub（🟠）/ grub2-mkconfig（🔵）產生；檔案都在但 NVRAM 沒有開機項時，用 efibootmgr -c 補
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **GRUB / grub-install / grub2-install** | 開機載入器 / 把它安裝到 MBR 或 ESP 的指令 | 🟠 UEFI 與 BIOS 都用 `grub-install`；🔵 UEFI **不要**跑，會蓋掉簽署過的檔案破壞 Secure Boot |
| **ESP** | EFI System Partition，FAT32 小分割區掛在 `/boot/efi`，UEFI 只從這裡找開機檔 | `efibootmgr -c -p` 的分割區編號指的就是它 |
| **shim** | Microsoft 簽署的第一階段載入器，再載入發行版簽的 GRUB | UEFI 開機項要指向 `shimx64.efi` 而非 `grubx64.efi`，Secure Boot 才過得了 |
| **UEFI 開機項（NVRAM）/ efibootmgr** | 主機板 NVRAM 裡的開機項清單（Boot0000…）與管理它的工具 | 換主機板、別的 OS 覆寫、NVRAM 重置後 ESP 檔案都在但韌體找不到，就要用它重建 |
| **update-grub / grub2-mkconfig** | 掃描已安裝核心並重新產生 `grub.cfg` | 重裝 GRUB 檔案後一定要跑，否則選單是空的 |

**為什麼**：換碟、複製系統、其他 OS 覆寫 EFI 項目、或 `/boot` 被誤刪之後，GRUB 檔案或 UEFI 開機項會遺失，症狀是直接進 `grub rescue>` 或韌體找不到開機項。

Ubuntu 用上游 `grub-install` 產生 EFI 檔案；Fedora 的 EFI 檔案來自簽署過的 `grub2-efi-x64` / `shim-x64` 套件（配合 Secure Boot），自行 `grub2-install` 會產生未簽署的檔案並破壞 Secure Boot，所以修法是重裝套件而不是重跑 install。

**怎麼做**：

```bash
# 在 chroot 內（§3.1）
# 🟠 Ubuntu
grub-install /dev/sda                    # BIOS
grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=ubuntu   # UEFI
update-grub
# 🔵 Fedora
grub2-install /dev/sda                   # BIOS 才需要；UEFI 不要跑 grub2-install！
dnf reinstall -y grub2-efi-x64 shim-x64  # UEFI：重裝套件即可（會重放 /boot/efi/EFI/fedora/）
grub2-mkconfig -o /boot/grub2/grub.cfg
# UEFI 開機項壞掉（兩者）
efibootmgr -v
efibootmgr -c -d /dev/sda -p 1 -L "Fedora" -l '\EFI\fedora\shimx64.efi'     # 🔵；🟠 用 \EFI\ubuntu\shimx64.efi
```

**注意**：`efibootmgr -c` 的 `-p` 是 ESP 分割區編號，用 `lsblk -f` 確認是哪一個，指錯會產生一個開不了的項目。

---

## 4. 磁碟與檔案系統

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **inode** | 檔案系統裡每個檔案 / 目錄的中繼資料節點，數量在格式化時固定 | 大量小檔會先用光 inode（`df -i`），此時 `df -h` 還有空間卻寫不進去 |
| **已刪除但仍開啟的檔案** | 檔案被 `rm` 後只要還有程序持有描述子，區塊就不會釋放 | `lsof +L1` 列出；重啟該程序或 `: > /proc/PID/fd/N` 清空才能回收 |
| **remount ro** | 核心偵測到檔案系統錯誤或 I/O 錯誤後，自動把它改成唯讀以免擴大損壞 | 症狀是「什麼都存不了」；`dmesg` 會有 EXT4-fs error / XFS corrupt 訊息 |
| **SMART / smartctl** | 磁碟的自我監測資料 / 讀取它的工具 | `Reallocated` / `Pending` 非零就是硬體在壞，先備份再修 |
| **fsck** | 檔案系統一致性檢查與修復 | 必須 umount 後執行；SMART 有壞軌時先備份，修復可能加速資料流失 |
| **ncdu / du -x** | 互動式磁碟用量瀏覽器 / 只算同一檔案系統的 du | `-x` 避免算進其他掛載點而誤判 |
| **root 預留空間** | ext4 預設保留 5% 只給 root 寫 | 磁碟「100%」時 root 仍能操作，正是為了讓 `sudo` 與日誌不會一起掛掉 |
| **PV / VG / LV 與 metadata archive** | LVM 的三層（實體卷、卷群組、邏輯卷）；`/etc/lvm/archive` 是每次變更前自動備份的 metadata | VG 找不到時先 `vgscan`、`vgchange -ay`，再不行用 `vgcfgrestore` 從 archive 還原 |

**為什麼**：磁碟滿是最常見也最傷的故障：日誌寫不進、資料庫拒絕交易、連 `sudo` 都可能因為寫不了暫存檔而失敗。而「刪了檔案空間卻沒回來」幾乎都是被刪的檔還被某個程序開著；inode 用光則是 `df -h` 看起來還有空間卻寫不進去。另一類是磁碟出現 I/O 錯誤後核心為保護資料自動 remount 成唯讀，症狀是整台機器突然「什麼都存不了」。

**怎麼做**：先分清是空間、inode 還是唯讀，再逐層找出佔用者。

```bash
# 磁碟爆滿
df -h; df -i                                    # inode 也要看
sudo du -xsh /* 2>/dev/null | sort -h | tail    # 逐層往下
sudo ncdu -x /
sudo lsof +L1                                   # 已刪除但仍被開啟的檔案（空間不會釋放）→ 重啟該程序或 : > /proc/PID/fd/N
sudo journalctl --vacuum-size=200M              # 日誌
sudo apt clean; sudo dnf clean all              # 套件快取
docker system prune -af; podman system prune -af   # 容器
sudo find /var/log -name "*.gz" -mtime +30 -delete
# 🟠 舊核心：sudo apt autoremove --purge；snap 舊版：snap list --all | awk '/disabled/{print $1, $3}' | while read n r; do sudo snap remove "$n" --revision="$r"; done
# 🔵 舊核心：sudo dnf remove --oldinstallonly；Btrfs 看真實用量：btrfs filesystem usage /
# 根目錄滿到連 sudo 都失敗：先刪 /var/tmp 或 /var/cache 內容；ext4 預留 5% root 空間即為此設計

# 唯讀檔案系統（I/O 錯誤後核心自動 remount ro）
dmesg -T | grep -iE "I/O error|remount|EXT4-fs error|XFS.*corrupt"
sudo smartctl -a /dev/sda | grep -iE "reallocated|pending|uncorrect"
# 先備份資料 → umount → fsck（06 章 §4.3）→ 若硬體壞就換碟
# LVM 找不到 VG
sudo pvscan; sudo vgscan; sudo vgchange -ay; sudo vgcfgrestore -l vg0   # metadata 備份在 /etc/lvm/archive
```

**注意**：唯讀故障不要急著 `fsck` 或 remount rw，先確認 SMART 是否有壞軌並把資料備出來——對正在壞的磁碟做修復可能加速資料流失；`docker system prune -af` 會刪掉所有未使用的映像與 volume，確認沒有要留的再跑。

---

## 5. 記憶體與 CPU

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **OOM killer** | 核心記憶體耗盡時挑一個程序殺掉的機制，只在 `dmesg` / `journalctl -k` 留紀錄 | 應用日誌完全沒線索，所以「服務憑空消失」先查它 |
| **systemd-oomd / oomctl** | systemd 的使用者空間 OOM daemon，依 cgroup 的記憶體壓力（PSI）提早整組殺掉服務；`oomctl` 查它的狀態 | 🔵 預設啟用、🟠 桌面版啟用；比核心 OOM killer 早動手，紀錄在 `journalctl -u systemd-oomd` |
| **load / %wa** | load average 含等 I/O 的程序；`%wa` 是 CPU 閒著等 I/O 的比例（09 章 §1） | 「load 高但 CPU 閒」＝ `%wa` 高 ＝ I/O 瓶頸 |
| **D 狀態 / wchan** | 不可中斷睡眠（等 I/O 或驅動）；`wchan` 欄顯示它在核心的哪個函式裡等 | 大量 D 狀態指向儲存或 NFS；`kill` 對 D 狀態無效 |
| **殭屍（Z 狀態）** | 已結束但父程序尚未 `wait()` 回收的程序，只剩 PID 表項 | 不佔 CPU 與記憶體；殺父程序才會消失，大量出現是父程序的 bug |
| **soft lockup / hung task** | 核心偵測到某顆 CPU 超過 20 秒沒排程 / 某程序在 D 狀態超過 120 秒時的警告 | 通常是驅動或儲存問題；反覆出現要開 kdump（§8）抓證據 |
| **cpupower / tuned-adm** | 查 CPU 頻率與 governor / 查目前 tuned profile（09 章 §2、§7） | 「所有東西都慢但沒有瓶頸」時檢查是否被鎖在省電模式 |

**為什麼**：OOM killer 殺程序時應用日誌不會有任何線索，只有 `dmesg` 有記錄，所以「服務莫名其妙消失」第一件事是查 OOM；「負載高但 CPU 閒」則幾乎都是 I/O 等待，往 CPU 方向查會白費時間。這一節的指令是把幾種看似相同的「機器很慢」症狀拆開。

**怎麼做**：

```bash
# OOM
dmesg -T | grep -iE "out of memory|oom-kill|killed process"
journalctl -k -g "Out of memory"
systemctl status systemd-oomd; oomctl                  # 🔵 預設啟用；🟠 桌面版啟用
# 找兇手：ps aux --sort=-rss | head；smem；容器 docker stats
# 處理：加 swap（06 章）、限制服務 MemoryMax、修應用 leak、調 vm.overcommit
# 負載高但 CPU 閒（load 高、%wa 高）→ I/O 瓶頸：iostat -xz 1、iotop；D 狀態程序：ps -eo pid,stat,wchan:32,cmd | awk '$2 ~ /D/'
# 單核心 100%：mpstat -P ALL 1；perf top -p PID
# 殭屍程序：ps -eo pid,ppid,stat,cmd | awk '$3 ~ /Z/'；kill 父程序
# CPU 頻率被鎖低：cpupower frequency-info；tuned-adm active；BIOS 電源設定
# 核心 soft lockup / hung task 訊息：dmesg；通常是 I/O 或驅動；kdump 分析（§8）
```

**注意**：🔵 Fedora 的 systemd-oomd 會比核心 OOM killer 更早、依 cgroup 整組殺掉服務，查 OOM 時 `oomctl` 與 `journalctl -u systemd-oomd` 也要看；殭屍程序本身不佔資源，大量出現才代表父程序有 bug。

---

## 6. 網路

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **監聽位址** | 服務綁定的 IP：`0.0.0.0` / `::` 收所有介面，`127.0.0.1` 只收本機 | 綁錯是「本機通、外部不通」最常見的原因 |
| **ufw / firewalld / nftables** | 🟠 / 🔵 的防火牆前端，底層都是 nftables | 前端沒開埠或 zone 不對時，`nft list ruleset` 看最終規則 |
| **SELinux 埠標籤 / AVC** | 🔵 SELinux 限制服務只能綁特定埠類型；AVC 是它的拒絕紀錄 | `ausearch -m avc` 看是否被擋；非標準埠要 `semanage port` |
| **安全群組 / NAT** | 雲端主機外的虛擬防火牆 / 位址轉換 | 主機四步都通仍不通時的嫌疑犯，在主機上查不到 |
| **resolvectl / dig +trace** | systemd-resolved 的查詢工具 / 從根伺服器逐層追蹤 DNS | 分辨是本機解析設定錯還是上游 DNS 錯 |
| **MTU / DF（ping -M do）** | 最大封包長度 / 設「不要分段」旗標測試路徑 MTU | 「小封包通、大封包卡」的固定驗證法 |
| **tcpdump / Wireshark** | 命令列抓包 / 圖形化分析 | 前面都排除後，看實際封包才知道是誰送了 reset |
| **chrony / timedatectl** | NTP 客戶端 / 時間狀態查詢 | 時間偏差會讓 TLS 憑證與 Kerberos 驗證失敗，症狀像網路故障 |

**為什麼**：「服務對外不通」可能壞在監聽位址、本機防火牆、SELinux 埠標籤、外部防火牆 / 安全群組任何一層，從外面看症狀完全一樣；依「由內而外」的順序逐層驗證，每一步都能明確排除一層，比隨機猜快得多。DNS、MTU 與時間不同步則是三個常被忽略、症狀卻很像網路故障的來源。

**怎麼做**：流程見 [05 章 §10](05-網路與防火牆.md)。補充：

```bash
# 服務對外不通四步驟
ss -ltnp | grep :80                       # 1. 有監聽？綁 0.0.0.0 或 :: 而非 127.0.0.1？
curl -v http://127.0.0.1:80               # 2. 本機通？
sudo ufw status / sudo firewall-cmd --list-all; sudo nft list ruleset | grep -B2 -A2 80   # 3. 防火牆
sudo ausearch -m avc -ts recent | grep -i port   # 4. 🔵 SELinux 埠標籤
# 從外部：nc -zv host 80；traceroute；雲端安全群組 / NAT
# DNS 慢或錯：resolvectl status；resolvectl query；dig +trace；/etc/nsswitch.conf 的 hosts 順序
# 連線被 reset / 丟包：ss -ti；nstat -az | grep -i retrans；ethtool -S；MTU（ping -M do -s 1472）
# 抓包比對：sudo tcpdump -i any -nn host x.x.x.x -w /tmp/cap.pcap（用 Wireshark 開）
# 時間不同步導致 TLS / Kerberos 失敗：timedatectl；chronyc tracking
```

**注意**：本機四步都通、外部仍不通時，問題幾乎都在雲端安全群組、上游防火牆或 NAT，不要再回頭改主機設定。

---

## 7. 服務與應用

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **MAC（SELinux / AppArmor）** | 強制存取控制：🔵 SELinux 依標籤、🟠 AppArmor 依 profile 限制程序能碰什麼 | 權限看似正確卻被拒，多半是它；`setenforce 0` / `aa-complain` 是判斷法不是解法 |
| **sealert / aa-logprof** | 🔵 分析 AVC 拒絕並建議修法 / 🟠 依日誌互動式補 AppArmor 規則 | 確認是 MAC 擋住後用它們補政策，見 08 章 |
| **StartLimit / NRestarts** | systemd 對 unit 短時間內重啟次數的上限；`NRestarts` 是已重啟次數 | 超限後 `start` 直接被拒，要先 `reset-failed` |
| **systemd-analyze verify** | 檢查 unit 檔語法與引用的路徑是否存在 | 自寫 unit 起不來時第一個跑 |
| **systemctl show** | 印出 unit 的所有屬性（結束碼、環境變數、限制值） | `status` 只給摘要；`show -p Environment` 才看得到與手動執行的差異 |
| **network-online.target** | 「網路真正可用」的 target（09 章 §9） | 需要對外連線的服務要 `After=` + `Wants=` 它，否則比網路先起來 |
| **LimitNOFILE** | unit 的檔案描述子上限（09 章 §6） | "Too many open files" 的解法在 unit 不在 limits.conf |
| **strace / ltrace / gdb** | 追系統呼叫 / 追函式庫呼叫 / 除錯器 | 日誌看不出原因時，`strace -f` 看它最後開了什麼檔、被什麼 errno 拒絕 |
| **perf top / py-spy / jstack / pprof** | 通用 CPU 熱點 / Python / Java / Go 各語言的取樣分析器 | 服務「活著但慢」時用，09 章 §1.1 |

**為什麼**：服務啟動失敗的原因八成落在幾個固定類別：設定檔語法、權限 / MAC 標籤、埠被佔、環境與手動執行不同、相依順序、資源限制、以及 systemd 的啟動次數上限。`systemctl status` 只告訴你「失敗了」，要找到是哪一類得看 journal 與 `show` 的結果。

**怎麼做**：先看狀態與最近 100 行日誌，再跑設定檔語法檢查，最後才用 strace / 除錯器逐步追。

```bash
systemctl status nginx -l --no-pager; journalctl -u nginx -b -n 100
sudo systemctl show nginx -p ExecMainStatus -p Result -p NRestarts
sudo nginx -t; sudo apachectl configtest / httpd -t; sudo sshd -t; sudo haproxy -c -f ...; sudo named-checkconf   # 設定檔語法
sudo systemd-analyze verify /etc/systemd/system/myapp.service
# 啟動失敗常見：
#   權限（User= 沒權限讀設定 / 寫目錄；🔵 SELinux 標籤）→ ls -lZ；sudo -u myapp cat file
#   埠被佔 → ss -ltnp | grep :PORT
#   環境變數 / 路徑不同於手動執行 → systemctl show -p Environment；ExecStart 用絕對路徑
#   相依順序 → After=network-online.target + Wants=
#   資源限制 → LimitNOFILE；systemctl status 顯示 "Too many open files"
#   StartLimitHit → systemctl reset-failed 後再 start
# 逐步除錯：sudo -u myapp strace -f -o /tmp/trace.log /opt/myapp/bin/app；ltrace；gdb -p
# 慢：perf top、py-spy / jstack / pprof（依語言）、資料庫慢查詢日誌
# 🔵 SELinux 判斷法：setenforce 0 後正常 → 是 SELinux；用 sealert 找建議，不要就此關閉
# 🟠 AppArmor 判斷法：aa-complain profile 後正常 → 用 aa-logprof 補規則
```

**注意**：`setenforce 0` / `aa-complain` 只是判斷用，確認是 MAC 擋住後要補政策或標籤，不能就這樣留在寬鬆模式上線。

---

## 8. 核心當機分析（kdump）

**名詞與關係**：kdump 不是一個程式，而是一整套機制；名詞對應到機制的各個環節：

```
正常核心 ── panic ──▶ kexec 直接跳到預留記憶體中的「捕捉核心」（不經韌體重開）──▶ 把主核心的記憶體映像寫成 vmcore ──▶ 重開機
   ▲                                                                                        │
   crashkernel=<size> 開機時預留這塊記憶體（改完要重開才生效）                               /var/crash/<date>/vmcore
                                                                                            ▼
                                                    crash 工具 + 該核心版本的 debug symbol（vmlinux）──▶ bt / log / ps 讀出當時狀態
kdump = 上面整套機制；🟠 kdump-tools / kdump-config、🔵 kexec-tools + kdump-utils / kdumpctl 是各自的管理腳本
sysrq-trigger 寫入 c 是人為觸發 panic 來測試
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **kdump** | 核心當機時自動保存記憶體映像的機制 | 由 kexec、crashkernel、捕捉核心、vmcore 組成 |
| **kexec** | 從執行中的核心直接載入並跳到另一個核心，不經過韌體 | kdump 用它在 panic 時切到捕捉核心；套件 `kexec-tools` |
| **crashkernel=** | 核心參數，開機時預留一塊記憶體給捕捉核心 | 沒有它 kdump 服務起不來；改了要重開機 |
| **vmcore** | 保存下來的主核心記憶體映像（`/var/crash/<date>/`） | 磁碟不夠會靜默失敗；分析靠 crash 工具 |
| **crash 工具** | 讀 vmcore 的互動式分析器（`bt` 呼叫堆疊、`log` 核心日誌、`ps` 程序） | 必須配同版核心的 debug symbol |
| **debug symbol** | 帶符號的核心映像（🟠 `-dbgsym` 來自 ddebs repo、🔵 `kernel-debuginfo`） | 版本要與 vmcore 的核心完全一致 |
| **kdump-config / kdumpctl** | 🟠 / 🔵 管理 kdump 的指令（show / test / status） | `test` 會真的觸發當機 |
| **sysrq** | 核心的魔術鍵；對 `/proc/sysrq-trigger` 寫 `c` 即人為 panic | 用來驗證 kdump 設定 |
| **grubby** | 🔵 改核心參數的工具（§3.2） | 用來加 `crashkernel=` |

**為什麼**：核心 panic 之後畫面上的訊息往往來不及看就重開，沒有 kdump 就永遠只能猜是驅動、硬體還是核心 bug；kdump 在 panic 時用預留記憶體啟動第二個小核心，把當時的記憶體映像存下來，是回報核心問題給發行版或硬體廠商時的必要證據。

Ubuntu 用 `linux-crashdump` 元套件與 `kdump-tools` 腳本，安裝時互動式詢問並自動加 `crashkernel=`；Fedora 沿用 RHEL 的 `kexec-tools` / `kdumpctl`，44 起把服務拆進 `kdump-utils`，核心參數要自己加。

**怎麼做**：

```bash
# 🟠 Ubuntu
sudo apt install -y linux-crashdump          # 會問是否啟用 kdump-tools；核心參數自動加 crashkernel=
cat /etc/default/kdump-tools; kdump-config show
sudo kdump-config test
# 🔵 Fedora（實測 44：kdumpctl 與 kdump.service 已拆到 kdump-utils 套件）
sudo dnf install -y kexec-tools kdump-utils
sudo grubby --update-kernel=ALL --args="crashkernel=auto"   # 或 kdumpctl estimate 建議值
sudo systemctl enable --now kdump
sudo kdumpctl status; sudo kdumpctl test
# 兩者：測試（⚠️ 會當機重開）
echo 1 | sudo tee /proc/sys/kernel/sysrq; echo c | sudo tee /proc/sysrq-trigger
# 分析：vmcore 在 /var/crash/<date>/；用 crash 工具 + 對應 debug symbol
# 🟠 apt install crash linux-image-$(uname -r)-dbgsym（需 ddebs repo）；🔵 dnf install crash kernel-debuginfo（dnf debuginfo-install kernel）
sudo crash /usr/lib/debug/lib/modules/$(uname -r)/vmlinux /var/crash/*/vmcore
#   crash> bt; log; ps; sys
```

**注意**：`crashkernel=` 需要重開機才生效，且會永久預留一塊記憶體（通常 200~500 MB）；`/var/crash` 要有足夠空間放 vmcore，磁碟滿的機器 kdump 會靜默失敗。

---

## 9. 硬體診斷

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **MCE** | Machine Check Exception：CPU 偵測到的硬體錯誤（記憶體、快取、匯流排） | 記在 dmesg / journal；持續出現代表硬體在壞 |
| **EDAC / rasdaemon / ras-mc-ctl** | EDAC 是核心回報記憶體 ECC 錯誤的子系統；rasdaemon 把 MCE / EDAC 事件記進資料庫；`ras-mc-ctl` 查詢 | 可糾正錯誤（CE）持續累積是換記憶體的前兆 |
| **SMART / nvme smart-log** | SATA 磁碟 / NVMe 的自我監測資料 | Reallocated / Pending / Media errors 非零就換 |
| **IPMI / BMC / ipmitool** | BMC 是伺服器的獨立管理控制器，IPMI 是與它溝通的協定，`ipmitool` 是工具 | SEL（系統事件記錄）與感測器由 BMC 保存，OS 當掉也還在 |
| **dmidecode / lshw / lspci** | 讀 SMBIOS 表 / 列硬體 / 列 PCI 裝置與綁定的驅動 | 確認型號、插槽、驅動是否正確載入 |
| **lm-sensors** | 讀主機板溫度、電壓、風扇的工具（🟠 `lm-sensors`、🔵 `lm_sensors`） | 過熱降頻造成的「莫名變慢」由此看 |
| **memtester / memtest86+** | 線上（OS 內）/ 離線（開機時）記憶體測試 | 線上測不到核心佔用的部分，可疑時要離線 |
| **fwupd / LVFS** | 韌體更新 daemon / 廠商上傳韌體的公共服務 | 韌體 bug 造成的間歇問題靠更新解 |

**為什麼**：間歇性當機、隨機的資料錯誤、莫名的效能下降，若軟體層查不到原因，常是記憶體位元錯誤、磁碟壞軌、過熱降頻或韌體 bug；這些在 `journalctl` 只留下 MCE / EDAC 一行訊息，需要專門工具讀硬體計數器與 SMART。

**怎麼做**：

```bash
sudo dmidecode -t system -t memory | head -40      # 型號、記憶體插槽
sudo lshw -short; lspci -nnk; lsusb; sensors     # lm-sensors（🟠 lm-sensors；🔵 lm_sensors）
sudo journalctl -k -g "mce|Machine Check|EDAC"   # 記憶體 / CPU 錯誤
sudo rasdaemon; ras-mc-ctl --errors              # EDAC 記錄（兩者 rasdaemon 套件）
sudo memtester 4G 1                              # 線上記憶體測試；離線用 memtest86+（🟠 預設在 GRUB 選單）
sudo smartctl -a /dev/sda; sudo nvme smart-log /dev/nvme0
sudo ipmitool sel list; ipmitool sensor          # 伺服器 BMC
sudo fwupdmgr get-devices; sudo fwupdmgr update  # 韌體更新（LVFS；兩者 fwupd）
```

**注意**：`memtester` 只能測到它拿得到的記憶體，通過不代表沒問題，可疑時還是要離線跑 memtest86+；SMART 的 `Reallocated` 或 `Pending` 一旦非零就該準備換碟。

---

## 10. 收集診斷資訊給支援 / 事後分析

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **sos / sosreport** | Red Hat 起源的診斷打包工具，收集設定、日誌、硬體、指令輸出成一個 tar.xz | 🟠 套件名沿用舊名 `sosreport`，兩者指令都是 `sos report` |
| **--batch / --clean** | 不互動直接跑 / 遮蔽主機名、IP、密碼等敏感資訊 | 交給外部前用 `--clean`，但仍要自己檢查 |
| **apport / ubuntu-bug** | 🟠 桌面 crash 攔截器（寫 `/var/crash/*.crash`）/ 向 Launchpad 回報的指令 | 只管程式當掉，不做全系統打包 |
| **abrt** | 🔵 對應的 crash 攔截與回報工具 | 紀錄在 journal 的 `abrt` 標籤與 `abrt-cli list` |

**為什麼**：向廠商或社群求助時，「系統很慢」這種描述沒人能幫；sos 一次打包設定檔、日誌、硬體資訊與指令輸出，雙方都省去來回索取資料的時間，也是事後根因分析的完整快照。

sos 原是 Red Hat 的支援工具（`sosreport`），Fedora Server 預設帶著；Ubuntu 後來也採用，套件名沿用舊名 `sosreport`，桌面 crash 則各自用 apport 與 abrt。

**怎麼做**：

```bash
# 🟠 sosreport（Ubuntu 也用 sos；apport 收 crash）
sudo apt install -y sosreport && sudo sos report --batch          # 產生 /tmp/sosreport-*.tar.xz
# 🔵 sos（預設 Server 有）
sudo dnf install -y sos && sudo sos report --batch
# 快速版
sudo journalctl -b -p warning --no-pager > /tmp/journal.txt
sudo dmesg -T > /tmp/dmesg.txt; systemctl --failed > /tmp/failed.txt
hostnamectl; lsblk -f; df -h; free -h; ip -br a
# 🟠 桌面 crash：/var/crash/*.crash（apport）；ubuntu-bug <pkg>
# 🔵 桌面 crash：abrt（journalctl -t abrt）；abrt-cli list
```

**注意**：sos 打包內容含主機名、IP、設定檔，交給外部前先檢查是否有密碼或金鑰（`sos report --clean` 可做部分遮蔽）。

---

## 11. 本章差異總結

監控工具的差異主要來自套件庫政策（Ubuntu 沿用 Debian 命名與較保守的版本、Grafana 需官方 repo；Fedora 更新快但 Alertmanager 未收錄）；故障排除的差異則源自開機鏈設計：Ubuntu 有 recovery mode 與上游 GRUB，Fedora 沿用 RHEL 的 dracut / grubby / kdumpctl 工具鏈與 SELinux 重標記需求。

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| node_exporter 套件 / 服務 | `prometheus-node-exporter` / `prometheus-node-exporter` | `node-exporter` / `prometheus-node-exporter`（同名 unit） |
| Prometheus 套件版本 | 2.45（套件庫） | 3.13（套件庫） |
| Grafana | 官方 apt repo | 套件庫有 10.2（或官方 repo） |
| Alertmanager | `prometheus-alertmanager` | 官方二進位 |
| Netdata | 套件庫 1.43 | 官方腳本 |
| GRUB 選單 | 預設隱藏（Shift / Esc） | 預設顯示 5 秒 |
| 內建救援 | recovery mode 選單 | 無（用 rd.break / rescue.target） |
| 跳過 init | `init=/bin/bash` | `rd.break` + `chroot /sysroot` + `/.autorelabel` |
| Live 救援 | Try Ubuntu + 手動 chroot | 安裝媒體的 Rescue 模式自動掛載 |
| 重裝 GRUB（UEFI） | `grub-install --target=x86_64-efi ...` + `update-grub` | `dnf reinstall grub2-efi-x64 shim-x64` + `grub2-mkconfig`（**不要** `grub2-install`） |
| initramfs 重建 | `update-initramfs -u -k all` | `dracut -f --regenerate-all` |
| kdump | `linux-crashdump` / `kdump-tools`、`kdump-config` | `kexec-tools` + `kdump-utils`、`kdumpctl` |
| debug symbols | ddebs repo（`-dbgsym`） | `dnf debuginfo-install` |
| crash 收集 | apport、`ubuntu-bug` | abrt |
| 診斷打包 | `sosreport`（sos） | `sos` |
| 感測器套件 | `lm-sensors` | `lm_sensors` |
| MAC 除錯 | `aa-complain` / `aa-logprof` | `setenforce 0` 測試 / `sealert` |
