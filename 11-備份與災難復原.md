# 11 - 備份與災難復原

（備份頻率與方式由 [00b 章 §2.2](00b-設計階段的需求考量.md) 的 RPO / RTO 推導；本章假設你已經量化了這兩個數字。）

**本章解決什麼問題**：HA（10 章）擋得住硬體故障，卻擋不住誤刪、勒索軟體、升級失敗與整個機房出事——這些災難會被複製機制忠實地同步到每一個節點。真實情境：工程師在正式機打了 `rm -rf` 少一個字元、勒索軟體把 NAS 上的備份一起加密、資料庫升級失敗但快照早就滿了。沒有備份的後果是資料永久遺失；**有備份但沒測試過還原**的後果一樣——還原時才發現密碼找不到、repo 損毀、或漏備了某個目錄。本章的取捨原則是：先用去重加密的通用工具（restic）搭排程解決 90% 的需求，再依 RPO / RTO 加上快照、資料庫 PITR 與裸機映像；每一種備份都要對應一種明確的災難，並定期演練還原。

沒有測試過還原的備份等於沒有備份。本章涵蓋策略（3-2-1、RPO / RTO）、工具（rsync、restic、borg、快照）、資料庫備份、整機映像與裸機復原、災難復原計畫與演練。腳本 [`scripts/backup-restic.sh`](scripts/backup-restic.sh) 已在兩個發行版實測（含 LVM 快照、restic 備份 / 保留 / 還原）。

---

## 1. 策略

**名詞與關係**：本章的工具很多，但它們只分四類，每一類對應的災難不同，先分清楚才不會「有備份卻救不回來」：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **鏡像（mirror）** | 讓目標與來源保持一致的複本（rsync `--delete`、HA 的複製機制） | 只有「現在」這一份；誤刪與勒索會被同步過去，所以鏡像不算備份 |
| **快照（snapshot）** | 在同一顆碟上瞬間凍結的某個時間點狀態（LVM / Btrfs / ZFS / VM） | 保證一致、秒級回滾，但碟壞了一起消失；是備份的前置步驟 |
| **備份（backup）** | 存放在另一個地方、保留多個時間點、可獨立還原的資料副本（restic / borg） | 對付誤刪、勒索、硬體故障；靠快照取得一致性、靠異地與不可變對付災難 |
| **映像（image）** | 整顆磁碟或整台機器的複本，含分割區與開機環境（rear / Clonezilla / dd） | 主機報廢時先靠它把系統開起來，再用備份還原資料 |
| **勒索軟體** | 入侵後加密所有可寫入的資料（含掛載中的備份目標）再勒索贖金的惡意程式 | 3-2-1-1-0 裡「離線 / 不可變」那一份就是專門防它 |

**為什麼**：沒有策略的備份通常是「有一份 cron 跑 rsync 到隔壁機器」，等到勒索軟體同時加密兩台、或機房淹水時才發現這一份也沒了。策略先決定「要防哪些災難、能丟多少、能停多久」，工具才有選擇依據。

### 1.1 3-2-1-1-0 原則

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **3-2-1-1-0** | 「3 份、2 種媒體、1 份異地、1 份離線或不可變、0 還原錯誤」的備份守則 | 後三項分別在 §4（restic）、§9（雲端）、§10（演練）落實 |
| **媒體** | 存放資料的實體型態：磁碟、磁帶、雲端物件儲存 | 同一種媒體有共同弱點（同批韌體 bug、同一控制器），所以要兩種 |
| **異地（off-site）** | 放在另一個機房或雲端、與正本不共用電力與網路的副本 | 防火災、水災、整棟斷電；通常靠 rclone / restic 後端上雲（§9） |
| **immutable（不可變）** | 寫入後在保留期內誰都不能改或刪，連管理員也不行（S3 Object Lock、append-only repo） | 線上但防勒索；攻擊者拿到備份主機也刪不掉 |
| **air-gap（離線）** | 平常物理上不連線的副本：拔掉的外接碟、磁帶、離線保管的媒體 | 與 immutable 擇一或並用，都是第四個數字「1」 |
| **還原驗證** | 定期真的把備份還原出來比對，而不是只看備份任務回報成功 | 第五個數字「0」；沒驗證的備份可能早已損毀 |

**為什麼**：每一個數字對應一種災難——同一顆碟壞掉（3 份）、同一種媒體的共同弱點如韌體 bug（2 種媒體）、機房火災（1 份異地）、勒索軟體把線上備份一起加密（1 份離線 / 不可變）、備份檔案本身早已損毀（0 還原錯誤）。少一項就有一種災難防不了。

**怎麼做**：

- **3** 份資料（1 正本 + 2 備份）
- **2** 種不同媒體（磁碟 + 磁帶 / 雲端 / 另一台機器）
- **1** 份異地（另一機房或雲端）
- **1** 份離線或不可變（immutable / air-gap，防勒索軟體）
- **0** 還原錯誤（定期驗證）

### 1.2 RPO 與 RTO

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **RPO** | Recovery Point Objective：災難發生時最多能接受「丟掉多久」的資料 | 由備份頻率決定：每天備 = RPO 24 小時；要 RPO 分鐘級就得靠交易日誌歸檔或同步複製 |
| **RTO** | Recovery Time Objective：從災難到服務恢復最多能接受停多久 | 由還原方式決定：整機映像與自動化腳本快、憑記憶重建慢；要 RTO 分鐘級就得靠 HA（10 章） |
| **同步 / 非同步複製** | 同步 = 寫入等副本確認才成功（RPO ≈ 0）；非同步 = 先成功再送副本（可能丟最後幾筆） | 10 章的 HA 機制；複製只降 RPO，不能取代備份 |
| **增量備份** | 只備份上次以來變動的部分 | 讓高頻率備份（RPO 1 小時）在空間與時間上可行；§2 再細分 full / incremental / differential |

**為什麼**：「每天備份一次」對交易系統可能是災難（丟一整天訂單），對靜態網站又太浪費；RPO / RTO 是和業務談出來的數字，直接決定備份頻率、複製方式與要不要買備援硬體。沒有這兩個數字，備份預算與做法都沒有依據。

**怎麼做**：先訂目標，再反推技術。

| 指標 | 意義 | 決定的技術 |
|---|---|---|
| **RPO**（Recovery Point Objective） | 最多能接受丟失多少時間的資料 | 備份頻率、複製方式（同步 / 非同步） |
| **RTO**（Recovery Time Objective） | 最多能接受停多久 | 還原方式（映像 vs 重建）、自動化程度、HA |

範例：RPO 1 小時 + RTO 4 小時 → 每小時增量備份 + 已測試的還原腳本 + 備援硬體待命。

### 1.3 備份什麼

**名詞與關係**：清單裡幾個容易漏掉的名詞：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **套件清單** | 系統上手動安裝了哪些套件的文字清單（`apt-mark showmanual` / `dnf repoquery --userinstalled`） | 重建時先還原清單再還原 `/etc`，設定才有對應的程式 |
| **邏輯傾印（dump）** | 把資料庫內容輸出成 SQL 或工具自訂格式的檔案 | 資料庫不能直接 cp，要 dump 或快照（§7） |
| **WAL / binlog** | PostgreSQL / MariaDB 的交易日誌，記錄每一筆變更 | 配合物理備份可還原到任意時間點（PITR，§7） |
| **TLS 私鑰 / SSH host key** | 服務憑證的私鑰 / 主機的 SSH 身分金鑰 | 漏備會讓重建後憑證失效、所有客戶端跳 host key 警告 |
| **LUKS header** | 全碟加密分割區開頭的中繼資料，含金鑰槽 | header 壞了整顆碟等於亂數；備份方式見 §8.2 |
| **`sfdisk -d` / `vgcfgbackup`** | 匯出分割表 / 匯出 LVM 卷群組中繼資料的指令 | 裸機重建時照著重畫分割區與 LV（§8.2） |
| **ESP** | EFI System Partition，UEFI 的開機分割區（01 章） | 檔案級備份通常不含，整機映像（§8）才會涵蓋 |
| **偽檔案系統** | `/proc`、`/sys`、`/dev`、`/run` 這類由核心即時產生、不在磁碟上的目錄 | 備份它們沒意義且會出錯，一律排除 |
| **容器映像層 / volume** | 映像層是可從 Dockerfile 重建的唯讀層；volume 才是容器的持久資料 | 備 Dockerfile 與 volume，不備 `overlay2` |

**為什麼**：最常見的還原失敗不是工具問題，而是漏備——備了資料庫卻沒備 TLS 私鑰、備了 `/home` 卻沒備 `/etc` 與套件清單，重建時要花好幾小時回憶當初裝了什麼。反過來備太多（`/proc`、快取、容器映像層）會讓備份又大又慢，還可能備到會變動的偽檔案系統而出錯。

**怎麼做**：依下表分類，小而關鍵的（設定、清單、秘密、佈局）每天備，大的依變動量。

| 類別 | 路徑 / 內容 | 備註 |
|---|---|---|
| 系統設定 | `/etc`、`/usr/local`、`/opt`、`/root`、crontab、systemd 單元 | 小，每日 |
| 套件清單 | 🟠 `dpkg --get-selections`、`apt-mark showmanual`；🔵 `dnf repoquery --userinstalled`、`rpm -qa` | 重建時用 |
| 使用者資料 | `/home`、`/srv`、`/var/www`、應用資料目錄 | 依變動量 |
| 資料庫 | 邏輯傾印 + WAL / binlog；或快照 | 一致性！ |
| 日誌 | `/var/log` | 稽核需求才留長期 |
| 秘密 | TLS 私鑰、SSH host key、LUKS header、restic 密碼 | **另外**加密保存，且不要只放在同一份備份裡 |
| 分割區 / LVM 佈局 | `sfdisk -d`、`vgcfgbackup`、`lsblk -f` | 裸機復原時重建用 |
| 開機環境 | `/boot`、ESP | 整機映像才含 |

**不要備份**：`/proc`、`/sys`、`/dev`、`/run`、`/tmp`、快取（`~/.cache`、`/var/cache/apt`、`/var/cache/libdnf5`）、容器映像層（`/var/lib/docker/overlay2`，改備份 Dockerfile 與 volume）。

**注意**：「秘密」那一列是循環依賴的陷阱——restic 密碼若只存在被 restic 加密的備份裡，主機報廢後就永遠打不開；LUKS header 若只在被 LUKS 加密的碟上也一樣。這些要另外放到密碼管理器或離線媒體。

---

## 2. 工具比較

**名詞與關係**：表格的欄位就是挑工具的幾個維度：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **full / incremental / differential** | 全量 = 整份備；增量 = 只備上次「任一備份」以來的變動；差異 = 只備上次「全量」以來的變動 | 增量省空間但還原要串一整條鏈；差異還原只需全量 + 最後一份 |
| **去重（檔案級 / 區塊級）** | 相同內容只存一份：檔案級以整個檔案比對（hardlink）；區塊級把檔案切塊比對，改一點只存那一塊 | 區塊級去重讓「每次都是全量」卻只佔增量空間，是 restic / borg 的核心 |
| **hardlink 快照** | rsnapshot 的做法：沒變的檔案用 hardlink 指向上一份，看起來每天都是完整目錄 | 檔案級去重的實作；可直接 `ls` 瀏覽，不需要專用工具還原 |
| **repository（repo）** | restic / borg 存放去重區塊、索引與快照清單的目錄或遠端位置 | 一個 repo 可裝多台主機多個時間點；密碼是它唯一的鑰匙 |
| **加密（at rest）** | 資料在存入 repo 前就在來源端加密，目標端看不到內容 | 讓不受信任的雲端或別人的 NAS 也能當異地 |
| **後端（remote）** | repo 實際存放的位置類型：本機、SFTP、S3、rclone 支援的任何雲端 | restic 後端最多；borg 只能 SSH；影響能否做異地與不可變 |
| **整機映像** | 含分割區、開機載入器與所有資料的整顆碟複本 | 上表 Clonezilla / dd / rear 那列；唯一能應付「主機報廢」的類型（§8） |
| **duplicity** | 以 tar 增量檔 + GPG 加密的舊式工具 | 沒有去重、增量鏈長還原慢，已被 restic / borg 取代 |
| **Bacula / Bareos / Amanda、Velero** | 企業級主從式（C/S）備份系統，集中管理多主機與磁帶 / Velero 是 Kubernetes 專用備份工具 | 多主機集中管理才需要；單機或少量主機用 restic 即可 |

**為什麼**：每種工具對應不同災難：rsync 只是鏡像（誤刪會同步到目標，等於沒備份）、快照只在同一顆碟上（碟壞了一起消失）、去重備份工具才能保留多個時間點又不爆空間、整機映像才能應付主機報廢。選錯工具最常見的後果是「有備份，但備份的是已經被刪掉之後的狀態」。

**怎麼做**：一般伺服器的組合是 **restic（多版本、加密、異地）+ 快照（一致性）+ 資料庫邏輯備份 + rear（裸機）**；下表依需求挑選。

| 工具 | 類型 | 去重 | 加密 | 增量 | 遠端 | 適合 |
|---|---|---|---|---|---|---|
| **rsync** | 檔案同步 | ✗ | ✗（靠 SSH） | ✓（差異傳輸） | SSH | 簡單鏡像、搬資料 |
| **rsnapshot** | rsync + hardlink 快照 | 檔案級 | ✗ | ✓ | SSH | 傳統每日快照，可直接瀏覽 |
| **restic** | 去重備份 | 區塊級 | ✓ | ✓ | 本機 / SFTP / S3 / B2 / rclone | **推薦通用方案** |
| **borgbackup** | 去重備份 | 區塊級 | ✓ | ✓ | SSH（需遠端有 borg） | 效能極佳、壓縮好；只能 SSH |
| **duplicity** | tar + GPG | ✗ | ✓ | ✓ | 多種 | 舊方案，還原慢 |
| **LVM / Btrfs / ZFS 快照** | 區塊 / FS 快照 | — | — | — | 本機（ZFS/Btrfs send 可遠端） | 一致性、秒級回滾；**不是備份**（同一顆碟） |
| **Clonezilla / dd / Relax-and-Recover (rear)** | 整機映像 | — | — | ✗ | — | 裸機復原 |
| **Timeshift** | 系統快照（rsync / Btrfs） | — | — | ✓ | 本機 | 桌面系統升級前 |
| **Bacula / Bareos / Amanda** | 企業級 C/S | ✓ | ✓ | ✓ | 磁帶 / 磁碟 | 多主機集中管理 |
| **Velero** | Kubernetes | — | — | — | S3 | K8s 資源 + PV |

套件（兩者官方庫皆有，實測版本）：restic 🟠 0.16 / 🔵 0.19、borgbackup 🟠 1.2 / 🔵 1.4、rsnapshot、duplicity、rear、timeshift。Fedora 滾動較快所以版本較新；Ubuntu LTS 凍結版本，restic 可用 `self-update` 補齊。

---

## 3. rsync

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **rsync** | 只傳輸差異部分的檔案同步工具，透過 SSH 在本機與遠端之間複製目錄 | 單一版本的鏡像；配 rsnapshot 或目標端快照才能保留歷史 |
| **`--delete`** | 來源刪掉的檔案在目標也刪掉，讓目標與來源完全一致 | 鏡像的必要選項，也是「誤刪同步過去」的來源 |
| **`-a` / `-H` / `-A` / `-X`** | `-a` 保留權限、時間、擁有者等基本屬性；`-H` 保留 hardlink；`-A` 保留 ACL；`-X` 保留擴充屬性（xattr） | 少了 `-X`，🔵 SELinux 標籤與 capabilities 都會遺失 |
| **`--numeric-ids`** | 以 UID / GID 數字而非名稱比對擁有者 | 兩台機器的使用者名稱對應不同 ID 時避免錯亂 |
| **`-x`** | 不跨越檔案系統邊界 | 備根目錄時自動跳過 `/proc`、`/sys` 與其他掛載點 |
| **hardlink** | 同一個 inode 的多個檔名，不佔額外空間 | `-H` 保留它；rsnapshot 用它做去重快照 |
| **xattr** | 檔案的擴充屬性，SELinux 標籤（`security.selinux`）就存在這裡 | 還原後政策可能已變，仍建議 `restorecon -R` |
| **rsnapshot** | 用 rsync + hardlink 做輪替快照（daily / weekly / monthly）的工具 | 讓 rsync 有多版本；設定檔欄位必須用 Tab |

**為什麼**：rsync 是最簡單、到處都有的工具，適合「搬資料」與「維持一份可直接瀏覽的鏡像」（例如同步到 NAS 之後 NAS 自己再做快照）。但它只有一個版本——`--delete` 會把來源的誤刪忠實同步到目標，所以單靠 rsync 防不了誤刪與勒索，要配 rsnapshot 或目標端快照才算備份。

**怎麼做**：`-aHAX` 保留所有屬性（hardlink、ACL、xattr），排除清單避免備到偽檔案系統與快取；備份整個根目錄時加 `-x` 不跨檔案系統：

```bash
# 本機 → 遠端鏡像（-a 保留屬性、-H hardlink、-A ACL、-X xattr、--delete 讓目標與來源一致）
rsync -aHAXv --delete --numeric-ids --exclude-from=/etc/rsync-exclude.txt /srv/ backup@nas:/backup/$(hostname)/srv/
# 常用選項：-z 壓縮（慢網路）、--partial --append-verify（大檔續傳）、--bwlimit=20M、-n 模擬、--info=progress2
# 系統級排除
cat > /etc/rsync-exclude.txt <<'EOF'
/dev/*
/proc/*
/sys/*
/run/*
/tmp/*
/var/tmp/*
/var/cache/*
/mnt/*
/media/*
/lost+found
*/.cache/*
EOF
# 整個根目錄（做「窮人的映像」）
sudo rsync -aHAXx --delete --numeric-ids --exclude-from=/etc/rsync-exclude.txt / /mnt/backupdisk/root/
# 🔵 SELinux：加 -X 保留 security.selinux xattr，還原後仍建議 restorecon -R
# rsnapshot：/etc/rsnapshot.conf（⚠️ 欄位以 Tab 分隔）→ rsnapshot daily；配 systemd timer
```

**注意**：來源路徑結尾的 `/` 有無意義不同（`/srv/` 是內容、`/srv` 是目錄本身），第一次跑先加 `-n` 模擬看清楚，尤其搭配 `--delete` 時。🔵 Fedora 還原後即使保留了 xattr，仍建議 `restorecon -R`，因為政策版本可能已變。

---

## 4. restic（推薦；已實測）

**名詞與關係**：restic 的運作可以用一條流程看完：

```
 來源目錄 ──backup──▶ 切塊、去重、加密 ──▶ repository（本機 / SFTP / S3 / rclone 後端）
                                              │ 每次 backup 產生一個 snapshot（時間點 + tag）
                                              ▼
   forget（依 --keep-* 決定哪些 snapshot 該忘）→ prune（真正刪除沒人引用的區塊）
   check（結構）/ check --read-data-subset（抽樣讀資料）；restore / mount / dump（取回資料）
   密碼檔 = 解開 repo 的唯一鑰匙；append-only / Object Lock = 讓 forget --prune 在遠端無法刪
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **restic** | 單一二進位、區塊級去重、加密、多後端的備份工具 | 本章推薦的通用方案；腳本 `backup-restic.sh` 就是它的包裝 |
| **repository / RESTIC_REPOSITORY** | 存放所有加密區塊、索引與 snapshot 的位置；用環境變數或 `-r` 指定 | `init` 建立一次，之後所有指令都對它操作 |
| **密碼檔 / RESTIC_PASSWORD_FILE** | 解開 repo 的主密碼所在檔案 | 遺失等於全毀；不能只存在 repo 本身或同一份備份裡（§1.3） |
| **後端（backend）** | repo 的儲存類型：本機路徑、`sftp:`、`s3:`、`rclone:` | 決定能否異地與不可變；`rclone:` 讓任何雲端都能用 |
| **snapshot** | 一次 `backup` 產生的時間點紀錄，含來源路徑、主機、tag | 還原、diff、ls 都以 snapshot id 或 `latest` 為對象 |
| **tag** | 附在 snapshot 上的標籤（`--tag daily`） | 讓 forget 可依標籤分別套用保留策略 |
| **forget / prune** | forget 依 `--keep-daily 7` 等策略把過期 snapshot 從清單移除；prune 才真正刪掉沒人引用的區塊釋放空間 | 兩步分開是為了安全；`--prune` 合併執行 |
| **check / --read-data-subset** | check 驗證 repo 結構完整；`--read-data-subset=10%` 抽樣實際讀回並驗證區塊 | 只有後者能抓到磁碟靜默損毀 |
| **restore / mount / dump** | restore 還原到目錄；mount 用 FUSE 把所有 snapshot 掛成可瀏覽的目錄；dump 把單檔輸出到 stdout | 三種取回資料的方式，依需要的量選 |
| **unlock** | 移除上次中斷留下的 repo 鎖 | 只在確定沒有其他 restic 在跑時用 |
| **rest-server --append-only** | restic 官方的 HTTP 後端伺服器，append-only 模式下客戶端只能新增不能刪 | 備份主機被入侵也刪不掉舊備份；與 S3 Object Lock 同一目的 |
| **systemd timer / Persistent** | 用 `.timer` 單元取代 cron；`Persistent=true` 讓錯過的排程在開機後補跑 | 排程 `backup-restic.sh` 的方式；日誌進 journal |

**為什麼**：restic 一次解決備份最痛的幾件事：區塊級去重（每天全量備份只佔增量空間）、內建加密（丟到雲端或別人的 NAS 也不怕外洩）、多後端（本機、SFTP、S3、任何 rclone 支援的服務）、可保留多個時間點並依策略自動清理。這正是對付誤刪與勒索軟體所需的「多版本 + 異地 + 可不可變」組合；單一靜態二進位檔也讓救援環境還原很方便。

**怎麼做**：密碼檔是整個 repo 的唯一鑰匙，先產生並另存；接著用環境變數指定 repo 位置就能 init、backup、restore：

```bash
sudo apt install -y restic    # 🟠      sudo dnf install -y restic    # 🔵
sudo restic self-update       # 🟠 套件版較舊時可更新（🔵 通常已新）
# 密碼檔
openssl rand -base64 32 | sudo tee /root/.restic-pw && sudo chmod 600 /root/.restic-pw
# ⚠️ 密碼遺失 = 備份全毀；另存到密碼管理器

# Repository 位置（環境變數或 -r）
export RESTIC_REPOSITORY=/backup/restic                          # 本機 / 外接碟 / NFS
export RESTIC_REPOSITORY=sftp:backup@nas:/srv/restic             # SFTP（需 SSH 金鑰）
export RESTIC_REPOSITORY=s3:https://s3.ap-northeast-1.amazonaws.com/mybucket   # S3（+ AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY）
export RESTIC_REPOSITORY=rclone:gdrive:backup                    # 任何 rclone 支援的後端
export RESTIC_PASSWORD_FILE=/root/.restic-pw

restic init                                                      # 第一次
restic backup /etc /home /srv --exclude-caches --exclude '*/node_modules' --one-file-system --tag daily
restic snapshots; restic snapshots --latest 3
restic ls latest /etc/ssh; restic diff <id1> <id2>
restic find "*.conf" --long
# 還原
restic restore latest --target /tmp/restore --include /etc/nginx
restic restore <snapshot-id> --target /                          # ⚠️ 覆蓋
restic mount /mnt/restic && ls /mnt/restic/snapshots/latest/     # FUSE 瀏覽（需 fuse）；Ctrl-C 卸載
restic dump latest /etc/hosts > hosts.bak                        # 單檔直接輸出
# 保留策略與清理
restic forget --keep-daily 7 --keep-weekly 4 --keep-monthly 6 --keep-yearly 2 --prune
restic check                                                     # 結構檢查
restic check --read-data-subset=10%                              # 抽樣讀資料（每週）
restic stats --mode raw-data
restic unlock                                                    # 上次中斷留下鎖
# 不可變 / 防勒索：S3 Object Lock、或 repo 在只允許 append 的 SFTP（restic 的 rest-server --append-only）
```

**注意**：`restic check` 只驗證 repo 結構，磁碟靜默損毀要靠 `--read-data-subset` 抽樣讀實際資料才抓得到，排程每週做。備份主機本身若被入侵，攻擊者拿到密碼檔就能 `forget --prune` 刪光備份，所以遠端要用 append-only（rest-server）或 Object Lock，讓刪除只能從另一個受控端進行。

排程用 systemd timer 而不是 cron，因為 `Persistent=true` 能在關機錯過時補跑、日誌也統一進 journal。排程（`scripts/examples/restic-backup.service` + `.timer`）：

```bash
sudo cp scripts/backup-restic.sh /usr/local/bin/ && sudo chmod 700 /usr/local/bin/backup-restic.sh
sudo cp scripts/examples/restic-backup.{service,timer} /etc/systemd/system/
sudo systemctl daemon-reload && sudo systemctl enable --now restic-backup.timer
sudo systemctl start restic-backup.service && journalctl -u restic-backup -o cat
```

`backup-restic.sh` 重點：初始化 repo → （可選）建 LVM 快照掛載為唯讀 → 有裝 PostgreSQL / MariaDB 就先邏輯傾印 → `restic backup` → `forget --prune` → `check --read-data-subset=5%`。用 `--snapshot vg0/lv_data:/data` 啟用快照模式。

---

## 5. borgbackup

**名詞與關係**：borg 的概念與 restic 一一對應，只是名稱不同：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **borg（borgbackup）** | 區塊級去重、加密、壓縮的備份工具，遠端只支援 SSH | 與 restic 二擇一；效能與壓縮較好，後端較少 |
| **repository / BORG_REPO** | 存放去重區塊與封存清單的位置（`ssh://user@host/./path`） | 遠端主機也要裝 borg，因為是遠端的 `borg serve` 在寫 |
| **archive（封存，`::name`）** | 一次 `create` 產生的時間點，相當於 restic 的 snapshot | 名稱含 `{hostname}-{now}` 才不會撞名 |
| **repokey / keyfile** | 加密金鑰的存放模式：repokey 存在 repo 內（靠 passphrase 保護）；keyfile 存在本機 `~/.config/borg/keys` | repokey 方便，但 repo 壞掉金鑰一起丟，要 `key export` 另存 |
| **passphrase / BORG_PASSCOMMAND** | 解鎖金鑰的密碼；`BORG_PASSCOMMAND` 指定讀取密碼的指令 | 排程時用它免互動輸入 |
| **compression zstd,3** | 存入前壓縮，zstd 等級 3 在速度與壓縮率間取平衡 | restic 舊版無壓縮，是 borg 的優勢之一 |
| **prune / compact** | prune 依保留策略標記過期 archive；compact 才真正回收空間 | 對應 restic 的 forget / prune |
| **`borg serve --append-only`** | 在遠端 `authorized_keys` 限制客戶端只能新增不能刪 | borg 的不可變做法，對應 restic 的 rest-server append-only |

**為什麼**：borg 與 restic 同為去重加密備份，壓縮率與大檔效能通常更好、`mount` 瀏覽更快，適合資料量大且備份目標是自己控制的 SSH 主機的情況。限制是遠端必須也裝 borg、不支援 S3 等物件儲存，所以要做雲端異地時 restic 較方便；兩者擇一即可，不必都用。

**怎麼做**：repo 用 `repokey` 模式把金鑰存在 repo 內（仍需 passphrase），封存名稱帶主機名與時間戳：

```bash
sudo apt install -y borgbackup    # 🟠      sudo dnf install -y borgbackup    # 🔵（兩端都要有 borg）
export BORG_REPO=ssh://backup@nas/./repo/$(hostname)
export BORG_PASSPHRASE_FD=... 或 BORG_PASSCOMMAND="cat /root/.borg-pw"
borg init --encryption=repokey-blake2
borg create --stats --progress --compression zstd,3 --exclude-caches --exclude '*/node_modules' \
  ::'{hostname}-{now:%Y-%m-%d_%H%M}' /etc /home /srv
borg list; borg info
borg extract ::hostname-2026-08-28_0200 etc/nginx       # 還原到目前目錄
borg mount ::hostname-2026-08-28_0200 /mnt/borg
borg prune --keep-daily 7 --keep-weekly 4 --keep-monthly 6 && borg compact
borg check --verify-data
# 遠端 append-only 防勒索：authorized_keys 加 command="borg serve --append-only --restrict-to-path /srv/repo"
```

**注意**：🟠 1.2 與 🔵 1.4 的 repo 格式相容，但跨主機還原時 borg 版本不要比建 repo 時舊。`repokey` 模式的金鑰在 repo 裡，仍要 `borg key export` 另存一份，否則 repo 損毀連金鑰一起丟。

---

## 6. 快照（一致性與快速回滾）

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **快照** | 把某個卷或子卷在某一瞬間的狀態凍結成可讀的複本，之後原卷繼續變動 | LVM、Btrfs、ZFS、VM 各有自己的實作，本節依序介紹 |
| **一致性** | 備份出來的資料屬於同一個時間點，沒有「一半新一半舊」 | 快照的第一個用途：備份工具讀快照而不是讀活著的目錄 |
| **回滾（rollback）** | 把卷還原到快照當下的狀態 | 快照的第二個用途：升級失敗幾秒內復原 |
| **copy-on-write（COW）** | 快照不複製資料，只在原卷被改寫時才把舊區塊留一份給快照 | 所以建快照是秒級，但寫入越多快照佔越大（LVM 甚至會失效） |

**為什麼**：直接備份運作中的資料目錄會拿到「一半新一半舊」的檔案——資料庫寫到一半、應用剛好在改設定，還原回去可能根本打不開。快照在瞬間凍結整個卷的狀態，備份工具從快照讀就能保證一致；另一個用途是升級前拍一張，失敗時幾秒內回滾。但快照與正本在同一顆碟上，**碟壞了一起消失**，它是備份的前置步驟，不是備份本身。

### 6.1 LVM 快照（兩者；伺服器常用）

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **VG / LV** | Volume Group 是一群實體磁碟合成的儲存池；Logical Volume 是從池裡切出的卷（15 章） | 快照是一個特殊的 LV，佔用 VG 的剩餘空間（`VFree`） |
| **`lvcreate -s`** | 對某個 LV 建立 COW 快照 LV，`-L` 指定保留多少空間給被改寫的舊區塊 | 寫入量超過 `-L` 快照就失效，所以只能短期使用 |
| **`nouuid`** | xfs 掛載選項，忽略 UUID 重複的檢查 | 快照與原卷 UUID 相同，xfs 預設拒絕同時掛載 |
| **`lvconvert --merge`** | 把快照的內容合併回原 LV，等於回滾到快照時點 | 根目錄要重開機後才會合併；合併後快照消失 |
| **VFree** | `vgs` 顯示的 VG 剩餘空間 | 沒有 VFree 就建不了快照；安裝時要預留 |

**為什麼**：兩個發行版的伺服器安裝預設都是 LVM（🟠 `ubuntu-vg/ubuntu-lv`、🔵 `fedora/root`），不需要換檔案系統就能用；適合「備份前拍快照、備完刪掉」與「大版本升級前的保險」。取捨：快照是 copy-on-write，寫入量超過保留空間（範例 10G）快照就失效，所以不能長期留著。

**怎麼做**：建立 → 唯讀掛載 → 從快照備份 → 卸載並刪除。xfs 掛載快照要加 `nouuid`，否則核心會拒絕掛載 UUID 重複的檔案系統：

```bash
sudo lvcreate -s -L 10G -n lv_data_snap /dev/vg0/lv_data      # 建立
sudo mount -o ro,nouuid /dev/vg0/lv_data_snap /mnt/snap        # xfs 需 nouuid；ext4 不需
restic backup /mnt/snap ...                                    # 從一致的快照備份
sudo umount /mnt/snap && sudo lvremove -y /dev/vg0/lv_data_snap
# 升級前保護根目錄（🟠 ubuntu-vg/ubuntu-lv；🔵 fedora/root）：
sudo lvcreate -s -L 10G -n root_pre_upgrade /dev/ubuntu-vg/ubuntu-lv
# 失敗回滾：sudo lvconvert --merge /dev/ubuntu-vg/root_pre_upgrade && sudo reboot（實測 merge 後資料回到快照時點）
```

**注意**：`lvconvert --merge` 對根目錄要重開機才會真正合併；合併後快照消失、升級後的所有變更都回到快照時點，包含這段期間寫入的資料，所以升級前先確認沒有活躍寫入。VG 要預留快照空間，`vgs` 看 VFree。

### 6.2 Btrfs 快照（🔵 Fedora Workstation 預設）

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **subvolume（子卷）** | Btrfs 內可獨立掛載、獨立快照的目錄樹（🔵 預設有 `root`、`home`） | 快照的單位；回滾根目錄就是換掛哪個子卷 |
| **唯讀快照（`-r`）** | 建立後不能修改的快照 | `send` 只接受唯讀快照，因為傳輸中內容不能變 |
| **send / receive** | send 把快照序列化成資料流；receive 在另一端重建成子卷 | 讓 Btrfs 快照能透過 SSH 到遠端，成為真正的異地備份 |
| **`-p`（parent，增量）** | 只送與父快照之間的差異 | 父快照必須兩端都存在，否則 receive 失敗 |
| **snapper** | 管理快照生命週期的工具：自動建立、編號、清理、比對、undochange | dnf / apt 外掛在每次套件交易前後呼叫它 |
| **`python3-dnf-plugin-snapper`** | 讓 dnf 在交易前後自動呼叫 snapper 拍快照的外掛 | 升級壞了用 `snapper undochange` 或 rollback |
| **`rootflags=subvol=`** | 核心參數，指定開機時把哪個子卷當根目錄 | 手動回滾根目錄的方式之一 |

**為什麼**：Btrfs 快照是檔案系統原生功能，幾乎零成本、可以留很多份，且 `send / receive` 可以把快照增量送到遠端——這一點讓它不只是本機快照，而能當真正的異地備份。Fedora Workstation 預設 Btrfs，配 snapper 可在每次 dnf 交易前後自動快照，升級壞了直接 rollback。

**怎麼做**：🟠 Ubuntu 預設 ext4 沒有這功能，但若安裝時選了 Btrfs 同樣可用 snapper + apt hook。

```bash
sudo btrfs subvolume snapshot -r / /.snapshots/root-$(date +%F)
sudo btrfs subvolume snapshot -r /home /.snapshots/home-$(date +%F)
sudo btrfs send /.snapshots/home-2026-08-28 | ssh nas "btrfs receive /backup/"      # 異地
sudo btrfs send -p /.snapshots/home-2026-08-27 /.snapshots/home-2026-08-28 | ...   # 增量
# 回滾根目錄：把 fstab / 核心參數 rootflags=subvol= 指向快照，或用 snapper rollback
sudo dnf install -y snapper python3-dnf-plugin-snapper    # dnf 交易前後自動快照（🟠 apt 可用 snapper + apt hook）
sudo snapper -c root create-config /
sudo snapper -c root create --description "before upgrade"
sudo snapper -c root list; sudo snapper -c root undochange 5..6
```

**注意**：`send` 只能送唯讀快照（`-r`），增量 `-p` 的父快照必須在兩端都存在。快照多了會佔空間又不顯示在 `df`，要設 snapper 的清理策略。

### 6.3 ZFS（🟠）

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **pool / dataset** | pool（`tank`）是一群磁碟組成的儲存池；dataset（`tank/data`）是池內可獨立設定與快照的檔案系統 | 相當於 LVM 的 VG + Btrfs 的 subvolume 合而為一 |
| **`snapshot -r`** | 遞迴對 dataset 及其所有子 dataset 建快照 | 一次凍結整個 pool 的一致狀態 |
| **`send -R -I`** | `-R` 遞迴含所有子 dataset 與屬性；`-I` 送兩個快照之間的所有增量快照 | 對應 Btrfs 的 `send -p`，但一次能帶多個中間快照 |
| **`receive -F`** | 接收端先強制回滾到最新共同快照再套用 | 目標端若有人動過會被覆蓋，只能用於純備份目標 |
| **rollback** | 把 dataset 退回某個快照 | 預設只能退到最新快照，`-r` 會刪掉中間的 |
| **校驗和 / 自我修復** | 每個區塊帶校驗和，讀到損毀時從鏡像或 RAID-Z 的另一份自動修復 | Btrfs 也有校驗和，但 ZFS 的 RAID-Z 整合更成熟 |
| **sanoid / syncoid** | sanoid 依策略自動建與清快照；syncoid 用 send / receive 自動同步到遠端 | 把手動的 snapshot + send 排程化 |

**為什麼**：ZFS 提供與 Btrfs 類似的快照與 `send / receive`，加上校驗和與自我修復，資料完整性最強；🟠 Ubuntu 原生支援（🔵 Fedora 因授權問題只有第三方模組），適合儲存伺服器。

**怎麼做**：遞迴快照整個 pool，`send -R -I` 增量送到遠端；日常自動化交給 sanoid / syncoid：

```bash
sudo zfs snapshot -r tank@$(date +%F)
sudo zfs send -R -I tank@2026-08-27 tank@2026-08-28 | ssh nas zfs receive -F backup/tank
sudo zfs rollback tank/data@2026-08-28
# 自動化：sanoid / syncoid（apt install sanoid）
```

**注意**：`zfs rollback` 只能回到最新快照，要回更早的要加 `-r` 並會刪掉中間的快照；`receive -F` 會強制覆蓋目標端的變更，確認目標只當備份用。

### 6.4 虛擬機 / 雲端快照

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **hypervisor / guest** | hypervisor 是跑 VM 的宿主層（KVM、Proxmox、VMware、雲端）；guest 是 VM 內的作業系統 | 快照在 hypervisor 拍，但一致性只有 guest 自己知道 |
| **fsfreeze** | 讓 guest 內檔案系統暫停寫入並把快取刷到磁碟的指令 | 拍快照前執行才能得到一致狀態，拍完立刻 unfreeze |
| **qemu-guest-agent** | 安裝在 guest 內、讓 hypervisor 能對 guest 下指令的代理 | hypervisor 拍快照時透過它自動 fsfreeze / unfreeze |
| **crash recovery** | 資料庫或日誌式檔案系統在「非正常關機」後開機時的自我修復 | 沒 freeze 的快照等於斷電，靠它才能開，但不保證不損毀 |
| **qcow2 外部快照 / overlay** | `--disk-only` 快照把之後的寫入導到新的 overlay 檔，原檔成為唯讀基底 | 疊越多層 I/O 越慢；`blockcommit` 把 overlay 合併回基底 |
| **`--atomic`** | 多顆磁碟的快照要嘛全部成功要嘛全部不做 | 避免多碟 VM 快照時各碟時間點不一致 |

**為什麼**：VM 層的快照不需要進 guest 就能拍整台機器，最適合升級前保險；但 hypervisor 看不到 guest 的檔案系統狀態，沒有 freeze 的快照等同「突然斷電」的磁碟，資料庫可能需要 crash recovery 甚至損毀。

**怎麼做**：

- KVM：`virsh snapshot-create-as vm1 pre-upgrade --disk-only --atomic`（qcow2 外部快照）；`virsh snapshot-revert`。
- Proxmox / VMware / 雲端磁碟快照：**先** `fsfreeze`（`qemu-guest-agent` 會自動）確保一致性；快照不是異地備份。

**注意**：qcow2 外部快照會留下 overlay 鏈，長期不合併會拖慢 I/O；快照用完要 `blockcommit` 或刪除。

---

## 7. 資料庫備份

**名詞與關係**：資料庫備份的兩條路線，以及 PITR 怎麼從第二條路線長出來：

```
 邏輯備份：pg_dump / mariadb-dump ──▶ SQL 或自訂格式檔 ──▶ 只能回到 dump 那一刻；可跨版本、可只還原一張表
 物理備份：pg_basebackup / mariabackup ──▶ 資料目錄的一致複本（base）
              + 持續歸檔交易日誌（WAL / binlog）
              ──▶ PITR：base + 重放日誌到指定時間 ──▶ 可停在「DROP TABLE 前一秒」
 pgBackRest / Barman = 把「base + WAL 歸檔 + 保留策略 + PITR 還原」整套自動化的工具
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **邏輯備份（dump）** | 把資料庫內容匯出成 SQL 語句或工具自訂格式 | 可跨版本、可部分還原；大庫慢，只能回到 dump 時點 |
| **物理備份** | 以一致的方式直接複製資料目錄，還原時原樣放回 | 快但綁版本與平台；配交易日誌才能 PITR |
| **WAL** | Write-Ahead Log，PostgreSQL 每筆變更先寫入的交易日誌 | `archive_command` 把寫滿的 WAL 段複製到備份位置 = 持續歸檔 |
| **binlog** | MariaDB / MySQL 的二進位日誌，記錄所有變更 | MariaDB 的 PITR 來源，角色同 WAL |
| **PITR** | Point-in-Time Recovery：從物理備份出發重放日誌到指定時刻 | 唯一能還原到「誤刪前一秒」的方法 |
| **pg_dump / pg_dumpall / `-Fc`** | 單庫 / 全叢集（含角色）邏輯備份；`-Fc` 自訂格式可壓縮、可用 `pg_restore` 選表還原 | 小庫每日備份的標準做法 |
| **pg_basebackup** | 取得一致的 PostgreSQL 資料目錄複本（物理備份）的官方工具 | PITR 的起點（base） |
| **pgBackRest / Barman** | 第三方 PostgreSQL 備份管理工具：自動 base + WAL 歸檔、保留策略、平行與增量、一鍵 PITR | 正式環境取代手工 pg_basebackup + archive_command |
| **mariadb-dump `--single-transaction`** | 在一個一致性快照交易內匯出，InnoDB 不需鎖表 | MariaDB 的邏輯備份 |
| **mariabackup** | MariaDB 的熱物理備份工具（Percona XtraBackup 分支） | 配 binlog 做 PITR；也是 Galera SST 的傳輸方式（10 章） |
| **RDB / AOF** | Redis 的兩種持久化：RDB 是定期整份記憶體快照（`dump.rdb`，`BGSAVE` 觸發）；AOF 是每筆寫入命令的追加日誌 | RDB 小、快、可能丟最近幾分鐘；AOF 接近零遺失 |
| **`etcdctl snapshot save`** | 匯出 etcd 整個鍵值庫的快照 | Kubernetes 的所有狀態都在 etcd，這是 K8s 備份的核心 |

**為什麼**：資料庫是「直接 cp 檔案」最容易出錯的地方——執行中的資料檔隨時在變，複製出來的是不一致狀態；用檔案快照又只能回到快照時點。邏輯備份（dump）可跨版本、可部分還原，但大庫要跑很久且只能回到 dump 時點；物理備份 + WAL / binlog 歸檔才能做 PITR（point-in-time recovery），把資料庫還原到「誤刪前一秒」，這是對付 `DROP TABLE` 的唯一辦法。

**怎麼做**：小庫每天邏輯 dump 即可；RPO 要求小時級以下的用 pgBackRest / mariabackup + 持續歸檔。兩邊主要差異在資料目錄路徑。

| DB | 邏輯備份 | 物理 / PITR | 注意 |
|---|---|---|---|
| PostgreSQL | `pg_dumpall \| gzip`、`pg_dump -Fc db`（自訂格式可選擇性還原） | `pg_basebackup -D /backup/base -Ft -z -P` + `archive_command` 歸檔 WAL；工具 **pgBackRest**（兩者有套件）/ Barman | 🔵 資料目錄 `/var/lib/pgsql/data`；🟠 `/var/lib/postgresql/16/main` |
| MariaDB / MySQL | `mariadb-dump --all-databases --single-transaction --routines --events` | `mariabackup --backup`（🟠 `mariadb-backup`、🔵 `mariadb-backup`）+ binlog | InnoDB 用 `--single-transaction` 免鎖表 |
| Redis / Valkey | 複製 `dump.rdb`（`BGSAVE` 後） | AOF | 🔵 `/var/lib/valkey/`；🟠 `/var/lib/redis/` |
| MongoDB | `mongodump` | 檔案系統快照（需 journal 同碟） | |
| SQLite | `sqlite3 db.sqlite ".backup 'b.sqlite'"` | — | 不要直接 cp 使用中的檔案 |
| etcd | `etcdctl snapshot save` | — | K8s 必備 |

**注意**：dump 檔本身也要驗證——`pg_dump` 遇到錯誤可能只印警告仍回傳成功，排程要檢查輸出檔大小與 exit code。還原演練：`pg_restore -d newdb backup.dump`、`mariadb < all.sql`，並比對 row count / checksum。

---

## 8. 整機映像與裸機復原（Bare-metal Recovery）

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **檔案級備份** | 以檔案為單位備份（restic / borg / rsync），還原時要有一個能跑的系統來執行還原工具 | 對付誤刪與資料遺失；對付主機報廢要先靠映像 |
| **整機映像** | 含分割區、開機載入器、檔案系統、所有資料的整台機器複本 | 裸機復原的起點；rear 產生的是「佈局 + tar」，Clonezilla / dd 是位元級複本 |
| **裸機復原（bare-metal recovery）** | 在一台全新、空白的硬體上從零把系統與資料還原回來 | 步驟：重建佈局 → 還原資料 → 修好開機；rear 把三步自動化 |
| **磁碟佈局** | 分割表、LVM、RAID、LUKS 的結構與參數 | 憑記憶最容易漏；`sfdisk -d`、`vgcfgbackup`、LUKS header 就是備它 |
| **開機環境** | ESP、`/boot`、GRUB 設定與 initramfs | 資料還原完但這裡沒修好，機器仍開不了 |

**為什麼**：檔案級備份還原的前提是「已經有一台能開機的 Linux」——主機板燒掉、磁碟全毀、或整台被勒索軟體加密時，你要先重灌系統、重建分割區 / LVM / RAID / LUKS 佈局、還原開機環境，才輪得到 restic。這段手工作業動輒半天，而且憑記憶重建幾乎一定漏掉東西，是 RTO 最大的變數。

### 8.1 Relax-and-Recover（rear，兩者皆有套件）

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **rear（Relax-and-Recover）** | 產生「含本機磁碟佈局的救援開機媒體 + 資料備份」並能一鍵重建的裸機復原工具 | 把 §8 三個步驟串起來；資料備份可用自己的 tar 或交給外部工具 |
| **救援 ISO（`OUTPUT=ISO`）** | rear 做出的可開機映像，內含本機核心、驅動、佈局描述與 `rear recover` | 用它開機的環境就是「一台能跑還原工具的 Linux」 |
| **`OUTPUT_URL` / `BACKUP_URL`** | 分別是救援 ISO 與資料備份的存放位置（NFS、USB、SFTP …） | 兩者都指向 NFS 是最簡單的組合 |
| **`BACKUP=NETFS`** | rear 內建的 tar 備份方式，寫到網路檔案系統 | 也可設 `BACKUP=RESTIC` 等交給外部工具，只讓 rear 負責佈局與開機 |
| **`mkrescue` / `mkbackup`** | 只做救援 ISO / 做 ISO + 資料備份 | 佈局變了至少要重跑 `mkrescue` |
| **`rear recover`** | 在救援環境內執行：重建分割區、LVM、RAID、LUKS、檔案系統，還原資料，重裝 GRUB | 裸機復原的實際動作 |
| **`checklayout`** | 比對目前磁碟佈局與上次做救援 ISO 時是否相同 | 排進 timer，佈局變了才提醒重做 |

**為什麼**：rear 把「重建佈局 + 還原資料 + 修好開機」整個流程自動化：產生一張含有本機磁碟佈局的救援 ISO，用它開機後 `rear recover` 就會重建分割區、LVM、RAID、LUKS 並還原 tar 備份。這是把裸機復原 RTO 從半天壓到一小時內最省力的方法。

**怎麼做**：兩邊版本不同（🟠 2.7、🔵 2.9）但設定相同；`OUTPUT_URL` 放救援 ISO、`BACKUP_URL` 放資料，都指向 NFS：

```bash
sudo apt install -y rear genisoimage syslinux    # 🟠 2.7
sudo dnf install -y rear genisoimage syslinux    # 🔵 2.9
sudo tee /etc/rear/local.conf <<'EOF'
OUTPUT=ISO
OUTPUT_URL=nfs://nas/export/rear
BACKUP=NETFS
BACKUP_URL=nfs://nas/export/rear
BACKUP_PROG_EXCLUDE=("${BACKUP_PROG_EXCLUDE[@]}" '/var/cache/*' '/var/lib/docker/*')
EOF
sudo rear -v mkbackup            # 產生可開機救援 ISO + tar 備份（含分割區 / LVM / RAID / LUKS 佈局）
# 復原：用 ISO 開機 → rear recover → 自動重建分割區與檔案系統並還原資料 → reboot
sudo rear checklayout            # 佈局變更時提醒重做
```

**注意**：救援 ISO 記錄的是製作當下的磁碟佈局，加碟、改 LVM 後要重跑 `mkrescue` / `mkbackup`；把 `rear checklayout` 排進 timer，佈局變了才會提醒。還原到不同大小的磁碟時 rear 會進入互動模式要你調整佈局，先在 VM 練過一次。

### 8.2 Clonezilla / dd

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Clonezilla Live** | 可開機的映像工具，能整碟或整分割區做映像，只複製有資料的區塊 | 比 dd 快且映像小；仍必須離線做 |
| **dd** | 逐位元複製裝置的最原始工具 | 不懂檔案系統，空白區也複製；`conv=sync,noerror` 讓壞軌不中斷 |
| **位元級映像** | 與來源逐位元相同的複本 | 唯一能保留非 Linux 分割區與特殊開機程式的方式；不能增量 |
| **`sfdisk -d`** | 以文字匯出分割表 | 重建時 `sfdisk /dev/sda < file` 一秒畫回原樣 |
| **LVM metadata / `vgcfgbackup`** | VG 的結構描述（哪些 PV、LV 大小與位置）；`vgcfgbackup` 匯出、`vgcfgrestore` 還原 | 分割表之後的第二層佈局（15 章） |
| **LUKS header / 金鑰槽** | 加密分割區開頭的中繼資料，內含以密碼包裝的主金鑰（金鑰槽） | header 損毀 = 資料永久遺失；`luksHeaderBackup` 備它，改密碼後要重備 |
| **`lsblk -f` / `fstab`** | 目前的裝置、檔案系統與 UUID 清單 / 掛載表 | 重建時對照用，確認 UUID 與掛載點 |

**為什麼**：沒有 rear 或需要「位元級一模一樣」的複本（例如含特殊開機程式、非 Linux 分割區）時用整碟映像；但映像不能增量、必須離線做、目標碟不能比來源小。更實用的是只備份**分割表、LVM metadata 與 LUKS header**——這些檔案很小，卻是手動重建時最難憑記憶還原的東西。

**怎麼做**：

```bash
# Clonezilla Live（開機 USB）：整碟或分割區映像到 NFS / SSH / 本機；支援 LVM、LUKS（需先解鎖）
# dd（最原始；離線、目標 ≥ 來源）
sudo dd if=/dev/sda of=/mnt/backup/sda.img bs=4M status=progress conv=sync,noerror
sudo dd if=/dev/sda bs=4M | gzip -c | ssh nas "cat > /backup/sda.img.gz"
# 只備份分割表與 LVM metadata（小、重建時極有用）
sudo sfdisk -d /dev/sda > /backup/sda.sfdisk         # 還原：sfdisk /dev/sda < sda.sfdisk
sudo vgcfgbackup -f /backup/vg-%s.cfg                 # 還原：vgcfgrestore -f file vg0
sudo cryptsetup luksHeaderBackup /dev/sda3 --header-backup-file /backup/sda3-luks.img
lsblk -f > /backup/lsblk.txt; cat /etc/fstab > /backup/fstab
```

**注意**：LUKS header 損毀等於整顆碟的資料永久遺失（金鑰槽在 header 裡），header 備份務必另外加密存放且不要只放在同一份 restic 備份中；改過密碼後要重做備份。`dd` 對執行中的檔案系統會拿到不一致映像，一定要從救援媒體開機做。

### 8.3 手動重建流程（無映像時）

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **autoinstall / Kickstart** | 🟠 / 🔵 的無人值守安裝描述檔（01 章） | 讓「重裝最小系統」這步可重複且幾分鐘完成 |
| **`dpkg --set-selections` / `dselect-upgrade`** | 匯入套件選擇狀態 / 依該狀態安裝 | 🟠 還原套件清單的配對指令 |
| **`fstab` / `crypttab` / `machine-id`** | 掛載表 / LUKS 自動解鎖表 / 每台機器唯一的識別碼 | 新機器的裝置 UUID 與 ID 不同，整個覆蓋 `/etc` 會開不了機 |
| **relabel / `restorecon` / `.autorelabel`** | 依 SELinux 政策重設所有檔案的安全標籤（16 章） | 🔵 還原的檔案標籤不對，服務會被 SELinux 擋 |
| **initramfs / `update-initramfs` / `dracut`** | 開機初期載入的迷你根檔案系統，含找到真正根目錄所需的驅動與 LVM / LUKS 工具 | 硬體或佈局變了不重建，核心找不到根目錄 |
| **`update-grub` / `grub2-mkconfig`** | 重新產生 GRUB 選單設定 | 核心與根裝置變了要重跑 |
| **Ansible playbook** | 以 YAML 描述整台機器該有的狀態並自動套用（13 章） | 有 playbook 的機器重建 = 跑 playbook + 還原資料，RTO 最短 |

**為什麼**：沒有整機映像時，靠「套件清單 + `/etc` + 資料」也能重建，但順序與細節很多——`/etc` 整個覆蓋會把新機器的 `fstab`、`machine-id`、網卡名一起蓋掉導致開不了機；🔵 忘了 relabel 會讓 SELinux 擋住所有服務；忘了重建 initramfs 會找不到根目錄。把流程寫成清單才不會在壓力下漏步驟。

**怎麼做**：

1. 用相同版本安裝媒體重裝最小系統（或 autoinstall / Kickstart 快速完成，見 01 章）。
2. 還原套件清單：
   - 🟠 `sudo dpkg --set-selections < pkgs.txt && sudo apt-get dselect-upgrade`；或 `xargs sudo apt install -y < manual-pkgs.txt`
   - 🔵 `xargs sudo dnf install -y < userinstalled.txt`
3. 還原 `/etc`（⚠️ 不要整個覆蓋：`fstab`、`crypttab`、網卡名、`machine-id` 可能不同，逐項比對）。
4. 還原資料目錄與資料庫。
5. 🔵 `sudo restorecon -R /`（或 `touch /.autorelabel && reboot`）。
6. 🟠 `sudo update-initramfs -u && sudo update-grub`；🔵 `sudo dracut -f && sudo grub2-mkconfig -o /boot/grub2/grub.cfg`。
7. 驗證服務、監控、備份任務都恢復。

💡 用 Ansible（13 章）描述整台機器，重建 = 重跑 playbook + 還原資料，RTO 最短。

---

## 9. 異地與雲端

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **rclone** | 支援數十種雲端與協定的檔案同步 CLI（「雲端的 rsync」） | 可把整個 restic repo 推上雲，也可作為 restic 的後端 |
| **remote** | rclone 設定檔裡的一個雲端連線定義（`b2:`、`gdrive:`） | `rclone config` 建立；之後以 `remote:path` 引用 |
| **`rclone sync`** | 讓目標與來源一致（會刪目標多出的檔） | 與 rsync `--delete` 同義，所以本機 repo 壞了會同步壞 |
| **物件儲存** | 以 HTTP API 存取、按用量計費、幾乎無限容量的雲端儲存（S3、GCS、Azure Blob、B2） | 異地與不可變最便宜的落點 |
| **Object Lock / Retention Policy** | 物件在保留期內不能刪改，compliance mode 連帳號擁有者也無法解除 | 不可變的雲端實作；對應本機的 append-only |
| **版本控制（versioning）** | bucket 保留每個物件的舊版本 | 比 Object Lock 弱（管理員仍可刪版本），但能救回被 sync 覆蓋的檔 |
| **冷儲存** | 存取費高、取回慢（數小時到一天）但存放極便宜的儲存層級 | 適合長期備份，但取回時間要算進 RTO |
| **`--bwlimit` / QoS** | rclone 的頻寬上限 / 網路層的流量優先權 | 避免備份塞爆對外頻寬影響服務 |

**為什麼**：本機與同機房的備份擋不住火災、水災、竊盜、整棟斷電，也擋不住入侵者從主機一路刪到 NAS；3-2-1 裡的「1 份異地」與「1 份不可變」通常都靠雲端物件儲存達成——便宜、免維護、且 Object Lock 讓連管理員都無法在保留期內刪除。

**怎麼做**：rclone 幾乎支援所有雲端，可把本機 restic repo 整份同步上去（repo 已加密，雲端看不到內容）；或直接讓 restic 用 S3 / rclone 後端：

```bash
# rclone：把本機 repo 同步到任何雲端（S3 / GCS / Azure / B2 / Google Drive / OneDrive）
sudo apt install -y rclone    # 🟠      sudo dnf install -y rclone    # 🔵
rclone config                 # 互動式建立 remote
rclone sync /backup/restic b2:mybucket/restic --transfers 8 --checksum -P
# 或 restic 直接用 rclone / S3 後端（§4）
# 雲端不可變：S3 Object Lock（compliance mode）、B2 Object Lock、GCS Retention Policy
# 頻寬：systemd timer 排離峰 + --bwlimit；用 QoS
```

**注意**：`rclone sync` 會讓目標與來源一致——本機 repo 被勒索軟體破壞後下一次 sync 就把雲端也同步壞了，所以雲端要開 Object Lock 或版本控制，才是真正的第二份。雲端的下載費用與速度也要納入 RTO 估算：幾 TB 從冷儲存拉回來可能要一天以上。

---

## 10. 災難復原計畫（DRP）

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **DRP（災難復原計畫）** | 寫明「災難發生時誰、依什麼順序、用哪份備份、怎麼驗證」的文件 | 備份是材料，DRP 是食譜；沒有它 RTO 會被找人、找密碼吃掉 |
| **runbook** | DRP 裡每個服務一份、可以照著打指令的逐步手冊 | 10 章演練用的也是 runbook；每次演練後更新 |
| **演練** | 依 DRP 實際執行一次還原並計時 | 唯一能發現步驟過時、密碼過期、映像失效的方法 |

**為什麼**：災難發生時最缺的是冷靜與資訊——誰有 repo 密碼、先還原哪個服務、DNS 該切到哪、要通知誰。沒有事先寫好的計畫，還原時間會被「找人、找密碼、猜順序」吃掉大半；而計畫沒演練過，第一次執行就會發現步驟過時。

### 10.1 文件內容

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **資產清單** | 所有主機與服務的清單，含相依關係與負責人 | 決定還原順序：被依賴的（DNS、認證）先還 |
| **Tier 分級** | 依 RPO / RTO 把服務分等：Tier 0 是其他一切都依賴的基礎服務 | 資源有限時先救 Tier 0，再依序往下 |
| **升級流程（escalation）** | 第一線處理不了時該通知誰、多久沒回應就往上找誰 | 避免半夜卡在「找不到有權限的人」 |
| **溝通模板** | 對內對外的通知範本（發生什麼、預計多久、下次更新時間） | 讓還原的人專心還原，不用邊做邊寫公告 |
| **演練紀錄** | 每次演練的日期、情境、實測 RTO / RPO、發現的問題 | 讓 DRP 隨系統演進，也證明備份可用 |

**為什麼**：DRP 文件是給「半夜被叫醒、可能不是原本負責人」的人看的，所以要具體到可以照著打指令，且密碼與存取方式要能在主要系統全掛時仍取得。

**怎麼做**：

1. **資產清單**：主機、服務、相依關係、擁有者、RPO / RTO 分級（Tier 0：DNS / AD / 監控；Tier 1：核心業務…）。
2. **備份位置與存取方式**：repo 路徑、密碼放哪（密碼管理器 + 離線封存）、誰有權限。
3. **還原 runbook**：每個服務一份逐步指令（本章 §8.3 的具體化），含驗證方式。
4. **聯絡與升級流程**、**溝通模板**。
5. **演練紀錄**：日期、情境、實際 RTO / RPO、發現的問題與修正。

**注意**：DRP 文件本身不能只放在會一起掛掉的 wiki 或 NAS 上，要有離線 / 列印 / 另一雲端的副本。

### 10.2 演練情境（至少每半年）

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **演練情境** | 一種假想災難與它對應的還原路徑 | 每種情境走不同工具（restic、快照、rear、離線副本），要輪流演練 |
| **實測 RTO / RPO** | 演練中實際量到的還原時間與資料遺失量 | 與 §1.2 訂的目標比對，不符就調整備份頻率或自動化 |
| **checksum 比對** | 用 sha256sum 等比對還原出的檔案與原檔是否一致 | 證明「還原成功」不只是檔案存在 |

**為什麼**：每種災難走的還原路徑不同——誤刪走 restic 單檔還原、勒索走離線副本、主機報廢走 rear；只演練其中一種，其他路徑的問題（密碼過期、映像過時、異地副本其實沒同步）就永遠不會被發現。

**怎麼做**：每次演練挑一到兩種情境，計時並記錄實際 RTO / RPO：

| 情境 | 驗證 |
|---|---|
| 誤刪檔案 / 目錄 | 從 restic / 快照還原單一路徑，比對 checksum |
| 資料庫損毀 | 邏輯還原到測試機、PITR 到指定時間點 |
| 整台主機報廢 | rear / 重建流程到全新 VM，服務上線計時 |
| 勒索軟體 | 從不可變 / 離線備份還原；確認備份本身未被加密 |
| 機房斷電 / 網路 | 異地副本可用性；DNS 切換 |
| 備份系統本身故障 | repo 完整性 `restic check`；密碼可取得 |

### 10.3 備份監控

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **最後成功時間戳** | 每次備份成功時記錄的 Unix 時間 | 監控的對象是「距今多久」，而不是「任務有沒有執行」 |
| **Prometheus textfile collector** | node_exporter 讀取指定目錄下 `.prom` 檔的機制，讓腳本能匯出自訂指標 | 備份腳本寫一行 `backup_last_success_timestamp` 即可被抓取（12 章） |
| **dead-man switch** | 「定期收到心跳才算正常，沒收到就告警」的反向監控 | healthchecks.io、Uptime Kuma 都提供；任何原因的失敗（含根本沒跑）都會被抓 |
| **healthchecks.io / Uptime Kuma** | 託管 / 自架的 dead-man switch 服務，每個任務一個 ping URL | 沒有 Prometheus 時的最簡做法 |

**為什麼**：備份最常見的失敗模式是「安靜地失敗好幾個月」——timer 被停用、磁碟滿、密碼檔權限錯、遠端憑證過期，沒人發現直到需要還原那天。監控要看的不是「有沒有跑」，而是「最後一次成功是什麼時候」。

**怎麼做**：每次成功結束寫入時間戳到監控，用「距離上次成功太久」觸發告警；沒有監控系統時用 dead-man switch：

```bash
# 每次備份結束把結果推到監控（見 12 章）
# Prometheus textfile：echo "backup_last_success_timestamp $(date +%s)" > /var/lib/node_exporter/textfile/backup.prom
# 告警：time() - backup_last_success_timestamp > 86400*1.5
# 或 healthchecks.io / Uptime Kuma 的 dead-man switch：備份成功後 curl 一個 URL，沒收到就告警
```

**注意**：只在「成功」時才推時間戳或 curl，失敗時什麼都不做——這樣任何型態的失敗（包含腳本根本沒啟動）都會因為超時而被告警抓到。

---

## 11. 本章差異總結

備份工具本身（restic、borg、rear、rclone）兩邊都有且用法相同，差異來自三個層面：**版本**（Fedora 更新快、Ubuntu LTS 凍結）、**預設檔案系統與佈局**（Ubuntu Server 是 ext4 on LVM，Fedora Server 是 xfs on LVM、Workstation 是 Btrfs，決定了快照方式與資料目錄路徑）、以及**套件管理與安全框架**（dpkg / dnf 的清單匯出還原指令不同；Fedora 的 SELinux 讓還原後必須 relabel，Ubuntu 的 AppArmor 依路徑不受影響）。下表彙整本章所有差異。

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| restic / borg 版本 | 0.16 / 1.2（可 `restic self-update`） | 0.19 / 1.4 |
| 預設根 LV | `ubuntu-vg/ubuntu-lv`（ext4；LVM 快照） | Server `fedora/root`（xfs；LVM 快照）；Workstation Btrfs 子卷快照 + snapper |
| 套件清單匯出 | `dpkg --get-selections`、`apt-mark showmanual` | `dnf repoquery --userinstalled`、`rpm -qa` |
| 套件清單還原 | `dpkg --set-selections` + `dselect-upgrade` | `xargs dnf install` |
| 套件交易快照 | 無內建（可裝 snapper + apt hook） | `python3-dnf-plugin-snapper`（Btrfs） |
| PostgreSQL 資料目錄 | `/var/lib/postgresql/<ver>/main` | `/var/lib/pgsql/data` |
| Redis 資料目錄 | `/var/lib/redis` | `/var/lib/valkey` |
| rear 版本 | 2.7 | 2.9 |
| ZFS 快照 | 原生 | 非官方 |
| 還原後 MAC 處理 | 通常不需（AppArmor 依路徑） | `restorecon -R` 或 `/.autorelabel` |
| 還原後開機環境 | `update-initramfs -u` + `update-grub` | `dracut -f` + `grub2-mkconfig` |
