# 00 - Ubuntu 與 Fedora 差異速查表

> 本表整理兩個發行版在系統管理上的所有關鍵差異，之後各章會再詳細說明。
> 驗證環境：Ubuntu 24.04.4 LTS、Fedora 44。

**名詞與關係**：本章所有差異都建立在「兩個家族」之上，先把血統與商業角色串起來（各表內的工具名詞於 §2～§6 各表前再逐一定義）：

```
Debian 家族（社群主導）                         Red Hat 家族（企業主導）
──────────────────────                         ────────────────────────
Debian ──衍生──▶ Ubuntu（Canonical 開發）        Fedora（社群開發，Red Hat 贊助）＝ RHEL 的上游
                   ├─ LTS：每 2 年一版，支援 5 年        │ 新技術先在這裡驗證
                   └─ Ubuntu Pro：付費延長至 10～12 年    ▼
                                                    CentOS Stream ＝ RHEL 下一個小版的滾動預覽（位於 Fedora 與 RHEL 之間）
                                                         │
                                                         ▼
                                                    RHEL ＝ Red Hat 付費企業版（每版支援 10 年）
                                                         │ 拿 RHEL 原始碼 1:1 重建
                                                         ▼
                                                    Rocky Linux / AlmaLinux ＝ 免費、二進位相容的 RHEL 重建版
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **上游 / 下游** | 上游是原始碼或發行版的來源；下游取用上游成果再加工發布 | Debian 是 Ubuntu 的上游；Fedora → CentOS Stream → RHEL 由上而下；Rocky / Alma 是 RHEL 的下游 |
| **Debian** | 1993 年起的純社群發行版，以政策文件與 `.deb` 套件格式著稱 | Ubuntu 每 6 個月從 Debian 開發分支取一份快照再加工 |
| **Ubuntu / Canonical** | Ubuntu 是 Debian 衍生版；Canonical 是開發它並靠支援服務獲利的公司 | Canonical 決定了 Snap、Netplan、Ubuntu Pro 等「自家方案」 |
| **Fedora / Fedora Project** | Red Hat 贊助的社群發行版，任務是搶先驗證將進入 RHEL 的技術 | 本章 🔵 欄指的就是它；它是 RHEL 的上游、不是 RHEL 的免費版 |
| **Red Hat** | IBM 旗下的企業 Linux 公司，systemd、NetworkManager、SELinux、firewalld 等的主要開發者 | 做法是「功能先推進上游社群專案 → 進 Fedora → 進 RHEL」 |
| **RHEL** | Red Hat Enterprise Linux，付費企業版，每版支援 10 年 | 取材自 Fedora、經 CentOS Stream 定型；Rocky / Alma 是它的重建版 |
| **CentOS Stream** | RHEL 下一個小版本的滾動預覽版 | 位於 Fedora 與 RHEL 之間：比 Fedora 保守、比 RHEL 略新；2021 年取代了原本「RHEL 重建版」的 CentOS Linux |
| **Rocky Linux / AlmaLinux** | 以 RHEL 原始碼重建、與 RHEL 二進位相容的免費發行版 | CentOS Linux 停止後的替代品；「正式環境要 RHEL 相容」通常就是指這兩者 |
| **Debian 工具鏈** | `.deb` 格式、`dpkg`（低階）/ `apt`（高階）、AppArmor、ufw；Canonical 再加上 Snap、Netplan、Livepatch、Landscape | 🟠 欄大多數項目的來源；逐一定義見 §1～§3 |
| **Red Hat 工具鏈** | `.rpm` 格式、`rpm` / `dnf`（Fedora 41+ 為重寫的 dnf5）、SELinux、firewalld、NetworkManager、systemd、chrony、tuned、Flatpak、Btrfs 預設 | 🔵 欄大多數項目的來源；Fedora 直接用上游原貌，逐一定義見 §2～§5 |

**為什麼這兩個發行版會差這麼多**：Ubuntu 與 Fedora 的差異不是隨機的，幾乎每一項都能追溯到三個根源。第一是**血統**：Ubuntu 承襲 Debian 的社群傳統（自由軟體優先、政策文件驅動、`.deb` / `dpkg` 工具鏈），Fedora 則是 Red Hat 的上游實驗場，工具鏈（`rpm`、`dnf`、SELinux、firewalld、NetworkManager、systemd）多半由 Red Hat 工程師直接開發，所以 Fedora 幾乎總是「第一個用上」。第二是**發行節奏與定位**：Canonical 靠 LTS 賣長期支援（Ubuntu Pro），所以 Ubuntu 傾向保守、向後相容、一裝五年不動；Fedora 的任務是替 RHEL 提前驗證新技術，每 13 個月就淘汰一版，因此敢於把 Btrfs、dnf5、新核心當預設。第三是**商業模式**：Canonical 傾向自建生態（Snap、Netplan、Livepatch、Landscape）以形成差異化；Red Hat 則把功能推進上游社群專案再回收到 RHEL，所以 Fedora 的做法通常就是「上游的做法」。讀下表時，只要記得「Ubuntu 求穩與自家整合、Fedora 求新與上游一致」，大多數差異就不需要死背。

## 1. 基本定位

**名詞與關係**：本表的名詞都圍繞「一個版本能被支援多久、由誰付錢、附帶哪個核心」：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **LTS** | Long Term Support，Ubuntu 每兩年（偶數年 4 月）發布的長期支援版，標準支援 5 年 | 非 LTS 只支援 9 個月；Ubuntu Pro 在 LTS 之上再延長 |
| **非 LTS（中間版）** | LTS 之間每 6 個月一版的過渡版本（如 24.10、25.04） | 用來預告下一個 LTS 的變化；伺服器極少採用 |
| **Ubuntu Pro** | Canonical 的訂閱服務（個人最多 5 台免費），內含 ESM、Livepatch、Landscape 與合規工具 | 把 LTS 的 5 年支援延長到 10 年（再付費可到 12 年） |
| **ESM** | Expanded Security Maintenance，Ubuntu Pro 中「LTS 過了 5 年後仍提供安全更新」的部分，也涵蓋 universe 套件庫 | Ubuntu Pro 能延長支援期的核心；沒有 Pro 就沒有 ESM |
| **Livepatch** | Canonical 的核心即時修補服務，不重開機就套用核心安全修補 | 隨 Ubuntu Pro 提供；Fedora 沒有對應的官方服務（§3 末列） |
| **Landscape** | Canonical 的多主機集中管理平台 | 隨 Ubuntu Pro 提供；本手冊不使用 |
| **GA 核心** | General Availability，LTS 發布當下附帶的核心版本，整個生命週期只修 bug 不升大版 | 與 HWE 核心二選一；Server 預設 GA |
| **HWE 核心** | Hardware Enablement，把較新中間版的核心回移到 LTS 的套件（`linux-generic-hwe-24.04`） | 用於新硬體；Desktop 安裝媒體自 24.04.2 起預設 HWE |
| **Fedora 版本壽命** | 每版約 13 個月：到「下下版」發布後一個月即停止支援 | 等於每年至少要大版本升級一次；升級指令見 §2 末列 |
| **EOL** | End of Life，停止安全更新的日期 | 過了 EOL 就收不到修補，是「選 LTS 還是選 Fedora」的核心考量 |

**為什麼**：支援期限與版本號的差異直接反映兩者的商業角色——Canonical 靠 LTS 的 5～12 年支援賣 Ubuntu Pro，所以每兩年鎖定一個 LTS 並長期維護；Fedora 的角色是 RHEL 的先行版，只需要跑在 RHEL 前面一步，維護太久反而拖慢實驗，因此每版僅約 13 個月。這也是為什麼「正式環境用 RHEL 系」時，通常是選 RHEL / Rocky / Alma 而不是 Fedora 本身。

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 上游 / 家族 | Debian 系 | RHEL 系（Red Hat 的上游社群版） |
| 維護者 | Canonical | Fedora Project（Red Hat 贊助） |
| 發行週期 | 每 6 個月一版；每 2 年一個 **LTS**（4 月，如 22.04、24.04、26.04） | 每 6 個月一版（約 4 月 / 10 月） |
| 支援期限 | LTS：5 年標準支援 + Ubuntu Pro 可延長至 10~12 年；非 LTS：9 個月 | 每版約 **13 個月**（到下下版發布後 1 個月） |
| 版本號 | `YY.MM`（24.04 = 2024 年 4 月） | 流水號（42、43、44…） |
| 適合場景 | 長期穩定的伺服器、雲端、大量教學資源 | 追求最新核心與工具鏈、RHEL / Rocky / Alma 的前哨站 |
| 對應企業版 | Ubuntu Pro | RHEL、CentOS Stream、Rocky Linux、AlmaLinux |
| 預設核心（2026 中） | 24.04 GA：6.8；HWE：7.0 | 44：7.1 |

💡 若目標是「安裝一次跑五年」選 Ubuntu LTS；若目標是「熟悉 RHEL 生態系但要新軟體」選 Fedora。正式環境若需 RHEL 相容性，Fedora 通常只用於開發／測試，正式主機改用 RHEL / Rocky / Alma。

## 2. 套件管理

**名詞與關係**：套件管理是「格式 → 低階工具 → 高階工具 → 套件庫」四層，兩家族每一層都有對應物；通用套件格式則是與這四層平行的另一套系統：

```
                🟠 Ubuntu                                      🔵 Fedora
套件格式        .deb                                           .rpm
低階工具        dpkg（裝 / 拆 / 查單一套件檔，不解相依）          rpm（同左）
高階工具        apt（讀索引、解相依、下載後交給 dpkg）            dnf / dnf5（同左，另有交易歷史可 undo）
官方套件庫      ubuntu.sources（deb822 格式）                    fedora.repo、fedora-updates.repo（.repo 格式）
  └ 分區        main / universe / restricted / multiverse        （不分元件，以不同 repo 檔區分）
社群套件庫      PPA（由 Launchpad 代為建置）                      COPR（由 Fedora 建置系統代為建置）
第三方套件庫    —（非自由軟體已在 multiverse / restricted）       RPM Fusion（free / nonfree）
通用套件格式    Snap（Canonical 獨家商店）                        Flatpak（開放的 Flathub 商店）  ← 與上面四層平行、互不相干
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **`.deb` / `.rpm`** | 兩家族的套件檔格式：一個壓縮檔加上 metadata（名稱、版本、相依、安裝腳本） | 低階工具只認它；高階工具負責從套件庫把它抓下來 |
| **`dpkg` / `rpm`** | 低階工具：安裝、移除、查詢單一套件檔，並維護「已安裝」資料庫 | 不會自動解相依；`apt` / `dnf` 最後都是呼叫它們完成實際安裝 |
| **`apt` / `dnf`** | 高階工具：讀套件庫索引、計算相依、下載後交給低階工具 | `apt` 是 `apt-get` + `apt-cache` 的整合介面；`dnf` 取代了 yum，Fedora 41+ 的 dnf5 是 C++ 重寫版 |
| **交易（transaction）** | 把一次安裝 / 移除牽涉到的所有套件當成一個整體記錄 | `rpm` 與 `dnf` 以交易為單位記錄，因此有 `dnf history undo`；`dpkg` 沒有 |
| **套件庫（repository）** | 放套件檔與索引的伺服器，由本機設定檔指向它 | 🟠 用 deb822 格式的 `.sources`（多行 `Key: value`）；🔵 用 INI 格式的 `.repo` |
| **metapackage / group** | 把一組套件綁在一起：🟠 metapackage 是內容為空、只有相依的套件（`build-essential`）；🔵 group 是套件庫索引裡的群組定義 | `apt install` 直接裝 metapackage；🔵 要用 `dnf group install` 才會查群組 |
| **PPA / COPR** | 個人套件庫：使用者上傳原始碼，由 Launchpad / Fedora 建置系統代為編譯並提供套件庫 | 都是「社群、未經發行版審核」；PPA 只服務 Ubuntu，COPR 只服務 Fedora / EPEL |
| **multiverse / restricted / RPM Fusion** | 放非自由或有授權疑慮軟體的地方：🟠 是官方套件庫內的兩個元件；🔵 是官方以外的第三方套件庫 | Fedora 官方拒收非自由軟體，所以 NVIDIA 驅動、多媒體 codec 都靠 RPM Fusion |
| **Snap / Flatpak** | 與發行版無關的通用套件格式，把程式連同相依打包並隔離執行 | 與 `.deb` / `.rpm` 平行存在；Snap 商店由 Canonical 獨家經營，Flatpak 用開放的 Flathub |
| **`unattended-upgrades` / `dnf-automatic`** | 定期自動套用（安全）更新的服務 | 分別建立在 apt / dnf 之上；🔵 由 `dnf-automatic.timer` 觸發 |
| **版本鎖定** | 阻止某套件被升級 | 🟠 `apt-mark hold` 寫進 dpkg 狀態；🔵 `versionlock` 是 dnf 外掛 |
| **大版本升級** | 從 24.04 升 26.04、從 44 升 45 | 🟠 `do-release-upgrade` 是 Canonical 專用工具；🔵 `dnf system-upgrade` 是 dnf 外掛，先下載、重開機後離線套用 |

**為什麼**：`.deb` 與 `.rpm` 是兩個家族各自獨立發展二十多年的格式，工具鏈自然不同，但最影響運維的是「交易」概念：`rpm` 資料庫本身就以交易為單位記錄每次安裝／移除，`dnf` 再把交易歷史存進 `/var/lib/dnf/history.sqlite`，所以能 `dnf history undo` 整批倒回；`dpkg` 只逐一套件寫入狀態、`apt` 只留純文字日誌，沒有可反向執行的交易紀錄，因此沒有內建 rollback。通用套件格式的分歧則是商業選擇：Snap 由 Canonical 自建且商店由其獨家經營，Flatpak 是 Red Hat 支持的社群專案並以 Flathub 為開放商店；PPA 與 COPR 同理，各自對應 Launchpad 與 Fedora 的建置基礎設施。

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 套件格式 | `.deb` | `.rpm` |
| 高階工具 | `apt`（`apt-get` / `apt-cache`） | `dnf`（Fedora 41+ 為 **dnf5**） |
| 低階工具 | `dpkg` | `rpm` |
| 更新索引 | `sudo apt update` | 自動（`dnf` 每次會檢查 metadata；`dnf makecache` 可手動） |
| 升級全部 | `sudo apt upgrade` / `full-upgrade` | `sudo dnf upgrade` |
| 安裝 | `sudo apt install pkg` | `sudo dnf install pkg` |
| 移除 | `sudo apt remove pkg` / `purge` | `sudo dnf remove pkg` |
| 移除孤兒相依 | `sudo apt autoremove` | `sudo dnf autoremove` |
| 搜尋 | `apt search kw` | `dnf search kw` |
| 查資訊 | `apt show pkg` | `dnf info pkg` |
| 檔案屬於哪個套件 | `dpkg -S /path` / `apt-file search` | `rpm -qf /path` / `dnf provides /path` |
| 列出已安裝 | `apt list --installed` / `dpkg -l` | `dnf list --installed` / `rpm -qa` |
| 列出套件檔案 | `dpkg -L pkg` | `rpm -ql pkg` |
| 鎖定版本 | `sudo apt-mark hold pkg` | `sudo dnf versionlock add pkg` |
| 交易歷史 / 復原 | `/var/log/apt/history.log`（無內建 rollback） | `dnf history list` / `dnf history undo <ID>` |
| 套件群組 | metapackage（如 `build-essential`） | `dnf group install "Development Tools"` |
| 套件庫設定 | `/etc/apt/sources.list.d/*.sources`（deb822 格式） | `/etc/yum.repos.d/*.repo` |
| 社群套件庫 | PPA：`add-apt-repository ppa:user/name` | COPR：`dnf copr enable user/project` |
| 第三方（非自由）套件庫 | `multiverse` / `restricted` 元件 | RPM Fusion（free / nonfree） |
| 自動安全更新 | `unattended-upgrades` | `dnf5-plugin-automatic`（`dnf-automatic.timer`） |
| 通用套件格式 | **Snap**（預設，Firefox 等為 snap） | **Flatpak**（預設，Flathub） |
| 判斷需重開機 | `needrestart` / `/var/run/reboot-required` | `dnf needs-restarting -r` |
| 大版本升級 | `do-release-upgrade` | `dnf system-upgrade download --releasever=45` → `dnf system-upgrade reboot` |

## 3. 系統設定與服務

**名詞與關係**：這張表的名詞可分成網路、防火牆、開機三條「前端 → 後端」鏈，加上幾個獨立的守護程式：

```
網路    🟠 /etc/netplan/*.yaml ──Netplan 翻譯──▶ systemd-networkd（Server）或 NetworkManager（Desktop）──▶ 核心
        🔵 nmcli / nmtui ──寫 keyfile──▶ NetworkManager ──▶ 核心
        兩家的 DNS 都交給 systemd-resolved（/etc/resolv.conf 指向 127.0.0.53）

防火牆  🟠 ufw ──────┐
        🔵 firewalld ─┴──產生規則──▶ nftables（核心 netfilter 的規則語言）   ← 前端不同、後端相同

開機    /etc/default/grub ──🟠 update-grub / 🔵 grub2-mkconfig──▶ grub.cfg ──▶ GRUB 載入核心 + initramfs
        🔵 另有 BLS：核心套件把每個版本寫成 /boot/loader/entries/*.conf，grubby 直接改這些檔，不必重生 grub.cfg
        initramfs 由 🟠 update-initramfs / 🔵 dracut 產生，每個核心版本各一份
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **init 系統 / systemd** | init 是核心啟動後的第一個程序（PID 1）；systemd 是現代發行版通用的 init 實作，也管服務、日誌、計時器 | 兩家皆用；networkd、resolved、timesyncd、journald 都是 systemd 的元件 |
| **Netplan** | Canonical 的網路設定抽象層：讀 YAML，產生後端（renderer）的設定 | 本身不管網路，只翻譯給 systemd-networkd 或 NetworkManager |
| **systemd-networkd** | systemd 內建的輕量網路管理程式，適合伺服器 | Ubuntu Server 上 Netplan 的預設後端；Fedora 不用 |
| **NetworkManager** | Red Hat 開發的網路管理服務，處理有線、無線、VPN；`nmcli` / `nmtui` 是它的 CLI / TUI | Fedora 直接用；Ubuntu Desktop 透過 Netplan 用；連線設定存成 keyfile |
| **systemd-resolved** | systemd 的本機 DNS 快取與轉發程式 | 兩家皆用；上游 DNS 伺服器由 Netplan 或 NetworkManager 餵給它 |
| **nftables** | Linux 核心 netfilter 的現行規則語言與工具，取代 iptables | ufw 與 firewalld 都把規則翻譯成 nftables；也可直接手寫 |
| **ufw / firewalld** | 防火牆前端：ufw 是 Ubuntu 的簡易指令式工具；firewalld 是 Red Hat 的區域（zone）＋服務式守護程式 | 兩者互斥（同時啟用會互相覆蓋）；後端都是 nftables |
| **MAC / AppArmor / SELinux** | 強制存取控制：由核心強制、連 root 也受限的權限系統；AppArmor 以「路徑」寫規則，SELinux 以「標籤（context）」寫規則 | 兩者互斥，由核心的 LSM 框架二選一；SELinux 的拒絕紀錄要靠 auditd 落地（§4） |
| **`sudo` 群組 / `wheel` 群組** | 被允許用 `sudo` 提權的使用者群組 | 同一功能、不同群組名；由 `/etc/sudoers` 決定哪個群組有權 |
| **systemd-timesyncd / chrony** | NTP 時間同步：timesyncd 是 systemd 內建的簡易客戶端；chrony 是 Red Hat 主導的完整實作，可兼作伺服器 | 兩者互斥；裝 chrony 會停用 timesyncd |
| **`ssh.socket`** | systemd 的 socket 啟動單元：由 systemd 先聽 22 埠，有連線才拉起 sshd | Ubuntu 24.04 用它啟動 `ssh.service`，所以改 Port 要改 socket；Fedora 的 `sshd.service` 直接常駐 |
| **crypto-policies** | Red Hat 的系統級加密策略：一份設定同時套用到 SSH、TLS、Kerberos 等 | Fedora 的 sshd 讀它產生的檔案，直接改 `sshd_config` 的演算法可能被覆蓋 |
| **GRUB / `update-grub` / `grub2-mkconfig`** | GRUB 是兩家共用的開機載入器；後兩者是依 `/etc/default/grub` 與偵測結果重新產生 `grub.cfg` 的指令（`update-grub` 只是 `grub-mkconfig` 的包裝） | Fedora 保留 `grub2-` 前綴與 `/boot/grub2/` 目錄（§6） |
| **BLS / `grubby`** | BLS（Boot Loader Specification）把每個核心的開機項存成獨立檔案；`grubby` 是編輯這些檔案與核心參數的工具 | Fedora 用 BLS，裝新核心不必重生 grub.cfg；Ubuntu 不用 BLS，靠 `update-grub` |
| **initramfs / `update-initramfs` / `dracut`** | initramfs 是核心開機時先載入的迷你根檔案系統（含驅動、LVM、LUKS 工具）；後兩者是產生它的工具 | 每個核心版本各有一份；改驅動、加密或模組黑名單後要重建 |
| **核心套件** | 🟠 `linux-generic`（GA）與 `linux-generic-hwe-24.04`（HWE，見 §1）；🔵 `kernel` + `kernel-core` | 裝新核心後由套件觸發重建 initramfs 與開機項 |
| **`/etc/default/` / `/etc/sysconfig/`** | 服務啟動時讀取的環境變數檔目錄（家族慣例） | 由 unit 檔的 `EnvironmentFile=` 引用；內容格式相同、目錄不同 |
| **tuned** | Red Hat 的效能設定檔守護程式，依 profile 調整核心參數與 CPU 調速 | Fedora 預設安裝；Ubuntu 可自行安裝 |
| **cron / cronie / systemd timer** | cron 是傳統排程機制；`cron` 與 `cronie` 是同一功能的兩個實作套件；systemd timer 是 systemd 的排程單元 | 兩家皆建議新排程改用 timer（有日誌、可依賴服務） |
| **cloud-init** | 雲端 / 虛擬機第一次開機時讀取 metadata 做初始設定（主機名、使用者、網路）的工具 | 兩家皆用；Ubuntu 的 autoinstall（§5）與 Netplan 的 YAML 都以它的格式為基礎 |
| **`ubuntu-drivers` / `akmod`** | 🟠 偵測並安裝專有驅動的工具；🔵 `akmod-*` 是 RPM Fusion 提供、隨核心更新自動重編模組的套件 | 兩者都在處理「第三方核心模組」；Secure Boot 下需 MOK 簽章（01 章 §1.5） |
| **Livepatch / kpatch** | 不重開機套用核心修補：Livepatch 是 Canonical 的服務（§1）；kpatch 是 Red Hat 的開源機制 | Fedora 沒有現成修補檔，需自建 |

**為什麼**：init 都是 systemd，但外圍工具幾乎全不同，原因是「誰寫的、誰在維護」。Red Hat 是 NetworkManager、firewalld、SELinux（與 NSA 共同）、chrony、tuned、crypto-policies 的主要開發者，Fedora 直接用上游原貌；Canonical 則為了讓 Server（systemd-networkd）與 Desktop（NetworkManager）共用同一份 YAML 並方便 cloud-init 產生設定，自製 **Netplan** 當抽象層，並沿用 Debian 選擇的 AppArmor（以路徑而非標籤定義規則，學習成本低）與 `sudo` 群組。GRUB 也是同一套軟體，只是 Fedora 保留 `grub2-` 前綴並採用 BLS（`/boot/loader/entries/`）讓核心套件自行註冊開機項，所以能用 `grubby` 而不必重生 `grub.cfg`。

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| init 系統 | systemd | systemd |
| 網路設定 | **Netplan**（`/etc/netplan/*.yaml`）；Server 後端為 systemd-networkd、Desktop 為 NetworkManager | **NetworkManager**（`nmcli` / `nmtui`；keyfile 在 `/etc/NetworkManager/system-connections/`） |
| DNS 解析 | systemd-resolved | systemd-resolved |
| 防火牆 | **ufw**（前端）+ nftables | **firewalld**（前端）+ nftables |
| 強制存取控制（MAC） | **AppArmor** | **SELinux**（預設 enforcing） |
| 管理員群組 | `sudo` | `wheel` |
| 時間同步 | `systemd-timesyncd`（Server 預設）；可改裝 chrony（`/etc/chrony/chrony.conf`） | **chrony**（`/etc/chrony.conf`） |
| SSH 服務單元 | `ssh.service`（24.04 以 `ssh.socket` 啟動） | `sshd.service` |
| SSH 加密策略 | 直接編輯 `sshd_config` | 走 **crypto-policies**（`update-crypto-policies`） |
| 開機載入器設定 | `/etc/default/grub` → `sudo update-grub` | `/etc/default/grub` → `sudo grub2-mkconfig -o /boot/grub2/grub.cfg`；或用 `grubby` |
| initramfs | `update-initramfs -u` | `dracut --force` |
| 核心套件 | `linux-generic`、`linux-generic-hwe-24.04` | `kernel`、`kernel-core`（預設保留 3 個版本） |
| 核心參數 | `GRUB_CMDLINE_LINUX_DEFAULT` | `grubby --update-kernel=ALL --args="..."` |
| 服務環境檔 | `/etc/default/<svc>` | `/etc/sysconfig/<svc>` |
| 效能設定檔工具 | 無（可自行安裝 tuned） | **tuned**（`tuned-adm`） |
| 排程 | `cron` 套件 | `cronie` 套件（皆建議改用 systemd timer） |
| 雲端初始化 | cloud-init | cloud-init |
| 硬體驅動工具 | `ubuntu-drivers` | 依賴 RPM Fusion（如 `akmod-nvidia`） |
| Live patching | Canonical Livepatch（Ubuntu Pro） | 無官方；kpatch 需自建 |

## 4. 日誌

**名詞與關係**：日誌是一條「收集 → 轉存 → 輪替」的流水線，兩家差別只在中段的 rsyslog 有沒有裝；稽核則是獨立的另一條線：

```
程式 stdout/stderr、syslog 訊息、核心訊息 ──▶ journald（二進位索引，存 /var/log/journal/）──▶ journalctl 查詢
                                                 │
                                                 └──轉發──▶ rsyslog（🟠 Server 預設；🔵 需另裝）──▶ 🟠 /var/log/syslog、auth.log、kern.log
                                                                                                    🔵 /var/log/messages、secure
                                                                                                          └──▶ logrotate 定期改名、壓縮、刪舊
核心 audit 子系統 ──▶ auditd ──▶ /var/log/audit/audit.log（SELinux 拒絕、系統呼叫稽核）   ← 獨立於 journald
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **journald / `journalctl`** | systemd 的日誌收集器與查詢工具，以二進位索引格式存放，可依 unit、時間、優先級過濾 | 所有服務輸出與 syslog 訊息先進 journald；rsyslog 從它取資料 |
| **rsyslog** | 傳統 syslog 守護程式，把訊息依規則寫成純文字檔或轉送遠端 | 位於 journald 之後；沒有它就沒有 `/var/log/syslog`、`/var/log/messages` |
| **`/var/log/syslog`、`auth.log` / `/var/log/messages`、`secure`** | rsyslog 產生的文字日誌檔，兩家族命名不同 | Fail2ban、logwatch 等傳統工具讀的就是這些檔 |
| **auditd / `audit`** | Linux 核心稽核子系統的使用者端守護程式（套件名 🟠 `auditd`、🔵 `audit`） | 與 journald 各走各的；SELinux 的拒絕（AVC）紀錄必須經它落地，所以 Fedora 預設裝 |
| **logrotate** | 定期把日誌檔改名、壓縮、刪舊的工具（設定在 `/etc/logrotate.d/`） | 只處理文字檔；journald 自己有大小上限，不需要它 |
| **Fail2ban / logwatch** | 讀日誌找攻擊並封鎖來源 IP / 每日彙整日誌寄報告的工具 | 傳統上讀 rsyslog 文字檔；新版也可改讀 journald |

**為什麼**：systemd 由 Red Hat 主導，Fedora 早在 Fedora 20（2013）就認定 journald 的持久化日誌已足夠，預設拿掉 rsyslog，所以 Fedora 上沒有 `/var/log/messages`、`/var/log/secure`，一切靠 `journalctl`。Ubuntu 顧及大量既有工具與運維習慣（Fail2ban、logwatch、各種 `grep /var/log/auth.log` 的腳本）仍在讀傳統文字檔，Server 版保留 rsyslog 與 journald 並存。稽核方面 Fedora 預設裝 `audit`，是因為 SELinux 的拒絕紀錄必須經由 auditd 落地，兩者是配套。

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| journald | 持久化（`/var/log/journal/`） | 持久化（`/var/log/journal/`） |
| journald 設定 | `/etc/systemd/journald.conf` + `journald.conf.d/` | `/usr/lib/systemd/journald.conf`（範本）+ `/etc/systemd/journald.conf.d/`（覆寫） |
| rsyslog | Server 預設安裝 | 預設 **不安裝**（純 journald） |
| 一般系統日誌 | `/var/log/syslog` | `/var/log/messages`（裝 rsyslog 後） |
| 認證日誌 | `/var/log/auth.log` | `/var/log/secure`（裝 rsyslog 後） |
| 核心日誌 | `/var/log/kern.log` | `journalctl -k` |
| 套件日誌 | `/var/log/apt/history.log`、`/var/log/dpkg.log` | `dnf history`、`/var/log/dnf5.log` |
| 稽核 | `auditd`（需安裝） | `audit`（預設安裝，SELinux 拒絕紀錄在 `/var/log/audit/audit.log`） |
| logrotate 預設 | 每週、保留 4 份、`su root adm` | 每週、保留 4 份、`dateext` |

## 5. 儲存與檔案系統

**名詞與關係**：儲存是一疊可選的層，安裝程式負責把這疊建出來，無人值守檔則是給安裝程式的答案檔：

```
實體磁碟 / 分割區
   └─▶ LUKS（可選：全碟加密層）
          └─▶ LVM（可選：PV → VG → LV，線上擴縮、快照）
                 └─▶ 檔案系統  ext4 / XFS   ：傳統型，沒有自己的快照，要靠 LVM
                               Btrfs / ZFS  ：自帶卷管理與快照，通常不再疊 LVM
安裝程式 🟠 Subiquity（Server）/ Ubuntu Desktop Installer、🔵 Anaconda ──建出上面這疊
無人值守 🟠 autoinstall（YAML）、🔵 Kickstart（.ks）──是分別餵給 Subiquity / Anaconda 的答案檔，互不相通
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **ext4** | Linux 最普及的日誌式檔案系統，成熟、工具齊、可縮可擴 | Ubuntu 預設；沒有快照功能，需搭 LVM |
| **XFS** | 高效能日誌式檔案系統，擅長大檔與平行 I/O，只能擴不能縮 | RHEL 7 起與 Fedora Server 的預設；Red Hat 雇有其主要開發者 |
| **Btrfs** | 新世代 copy-on-write 檔案系統，內建子卷、快照、透明壓縮、多碟 | Fedora Workstation 自 33 起預設；子卷 `root` / `home` 讓快照可分開做 |
| **ZFS** | 源自 Solaris 的 copy-on-write 檔案系統兼卷管理，功能最完整 | 授權為 CDDL，與核心的 GPL 相容性有爭議：Canonical 直接內建模組，Red Hat 不收錄，Fedora 需 OpenZFS repo 或 DKMS 自編 |
| **LVM** | Logical Volume Manager：把磁碟抽象成 PV → VG → LV，可線上調整大小與做快照 | 位於檔案系統之下；ext4 / XFS 的快照能力來自它 |
| **LUKS** | Linux 全碟加密標準，開機時輸入密碼解鎖 | 位於最底層；可綁 TPM 自動解鎖（01 章 §1.5） |
| **快照** | 某一時刻的檔案系統狀態，可回滾或當備份來源 | LVM 快照在區塊層；Btrfs / ZFS 快照在檔案系統層，幾乎零成本 |
| **DKMS** | 隨核心更新自動重新編譯第三方模組的機制 | Fedora 上裝 ZFS 就靠它；與 §3 的 `akmod` 同類 |
| **Subiquity / Ubuntu Desktop Installer / Anaconda** | 安裝程式：Subiquity 是 Ubuntu Server 的文字介面安裝程式，Desktop 另用一套圖形安裝程式；Anaconda 是 Red Hat 家族共用的安裝程式 | 各家族獨有，無人值守格式也隨之不同 |
| **autoinstall / Kickstart** | 無人值守安裝的答案檔：🟠 autoinstall 是 cloud-init 風格的 YAML；🔵 Kickstart 是 `.ks` 文字腳本 | 分別只有 Subiquity / Anaconda 看得懂，互不相通 |

**為什麼**：Fedora 敢在 Workstation 預設 Btrfs（自 Fedora 33 起），是因為它本來就是新技術的試驗場，而 Server 沿用 XFS 則是跟隨 RHEL——Red Hat 聘有 XFS 主要開發者，RHEL 7 起即以 XFS 為預設。Ubuntu 選 ext4 是 Debian 式的保守：最成熟、工具最齊、最少意外。ZFS 的差異純粹是授權判斷：CDDL 與 GPL 的相容性有爭議，Canonical 評估後決定把 ZFS 模組直接打進核心套件，Red Hat 則拒絕承擔這個法律風險。安裝程式與無人值守格式（autoinstall 對 Kickstart）也各自跟隨家族傳統，互不相通。

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 預設檔案系統（桌面） | ext4 | **Btrfs**（子卷 `root` 與 `home`，zstd 壓縮） |
| 預設檔案系統（伺服器） | ext4 + LVM（安裝時勾選） | XFS + LVM |
| 安裝程式 | Subiquity（Server）、Ubuntu Desktop Installer | Anaconda |
| 無人值守安裝 | **autoinstall**（cloud-init YAML） | **Kickstart**（`.ks`） |
| 快照 | LVM 快照；Btrfs / ZFS 需自行選擇 | Btrfs 快照（Workstation）；LVM 快照（Server） |
| ZFS | 核心內建模組（`zfsutils-linux`） | 非官方（需 OpenZFS repo 或 DKMS） |
| 磁碟加密 | LUKS（安裝時勾選） | LUKS（安裝時勾選） |

## 6. 常用路徑對照

**名詞與關係**：本表大多數名詞已在 §2～§5 定義，這裡只補路徑本身牽涉的幾個：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **GPG 金鑰 / keyring** | 套件庫用來簽章索引的公鑰；keyring 是存放這些公鑰的檔案 | 🟠 每個第三方套件庫一個 keyring 檔放 `/etc/apt/keyrings/`；🔵 集中在 `/etc/pki/rpm-gpg/` |
| **`/etc/pki/`** | Red Hat 家族集中放憑證與金鑰（Public Key Infrastructure）的目錄 | TLS 憑證（`tls/`）與 RPM 簽章公鑰（`rpm-gpg/`）都在這；Debian 家族沒有對應的單一目錄 |
| **legacy GRUB / GRUB 2** | 舊版 GRUB 0.9x 與現行的 GRUB 2 | Red Hat 當年為了並存把 GRUB 2 裝進 `grub2` 目錄並沿用至今；Ubuntu 直接用 `grub` 目錄 |
| **`/etc/os-release`** | systemd 定義的發行版識別檔（`ID`、`VERSION_ID`、`VERSION_CODENAME`） | 兩家皆有，是腳本判斷發行版最可靠的方式；`lsb_release`、`/etc/fedora-release` 是各家族的舊介面 |
| **`/etc/modprobe.d/`** | 核心模組載入參數與黑名單（blacklist）設定目錄 | 改完要重建 initramfs（§3）才對早期載入的模組生效 |
| **`/etc/profile.d/`** | 登入 shell 啟動時逐一讀取的腳本目錄 | 兩家皆有；差別只在全域 bashrc 檔名（`bash.bashrc` 對 `bashrc`） |

**為什麼**：路徑差異大多是家族慣例的直接遺產：Debian 系把服務環境變數放 `/etc/default/`、Red Hat 系放 `/etc/sysconfig/`；Red Hat 統一把憑證與 GPG 金鑰集中在 `/etc/pki/`，Debian 則散在 `/etc/apt/keyrings/` 與 `/usr/share/keyrings/`。GRUB 目錄名不同（`grub` 對 `grub2`）是因為 Red Hat 當年為了與 legacy GRUB 並存而保留了 `grub2` 命名，之後就沒再改回來。這些路徑背錯最常導致「設定改了卻沒生效」，因此值得單獨列表。

| 用途 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 套件庫 | `/etc/apt/sources.list.d/ubuntu.sources` | `/etc/yum.repos.d/fedora.repo`、`fedora-updates.repo` |
| GPG 金鑰 | `/etc/apt/keyrings/`、`/usr/share/keyrings/` | `/etc/pki/rpm-gpg/` |
| 網路 | `/etc/netplan/` | `/etc/NetworkManager/system-connections/` |
| 防火牆 | `/etc/ufw/` | `/etc/firewalld/` |
| MAC | `/etc/apparmor.d/` | `/etc/selinux/config`、`/etc/selinux/targeted/` |
| 主機名 | `/etc/hostname` | `/etc/hostname` |
| 時間同步 | `/etc/systemd/timesyncd.conf` 或 `/etc/chrony/chrony.conf` | `/etc/chrony.conf` |
| 服務環境變數 | `/etc/default/` | `/etc/sysconfig/` |
| 開機載入器 | `/boot/grub/grub.cfg` | `/boot/grub2/grub.cfg`（含 UEFI）、`/boot/loader/entries/*.conf`（BLS） |
| 核心模組黑名單 | `/etc/modprobe.d/blacklist.conf` | `/etc/modprobe.d/blacklist.conf` |
| 預設 shell 設定 | `/etc/bash.bashrc`、`/etc/profile.d/` | `/etc/bashrc`、`/etc/profile.d/` |
| 系統資訊 | `lsb_release -a`、`/etc/os-release` | `/etc/fedora-release`、`/etc/os-release` |

## 7. 一鍵對照：同一件事的兩種指令

**為什麼**：前面六張表拆成領域對照，適合查「某個東西在另一邊叫什麼」，但實際操作時常需要的是「同一件事，兩邊各敲哪一行」。下面把最常做的動作並排列出；每一組差異的成因都已在上面對應的表格說明（例如靜態 IP 的差異來自 Netplan 對 NetworkManager、認證日誌的差異來自 rsyslog 有無）。兩行末尾的 🟠 / 🔵 只是標記，複製時記得去掉註解。

```bash
# 更新系統
sudo apt update && sudo apt upgrade -y            # 🟠
sudo dnf upgrade -y                               # 🔵

# 安裝 nginx 並開機自啟
sudo apt install -y nginx && sudo systemctl enable --now nginx   # 🟠
sudo dnf install -y nginx && sudo systemctl enable --now nginx   # 🔵

# 開放 80 埠
sudo ufw allow 80/tcp                                                        # 🟠
sudo firewall-cmd --permanent --add-service=http && sudo firewall-cmd --reload  # 🔵

# 把使用者加入管理員群組
sudo usermod -aG sudo alice     # 🟠
sudo usermod -aG wheel alice    # 🔵

# 看誰擁有 /usr/bin/curl
dpkg -S /usr/bin/curl           # 🟠
rpm -qf /usr/bin/curl           # 🔵

# 靜態 IP
sudo vim /etc/netplan/01-static.yaml && sudo netplan apply                 # 🟠
sudo nmcli con mod eth0 ipv4.addresses 192.168.1.10/24 ipv4.gateway 192.168.1.1 \
     ipv4.dns 1.1.1.1 ipv4.method manual && sudo nmcli con up eth0        # 🔵

# 修改核心參數後重建開機選單
sudo update-grub                                          # 🟠
sudo grub2-mkconfig -o /boot/grub2/grub.cfg               # 🔵

# 判斷是否需要重開機
needrestart -r l ; cat /var/run/reboot-required           # 🟠
sudo dnf needs-restarting -r                              # 🔵

# 認證失敗紀錄
sudo grep "Failed password" /var/log/auth.log             # 🟠
sudo grep "Failed password" /var/log/secure               # 🔵
journalctl -u ssh  -g "Failed password"                   # 🟠（免 rsyslog）
journalctl -u sshd -g "Failed password"                   # 🔵（免 rsyslog）
```
