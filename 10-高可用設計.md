# 10 - 高可用設計

**本章解決什麼問題**：一台主機、一條網路線、一顆磁碟，任何一個壞掉就整個服務停擺——這就是單點故障（SPOF）。真實情境是凌晨三點硬體故障、值班人員被叫醒手動切 IP、DNS 快取要等 TTL 過期，停機動輒數小時。高可用（HA）的目標是**消除單點故障並在故障時自動切換**，把停機從「人來處理」縮短到「秒級自動完成」。代價是每一層都要多一份硬體與更複雜的設定，還要面對腦裂（split-brain）風險；本章的取捨原則是：**先做無狀態化與負載平衡（便宜、簡單），再做叢集與同步複製（昂貴、需要 fencing）**，並且每季實際演練，因為沒演練過的自動切換通常不會如預期動作。

本章從架構原則開始，依序介紹 VIP 漂移（Keepalived）、負載平衡（HAProxy）、叢集管理（Pacemaker / Corosync）、共享儲存（DRBD / 分散式儲存）、資料庫 HA、以及應用層無狀態化。所有設定檔在 [`scripts/examples/ha/`](scripts/examples/ha/)，已用 `haproxy -c` 與 `keepalived -t` 驗證語法。

兩個發行版在 HA 軟體上差異不大（套件多為同名），主要差異在 **防火牆開埠方式、SELinux boolean、套件版本** 與 **Fedora 生命週期短不適合長期 HA 叢集**（正式環境建議 Ubuntu LTS 或 RHEL / Rocky / Alma）。

---

## 1. 架構原則

**為什麼**：HA 最常見的失敗不是技術做不到，而是只在某一層做了冗餘——例如兩台應用伺服器前面只有一台負載平衡器，或資料庫做了複製但儲存只有一顆 NAS。下表是逐層檢查 SPOF 的清單；任何一列沒做到，該層就是整個系統的可用性上限。

**怎麼做**：依表逐層盤點，每一層都要能回答「這個元件壞了，誰接手、多久接手」。

| 原則 | 說明 |
|---|---|
| 冗餘（N+1 / 2N） | 每個層級至少兩個節點：負載平衡器、應用、資料庫、儲存、網路（雙交換器、bonding） |
| 無狀態應用 | Session 放 Redis / DB、檔案放共享儲存或物件儲存，任何一台應用可隨時被替換 |
| 健康檢查 + 自動切換 | 由負載平衡器或叢集軟體偵測並轉移；切換時間 = 偵測時間 + 轉移時間 |
| 避免腦裂（split-brain） | 奇數節點的 quorum、fencing / STONITH、witness 節點 |
| 資料一致性 | 同步複製（不丟資料、延遲高）vs 非同步（可能丟最後幾筆） |
| 可觀測 | 監控每個節點與切換事件（見 12 章）；HA 不是備份的替代品（見 11 章） |
| 定期演練 | 每季實際拔一台機器 / 網路線，驗證自動切換 |

可用性目標與允許停機：99.9% ≈ 8.7 小時/年、99.95% ≈ 4.4 小時/年、99.99% ≈ 53 分鐘/年。每多一個 9，成本大約是上一級的數倍，先和業務確認真正需要的等級，不要一開始就追 99.99%。

**注意**：HA 保護的是「硬體 / 程序故障」，不保護「誤刪資料」與「勒索軟體」——同步複製會忠實地把 `DROP TABLE` 複製到所有節點，備份（11 章）仍然必要。

典型三層架構：

```
                  ┌── DNS（多 A 記錄 / GSLB / 健康檢查 DNS） ──┐
                  ▼                                            ▼
        [ LB1 ]═══VIP (Keepalived VRRP)═══[ LB2 ]            (另一站點)
        HAProxy / Nginx / L4                                   ...
             │           │           │
        [ app1 ]    [ app2 ]    [ app3 ]        ← 無狀態，水平擴展
             │           │           │
        [ Redis Sentinel / Valkey ]  [ 物件儲存 / NFS-HA / CephFS ]
             │
   [ PostgreSQL primary ]══串流複製══[ replica ]   ← Patroni + etcd 自動 failover
```

---

## 2. VIP 漂移：Keepalived（VRRP）

**為什麼**：負載平衡器本身就是最前面的 SPOF——HAProxy 再穩，那台主機當機或網卡壞掉，所有流量就進不來，而客戶端 DNS 快取又不能秒級改指向。Keepalived 讓兩台以上主機共享一個虛擬 IP，MASTER 失效時 BACKUP 在秒級接手，客戶端完全不用改設定。取捨：VRRP 只解決「IP 在誰身上」，不會檢查服務是否真的健康，所以一定要搭配 `vrrp_script` 追蹤 HAProxy 狀態，否則會出現「VIP 在 MASTER 上但 HAProxy 早已掛掉」的假存活。

**怎麼做**：兩個發行版套件同名同版本，直接安裝。

```bash
sudo apt install -y keepalived     # 🟠 2.2.8
sudo dnf install -y keepalived     # 🔵 2.2.8
```

設定檔的重點是 `vrrp_script`（健康檢查）與 `priority`（決定誰是 MASTER）：檢查失敗時 `weight -20` 讓 MASTER 的 priority 降到低於 BACKUP，VIP 就會漂移。`/etc/keepalived/keepalived.conf`（MASTER；範例檔 `scripts/examples/ha/keepalived-master.conf`）：

```
global_defs {
    router_id lb01
    enable_script_security
    script_user root
}
vrrp_script chk_haproxy {
    script "/usr/bin/systemctl is-active --quiet haproxy"    # 也可 "/usr/bin/curl -sf http://127.0.0.1:8404/stats"；腳本路徑必須存在且可執行，否則 keepalived 會停用該 track_script
    interval 2
    weight -20                               # 失敗時 priority 減 20 → 低於 BACKUP 就切換
    fall 2
    rise 2
}
vrrp_instance VI_1 {
    state MASTER                             # BACKUP 節點寫 BACKUP
    interface eth0
    virtual_router_id 51                     # 同一組必須一致、同網段不同組必須不同
    priority 150                             # BACKUP 寫 100
    advert_int 1
    unicast_src_ip 192.168.1.11              # 雲端 / 不支援 multicast 時用 unicast
    unicast_peer { 192.168.1.12 }
    authentication { auth_type PASS  auth_pass s3cret }
    virtual_ipaddress { 192.168.1.100/24 dev eth0 label eth0:vip }
    track_script { chk_haproxy }
    notify_master "/etc/keepalived/notify.sh master"
}
```

啟動前先做語法檢查，啟動後用 `ip addr` 與日誌確認 VIP 真的落在 MASTER 上。VRRP 使用 IP 協定 112，防火牆沒放行時兩台會互相看不到對方、**各自宣告自己是 MASTER**（VIP 同時出現在兩台，這就是腦裂），所以開埠不可省略：

```bash
sudo keepalived -t -f /etc/keepalived/keepalived.conf    # 語法檢查（實測 OK）
sudo systemctl enable --now keepalived
ip -br addr show eth0                                    # MASTER 上會看到 VIP
journalctl -u keepalived -f                              # 觀察 "Entering MASTER STATE"
# 🟠 ufw：sudo ufw allow from 192.168.1.12 to 224.0.0.18 proto vrrp（multicast）或 allow proto vrrp from 192.168.1.12（unicast 也用 IP 協定 112）
sudo ufw allow in on eth0 proto vrrp
# 🔵 firewalld：
sudo firewall-cmd --permanent --add-rich-rule='rule protocol value="vrrp" accept' && sudo firewall-cmd --reload
# 🔵 SELinux：keepalived 執行檢查腳本需要 setsebool -P keepalived_connect_any 1（若腳本連網）
# 服務要綁 VIP（尚未漂移過來時）：sysctl net.ipv4.ip_nonlocal_bind=1
# 非搶佔（避免 MASTER 復活時再切一次）：state BACKUP + nopreempt（兩邊都寫 BACKUP）
```

**注意**：`nopreempt` 值得考慮——預設行為是原 MASTER 一復活就搶回 VIP，等於一次故障造成兩次切換；若切換本身會中斷連線（L4 代理），改成非搶佔可以少掉一次。雲端環境（AWS / GCP / Azure）不支援 VRRP 的 VIP，改用雲端負載平衡器或 `notify_master` 腳本呼叫 API 搬移 Elastic IP / 路由。

---

## 3. 負載平衡：HAProxy

**為什麼**：有了 VIP 只是解決「入口」的 SPOF，後面的應用節點仍然需要有人做健康檢查與流量分配——否則一台應用當機，DNS 輪詢或客戶端仍會把 1/N 的請求送到死掉的節點，使用者看到間歇性錯誤。HAProxy 主動探測每個後端（`option httpchk`），失敗達 `fall` 次就自動剔除，恢復後再加回，還能在維護時手動下線節點做零停機更新。相較 Nginx 開源版，HAProxy 的健康檢查與 stats 介面更完整，也能做 L4 TCP 代理（資料庫）。

**怎麼做**：兩個發行版版本不同（🟠 2.8 LTS 對 🔵 3.0），設定語法相容，範例在兩邊都通過 `haproxy -c`。

```bash
sudo apt install -y haproxy      # 🟠 2.8 LTS
sudo dnf install -y haproxy      # 🔵 3.0
```

設定檔分四段：`global`（stats socket 是無縫重載與線上管理的關鍵）、`defaults`（timeout 避免慢客戶端佔住連線）、`frontend`（收流量、ACL 分流）、`backend`（健康檢查、分配演算法）。`/etc/haproxy/haproxy.cfg` 重點（完整範例 `scripts/examples/ha/haproxy.cfg`，實測 `haproxy -c` 通過）：

```
global
    log /dev/log local0
    maxconn 50000
    stats socket /run/haproxy/admin.sock mode 660 level admin expose-fd listeners   # 無縫重載
defaults
    mode http
    timeout connect 5s
    timeout client 50s
    timeout server 50s
frontend stats
    option httplog
    bind *:8404
    stats enable
    stats uri /stats
    stats auth admin:changeme
frontend http_in
    option httplog
    option forwardfor
    bind *:80
    bind *:443 ssl crt /etc/haproxy/certs/site.pem alpn h2,http/1.1
    http-request redirect scheme https unless { ssl_fc }
    acl is_api path_beg /api
    use_backend api_servers if is_api
    default_backend web_servers
backend web_servers
    balance roundrobin                          # leastconn / source / uri / hdr(host)
    option httpchk GET /healthz
    http-check expect status 200
    cookie SRV insert indirect nocache          # session 黏著（無狀態應用不需要）
    server web1 192.168.1.21:8080 check inter 3s fall 3 rise 2 cookie web1
    server web2 192.168.1.22:8080 check inter 3s fall 3 rise 2 cookie web2
    server web3 192.168.1.23:8080 check backup  # 只有其他都掛才用
listen pgsql                                    # L4 TCP 代理（資料庫）
    mode tcp
    bind *:5432
    option tcplog
    option tcp-check
    balance first
    server pg1 192.168.1.41:5432 check port 8008 inter 3s          # Patroni REST API 健康檢查
    server pg2 192.168.1.42:5432 check port 8008 inter 3s backup
```

日常操作透過 admin socket：`show stat` 看後端狀態、`disable server` 在更新前把節點優雅下線（不再派新連線，現有連線做完）。`reload` 因為有 `expose-fd listeners` 不會掉任何連線：

```bash
sudo haproxy -c -f /etc/haproxy/haproxy.cfg       # 語法檢查
sudo systemctl enable --now haproxy
sudo systemctl reload haproxy                     # 無縫重載
echo "show stat" | sudo socat stdio /run/haproxy/admin.sock | cut -d, -f1,2,18   # 後端狀態
echo "disable server web_servers/web1" | sudo socat stdio /run/haproxy/admin.sock   # 維護時下線
# 憑證：cat fullchain.pem privkey.pem > /etc/haproxy/certs/site.pem
# 🟠 ufw allow 80,443,8404/tcp
# 🔵 firewall-cmd --permanent --add-service={http,https} --add-port=8404/tcp && firewall-cmd --reload
# 🔵 SELinux：setsebool -P haproxy_connect_any 1（後端非標準埠必要）
# 日誌：🟠 rsyslog 已有 /etc/rsyslog.d/49-haproxy.conf → /var/log/haproxy.log；🔵 用 journalctl -u haproxy 或自建 rsyslog 規則
```

**注意**：🔵 Fedora 的 SELinux 預設只允許 HAProxy 連到標準埠（80 / 443 等），後端用 8080 或 5432 時 HAProxy 會回 503 且日誌只寫 "Connection refused"，要先開 `haproxy_connect_any`。`stats auth` 的密碼務必改掉，且 8404 只對管理網開放。替代方案：Nginx（`upstream` + `proxy_pass`；開源版健康檢查較弱）、**LVS / IPVS**（L4 核心層，`ipvsadm`，配合 Keepalived 的 `virtual_server` 區塊）、Traefik / Envoy（容器環境）、雲端 LB。

---

## 4. 叢集管理：Pacemaker + Corosync

**為什麼**：Keepalived + HAProxy 只能處理「無狀態」的東西；NFS server、傳統資料庫、自訂 daemon、共享儲存掛載這類**同一時間只能有一個節點在跑**的服務，需要有人保證「舊節點確實停了，新節點才啟動」。Pacemaker 管理資源在節點間的啟停與轉移，Corosync 負責節點間的心跳與 quorum。**必須有 fencing（STONITH）**：網路分割時兩邊都以為對方死了、同時掛載同一個檔案系統寫入，結果就是資料毀損；fencing 用 IPMI / 虛擬化 API 把對方真的關機，是唯一可靠的保證。

**怎麼做**：兩邊套件名稱略有不同（fence / resource agents 的拆分方式），pcs 與 Pacemaker 版本也不同（🟠 0.11 / 2.1 對 🔵 0.12 / 3.0），指令相同。叢集通訊埠 🔵 有現成的 `high-availability` 服務定義，🟠 要手動列埠。

```bash
# 🟠 Ubuntu（pcs 0.11 + Pacemaker 2.1；Ubuntu 也可用 crmsh）
sudo apt install -y pacemaker corosync pcs fence-agents resource-agents-base resource-agents-extra
# 🔵 Fedora（pcs 0.12 + Pacemaker 3.0）
sudo dnf install -y pacemaker corosync pcs fence-agents-all resource-agents
# 🔵 firewall-cmd --permanent --add-service=high-availability && firewall-cmd --reload
# 🟠 ufw allow from <peer> to any port 2224,3121,5403 proto tcp; ufw allow from <peer> to any port 5404,5405 proto udp

# 兩節點皆執行
sudo passwd hacluster
sudo systemctl enable --now pcsd
# 任一節點執行
sudo pcs host auth node1 node2 -u hacluster
sudo pcs cluster setup mycluster node1 node2 --start --enable
sudo pcs status
sudo pcs property set no-quorum-policy=ignore          # 兩節點叢集（建議加第三節點或 qdevice 取代）
sudo pcs stonith create fence_vm fence_virsh ip=kvm-host username=root ... pcmk_host_map="node1:vm1;node2:vm2"   # 依環境選 fence agent（IPMI: fence_ipmilan；雲端: fence_aws / fence_gce / fence_azure_arm）
sudo pcs property set stonith-enabled=true

# 資源範例：VIP + NFS server + 檔案系統
sudo pcs resource create vip ocf:heartbeat:IPaddr2 ip=192.168.1.100 cidr_netmask=24 op monitor interval=10s
sudo pcs resource create drbd_fs ocf:heartbeat:Filesystem device=/dev/drbd0 directory=/export fstype=xfs
sudo pcs resource create nfsd ocf:heartbeat:nfsserver nfs_shared_infodir=/export/nfsinfo
sudo pcs resource group add nfs_group drbd_fs nfsd vip   # 群組：同節點、依序啟動
sudo pcs constraint order promote drbd_clone then start nfs_group
sudo pcs constraint colocation add nfs_group with master drbd_clone INFINITY
sudo pcs resource move nfs_group node2                   # 手動切換（測試）；之後 pcs resource clear nfs_group
sudo pcs node standby node1 / unstandby node1            # 維護
sudo pcs cluster stop node1
crm_mon -1                                               # 狀態
sudo pcs config; sudo pcs cluster cib > backup.xml       # 備份設定
# quorum device（第三方仲裁）：pcs qdevice setup model net（在第三台）；pcs quorum device add model net host=qnode algorithm=ffsplit
```

**注意**：兩節點叢集沒有多數決，`no-quorum-policy=ignore` 只是讓它能動，腦裂風險完全靠 STONITH 擋——正式環境要嘛加第三節點，要嘛在第三台放 qdevice 當仲裁。`pcs resource move` 會留下位置限制，測試完務必 `pcs resource clear`，否則資源永遠不會回到原節點。

---

## 5. 共享儲存與資料複製

**為什麼**：應用與資料庫都做了 HA，但資料仍放在同一顆磁碟或同一台 NAS 上，那顆碟壞了整個叢集一起停。儲存層 HA 有兩條路：區塊層級複製（DRBD，兩節點、簡單）或分散式儲存（Ceph / GlusterFS，3+ 節點、複雜）；最乾淨的做法其實是讓應用改用物件儲存 API，把「共享檔案系統」這個難題整個拿掉。

### 5.1 DRBD（區塊層級同步複製，兩節點）

**為什麼**：DRBD 把兩台主機的磁碟做成「網路 RAID-1」，每次寫入同步到對端才回報完成（protocol C），主節點壞掉時備節點的資料一筆不少，最適合 NFS-HA 或沒有原生複製能力的傳統服務。取捨：只能兩節點主備、同步寫入受網路延遲影響，且必須交給 Pacemaker 管理 primary / secondary 切換，不要手動切。

**怎麼做**：兩邊 `drbd-utils` 都有，但核心模組不同——🟠 用核心內建的 8.4 模組即可；🔵 Fedora 沒有 DRBD 9 的 kmod，要嘛用內建 8.4、要嘛自行從 LINBIT 取得。

```bash
sudo apt install -y drbd-utils      # 🟠 9.22；核心內建 drbd 模組（8.4）
sudo dnf install -y drbd-utils      # 🔵 9.34；DRBD 9 核心模組需 LINBIT 的 kmod（Fedora 不提供），或用核心內建 8.4
```

資源定義兩節點必須完全相同；`protocol C` 是同步（不丟資料），`shared-secret` 防止別的節點誤連。`/etc/drbd.d/r0.res`（兩節點相同）：

```
resource r0 {
    device    /dev/drbd0;
    disk      /dev/vg_data/lv_drbd;
    meta-disk internal;
    protocol  C;                              # 同步（A 非同步、B 半同步）
    net { cram-hmac-alg sha1; shared-secret "s3cret"; }
    on node1 { address 10.0.0.1:7789; }
    on node2 { address 10.0.0.2:7789; }
}
```

初始化後只在一個節點強制 primary 並建檔案系統，之後把 primary / secondary 的切換交給 Pacemaker：

```bash
sudo drbdadm create-md r0 && sudo drbdadm up r0          # 兩節點
sudo drbdadm primary --force r0                          # 只在 node1
sudo mkfs.xfs /dev/drbd0
cat /proc/drbd; sudo drbdadm status
# 交給 Pacemaker：pcs resource create drbd_r0 ocf:linbit:drbd drbd_resource=r0 promotable promoted-max=1 ...
# 🔵 firewall-cmd --permanent --add-port=7789/tcp；🟠 ufw allow from 10.0.0.2 to any port 7789
```

**注意**：複製流量建議走獨立網段（範例用 10.0.0.x），與服務流量分開，否則大量寫入時會拖慢服務並增加心跳逾時誤判。

### 5.2 其他選項

**為什麼**：DRBD 只能兩節點，資料量大、節點多或需要物件儲存時要換方案；每種方案的節點數門檻與運維複雜度差很多，選錯會變成新的維運負擔。

**怎麼做**：依規模與存取型態（區塊 / 檔案 / 物件）挑選；兩個發行版套件供應大致相同，Ceph 建議用 cephadm 容器化部署以避開套件版本差異。

| 方案 | 適合 | 備註 |
|---|---|---|
| **NFS-HA**（DRBD + Pacemaker 或雙控制器 NAS） | 傳統共享檔案 | 單一 NFS server 是 SPOF |
| **GlusterFS** | 3+ 節點複製卷、簡單 | 兩者皆有套件（`glusterfs-server`）；開發已趨緩 |
| **Ceph**（RBD / CephFS / RGW） | 大規模、區塊 + 檔案 + 物件 | 至少 3 節點、資源需求高；兩者皆有套件，建議 cephadm 容器化部署 |
| **MinIO / SeaweedFS** | S3 相容物件儲存 | 應用改用物件儲存 API 是最乾淨的 HA 做法 |
| **rsync + lsyncd** | 準即時單向同步 | 非真正 HA，適合靜態內容 |
| 雲端 | EFS / Filestore / Azure Files、S3 / GCS | 直接用託管服務 |

---

## 6. 資料庫高可用

**為什麼**：資料庫幾乎永遠是架構中最難消除的 SPOF——它有狀態、要保證一致性、又不能隨便同時有兩個 primary 寫入。手動 failover 的典型流程是：主庫當機 → 有人發現 → 手動 promote replica → 改應用連線字串 → 重啟應用，動輒 30 分鐘以上；本節的工具把這個流程自動化到秒級。

### 6.1 PostgreSQL：Patroni + etcd + HAProxy（業界標準）

**為什麼**：PostgreSQL 原生只有串流複製，沒有「自動 promote」與「誰是 primary」的仲裁機制。Patroni 用 etcd 的 leader lease 決定唯一 primary（etcd 需 3 節點才有多數決），主庫失聯就自動 promote 最新的 replica；HAProxy 檢查 Patroni 的 REST API `/primary`，應用永遠連 HAProxy 就會被導向正確的主庫。`synchronous_mode: true` 保證切換不丟資料，代價是每次 commit 多一次網路往返。

**怎麼做**：🟠 Ubuntu 套件庫有 patroni，🔵 Fedora 44 沒收錄，要用 pip 或 COPR。

```bash
# 3 節點 etcd（或 Consul / ZooKeeper）+ 2~3 節點 PostgreSQL + Patroni；HAProxy 檢查 Patroni REST :8008/primary 與 /replica
sudo apt install -y postgresql-16 patroni etcd-server     # 🟠（patroni 3.2；etcd-server 3.4）
sudo dnf install -y postgresql-server patroni etcd         # 🔵（etcd 3.5；patroni 需 pip 或 COPR，Fedora 44 套件庫未收錄）
```

`/etc/patroni/config.yml` 重點：`scope`、`restapi.listen: 0.0.0.0:8008`、`etcd3.hosts`、`bootstrap.dcs.synchronous_mode: true`（不丟資料）、`postgresql.data_dir`、`replication` 帳號。

啟動後用 `patronictl list` 確認 Leader；計畫性維護用 `switchover`（先確認 replica 追上再切，零資料遺失），`failover` 是強制切換只在主庫真的掛掉時用：

```bash
sudo systemctl enable --now patroni
patronictl -c /etc/patroni/config.yml list          # 看誰是 Leader
patronictl -c /etc/patroni/config.yml switchover    # 計畫性切換
patronictl -c /etc/patroni/config.yml failover      # 強制
```

**注意**：Patroni 接管後 PostgreSQL 的 systemd 服務要 disable，否則開機時兩者搶著啟動資料目錄。替代：**repmgr**（簡單）、**pgpool-II**（連線池 + 負載平衡）、CloudNativePG（Kubernetes）、託管 RDS / Cloud SQL。

### 6.2 MariaDB Galera（多主同步複製）

**為什麼**：傳統 MySQL 主從複製是非同步的，主庫當機時最後幾筆交易可能還沒送到從庫；Galera 讓每個節點都可以寫、每筆交易在多數節點認證後才 commit，不需要 failover 這個動作——任何一台掛掉，應用連到另一台就好。代價是寫入延遲受最慢節點影響、大交易與熱點列衝突會失敗需重試。

**怎麼做**：套件與設定檔路徑兩邊不同——🟠 拆成 `mariadb-server` + `galera-4`，🔵 合併成 `mariadb-server-galera`；libgalera 路徑也因 🟠 用 `/usr/lib`、🔵 用 `/usr/lib64` 而不同。

```bash
sudo apt install -y mariadb-server galera-4          # 🟠 10.11
sudo dnf install -y mariadb-server-galera            # 🔵 11.8
# /etc/mysql/mariadb.conf.d/60-galera.cnf（🟠）/ /etc/my.cnf.d/galera.cnf（🔵）：
#   wsrep_on=ON, wsrep_provider=/usr/lib/galera/libgalera_smm.so（🟠）/ /usr/lib64/galera/libgalera_smm.so（🔵）
#   wsrep_cluster_address="gcomm://10.0.0.1,10.0.0.2,10.0.0.3", wsrep_sst_method=mariabackup, binlog_format=ROW
sudo galera_new_cluster                              # 第一個節點 bootstrap
sudo systemctl start mariadb                         # 其餘節點
mysql -e "SHOW STATUS LIKE 'wsrep_cluster_size';"
# 埠：3306、4567（複製）、4568（IST）、4444（SST）；🔵 firewall-cmd --add-service=mysql --add-port={4567/tcp,4568/tcp,4444/tcp}
# 🔵 SELinux：setsebool -P mysql_connect_any 1 或 semanage port 標記
```

**注意**：奇數節點（3 或 5）避免腦裂——兩節點叢集網路一斷，兩邊都失去多數而全部進入唯讀。整個叢集全停後重啟，必須從 `grastate.dat` 中 `safe_to_bootstrap: 1` 的節點做 `galera_new_cluster`，否則會遺失最後的交易。前端用 HAProxy / ProxySQL / MaxScale。

### 6.3 Redis / Valkey

**為什麼**：Session 與快取搬到 Redis 之後，Redis 自己就成了新的 SPOF——單機 Redis 一掛，所有使用者同時被登出。Sentinel 提供主從自動 failover（適合資料量單機放得下的情況），Cluster 則同時解決容量分片與複製。

**怎麼做**：

- **Sentinel**：3 個 Sentinel 監控 1 主 N 從，自動 failover；客戶端需支援 Sentinel 協定。
- **Cluster**：分片 + 複製，最少 6 節點（3 主 3 從）。
- 🟠 `redis-server` + `redis-sentinel` 套件；🔵 `valkey`（`valkey-sentinel` 同套件，服務 `valkey-sentinel`）。

**注意**：🔵 Fedora 已用 Valkey 取代 Redis（授權變更後的分支），指令與協定相容，但套件與服務名稱、資料目錄都不同。

### 6.4 其他

**為什麼**：每種資料系統的 HA 機制各異，但共通原則相同——複製 + 奇數節點的多數決；下表列出各系統的標準做法，避免自己土砲主從切換腳本。

**怎麼做**：

| 系統 | 做法 |
|---|---|
| MySQL | InnoDB Cluster（Group Replication + MySQL Router）、Orchestrator |
| MongoDB | Replica Set（3 節點） |
| Elasticsearch / OpenSearch | 3 master-eligible + data 節點、replica shard |
| RabbitMQ | Quorum queues + 3 節點 |
| Kafka | KRaft 3 controller + replication factor 3 |
| etcd / Consul / ZooKeeper | 3 或 5 節點 |

---

## 7. 應用層與系統層 HA

**為什麼**：前面幾節都是基礎設施，但如果應用把 session 存在本機記憶體、上傳檔案寫在本機磁碟，負載平衡器再怎麼切換，使用者還是會被登出、檔案還是找不到。應用本身必須「可被隨時替換」，HA 才真正成立。

### 7.1 應用無狀態化

**為什麼**：有狀態的應用節點無法被隨意剔除——LB 把使用者導到另一台，session 就不見了。無狀態化之後，任何一台掛掉或滾動更新都不影響使用者，也才能水平擴展。

**怎麼做**：

- Session → Redis / DB；上傳檔案 → 物件儲存；排程任務 → 只在 leader 執行（DB lock / etcd lease）。
- 健康檢查端點 `/healthz`（存活）與 `/readyz`（就緒，含依賴檢查），供 LB 使用。
- 優雅關閉：收到 SIGTERM 後停止接新連線、完成進行中的請求再退出（systemd `TimeoutStopSec`）。
- 滾動更新：LB 先下線節點（`disable server`）→ 更新 → 健康檢查通過 → 上線。

**注意**：`/readyz` 若把資料庫連線也納入檢查，資料庫短暫抖動時所有應用節點會同時被 LB 剔除，反而造成全站 503；存活與就緒要分開，依賴檢查要有寬限。

### 7.2 systemd 層級自癒

**為什麼**：很多停機不是硬體壞，而是程序 crash、記憶體洩漏被 OOM killer 殺掉、或整個核心 hang 住。這些單機層級的故障靠 systemd 自動重啟與 watchdog 就能在幾秒內恢復，比觸發叢集切換便宜得多。

**怎麼做**：`Restart=always` 搭配 `StartLimit*` 避免無限重啟風暴；`WatchdogSec` 要程式配合定期 `sd_notify`，否則會被誤判當機而不斷重啟：

```ini
[Service]
Restart=always
RestartSec=3s
StartLimitIntervalSec=300
StartLimitBurst=5
WatchdogSec=30s              # 程式需定期 sd_notify(WATCHDOG=1)
OOMPolicy=continue
```

核心層級的 watchdog 則處理「整台機器 hang 住、連 SSH 都進不去」的情況——沒人餵狗就硬體重開機。🟠 用 `watchdog` 套件或 systemd 皆可，🔵 直接用 systemd 內建：

```bash
# 硬體 / 軟體 watchdog：核心當機自動重開機
sudo apt install -y watchdog   # 🟠   /   🔵 systemd 內建；/etc/systemd/system.conf: RuntimeWatchdogSec=30s（需硬體 watchdog 或 softdog 模組）
echo softdog | sudo tee /etc/modules-load.d/softdog.conf
```

**注意**：`softdog` 是軟體模擬，核心本身完全 hang 住時它也不會動；有 IPMI / BMC 的實體機請用硬體 watchdog。

### 7.3 網路層冗餘

**為什麼**：一條網路線、一台交換器、一個電源，都是常被忽略的 SPOF——交換器韌體升級重啟五分鐘，整個機櫃的 HA 叢集一起失聯，甚至因為心跳中斷觸發互相 fencing。

**怎麼做**：

- 雙網卡 **bonding**（802.3ad LACP 或 active-backup）接不同交換器（見 05 章）。
- 雙電源、雙 PDU；管理網（IPMI / BMC）獨立。
- DNS：多台權威 DNS、短 TTL + 健康檢查 DNS（Route 53 / Cloudflare LB）做站點級 failover。

### 7.4 虛擬化 / 容器層

**為什麼**：虛擬化平台與 Kubernetes 本身就內建了 HA 機制（VM 自動在別台 host 重啟、Pod 自動重新排程），善用它們可以少做很多底層工作；但也要防止「所有 replica 都排在同一台 host」讓冗餘形同虛設。

**怎麼做**：

- KVM + Pacemaker（`VirtualDomain` 資源）或 Proxmox HA（內建）。
- Kubernetes：Deployment replicas ≥ 2、PodDisruptionBudget、多可用區、topologySpreadConstraints。
- 🟠 Ubuntu：MicroK8s / Charmed Kubernetes；🔵 Fedora：Podman + Quadlet（單機）、OKD / OpenShift（多節點）。

---

## 8. HA 測試（每季演練清單）

**為什麼**：沒演練過的 HA 幾乎一定有問題——常見的是 fencing 帳號密碼過期、健康檢查腳本路徑被升級改掉、BACKUP 的設定跟 MASTER 不同步，這些平時完全看不出來，真正故障時才發現「自動切換」沒有發生。演練還能量出真實的切換時間與資料遺失量，作為 RPO / RTO（11 章）的依據。

**怎麼做**：在維護時段依序執行，每一項都要記錄結果並修正 runbook：

```bash
# 1. 拔 LB MASTER：systemctl stop keepalived（或關機）→ VIP 應在 BACKUP 出現，ping VIP 不中斷超過 3 秒
# 2. 殺應用節點：systemctl stop myapp → HAProxy stats 顯示 DOWN、流量轉移、無 5xx
# 3. DB failover：patronictl failover → 應用自動重連（HAProxy 檢查 /primary）
# 4. 網路分割：iptables -A INPUT -s <peer> -j DROP → 確認 fencing 動作、無腦裂（兩邊不會同時 primary）
# 5. 磁碟滿 / OOM：stress-ng --vm ... → 觀察 systemd 重啟與監控告警
# 6. 整站斷電：確認開機順序（儲存 → DB → 應用 → LB）與 Persistent timer 補跑
# 記錄：切換時間、資料遺失量（RPO）、告警是否觸發、runbook 是否正確
```

**注意**：第 4 項（網路分割）是最重要也最危險的一項，會真的觸發 fencing 把節點關機；先在測試環境做，且確認監控告警也同時被觸發，否則正式環境故障時沒人知道已經切換過了。

---

## 9. 本章差異總結

HA 軟體本身在兩個發行版幾乎同名同源，差異集中在三件事：**生命週期**（Fedora 13 個月的支援期不適合要跑好幾年的叢集）、**套件拆分方式**（Fedora 沿用 RHEL 的套件命名，Ubuntu 沿用 Debian 的拆分習慣，所以 fence / resource agents、Galera 的套件名與 lib 路徑不同）、**安全框架**（Fedora 的 SELinux 需要額外 boolean，firewalld 有預定義的 `high-availability` 服務；Ubuntu 的 ufw 要手動列埠）。下表彙整本章所有差異，方便在兩種平台間對照。

| 項目 | 🟠 Ubuntu 24.04 | 🔵 Fedora 44 |
|---|---|---|
| 適合長期 HA 叢集 | ✅ LTS 5~12 年 | ⚠️ 13 個月生命週期，需頻繁升級；正式環境改用 RHEL / Rocky / Alma |
| Keepalived | 2.2.8 | 2.2.8 |
| HAProxy | 2.8 LTS（rsyslog 預設有 haproxy 日誌規則） | 3.0（日誌用 journal） |
| Pacemaker / pcs | 2.1 / 0.11；另有 crmsh | 3.0 / 0.12；無 crmsh |
| fence agents 套件 | `fence-agents` | `fence-agents-all` |
| resource agents 套件 | `resource-agents-base` + `resource-agents-extra` | `resource-agents` |
| DRBD | `drbd-utils` + 核心內建模組 | `drbd-utils`；DRBD 9 kmod 需 LINBIT |
| Patroni | 套件庫有（3.2） | 套件庫無（pip / COPR） |
| etcd | `etcd-server` 3.4 | `etcd` 3.5 |
| Galera | `mariadb-server` + `galera-4`；lib 在 `/usr/lib/galera/` | `mariadb-server-galera`；lib 在 `/usr/lib64/galera/` |
| Redis | `redis-server` / `redis-sentinel` | `valkey`（含 sentinel） |
| 防火牆開 VRRP | `ufw allow in on eth0 proto vrrp` | rich rule `protocol value="vrrp"` |
| 叢集埠 | 手動開 2224/3121/5403 tcp、5404-5405 udp | `--add-service=high-availability` |
| SELinux booleans | — | `haproxy_connect_any`、`keepalived_connect_any`、`mysql_connect_any` |
| watchdog | `watchdog` 套件或 systemd | systemd `RuntimeWatchdogSec` |
| Kubernetes 入門 | MicroK8s（snap） | Podman + Quadlet / OKD |
