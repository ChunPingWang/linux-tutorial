# 08 - 安全強化

本章依「攻擊面由外而內」排列：更新 → 帳號與 SSH → 防火牆與入侵防禦 → 強制存取控制（AppArmor / SELinux）→ 核心與系統層 → 檔案完整性與稽核 → 憑證 → 合規檢查。所有指令皆於 Ubuntu 24.04 / Fedora 44 實驗機驗證；防火牆與網路部分詳見 [05 網路與防火牆](05-網路與防火牆.md)。

---

## 1. 保持更新

### 1.1 自動安全更新

```bash
# 🟠 Ubuntu：unattended-upgrades（預設安裝且啟用，只裝 security）
cat /etc/apt/apt.conf.d/20auto-upgrades          # APT::Periodic::Unattended-Upgrade "1";
sudo dpkg-reconfigure -plow unattended-upgrades  # 互動式開關
sudo vim /etc/apt/apt.conf.d/50unattended-upgrades
#   Unattended-Upgrade::Allowed-Origins { "${distro_id}:${distro_codename}-security"; ... };   ← 實測預設含 ESM
#   Unattended-Upgrade::Remove-Unused-Dependencies "true";
#   Unattended-Upgrade::Automatic-Reboot "true";
#   Unattended-Upgrade::Automatic-Reboot-Time "03:30";
#   Unattended-Upgrade::Mail "ops@example.com";
sudo unattended-upgrades --dry-run --debug       # 測試
cat /var/log/unattended-upgrades/unattended-upgrades.log
systemctl list-timers apt-daily*                 # apt-daily.timer（下載）與 apt-daily-upgrade.timer（安裝）

# 🔵 Fedora：dnf5-plugin-automatic
sudo dnf install -y dnf5-plugin-automatic
[ -f /etc/dnf/automatic.conf ] || sudo cp /usr/share/dnf5/dnf5-plugins/automatic.conf /etc/dnf/automatic.conf   # %ghost 檔，實測不存在
sudo vim /etc/dnf/automatic.conf
#   [commands]
#   upgrade_type = security        # default = 全部；security = 只安全性
#   apply_updates = yes            # no = 只下載
#   reboot = when-needed           # never / when-changed / when-needed
#   [emitters]
#   emit_via = stdio               # 或 email / motd（實測有 /etc/motd.d/dnf5-automatic）
sudo systemctl enable --now dnf-automatic.timer  # 每日 06:00 + 隨機延遲 60 分鐘（實測 timer 定義）
journalctl -u dnf5-automatic
```

### 1.2 Live patch 與延伸支援

- 🟠 **Ubuntu Pro**（個人 5 台免費）：`sudo pro attach <token>`；提供 ESM（universe 套件 10 年安全更新）、**Livepatch**（核心免重開機修補）、FIPS、CIS/USG 工具。`pro status`、`pro enable livepatch`。
- 🔵 Fedora 無 livepatch；核心更新頻繁（每 1~2 週），搭配 `reboot = when-needed` 或維護窗口重開機。

### 1.3 需重開機提醒

```bash
[ -f /var/run/reboot-required ] && cat /var/run/reboot-required.pkgs     # 🟠
sudo dnf needs-restarting -r                                             # 🔵（rc=1 需重開機）
```

---

## 2. 帳號與認證

### 2.1 密碼政策

```bash
# 密碼複雜度：pam_pwquality（🟠 apt install libpam-pwquality 後自動加入 PAM；🔵 預設已整合）
sudo vim /etc/security/pwquality.conf
#   minlen = 14
#   minclass = 3          # 至少 3 類（大小寫、數字、符號）
#   maxrepeat = 3
#   dictcheck = 1
#   usercheck = 1
#   enforce_for_root
# 到期政策 /etc/login.defs（新帳號）：PASS_MAX_DAYS 90、PASS_MIN_DAYS 1、PASS_WARN_AGE 14
sudo chage -M 90 -m 1 -W 14 alice            # 既有帳號

# 登入失敗鎖定：pam_faillock（兩者都有 /etc/security/faillock.conf，實測存在）
sudo vim /etc/security/faillock.conf
#   deny = 5
#   unlock_time = 900
#   even_deny_root
# 🔵 啟用：sudo authselect select local with-faillock   （Fedora 42+ 預設 profile 為 local；舊版為 sssd）
# 🟠 啟用：在 /etc/pam.d/common-auth 手動加 pam_faillock 行（auth required pam_faillock.so preauth / authfail），
#         或用 pam-auth-update（/usr/share/pam-configs/ 需自建 faillock 設定檔）
sudo faillock --user alice; sudo faillock --user alice --reset
```

### 2.2 sudo 強化

```bash
sudo visudo -f /etc/sudoers.d/10-hardening
#   Defaults use_pty                     # 防止 sudo 內的程式劫持終端
#   Defaults logfile="/var/log/sudo.log" # 獨立日誌
#   Defaults timestamp_timeout=5         # 5 分鐘要重輸密碼
#   Defaults passwd_tries=3
#   Defaults !visiblepw
#   Defaults env_reset, secure_path="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
# 審計每一條 sudo：journalctl -t sudo
# 移除不需要的 NOPASSWD；限制群組 → 見 02 章
```

### 2.3 兩步驟驗證（TOTP）

```bash
sudo apt install -y libpam-google-authenticator   # 🟠
sudo dnf install -y google-authenticator          # 🔵
google-authenticator -t -d -f -r 3 -R 30 -W       # 每個使用者執行，產生 ~/.google_authenticator
# /etc/pam.d/sshd 最上方加：auth required pam_google_authenticator.so nullok   （nullok = 未設定的使用者可跳過，佈署完拿掉）
# sshd_config drop-in：
#   KbdInteractiveAuthentication yes
#   AuthenticationMethods publickey,keyboard-interactive     # 金鑰 + TOTP 雙因子
sudo systemctl restart ssh   # 🟠  /  sshd  # 🔵
# 🔵 SELinux：家目錄的 .google_authenticator 需正確標籤（restorecon ~/.google_authenticator）
```

### 2.4 帳號清查

```bash
awk -F: '$3 == 0' /etc/passwd                           # UID 0 只能有 root
awk -F: '($2 == "" )' /etc/shadow                       # 空密碼
awk -F: '$7 !~ /nologin|false/ {print $1, $7}' /etc/passwd   # 可登入 shell 的帳號
sudo lastlog | grep -v "Never"                          # 有登入過的
sudo find / -xdev \( -perm -4000 -o -perm -2000 \) -type f -exec ls -l {} \; 2>/dev/null   # SUID/SGID
sudo find / -xdev -nouser -o -nogroup 2>/dev/null       # 無主檔案
sudo find / -xdev -type f -perm -0002 2>/dev/null       # 全域可寫檔案
```

---

## 3. SSH 強化（完整版）

`/etc/ssh/sshd_config.d/10-hardening.conf`（承接 02 章；🔵 Fedora 檔名數字需小於 40 以覆蓋 `40-redhat-crypto-policies.conf`、`50-redhat.conf`）：

```
Port 22
AddressFamily inet
ListenAddress 0.0.0.0
Protocol 2
PermitRootLogin no
PubkeyAuthentication yes
PasswordAuthentication no
KbdInteractiveAuthentication no
ChallengeResponseAuthentication no
AuthenticationMethods publickey
PermitEmptyPasswords no
MaxAuthTries 3
MaxSessions 5
MaxStartups 10:30:60
LoginGraceTime 30
ClientAliveInterval 300
ClientAliveCountMax 2
X11Forwarding no
AllowAgentForwarding no
AllowTcpForwarding no
PermitTunnel no
GatewayPorts no
UseDNS no
Banner /etc/issue.net
LogLevel VERBOSE
AllowGroups sshusers
# 🟠 Ubuntu 可直接指定演算法（🔵 Fedora 由 crypto-policies 管理，這幾行會被忽略）
KexAlgorithms sntrup761x25519-sha512@openssh.com,curve25519-sha256,curve25519-sha256@libssh.org
Ciphers chacha20-poly1305@openssh.com,aes256-gcm@openssh.com,aes128-gcm@openssh.com
MACs hmac-sha2-512-etm@openssh.com,hmac-sha2-256-etm@openssh.com
HostKeyAlgorithms ssh-ed25519,ssh-ed25519-cert-v01@openssh.com,rsa-sha2-512,rsa-sha2-256
```

```bash
sudo groupadd sshusers && sudo usermod -aG sshusers alice
sudo sshd -t && sudo sshd -T | grep -E "^(permitrootlogin|passwordauthentication|ciphers|kexalgorithms)"
sudo systemctl reload ssh     # 🟠   /   sshd   # 🔵
# 只留 ed25519 host key
sudo rm /etc/ssh/ssh_host_{dsa,ecdsa}_key*     # 若存在
# 稽核工具
pipx install ssh-audit && ssh-audit localhost
```

### 3.1 🔵 Fedora crypto-policies（系統層級加密策略）

```bash
sudo dnf install -y crypto-policies-scripts       # 標準安裝已含（容器最小環境需另裝）
update-crypto-policies --show                     # 實測：DEFAULT
sudo update-crypto-policies --set DEFAULT:NO-SHA1 # 子策略（實測有 NO-SHA1、NO-CAMELLIA、NO-PQ、ECDHE-ONLY、AD-SUPPORT…）
sudo update-crypto-policies --set FUTURE          # 更嚴格（RSA ≥ 3072、禁 SHA-1 / CBC）
sudo update-crypto-policies --set FIPS            # 或 fips-mode-setup --enable
ls /usr/share/crypto-policies/policies/           # DEFAULT LEGACY FUTURE FIPS BSI NEXT …
cat /etc/crypto-policies/back-ends/opensshserver.config      # sshd 實際套用的演算法
# 自訂：/etc/crypto-policies/policies/modules/MYPOL.pmod → update-crypto-policies --set DEFAULT:MYPOL
# 影響：OpenSSH、OpenSSL、GnuTLS、NSS、Java、Kerberos、libreswan 全部一致
```

🟠 Ubuntu 沒有對應機制；各服務個別設定。OpenSSL 全域可在 `/etc/ssl/openssl.cnf` 設 `MinProtocol = TLSv1.2`、`CipherString = DEFAULT@SECLEVEL=2`。

---

## 4. 入侵防禦：Fail2ban（已實測）

```bash
sudo apt install -y fail2ban    # 🟠 1.0.2
sudo dnf install -y fail2ban    # 🔵 1.1.0（自動拉入 fail2ban-firewalld，實測 jail.d/00-firewalld.conf 設 banaction = firewallcmd-rich-rules）
```

`/etc/fail2ban/jail.local`（不要改 `jail.conf`，升級會覆蓋）：

```ini
[DEFAULT]
bantime  = 1h
findtime = 10m
maxretry = 5
bantime.increment = true          # 累犯加倍
bantime.maxtime   = 1w
backend  = systemd                # 讀 journald（🟠 實測 jail.d/defaults-debian.conf 已預設 systemd + nftables；🔵 需指定）
ignoreip = 127.0.0.1/8 ::1 192.168.1.0/24
destemail = ops@example.com
action = %(action_mwl)s           # 封鎖 + 寄信含日誌（需 MTA）

# 🟠 banaction = ufw            （預設 nftables 也可；ufw 用戶建議改 ufw 以便 ufw status 看得到）
# 🔵 banaction = firewallcmd-rich-rules   （套件預設，不必寫）

[sshd]
enabled = true
mode    = aggressive              # 也抓 "Connection closed by authenticating user" 等

[nginx-http-auth]
enabled = true
[nginx-botsearch]
enabled = true
[recidive]                        # 重複被封的 IP 封更久
enabled  = true
bantime  = 1w
findtime = 1d
```

```bash
sudo systemctl enable --now fail2ban
sudo fail2ban-client status
sudo fail2ban-client status sshd            # 實測 Journal matches: _SYSTEMD_UNIT=sshd.service + _COMM=sshd（🟠 為 ssh.service 但 fail2ban 仍用 sshd.service 過濾，實測可運作）
sudo fail2ban-client set sshd unbanip 1.2.3.4
sudo fail2ban-client set sshd banip 5.6.7.8
sudo fail2ban-regex /var/log/auth.log /etc/fail2ban/filter.d/sshd.conf     # 測試過濾器（🔵 用 systemd-journal 當來源）
sudo tail -f /var/log/fail2ban.log
```

替代：**CrowdSec**（社群威脅情報、兩者有官方 repo）、SSH 改用 **WireGuard 後才開放**（最有效）。

---

## 5. 強制存取控制

### 5.1 🟠 AppArmor

```bash
sudo aa-status                                    # 載入的 profile 與 enforce / complain 數
sudo apt install -y apparmor-utils apparmor-profiles apparmor-profiles-extra
ls /etc/apparmor.d/                               # profile 檔名 = 路徑把 / 換成 .（usr.sbin.nginx）
sudo aa-enforce /etc/apparmor.d/usr.sbin.nginx    # 強制
sudo aa-complain /etc/apparmor.d/usr.sbin.nginx   # 只記錄不阻擋（除錯）
sudo aa-disable /etc/apparmor.d/usr.sbin.nginx
sudo apparmor_parser -r /etc/apparmor.d/usr.sbin.nginx   # 重載單一 profile
sudo systemctl reload apparmor                    # 重載全部
# 產生新 profile
sudo aa-genprof /usr/local/bin/myapp              # 互動：另一終端操作程式，回來按 S 掃描、A 允許
sudo aa-logprof                                   # 依日誌中的 DENIED 更新 profile
# 除錯
sudo journalctl -k -g apparmor                    # 或 dmesg | grep DENIED；/var/log/kern.log（rsyslog）
# 24.04 新增：未特權 user namespace 限制（影響 Chrome、Electron 等）
sysctl kernel.apparmor_restrict_unprivileged_userns    # =1；需要時為程式建 profile 或暫時設 0
```

profile 範例 `/etc/apparmor.d/usr.local.bin.myapp`：

```
abi <abi/4.0>,
include <tunables/global>
/usr/local/bin/myapp {
  include <abstractions/base>
  include <abstractions/nameservice>
  network inet stream,
  /usr/local/bin/myapp mr,
  /etc/myapp/** r,
  /var/lib/myapp/** rwk,
  /var/log/myapp/*.log w,
  deny /home/** rw,
}
```

### 5.2 🔵 SELinux

```bash
getenforce; sestatus                              # Enforcing / Permissive / Disabled；policy targeted
sudo setenforce 0                                 # 暫時 permissive（重開機恢復；除錯用）
sudo setenforce 1
cat /etc/selinux/config                           # SELINUX=enforcing（⚠️ 設 disabled 後要改回需 touch /.autorelabel 並重開機重標籤）
# 工具（policycoreutils-python-utils、setroubleshoot-server，實測皆可安裝）
ls -Z /var/www/html; ps -eZ | grep nginx; id -Z
sudo restorecon -Rv /var/www/html                 # 依政策修正標籤（複製 / 搬移檔案後常需）
sudo chcon -t httpd_sys_content_t /srv/www/index.html     # 臨時改標籤（restorecon 會改回）
sudo semanage fcontext -a -t httpd_sys_content_t "/srv/www(/.*)?" && sudo restorecon -Rv /srv/www   # 永久
sudo semanage fcontext -l | grep "/srv"
sudo semanage port -l | grep http; sudo semanage port -a -t http_port_t -p tcp 8080
sudo getsebool -a | grep httpd; sudo setsebool -P httpd_can_network_connect 1
sudo semanage boolean -l | grep httpd
sudo semanage login -l; sudo semanage user -l   # SELinux 使用者對應（一般用不到）
# 除錯
sudo ausearch -m avc -ts recent                   # 最近的拒絕
sudo sealert -a /var/log/audit/audit.log          # 人類可讀分析 + 建議指令
sudo journalctl -t setroubleshoot
sudo audit2why < /var/log/audit/audit.log
sudo ausearch -m avc -ts recent | audit2allow -M mypol && sudo semodule -i mypol.pp   # ⚠️ 產生自訂模組放行（先看 mypol.te 內容確認合理）
sudo semodule -l | head; sudo semodule -r mypol
# 常見 boolean
#   httpd_can_network_connect / httpd_can_network_connect_db / httpd_enable_homedirs / httpd_use_nfs
#   samba_enable_home_dirs / samba_export_all_rw / use_nfs_home_dirs / nis_enabled
#   container_manage_cgroup / virt_use_nfs / ssh_sysadm_login
# 容器（Podman）：掛載加 :Z（私有）或 :z（共享）自動標籤
```

三個原則：**不關 SELinux**、**先 `restorecon`**、**再查 boolean**、最後才 `audit2allow`。

---

## 6. 核心與系統層

### 6.1 sysctl 強化（`/etc/sysctl.d/99-hardening.conf`，兩者共通）

```ini
# 網路
net.ipv4.conf.all.rp_filter = 1                    # 🟠 預設 2（loose）；嚴格模式用 1，多網卡非對稱路由時保持 2
net.ipv4.conf.default.rp_filter = 1
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.default.accept_redirects = 0
net.ipv6.conf.all.accept_redirects = 0
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.all.accept_source_route = 0
net.ipv6.conf.all.accept_source_route = 0
net.ipv4.conf.all.log_martians = 1
net.ipv4.icmp_echo_ignore_broadcasts = 1
net.ipv4.icmp_ignore_bogus_error_responses = 1
net.ipv4.tcp_syncookies = 1
net.ipv4.ip_forward = 0                            # 路由器 / 容器主機需 1
# 核心
kernel.kptr_restrict = 2                           # 🟠 預設 1（實測 10-kernel-hardening.conf）
kernel.dmesg_restrict = 1
kernel.yama.ptrace_scope = 1                       # 🟠 預設 1（10-ptrace.conf）；🔵 預設 0（10-default-yama-scope.conf）
kernel.sysrq = 0                                   # 或 176 只留安全用途
kernel.unprivileged_bpf_disabled = 1
kernel.randomize_va_space = 2
kernel.perf_event_paranoid = 3
fs.protected_hardlinks = 1
fs.protected_symlinks = 1
fs.protected_fifos = 2
fs.protected_regular = 2
fs.suid_dumpable = 0
vm.unprivileged_userfaultfd = 0
```

```bash
sudo sysctl --system
```

### 6.2 檔案系統掛載選項

```fstab
/dev/mapper/vg-tmp   /tmp        ext4   defaults,nodev,nosuid,noexec   0 2
tmpfs                /dev/shm    tmpfs  defaults,nodev,nosuid,noexec   0 0
/dev/mapper/vg-home  /home       ext4   defaults,nodev,nosuid          0 2
/dev/mapper/vg-var   /var        xfs    defaults,nodev                 0 2
/dev/sdb1            /media/usb  vfat   defaults,nodev,nosuid,noexec,noauto,user 0 0
```

⚠️ `/tmp` 加 `noexec` 會讓部分安裝程式（某些 `.run`、pip build）失敗，可用 `TMPDIR=/var/tmp` 繞過。

### 6.3 停用不需要的服務與協定

```bash
systemctl list-unit-files --state=enabled
sudo systemctl disable --now avahi-daemon cups bluetooth ModemManager 2>/dev/null   # 伺服器通常不需要
# 停用少用的核心模組
sudo tee /etc/modprobe.d/blacklist-rare.conf <<'EOF'
install dccp /bin/false
install sctp /bin/false
install rds /bin/false
install tipc /bin/false
install cramfs /bin/false
install freevxfs /bin/false
install jffs2 /bin/false
install hfs /bin/false
install hfsplus /bin/false
install udf /bin/false
install usb-storage /bin/false      # 若不需 USB 儲存
EOF
# USB 裝置白名單：usbguard（兩者皆有套件）
```

### 6.4 開機安全

```bash
# GRUB 密碼（防止改核心參數進 single user）
# 🟠 grub-mkpasswd-pbkdf2 → /etc/grub.d/40_custom 加 set superusers="admin" / password_pbkdf2 admin grub.pbkdf2...；
#    /etc/grub.d/10_linux 的 CLASS 加 --unrestricted 讓正常開機不需密碼；update-grub
# 🔵 grub2-setpassword（一行搞定，寫入 /boot/grub2/user.cfg）；正常開機仍不需密碼
mokutil --sb-state                                # Secure Boot 狀態
# 🔵 核心 lockdown（Secure Boot 啟用時自動 integrity 模式）：cat /sys/kernel/security/lockdown
```

### 6.5 Secure Boot 下的自訂核心模組

```bash
# 兩者：產生 MOK 金鑰 → 簽署模組 → mokutil --import → 重開機 enroll
# 🟠 DKMS 安裝時自動處理（/var/lib/shim-signed/mok/）；🔵 akmods 需 kmodgenca + mokutil --import /etc/pki/akmods/certs/public_key.der
```

---

## 7. 檔案完整性與惡意軟體

```bash
# AIDE（兩者同名套件；實測 🟠 0.18.6、🔵 0.19.2）
sudo apt install -y aide      # 🟠 設定 /etc/aide/aide.conf，資料庫 /var/lib/aide/aide.db
sudo dnf install -y aide      # 🔵 設定 /etc/aide.conf，資料庫 /var/lib/aide/aide.db.gz
sudo aide --init              # 🟠 aideinit；🔵 aide --init 後 mv aide.db.new.gz aide.db.gz
sudo aide --check             # 排程每日（🟠 套件附 dailyaidecheck.timer，實測已啟用；🔵 自建 timer）
sudo aide --update

# 套件檔案驗證（免安裝）
sudo debsums -c               # 🟠（apt install debsums）
sudo rpm -Va | grep -vE '^\.{8}' # 🔵（S 大小 M 權限 5 checksum U 擁有者 改變的檔案）

# Rootkit 掃描
sudo apt install -y rkhunter chkrootkit   # 🟠  /  sudo dnf install -y rkhunter chkrootkit   # 🔵
sudo rkhunter --propupd && sudo rkhunter --check --sk
# ClamAV（檔案伺服器 / 郵件）
sudo apt install -y clamav clamav-daemon   # 🟠  /  sudo dnf install -y clamav clamd clamav-update   # 🔵
sudo freshclam && clamscan -r --infected /srv/share
```

---

## 8. 稽核（auditd）

見 [07 日誌管理 §5](07-日誌管理.md)。安全基準建議至少監看：`/etc/passwd`、`/etc/shadow`、`/etc/sudoers*`、`/etc/ssh/sshd_config*`、特權指令執行、檔案刪除、時間變更、核心模組載入。🔵 Fedora 可直接複製 `/usr/share/audit-rules/30-stig.rules` 或 `30-pci-dss-v31.rules` 到 `/etc/audit/rules.d/`。

---

## 9. TLS 憑證

### 9.1 Let's Encrypt（certbot，兩者同名）

```bash
sudo apt install -y certbot python3-certbot-nginx     # 🟠 2.9
sudo dnf install -y certbot python3-certbot-nginx     # 🔵 5.7
sudo certbot --nginx -d www.example.com -d example.com --agree-tos -m ops@example.com --redirect
sudo certbot certonly --standalone -d mail.example.com   # 無網頁伺服器時（需 80 埠空著）
sudo certbot certonly --dns-cloudflare --dns-cloudflare-credentials ~/.secrets/cf.ini -d '*.example.com'   # 萬用憑證（DNS-01）
sudo certbot renew --dry-run
systemctl list-timers certbot*        # 🟠 certbot.timer；🔵 certbot-renew.timer（實測套件皆附 timer）
ls /etc/letsencrypt/live/www.example.com/    # fullchain.pem、privkey.pem
```

### 9.2 內部 CA / 自簽

```bash
# 現代做法：用 step-ca / smallstep 或 mkcert（開發）；快速自簽：
openssl req -x509 -newkey rsa:4096 -sha256 -days 365 -nodes \
  -keyout server.key -out server.crt -subj "/CN=internal.example.com" \
  -addext "subjectAltName=DNS:internal.example.com,IP:10.0.0.10"
# 信任內部 CA（讓 curl / 瀏覽器 / 系統程式都認）
sudo cp ca.crt /usr/local/share/ca-certificates/internal-ca.crt && sudo update-ca-certificates   # 🟠
sudo cp ca.crt /etc/pki/ca-trust/source/anchors/ && sudo update-ca-trust                        # 🔵
openssl s_client -connect host:443 -servername host </dev/null 2>/dev/null | openssl x509 -noout -dates -subject   # 檢查
```

### 9.3 服務端 TLS 設定原則

- 只開 TLS 1.2 / 1.3；用 Mozilla SSL Configuration Generator（intermediate profile）產生 nginx / apache / haproxy 設定。
- 開 HSTS、OCSP stapling；金鑰 ECDSA P-256 或 RSA 2048+（🔵 FUTURE 策略需 3072+）。
- 測試：`testssl.sh https://host` 或 SSL Labs。

---

## 10. 合規檢查與自動化強化

```bash
# Lynis（兩者皆有套件；快速健檢）
sudo apt install -y lynis    # 🟠 3.0.9      sudo dnf install -y lynis    # 🔵 3.1.7
sudo lynis audit system --quick           # 看 Hardening index 與 Suggestions；報告 /var/log/lynis-report.dat

# OpenSCAP + SCAP Security Guide（CIS / STIG / PCI-DSS 基準，可自動修復）
sudo apt install -y openscap-scanner ssg-base ssg-debderived      # 🟠 ⚠️ 實測 24.04 套件庫的 ssg-debderived 0.1.71 只含到 ssg-ubuntu2204；
#    24.04 的內容請到 https://github.com/ComplianceAsCode/content/releases 下載 ssg-ubuntu2404-ds.xml
sudo dnf install -y openscap-scanner scap-security-guide           # 🔵（實測 0.1.81）
ls /usr/share/xml/scap/ssg/content/
oscap info /usr/share/xml/scap/ssg/content/ssg-fedora-ds.xml | grep -A20 Profiles       # 🔵
sudo oscap xccdf eval --profile xccdf_org.ssgproject.content_profile_cis \
  --report /root/cis-report.html /usr/share/xml/scap/ssg/content/ssg-fedora-ds.xml       # 🔵 評估
sudo oscap xccdf eval --profile xccdf_org.ssgproject.content_profile_cis_level1_server \
  --report /root/cis-report.html /usr/share/xml/scap/ssg/content/ssg-ubuntu2404-ds.xml   # 🟠
sudo oscap xccdf eval --remediate --profile ... ssg-*.xml        # ⚠️ 自動修復，先在測試機跑
# 🟠 Ubuntu Pro 用戶另有 USG（Ubuntu Security Guide）：sudo usg audit cis_level1_server / usg fix
# 🔵 Fedora / RHEL 安裝時 Anaconda 可直接選 Security Profile（Kickstart: %addon org_fedora_oscap）
```

### 10.1 最小強化檢查清單

- [ ] 自動安全更新已啟用，並有重開機策略
- [ ] root 鎖定；管理員在 `sudo` / `wheel`；sudo 需密碼；`use_pty`
- [ ] SSH：金鑰登入、禁密碼、禁 root、`AllowGroups`、非必要不轉發
- [ ] 防火牆預設 deny incoming，只開必要埠；管理埠限來源 IP
- [ ] Fail2ban 或等效機制
- [ ] AppArmor / SELinux **enforcing**，服務有 profile / 標籤正確
- [ ] 密碼政策 + faillock；不需要的帳號移除
- [ ] sysctl 強化；不需要的服務 / 模組停用
- [ ] `/tmp`、`/dev/shm` 的 `noexec,nosuid,nodev`
- [ ] auditd 規則；日誌集中外送
- [ ] AIDE 或套件完整性定期檢查
- [ ] TLS 只用 1.2+；憑證自動續期
- [ ] 定期 `lynis` / `oscap` 掃描；備份可還原（見 11 章）

---

## 11. 本章差異總結

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 自動更新 | unattended-upgrades（預設開） | dnf5-plugin-automatic（需裝；設定檔為 %ghost） |
| Livepatch / 延伸支援 | Ubuntu Pro（ESM、Livepatch、USG） | 無 |
| MAC | AppArmor（path-based；`aa-*` 工具） | SELinux（label-based；`semanage`、`restorecon`、`sealert`） |
| 加密策略 | 各服務自行設定 | crypto-policies 全系統統一（`update-crypto-policies`） |
| faillock 啟用 | 手動改 PAM | `authselect select local with-faillock` |
| Fail2ban 預設 | backend systemd、banaction nftables | backend 需指定、banaction firewallcmd-rich-rules |
| sysctl 預設 | `rp_filter=2`、`kptr_restrict=1`、`ptrace_scope=1` | `ptrace_scope=0`（其餘依核心預設） |
| GRUB 密碼 | 手動編輯 `40_custom` | `grub2-setpassword` |
| CA 信任 | `/usr/local/share/ca-certificates/` + `update-ca-certificates` | `/etc/pki/ca-trust/source/anchors/` + `update-ca-trust` |
| SCAP 內容 | `ssg-debderived`（ssg-ubuntu2404-ds.xml）；Pro 有 USG | `scap-security-guide`（ssg-fedora-ds.xml）；安裝時可選 profile |
| AIDE 設定 | `/etc/aide/aide.conf`，`aideinit` | `/etc/aide.conf`，`aide --init` |
| certbot timer | `certbot.timer` | `certbot-renew.timer` |
| 核心模組簽署 | DKMS + shim MOK 自動 | akmods + `kmodgenca` + `mokutil` |
| 非標準埠 | 開防火牆即可 | 開防火牆 + `semanage port` |
