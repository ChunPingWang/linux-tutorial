# 08 - 安全強化

本章依「攻擊面由外而內」排列：更新 → 帳號與 SSH → 防火牆與入侵防禦 → 強制存取控制（AppArmor / SELinux）→ 核心與系統層 → 檔案完整性與稽核 → 憑證 → 合規檢查。所有指令皆於 Ubuntu 24.04 / Fedora 44 實驗機驗證；防火牆與網路部分詳見 [05 網路與防火牆](05-網路與防火牆.md)。

**本章解決什麼問題**：一台放上公網的 Linux 主機，開機幾分鐘內就會被自動化掃描器找到。攻擊者絕大多數不是針對你的人，而是跑腳本的殭屍網路：對 22 埠暴力猜密碼、對已知 CVE（有公開編號、已被記錄的漏洞）的舊版服務（OpenSSH、nginx、PHP、Log4j 之類）丟現成 exploit（利用該漏洞的攻擊程式）、拿外洩或重複使用的密碼直接登入。真正常見的入侵路徑依序是：**沒打補丁的已知漏洞**、**開著密碼登入的 SSH 加弱密碼**、**設定錯誤的服務**（預設帳密、對外開放的管理介面）、以及**進來之後的橫向提權**（SUID 程式、可寫的 cron、sudo 設錯）；針對性的 0-day（尚未公開、也還沒有修補的漏洞）反而罕見。因此本章的取捨原則是：先做能擋掉九成自動化攻擊的幾件事（更新、金鑰登入、防火牆、Fail2ban），再做「攻擊者進來後限制他能做什麼」的縱深防禦（MAC、sysctl、掛載選項、稽核、完整性檢查），最後用合規工具找漏網之魚。每一項都有副作用，文中會標明；不要為了分數把機器強化到自己都登不進去。

---

## 1. 保持更新

**為什麼**：多數入侵用的是已公開、已有修補的漏洞——PoC 釋出後幾天內就有人掃全網，而沒打補丁的主機常常一擺就是幾個月。自動安全更新是投資報酬率最高的強化項目；風險只在「更新後服務行為改變」，所以只裝 security 通道（發行版單獨標記為「只修安全漏洞、不加功能」的更新來源），並事先訂好重開機策略。

### 1.1 自動安全更新

**名詞與關係**：兩個發行版的「自動安全更新」由不同套件實作，但結構一樣——一個 systemd timer 定時喚醒、一個排程程式決定裝什麼、一個設定檔限定只裝安全性通道：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **unattended-upgrades** | 🟠 Ubuntu 的自動更新程式（Python 腳本），依 `50unattended-upgrades` 的 `Allowed-Origins` 決定哪些來源的更新可以自動裝 | 由 `apt-daily-upgrade.timer` 每天呼叫；`20auto-upgrades` 只是總開關（`"1"` = 啟用） |
| **apt-daily.timer / apt-daily-upgrade.timer** | 🟠 兩個 systemd timer：前者下載套件清單與更新檔，後者實際安裝 | 是 unattended-upgrades 的觸發器；`systemctl list-timers` 看得到下次執行時間 |
| **security 通道** | 發行版把「只修安全漏洞、不加功能」的更新單獨標記：🟠 是 `noble-security` 這個套件來源，🔵 是 dnf 更新通告（advisory）裡 `type=security` 的那些 | `Allowed-Origins` 與 `upgrade_type = security` 都是在說「只裝這一類」，避免功能更新改變服務行為 |
| **ESM** | Expanded Security Maintenance，Ubuntu Pro 提供的延伸安全更新來源（`esm-infra`、`esm-apps`） | 出現在 `Allowed-Origins` 預設清單裡，沒有 attach Ubuntu Pro 時這行不會生效；詳見 §1.2 |
| **dnf5-plugin-automatic** | 🔵 Fedora 的 dnf5 外掛，負責自動下載 / 安裝更新並發通知 | 對應 unattended-upgrades；由 `dnf-automatic.timer` 觸發；設定檔 `/etc/dnf/automatic.conf` 是 %ghost 檔（套件宣告擁有但不建立，要自己從範本複製） |
| **dnf-automatic.timer** | 🔵 觸發 dnf5-plugin-automatic 的 systemd timer | 對應 Ubuntu 的 apt-daily-upgrade.timer |

**為什麼**：靠人手動 `apt upgrade` / `dnf upgrade` 一定會漏，尤其是那幾台「沒人記得」的機器。兩個發行版都有只套用安全性更新的排程機制，差別在 Ubuntu 預設已啟用，Fedora 要自己裝外掛並補設定檔。

**怎麼做**：

```bash
# 🟠 Ubuntu：unattended-upgrades（預設安裝且啟用，只裝 security）
cat /etc/apt/apt.conf.d/20auto-upgrades          # APT::Periodic::Unattended-Upgrade "1";
sudo dpkg-reconfigure -plow unattended-upgrades  # 互動式開關
sudo vim /etc/apt/apt.conf.d/50unattended-upgrades
#   Unattended-Upgrade::Allowed-Origins { "${distro_id}:${distro_codename}-security"; ... };   ← 實測預設含 ESM
#   Unattended-Upgrade::Remove-Unused-Dependencies "true";
#   Unattended-Upgrade::Automatic-Reboot "true";
#   Unattended-Upgrade::Automatic-Reboot-Time "03:30";
#   Unattended-Upgrade::Mail "ops@example.com";
sudo unattended-upgrades --dry-run --debug       # 測試
cat /var/log/unattended-upgrades/unattended-upgrades.log
systemctl list-timers apt-daily*                 # apt-daily.timer（下載）與 apt-daily-upgrade.timer（安裝）

# 🔵 Fedora：dnf5-plugin-automatic
sudo dnf install -y dnf5-plugin-automatic
[ -f /etc/dnf/automatic.conf ] || sudo cp /usr/share/dnf5/dnf5-plugins/automatic.conf /etc/dnf/automatic.conf   # %ghost 檔，實測不存在
sudo vim /etc/dnf/automatic.conf
#   [commands]
#   upgrade_type = security        # default = 全部；security = 只安全性
#   apply_updates = yes            # no = 只下載
#   reboot = when-needed           # never / when-changed / when-needed
#   [emitters]
#   emit_via = stdio               # 或 email / motd（實測有 /etc/motd.d/dnf5-automatic）
sudo systemctl enable --now dnf-automatic.timer  # 每日 06:00 + 隨機延遲 60 分鐘（實測 timer 定義）
journalctl -u dnf5-automatic
```

**注意**：`Automatic-Reboot` / `reboot = when-needed` 會在深夜自動重開機，資料庫或有狀態服務要先確認能自動恢復；不放心就設 `false` / `never`，搭配 1.3 的提醒手動處理。

### 1.2 Live patch 與延伸支援

**名詞與關係**：這一節的名詞都掛在 Ubuntu Pro 之下——它是一個訂閱入口，ESM、Livepatch、FIPS、USG 是底下的個別服務，用 `pro enable <服務>` 各自開關：

```
Ubuntu Pro（pro attach <token>；帳號 / 訂閱層）
 ├─ ESM        延伸安全更新：main 套件延長到 10 年（esm-infra）、universe 套件也納入維護（esm-apps）
 ├─ Livepatch  核心熱修補：不重開機就把安全補丁載進正在執行的核心
 ├─ FIPS       通過 FIPS 140 驗證的加密模組（政府 / 金融合規用）
 └─ USG        Ubuntu Security Guide：CIS / DISA-STIG 稽核與自動修復工具（見 §10）
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Ubuntu Pro** | Canonical 的訂閱服務（個人 5 台免費），用 `pro` 指令把機器掛到帳號下 | 其他四個都是它的子服務；沒 attach 就沒有 ESM 套件來源，也沒有 Livepatch |
| **main / universe** | Ubuntu 套件庫的兩個區：main 由 Canonical 負責安全維護，universe 由社群維護、Canonical 不保證修補 | ESM 的意義就是把 universe（很多 Python / PHP 函式庫都在這）納入 Canonical 的安全維護 |
| **ESM** | Expanded Security Maintenance：`esm-infra`（main 延長到 10 年）與 `esm-apps`（universe 補上安全更新） | attach 後會加進 APT 來源，並被 §1.1 的 `Allowed-Origins` 自動套用 |
| **Livepatch** | 用核心的 livepatch 機制，把安全修補以模組形式熱載入正在執行的核心 | 只補安全漏洞不補功能，也不是每個 CVE 都做得到；仍要定期重開機換到新核心（§1.3） |
| **FIPS** | 美國政府的加密模組驗證標準（FIPS 140）；FIPS 模式會禁用未經驗證的演算法 | 一般伺服器不需要；🔵 Fedora 的對應是 §3.1 的 `FIPS` crypto-policy |
| **USG / CIS** | USG 是 Ubuntu 的合規工具；CIS 是它對照的業界安全基準 | 屬於 §10 的合規檢查，這裡只是 Ubuntu Pro 的其中一項功能 |

**為什麼**：核心提權漏洞的修補通常要重開機，重開機又要排維護窗口，中間那幾天就是暴露期，Livepatch 補的就是這段空窗。ESM 則解決「universe 套件沒人負責安全更新」的問題——很多 Python / PHP 函式庫都在 universe，正是常被利用的目標。

**怎麼做**：

- 🟠 **Ubuntu Pro**（個人 5 台免費）：`sudo pro attach <token>`；提供 ESM（universe 套件 10 年安全更新）、**Livepatch**（核心免重開機修補）、FIPS、CIS/USG 工具。`pro status`、`pro enable livepatch`。
- 🔵 Fedora 無 livepatch；核心更新頻繁（每 1~2 週），搭配 `reboot = when-needed` 或維護窗口重開機。

### 1.3 需重開機提醒

**為什麼**：更新了核心或 glibc（幾乎所有程式都載入的 C 函式庫，換了新版但正在跑的程序仍用著舊的）但沒重開機，等於補丁沒生效，而這種狀態沒人提醒就會持續到下一次「意外」重開機。把這兩條放進監控或 MOTD。

**怎麼做**：

```bash
[ -f /var/run/reboot-required ] && cat /var/run/reboot-required.pkgs     # 🟠
sudo dnf needs-restarting -r                                             # 🔵（rc=1 需重開機）
```

---

## 2. 帳號與認證

**為什麼**：帳號是攻擊者的第一個落腳點：撞庫（別處外洩的密碼拿來試）、猜弱密碼、以及拿到低權限帳號後靠 sudo 設錯或 SUID 程式（執行時暫時以檔案擁有者——通常是 root——的身分執行的程式）提權。本節依「讓密碼難猜」→「猜錯就鎖」→「拿到密碼也不夠」→「進來後找不到後門」四個層次處理。

### 2.1 密碼政策

**名詞與關係**：Linux 的登入驗證不是各程式自己寫的，而是全部交給 PAM 這一層；密碼複雜度、失敗鎖定、TOTP（§2.3）都是插進 PAM 堆疊的模組，所以先看這條鏈：

```
login / sshd / sudo / su ──呼叫──▶ PAM（/etc/pam.d/<服務名>：每個服務一份「模組堆疊」，由上而下逐一執行）
                                    ├─ pam_faillock.so preauth   驗證前：這個帳號是否已被鎖？
                                    ├─ pam_unix.so               核心：對照 /etc/shadow 的密碼雜湊
                                    ├─ pam_faillock.so authfail  驗證失敗：計數 +1，達 deny 次就鎖 unlock_time 秒
                                    └─ pam_pwquality.so          只在「改密碼」（passwd）時介入：檢查新密碼強度
        堆疊由誰產生？  🔵 authselect（選 profile + 開關功能）  /  🟠 pam-auth-update 或手動編輯 common-auth
        到期政策不在 PAM：/etc/login.defs（新帳號預設）與 chage（既有帳號）寫的是 /etc/shadow 的到期欄位
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **PAM** | Pluggable Authentication Modules：一套共用的驗證框架，程式只問 PAM「這個人能不能登入」，實際規則由 `/etc/pam.d/` 裡的模組堆疊決定 | pwquality、faillock、google-authenticator 都是 PAM 模組；改壞 PAM 會讓所有登入路徑一起失效 |
| **pam_pwquality** | 檢查新密碼強度的 PAM 模組，規則讀 `/etc/security/pwquality.conf` | 只在設定 / 更改密碼時生效，對既有弱密碼無效；🟠 要另裝 `libpam-pwquality`，🔵 預設已在堆疊裡 |
| **pam_faillock** | 記錄登入失敗次數並暫時鎖定帳號的 PAM 模組，規則讀 `/etc/security/faillock.conf`，計數存在 `/var/run/faillock/` | 要在堆疊裡放兩次（`preauth` 與 `authfail`）才完整；`faillock` 指令查詢 / 重設的是同一份計數 |
| **authselect** | 🔵 Fedora 產生與管理 `/etc/pam.d/` 的工具：先選一個 profile（`local`、`sssd`…）再開關功能（`with-faillock`） | 有它就不要手改 pam.d，否則下次 `authselect apply-changes` 會覆蓋；🟠 Ubuntu 沒有 |
| **pam-auth-update / common-auth** | 🟠 Ubuntu 的對應機制：`common-auth` 等共用檔被各服務 include，`pam-auth-update` 依 `/usr/share/pam-configs/` 的片段組裝它們 | 內建片段沒有 faillock，所以本節說要自建片段或手動加行 |
| **/etc/login.defs、chage** | 前者是新帳號的密碼到期預設（`PASS_MAX_DAYS` 等），後者修改既有帳號 | 與 PAM 無關，寫的是 `/etc/shadow` 的到期欄位；`chage -l alice` 可查 |

**為什麼**：字典攻擊與撞庫靠的是短密碼、常見詞、一個密碼用到永遠。`pam_pwquality` 在設密碼時就擋掉弱密碼；`pam_faillock` 讓線上暴力猜測 5 次就鎖，把每秒上千次的嘗試變成每 15 分鐘 5 次。副作用是使用者手滑被鎖時需要管理員 `faillock --reset`。

**怎麼做**：

```bash
# 密碼複雜度：pam_pwquality（🟠 apt install libpam-pwquality 後自動加入 PAM；🔵 預設已整合）
sudo vim /etc/security/pwquality.conf
#   minlen = 14
#   minclass = 3          # 至少 3 類（大小寫、數字、符號）
#   maxrepeat = 3
#   dictcheck = 1
#   usercheck = 1
#   enforce_for_root
# 到期政策 /etc/login.defs（新帳號）：PASS_MAX_DAYS 90、PASS_MIN_DAYS 1、PASS_WARN_AGE 14
sudo chage -M 90 -m 1 -W 14 alice            # 既有帳號

# 登入失敗鎖定：pam_faillock（兩者都有 /etc/security/faillock.conf，實測存在）
sudo vim /etc/security/faillock.conf
#   deny = 5
#   unlock_time = 900
#   even_deny_root
# 🔵 啟用：sudo authselect select local with-faillock   （Fedora 42+ 預設 profile 為 local；舊版為 sssd）
# 🟠 啟用：在 /etc/pam.d/common-auth 手動加 pam_faillock 行（auth required pam_faillock.so preauth / authfail），
#         或用 pam-auth-update（/usr/share/pam-configs/ 需自建 faillock 設定檔）
sudo faillock --user alice; sudo faillock --user alice --reset
```

**注意**：🟠 Ubuntu 沒有 authselect，手動改 `common-auth` 前先留一個 root shell 開著測試，PAM 寫錯會讓所有人登不進去。`even_deny_root` 在只有密碼 console 的環境可能把自己鎖在外面，確認有金鑰或 out-of-band 存取再開。

### 2.2 sudo 強化

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **sudoers / sudoers.d** | sudo 的政策檔：誰可以用誰的身分執行什麼；`/etc/sudoers` 是主檔，`/etc/sudoers.d/*` 是被 `@includedir` 載入的片段 | 本節所有 `Defaults` 都寫進 `sudoers.d/10-hardening`，不動主檔，套件升級才不會衝突 |
| **visudo** | 編輯 sudoers 的專用工具：存檔前檢查語法，避免寫錯後全員失去 sudo | `-f` 指定編輯 `sudoers.d` 內的片段；直接用 `vim` 改壞了會連 `sudo visudo` 都跑不了 |
| **Defaults** | sudoers 內的全域選項行，改變 sudo 的行為而非授權 | `use_pty`、`logfile`、`timestamp_timeout` 等都是 Defaults 選項 |
| **NOPASSWD** | 授權規則的標籤，允許不輸密碼就執行 | 讓「偷到一個已登入的 session」直接等於 root，是本節要移除的東西 |
| **use_pty** | 強制 sudo 在新的偽終端（pty）內執行指令，讓子程序碰不到呼叫者原本的終端 | 沒有它，惡意程式可用 `TIOCSTI` 之類的 ioctl 把按鍵注入回你的 shell（終端劫持） |
| **timestamp_timeout** | sudo 記住密碼的分鐘數（預設 15） | 太長時離開座位的 session 可被人接手；`passwd_tries` 則限制每次輸錯的次數 |
| **env_reset / secure_path** | 執行前清空環境變數、並固定 `PATH` | 防止用 `LD_PRELOAD`、自訂 `PATH` 讓 sudo 執行到假的指令 |

**為什麼**：拿到一般帳號後，sudo 是最直接的提權路徑：`NOPASSWD` 讓偷到的 session 直接變 root；沒有 `use_pty` 時，透過 sudo 執行的惡意程式能把按鍵注入回呼叫者的終端；`timestamp_timeout` 太長則讓離開座位的 session 被人接手。獨立日誌是事後追查「誰用 sudo 做了什麼」的依據。

**怎麼做**：

```bash
sudo visudo -f /etc/sudoers.d/10-hardening
#   Defaults use_pty                     # 防止 sudo 內的程式劫持終端
#   Defaults logfile="/var/log/sudo.log" # 獨立日誌
#   Defaults timestamp_timeout=5         # 5 分鐘要重輸密碼
#   Defaults passwd_tries=3
#   Defaults !visiblepw
#   Defaults env_reset, secure_path="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
# 審計每一條 sudo：journalctl -t sudo
# 移除不需要的 NOPASSWD；限制群組 → 見 02 章
```

### 2.3 兩步驟驗證（TOTP）

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **2FA / 雙因子** | 登入要同時提供兩種不同類別的證明：你知道的（密碼）、你擁有的（金鑰、手機）、你本身（生物特徵） | 「金鑰 + TOTP」是兩個因子；「密碼 + 金鑰」也是 |
| **TOTP** | Time-based One-Time Password（RFC 6238）：由共享密鑰與目前時間算出的 6 位數碼，每 30 秒換一次 | Google Authenticator、Authy、1Password 等 App 都是 TOTP 產生器；伺服器與手機時間差太大就會驗證失敗 |
| **google-authenticator / pam_google_authenticator** | 前者是產生 TOTP 密鑰與 QR code 的指令（寫進 `~/.google_authenticator`），後者是驗證動態碼的 PAM 模組 | 就是 §2.1 那條 PAM 堆疊的另一個模組，掛在 `/etc/pam.d/sshd` 讓 sshd 的互動式提示改問動態碼 |
| **nullok** | pam_google_authenticator 的選項：使用者還沒有 `~/.google_authenticator` 時直接放行 | 佈署過渡用，全員設定完要拿掉 |
| **KbdInteractiveAuthentication** | sshd 的「鍵盤互動式」認證：把提示交給 PAM 決定要問什麼 | §3 的強化設定會關掉它（因為它也能問密碼）；要用 TOTP 就得開，並靠 `AuthenticationMethods` 保證它不會單獨使用 |
| **AuthenticationMethods** | sshd 設定：列出「必須依序全部通過」的認證方法，逗號分隔 | `publickey,keyboard-interactive` = 先驗金鑰、再走 PAM 問 TOTP，兩關都過才登入 |
| **Match** | sshd_config 的條件區塊，依使用者 / 群組 / 來源 IP 覆寫設定 | 自動化帳號可用 `Match User backup` 把 `AuthenticationMethods` 改回只用 `publickey` |

**為什麼**：密碼或私鑰外洩（筆電被偷、`~/.ssh` 被惡意 npm 套件讀走）後，單因子就全盤皆輸；TOTP 讓攻擊者還得拿到手機上的動態碼。對外開放 SSH 又無法限制來源 IP 時尤其值得做。代價是每次登入多一步，自動化腳本要另開只用金鑰的帳號或以 `Match` 例外處理。

**怎麼做**：

```bash
sudo apt install -y libpam-google-authenticator   # 🟠
sudo dnf install -y google-authenticator          # 🔵
google-authenticator -t -d -f -r 3 -R 30 -W       # 每個使用者執行，產生 ~/.google_authenticator
# /etc/pam.d/sshd 最上方加：auth required pam_google_authenticator.so nullok   （nullok = 未設定的使用者可跳過，佈署完拿掉）
# sshd_config drop-in：
#   KbdInteractiveAuthentication yes
#   AuthenticationMethods publickey,keyboard-interactive     # 金鑰 + TOTP 雙因子
sudo systemctl restart ssh   # 🟠  /  sshd  # 🔵
# 🔵 SELinux：家目錄的 .google_authenticator 需正確標籤（restorecon ~/.google_authenticator）
```

**注意**：`nullok` 只是佈署期的過渡，全員設定完就拿掉，否則沒設 TOTP 的帳號等於沒有第二因子。

### 2.4 帳號清查

**名詞與關係**：下面每一條指令各對應一種後門型態：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **UID 0** | Linux 只看數字判斷 root：任何 UID 為 0 的帳號都是 root，不管名字叫什麼 | 攻擊者最愛加一個名字普通、UID 卻是 0 的帳號；看 `/etc/passwd` 第三欄 |
| **登入 shell（`nologin` / `false`）** | `/etc/passwd` 第七欄；設成 `/usr/sbin/nologin` 或 `/bin/false` 的帳號不能互動登入 | 服務帳號（www-data、postgres）本該是 nologin，被改成 `/bin/bash` 就是後門 |
| **SUID / SGID** | 檔案權限位元：執行時以檔案擁有者 / 群組的身分執行（`-perm -4000` / `-2000`） | 正常系統只有 `passwd`、`sudo` 等少數幾個；多出一個 SUID 的 bash 副本就是提權後門 |
| **無主檔案** | 擁有者 UID / GID 在 `/etc/passwd`、`/etc/group` 裡已不存在的檔案 | 通常是刪帳號後殘留，也可能是攻擊者建的帳號被刪後留下的痕跡 |
| **全域可寫（world-writable）** | 其他人（other）有寫入權限的檔案（`-perm -0002`） | 若是被 root 的 cron 執行的腳本，任何人都能改它的內容 = 提權 |

**為什麼**：入侵後的持久化手法很固定：多加一個 UID 0 帳號、給某個服務帳號一個 shell、留一個 SUID 的 bash 副本、在全域可寫目錄放後門。定期跑這幾條就是在找這些痕跡，順便揪出安裝套件時留下、根本不該能登入的帳號。

**怎麼做**：

```bash
awk -F: '$3 == 0' /etc/passwd                           # UID 0 只能有 root
awk -F: '($2 == "" )' /etc/shadow                       # 空密碼
awk -F: '$7 !~ /nologin|false/ {print $1, $7}' /etc/passwd   # 可登入 shell 的帳號
sudo lastlog | grep -v "Never"                          # 有登入過的
sudo find / -xdev \( -perm -4000 -o -perm -2000 \) -type f -exec ls -l {} \; 2>/dev/null   # SUID/SGID
sudo find / -xdev -nouser -o -nogroup 2>/dev/null       # 無主檔案
sudo find / -xdev -type f -perm -0002 2>/dev/null       # 全域可寫檔案
```

---

## 3. SSH 強化（完整版）

**名詞與關係**：一條 SSH 連線的建立順序決定了設定檔裡各參數的作用位置，先看這條鏈，再看每個參數在擋什麼：

```
TCP 連線 ──▶ ① 金鑰交換 KEX ──▶ ② 驗證主機金鑰 HostKey ──▶ ③ 對稱加密通道 Cipher + MAC ──▶ ④ 使用者認證 ──▶ ⑤ session
              KexAlgorithms         HostKeyAlgorithms            Ciphers / MACs                 AuthenticationMethods     X11 / Agent / TCP Forwarding
              （擋降級攻擊）        （只留 ed25519 / rsa-sha2）   （淘汰 CBC、SHA-1）            publickey / password /     PermitTunnel、GatewayPorts
                                                                                              keyboard-interactive       （被入侵的帳號能否當跳板）
🔵 Fedora：①②③ 的演算法清單由 crypto-policies（§3.1）集中產生，sshd_config 內同名的行會被忽略
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **sshd_config.d/ drop-in** | sshd 先讀 `/etc/ssh/sshd_config.d/*.conf` 再讀主檔，**同一參數以第一次出現為準** | 所以檔名的數字排序決定誰贏：🔵 要壓過 `40-redhat-crypto-policies.conf`、`50-redhat.conf` 就得小於 40 |
| **KEX（KexAlgorithms）** | Key Exchange，雙方協商出本次連線對稱金鑰的演算法 | 只留 curve25519 / sntrup761（後量子混合）、拿掉 DH group1 與 SHA-1 類，降級攻擊就沒有可退的選項 |
| **HostKey / HostKeyAlgorithms** | 伺服器的身分金鑰（`/etc/ssh/ssh_host_*_key`）與允許用來簽署的演算法 | 客戶端 `known_hosts` 記的就是它；DSA 已不安全、ECDSA 依賴 NIST 曲線，故只留 ed25519 與 rsa-sha2 |
| **Cipher（Ciphers）** | 通道的對稱加密演算法 | 只留 AEAD 類（chacha20-poly1305、AES-GCM）；CBC 模式有 padding oracle 類攻擊 |
| **MAC（MACs）** | Message Authentication Code，驗證每個封包沒被竄改（與 §5 的 MAC 強制存取控制只是縮寫相同） | 只留 `-etm`（encrypt-then-MAC）的 SHA-2；AEAD cipher 自帶完整性，MAC 只在非 AEAD 時才用到 |
| **AuthenticationMethods / PasswordAuthentication / KbdInteractiveAuthentication** | 使用者認證階段允許的方法 | 見 §2.3；這裡全關只留 `publickey`，要加 TOTP 再依 §2.3 開 keyboard-interactive |
| **MaxAuthTries / MaxStartups / LoginGraceTime** | 單一連線可嘗試認證的次數 / 同時處於「尚未認證」狀態的連線上限（`10:30:60` = 超過 10 條開始隨機拒絕，到 60 條全拒）/ 認證必須在幾秒內完成 | 三者都在限制暴力嘗試能占用的資源；§4 的 Fail2ban 從來源 IP 這一端再補一層 |
| **AllowGroups / Match** | 白名單群組；條件式覆寫 | `AllowGroups sshusers` 讓沒加進群組的帳號一律拒絕；例外用 `Match User` 個別放寬 |
| **Forwarding（X11 / Agent / TCP）、PermitTunnel、GatewayPorts** | session 建立後可額外開的通道：轉發圖形、轉發 ssh-agent、埠轉發（`ssh -L/-R`）、VPN 式 tun 裝置 | 全關後被入侵的帳號無法拿主機當跳板；但 VS Code Remote、`ssh -L` 也會失效 |
| **sshd -t / sshd -T** | 前者檢查設定語法，後者印出「合併所有 drop-in 後」的實際生效值 | 改完一定先 `-t` 再 reload；`-T` 是確認 drop-in 順序有沒有搞錯的唯一方法 |
| **ssh-audit** | 第三方工具，從外部連線列出伺服器支援的演算法並標出弱項 | 用來驗證 KEX / Cipher / MAC 設定真的生效（🔵 也能看出 crypto-policies 實際給了什麼） |
| **金鑰指紋（fingerprint）** | 公鑰的 SHA256 雜湊；`LogLevel VERBOSE` 時每次登入都會記在日誌 | 追查「哪把鑰匙被拿走」的依據；`ssh-keygen -lf 公鑰檔` 可算出來 |

**為什麼**：SSH 是伺服器唯一必開的遠端管理入口，也是被暴力猜測最多的服務。這份設定一次做完幾件事：禁密碼、禁 root（攻擊者連帳號名都得猜）、限制群組（新建的服務帳號不會意外開放登入）、關掉轉發（被入侵的帳號不能拿主機當跳板進內網）、限制演算法（擋降級攻擊、淘汰 SHA-1 / CBC）。`LogLevel VERBOSE` 會記下登入用的金鑰指紋，追查時才知道是誰的鑰匙被拿走了。

**怎麼做**：sshd 會讀 drop-in 目錄，同一參數以先讀到的為準。`/etc/ssh/sshd_config.d/10-hardening.conf`（承接 02 章；🔵 Fedora 檔名數字需小於 40 以覆蓋 `40-redhat-crypto-policies.conf`、`50-redhat.conf`）：

```
Port 22
AddressFamily inet
ListenAddress 0.0.0.0
Protocol 2
PermitRootLogin no
PubkeyAuthentication yes
PasswordAuthentication no
KbdInteractiveAuthentication no
ChallengeResponseAuthentication no
AuthenticationMethods publickey
PermitEmptyPasswords no
MaxAuthTries 3
MaxSessions 5
MaxStartups 10:30:60
LoginGraceTime 30
ClientAliveInterval 300
ClientAliveCountMax 2
X11Forwarding no
AllowAgentForwarding no
AllowTcpForwarding no
PermitTunnel no
GatewayPorts no
UseDNS no
Banner /etc/issue.net
LogLevel VERBOSE
AllowGroups sshusers
# 🟠 Ubuntu 可直接指定演算法（🔵 Fedora 由 crypto-policies 管理，這幾行會被忽略）
KexAlgorithms sntrup761x25519-sha512@openssh.com,curve25519-sha256,curve25519-sha256@libssh.org
Ciphers chacha20-poly1305@openssh.com,aes256-gcm@openssh.com,aes128-gcm@openssh.com
MACs hmac-sha2-512-etm@openssh.com,hmac-sha2-256-etm@openssh.com
HostKeyAlgorithms ssh-ed25519,ssh-ed25519-cert-v01@openssh.com,rsa-sha2-512,rsa-sha2-256
```

寫完先把自己加進 `sshusers`、用 `sshd -t` 檢查語法再 reload，並保留現有連線，直到另開一個新連線確認登得進去：

```bash
sudo groupadd sshusers && sudo usermod -aG sshusers alice
sudo sshd -t && sudo sshd -T | grep -E "^(permitrootlogin|passwordauthentication|ciphers|kexalgorithms)"
sudo systemctl reload ssh     # 🟠   /   sshd   # 🔵
# 只留 ed25519 host key
sudo rm /etc/ssh/ssh_host_{dsa,ecdsa}_key*     # 若存在
# 稽核工具
pipx install ssh-audit && ssh-audit localhost
```

**注意**：`AllowGroups sshusers` 漏加自己會立刻被鎖在外面；`AllowTcpForwarding no` 會讓 `ssh -L` 與 VS Code Remote 之類的工具失效，需要的人用 `Match User` 個別放行。

### 3.1 🔵 Fedora crypto-policies（系統層級加密策略）

**名詞與關係**：crypto-policies 不是加密函式庫，而是「替所有加密函式庫產生設定檔」的機制，先看資料怎麼流：

```
/etc/crypto-policies/config  ＝  一個策略名稱（DEFAULT / LEGACY / FUTURE / FIPS），可疊子策略：DEFAULT:NO-SHA1
        │  update-crypto-policies --set 〈策略〉
        ▼
/usr/share/crypto-policies/policies/*.pol（+ modules/*.pmod 子策略）──產生──▶ /etc/crypto-policies/back-ends/*.config
                                                                                    ├─ opensshserver.config → sshd 讀（所以 §3 那幾行演算法會被忽略）
                                                                                    ├─ openssl.config       → OpenSSL：nginx、curl、Python…
                                                                                    └─ gnutls / nss / java / krb5 / libreswan .config …
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **crypto-policies** | 🔵 Fedora / RHEL 的系統層加密策略套件：用一個策略名稱控制所有加密函式庫允許的演算法、金鑰長度與協定版本 | 它替各函式庫**產生設定檔**（back-ends），函式庫再各自讀取，這就是「一次設定、全系統一致」的機制 |
| **update-crypto-policies** | 切換 / 顯示目前策略的指令（`crypto-policies-scripts` 套件） | 執行後重新產生所有 back-end 設定檔；服務要重啟才讀到新值 |
| **策略（DEFAULT / LEGACY / FUTURE / FIPS / BSI / NEXT）** | 內建的幾個等級：LEGACY 相容舊裝置、DEFAULT 現行安全、FUTURE 為未來十年、FIPS 只留 FIPS 140 核准的演算法 | 越往右越嚴，但也越容易讓舊客戶端連不上 |
| **子策略（`:NO-SHA1` 等 `.pmod`）** | 疊在主策略上的增減片段，用冒號串接 | 想「DEFAULT 但禁 SHA-1」不必自訂整份策略；自訂片段放 `policies/modules/MYPOL.pmod` |
| **back-ends** | `/etc/crypto-policies/back-ends/` 內每個函式庫一份的實際設定檔 | OpenSSH、OpenSSL、GnuTLS、NSS、Java、Kerberos、libreswan 各讀自己那份 |
| **FIPS / fips-mode-setup** | FIPS 140 是美國政府的加密模組驗證標準；`fips-mode-setup --enable` 除了切策略還會加核心參數 `fips=1` 並重建 initramfs | 單純 `--set FIPS` 只換策略，不等於完整的 FIPS 模式 |
| **OpenSSL / GnuTLS / NSS** | 三個常見的 TLS / 加密函式庫：nginx、curl 用 OpenSSL；GNOME 類程式用 GnuTLS；Firefox、Chrome 用 NSS | 🟠 Ubuntu 沒有 crypto-policies，這三者要各自設定（如 `openssl.cnf`） |

Red Hat 系把「哪些加密演算法可用」集中在 crypto-policies 一處管理，是因為過去每個程式各自設定，要淘汰 SHA-1 或 TLS 1.0 時得改幾十個設定檔還常漏掉；Ubuntu 沿用各上游套件的預設值，沒有這一層。

**為什麼**：舊演算法（SHA-1 簽章、RSA 1024、CBC 模式、TLS 1.0 / 1.1）已有實際可行的攻擊或降級手法；用一個指令把全系統切到同一基準，比逐一檢查 sshd、nginx、Java、Kerberos 可靠得多。副作用是老舊客戶端（舊交換器、嵌入式裝置）可能連不上，切換前先盤點。

**怎麼做**：

```bash
sudo dnf install -y crypto-policies-scripts       # 標準安裝已含（容器最小環境需另裝）
update-crypto-policies --show                     # 實測：DEFAULT
sudo update-crypto-policies --set DEFAULT:NO-SHA1 # 子策略（實測有 NO-SHA1、NO-CAMELLIA、NO-PQ、ECDHE-ONLY、AD-SUPPORT…）
sudo update-crypto-policies --set FUTURE          # 更嚴格（RSA ≥ 3072、禁 SHA-1 / CBC）
sudo update-crypto-policies --set FIPS            # 或 fips-mode-setup --enable
ls /usr/share/crypto-policies/policies/           # DEFAULT LEGACY FUTURE FIPS BSI NEXT …
cat /etc/crypto-policies/back-ends/opensshserver.config      # sshd 實際套用的演算法
# 自訂：/etc/crypto-policies/policies/modules/MYPOL.pmod → update-crypto-policies --set DEFAULT:MYPOL
# 影響：OpenSSH、OpenSSL、GnuTLS、NSS、Java、Kerberos、libreswan 全部一致
```

**注意**：🟠 Ubuntu 沒有對應機制；各服務個別設定。OpenSSL 全域可在 `/etc/ssl/openssl.cnf` 設 `MinProtocol = TLSv1.2`、`CipherString = DEFAULT@SECLEVEL=2`。

---

## 4. 入侵防禦：Fail2ban（已實測）

**名詞與關係**：Fail2ban 是一條「讀日誌 → 比對 → 計數 → 叫防火牆封鎖」的管線，設定檔裡的每個名詞對應管線的一段：

```
日誌來源 backend ──▶ filter（正則抓「登入失敗 + 來源 IP」）──▶ jail（同一 IP 在 findtime 內失敗 ≥ maxretry？）──▶ action / banaction ──▶ 防火牆規則（封 bantime）
 systemd = journald            filter.d/sshd.conf                   jail.local 的 [sshd] 區段                          action.d/<banaction>.conf        🟠 nftables 或 ufw
 auto / pyinotify = 讀檔案      fail2ban-regex 可單獨測它            bantime.increment：累犯乘倍                         action_mwl = 封 + 寄信 + 附日誌     🔵 firewallcmd-rich-rules
                                                                    ignoreip：白名單永不封                                                              （解封也由同一 action 做）
[recidive] 是特殊 jail：讀 fail2ban 自己的 /var/log/fail2ban.log，把短期內被封多次的 IP 封更久
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Fail2ban** | 讀日誌、找出重複失敗的來源 IP、呼叫防火牆暫時封鎖的守護程式（Python） | 依賴日誌與防火牆，本身不擋任何封包；防火牆後端見 05 章 |
| **jail** | 一組「用哪個 filter 看哪個日誌、閾值多少、封鎖用哪個 action」的設定單元；`[sshd]`、`[nginx-http-auth]` 各是一個 | `jail.conf` 是套件預設（升級會覆蓋），`jail.local` 與 `jail.d/*.conf` 是使用者覆寫，`[DEFAULT]` 區段給所有 jail 繼承 |
| **filter** | `filter.d/*.conf` 內的正則表示式，定義什麼樣的日誌行算「一次失敗」並抓出 `<HOST>` | `mode = aggressive` 是 sshd filter 提供的多組正則之一；`fail2ban-regex` 用來測 filter 對不對 |
| **backend** | jail 取得日誌的方式：`systemd` 直接查 journald，`auto` / `pyinotify` / `polling` 讀文字檔 | 🔵 Fedora 預設沒有 rsyslog 寫 `/var/log/secure`，用讀檔案的 backend 會讀不到東西，所以要明確指定 `systemd`；🟠 Debian 套件已預設 |
| **bantime / findtime / maxretry** | 封多久 / 觀察窗口多長 / 窗口內允許幾次失敗 | `bantime.increment` 讓同一 IP 每次再被封時 bantime 乘倍，上限 `bantime.maxtime` |
| **action / banaction** | action 是封鎖時要做的整套動作（封 + 寄信…），banaction 是其中「呼叫哪個防火牆」的那一項 | `%(action_mwl)s` 展開後會用到 `banaction`；banaction 對應 `action.d/` 內的檔案（`nftables`、`ufw`、`firewallcmd-rich-rules`） |
| **ignoreip** | 永不封鎖的 IP / 網段白名單 | 沒把自己的管理網段放進去，就會把自己封掉 |
| **recidive** | 內建的「累犯」jail，資料來源是 fail2ban 自己的日誌 | 與 `bantime.increment` 目的相近，兩者可並用 |
| **MTA** | Mail Transfer Agent（postfix、msmtp），能把信送出去的本機程式 | `action_mwl` 的寄信部分需要它；沒有就用純封鎖的 `action_` |
| **CrowdSec** | 同類的開源替代品：多了社群共享的惡意 IP 情報（別人被打過的 IP 你先封） | 架構與 Fail2ban 相同（讀日誌 → 判定 → bouncer 封鎖），可擇一 |
| **WireGuard** | 現代 VPN 協定，核心內建 | SSH 只對 VPN 內開放後，公網上根本看不到 22 埠，暴力嘗試就沒有起點；設定見 05 章 |

**為什麼**：就算已經禁密碼登入，暴力嘗試仍會持續占用 sshd 連線數（`MaxStartups`）與塞滿日誌；對還開著密碼登入的 web 後台（nginx basic auth、WordPress）更是直接威脅。Fail2ban 讀日誌，把短時間內多次失敗的來源 IP 丟進防火牆封鎖，「累犯加倍」讓分散式慢速猜測也划不來。它不是防火牆的替代品——來源限制與金鑰登入仍是第一線。

**怎麼做**：安裝：

```bash
sudo apt install -y fail2ban    # 🟠 1.0.2
sudo dnf install -y fail2ban    # 🔵 1.1.0（自動拉入 fail2ban-firewalld，實測 jail.d/00-firewalld.conf 設 banaction = firewallcmd-rich-rules）
```

`/etc/fail2ban/jail.local`（不要改 `jail.conf`，升級會覆蓋）：

```ini
[DEFAULT]
bantime  = 1h
findtime = 10m
maxretry = 5
bantime.increment = true          # 累犯加倍
bantime.maxtime   = 1w
backend  = systemd                # 讀 journald（🟠 實測 jail.d/defaults-debian.conf 已預設 systemd + nftables；🔵 需指定）
ignoreip = 127.0.0.1/8 ::1 192.168.1.0/24
destemail = ops@example.com
action = %(action_mwl)s           # 封鎖 + 寄信含日誌（需 MTA）

# 🟠 banaction = ufw            （預設 nftables 也可；ufw 用戶建議改 ufw 以便 ufw status 看得到）
# 🔵 banaction = firewallcmd-rich-rules   （套件預設，不必寫）

[sshd]
enabled = true
mode    = aggressive              # 也抓 "Connection closed by authenticating user" 等

[nginx-http-auth]
enabled = true
[nginx-botsearch]
enabled = true
[recidive]                        # 重複被封的 IP 封更久
enabled  = true
bantime  = 1w
findtime = 1d
```

啟用後用 `status sshd` 確認真的有在讀日誌、有抓到東西：

```bash
sudo systemctl enable --now fail2ban
sudo fail2ban-client status
sudo fail2ban-client status sshd            # 實測 Journal matches: _SYSTEMD_UNIT=sshd.service + _COMM=sshd（🟠 為 ssh.service 但 fail2ban 仍用 sshd.service 過濾，實測可運作）
sudo fail2ban-client set sshd unbanip 1.2.3.4
sudo fail2ban-client set sshd banip 5.6.7.8
sudo fail2ban-regex /var/log/auth.log /etc/fail2ban/filter.d/sshd.conf     # 測試過濾器（🔵 用 systemd-journal 當來源）
sudo tail -f /var/log/fail2ban.log
```

**注意**：`ignoreip` 一定要含自己的管理網段與跳板機，否則打錯幾次密碼就把自己封 1 小時；`action_mwl` 需要能寄信的 MTA，沒有就用預設的 `action_`。

替代：**CrowdSec**（社群威脅情報、兩者有官方 repo）、SSH 改用 **WireGuard 後才開放**（最有效）。

---

## 5. 強制存取控制

**名詞與關係**：Linux 每一次存取（開檔、綁埠、送訊號）都要過兩道關，DAC 先、MAC 後，兩道都放行才成功：

```
程序（以 www-data 身分）要開 /home/alice/secret
   │
   ├─ ① DAC（Discretionary Access Control）：看檔案的 owner / group / rwx 位元對上程序的 UID / GID。root 一律通過。
   │        ↓ 通過
   └─ ② MAC（Mandatory Access Control）：看系統政策——「這個程序（依它的 profile / label）被允許碰這個檔案嗎？」root 也不例外。
            🟠 AppArmor：政策 = 以執行檔路徑為單位的 profile（/etc/apparmor.d/usr.sbin.nginx），列出可碰的路徑與網路
            🔵 SELinux ：政策 = 標籤規則；每個程序、檔案、埠都貼 label（context），規則寫「httpd_t 可讀 httpd_sys_content_t」
            兩者都有「只記錄不阻擋」模式：AppArmor 的 complain / SELinux 的 permissive；正式運作是 enforce / enforcing
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **DAC** | 傳統 Unix 權限：檔案擁有者自行決定（discretionary）誰能讀寫，root 不受限 | 永遠先檢查；服務跑在 www-data 這種帳號下，DAC 准它讀的東西（`/home` 若 o+r、`/tmp`）它都能讀 |
| **MAC** | 由系統政策強制（mandatory）決定，程序與擁有者都改不了（與 §3 的 MAC 訊息驗證碼只是縮寫相同） | 在 DAC 之後再加一層；AppArmor 與 SELinux 是兩種實作，**一台機器只跑一種** |
| **RCE** | Remote Code Execution，攻擊者透過服務漏洞在伺服器上執行任意指令 | MAC 的前提就是「服務終究會被 RCE」，所以先把它能碰的東西圈死 |
| **www-data** | 🟠 Ubuntu 上 nginx / Apache / PHP-FPM 的執行帳號（🔵 Fedora 為 `nginx` / `apache`） | 服務被 RCE 後攻擊者拿到的就是這個身分；MAC 限制的對象也是它 |
| **AppArmor** | 🟠 Ubuntu / SUSE 採用的 MAC，以路徑為單位（path-based） | profile 是它的政策單位；檔案搬離 profile 寫的路徑就脫離管制，見 §5.1 |
| **SELinux** | 🔵 Fedora / RHEL 採用的 MAC（源自 NSA），以標籤為單位（label-based） | 標籤跟著檔案走，搬檔案要 `restorecon` 重貼；細節與工具見 §5.2 |
| **profile / label（context）** | AppArmor 的政策檔 / SELinux 貼在每個物件上的標籤 | 兩者扮演同一個角色（「這個東西適用哪條規則」）的不同載體 |
| **enforce / complain（AppArmor）、enforcing / permissive（SELinux）** | 阻擋並記錄 / 只記錄不阻擋 | 除錯時切到記錄模式收集拒絕紀錄，確認無誤再切回強制 |

**為什麼**：前面幾節都在擋「進不來」，MAC 處理的是「服務被攻破之後」——nginx 或 PHP 被 RCE，攻擊者拿到的是 www-data 權限，傳統 DAC 下它照樣能讀 `/home`、連外拉 payload、寫進 `/tmp` 再執行。AppArmor / SELinux 把每個服務限制在政策允許的檔案、埠與能力內，超出就拒絕並留紀錄。代價是搬檔案、改埠後常遇到「莫名其妙的 Permission denied」，所以本節主要教怎麼查，而不是怎麼關。

兩個發行版的 MAC 走的是不同設計哲學：**AppArmor**（Ubuntu）以**路徑**為單位寫 profile，一個程式一個檔案，好讀、好寫、好除錯，但檔案搬到別的路徑就脫離管制；**SELinux**（Fedora / RHEL，源自 NSA）以**標籤**為單位，每個檔案、程序、埠都帶 context，政策決定哪些 context 之間可以互動，覆蓋更完整、能做到多層級安全，但學習曲線陡，標籤錯了服務就起不來。兩者都做到「預設 enforcing、常用服務有現成政策」，日常差別主要在除錯工具。

### 5.1 🟠 AppArmor

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **profile** | 一份文字檔（`/etc/apparmor.d/`），以「執行檔路徑」為開頭，列出它能讀寫執行哪些路徑、能開哪些網路、能用哪些 capability；沒列的一律拒絕 | 檔名慣例 = 路徑把 `/` 換成 `.`；一個程式一份，子程序可用 `px` / `cx` 轉到別的 profile |
| **enforce / complain / disable** | profile 的三種狀態：阻擋並記錄 / 只記錄不阻擋 / 完全不載入 | `aa-enforce`、`aa-complain`、`aa-disable` 切換；`aa-status` 列出各狀態的數量 |
| **apparmor-utils / apparmor-profiles(-extra)** | 前者是 `aa-*` 管理工具套件，後者是社群維護的現成 profile 集（nginx、php-fpm、dovecot…） | 預設只裝核心元件，**不裝** profiles，所以 nginx 之類其實沒被保護 |
| **apparmor_parser** | 把 profile 文字編譯並載入核心的底層工具 | `aa-*` 工具最後都呼叫它；`-r` 重載單一 profile，`systemctl reload apparmor` 重載全部 |
| **aa-genprof / aa-logprof** | 互動式產生新 profile / 依日誌中的 DENIED 逐條問你要不要加進 profile | 標準流程：genprof 建骨架 → complain 跑一陣子 → logprof 補齊 → enforce |
| **abstractions / tunables / abi** | 可 include 的共用規則片段（`base`、`nameservice` 等）/ 變數定義（`@{HOME}`）/ profile 語法版本宣告 | 自訂 profile 幾乎都從 `include <abstractions/base>` 開始，免得連 libc 都要自己列 |
| **DENIED** | 核心日誌（`journalctl -k`、`dmesg`）中 AppArmor 拒絕事件的關鍵字 | complain 模式下同樣記錄但寫作 `ALLOWED`；`aa-logprof` 就是讀這些行 |
| **unprivileged user namespace** | 一般使用者不需 root 就能建立的隔離命名空間，瀏覽器 sandbox 與 rootless 容器都靠它 | 24.04 起 `apparmor_restrict_unprivileged_userns=1` 只允許有 profile 的程式使用，所以 Chrome / Electron 需要 profile |

**為什麼**：Ubuntu 預設只對少數程式（snapd、cups、evince 等）套 profile，nginx 或自己寫的服務通常沒有，等於 MAC 沒在保護真正對外的東西；裝 `apparmor-profiles` 並把重要服務切到 enforce 才有意義。先用 complain 跑一陣子收集 DENIED、確認不影響功能，再切 enforce。

**怎麼做**：

```bash
sudo aa-status                                    # 載入的 profile 與 enforce / complain 數
sudo apt install -y apparmor-utils apparmor-profiles apparmor-profiles-extra
ls /etc/apparmor.d/                               # profile 檔名 = 路徑把 / 換成 .（usr.sbin.nginx）
sudo aa-enforce /etc/apparmor.d/usr.sbin.nginx    # 強制
sudo aa-complain /etc/apparmor.d/usr.sbin.nginx   # 只記錄不阻擋（除錯）
sudo aa-disable /etc/apparmor.d/usr.sbin.nginx
sudo apparmor_parser -r /etc/apparmor.d/usr.sbin.nginx   # 重載單一 profile
sudo systemctl reload apparmor                    # 重載全部
# 產生新 profile
sudo aa-genprof /usr/local/bin/myapp              # 互動：另一終端操作程式，回來按 S 掃描、A 允許
sudo aa-logprof                                   # 依日誌中的 DENIED 更新 profile
# 除錯
sudo journalctl -k -g apparmor                    # 或 dmesg | grep DENIED；/var/log/kern.log（rsyslog）
# 24.04 新增：未特權 user namespace 限制（影響 Chrome、Electron 等）
sysctl kernel.apparmor_restrict_unprivileged_userns    # =1；需要時為程式建 profile 或暫時設 0
```

自訂服務的 profile 就是一份「這個執行檔能碰哪些路徑、開哪些網路」的白名單，沒列的一律拒絕；範例 `/etc/apparmor.d/usr.local.bin.myapp`：

```
abi <abi/4.0>,
include <tunables/global>
/usr/local/bin/myapp {
  include <abstractions/base>
  include <abstractions/nameservice>
  network inet stream,
  /usr/local/bin/myapp mr,
  /etc/myapp/** r,
  /var/lib/myapp/** rwk,
  /var/log/myapp/*.log w,
  deny /home/** rw,
}
```

**注意**：24.04 的 `apparmor_restrict_unprivileged_userns` 會讓沒有 profile 的 Chrome / Electron / 部分容器工具的 sandbox 失敗，症狀是程式起不來或報 `clone` 錯誤；先看 `journalctl -k -g apparmor` 再決定是建 profile 還是放寬。

### 5.2 🔵 SELinux

**名詞與關係**：SELinux 的所有操作都圍繞「標籤」與「政策規則」兩件事，除錯工具則是沿著「拒絕紀錄」往回找：

```
程序 domain ────政策規則允許？────▶ 物件 type
 httpd_t（ps -eZ）                    檔案 httpd_sys_content_t（ls -Z）／埠 http_port_t（semanage port）
      │                               檔案標籤從哪來：semanage fcontext 規則表 ──restorecon──▶ 貼到檔案上（chcon 只是臨時貼、不進表）
      │                               boolean：政策裡預留的開關，setsebool 切換（httpd_can_network_connect = 允許 httpd_t 連外）
      ▼ 被拒
  AVC 拒絕紀錄 ──▶ auditd 寫進 /var/log/audit/audit.log ──▶ ausearch -m avc（原始）／sealert（setroubleshoot 翻譯 + 建議）／audit2why（解釋原因）
                                                              └─ 真的需要放行：audit2allow 產生 .te → 編成 .pp 模組 → semodule -i 載入
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **context / label** | 貼在每個程序、檔案、埠上的標籤，格式 `user:role:type:level`（如 `system_u:object_r:httpd_sys_content_t:s0`） | 日常只看第三欄 **type**；`ls -Z`、`ps -eZ`、`id -Z` 分別看檔案、程序、自己 |
| **type / domain** | type 是標籤的核心欄位；程序的 type 習慣稱 domain（`httpd_t`），檔案的 type 稱 file type（`httpd_sys_content_t`） | 政策規則就是「哪個 domain 可對哪個 type 做什麼」 |
| **targeted 政策** | Fedora 預設的政策：只把網路服務等「目標」關進各自 domain，一般使用者程序在 `unconfined_t` 幾乎不受限 | 所以桌面操作感覺不到 SELinux，服務放錯路徑才會撞到 |
| **enforcing / permissive / disabled** | 阻擋並記錄 / 只記錄 / 完全關閉（連標籤都不維護） | `setenforce` 只切前兩者且重開機失效；`disabled` 要改 `/etc/selinux/config`，改回來時需 `/.autorelabel` 全碟重貼 |
| **fcontext** | 「路徑樣式 → 應有的 type」的規則表（`semanage fcontext -l`） | `restorecon` 依這張表貼標籤；`chcon` 直接改檔案標籤但不進表，下次 restorecon 就被改回，所以永久改要 `semanage fcontext -a` |
| **restorecon / chcon** | 依 fcontext 表重貼標籤 / 直接手動貼標籤 | 複製、搬移、從別處 rsync 來的檔案標籤常常是錯的，第一步永遠是 `restorecon -Rv` |
| **boolean** | 政策作者預留的開關，讓你不用寫政策就能放行常見需求（httpd 連外、連 DB、讀家目錄） | `getsebool -a` 列出、`setsebool -P` 永久切換；比 audit2allow 安全得多 |
| **semanage** | 管理「政策的可調部分」的指令：`fcontext`、`port`、`boolean`、`login`、`user` 子命令 | 改的是規則表而非單一物件；非標準埠要 `semanage port -a` 才准服務綁 |
| **AVC** | Access Vector Cache，SELinux 在核心內的決策快取；「AVC denial」就是它記錄的一次拒絕 | 每筆 AVC 都經 auditd 落地到 `audit.log`，是所有除錯工具的資料來源 |
| **auditd / audit.log** | 核心稽核子系統的守護程式與其日誌檔（見 §8） | SELinux 沒有自己的日誌，全靠 auditd；auditd 沒跑時 AVC 會退到 `journalctl -k` |
| **ausearch / sealert / setroubleshoot / audit2why** | 查 audit.log 的工具 / 把 AVC 翻成人話並給建議指令 / sealert 背後的守護程式 / 解釋「為什麼被拒、有無 boolean 可解」 | 依序使用：ausearch 看原始、audit2why 看原因、sealert 看建議 |
| **audit2allow / semodule / .te / .pp** | 把 AVC 反推成放行規則（`.te` 原始碼）並編成 `.pp` 模組 / 載入、列出、移除模組 | 最後手段；先讀 `.te` 確認沒把攻擊行為一起放行 |
| **`:Z` / `:z`（Podman）** | 容器掛載選項：自動把該目錄貼上容器專用標籤（Z 私有、z 多容器共享） | 容器版的 `chcon`；不加就會在容器內看到 Permission denied |

**為什麼**：Fedora 的 targeted 政策預設已把幾乎所有網路服務關進各自的 domain，所以最常遇到的不是「要不要開」，而是自己把檔案放到非標準路徑（`/srv/www`）、改了非標準埠（8080）、或服務需要連外（PHP 連遠端 DB）時被擋。碰到 AVC 拒絕就 `setenforce 0` 或改 `disabled`，等於把整層防禦丟掉；正確順序是修標籤、開 boolean、最後才寫自訂模組。

**怎麼做**：

```bash
getenforce; sestatus                              # Enforcing / Permissive / Disabled；policy targeted
sudo setenforce 0                                 # 暫時 permissive（重開機恢復；除錯用）
sudo setenforce 1
cat /etc/selinux/config                           # SELINUX=enforcing（⚠️ 設 disabled 後要改回需 touch /.autorelabel 並重開機重標籤）
# 工具（policycoreutils-python-utils、setroubleshoot-server，實測皆可安裝）
ls -Z /var/www/html; ps -eZ | grep nginx; id -Z
sudo restorecon -Rv /var/www/html                 # 依政策修正標籤（複製 / 搬移檔案後常需）
sudo chcon -t httpd_sys_content_t /srv/www/index.html     # 臨時改標籤（restorecon 會改回）
sudo semanage fcontext -a -t httpd_sys_content_t "/srv/www(/.*)?" && sudo restorecon -Rv /srv/www   # 永久
sudo semanage fcontext -l | grep "/srv"
sudo semanage port -l | grep http; sudo semanage port -a -t http_port_t -p tcp 8080
sudo getsebool -a | grep httpd; sudo setsebool -P httpd_can_network_connect 1
sudo semanage boolean -l | grep httpd
sudo semanage login -l; sudo semanage user -l   # SELinux 使用者對應（一般用不到）
# 除錯
sudo ausearch -m avc -ts recent                   # 最近的拒絕
sudo sealert -a /var/log/audit/audit.log          # 人類可讀分析 + 建議指令
sudo journalctl -t setroubleshoot
sudo audit2why < /var/log/audit/audit.log
sudo ausearch -m avc -ts recent | audit2allow -M mypol && sudo semodule -i mypol.pp   # ⚠️ 產生自訂模組放行（先看 mypol.te 內容確認合理）
sudo semodule -l | head; sudo semodule -r mypol
# 常見 boolean
#   httpd_can_network_connect / httpd_can_network_connect_db / httpd_enable_homedirs / httpd_use_nfs
#   samba_enable_home_dirs / samba_export_all_rw / use_nfs_home_dirs / nis_enabled
#   container_manage_cgroup / virt_use_nfs / ssh_sysadm_login
# 容器（Podman）：掛載加 :Z（私有）或 :z（共享）自動標籤
```

**注意**：`audit2allow` 會把日誌裡所有被拒的動作照單全收做成放行模組，若日誌裡剛好有攻擊留下的 AVC，就等於幫攻擊者開門，所以一定要先讀 `mypol.te`。三個原則：**不關 SELinux**、**先 `restorecon`**、**再查 boolean**、最後才 `audit2allow`。

---

## 6. 核心與系統層

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **提權（privilege escalation）** | 從低權限帳號（www-data、一般使用者）取得 root | 本節所有設定都在拉高提權的門檻，而非擋「進來」 |
| **ROP gadget** | Return-Oriented Programming：利用核心或程式裡既有的指令片段拼出攻擊碼 | 需要知道程式碼在記憶體的位址，所以 §6.1 用 ASLR 與 `kptr_restrict` 藏位址 |
| **ptrace** | 讓一個程序讀寫、控制另一個程序的系統呼叫，除錯器（gdb、strace）都靠它 | 同一使用者的惡意程序可用它從你的 shell、ssh-agent 讀出憑證；§6.1 的 `ptrace_scope` 限制它 |
| **核心模組** | 動態載入核心的驅動或協定實作（`.ko`） | 冷門模組可被一般使用者「觸發自動載入」，是提權 CVE 的常見入口；§6.3 黑名單、§6.4 / 6.5 的簽章驗證都在管它 |
| **single user / rescue 模式** | 只起最小系統、直接給 root shell 的開機目標，用來救援 | 從 GRUB 選單改核心參數就能進入，所以 §6.4 要給 GRUB 上密碼 |

**為什麼**：攻擊者拿到低權限 shell 後，下一步是提權與藏身：利用核心資訊外洩找 ROP gadget、ptrace 別的程序偷憑證、在 `/tmp` 放可執行檔、載入冷門核心模組觸發漏洞、或在 console 重開機進 single user 改 root 密碼。本節每一項都在拿掉其中一條路，且多數沒有效能成本，但少數會影響特定工具（容器、除錯器、安裝程式），文中會標出。

### 6.1 sysctl 強化（`/etc/sysctl.d/99-hardening.conf`，兩者共通）

**名詞與關係**：`sysctl` 是核心參數的統一入口，下面的每一行都是一個核心開關，先看它們分別在擋什麼：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **sysctl / `/etc/sysctl.d/`** | 讀寫 `/proc/sys/` 下核心參數的指令；`sysctl.d/*.conf` 是開機時依檔名排序套用的設定檔 | `99-` 開頭確保最後載入、蓋過發行版自帶的 `10-*.conf`；`sysctl --system` 重新套用全部 |
| **rp_filter** | Reverse Path Filtering：收到封包時檢查「回這個來源 IP 的路由是否走同一張網卡」，不是就丟 | 擋 IP spoofing（偽造來源位址）；`1` 嚴格、`2` 寬鬆；非對稱路由（多 WAN、某些 VPN）用 1 會丟合法封包 |
| **ICMP redirect / source route / martians** | 路由器用 ICMP 叫主機改走別的閘道 / 封包自帶路徑指定 / 來源位址不合理的封包 | 三者都是舊網路功能，現代主機用不到卻能被用來導流或探測，一律關閉並記錄 |
| **tcp_syncookies** | SYN flood 時不在核心記憶體保留半開連線，改用帶簽章的序號回應 | 擋掉最簡單的 DoS；幾乎沒有副作用 |
| **ip_forward** | 允許核心把封包從一張網卡轉到另一張（當路由器） | Docker / Podman / VPN 閘道需要 `1`，否則容器對外網路會斷 |
| **kptr_restrict / dmesg_restrict** | 對非 root 隱藏 `/proc/kallsyms` 等處的核心位址 / 禁止非 root 讀 `dmesg` | 兩者都在防「核心位址外洩」，讓 ROP 類提權 exploit 找不到目標 |
| **ptrace_scope（Yama）** | Yama 安全模組的開關：`0` 同使用者可任意 attach、`1` 只能 attach 自己的子程序、`2` 只有 root、`3` 完全禁止 | 限制 §6 提到的 ptrace 濫用；🔵 預設 0 是為了開發方便 |
| **sysrq** | 鍵盤魔術鍵 / `/proc/sysrq-trigger`，可直接叫核心重開機、kill 全部程序、dump 記憶體 | 有 console 的人能用它繞過正常關機流程；`176` 只保留同步 / 重掛載 / 重開機等安全子集 |
| **unprivileged_bpf_disabled** | 禁止非 root 使用 BPF 系統呼叫（在核心內執行使用者提供的小程式，用於封包過濾、追蹤） | BPF 驗證器近年是提權 CVE 的大宗；一般伺服器不需要非 root 的 BPF |
| **randomize_va_space（ASLR）** | Address Space Layout Randomization：每次執行把堆疊、函式庫、堆積放在隨機位址 | `2` 為完整隨機；與 `kptr_restrict` 一樣是讓攻擊者猜不到位址 |
| **perf_event_paranoid** | 限制非 root 使用 perf 效能計數器 | perf 可觀察其他程序的行為、也出過漏洞；`3` 完全禁止非 root |
| **protected_symlinks / hardlinks / fifos / regular** | 在全域可寫且有 sticky bit 的目錄（`/tmp`）內，限制 root 程式跟隨他人建立的 symlink、hardlink、FIFO 或一般檔案 | 擋掉「在 /tmp 預先放一個指向 /etc/passwd 的連結，等 root 程式寫入」這類 TOCTOU 攻擊 |
| **suid_dumpable** | SUID 程式崩潰時是否產生 core dump | dump 裡可能含 root 才讀得到的資料（如 `/etc/shadow` 內容），設 `0` |
| **unprivileged_userfaultfd** | 是否允許非 root 使用 userfaultfd（由使用者空間處理缺頁） | 常被用來拉長核心競態條件的時間窗，是 exploit 的輔助工具，關閉 |

**為什麼**：網路部分擋的是 IP spoofing（`rp_filter`）、ICMP redirect 導流、SYN flood（`tcp_syncookies`）；核心部分讓提權 exploit 更難寫：`kptr_restrict` 藏核心位址、`ptrace_scope` 禁止同使用者的程序互相注入、`unprivileged_bpf_disabled` 關掉近年大量 CVE 的 BPF 入口、`protected_*` 擋掉在 `/tmp` 用 symlink / hardlink 騙 root 程式的老招。兩個發行版預設已接近，這份設定檔是把差異補齊並明文固定。

**怎麼做**：

```ini
# 網路
net.ipv4.conf.all.rp_filter = 1                    # 🟠 預設 2（loose）；嚴格模式用 1，多網卡非對稱路由時保持 2
net.ipv4.conf.default.rp_filter = 1
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.default.accept_redirects = 0
net.ipv6.conf.all.accept_redirects = 0
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.all.accept_source_route = 0
net.ipv6.conf.all.accept_source_route = 0
net.ipv4.conf.all.log_martians = 1
net.ipv4.icmp_echo_ignore_broadcasts = 1
net.ipv4.icmp_ignore_bogus_error_responses = 1
net.ipv4.tcp_syncookies = 1
net.ipv4.ip_forward = 0                            # 路由器 / 容器主機需 1
# 核心
kernel.kptr_restrict = 2                           # 🟠 預設 1（實測 10-kernel-hardening.conf）
kernel.dmesg_restrict = 1
kernel.yama.ptrace_scope = 1                       # 🟠 預設 1（10-ptrace.conf）；🔵 預設 0（10-default-yama-scope.conf）
kernel.sysrq = 0                                   # 或 176 只留安全用途
kernel.unprivileged_bpf_disabled = 1
kernel.randomize_va_space = 2
kernel.perf_event_paranoid = 3
fs.protected_hardlinks = 1
fs.protected_symlinks = 1
fs.protected_fifos = 2
fs.protected_regular = 2
fs.suid_dumpable = 0
vm.unprivileged_userfaultfd = 0
```

寫好後套用並看輸出有沒有報錯的鍵：

```bash
sudo sysctl --system
```

**注意**：`ip_forward = 0` 會讓 Docker / Podman 網路與 VPN 閘道失效；`ptrace_scope = 1` 下 `gdb -p`、`strace -p` 只能 attach 自己的子程序，開發機可保留預設。

### 6.2 檔案系統掛載選項

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **/etc/fstab** | 開機時要掛載哪些檔案系統到哪、用什麼選項的清單 | 掛載選項寫在第四欄；改完用 `mount -o remount /tmp` 生效 |
| **noexec** | 禁止直接執行此檔案系統上的檔案 | 擋「下載到 /tmp 直接執行」；但 `bash script.sh`、`python x.py` 這種由直譯器讀取的方式仍可繞過，所以要搭配 §5 的 MAC |
| **nosuid** | 忽略此檔案系統上檔案的 SUID / SGID 位元 | 讓攻擊者放進 `/tmp` 或 `/home` 的 SUID bash 副本失效（見 §2.4） |
| **nodev** | 不承認此檔案系統上的裝置檔（character / block device） | 防止在可寫目錄建一個指向 `/dev/sda` 的裝置檔繞過權限 |
| **tmpfs / `/dev/shm`** | 存在記憶體的檔案系統 / POSIX 共享記憶體的掛載點，所有人可寫 | 與 `/tmp` 同樣是攻擊者落腳處，選項要一致 |
| **noauto / user** | 開機不自動掛 / 允許一般使用者自行掛載 | 可移除媒體用；`user` 會隱含 `nosuid,nodev`，本例仍明寫以免誤解 |

**為什麼**：`/tmp`、`/dev/shm` 是所有使用者與服務都能寫的地方，也是 web shell、挖礦程式下載後第一個落腳點；`noexec` 讓「下載到 /tmp 直接執行」失敗，`nosuid` 讓放進去的 SUID 檔失效，`nodev` 擋掉偽造裝置檔。`/home` 加 `nosuid` 則讓使用者無法自己留 SUID 後門。

**怎麼做**：`/etc/fstab` 範例：

```fstab
/dev/mapper/vg-tmp   /tmp        ext4   defaults,nodev,nosuid,noexec   0 2
tmpfs                /dev/shm    tmpfs  defaults,nodev,nosuid,noexec   0 0
/dev/mapper/vg-home  /home       ext4   defaults,nodev,nosuid          0 2
/dev/mapper/vg-var   /var        xfs    defaults,nodev                 0 2
/dev/sdb1            /media/usb  vfat   defaults,nodev,nosuid,noexec,noauto,user 0 0
```

**注意**：⚠️ `/tmp` 加 `noexec` 會讓部分安裝程式（某些 `.run`、pip build）失敗，可用 `TMPDIR=/var/tmp` 繞過。

### 6.3 停用不需要的服務與協定

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **listening socket / 攻擊面** | 服務在某個埠等待連線；每個在聽的 socket 都是外部碰得到的程式碼 | `ss -tlnp` 列出；沒人用的服務就 `systemctl disable --now` |
| **avahi（mDNS）/ cups / bluetooth / ModemManager** | 區網服務探索（5353/udp）/ 列印（631）/ 藍牙 / 行動網路撥號 | 桌面環境有用、伺服器幾乎用不到，卻都出過遠端漏洞 |
| **DCCP / SCTP / RDS / TIPC** | 四個冷門傳輸層協定（資料報壅塞控制 / 串流控制傳輸 / 可靠資料報 / 叢集透明 IPC） | 都以核心模組實作，多年缺乏審計；一般使用者建立該類 socket 就會觸發自動載入 |
| **cramfs / freevxfs / jffs2 / hfs / hfsplus / udf** | 冷門或嵌入式檔案系統模組 | 掛載惡意映像可觸發解析漏洞；伺服器不需要 |
| **模組自動載入（autoload）** | 核心遇到未知協定 / 檔案系統時，透過 `modprobe` 自動載入對應模組 | 這是「一般使用者也能讓核心載入模組」的原因，黑名單就是在斷這條路 |
| **`/etc/modprobe.d/` 與 `install <模組> /bin/false`** | modprobe 的設定目錄；`install` 行把「載入某模組」替換成執行指定指令 | 指到 `/bin/false` 就永遠載不進來；比 `blacklist` 更徹底（`blacklist` 只擋自動載入，手動 `modprobe` 仍可） |
| **usb-storage** | USB 隨身碟 / 外接硬碟的驅動模組 | 停用它擋實體拷貝；只擋儲存類，鍵盤、網卡等其他 USB 裝置不受影響 |
| **usbguard** | 依裝置 ID 建立 USB 白名單的守護程式 | 比停用 usb-storage 精細：允許特定隨身碟、擋掉偽裝成鍵盤的 BadUSB |

**為什麼**：每個在聽的 socket 都是攻擊面（avahi 的 mDNS、cups 的 631 埠都出過遠端漏洞）。DCCP、SCTP、RDS、TIPC 這些冷門協定與檔案系統模組多年沒人審計，卻能被一般使用者觸發自動載入，歷來出過多個提權 CVE；設成 `install ... /bin/false` 就無法載入。`usb-storage` 則擋實體接觸拷貝資料。

**怎麼做**：

```bash
systemctl list-unit-files --state=enabled
sudo systemctl disable --now avahi-daemon cups bluetooth ModemManager 2>/dev/null   # 伺服器通常不需要
# 停用少用的核心模組
sudo tee /etc/modprobe.d/blacklist-rare.conf <<'EOF'
install dccp /bin/false
install sctp /bin/false
install rds /bin/false
install tipc /bin/false
install cramfs /bin/false
install freevxfs /bin/false
install jffs2 /bin/false
install hfs /bin/false
install hfsplus /bin/false
install udf /bin/false
install usb-storage /bin/false      # 若不需 USB 儲存
EOF
# USB 裝置白名單：usbguard（兩者皆有套件）
```

### 6.4 開機安全

**名詞與關係**：開機鏈上每一層各有一道保護，先看它們守在哪裡：

```
韌體 UEFI ──Secure Boot 驗簽──▶ shim ──▶ GRUB ──▶ 核心 ──lockdown──▶ root 也不能碰的核心內部
                                          │ GRUB 密碼：擋「按 e 改參數 / 進 GRUB 命令列」
                                          │ --unrestricted：正常開機項不需密碼，只有改參數才問
                                          └ 沒密碼時：init=/bin/bash 或 rd.break 直接拿 root shell
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **GRUB** | 兩個發行版預設的開機載入器，負責選核心、傳核心參數 | 它的選單可互動編輯，是實體存取者最容易下手的地方 |
| **`init=/bin/bash`** | 核心參數，叫核心不啟動 systemd、直接執行 bash 當第一個程序 | 得到的是無密碼 root shell；GRUB 密碼就是在擋這一招（以及 `single`、`rd.break`） |
| **superusers / password_pbkdf2 / `--unrestricted`** | GRUB 設定：管理員名稱 / 以 PBKDF2 雜湊儲存的密碼 / 標記「此選單項不需密碼即可正常開機」 | 🟠 手動寫進 `40_custom` 並改 `10_linux`；🔵 `grub2-setpassword` 一次完成且預設 unrestricted |
| **Secure Boot** | UEFI 韌體只執行有信任簽章的開機檔（shim → GRUB → 核心） | 擋「換掉開機鏈」；`mokutil --sb-state` 查狀態，完整信任鏈見 01 章 §1.5 與 14 章 |
| **核心 lockdown** | 核心的安全模式：`integrity` 禁止 root 修改執行中的核心（載未簽署模組、寫 `/dev/mem`、kexec 未簽核心），`confidentiality` 再禁止讀取核心記憶體 | 🔵 Fedora 在 Secure Boot 開啟時自動進 integrity；沒有 lockdown，root 能繞過 Secure Boot 的保護 |
| **`/dev/mem`** | 直接映射實體記憶體的裝置檔 | root 寫它就能改核心；lockdown 的封鎖對象之一 |

**為什麼**：碰得到實體機或 console 的人，在 GRUB 選單按 `e` 加上 `init=/bin/bash` 就能不用密碼拿到 root，前面所有強化都白做。GRUB 密碼只擋「改參數」，正常開機不必輸入；Secure Boot 與核心 lockdown 則擋掉開機鏈被換掉，以及 root 直接對核心動手（載未簽署模組、寫 `/dev/mem`）。

**怎麼做**：

```bash
# GRUB 密碼（防止改核心參數進 single user）
# 🟠 grub-mkpasswd-pbkdf2 → /etc/grub.d/40_custom 加 set superusers="admin" / password_pbkdf2 admin grub.pbkdf2...；
#    /etc/grub.d/10_linux 的 CLASS 加 --unrestricted 讓正常開機不需密碼；update-grub
# 🔵 grub2-setpassword（一行搞定，寫入 /boot/grub2/user.cfg）；正常開機仍不需密碼
mokutil --sb-state                                # Secure Boot 狀態
# 🔵 核心 lockdown（Secure Boot 啟用時自動 integrity 模式）：cat /sys/kernel/security/lockdown
```

### 6.5 Secure Boot 下的自訂核心模組

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **模組簽章驗證** | Secure Boot 開啟（且 lockdown 生效）時，核心只載入「簽章能被核心信任的憑證驗證」的 `.ko` | 發行版自帶模組由發行版簽；第三方模組沒有這個簽章 |
| **DKMS / akmods** | 核心更新後自動重新編譯第三方模組的機制（🟠 DKMS、🔵 akmods） | 編出來的模組是本機生成的，沒有任何內建信任的簽章 |
| **MOK** | Machine Owner Key：機器擁有者自己登錄進 shim 的憑證 | 用 MOK 私鑰簽模組，核心（透過 shim 傳入的信任清單）就會認 |
| **mokutil** | 管理 MOK 的指令：`--import` 排入登錄、`--sb-state` 查 Secure Boot 狀態 | `--import` 後要重開機，在 MokManager 藍畫面輸入當時設的密碼才完成登錄 |
| **shim / MokManager** | 由 Microsoft 簽署的第一階段載入器 / 它附帶的開機時 MOK 登錄介面 | MOK 存在 UEFI 變數中，由 shim 讀取並交給核心 |
| **kmodgenca** | 🔵 akmods 套件的工具，產生一組本機 CA 金鑰（`/etc/pki/akmods/`）給 akmods 簽模組 | 🟠 DKMS 會自動生在 `/var/lib/shim-signed/mok/`，不需手動 |

**為什麼**：Secure Boot 啟用後核心只載入已簽署的模組，這正是它的保護目的；代價是 DKMS / akmods 編出來的 NVIDIA、VirtualBox、ZFS 模組會被拒絕。解法不是關 Secure Boot，而是註冊自己的 MOK 金鑰並簽署。

**怎麼做**：

```bash
# 兩者：產生 MOK 金鑰 → 簽署模組 → mokutil --import → 重開機 enroll
# 🟠 DKMS 安裝時自動處理（/var/lib/shim-signed/mok/）；🔵 akmods 需 kmodgenca + mokutil --import /etc/pki/akmods/certs/public_key.der
```

---

## 7. 檔案完整性與惡意軟體

**名詞與關係**：這一節有兩類工具：**完整性檢查**（比對「現在」與「已知乾淨的基準」）與**簽章式掃描**（比對「已知惡意樣本」）；前者抓未知的改動、後者只抓已知的東西：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **AIDE** | Advanced Intrusion Detection Environment：把每個檔案的雜湊、權限、擁有者、inode 等記進資料庫，之後比對差異 | 基準（`aide.db`）是一切的前提，被改過的基準毫無意義；每次正常更新後要 `--update` 換新基準 |
| **基準 / 資料庫（`aide.db` / `aide.db.new`）** | `--init` 產生的檔案快照；`--check` 拿目前狀態與它比 | 應另存唯讀媒體或異地，否則入侵者可一併改掉 |
| **debsums / `rpm -V`** | 用套件管理器內建的檔案 checksum 驗證已安裝檔案是否被改（🟠 / 🔵） | 不用先建基準，但只涵蓋套件安裝的檔案，且 checksum 資料庫本身也在本機 |
| **rootkit** | 藏在系統裡、隱藏自身與攻擊者活動的惡意程式，常替換 `ps`、`ss`、`sshd` 或載入核心模組 | AIDE / `rpm -V` 能抓到「二進位檔被換」；rkhunter / chkrootkit 則比對已知 rootkit 特徵 |
| **rkhunter / chkrootkit** | 兩個 rootkit 掃描器：檢查已知 rootkit 檔案、可疑的隱藏程序與埠 | 簽章式；`--propupd` 是建它自己的檔案屬性基準；對客製攻擊無效 |
| **ClamAV / freshclam / clamd** | 開源防毒引擎 / 更新病毒庫的工具 / 常駐掃描守護程式 | 主要用在檔案伺服器與郵件閘道掃 Windows 樣本；對 Linux 主機自身的防護價值有限 |
| **簽章式** | 靠「已知惡意樣本的特徵」比對 | 與完整性檢查互補：新型或客製攻擊沒有簽章，只能靠基準比對發現 |

**為什麼**：入侵者留後門的典型做法是替換系統二進位檔（`ss`、`ps`、`sshd`）或改 PAM 模組來藏行蹤、偷密碼。完整性檢查靠的是「先建基準、之後比對」：AIDE 記錄每個檔案的雜湊與屬性，套件管理器本身也存有原始檔案的 checksum，可以免安裝直接驗。Rootkit 掃描器與 ClamAV 是簽章式，抓得到已知樣本，對客製化攻擊沒用，別當主力。

**怎麼做**：

```bash
# AIDE（兩者同名套件；實測 🟠 0.18.6、🔵 0.19.2）
sudo apt install -y aide      # 🟠 設定 /etc/aide/aide.conf，資料庫 /var/lib/aide/aide.db
sudo dnf install -y aide      # 🔵 設定 /etc/aide.conf，資料庫 /var/lib/aide/aide.db.gz
sudo aide --init              # 🟠 aideinit；🔵 aide --init 後 mv aide.db.new.gz aide.db.gz
sudo aide --check             # 排程每日（🟠 套件附 dailyaidecheck.timer，實測已啟用；🔵 自建 timer）
sudo aide --update

# 套件檔案驗證（免安裝）
sudo debsums -c               # 🟠（apt install debsums）
sudo rpm -Va | grep -vE '^\.{8}' # 🔵（S 大小 M 權限 5 checksum U 擁有者 改變的檔案）

# Rootkit 掃描
sudo apt install -y rkhunter chkrootkit   # 🟠  /  sudo dnf install -y rkhunter chkrootkit   # 🔵
sudo rkhunter --propupd && sudo rkhunter --check --sk
# ClamAV（檔案伺服器 / 郵件）
sudo apt install -y clamav clamav-daemon   # 🟠  /  sudo dnf install -y clamav clamd clamav-update   # 🔵
sudo freshclam && clamscan -r --infected /srv/share
```

**注意**：AIDE 基準要在機器乾淨時建立（最好裝完就建），並把資料庫另存到唯讀媒體或異地，否則入侵者改完檔案再跑 `aide --update` 就沒意義；每次正常更新套件後也要更新基準，不然每天都是一堆誤報。

---

## 8. 稽核（auditd）

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **auditd** | Linux 稽核子系統的使用者空間守護程式：接收核心送出的事件並寫進 `/var/log/audit/audit.log` | 事件由核心產生，程序無法繞過或刪除自己的紀錄；SELinux 的 AVC（§5.2）也走這條路 |
| **audit 規則** | 告訴核心要記錄什麼：`-w 路徑 -p wa` 監看檔案、`-a always,exit -S 系統呼叫` 監看系統呼叫；放在 `/etc/audit/rules.d/*.rules` | `augenrules --load` 合併載入；`auditctl -l` 列出生效規則，語法見 07 章 §5 |
| **ausearch / aureport** | 查詢 audit.log（依時間、使用者、key、事件類型）/ 產生摘要報表 | 前者找單一事件，後者看趨勢 |
| **CIS / PCI-DSS / STIG** | 三套安全基準：CIS Benchmark 是業界通用、PCI-DSS 是信用卡產業規範、STIG 是美國國防部標準 | 都明文要求稽核特定事件；🔵 `/usr/share/audit-rules/30-stig.rules`、`30-pci-dss-v31.rules` 就是照這些基準預先寫好的規則檔，§10 的合規掃描也對照它們 |

**為什麼**：日誌告訴你「有人登入了」，auditd 才能告訴你「登入後誰讀了 `/etc/shadow`、誰改了 sudoers、誰載入了核心模組」，而且是核心層記錄、程序繞不過。沒有稽核紀錄，事後鑑識只能靠猜，也過不了 CIS / PCI-DSS 這類合規要求。

**怎麼做**：設定與規則語法見 [07 日誌管理 §5](07-日誌管理.md)。安全基準建議至少監看：`/etc/passwd`、`/etc/shadow`、`/etc/sudoers*`、`/etc/ssh/sshd_config*`、特權指令執行、檔案刪除、時間變更、核心模組載入。🔵 Fedora 可直接複製 `/usr/share/audit-rules/30-stig.rules` 或 `30-pci-dss-v31.rules` 到 `/etc/audit/rules.d/`。

---

## 9. TLS 憑證

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **TLS** | Transport Layer Security，HTTPS、SMTPS、LDAPS 底下那層加密與身分驗證協定（SSL 的後繼） | 憑證是 TLS 用來證明「你連到的是本尊」的手段；沒有可信憑證，加密只能防竊聽、防不了冒充 |
| **憑證（certificate）** | 一份被 CA 簽章的檔案，內容是「這把公鑰屬於 example.com」 | 伺服器持有憑證與對應私鑰；客戶端檢查簽章是否來自它信任的 CA、名稱是否相符、是否過期 |
| **CA** | Certificate Authority，簽發憑證的機構；瀏覽器與作業系統內建一份「信任的公開 CA 清單」 | Let's Encrypt 是公開 CA；§9.2 的內部 CA 是自己當 CA，需手動加進每台機器的信任庫 |
| **自簽憑證** | 用憑證自己的私鑰簽自己，沒有任何 CA 背書 | 客戶端一律警告；只適合測試，或依 §9.2 把它當內部 CA 發到信任庫 |
| **中間人攻擊（MITM）** | 攻擊者站在使用者與伺服器之間轉發並讀改流量 | TLS 憑證驗證正是為了讓中間人無法冒充伺服器；使用者習慣按「仍要繼續」就等於關掉這道驗證 |
| **Let's Encrypt** | 免費、自動化的公開 CA | 憑證 90 天有效，強迫自動續期；工具是 §9.1 的 certbot |

**為什麼**：沒有 TLS、或用了過期 / 自簽憑證讓使用者習慣按「仍要繼續」，中間人攻擊（公共 Wi-Fi、被劫持的 DNS）就能直接讀寫流量與 session cookie。Let's Encrypt 把憑證成本降到零，剩下的問題只有「自動續期有沒有真的在跑」。

### 9.1 Let's Encrypt（certbot，兩者同名）

**名詞與關係**：certbot 與 Let's Encrypt 之間跑的是 ACME 協定，「驗證你真的控制這個網域」的方式決定了你該用哪個指令：

```
certbot（ACME 客戶端）──▶ Let's Encrypt（ACME 伺服器 / CA）：「請簽 www.example.com」
        ◀── 挑戰（challenge）：證明你控制這個網域
        │ HTTP-01：在 http://www.example.com/.well-known/acme-challenge/ 放一個檔案（需 80 埠可從公網連到）
        │     ├─ --nginx：certbot 暫時改 nginx 設定放檔，並順手寫入 TLS 設定與 --redirect
        │     └─ --standalone：certbot 自己開一個臨時 80 埠 web server（沒有網頁伺服器、或它可暫停時）
        └ DNS-01：在 _acme-challenge.example.com 加一筆 TXT 記錄（透過 DNS 供應商 API，如 --dns-cloudflare）
              → 唯一能簽萬用憑證 *.example.com 的方式，也不需要對外開 80
簽好 → /etc/letsencrypt/live/<網域>/fullchain.pem + privkey.pem（symlink 指向最新版）
續期 → certbot.timer / certbot-renew.timer 每天跑 certbot renew，剩 30 天內才真的續；--dry-run 走一遍流程但不簽
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **certbot** | EFF 開發的 ACME 客戶端（Python），負責申請、安裝、續期憑證 | 兩個發行版套件同名；外掛（`python3-certbot-nginx`、`-dns-cloudflare`）提供不同的挑戰方式 |
| **ACME** | Automatic Certificate Management Environment（RFC 8555）：自動申請憑證的協定 | Let's Encrypt 是它的主要伺服器；acme.sh、lego、Caddy 內建等都是替代客戶端 |
| **challenge（HTTP-01 / DNS-01）** | ACME 驗證網域控制權的兩種方式 | HTTP-01 簡單但需要 80 埠對外；DNS-01 需要 DNS API 權限但能簽萬用憑證與內網主機 |
| **萬用憑證（wildcard）** | `*.example.com`，一張憑證涵蓋所有子網域 | 只能用 DNS-01 取得 |
| **fullchain.pem / privkey.pem** | 伺服器憑證 + 中繼 CA 憑證串 / 對應私鑰 | nginx 的 `ssl_certificate` / `ssl_certificate_key` 就指向這兩個檔；只給 `cert.pem` 會缺中繼憑證，部分客戶端驗證失敗 |
| **certbot.timer / certbot-renew.timer** | 每天觸發 `certbot renew` 的 systemd timer（🟠 / 🔵 名稱不同） | 續期後要 reload 服務：nginx 外掛會自動處理，其他情況用 `--deploy-hook` |

**為什麼**：憑證 90 天到期，手動換一定會忘，過期的那一刻就是服務中斷。certbot 的 timer 每天檢查、剩 30 天自動換；`renew --dry-run` 是在到期前發現 DNS 或 80 埠被擋的唯一辦法。

**怎麼做**：

```bash
sudo apt install -y certbot python3-certbot-nginx     # 🟠 2.9
sudo dnf install -y certbot python3-certbot-nginx     # 🔵 5.7
sudo certbot --nginx -d www.example.com -d example.com --agree-tos -m ops@example.com --redirect
sudo certbot certonly --standalone -d mail.example.com   # 無網頁伺服器時（需 80 埠空著）
sudo certbot certonly --dns-cloudflare --dns-cloudflare-credentials ~/.secrets/cf.ini -d '*.example.com'   # 萬用憑證（DNS-01）
sudo certbot renew --dry-run
systemctl list-timers certbot*        # 🟠 certbot.timer；🔵 certbot-renew.timer（實測套件皆附 timer）
ls /etc/letsencrypt/live/www.example.com/    # fullchain.pem、privkey.pem
```

### 9.2 內部 CA / 自簽

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **內部 CA（私有 CA）** | 自己建立的一組 CA 金鑰與根憑證，專門簽內網服務的憑證 | 根憑證發到每台機器的信任庫後，它簽的所有憑證都自動被信任；CA 私鑰要離線保管 |
| **自簽憑證** | 沒有上層 CA、自己簽自己的單張憑證（本節 `openssl req -x509` 產生的就是） | 每台客戶端要個別信任這一張；服務多了就該改用內部 CA |
| **CN / SAN** | Common Name（憑證主體欄位）/ Subject Alternative Name（列出所有適用主機名與 IP 的擴充欄位） | 現代瀏覽器與 curl **只看 SAN、不看 CN**；沒有 SAN 的憑證即使 CN 正確也被拒，所以要 `-addext subjectAltName` |
| **信任庫（trust store）** | 系統集中存放「信任的 CA 根憑證」的地方：🟠 `/usr/local/share/ca-certificates/` + `update-ca-certificates`、🔵 `/etc/pki/ca-trust/source/anchors/` + `update-ca-trust` | curl、Python requests、apt / dnf、Java（部分）都讀它；Firefox 有自己的一份要另外匯入 |
| **step-ca / smallstep、mkcert** | 前者是可跑 ACME 的自架 CA 伺服器（讓內網也能用 certbot 自動續期）；後者是開發機一鍵建本機 CA 並裝進信任庫的工具 | 都是「內部 CA」的現成實作，比手工 `openssl ca` 少踩坑 |
| **`openssl s_client`** | 手動建立 TLS 連線並印出伺服器憑證的除錯工具 | 加 `-servername` 才會送 SNI，多網域主機才會回對的憑證 |

**為什麼**：內網服務也需要 TLS（攻擊者進到內網後嗅探流量是常態），但公開 CA 不會簽私有網域。沒有 SAN 的自簽憑證會被現代瀏覽器與 curl 拒絕；建一個內部 CA 並發到每台機器的信任庫，才不用到處 `-k` / `verify=False`——那等於沒有 TLS。

**怎麼做**：

```bash
# 現代做法：用 step-ca / smallstep 或 mkcert（開發）；快速自簽：
openssl req -x509 -newkey rsa:4096 -sha256 -days 365 -nodes \
  -keyout server.key -out server.crt -subj "/CN=internal.example.com" \
  -addext "subjectAltName=DNS:internal.example.com,IP:10.0.0.10"
# 信任內部 CA（讓 curl / 瀏覽器 / 系統程式都認）
sudo cp ca.crt /usr/local/share/ca-certificates/internal-ca.crt && sudo update-ca-certificates   # 🟠
sudo cp ca.crt /etc/pki/ca-trust/source/anchors/ && sudo update-ca-trust                        # 🔵
openssl s_client -connect host:443 -servername host </dev/null 2>/dev/null | openssl x509 -noout -dates -subject   # 檢查
```

### 9.3 服務端 TLS 設定原則

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **TLS 1.0 / 1.1 / 1.2 / 1.3** | TLS 協定版本；1.0 / 1.1 已於 2021 年被 RFC 8996 正式廢止，1.3 拿掉所有不安全的舊套件 | 伺服器只開 1.2 / 1.3 即可支援所有現代客戶端 |
| **cipher suite / CBC** | 一次 TLS 連線用的整組演算法（金鑰交換 + 簽章 + 對稱加密 + 雜湊）/ 其中一種對稱加密模式 | CBC 模式的 padding 處理是 BEAST、POODLE、Lucky13 的根源；改用 AEAD（AES-GCM、ChaCha20-Poly1305） |
| **POODLE / BEAST** | 兩個針對 SSL 3.0 / TLS 1.0 + CBC 的著名攻擊 | 只要關掉舊版本與 CBC 套件就同時解決 |
| **降級攻擊** | 中間人干擾協商，讓雙方退回較弱的版本或套件 | 伺服器不提供弱選項就無從降級；TLS 1.3 另有內建的降級偵測 |
| **HSTS** | HTTP Strict Transport Security：伺服器回應 `Strict-Transport-Security` 標頭，瀏覽器之後在指定期間內一律直接用 HTTPS 連 | 擋「第一次連線走 HTTP 被剝除（SSL stripping）」；設錯（憑證壞掉）會讓使用者在期限內完全連不上，先用短 max-age 測 |
| **OCSP / OCSP stapling** | Online Certificate Status Protocol：查憑證是否被撤銷 / 由伺服器預先向 CA 取得帶簽章的狀態回應，隨握手一起送給客戶端 | 不 stapling 時每個瀏覽器自己去問 CA：慢、CA 掛了就卡、還讓 CA 知道誰在看哪個站 |
| **ECDSA P-256 / RSA 2048+** | 憑證金鑰的兩種演算法：橢圓曲線（短、快）/ 傳統（相容性最好） | 🔵 FUTURE crypto-policy（§3.1）要求 RSA ≥ 3072；ECDSA 憑證 Let's Encrypt 也支援 |
| **Mozilla SSL Configuration Generator / testssl.sh / SSL Labs** | Mozilla 維護的設定產生器（modern / intermediate / old 三檔）/ 本機掃描腳本 / Qualys 的線上掃描服務 | 用產生器寫設定，用掃描器驗證；intermediate 是「安全且相容」的常用選擇 |

**為什麼**：TLS 1.0 / 1.1 與 CBC 套件有 POODLE、BEAST 等已知攻擊，且能被降級；HSTS 防止第一次連線被剝成 HTTP；OCSP stapling 讓瀏覽器不必自己向 CA 查憑證狀態，既快也不外洩使用者在看哪個站。

**怎麼做**：

- 只開 TLS 1.2 / 1.3；用 Mozilla SSL Configuration Generator（intermediate profile）產生 nginx / apache / haproxy 設定。
- 開 HSTS、OCSP stapling；金鑰 ECDSA P-256 或 RSA 2048+（🔵 FUTURE 策略需 3072+）。
- 測試：`testssl.sh https://host` 或 SSL Labs。

---

## 10. 合規檢查與自動化強化

**名詞與關係**：合規檢查有兩條路線：Lynis 是一支獨立腳本，直接給建議；OpenSCAP 那條則是「基準文件 → 機器可讀內容 → 掃描器」三層，先看這條鏈：

```
安全基準（文件）：CIS Benchmark ／ DISA STIG ／ PCI-DSS ……
        │ 由 ComplianceAsCode 專案寫成機器可讀的規則
        ▼
SCAP 內容：SSG（SCAP Security Guide）套件 → ssg-fedora-ds.xml、ssg-ubuntu2404-ds.xml（一個 DataStream 檔含多個 profile）
        │ 每個 profile（cis、cis_level1_server、stig、pci-dss…）= 一個基準的規則子集
        ▼
掃描器：oscap（OpenSCAP）xccdf eval --profile ‹profile› → 逐條檢查 → --report 產出 HTML；--remediate 依規則附的腳本自動修
        │
        ├─ 🟠 USG（Ubuntu Pro）= 對 SSG 內容 + oscap 的包裝，指令簡化成 usg audit / usg fix
        └─ 🔵 Anaconda 安裝程式的 Security Profile = 安裝時就套一個 SSG profile
Lynis：與上面無關的獨立 shell 腳本，自帶檢查項目，輸出 Hardening index 與 Suggestions
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Lynis** | CISOfy 維護的開源健檢腳本，跑一次列出數百項檢查與建議 | 不對照正式基準、不會自動修；適合日常快速看「分數有沒有掉」 |
| **CIS / STIG / PCI-DSS** | 三套安全基準：CIS Benchmark（業界通用，分 Level 1 / 2）、DISA STIG（美國國防部）、PCI-DSS（信用卡產業） | 它們是「規範文件」；要自動檢查得靠下面的 SCAP 內容把它們寫成規則 |
| **SCAP** | Security Content Automation Protocol：NIST 制定的一組標準格式（XCCDF 寫檢查清單、OVAL 寫檢查方法、DataStream 打包） | 讓「基準」變成掃描器能執行的東西 |
| **SSG（SCAP Security Guide）** | ComplianceAsCode 專案產出的 SCAP 內容集，每個發行版一個 DataStream 檔（🟠 `ssg-debderived`、🔵 `scap-security-guide` 套件） | 內含多個 profile 對應不同基準；🟠 24.04 套件庫版本太舊，要自己下載 `ssg-ubuntu2404-ds.xml` |
| **profile（XCCDF profile）** | DataStream 內的一組規則選集，如 `xccdf_org.ssgproject.content_profile_cis` | `oscap info` 列出可用 profile；掃描時用 `--profile` 指定 |
| **OpenSCAP / oscap** | NIST 認證的 SCAP 掃描器與其指令（`openscap-scanner` 套件） | 讀 SSG 內容執行評估（`xccdf eval`）、產報告、可 `--remediate` |
| **remediate（自動修復）** | 依每條規則附帶的修復腳本（bash / Ansible）直接改系統 | 會改 PAM、sshd、掛載等大量設定，一定先在測試機跑並逐條看 |
| **USG** | Ubuntu Security Guide：Ubuntu Pro 附的合規工具，把 SSG + oscap 包成 `usg audit` / `usg fix` | 需要 Ubuntu Pro（§1.2）；底層仍是 SCAP |
| **Anaconda Security Profile** | 🔵 Fedora / RHEL 安裝程式內建的選項（Kickstart `%addon org_fedora_oscap`），安裝時就套用一個 SSG profile | 適合新機器一開始就合規，省掉事後 remediate |

**為什麼**：手動逐項檢查一定有漏，且每台機器的狀態會隨時間漂移。Lynis 幾分鐘就跑完，適合日常健檢；OpenSCAP + SSG 對照 CIS / STIG 這類正式基準逐條評估、還能自動修復，是稽核時要交的報告。但基準是為通用環境寫的，某些項目（`/tmp` 獨立分割、密碼定期過期）對你不一定合理，先看報告再決定要不要 `--remediate`。

**怎麼做**：

```bash
# Lynis（兩者皆有套件；快速健檢）
sudo apt install -y lynis    # 🟠 3.0.9      sudo dnf install -y lynis    # 🔵 3.1.7
sudo lynis audit system --quick           # 看 Hardening index 與 Suggestions；報告 /var/log/lynis-report.dat

# OpenSCAP + SCAP Security Guide（CIS / STIG / PCI-DSS 基準，可自動修復）
sudo apt install -y openscap-scanner ssg-base ssg-debderived      # 🟠 ⚠️ 實測 24.04 套件庫的 ssg-debderived 0.1.71 只含到 ssg-ubuntu2204；
#    24.04 的內容請到 https://github.com/ComplianceAsCode/content/releases 下載 ssg-ubuntu2404-ds.xml
sudo dnf install -y openscap-scanner scap-security-guide           # 🔵（實測 0.1.81）
ls /usr/share/xml/scap/ssg/content/
oscap info /usr/share/xml/scap/ssg/content/ssg-fedora-ds.xml | grep -A20 Profiles       # 🔵
sudo oscap xccdf eval --profile xccdf_org.ssgproject.content_profile_cis \
  --report /root/cis-report.html /usr/share/xml/scap/ssg/content/ssg-fedora-ds.xml       # 🔵 評估
sudo oscap xccdf eval --profile xccdf_org.ssgproject.content_profile_cis_level1_server \
  --report /root/cis-report.html /usr/share/xml/scap/ssg/content/ssg-ubuntu2404-ds.xml   # 🟠
sudo oscap xccdf eval --remediate --profile ... ssg-*.xml        # ⚠️ 自動修復，先在測試機跑
# 🟠 Ubuntu Pro 用戶另有 USG（Ubuntu Security Guide）：sudo usg audit cis_level1_server / usg fix
# 🔵 Fedora / RHEL 安裝時 Anaconda 可直接選 Security Profile（Kickstart: %addon org_fedora_oscap）
```

### 10.1 最小強化檢查清單

**為什麼**：合規工具會列出上百條建議，但真正擋掉多數入侵的就是下面這十幾項；新機上線前把它們勾完，比追求 Lynis 分數有意義。每一項都對應本章或前幾章的一個小節，勾不掉的請寫下理由。

**怎麼做**：逐項確認：

- [ ] 自動安全更新已啟用，並有重開機策略
- [ ] root 鎖定；管理員在 `sudo` / `wheel`；sudo 需密碼；`use_pty`
- [ ] SSH：金鑰登入、禁密碼、禁 root、`AllowGroups`、非必要不轉發
- [ ] 防火牆預設 deny incoming，只開必要埠；管理埠限來源 IP
- [ ] Fail2ban 或等效機制
- [ ] AppArmor / SELinux **enforcing**，服務有 profile / 標籤正確
- [ ] 密碼政策 + faillock；不需要的帳號移除
- [ ] sysctl 強化；不需要的服務 / 模組停用
- [ ] `/tmp`、`/dev/shm` 的 `noexec,nosuid,nodev`
- [ ] auditd 規則；日誌集中外送
- [ ] AIDE 或套件完整性定期檢查
- [ ] TLS 只用 1.2+；憑證自動續期
- [ ] 定期 `lynis` / `oscap` 掃描；備份可還原（見 11 章）

---

## 11. 本章差異總結

兩個發行版在安全工具上的差異，多半來自各自的上游：Ubuntu 承襲 Debian，偏好每個套件自行管理、AppArmor 這種易上手的方案，並把進階功能（Livepatch、USG）放進 Ubuntu Pro；Fedora 作為 RHEL 的上游，直接繼承 Red Hat 在企業與政府合規上的投資——SELinux、crypto-policies、authselect、SCAP 內容都是為了「一次設定、全系統一致、可稽核」而生。指令不同，目標一樣，對照如下：

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 自動更新 | unattended-upgrades（預設開） | dnf5-plugin-automatic（需裝；設定檔為 %ghost） |
| Livepatch / 延伸支援 | Ubuntu Pro（ESM、Livepatch、USG） | 無 |
| MAC | AppArmor（path-based；`aa-*` 工具） | SELinux（label-based；`semanage`、`restorecon`、`sealert`） |
| 加密策略 | 各服務自行設定 | crypto-policies 全系統統一（`update-crypto-policies`） |
| faillock 啟用 | 手動改 PAM | `authselect select local with-faillock` |
| Fail2ban 預設 | backend systemd、banaction nftables | backend 需指定、banaction firewallcmd-rich-rules |
| sysctl 預設 | `rp_filter=2`、`kptr_restrict=1`、`ptrace_scope=1` | `ptrace_scope=0`（其餘依核心預設） |
| GRUB 密碼 | 手動編輯 `40_custom` | `grub2-setpassword` |
| CA 信任 | `/usr/local/share/ca-certificates/` + `update-ca-certificates` | `/etc/pki/ca-trust/source/anchors/` + `update-ca-trust` |
| SCAP 內容 | `ssg-debderived`（ssg-ubuntu2404-ds.xml）；Pro 有 USG | `scap-security-guide`（ssg-fedora-ds.xml）；安裝時可選 profile |
| AIDE 設定 | `/etc/aide/aide.conf`，`aideinit` | `/etc/aide.conf`，`aide --init` |
| certbot timer | `certbot.timer` | `certbot-renew.timer` |
| 核心模組簽署 | DKMS + shim MOK 自動 | akmods + `kmodgenca` + `mokutil` |
| 非標準埠 | 開防火牆即可 | 開防火牆 + `semanage port` |
