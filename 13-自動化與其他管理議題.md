# 13 - 自動化與其他管理議題

本章收錄前面章節未涵蓋但日常會遇到的主題：設定管理（Ansible）、cloud-init、容器（Docker / Podman / Quadlet）、虛擬化（KVM / libvirt）、時間與 NTP 伺服器、郵件通知、印表機與桌面、多主機管理（Cockpit）、系統資訊與清單、以及發行版升級的實務清單。

**本章解決什麼問題**：前面各章的設定都是「在一台機器上手動做」。機器一多，手動就會出現三個問題：每台做的不一樣（設定漂移）、沒有人記得某台為什麼這樣設（無紀錄）、重灌或擴容要重做一遍（不可重現）。本章的主軸是把這些操作變成可重複執行的文字檔（playbook、cloud-config、Quadlet、VM 範本），再補上幾個「不做會在半夜出事」的周邊：時間同步、告警郵件、資產清單與升級流程。取捨原則是：兩個發行版都能用同一套描述檔，差異只留在變數與少數分支。

---

## 1. Ansible（設定管理，兩者共通）

**名詞與關係**：Ansible 的名詞看起來多，其實只是「誰對誰、做什麼、怎麼組織」幾個層次，先看它們怎麼疊起來：

```
控制機（裝 Ansible） ──SSH──▶ 受管機（只需 python3；agentless = 不裝代理程式）
      │
      ├─ inventory：受管機清單與分群（web / db / fedora / ubuntu）
      ├─ playbook（YAML）：對哪些群組、依序執行哪些 task
      │     ├─ task：呼叫一個 module（apt / dnf / package / copy / user…）並給參數
      │     ├─ handler：只在某個 task 回報「有改變」並 notify 時才執行的 task（如 restart sshd）
      │     └─ facts：執行前先從受管機蒐集的系統資訊（os_family、selinux…），供 when 分支
      ├─ collection：module 的打包單位（ansible.builtin 內建；community.general、ansible.posix 要另裝）
      └─ 冪等 + check mode：module 先比對現況再決定要不要動；--check 只比對、不動
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Ansible / ansible-core** | Red Hat 維護的設定管理工具；`ansible-core` 是執行引擎加內建 module，`ansible` 套件則是 core 再加一大包 collection | core 版本（2.16 vs 2.20）決定 module 行為，所以跨版本協作建議用 pipx 統一 |
| **控制機 / 受管機 / agentless** | 控制機是裝了 Ansible 的那台；受管機是被設定的機器；agentless 指受管機不需常駐任何代理程式 | 控制機透過 SSH 把 module 的 Python 程式推到受管機執行完就刪，所以受管機只要 SSH + python3 |
| **inventory** | 列出受管機、其連線參數（`ansible_host`、`ansible_user`）並分群的清單（`inventory.ini` 或 YAML） | playbook 的 `hosts:` 與 `-i` 參數都指向它；群組名也是變數的作用域 |
| **playbook** | 一份 YAML，描述「對哪些主機、依序執行哪些 task」 | 由 vars、tasks、handlers 組成；本節把它當「機器應有狀態」的紀錄 |
| **task / module** | task 是 playbook 裡的一個步驟；每個 task 呼叫一個 module——實際做事的 Python 程式（`apt`、`copy`、`user`…） | module 負責冪等；`package` module 會依 facts 自動選 apt 或 dnf |
| **handler** | 一種特殊 task：只有被 `notify` 且觸發它的 task 回報 changed 時才執行，且整個 play 只跑一次 | 用來做「改了設定才重啟服務」，避免每次執行都重啟 sshd |
| **facts / `ansible_facts`** | 執行 play 前由 `setup` module 從受管機蒐集的系統資訊（`os_family`、`selinux`、IP…） | `when:` 分支與套件名 map 都靠它辨別 Debian / RedHat |
| **collection** | 以 `namespace.name` 命名的 module / role 打包單位（`community.general`、`ansible.posix`） | 套件庫的 ansible-core 只含 `ansible.builtin`，用其他 collection 前要 `ansible-galaxy collection install` |
| **冪等（idempotent）** | 同一份 playbook 重跑多次，結果與跑一次相同；module 先比對現況，已符合就回報 ok 而不動 | 這是 playbook 能當「狀態紀錄」而不是「一次性腳本」的前提 |
| **check mode（`--check --diff`）** | 只比對、不修改的模擬執行；`--diff` 顯示會改的內容 | 建立在 module 的冪等實作上；`validate:` 則是真正寫檔前先用 `sshd -t` 檢查內容 |
| **pipx** | 把 Python CLI 工具各自裝進隔離 venv 的安裝器 | 用來裝與套件庫不同版本的 Ansible，且不污染系統 Python |
| **Terraform / OpenTofu、Packer、etckeeper** | 分別是建基礎設施（VM、雲端資源）、建映像、把 `/etc` 放進 git 追蹤的工具 | 與 Ansible 互補：它們負責「機器怎麼來」，Ansible 負責「機器裡面長什麼樣」 |

**為什麼**：Ansible 是 agentless（受管機只要 SSH + Python），對已經做好金鑰登入的機器零額外部署，是混合 Ubuntu / Fedora 環境最低成本的選擇；playbook 是冪等的，重跑不會重做已完成的步驟，可以放心當成「機器應有狀態」的紀錄。套件庫版本落差大（🟠 core 2.16 vs 🔵 core 2.20），跨版本協作時用 pipx 裝同一版能避免模組行為不一致。

**怎麼做**：控制機安裝 Ansible，受管機不用裝任何東西：

```bash
# 控制機（任一發行版；建議 pipx 裝最新版）
sudo apt install -y ansible          # 🟠 9.x（core 2.16）
sudo dnf install -y ansible          # 🔵 13.x（core 2.20）
pipx install --include-deps ansible  # ⚪ 最新
# 受管機只需要 python3 + SSH（兩者預設皆有）
```

inventory 是「哪些機器、怎麼連」的清單；把 Fedora / Ubuntu 主機分群，之後可以只對某群執行。`inventory.ini`：

```ini
[web]
web01 ansible_host=192.168.1.21
web02 ansible_host=192.168.1.22
[db]
db01 ansible_host=192.168.1.41
[fedora]
db01
[ubuntu]
web01
web02
[all:vars]
ansible_user=admin
ansible_become=true
```

跨發行版的關鍵是不要寫死 `apt` / `dnf` 與套件名：用 `ansible_facts.os_family`（Debian / RedHat）分支、套件名放進對照 map、通用的 `package` 模組自動選套件管理器；SSH 設定用 `validate` 先跑 `sshd -t`，避免推錯設定把自己鎖在外面。跨發行版 playbook `site.yml`（用 `ansible_facts` 分支，套件名用變數）：

```yaml
- hosts: all
  vars:
    base_packages_map:
      Debian: [vim, curl, htop, dnsutils, chrony]
      RedHat: [vim-enhanced, curl, htop, bind-utils, chrony]
    firewall_svc:
      Debian: ufw
      RedHat: firewalld
  tasks:
    - name: 更新套件快取並升級（Debian 系）
      ansible.builtin.apt: {update_cache: true, upgrade: dist, cache_valid_time: 3600}
      when: ansible_facts.os_family == "Debian"
    - name: 升級（RedHat 系）
      ansible.builtin.dnf: {name: "*", state: latest}
      when: ansible_facts.os_family == "RedHat"
    - name: 安裝基本套件（自動選 apt / dnf）
      ansible.builtin.package:
        name: "{{ base_packages_map[ansible_facts.os_family] }}"
        state: present
    - name: 時區
      community.general.timezone: {name: Asia/Taipei}
    - name: 管理員群組（sudo / wheel）
      ansible.builtin.user:
        name: deploy
        groups: "{{ 'sudo' if ansible_facts.os_family == 'Debian' else 'wheel' }}"
        append: true
        shell: /bin/bash
    - name: SSH 強化 drop-in
      ansible.builtin.copy:
        dest: /etc/ssh/sshd_config.d/10-hardening.conf
        content: |
          PermitRootLogin no
          PasswordAuthentication no
        validate: "sshd -t -f %s"
      notify: restart sshd
    - name: 防火牆開 SSH（Ubuntu）
      community.general.ufw: {rule: allow, name: OpenSSH, state: enabled}
      when: ansible_facts.os_family == "Debian"
    - name: 防火牆開 SSH（Fedora）
      ansible.posix.firewalld: {service: ssh, permanent: true, immediate: true, state: enabled}
      when: ansible_facts.os_family == "RedHat"
    - name: SELinux boolean（Fedora）
      ansible.posix.seboolean: {name: httpd_can_network_connect, state: true, persistent: true}
      when: ansible_facts.selinux.status == "enabled"
    - name: 是否需要重開機
      ansible.builtin.stat: {path: /var/run/reboot-required}
      register: reboot_required
      when: ansible_facts.os_family == "Debian"
  handlers:
    - name: restart sshd
      ansible.builtin.service:
        name: "{{ 'ssh' if ansible_facts.os_family == 'Debian' else 'sshd' }}"
        state: restarted
```

執行前先裝 collection（套件庫的 ansible-core 不含 `community.general` / `ansible.posix`），再用 `--check --diff` 模擬一遍看會改什麼，確認無誤才真正套用：

```bash
ansible-galaxy collection install community.general ansible.posix   # 套件庫的 ansible-core 不含 collection，必裝（實測缺少時 --syntax-check 會報 couldn't resolve module）
ansible all -i inventory.ini -m ping
ansible all -i inventory.ini -m setup -a "filter=ansible_os_family"      # Debian / RedHat
ansible-playbook -i inventory.ini site.yml --check --diff               # 先模擬
ansible-playbook -i inventory.ini site.yml
ansible-lint site.yml
```

**注意**：Ansible 管「機器內部的狀態」，不管「機器本身怎麼來」；建 VM / 雲端資源用 Terraform / OpenTofu，建映像用 Packer，兩者搭配才完整。`etckeeper` 則是最低成本的變更紀錄：就算還沒導入設定管理，至少讓 `/etc` 每次改動都有 git 歷史可查。其他工具：Puppet、Salt、Chef（兩者皆可裝）；Terraform / OpenTofu 建基礎設施；Packer 建映像；`etckeeper` 把 `/etc` 放進 git 追蹤變更（兩者有套件）。

---

## 2. cloud-init（兩者共通）

**名詞與關係**：cloud-init 的運作只有一句話：「第一次開機時，從某個來源讀兩份資料，然後執行一輪初始化」。名詞都掛在這句話上：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **cloud-init** | 雲端映像內建的第一次開機初始化服務（Canonical 維護），依 datasource 提供的資料設定主機名、使用者、金鑰、套件與指令 | 只在第一次開機或 instance-id 改變時完整執行；`cloud-init clean` 就是讓它忘記已執行過 |
| **datasource** | cloud-init 取得設定資料的來源：雲端平台的 metadata 服務（AWS、GCP、OpenStack…）或本機的 NoCloud | 決定 meta-data / user-data 從哪裡讀；映像本身不用改，換平台只是換 datasource |
| **meta-data** | 平台提供的實例資訊：instance-id、主機名、網路設定等（`cloud-init query` 看得到） | instance-id 變了 cloud-init 才視為「新機器」重跑，所以做範本要清掉舊紀錄 |
| **user-data / `#cloud-config`** | 使用者提供的初始化內容；以 `#cloud-config` 開頭的 YAML 是最常用格式 | 兩個發行版的雲端映像都內建 cloud-init，所以同一份 user-data 可通用 |
| **NoCloud** | 不靠雲端平台的 datasource：把 meta-data 與 user-data 放進一個標籤為 `cidata` 的小 ISO 或磁碟 | libvirt / 本機 VM 用它模擬雲端 metadata；§4 安裝的 `cloud-image-utils` / `cloud-utils` 就是產生這個 ISO 的工具 |
| **machine-id** | `/etc/machine-id`，systemd 為每台機器產生的唯一 ID；journald、DHCP client-id、資產工具都拿它辨識主機 | 複製 VM 前不清空，所有複製品都是「同一台」；與 SSH host key、cloud-init 紀錄並列做範本要清的三件事 |
| **SSH host key** | `/etc/ssh/ssh_host_*_key`，sshd 用來向 client 證明「我是這台主機」的金鑰對 | 範本沒清就每台同一把，失去識別意義；清掉後兩邊都會在下次開機自動重生 |
| **virt-sysprep** | libguestfs 工具，在 VM 關機狀態下離線清除 machine-id、host key、日誌等身分資訊 | 把上面三件事一次做完；§4 用 `virt-clone` 複製 VM 後也要跑 |
| **Netplan** | Ubuntu 的網路設定抽象層（05 章） | 🟠 雲端映像的網路設定由 cloud-init 產生成 Netplan 檔，自訂前要先停用 cloud-init 的網路模組 |

**為什麼**：VM 從範本複製出來後，如果靠人登入改主機名、建使用者、放金鑰，每台都要幾分鐘且容易漏；cloud-init 在第一次開機就讀 metadata / user-data 自動完成這些事，是雲端與 libvirt 範本「一開機就能 SSH 進去」的標準做法。Ubuntu 與 Fedora 的雲端映像都內建，同一份 `#cloud-config` 兩邊可用。

**怎麼做**：雲端映像 / VM 範本第一次開機的自動設定（主機名、使用者、SSH 金鑰、套件、指令）。常用的狀態查詢、日誌、驗證與重置指令：

```bash
cloud-init status --long; cloud-init status --wait
cat /var/log/cloud-init-output.log; journalctl -u cloud-init
ls /etc/cloud/cloud.cfg.d/                       # 本機覆寫
sudo cloud-init clean --logs                     # 重置（做範本前）；配合 rm /etc/machine-id; systemd-machine-id-setup
cloud-init schema --config-file user-data        # 驗證（已實測）
cloud-init query -a | head                       # instance metadata
```

下面的 `user-data` 把 02 章與 08 章的初始設定濃縮成一份：使用者同時列 `sudo` 與 `wheel`（不存在的群組會被忽略，所以兩邊通用）、SSH 強化 drop-in、依有無 `ufw` 決定用 ufw 或 firewalld，最後只在真的需要時重開機。`user-data` 範例（NoCloud / 雲端 metadata 皆同）：

```yaml
#cloud-config
hostname: app01
fqdn: app01.example.com
manage_etc_hosts: true
timezone: Asia/Taipei
users:
  - name: admin
    groups: [sudo, wheel]                  # 不存在的群組會被忽略，兩者通用
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh_authorized_keys: [ssh-ed25519 AAAA... admin@laptop]
package_update: true
package_upgrade: true
packages: [vim, htop, chrony]
write_files:
  - path: /etc/ssh/sshd_config.d/10-hardening.conf
    content: |
      PasswordAuthentication no
      PermitRootLogin no
runcmd:
  - systemctl restart ssh || systemctl restart sshd
  - [sh, -c, "command -v ufw && ufw --force enable && ufw allow OpenSSH || firewall-cmd --permanent --add-service=ssh && firewall-cmd --reload"]
power_state:
  mode: reboot
  condition: test -f /var/run/reboot-required
```

**注意**：做範本時最容易漏的是「身分」——`machine-id`、SSH host key、cloud-init 的 instance 紀錄若沒清，複製出來的每台都會長得一模一樣（journald、DHCP client-id、host key 全部撞在一起）。

- 🟠 Ubuntu 雲端映像 `50-cloud-init.yaml` 會管網路；要自訂 Netplan 需先停用 cloud-init 網路設定（05 章 §3.1）。
- 🔵 Fedora Cloud 映像預設使用者 `fedora`；Ubuntu 為 `ubuntu`。
- 把 VM 做成範本：`cloud-init clean --logs`、清 `/etc/machine-id`、清 SSH host key（下次開機 🟠 自動重生；🔵 `sshd-keygen.target` 自動重生）、`virt-sysprep`（🟠 `libguestfs-tools`；🔵 `guestfs-tools`）。

---

## 3. 容器

**名詞與關係**：容器技術裡有三個名詞永遠一起出現。**映像（image）**是應用與其相依打包成的唯讀檔案層；**OCI**（Open Container Initiative）是映像格式與執行環境的業界標準，所以 Docker 建的映像 Podman 能原樣執行；**registry**（Docker Hub、`registry.fedoraproject.org`）是存放與分發映像的伺服器；**引擎**（Docker、Podman）則負責從 registry 拉映像、用核心的 namespace / cgroup 建立隔離的程序並管理其生命週期。關係是：引擎 ──拉──▶ registry 上的 OCI 映像 ──執行成──▶ 容器。下面 §3.1 選引擎、§3.2 讓 systemd 管容器、§3.3 談跑整個系統的容器。

**為什麼**：容器把應用與其相依打包成映像，同一份映像在 Ubuntu 與 Fedora 上跑起來一模一樣，是消除「在我機器上可以」的最直接手段；本節依「選哪個引擎 → 怎麼交給 systemd 管 → 需要整個系統時用什麼」的順序展開。

### 3.1 Docker vs Podman

**名詞與關係**：兩個引擎的差異全在「有沒有 daemon」這一點衍生出來，下表的名詞依此串連：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Docker** | 最早普及的容器引擎，所有容器由一個常駐的 `dockerd` daemon 統一建立與管理 | CLI 透過 Unix socket 與 daemon 溝通；daemon 以 root 執行 |
| **daemon / socket** | daemon 是常駐背景程序；`/var/run/docker.sock` 是 CLI 與它溝通的 Unix socket | 能寫這個 socket 就能叫 daemon 以 root 做任何事，所以 `docker` 群組等同 root |
| **Podman** | Red Hat 主導的無 daemon 容器引擎，CLI 與 Docker 相容 | 每個容器都是使用者自己的子程序，因此能 rootless，也能自然交給 systemd 管 |
| **rootless** | 以一般使用者身分、不需 root 執行容器；靠 user namespace 把容器內的 root 映射到主機的普通 UID | 代價是綁 <1024 埠需調 sysctl，掛載主機目錄時要 `--userns=keep-id` 讓 UID 對齊 |
| **Compose** | 用一份 YAML 描述多容器應用的工具；Docker 為 `docker compose` plugin，Podman 用 `podman-compose` 或轉呼叫 docker-compose | 需要與既有 Compose 檔完全相容時，是選 Docker 的主要理由 |
| **Quadlet** | Podman 4.4+ 的 systemd 整合：把 `.container` 檔自動轉成 systemd unit（§3.2） | 取代 `podman generate systemd`；Docker 沒有對應機制，得手寫 unit |
| **netavark** | Podman 4+ 的網路後端（取代 CNI），負責容器網路、NAT 與埠轉發 | 透過 nftables / firewalld 規則運作，不像 Docker 直接改 iptables 而繞過 ufw |
| **`:z` / `:Z`** | 掛載 volume 時要求引擎把主機目錄重新標成容器可讀的 SELinux 標籤（`z` 多容器共用、`Z` 單一容器專屬） | 🔵 SELinux enforcing 下不加就 Permission denied；容器程序本身跑在 `container_t` domain |
| **`loginctl enable-linger`** | 讓該使用者的 systemd user session 在未登入時也持續存在 | rootless 容器的 user unit 要開機自啟、登出後不被殺，必須開 linger |
| **`registries.conf`** | `/etc/containers/registries.conf`，Podman 決定短名稱（如 `nginx`）要去哪些 registry 找的設定 | 🟠 預設沒列 docker.io，`podman pull nginx` 會要你選或直接失敗，所以要加 `unqualified-search-registries` |

**為什麼**：兩者 CLI 幾乎相容，差在架構：Docker 有一個以 root 執行的 daemon，任何能碰到 socket 的人等同 root，而且它直接改 iptables 會繞過 ufw（05 章 §6）；Podman 無 daemon、預設 rootless，容器和 systemd / firewalld / SELinux 的整合乾淨得多。選擇原則：需要跟 Docker Hub 生態、Compose 檔或第三方文件完全一致時用 Docker；新部署、伺服器上要用 systemd 管容器時優先 Podman。

兩個發行版在這裡的預設不同，是因為 Red Hat 自 RHEL 8 起以 Podman 取代 Docker 作為官方容器工具鏈，Fedora 跟著預設安裝；Ubuntu 則沒有預設容器引擎，Docker 由官方 repo 或 `docker.io` 套件提供，Podman 也在套件庫但版本較舊。

| | Docker | Podman |
|---|---|---|
| 架構 | daemon（root） | 無 daemon、rootless 為主 |
| 預設於 | 🟠 需安裝（官方 repo，03 章）；`docker.io` 套件是 Ubuntu 打包版 | 🔵 Workstation 預設安裝；Ubuntu 亦可 `apt install podman` |
| CLI | `docker` | `podman`（相容 Docker CLI；`alias docker=podman`） |
| Compose | `docker compose`（plugin） | `podman-compose` 或 `podman compose`（呼叫 docker-compose） |
| systemd 整合 | 自己寫 unit | **Quadlet**（`.container` 檔 → 自動產生 unit） |
| 防火牆互動 | 直接改 iptables，繞過 ufw（05 章 §6） | netavark + firewalld / nftables，較乾淨 |
| SELinux | 需 `:z` / `:Z`（Fedora） | 同，且預設 `container_t` 隔離 |

**怎麼做**：Docker 裝好後先設 `daemon.json`——沒設日誌上限的容器會把 `/var/lib/docker` 塞滿磁碟，`live-restore` 讓 daemon 重啟時容器不中斷，`default-address-pools` 避免預設 172.17 網段與公司內網衝突。Podman 則注意 rootless 的三件事：SELinux 標籤 `:Z`、`enable-linger` 讓使用者服務在未登入時也能跑、綁 <1024 埠需調 sysctl：

```bash
# Docker（兩者，官方 repo 見 03 章 §3.2）
sudo systemctl enable --now docker
sudo usermod -aG docker alice                      # ⚠️ docker 群組等同 root
sudo tee /etc/docker/daemon.json <<'EOF'
{ "log-driver": "json-file", "log-opts": {"max-size": "50m", "max-file": "5"},
  "live-restore": true, "default-address-pools": [{"base": "10.200.0.0/16", "size": 24}] }
EOF
sudo systemctl restart docker
# 🔵 Docker + SELinux：docker run -v /data:/data:Z ...；🔵 Docker + firewalld：Docker 會建立 docker zone

# Podman（🔵 預設；🟠 apt install podman）
podman run -d --name web -p 8080:80 docker.io/library/nginx
podman ps; podman logs web; podman stats
podman run --rm -it --userns=keep-id -v "$PWD":/work:Z fedora:44 bash     # rootless + SELinux 標籤
podman generate systemd --new --name web > ~/.config/systemd/user/web.service   # 舊做法
loginctl enable-linger $USER                                              # rootless 服務開機自啟
# rootless 綁 <1024 埠：sysctl net.ipv4.ip_unprivileged_port_start=80
# 🟠 Ubuntu 上 Podman 的 registries：/etc/containers/registries.conf 加 unqualified-search-registries = ["docker.io"]（🔵 預設已含 registry.fedoraproject.org、docker.io 等）
```

### 3.2 Quadlet（Podman 4.4+，兩者皆可）

**名詞與關係**：Quadlet 本身在 §3.1 已定義，這裡多出來的是它周邊的四個名詞：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **`.container` 檔** | 放在 `/etc/containers/systemd/`（rootless 為 `~/.config/containers/systemd/`）的 ini 檔，`[Container]` 段描述映像、埠、volume，其餘段落就是一般 systemd unit | systemd 的 generator 在每次 `daemon-reload` 把它轉成 `<檔名>.service`，所以只要維護這一份 |
| **`podman auto-update`** | 檢查帶 `AutoUpdate=registry` 的容器，其映像 tag 在 registry 上是否有新版，有就拉新並重啟 | 由 `podman-auto-update.timer` 定期觸發；只對「會移動的 tag」有意義 |
| **tag / digest** | tag 是映像的可變別名（`nginx:1.27` 會隨修補版換內容）；digest 是內容的 sha256，永不改變 | 指 tag 才能自動更新；指 digest 等於釘死版本 |
| **HealthCmd / HealthOnFailure** | 容器內定期執行的健康檢查指令，與檢查失敗時的動作（Podman 5 可 kill / restart） | 🟠 4.9 只更新健康狀態不採取動作，要靠 systemd 的 `Restart=` 補 |

**為什麼**：`podman generate systemd` 產生的 unit 是一次性的快照，改了參數要重產生、升級 Podman 後格式可能過時；Quadlet 反過來讓你只維護一個宣告式的 `.container` 檔，systemd 每次 `daemon-reload` 都重新產生 unit，容器因此享有 systemd 的開機順序、重啟策略、日誌與依賴管理，且能配合 `podman auto-update` 自動拉新映像。

**怎麼做**：把容器描述寫成 ini 格式，放在 `/etc/containers/systemd/web.container`（rootless 放 `~/.config/containers/systemd/`）：

```ini
[Unit]
Description=Nginx web
After=network-online.target

[Container]
Image=docker.io/library/nginx:1.27
PublishPort=8080:80
Volume=/srv/www:/usr/share/nginx/html:Z,ro
Environment=TZ=Asia/Taipei
AutoUpdate=registry
HealthCmd=curl -f http://localhost/ || exit 1

[Service]
Restart=always

[Install]
WantedBy=multi-user.target default.target
```

之後就像一般 systemd 服務一樣操作；`quadlet -dryrun` 可以在 reload 前先看產生的 unit 是否如預期：

```bash
sudo systemctl daemon-reload && sudo systemctl enable --now web.service    # unit 名 = 檔名.service
/usr/libexec/podman/quadlet -dryrun                                        # 檢查產生的 unit（實測兩者路徑相同）
sudo podman auto-update --dry-run; systemctl enable --now podman-auto-update.timer
# .pod / .network / .volume / .kube（直接跑 Kubernetes YAML）也支援
```

**注意**：`AutoUpdate=registry` 搭配的映像 tag 要是會移動的（如 `1.27`），指到固定 digest 就永遠不會更新；健康檢查失敗時 Podman 5 才會依 `HealthOnFailure` 處理，4.9（🟠）只記錄狀態。

### 3.3 系統容器：LXD / Incus（🟠）與 systemd-nspawn / Toolbox（🔵）

**名詞與關係**：這一節的工具都屬「系統容器」，差別只在誰維護、跑在什麼底層上：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **系統容器** | 跑完整 init（systemd）與多個服務的容器，對使用者像一台輕量 VM，但共用主機核心 | 與應用容器（一容器一程序）的差別在「裡面有沒有 init」，隔離技術相同 |
| **LXD** | Canonical 的系統容器與 VM 管理器，只以 snap 發行；`lxc` 是它的 CLI | 底層用 LXC；🟠 官方主推；因 Canonical 改授權與治理模式，社群 fork 出 Incus |
| **Incus** | LXD 的社群分支（2023 年起由 LXC 專案維護），指令與 LXD 幾乎相同 | Debian / Ubuntu 套件庫都有 `incus`，不想依賴 snap 時的替代 |
| **snap** | Canonical 的自包含套件格式與商店（03 章） | LXD 只透過 snap 發行，是 Ubuntu 上裝 LXD 的唯一官方途徑 |
| **systemd-nspawn / machinectl** | systemd 內建的容器執行器，把一個目錄樹或映像當成容器開機；`machinectl` 是管理 CLI | 🔵 由 `systemd-container` 套件提供；適合跑整個發行版 rootfs，與 Podman 定位不同 |
| **Toolbox** | Fedora 開發的工具：用 Podman 建一個與主機共用家目錄、可 sudo 的開發用容器 | 為不可變桌面（Silverblue）而生，讓人在容器裡裝開發工具而不動主機系統 |
| **Distrobox** | 與 Toolbox 概念相同但支援任意發行版映像的社群工具，底層可用 Podman 或 Docker | 兩邊都有套件；「在 Fedora 裡開一個 Ubuntu 環境」用它最方便 |

**為什麼**：應用容器（Docker / Podman）一個容器跑一個程序；系統容器則跑完整的 init 與多個服務，像輕量 VM 但共用核心，適合「要一台乾淨的 Ubuntu / Fedora 測東西，但不想開 VM」。兩個發行版在這塊各自押寶不同工具：Canonical 主推自家的 LXD（snap 發行），Fedora 則走 systemd 生態的 nspawn 與桌面導向的 Toolbox。

**怎麼做**：

- 🟠 Ubuntu：**LXD**（`snap install lxd`；`lxd init`；`lxc launch ubuntu:24.04 c1`）— Canonical 的系統容器與 VM 管理；社群分支 **Incus**（apt 有 `incus` 套件）。
- 🔵 Fedora：**Toolbox**（`toolbox create; toolbox enter`）給開發環境；**Distrobox**（兩者皆有）跑任意發行版；`systemd-nspawn`（`dnf install systemd-container`；`machinectl`）。

---

## 4. 虛擬化：KVM / libvirt（兩者共通）

**名詞與關係**：KVM 一節的名詞是一個明確的堆疊，從使用者工具往下到 CPU：

```
virt-manager（GUI）／ Cockpit cockpit-machines ／ virsh（CLI）／ virt-install ／ virt-clone
        └──────────── 全部透過 libvirt API（用 XML 描述 VM、虛擬網路、儲存池）────────────┘
                                   │
                    libvirt daemon（🟠 libvirtd 單體；🔵 virtqemud 等 modular daemons）
                                   │ 啟動並管理
                        QEMU 程序（模擬主機板、磁碟、網卡、顯示）
                                   │ 透過 /dev/kvm 把 CPU 指令交給硬體直接執行
                        KVM 核心模組（kvm_intel / kvm_amd） ← 需要 CPU 的 VT-x / AMD-V
   旁支：sVirt（🔵 SELinux virt_image_t、🟠 AppArmor per-VM profile）把每台 VM 的 QEMU 程序與映像檔關在自己的標籤裡
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **KVM** | Linux 核心內建的 hypervisor 模組（`kvm_intel` / `kvm_amd`），把 CPU 的硬體虛擬化功能暴露成 `/dev/kvm` | 本身不模擬任何裝置，一定要搭 QEMU |
| **VT-x / AMD-V** | Intel / AMD CPU 的硬體虛擬化指令集（`/proc/cpuinfo` 的 `vmx` / `svm`） | 沒有它 KVM 載不起來，只能純軟體模擬（慢十倍以上） |
| **QEMU** | 使用者空間的機器模擬器，負責虛擬主機板、磁碟、網卡、顯示 | 有 KVM 時 CPU 指令直接跑在硬體上，QEMU 只負責 I/O |
| **libvirt** | 統一的虛擬化管理 API 與 daemon，用 XML 描述 VM、虛擬網路（`net-list`）與儲存池（`pool-list`） | 上面所有前端都透過它；也支援 Xen、LXC，但本節只用 QEMU/KVM |
| **libvirtd / modular daemons** | 🟠 `libvirtd` 是單一 daemon；🔵 拆成 `virtqemud`、`virtnetworkd`、`virtstoraged` 等各管一塊 | Fedora 保留 `libvirtd` 相容 socket，所以 `enable --now libvirtd` 兩邊都能用 |
| **virsh** | libvirt 的 CLI：開關機、快照、`dumpxml`、`console` 都由它 | 加入 `libvirt` 群組後透過 polkit 授權，不需 sudo |
| **virt-install / virt-clone / virt-manager** | 分別是建 VM 的 CLI、複製 VM 的 CLI、桌面 GUI（🟠 `virtinst` / 🔵 `virt-install` 套件提供前兩者） | 都是 libvirt 的前端；Cockpit 的 `cockpit-machines` 也是（§8） |
| **os-variant / libosinfo** | libosinfo 是「各 OS 版本適合什麼虛擬硬體」的資料庫；`--os-variant` 告訴 virt-install 這台 VM 要裝哪個 OS | 選對才會拿到 virtio 磁碟 / 網卡等最佳裝置；`osinfo-query os` 列出可用名稱 |
| **快照（snapshot）** | libvirt 對 VM 磁碟（qcow2 內部快照）與記憶體狀態的時間點紀錄 | 升級前 `snapshot-create-as`、失敗就 `snapshot-revert`，是 §10 清單的第一項 |
| **virt-sysprep** | libguestfs 工具，離線清除複製後 VM 的 machine-id、host key、日誌（§2） | `virt-clone` 之後必跑，否則複製品身分重複 |
| **Kickstart / autoinstall** | 🔵 Anaconda / 🟠 Subiquity 的無人值守安裝描述檔（01 章 §5） | 透過 `--location` + `--extra-args inst.ks=` 餵給 VM，配合 `--graphics none` 走序列埠 |
| **sVirt** | libvirt 用 MAC（🔵 SELinux、🟠 AppArmor）為每台 VM 的 QEMU 程序與映像檔配上專屬標籤 / profile 的機制 | 映像放非預設路徑被擋，就是 sVirt 在做事；解法是改標籤，不是關 MAC |
| **巢狀虛擬化（nested）** | 讓 VM 裡面再跑 KVM | 需 `kvm_intel nested=1`（04 章 §5.4）；測試 libvirt 本身或跑 Kubernetes 時用得到 |
| **VirtualBox / Vagrant / Proxmox VE** | 桌面型 hypervisor／用描述檔自動建開發 VM 的工具／以 Debian 為基礎的獨立虛擬化平台 | 都不屬於 libvirt 生態，但 Vagrant 有 libvirt provider 可搭 KVM |

**為什麼**：KVM 是 Linux 核心內建的 hypervisor，效能接近原生且不需額外授權；libvirt 在上面提供統一的 XML 描述、快照、複製與網路 / 儲存池管理，`virsh` / `virt-manager` / Cockpit 都透過它操作，所以學一套 API 就能同時管本機與遠端。測試發行版升級、跑 Kickstart / autoinstall、隔離不信任的軟體，KVM 都比實體機便宜得多。

**怎麼做**：先確認 CPU 有 VT-x / AMD-V，再裝套件並把使用者加入 `libvirt` 群組（否則每個 `virsh` 都要 sudo）。兩邊套件名不同：Ubuntu 24.04 沒有 `qemu-kvm` 這個套件名，要用 `qemu-system-x86`；Fedora 直接裝 `@virtualization` 群組。之後建 VM、快照、複製、清理範本的常用指令如下：

```bash
egrep -c '(vmx|svm)' /proc/cpuinfo                     # > 0 支援
# 🟠
sudo apt install -y qemu-system-x86 libvirt-daemon-system libvirt-clients virtinst virt-manager cloud-image-utils   # 實測 24.04 無 qemu-kvm 套件名，用 qemu-system-x86
sudo usermod -aG libvirt,kvm alice
# 🔵
sudo dnf install -y @virtualization                     # 群組：qemu-kvm libvirt virt-install virt-manager 等
sudo dnf install -y virt-install libvirt-daemon-config-network cloud-utils   # 若只要 CLI
sudo usermod -aG libvirt alice
# ⚪
sudo systemctl enable --now libvirtd                    # 🔵 Fedora 42+ 改為 modular daemons（virtqemud 等），libvirtd 為相容 socket；enable libvirtd 仍可用
virsh list --all; virsh net-list --all; virsh pool-list
virt-install --name vm1 --memory 4096 --vcpus 2 --disk size=40,pool=default \
  --cdrom /iso/ubuntu-24.04.4-live-server-amd64.iso --os-variant ubuntu24.04 --network network=default --graphics vnc
virt-install ... --location https://download.fedoraproject.org/pub/fedora/linux/releases/44/Server/x86_64/os/ \
  --os-variant fedora-rawhide --extra-args "inst.ks=http://10.0.0.5/ks.cfg console=ttyS0" --graphics none   # 🔵 Kickstart 自動安裝
osinfo-query os | grep -iE "ubuntu24|fedora"            # os-variant 名稱（libosinfo）
virsh start vm1; virsh shutdown vm1; virsh destroy vm1; virsh console vm1
virsh snapshot-create-as vm1 pre-upgrade; virsh snapshot-revert vm1 pre-upgrade
virsh dumpxml vm1 > vm1.xml; virsh define vm1.xml
virt-clone --original vm1 --name vm2 --auto-clone
sudo virt-sysprep -d vm2 --operations defaults,-ssh-userdir   # 清 machine-id / host key（🟠 libguestfs-tools；🔵 guestfs-tools）
# 橋接網路給 VM：05 章的 br0；libvirt 用 --network bridge=br0
# 🔵 SELinux：VM 映像放非預設路徑要 semanage fcontext -a -t virt_image_t "/vmstore(/.*)?" && restorecon -R /vmstore
# 🟠 AppArmor：libvirt 有 per-VM profile（/etc/apparmor.d/libvirt/），映像放非標準路徑需加到 abstractions/libvirt-qemu
# 巢狀虛擬化：options kvm_intel nested=1（04 章 §5.4）
# 圖形管理：virt-manager（本機 / SSH 遠端）、Cockpit 的 cockpit-machines 外掛（兩者皆有）
```

**注意**：VM 映像放到 `/var/lib/libvirt/images` 以外的路徑時，兩邊的 MAC 都會擋——🔵 SELinux 要 `virt_image_t` 標籤、🟠 AppArmor 要加進 abstraction——症狀是 `virsh start` 報 permission denied 但檔案權限明明正確。`virt-clone` 出來的 VM 記得跑 `virt-sysprep`，否則和 §2 一樣會有重複的 machine-id 與 host key。

其他：VirtualBox（03 章）、Vagrant（`apt install vagrant` / `dnf install vagrant` + libvirt provider）、Proxmox VE（Debian 系，獨立產品）。

---

## 5. 時間服務：當 NTP 伺服器

**名詞與關係**：對時這件事只有一個協定與一個實作，其他名詞都是它的設定項：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **NTP** | Network Time Protocol，透過網路對時的協定（UDP 123） | chrony 是它的實作；PTP 是精度更高的另一套協定 |
| **chrony / chronyd / chronyc** | 兩個發行版預設的 NTP 實作：`chronyd` 是 daemon（既當 client 也能當 server），`chronyc` 是查詢與控制 CLI | 設定檔路徑兩邊不同；加了 `allow` 就從 client 變成 server |
| **stratum** | NTP 的層級：0 是原子鐘 / GPS，1 是直接接它的伺服器，每經一層加 1，數字越大越不可信 | `local stratum 10` 指「上游斷線時我自稱第 10 層」，數字刻意設大，是為了上游一恢復就會被優先採用 |
| **`allow`** | chrony.conf 指令，開放哪些網段可以向本機查詢時間 | 預設不允許任何 client，所以想當 server 必加 |
| **PTP / linuxptp** | Precision Time Protocol（IEEE 1588），靠網卡硬體時戳達到微秒級精度；`linuxptp` 是 Linux 實作 | 金融、電信、量測才需要；一般內網 NTP 的毫秒級已足夠 |

**為什麼**：內網幾十台機器各自對外連 NTP 既浪費對外連線，也可能因防火牆或對外斷線而各自飄移；時間不同步會讓 Kerberos / TLS 憑證驗證失敗、日誌無法對照、叢集（10 章）誤判節點故障。挑一兩台當內網 NTP 伺服器，其他機器指向它，對外斷線時內網仍保持一致（`local stratum 10` 就是為此）。

**怎麼做**：兩者都用 chrony，只是設定檔路徑不同（🟠 `/etc/chrony/chrony.conf`、🔵 `/etc/chrony.conf`）；加上 `allow` 開放內網來源、`local stratum` 允許失去上游時仍服務，再開防火牆 123/udp：

```bash
# 🟠 apt install chrony（/etc/chrony/chrony.conf）；🔵 預設 chrony（/etc/chrony.conf）
# 在 conf 加：
#   allow 192.168.0.0/16
#   local stratum 10          # 上游斷線時仍服務內網
sudo systemctl restart chronyd
chronyc clients; chronyc sources -v; chronyc tracking
# 🟠 ufw allow 123/udp    🔵 firewall-cmd --permanent --add-service=ntp && firewall-cmd --reload
# PTP（微秒級）：linuxptp（兩者套件同名）
```

---

## 6. 郵件通知（讓 cron / 監控 / fail2ban 能寄信）

**名詞與關係**：一封系統信從產生到寄出會經過「寫信程式 → 本機 MTA → 外部 relay」三站，名詞都對應其中一站：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **MTA** | Mail Transfer Agent，負責把信送到下一台郵件伺服器的程式（postfix、sendmail、msmtp 都是） | cron、`mail` 指令都只呼叫本機的 `/usr/sbin/sendmail` 介面，誰提供這個介面誰就在送信 |
| **SMTP relay** | 代為轉送郵件的外部 SMTP 伺服器（公司郵件伺服器、Gmail、SES…），需帳號密碼認證 | 自己直接對外投遞會因沒有反向 DNS / SPF 被當垃圾信，所以一律經 relay |
| **msmtp / msmtp-mta** | 只會「經 relay 送出」的極簡 MTA；🟠 `msmtp-mta` 套件把它註冊為系統的 `sendmail` | 🔵 `msmtp` 套件本身即含 sendmail 相容連結；設定檔是 `/etc/msmtprc` |
| **`mail` / mailx / mailutils / s-nail** | 命令列寫信程式（MUA）；`mailx` 相容指令 🟠 由 `mailutils` 提供、🔵 由 `s-nail` 提供 | 它們只負責組信件內容，然後交給本機 MTA 的 sendmail 介面 |
| **postfix** | 功能完整的 MTA，可收可送、可排隊重試 | 需要接收郵件或處理大量退信時才用；設 `relayhost` 一樣能走 relay |
| **`/etc/aliases`** | 本機帳號到實際信箱的轉寄表（例如 `root: ops@example.com`） | 系統信都寄給 root，沒有這行就算 MTA 正常也沒人收到 |
| **CA bundle** | 系統信任的根憑證集合檔，用來驗證 relay 的 TLS 憑證 | 路徑兩邊不同（`tls_trust_file`），寫錯 TLS 握手失敗、信全部寄不出 |

**為什麼**：cron 的錯誤輸出、smartd 的磁碟警告、fail2ban 的封鎖通知、unattended-upgrades 的報告，預設都是「寄給 root」；沒有 MTA 時這些信全部消失在 `/var/spool/mail` 或直接被丟掉，等於監控做了但沒人看到。自架完整 MTA 又太重且會被當垃圾信來源，所以用 msmtp 這種只會「透過外部 SMTP relay 送出」的輕量 MTA 最划算。

**怎麼做**：裝 msmtp 與 `mail` 指令，設定 relay 帳號並把密碼存在 600 權限的檔案，寄一封測試信確認：

```bash
# 輕量 MTA（只送出、透過外部 SMTP relay）
sudo apt install -y msmtp msmtp-mta mailutils     # 🟠
sudo dnf install -y msmtp mailx                    # 🔵（s-nail 提供 mailx）
sudo tee /etc/msmtprc <<'EOF'
defaults
auth on
tls on
tls_trust_file /etc/ssl/certs/ca-certificates.crt      # 🟠；🔵 為 /etc/pki/tls/certs/ca-bundle.crt
logfile /var/log/msmtp.log
account default
host smtp.example.com
port 587
from alerts@example.com
user alerts@example.com
passwordeval "cat /etc/msmtp-pw"
EOF
sudo chmod 600 /etc/msmtprc /etc/msmtp-pw
echo "test body" | mail -s "test from $(hostname)" ops@example.com
# 完整 MTA：postfix（兩者套件同名；🟠 dpkg-reconfigure postfix、🔵 /etc/postfix/main.cf 設 relayhost）
```

**注意**：CA bundle 路徑兩邊不同（🟠 `/etc/ssl/certs/ca-certificates.crt`、🔵 `/etc/pki/tls/certs/ca-bundle.crt`），寫錯會 TLS 驗證失敗而完全寄不出去；`/etc/aliases` 把 root 轉到真人的信箱，否則系統信仍寄不到人手上。

---

## 7. 桌面與周邊（Workstation 相關）

**名詞與關係**：桌面一節的名詞多但各自獨立，表中依「畫面 → 驅動 → 周邊 → 軟體來源」的順序排列：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **GNOME / KDE Plasma** | 兩大桌面環境；🟠 出貨 GNOME 客製版、🔵 GNOME 原味與 KDE Edition | 桌面決定顯示管理器、軟體中心與設定工具是哪一套 |
| **Wayland / X11** | 顯示伺服器協定：X11 是 1980 年代的舊架構，Wayland 是取代它、由合成器直接管畫面的新協定 | 🔵 GNOME 已移除 X11 session；NVIDIA 專有驅動對 Wayland 支援較晚，是很多「沒畫面」問題的來源 |
| **顯示管理器（gdm / gdm3）** | 登入畫面程式，登入後啟動桌面 session | 套件名 🟠 `gdm3`、🔵 `gdm`，功能相同 |
| **RPM Fusion** | Fedora 的第三方套件庫，收 Fedora 因授權政策不收的專有驅動與編解碼器 | `akmod-nvidia`、非 free 版 ffmpeg 都來自這裡 |
| **akmod / `ubuntu-drivers`** | 🔵 akmods 在每次核心升級後自動重編第三方模組；🟠 `ubuntu-drivers` 偵測硬體並安裝 Canonical 打包好的驅動 | Secure Boot 下兩者的模組都要 MOK 簽章（01 章 §1.5、14 章 §4.2） |
| **CUPS** | Common Unix Printing System，兩邊共用的列印服務，Web 管理介面在 631 埠 | 套件與介面兩邊相同，是本表少數無差異的項目 |
| **IBus** | 輸入法框架；`ibus-chewing`（注音）/ `ibus-libpinyin`（拼音）是掛在它上面的引擎 | GNOME 兩邊都用 IBus，差在設定入口 |
| **PipeWire / BlueZ** | PipeWire 是統一處理音訊與視訊串流的服務（取代 PulseAudio）；BlueZ 是 Linux 藍牙堆疊 | 兩邊皆已預設 |
| **snap / Flatpak** | 兩種跨發行版沙箱化應用格式；🟠 App Center 以 snap 為主、🔵 GNOME Software 走 Flatpak + RPM | 03 章有細節；決定「軟體中心裝的東西從哪來」 |
| **power-profiles-daemon / tuned-ppd** | 提供「省電 / 平衡 / 效能」切換的服務；🔵 42 起改用 tuned 的相容實作 | 桌面電源選單背後就是它 |
| **fwupd / `fwupdmgr`** | 韌體更新服務與 CLI（14 章 §9） | 🔵 GNOME Software 會直接顯示韌體更新 |

**為什麼**：桌面環境的差異大多不是技術優劣，而是兩家對「專有軟體」的態度：Canonical 願意打包 NVIDIA 驅動與受限編解碼器，Fedora 基於授權政策只收自由軟體，專有部分全部交給第三方的 RPM Fusion；再加上 Fedora 追最新上游（Wayland only、PipeWire、tuned-ppd），Ubuntu 則以 LTS 穩定為主。知道這個差異，就能理解為何同樣一件事兩邊的套件名與做法都不同。

**怎麼做**：依下表對照安裝；🔵 的 NVIDIA `akmod` 每次核心升級後要等幾分鐘重編，升級完立刻重開機會進不了圖形介面，稍等再重開即可：

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 桌面 | GNOME（Ubuntu 客製）；Kubuntu / Xubuntu 等變體 | GNOME（原味）；KDE Plasma 為官方 Edition（Fedora 42+） |
| 顯示伺服器 | Wayland 預設（NVIDIA 亦可） | Wayland 預設；Fedora 41+ GNOME 已移除 X11 session |
| 顯示管理器 | `gdm3` | `gdm` |
| 顯示驅動 | `ubuntu-drivers install`（NVIDIA 專有驅動由 Canonical 打包） | RPM Fusion `akmod-nvidia`（每次核心升級自動重編，需等幾分鐘） |
| 編解碼器 | `ubuntu-restricted-extras` | RPM Fusion + `dnf swap ffmpeg-free ffmpeg` + `dnf group install multimedia` |
| 印表機 | CUPS（`cups`；http://localhost:631） | CUPS（同） |
| 字型（中文） | `fonts-noto-cjk` | `google-noto-sans-cjk-fonts` |
| 輸入法 | IBus + `ibus-chewing` / `ibus-libpinyin` | IBus + `ibus-chewing` / `ibus-libpinyin`（GNOME 設定 → 鍵盤） |
| 藍牙 / 音效 | BlueZ、PipeWire（22.10+） | BlueZ、PipeWire |
| 軟體中心 | App Center（snap 為主） | GNOME Software（Flatpak + RPM） |
| 電源設定檔 | `power-profiles-daemon` | `tuned-ppd`（Fedora 42+） |
| 磁碟加密（桌面） | 安裝時 LUKS；TPM 自動解鎖為實驗功能 | 安裝時 LUKS；`systemd-cryptenroll` TPM2 |
| 韌體更新 | `fwupdmgr` | `fwupdmgr`（GNOME Software 整合） |

---

## 8. 多主機管理：Cockpit

**名詞與關係**：Cockpit 的設計重點是「不引入新後端」，所以它的名詞都是既有機制的延伸：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Cockpit** | Red Hat 開發的 Web 管理介面，透過 systemd / D-Bus / SSH 操作主機，不另起特權 daemon | 登入用系統帳號，權限就是該使用者的權限；串接其他主機走 SSH |
| **`cockpit.socket`** | socket activation：平時不佔資源，有人連 9090 才啟動 `cockpit-ws` | 所以 enable 的是 socket 不是 service |
| **外掛（cockpit-machines / -podman / -storaged / -selinux）** | 各自加一個頁面：管 libvirt VM（§4）、Podman 容器（§3）、磁碟 / LVM、SELinux | 都呼叫既有 API（libvirt、podman socket、udisks），沒有自己的後端 |
| **PAM** | Linux 的登入認證框架 | Cockpit 直接用它驗證，等於把密碼登入面搬到 Web，因此要限來源 |
| **backports** | 🟠 `noble-backports` 套件庫，提供較新版但未進主庫的套件 | 24.04 的 Cockpit 314 太舊時的替代來源 |

**為什麼**：不是每個操作都值得寫 playbook；看一台機器的負載、日誌、重啟服務、開個 VM，用瀏覽器比開 SSH 快，也讓不熟 CLI 的同事能安全地做基本維運。Cockpit 走 SSH 與 systemd 的 API，不另開特權 daemon，在一台上就能串起其他主機，是兩個發行版共同支援（Red Hat 開發、Ubuntu 有套件）的少數 Web 管理介面。

**怎麼做**：安裝核心與需要的外掛（虛擬機、容器、儲存；🔵 多一個 SELinux），啟用 socket 後開 9090 埠：

```bash
sudo apt install -y cockpit cockpit-machines cockpit-podman cockpit-storaged   # 🟠（外掛名稱相同）
sudo dnf install -y cockpit cockpit-machines cockpit-podman cockpit-storaged cockpit-selinux   # 🔵（Server 預設含基本）
sudo systemctl enable --now cockpit.socket
# https://host:9090；可在一台 Cockpit 加入其他主機（SSH），統一管理 systemd / 儲存 / 網路 / 容器 / VM / 日誌 / 更新
# 🟠 24.04 套件版 314；🔵 44 為 366（功能較新）
```

**注意**：9090 不要直接對外，放在 VPN 或防火牆限來源之後；Cockpit 登入用系統帳號密碼，等於把 PAM 登入面暴露到 Web，密碼弱就會被撞。🟠 24.04 的 314 版少了不少新功能（例如較新的 Podman 頁面），需要時改用 backports。

---

## 9. 系統資訊與資產清單

**名詞與關係**：清單工具分兩層——在單機上「讀出資訊」的指令，以及把多台資訊「集中起來」的系統：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **DMI / SMBIOS、`dmidecode`** | SMBIOS 是主機板韌體提供硬體資訊（型號、序號、記憶體插槽）的標準表，`dmidecode` 讀取它 | 報修用的序號只能從這裡拿；VM 裡看到的是 hypervisor 填的值 |
| **`lshw` / `inxi`** | `lshw` 掃描所有硬體並可輸出 JSON；`inxi` 把常見資訊排成一頁給人看 | 前者餵資產系統，後者給工程師 |
| **`lsblk` / `lspci` / `lscpu`** | 分別列磁碟（含序號）、PCI 裝置、CPU | util-linux / pciutils 內建，不需額外安裝 |
| **machine-id** | `/etc/machine-id`，systemd 的主機唯一 ID（§2） | 資產系統若用它當主鍵，複製 VM 沒重生就會撞 |
| **CMDB** | Configuration Management Database，集中記錄所有資產與其設定的資料庫 | Ansible facts、osquery、GLPI / Snipe-IT 都是把資料餵進去的途徑 |
| **osquery** | 把系統狀態（套件、程序、使用者、硬體）當 SQL 表查詢的代理程式，可集中收集 | 適合持續監控與稽核；兩邊有官方 repo |
| **GLPI / Snipe-IT** | 開源 IT 資產管理系統，搭配 agent 自動回報 | 需要「誰在用哪台、保固到期」這類非技術欄位時用 |
| **neofetch / fastfetch** | 顯示系統資訊與發行版 logo 的一頁式工具 | neofetch 已停止維護，fastfetch 是繼任者；🟠 24.04 尚未收錄 |

**為什麼**：出事時第一個問題永遠是「這台是什麼機器、哪個版本、序號多少、有哪些套件」；沒有清單就得臨時登入一台台查，硬體壞了也拿不出序號報修。定期把這些資訊收成檔案（或餵進 CMDB），升級、汰換與稽核都省下大量時間。

**怎麼做**：下面幾條涵蓋主機身分、硬體、網卡與磁碟；`lshw -json` 適合做成機器可讀的清單，`inxi` 適合人看：

```bash
hostnamectl; cat /etc/os-release; uname -a
sudo dmidecode -s system-manufacturer; sudo dmidecode -s system-product-name; sudo dmidecode -s system-serial-number
lscpu | head; free -h; lsblk -d -o NAME,SIZE,MODEL,SERIAL; ip -br a; lspci -nn | grep -iE "vga|ethernet|nvme"
sudo lshw -json > /var/lib/inventory/$(hostname).json    # 完整硬體 JSON
inxi -Fxz                                                # 一頁式（兩者套件 inxi）
neofetch / fastfetch                                     # 漂亮版（🔵 fastfetch 在套件庫；🟠 24.04 無 fastfetch，用 neofetch 或 PPA）
# 套件清單：🟠 apt list --installed；🔵 dnf list --installed
# 系統唯一 ID：/etc/machine-id（複製 VM 後必須重生）
# 集中式資產：Ansible facts 收集到 CMDB、osquery（兩者官方 repo）、GLPI / Snipe-IT + agent
```

**注意**：`/etc/machine-id` 是很多工具（journald、systemd-networkd 的 DHCP ID、資產系統）用來辨識主機的鍵，複製 VM 後沒重生就會出現「兩台機器在清單裡是同一台」。

---

## 10. 發行版升級實務清單（承 03 章 §9）

**名詞與關係**：清單裡的名詞分屬「升級前保命」與「升級後收尾」兩組：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **release notes** | 每個版本的官方變更說明 | 升級前讀，重點看「被移除的套件」與「預設值改變」 |
| **快照（LVM / Btrfs / VM）** | 升級前的檔案系統或 VM 時間點副本（11 章、§4） | 唯一可靠的「回得去」；沒有快照失敗就只能重灌 |
| **tmux / screen** | 終端多工器，SSH 斷線後程序仍在 session 裡繼續跑 | 升級中途 SSH 斷掉而 dpkg / dnf 卡一半，是最常見的災難 |
| **備援 SSH 埠** | 升級期間另開一個 sshd（🟠 `do-release-upgrade` 自動開 1022） | 升級中若原 sshd 重啟失敗，還有第二條路進去 |
| **`.dpkg-dist` / `.dpkg-old`** | 🟠 dpkg 遇到「你改過、新版也改了」的設定檔時，保留你的版本、把新版存成 `.dpkg-dist`；反之則舊版存成 `.dpkg-old` | 沒手動合併，新版需要的設定就不會生效 |
| **`.rpmnew` / `.rpmsave`、`rpmconf`** | 🔵 rpm 的同一機制：新版存 `.rpmnew`、舊版存 `.rpmsave`；`rpmconf -a` 逐一互動合併 | 與上一列一一對應 |
| **`remove-retired-packages`** | 🔵 工具，列出並移除已從 Fedora 下架、不再更新的套件 | 留著會變成永不更新的安全漏洞 |
| **`fixfiles -B onboot` / `.autorelabel`** | 🔵 排程下次開機重標 SELinux 標籤；`-B` 只處理上次重標之後改過的檔案 | policy 版本變了標籤定義也變，不重標會出零星 AVC |
| **`aa-status`** | 🟠 顯示 AppArmor 已載入的 profile 與其模式 | 升級後確認 profile 沒被停用 |

**為什麼**：大版本升級是一次同時更換核心、glibc、systemd 與上百個套件的操作，失敗時通常不是「某個服務壞掉」而是「開不了機」或「SSH 斷線後升級卡一半」。03 章 §9 講的是怎麼下指令，這裡補的是「怎麼確保出事能回得去、升級完能驗證」；每一項都對應一個真實踩過的坑。

**怎麼做**：升級前逐項打勾，重點是可還原（快照）、不會被斷線中斷（tmux + 備援埠）、知道第三方 repo 要重啟用：

升級前：
- [ ] 備份 + 快照（LVM / Btrfs / VM），確認可還原
- [ ] `systemctl --failed` 為空；磁碟 `/`、`/boot`、`/var` 有 ≥ 2 GB / 500 MB / 3 GB 空間
- [ ] 記錄第三方 repo（PPA / COPR / Docker / Grafana）與釘選，升級後要重新啟用
- [ ] 在 tmux / screen 中執行；遠端主機先開備援 SSH 埠（🟠 升級程式自動開 1022）
- [ ] 讀 release notes（🟠 <https://discourse.ubuntu.com/c/release>；🔵 <https://docs.fedoraproject.org/en-US/fedora/latest/release-notes/>）

升級後的重點是處理「套件管理器不敢覆寫的設定檔」（🟠 `.dpkg-dist`、🔵 `.rpmnew`）——這些新版設定沒合併進去，常在下一次重啟服務時才爆：

升級後：
- [ ] `cat /etc/os-release`；`uname -r`（新核心已啟動）
- [ ] 🟠 處理 `.dpkg-dist` / `.dpkg-old`：`find /etc -name "*.dpkg-*"`；🔵 `.rpmnew` / `.rpmsave`：`sudo rpmconf -a`
- [ ] `systemctl --failed`；`journalctl -p err -b`
- [ ] 🔵 `sudo dnf autoremove; sudo dnf install remove-retired-packages && remove-retired-packages`；🟠 `sudo apt autoremove --purge`
- [ ] 重新啟用第三方 repo，`apt update` / `dnf makecache` 無錯誤
- [ ] 🔵 SELinux：`sudo fixfiles -B onboot`（大版本後建議重標籤）；🟠 `aa-status` 確認 profile 正常
- [ ] 應用與監控正常；移除升級前快照

**注意**：🔵 大版本升級後 SELinux policy 可能改了檔案標籤定義，不重標籤會出現零星的 AVC 拒絕，`fixfiles -B onboot` 只重標籤上次升級後改過的檔案，比 `touch /.autorelabel` 快很多。

---

## 11. 本章差異總結

本章大部分工具（Ansible、cloud-init、KVM、chrony、msmtp、Cockpit）在兩個發行版上是同一套上游軟體，差異集中在三處：套件名與版本（Fedora 追新、Ubuntu LTS 凍結）、容器與桌面的預設選擇（Red Hat 生態押 Podman、自由軟體政策把專有驅動推給 RPM Fusion）、以及 MAC 與設定檔殘留的處理方式（AppArmor vs SELinux、dpkg vs rpm）。下表把這些差異集中列出，方便寫跨發行版 playbook 時查表：

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| Ansible 版本（套件庫） | 9.x / core 2.16 | 13.x / core 2.20 |
| Ansible os_family | `Debian` | `RedHat` |
| 雲端映像預設使用者 | `ubuntu` | `fedora` |
| 容器預設 | 無（Docker 官方 repo 或 `docker.io`） | Podman 預設安裝 |
| Podman 版本（套件庫） | 4.9 | 5.x |
| 系統容器 | LXD（snap）/ Incus | Toolbox / Distrobox / nspawn |
| KVM 安裝 | `qemu-system-x86 libvirt-daemon-system virtinst` | `@virtualization` 群組 |
| libvirt daemon | `libvirtd` 單體 | modular（`virtqemud` 等） |
| VM 映像 MAC | AppArmor per-VM profile | SELinux `virt_image_t` |
| CA bundle 路徑 | `/etc/ssl/certs/ca-certificates.crt` | `/etc/pki/tls/certs/ca-bundle.crt` |
| mail 指令套件 | `mailutils` | `s-nail`（mailx） |
| 桌面 | GNOME（客製）、X11 仍可選 | GNOME（原味，Wayland only）、KDE Edition |
| NVIDIA | `ubuntu-drivers` | RPM Fusion akmod |
| 中文字型 | `fonts-noto-cjk` | `google-noto-sans-cjk-fonts` |
| 電源設定檔 | power-profiles-daemon | tuned-ppd |
| Cockpit 版本 | 314 | 366 |
| 升級後設定檔殘留 | `*.dpkg-dist` / `*.dpkg-old` | `*.rpmnew` / `*.rpmsave`（`rpmconf -a`） |
| 升級後 MAC | 無需 | 建議 `fixfiles -B onboot` |
