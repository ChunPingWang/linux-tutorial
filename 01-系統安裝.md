# 01 - 系統安裝

本章涵蓋從下載映像到完成第一次開機的完整流程，包含互動式安裝、分割區與 LVM / 加密規劃、無人值守安裝（Ubuntu autoinstall 與 Fedora Kickstart）、雲端映像及 WSL2。

**本章解決什麼問題**：安裝是整個生命週期裡最難「事後補救」的一步——分割區切錯、檔案系統選錯、忘了 LVM，日後不是重裝就是停機搬資料；映像沒校驗、Secure Boot 沒處理，則會在裝完 NVIDIA 驅動或第一次 `fwupd` 時才爆出來。本章的取捨原則是：**伺服器一律 UEFI + LVM + 獨立 `/boot`、`/var`、`/var/log`**，能自動化就不要手動點（autoinstall / Kickstart / cloud-init），並在安裝完成後立刻用同一份檢查清單驗收，而不是等使用者回報問題。

---

## 1. 安裝前準備

**為什麼**：安裝前的十分鐘準備（選對映像、校驗、確認韌體模式）能避開大多數「裝到一半失敗」與「裝完才發現不能用」的情況；這一節的每個步驟都對應一種實際踩過的雷。

### 1.1 選擇版本與映像

**為什麼**：映像選錯的代價很具體——用 Desktop 映像裝伺服器會多出整套 GUI 與 snap 服務吃記憶體；`netinst` 在沒有網路的機房裝不起來；雲端映像沒有安裝程式，插進實體機開機只會看到黑畫面。先依用途對照下表挑映像，再開始下載。

**怎麼做**：兩者的映像分類不同，來自發行節奏的差異——Ubuntu 以 LTS 為核心（每 2 年一版、支援 5 年，適合長期運行的伺服器）；Fedora 每 6 個月一版、只支援約 13 個月，定位是追上游最新技術的「前哨站」，因此映像種類更多、也更常變動。

| | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 下載頁 | <https://ubuntu.com/download> | <https://fedoraproject.org/> |
| 伺服器映像 | `ubuntu-24.04.x-live-server-amd64.iso`（Subiquity 文字安裝） | `Fedora-Server-dvd-x86_64-44-x.iso`、`Fedora-Server-netinst-...iso` |
| 桌面映像 | `ubuntu-24.04.x-desktop-amd64.iso` | `Fedora-Workstation-Live-x86_64-44-x.iso` |
| 最小化 | Server 安裝時勾選「Minimized」 | `Fedora-Everything-netinst`（可自選套件群組） |
| 雲端映像 | `ubuntu-24.04-server-cloudimg-amd64.img` | `Fedora-Cloud-Base-Generic-44-x.x86_64.qcow2` |
| 不可變（immutable）版本 | Ubuntu Core（snap-based） | Fedora Silverblue / Kinoite / CoreOS（rpm-ostree / bootc） |

💡 **伺服器一律選 Ubuntu LTS**；Fedora 若用於伺服器，要接受每 13 個月必須升級大版本一次。

### 1.2 校驗映像完整性

**為什麼**：下載中斷或鏡像站快取損壞會產生「看起來完整、裝到一半才出錯」的 ISO，浪費一整輪開機安裝時間；更嚴重的是被竄改的映像。`sha256sum` 只能證明檔案沒壞，**不能**證明 `SHA256SUMS` 本身來自官方——所以要再驗證校驗檔的 GPG 簽章，才算閉環。

**怎麼做**：先比對 SHA256，再驗證校驗檔的簽章。

⚪ 共通（在任何 Linux/macOS 上）：

```bash
# 校驗 SHA256
sha256sum ubuntu-24.04.4-live-server-amd64.iso
# 與官方的 SHA256SUMS 檔比對
sha256sum -c SHA256SUMS --ignore-missing
```

```bash
# 🟠 Ubuntu：驗證 SHA256SUMS 檔本身的 GPG 簽章
gpg --keyid-format long --keyserver hkp://keyserver.ubuntu.com \
    --recv-keys 0x46181433FBB75451 0xD94AA3F0EFE21092
gpg --verify SHA256SUMS.gpg SHA256SUMS

# 🔵 Fedora：驗證 CHECKSUM 檔簽章
curl -O https://fedoraproject.org/fedora.gpg
gpgv --keyring ./fedora.gpg Fedora-Server-44-1.x-x86_64-CHECKSUM
sha256sum -c Fedora-Server-44-1.x-x86_64-CHECKSUM --ignore-missing
```

**注意**：`gpg --verify` 顯示 `Good signature` 但附帶「not certified with a trusted signature」警告是正常的（你尚未簽署該金鑰）；只要金鑰指紋與官方頁面一致即可。

### 1.3 製作開機 USB

**為什麼**：`dd` 沒有任何確認機制，`of=` 寫成工作機的系統碟（例如筆電的 `/dev/sda`）會在幾秒內毀掉整顆硬碟，這是製作開機 USB 最常見、也最不可逆的事故。因此第一步永遠是先用 `lsblk` 確認裝置名稱與容量，再執行 `dd`。

**怎麼做**：

```bash
# ⚪ 共通：dd（⚠️ of= 一定要指向 USB 裝置本身，不是分割區；寫錯會毀掉硬碟）
lsblk -o NAME,SIZE,MODEL,TRAN          # 確認 USB 裝置名稱，例如 /dev/sdb
sudo dd if=./image.iso of=/dev/sdb bs=4M status=progress conv=fsync
```

不想碰 `dd` 或需要在 Windows 上製作，可用圖形化工具；若要同時帶多個 ISO 到機房（Ubuntu、Fedora、救援工具），Ventoy 只需一支 USB。

圖形化工具：
- 🟠 Ubuntu：內建「Startup Disk Creator」（`usb-creator-gtk`），Windows 上用 Rufus / balenaEtcher。
- 🔵 Fedora：官方 **Fedora Media Writer**（跨平台，可直接下載映像）。
- ⚪ 多映像 USB：**Ventoy**（一支 USB 放多個 ISO）。

### 1.4 硬體需求（實務建議）

**為什麼**：Fedora 44 要求 x86-64-v2，在 2009 年前後的舊 CPU（缺 SSE4.2 / POPCNT）上會直接無法開機而非顯示錯誤；記憶體低於 2 GB 時安裝程式本身可能被 OOM 砍掉；磁碟給太小則 §2 的分割規劃根本擺不下。先對照下表確認機器再動手，尤其是回收再利用的舊硬體。

**怎麼做**：

| | 最低 | 建議（伺服器） |
|---|---|---|
| CPU | x86-64-v2 以上（Fedora 44 需 x86-64-v2；Ubuntu 24.04 仍支援 v1 但 26.04 起建議 v2 以上） | 4 核以上 |
| 記憶體 | 2 GB（Server）/ 4 GB（Desktop） | 8 GB 以上 |
| 磁碟 | 25 GB | 50 GB 以上，SSD |
| 韌體 | BIOS 或 UEFI | **UEFI + Secure Boot** |

### 1.5 UEFI、Secure Boot 與 TPM

**為什麼**：Secure Boot 開啟時，核心只載入有簽章的模組——裝完 NVIDIA 或 ZFS DKMS 重開機後「驅動不見了 / 沒畫面」幾乎都是這個原因；而 CSM 混合模式下安裝程式可能以 BIOS 模式開機，裝出來的系統就沒有 ESP，之後 `fwupd` 韌體更新、`systemd-boot`、TPM 解鎖全都用不了，只能重裝。這些都要在安裝**前**決定。

**怎麼做**：

- 兩者都支援 **Secure Boot**（使用 Microsoft 簽署的 shim）。安裝第三方核心模組（NVIDIA、VirtualBox、ZFS DKMS）時：
  - 🟠 Ubuntu：DKMS 會提示設定 **MOK（Machine Owner Key）** 密碼，重開機時在 MokManager 中登錄。
  - 🔵 Fedora：RPM Fusion 的 `akmod-nvidia` 需手動 `mokutil --import` 登錄金鑰（或關閉 Secure Boot）。
- 建議在 UEFI 中關閉 **CSM / Legacy Boot**，使用純 UEFI 模式，之後才能用 `systemd-boot`、`fwupd` 韌體更新等功能。
- 若要 TPM2 自動解鎖 LUKS（免輸密碼開機）：兩者都可用 `systemd-cryptenroll`，見 [08 安全強化](08-安全強化.md)。

**注意**：裝完後用 `[ -d /sys/firmware/efi ]` 與 `mokutil --sb-state` 確認實際的開機模式（見 §8），不要只看 UEFI 設定畫面。

---

## 2. 分割區規劃

**為什麼**：分割區是安裝後最難更動的決定：`/` 與 `/var` 混在一起，日誌或容器映像塞滿磁碟時 SSH 都登不進去；XFS 切太大不能縮；沒有 LVM 的機器要加硬碟只能停機搬資料。花五分鐘規劃，換來的是之後幾年都能線上調整。

### 2.1 建議配置（伺服器）

**為什麼**：獨立切出 `/var` 與 `/var/log`，是因為真實事故裡塞爆磁碟的幾乎都是日誌、資料庫與容器映像——它們獨立後最多讓該服務出錯，不會把整台系統拖垮到連 root 都寫不了檔案。`/boot` 必須獨立，是因為 GRUB 在 LVM / LUKS 上讀核心不可靠；`/tmp` 獨立則能加 `noexec` 擋掉最常見的「下載到 /tmp 再執行」攻擊路徑。

**怎麼做**：

| 掛載點 | 大小 | 檔案系統 | 說明 |
|---|---|---|---|
| `/boot/efi` | 512 MB～1 GB | FAT32 | UEFI 必要（ESP） |
| `/boot` | 1～2 GB | ext4 / xfs | 放核心與 initramfs；LVM 或加密時必須獨立 |
| `/` | 20～50 GB | ext4（🟠）/ xfs（🔵） | 系統本體 |
| `/var` | 依需求 | 同上 | 資料庫、容器映像、日誌；獨立可防止塞爆 `/` |
| `/var/log` | 5～20 GB | 同上 | 日誌爆量時不影響系統 |
| `/home` | 剩餘 | 同上 | 使用者資料 |
| `/tmp` | 2～10 GB 或 tmpfs | — | 可加 `noexec,nosuid,nodev` 掛載選項（CIS 建議） |
| swap | 依記憶體 | swap | 見下表 |

**為什麼要 swap**：swap 不是「記憶體不夠才用」，而是 OOM killer 動手前的最後緩衝——沒有 swap 的伺服器在記憶體暫時吃緊時會直接砍掉資料庫或 JVM，而非變慢。若要休眠，swap 還必須放得下整個記憶體內容，所以數字更大。

**swap 建議**（Red Hat 準則）：

| 記憶體 | 建議 swap | 若需休眠（hibernate） |
|---|---|---|
| ≤ 2 GB | 2 × RAM | 3 × RAM |
| 2～8 GB | = RAM | 2 × RAM |
| 8～64 GB | 4 GB～0.5 × RAM | 1.5 × RAM |
| > 64 GB | ≥ 4 GB | 不建議休眠 |

**注意**：兩者的預設 swap 做法不同，安裝後用 `free -h` 與 `swapon --show` 確認實際狀態：

> 🔵 Fedora 33+ 預設使用 **zram**（壓縮記憶體交換，`zram-generator`），Workstation 安裝時不再建立磁碟 swap。伺服器若記憶體吃緊仍建議加上磁碟 swap 做最後防線。
> 🟠 Ubuntu Server 預設建立 `/swap.img` 交換檔（非分割區），大小依 RAM 自動計算。

### 2.2 LVM 建議

**為什麼**：沒有 LVM 的伺服器，磁碟滿了只有兩條路：停機用 `gparted` 挪分割區，或重裝搬資料；有 LVM 則是加一顆硬碟、`vgextend` + `lvextend` 幾秒鐘線上完成。LVM 快照也是做資料庫一致性備份最簡單的方式，前提是 VG 有預留空間。

**怎麼做**：

✅ 伺服器**一律使用 LVM**：日後擴充磁碟、線上放大分割區、做快照備份都靠它。

```
sda
├─ sda1  /boot/efi   (ESP, 1 GB)
├─ sda2  /boot       (2 GB, ext4)
└─ sda3  LVM PV
   └─ vg0
      ├─ lv_root   /        (30 GB)
      ├─ lv_var    /var     (40 GB)
      ├─ lv_log    /var/log (10 GB)
      ├─ lv_home   /home    (50 GB)
      ├─ lv_swap   swap     (8 GB)
      └─ (預留 20% 空間給快照與未來擴充)
```

💡 **不要一次把 VG 用完**。保留空間給 LVM 快照（備份前建立）與緊急擴充。

**注意**：兩者安裝程式的 LVM 預設分配方式不同，不看清楚就會裝出「磁碟明明 200 GB，`/` 卻只有 100 GB」的機器：

- 🟠 Ubuntu Server 安裝程式預設「Use an entire disk」+「Set up this disk as an LVM group」勾選，但 **預設只分配約一半空間給 `/`**，安裝後需手動 `lvextend`（見 [04 系統管理](04-系統管理.md)）。
- 🔵 Fedora Server 安裝程式預設「Automatic」會建立 LVM（xfs），`/` 預設最多 70 GB，其餘給 `/home` 或保留。

### 2.3 檔案系統選擇

**為什麼**：檔案系統一旦格式化就只能靠搬資料換掉。最常見的踩雷是 XFS **不能縮小**——切太大的 `/home` 想讓給 `/var` 只能砍掉重建；反之 Btrfs 快照與壓縮對桌面很方便，但在資料庫伺服器上 CoW 可能拖慢寫入。

**怎麼做**：兩者預設不同，反映各自的上游路線——Ubuntu 沿用 Debian 的保守選擇 ext4（成熟、可縮小、工具最齊）；Fedora 跟隨 RHEL 在伺服器上用 XFS（大檔案與高並行表現好），桌面則主推 Btrfs 以取得快照與壓縮。依用途對照下表選擇：

| 檔案系統 | 適用 | 備註 |
|---|---|---|
| ext4 | 通用、穩定、可縮小 | 🟠 Ubuntu 預設 |
| XFS | 大檔案、高並行、企業伺服器 | 🔵 Fedora Server 預設；**無法縮小** |
| Btrfs | 快照、壓縮、子卷、RAID1 | 🔵 Fedora Workstation 預設（zstd:1 壓縮） |
| ZFS | 儲存伺服器、大量資料完整性 | 🟠 Ubuntu 有原生支援（`zfsutils-linux`）；Fedora 需外部 repo |

### 2.4 全碟加密（LUKS）

**為什麼**：硬碟報廢送修、機器被整台搬走、或雲端供應商回收磁碟時，未加密的資料就是明文外流；筆電與放在非自有機房的伺服器尤其需要。代價是每次開機要有人輸密碼——伺服器若不能接受，就搭配 TPM2 自動解鎖（§1.5）。

**怎麼做**：兩者安裝程式都提供一鍵勾選：
- 🟠 Ubuntu Server：Storage → 「Encrypt the LVM group with LUKS」。
- 🔵 Fedora：Installation Destination → 「Encrypt my data」。

**注意**：安裝後結構：`sda3 → LUKS (crypt) → LVM PV → LV`。`/boot` 必須是未加密的獨立分割區（GRUB 雖能解 LUKS1，但實務上不建議）。

---

## 3. 🟠 Ubuntu Server 24.04 互動式安裝（Subiquity）

**為什麼**：Subiquity 的步驟不多，但有幾個選項一旦按過就很難回頭：語言選中文會讓家目錄出現中文路徑、mirror 沒填台灣站點會讓後續每次 `apt` 都慢、LVM 預設只用一半磁碟、忘了勾 OpenSSH 就得跑機房接螢幕。照下面順序走，並在每個標粗體的步驟停一下確認。

**怎麼做**：

1. **開機**：從 USB 啟動 → 選「Try or Install Ubuntu Server」。
2. **語言 / 鍵盤**：選 English（避免路徑亂碼）；鍵盤依實際。
3. **Installation type**：
   - `Ubuntu Server`：標準。
   - `Ubuntu Server (minimized)`：無 man page、無 vim 等，適合容器 / 雲端。
4. **Network**：預設 DHCP；可在此設定靜態 IP（產生 `/etc/netplan/00-installer-config.yaml`）。
5. **Proxy / Mirror**：企業內網有 proxy 時填 `http://proxy:3128`；Mirror 台灣可填 `http://tw.archive.ubuntu.com/ubuntu`。
6. **Storage**：
   - 「Use an entire disk」+ 勾選 LVM（選擇性勾選 LUKS）。
   - 或「Custom storage layout」手動分割（參考 §2.2）。
   - ⚠️ 確認目標磁碟後才按 Done → Continue（會清除資料）。
7. **Profile**：主機名、使用者、密碼（此使用者自動加入 `sudo` 群組）。
8. **Ubuntu Pro**：可跳過（之後 `sudo pro attach <token>`）。
9. **SSH**：勾選「Install OpenSSH server」，可從 GitHub / Launchpad 匯入公鑰（強烈建議）。
10. **Featured Server Snaps**：一般跳過（之後需要再裝，snap 版本的 docker 等在企業環境常有權限與路徑問題）。
11. 等待安裝完成 → **Reboot Now** → 拔除 USB。

**為什麼要先更新**：ISO 的打包日期到你安裝當下可能相隔數月，中間累積的核心與 OpenSSL 安全更新都還沒套用；趁機器還沒對外提供服務時先更新並重開，比上線後再排維護窗口容易得多。

安裝完成後第一件事：

```bash
sudo apt update && sudo apt full-upgrade -y && sudo reboot
```

### 3.1 🟠 Ubuntu Desktop 安裝重點

**為什麼**：桌面安裝與伺服器的差別主要在第三方驅動與編解碼器——沒勾「third-party software」的 NVIDIA 機器開機後可能只有低解析度畫面，沒裝 `ubuntu-restricted-extras` 則瀏覽器放不了 H.264 影片。實驗性的 ZFS / TPM 加密選項不建議用在要長期使用的工作機。

**怎麼做**：

- 24.04 起為新的 Flutter 安裝程式；「Default installation」vs「Extended selection」（含 LibreOffice 等）。
- 「Install third-party software」勾選可安裝 NVIDIA 驅動與媒體編解碼器。
- 磁碟：預設 ext4，可選 **ZFS（實驗）**、LVM、LUKS、**TPM 備援全碟加密（實驗）**。
- 安裝完成建議：`sudo apt install ubuntu-restricted-extras`（編解碼器）。

---

## 4. 🔵 Fedora 44 互動式安裝（Anaconda）

**為什麼**：Anaconda 是「中央總覽 + 分項設定」的 hub 式介面，沒有固定順序，容易漏掉項目——最常見的是**網卡預設關閉**，裝完後才發現機器沒有 IP、SSH 連不上，得回機房接螢幕。root 預設鎖定也與 Ubuntu 一致，管理員走 `wheel` 群組。

### 4.1 Fedora Server

**怎麼做**：

1. 從 USB 啟動 → 「Install Fedora 44」（先做「Test this media」可避免 USB 損壞造成安裝失敗）。
2. 語言：English (United States)。
3. **Installation Summary** 中央畫面依序設定：
   - **Keyboard / Time & Date**：時區選 Asia/Taipei，開啟 Network Time。
   - **Installation Source**：netinst 版需要網路，預設「Closest mirror」。
   - **Software Selection**：Fedora Server Edition；可勾選 Guest Agents、Headless Management（Cockpit）等 add-on。
   - **Installation Destination**：
     - 「Automatic」：LVM + xfs。
     - 「Custom」→ 選 LVM 或 Btrfs 手動配置；「Advanced Custom (Blivet-GUI)」有更完整的 GUI。
     - 勾選「Encrypt my data」啟用 LUKS。
   - **Network & Host Name**：設定主機名（`server01.example.com`），開啟網卡（預設關閉！）並設 IP。
   - **Root Password**：Fedora 預設 **鎖定 root** 且不允許 SSH root 密碼登入；建議保持鎖定並建立管理員。
   - **User Creation**：勾選「Make this user administrator」→ 加入 `wheel` 群組。
4. **Begin Installation** → 完成後 Reboot。
5. Fedora Server 預設啟用 **Cockpit**（`https://<ip>:9090`）網頁管理介面。

**為什麼要先更新**：Fedora 更新頻率高，DVD 映像與 updates repo 之間常差數百個套件（含核心）；netinst 雖然裝的是較新的版本，仍建議在上線前先完整升級並重開一次，確認新核心能正常開機。

安裝完成後：

```bash
sudo dnf upgrade -y && sudo reboot
```

### 4.2 🔵 Fedora Workstation

**為什麼**：Workstation 的安裝流程與 Server 幾乎是兩套程式（Web UI 精靈 vs 傳統 hub），儲存預設也從 LVM + xfs 換成 Btrfs 子卷；知道這點才不會在「找不到 LVM 選項」或「使用者建立在哪裡」上卡住。

**怎麼做**：

- Live 映像：開機進 GNOME 後點「Install to Hard Drive」。
- Fedora 42 起 Workstation 使用**新版 Anaconda Web UI**，流程改為精靈式（語言 → 日期 → 儲存 → 確認）。
- 儲存預設 **Btrfs**（`root` 與 `home` 子卷，`compress=zstd:1`），自動加上 EFI 與 1 GB `/boot`（ext4）。
- 使用者建立改在**第一次開機的 GNOME Initial Setup** 完成（自動加入 `wheel`）。
- 安裝完成建議：啟用 RPM Fusion 與 Flathub（見 [03 套件與軟體安裝](03-套件與軟體安裝.md)）。

---

## 5. 無人值守安裝（Unattended Installation）

**為什麼**：手動裝第 3 台機器時就會開始出現「這台忘了勾 SSH」「那台 mirror 沒填」的差異，而這些差異日後都會變成難以重現的問題；無人值守把整份安裝決策寫成可版本控管、可 review 的檔案，裝 1 台與裝 50 台的成本一樣，也是 PXE 與 CI 自動建 VM 的前提。

**怎麼做**：兩者的機制完全不同，源自各自的歷史——Kickstart 是 Red Hat 1990 年代末就有的自家格式，Anaconda 原生支援；Ubuntu 在 20.04 放棄 debian-installer 的 preseed 後，改以 cloud-init 風格的 YAML 讓 Subiquity 讀取，因此格式與傳遞方式都向雲端映像的 NoCloud 看齊。

大量佈署、PXE 開機、CI 建 VM 時必備。兩者機制完全不同：

| | 🟠 Ubuntu **autoinstall** | 🔵 Fedora **Kickstart** |
|---|---|---|
| 格式 | YAML（cloud-init 風格） | 自訂 `.ks` 指令語法 |
| 傳遞方式 | 第二顆 ISO/USB（NoCloud：`user-data` + `meta-data`）、HTTP、或嵌入 ISO | 核心參數 `inst.ks=http://...`、`inst.ks=hd:LABEL=OEMDRV:/ks.cfg`、嵌入 ISO |
| 驗證工具 | `cloud-init schema --config-file` + Subiquity JSON schema | `ksvalidator -v F44`（`pykickstart` 套件） |
| 產生範本 | 安裝完成的機器 `/var/log/installer/autoinstall-user-data` | 安裝完成的機器 `/root/anaconda-ks.cfg` |
| 文件 | <https://canonical-subiquity.readthedocs-hosted.com/en/latest/reference/autoinstall-reference.html> | <https://pykickstart.readthedocs.io/> |

### 5.1 🟠 Ubuntu autoinstall 範例

**為什麼**：這份範例針對幾個實際會失敗的點做了預防：網卡用 `match: name: "en*"` 而不是寫死 `ens18`，因為同一份設定換到不同虛擬化平台時網卡名會變，寫死就直接卡在網路設定；`sizing-policy: all` 修掉 §2.2 提到的「只用一半磁碟」預設；密碼用 SHA-512 雜湊而非明文，因為 `user-data` 通常會進 git 或放在 HTTP 上；`allow-pw: false` 讓機器從第一次開機起就只接受金鑰登入。

**怎麼做**：`user-data`（**第一行必須是 `#cloud-config`**，Subiquity 靠這行辨識檔案格式，少了就會被當成一般安裝、整份設定被忽略）：

```yaml
#cloud-config
autoinstall:
  version: 1
  locale: en_US.UTF-8
  keyboard:
    layout: us
  timezone: Asia/Taipei
  refresh-installer:
    update: false
  apt:
    primary:
      - arches: [amd64]
        uri: http://tw.archive.ubuntu.com/ubuntu
  network:
    version: 2
    ethernets:
      # 用 match 避免因網卡命名（ens18 / enp0s3）不同而失敗
      primary:
        match:
          name: "en*"
        dhcp4: true
  storage:
    layout:
      name: lvm
      sizing-policy: all          # 把整個 VG 分配給 /（預設只用一半）
      # password: "changeme"      # 加上這行即啟用 LUKS 全碟加密
  identity:
    hostname: ubuntu-auto
    username: admin
    # 用 `openssl passwd -6` 或 `mkpasswd -m sha-512` 產生
    password: "$6$rounds=4096$saltsalt$4gZ3hVdJvJZk9pRQx5Ig7KmzuhBRD0xKmFGm5N1Q3wfZ1qKZ8Ri2CQ7O.pTNvB4E9JuYfN3hVbC5pQeXvpRIb/"
  ssh:
    install-server: true
    allow-pw: false
    authorized-keys:
      - "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIExampleKeyReplaceMe admin@laptop"
  packages:
    - vim
    - htop
    - curl
    - qemu-guest-agent
  updates: security
  late-commands:
    - curtin in-target --target=/target -- systemctl enable qemu-guest-agent
    - echo 'admin ALL=(ALL) NOPASSWD:ALL' > /target/etc/sudoers.d/90-admin
    - chmod 440 /target/etc/sudoers.d/90-admin
  shutdown: reboot
```

`meta-data`（可為空檔，但必須存在——NoCloud 資料源同時找兩個檔案，缺一個就不會被辨識為 NoCloud，安裝程式會退回互動模式）：

```yaml
instance-id: ubuntu-auto-001
```

**為什麼用 seed ISO**：Subiquity 讀取 NoCloud 資料的方式與雲端映像相同，所以最不需要額外基礎設施的做法就是把兩個檔案包成一顆小 ISO 當第二顆光碟；沒有 HTTP 伺服器的隔離環境也能用。

製作 NoCloud 資料 ISO 並開機：

```bash
# 需要 cloud-image-utils（Ubuntu）或 genisoimage/xorriso
sudo apt install -y cloud-image-utils
cloud-localds seed.iso user-data meta-data
# 在 VM 掛載兩顆光碟：安裝 ISO + seed.iso；開機時 GRUB 選單按 e，
# 在 linux 行末尾加上： autoinstall ds=nocloud;s=/cdrom/   (若 seed 在第二顆光碟則用 ds=nocloud\;s=/dev/sr1)
```

要裝多台或走 PXE（§5.3）時，改把檔案放到 HTTP 上，就不必為每台機器製作 seed ISO：

```bash
# 把 user-data / meta-data 放到 http://10.0.0.5/auto/ 下，開機核心參數：
# autoinstall ds=nocloud-net;s=http://10.0.0.5/auto/
```

**為什麼要先驗證**：YAML 縮排錯一格或欄位名打錯，要等到開機進安裝程式才會停在錯誤畫面，一輪就是好幾分鐘；在自己機器上先跑驗證，幾秒鐘就能抓到。`cloud-init schema` 只檢查 cloud-config 外層格式，autoinstall 專屬欄位要靠 Subiquity 的 JSON schema 才驗得到，所以兩步都做。

驗證語法（已在 Ubuntu 24.04 實測）：

```bash
# 1) 基本 YAML / cloud-config 格式檢查（一般系統即可執行）
cloud-init schema --config-file user-data
#    → Valid schema user-data

# 2) 完整 autoinstall 欄位驗證：使用 Subiquity 官方 JSON schema
sudo apt install -y python3-jsonschema python3-yaml
curl -sSLO https://raw.githubusercontent.com/canonical/subiquity/main/autoinstall-schema.json
python3 - <<'PY'
import json, yaml, jsonschema
schema = json.load(open('autoinstall-schema.json'))
doc = yaml.safe_load(open('user-data'))
jsonschema.validate(doc['autoinstall'], schema)
print('autoinstall OK')
PY
```

> 💡 安裝媒體內的 Subiquity 會在開機時再驗證一次；若格式錯誤，安裝程式會停在錯誤畫面並顯示原因（可按 F2 進 shell 看 `/var/log/installer/`）。

### 5.2 🔵 Fedora Kickstart 範例

**為什麼**：Kickstart 的幾個指令直接對應真實事故：`ignoredisk --only-use=sda` 避免 `clearpart --all` 把插著的資料碟或 USB 一起清掉；`rootpw --lock` + `wheel` 管理員與互動安裝的安全預設一致；`console=ttyS0` 讓沒有螢幕的機器能從序列埠 / IPMI 看到開機訊息；`%post` 在裝好的系統內做 sudo 免密碼與 guest agent 啟用，是 Ansible 接手前最起碼的自動化。

**怎麼做**：`ks.cfg`：

```
# Fedora 44 Server Kickstart
text
lang en_US.UTF-8
keyboard us
timezone Asia/Taipei --utc
timesource --ntp-server=time.stdtime.gov.tw

# 網路：DHCP + 主機名
network --bootproto=dhcp --device=link --activate --hostname=fedora-auto.example.com

# 安裝來源（netinst 用）；DVD 版可改成 cdrom
url --mirrorlist="https://mirrors.fedoraproject.org/mirrorlist?repo=fedora-44&arch=x86_64"
repo --name=updates --mirrorlist="https://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f44&arch=x86_64"

# 使用者：鎖 root、建立 wheel 管理員（密碼用 openssl passwd -6 產生）
rootpw --lock
user --name=admin --groups=wheel --iscrypted --password='$6$rounds=4096$saltsalt$4gZ3hVdJvJZk9pRQx5Ig7KmzuhBRD0xKmFGm5N1Q3wfZ1qKZ8Ri2CQ7O.pTNvB4E9JuYfN3hVbC5pQeXvpRIb/'
sshkey --username=admin "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIExampleKeyReplaceMe admin@laptop"

# 安全
selinux --enforcing
firewall --enabled --service=ssh
services --enabled=sshd,chronyd,firewalld

# 磁碟：清空 sda，UEFI + LVM + xfs
ignoredisk --only-use=sda
zerombr
clearpart --all --initlabel --disklabel=gpt
bootloader --location=mbr --append="console=tty0 console=ttyS0,115200n8"
part /boot/efi --fstype=efi   --size=1024
part /boot     --fstype=xfs   --size=2048
part pv.01     --fstype=lvmpv --size=1 --grow
volgroup vg0 pv.01
logvol /        --vgname=vg0 --name=lv_root --fstype=xfs --size=30720
logvol /var     --vgname=vg0 --name=lv_var  --fstype=xfs --size=20480
logvol /var/log --vgname=vg0 --name=lv_log  --fstype=xfs --size=8192
logvol swap     --vgname=vg0 --name=lv_swap --fstype=swap --size=4096
logvol /home    --vgname=vg0 --name=lv_home --fstype=xfs --size=1 --grow

%packages
@^server-product-environment
@headless-management
vim-enhanced
htop
curl
qemu-guest-agent
-plymouth*
%end

%post --log=/root/ks-post.log
echo 'admin ALL=(ALL) NOPASSWD:ALL' > /etc/sudoers.d/90-admin
chmod 440 /etc/sudoers.d/90-admin
systemctl enable qemu-guest-agent
%end

reboot
```

**為什麼有兩種傳遞方式**：HTTP 適合 PXE 與大量佈署；`OEMDRV` 標籤的 USB 則是 Anaconda 會自動搜尋的路徑，不必改任何核心參數，適合帶一支 USB 到機房裝單台。與 autoinstall 一樣，先用 `ksvalidator` 在本機驗證，不要用一次開機安裝來當語法檢查。

使用方式：

```bash
# 開機時按 Tab/e 編輯核心參數，加上：
#   inst.ks=http://10.0.0.5/ks.cfg
# 或把 ks.cfg 放在標籤為 OEMDRV 的 USB 根目錄，Anaconda 會自動讀取：
#   inst.ks=hd:LABEL=OEMDRV:/ks.cfg
# 純文字模式 + 序列埠： inst.text console=ttyS0

# 驗證語法（在任何 Fedora 上）
sudo dnf install -y pykickstart
ksvalidator -v F44 ks.cfg
```

**注意**：`ksvalidator` 只檢查語法與版本相容性，不會檢查磁碟名稱是否存在或 mirror 是否可達；裝完後看 `/root/anaconda-ks.cfg` 與 `/root/ks-post.log` 確認實際套用的內容。

### 5.3 PXE / 網路開機重點

**為什麼**：機房裡幾十台機器輪流插 USB 既慢又容易插錯台；PXE 讓機器一開機就從網路拿核心與 autoinstall / Kickstart 檔，整批裝機可以無人在場。UEFI 機器的 PXE 又比 BIOS 麻煩，因為 Secure Boot 只接受簽章過的 `shimx64.efi` 起頭，這是最常卡住的地方。

**怎麼做**：

- 兩者皆可用 **dnsmasq**（DHCP + TFTP）+ HTTP 提供 `vmlinuz` / `initrd`：
  - 🟠 Ubuntu：從 ISO 的 `casper/vmlinuz` 與 `casper/initrd`，核心參數 `ip=dhcp url=http://.../ubuntu.iso autoinstall ds=nocloud-net;s=http://.../`。
  - 🔵 Fedora：`images/pxeboot/vmlinuz` 與 `initrd.img`，核心參數 `inst.repo=http://.../os/ inst.ks=http://.../ks.cfg`。
- UEFI PXE 需提供 `grubx64.efi` / `shimx64.efi`；建議直接用 **iPXE** 或 **HTTP boot** 簡化。

---

## 6. 雲端 / 虛擬機映像

**為什麼**：測試環境或短命 VM 跑一次完整安裝程式要十幾分鐘，官方雲端映像已裝好系統並內建 cloud-init，只要餵一份 `user-data` 就能在幾十秒內拿到可 SSH 的機器；`qemu-img -b` 的 backing file 讓十台 VM 共用同一份基底映像，磁碟只存差異。這也是本機重現雲端行為（AWS / GCP 同樣用 cloud-init）最便宜的方式。

**怎麼做**：不必安裝，直接用官方雲端映像 + cloud-init（幾秒鐘開一台 VM）：

```bash
# ⚪ 在 KVM 主機（virt-install）
# 🟠 Ubuntu
wget https://cloud-images.ubuntu.com/noble/current/noble-server-cloudimg-amd64.img
# 🔵 Fedora
wget https://download.fedoraproject.org/pub/fedora/linux/releases/44/Cloud/x86_64/images/Fedora-Cloud-Base-Generic-44-1.x.x86_64.qcow2

# 共同：準備 cloud-init 資料
cat > user-data <<'EOF'
#cloud-config
hostname: vm01
users:
  - name: admin
    groups: [sudo, wheel]           # 兩者群組都寫，不存在的會被忽略
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh_authorized_keys:
      - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIExampleKeyReplaceMe admin@laptop
package_update: true
packages: [vim, htop]
EOF
echo "instance-id: vm01" > meta-data
cloud-localds seed.iso user-data meta-data

qemu-img create -f qcow2 -F qcow2 -b noble-server-cloudimg-amd64.img vm01.qcow2 40G
virt-install --name vm01 --memory 2048 --vcpus 2 \
  --disk vm01.qcow2 --disk seed.iso,device=cdrom \
  --os-variant ubuntu24.04 --import --network network=default --noautoconsole
```

**注意**：雲端映像沒有預設密碼，忘了在 `user-data` 放公鑰就進不去，只能刪掉重開；基底映像檔（`.img` / `.qcow2`）被移動或刪除後，所有依賴它的 VM 磁碟都會壞掉。

- Multipass（Canonical）：`multipass launch 24.04 --name dev` 一行開 Ubuntu VM（Windows/macOS/Linux）。
- 🔵 Fedora 對應：`virt-install --os-variant fedora-rawhide` 或使用 **Toolbox / Distrobox** 容器。

---

## 7. WSL2 安裝（Windows）

**為什麼**：在 Windows 工作機上要跑 Ansible、測 shell script 或編譯 Linux 工具，WSL2 比開一台 VM 輕得多，而且能直接存取 Windows 檔案；但它不是完整的 Linux 主機，把它當成伺服器測試防火牆或 SELinux 設定會得到錯誤結論。

**怎麼做**：

```powershell
wsl --install                       # 安裝 WSL 與預設 Ubuntu
wsl --list --online                 # 列出可用發行版
wsl --install Ubuntu-24.04          # 🟠
wsl --install FedoraLinux-44        # 🔵（Fedora 42 起為官方 WSL 映像）
wsl --set-default FedoraLinux-44
```

**注意**：WSL2 注意事項：
- 預設啟用 systemd（`/etc/wsl.conf` 內 `[boot] systemd=true`）；舊版本需手動加。
- 🔵 Fedora WSL 映像 SELinux 為 **disabled**、無 firewalld；並非完整 Fedora 環境。
- 網路由 Windows 代管，`ufw` / `firewalld` 規則對外部無效。

---

## 8. 安裝後檢查清單

**為什麼**：最常在交機後才被發現的問題——裝成 BIOS 模式而非 UEFI、Fedora 網卡沒開、`/` 只拿到一半磁碟、NTP 沒同步導致憑證驗證失敗、某個服務開機就失敗——全都能用下面十幾條指令在一分鐘內抓出來。把它當成每台機器的驗收 SOP，而不是出事後的排錯工具。

**怎麼做**：

```bash
# ⚪ 共通
cat /etc/os-release                      # 版本
hostnamectl                              # 主機名、核心、虛擬化類型
lsblk -f                                 # 分割區與檔案系統
df -h                                    # 掛載與使用率
free -h                                  # 記憶體與 swap
ip -br a                                 # 網卡與 IP
systemctl --failed                       # 失敗的服務
timedatectl                              # 時區、NTP 同步狀態
[ -d /sys/firmware/efi ] && echo UEFI || echo BIOS
mokutil --sb-state 2>/dev/null           # Secure Boot 狀態

# 🟠 Ubuntu 專屬
lsb_release -a
sudo ufw status
sudo aa-status | head -3
apt list --upgradable

# 🔵 Fedora 專屬
cat /etc/fedora-release
sudo firewall-cmd --state && sudo firewall-cmd --list-all
getenforce
dnf check-upgrade
```

**注意**：`systemctl --failed` 應為空、`timedatectl` 要看到 `System clock synchronized: yes`、`lsblk -f` 對照 §2 規劃確認 LVM 與 `/var` 確實獨立；任何一項不符就趁還沒上線時處理。

---

## 9. 本章差異總結

本章兩個發行版的差異幾乎都可以回到同一個根源：Ubuntu 的安裝程式與預設值圍繞「LTS 伺服器 + 雲端」設計（Subiquity 與 cloud-init 共用格式、預設 DHCP 啟用、Multipass），而 Fedora 承接 RHEL 的工具鏈與安全預設（Anaconda / Kickstart、xfs + LVM、Cockpit、網卡預設關閉、SELinux enforcing）。下表把本章出現過的差異集中列出，方便交接與排錯時快速對照。

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 安裝程式 | Subiquity（Server）/ Flutter 安裝程式（Desktop） | Anaconda（Server：傳統 GTK；Workstation：Web UI） |
| 預設檔案系統 | ext4（Server 可勾 LVM） | Server：xfs + LVM；Workstation：Btrfs |
| root 帳號 | 鎖定，第一位使用者在 `sudo` 群組 | 鎖定，管理員在 `wheel` 群組 |
| LVM 預設分配 | 只給 `/` 約一半 VG，需手動擴充 | `/` 最多 70 GB，其餘給 `/home` |
| 無人值守 | autoinstall（YAML） | Kickstart（.ks） |
| 網頁管理 | 無（可裝 Cockpit：`apt install cockpit`） | Server 預設有 Cockpit :9090 |
| swap | `/swap.img` 交換檔 | zram（Workstation）；Server 可建 swap LV |
| 安裝時網路 | 預設 DHCP 啟用 | **網卡預設關閉**，需手動開啟 |
| 官方 VM 工具 | Multipass | 無（用 virt-manager / Boxes） |
