# 01 - 系統安裝

本章涵蓋從下載映像到完成第一次開機的完整流程，包含互動式安裝、分割區與 LVM / 加密規劃、無人值守安裝（Ubuntu autoinstall 與 Fedora Kickstart）、雲端映像及 WSL2。

**本章解決什麼問題**：安裝是整個生命週期裡最難「事後補救」的一步——分割區切錯、檔案系統選錯、忘了 LVM，日後不是重裝就是停機搬資料；映像沒校驗、Secure Boot 沒處理，則會在裝完 NVIDIA 驅動或第一次 `fwupd` 時才爆出來。本章的取捨原則是：**伺服器一律 UEFI + LVM + 獨立 `/boot`、`/var`、`/var/log`**，能自動化就不要手動點（autoinstall / Kickstart / cloud-init），並在安裝完成後立刻用同一份檢查清單驗收，而不是等使用者回報問題。

---

## 1. 安裝前準備

**為什麼**：安裝前的十分鐘準備（選對映像、校驗、確認韌體模式）能避開大多數「裝到一半失敗」與「裝完才發現不能用」的情況；這一節的每個步驟都對應一種實際踩過的雷。

### 1.1 選擇版本與映像

**名詞與關係**：映像的檔名本身就在告訴你「裡面有沒有安裝程式、要不要網路、裝完是哪一種系統」，先把這些詞對上：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **LTS** | Long Term Support，Ubuntu 每兩年（偶數年 4 月）發布、提供 5 年安全更新的版本 | 伺服器只選 LTS；Fedora 沒有對應概念，每一版只支援約 13 個月 |
| **ISO 安裝映像** | 內含安裝程式與套件的光碟映像，寫入 USB 後開機即進入安裝流程 | Server / Desktop / netinst 都屬這一類；雲端映像不是 |
| **netinst** | 只帶安裝程式與最小開機環境的 ISO（數百 MB），套件全部在安裝時從網路下載 | 需要能連外的網路；DVD 版把套件帶在映像內，離線也能裝 |
| **Live 映像** | 可直接開機進完整桌面試用、再從桌面點「安裝」的 ISO | 🟠 Desktop 與 🔵 Workstation 都是 Live；Server 映像沒有試用模式 |
| **Subiquity / Anaconda** | 🟠 Ubuntu Server / 🔵 Fedora 的安裝程式，分別在 §3、§4 詳述 | 只存在於 ISO 映像內；無人值守安裝（§5）就是餵設定檔給它們 |
| **雲端映像（cloud image）** | 已經裝好、可直接開機的磁碟映像（`.img` / `.qcow2`），內建 cloud-init 在第一次開機時讀取設定 | 沒有安裝程式，只能給虛擬機或雲平台用（§6）；插實體機開機是黑畫面 |
| **qcow2** | QEMU 的磁碟映像格式，支援稀疏存放、快照與以另一個映像為基底（backing file） | 🔵 雲端映像的副檔名；🟠 的 `.img` 實際上也是 qcow2 格式 |
| **snap** | Canonical 的自包含套件格式，由常駐服務 `snapd` 管理，Desktop 用它裝 Firefox 等應用 | 用 Desktop 映像裝伺服器就會一併帶進整套桌面 snap；Server 映像預設只有 `snapd` 本身 |
| **不可變（immutable）發行版** | 系統目錄唯讀、以整份映像為單位更新與回滾的變體：🟠 Ubuntu Core 靠 snap，🔵 Silverblue / CoreOS 靠 rpm-ostree / bootc | 與本章的傳統「可變」安裝互斥：不能用 `apt` / `dnf` 隨意改系統檔，本章不涵蓋 |

**為什麼**：映像選錯的代價很具體——用 Desktop 映像裝伺服器會多出整套 GUI 與 snap 服務吃記憶體；`netinst` 在沒有網路的機房裝不起來；雲端映像沒有安裝程式，插進實體機開機只會看到黑畫面。先依用途對照下表挑映像，再開始下載。

**怎麼做**：兩者的映像分類不同，來自發行節奏的差異——Ubuntu 以 LTS 為核心（每 2 年一版、支援 5 年，適合長期運行的伺服器）；Fedora 每 6 個月一版、只支援約 13 個月，定位是追上游最新技術的「前哨站」，因此映像種類更多、也更常變動。

| | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 下載頁 | <https://ubuntu.com/download> | <https://fedoraproject.org/> |
| 伺服器映像 | `ubuntu-24.04.x-live-server-amd64.iso`（Subiquity 文字安裝） | `Fedora-Server-dvd-x86_64-44-x.iso`、`Fedora-Server-netinst-...iso` |
| 桌面映像 | `ubuntu-24.04.x-desktop-amd64.iso` | `Fedora-Workstation-Live-x86_64-44-x.iso` |
| 最小化 | Server 安裝時勾選「Minimized」 | `Fedora-Everything-netinst`（可自選套件群組） |
| 雲端映像 | `ubuntu-24.04-server-cloudimg-amd64.img` | `Fedora-Cloud-Base-Generic-44-x.x86_64.qcow2` |
| 不可變（immutable）版本 | Ubuntu Core（snap-based） | Fedora Silverblue / Kinoite / CoreOS（rpm-ostree / bootc） |

💡 **伺服器一律選 Ubuntu LTS**；Fedora 若用於伺服器，要接受每 13 個月必須升級大版本一次。

### 1.2 校驗映像完整性

**名詞與關係**：校驗是一條「信任鏈」，每一環只證明下一環沒被動過，所以要一路往上驗到人工能確認的東西為止：

```
官方網頁上的指紋 ──人工比對──▶ 公鑰 ──驗證 GPG 簽章──▶ SHA256SUMS / CHECKSUM 檔 ──比對雜湊值──▶ ISO
   （信任的起點）                              （證明清單真的來自官方）                （證明檔案沒壞、沒被換）
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **SHA256 雜湊** | 對整個檔案算出的 64 個十六進位字元指紋，內容改一個位元結果就完全不同 | 用 `sha256sum` 算出來，與校驗檔裡的值比對；只回答「檔案有沒有壞」 |
| **`SHA256SUMS` / `CHECKSUM` 檔** | 官方公布的「檔名 → SHA256」清單（🟠 `SHA256SUMS`、🔵 `Fedora-…-CHECKSUM`） | 它本身也是從網路下載的，可能被同一個中間人一起換掉，所以還需要簽章 |
| **GPG 簽章** | 發行版用私鑰對校驗檔產生的簽名（🟠 獨立檔 `SHA256SUMS.gpg`；🔵 直接內嵌在 CHECKSUM 檔內） | 用官方公鑰驗證通過，才能相信校驗檔來自官方；`gpg --verify` / `gpgv` 做的就是這件事 |
| **公鑰 / keyring / 指紋** | 驗證簽章用的官方公鑰（🟠 從 keyserver 抓；🔵 從 `fedora.gpg` 匯入本機 keyring），指紋是公鑰的雜湊 | 公鑰也可能被假冒，所以最後一環是人工比對指紋與官方網頁的值 |

**為什麼**：下載中斷或鏡像站快取損壞會產生「看起來完整、裝到一半才出錯」的 ISO，浪費一整輪開機安裝時間；更嚴重的是被竄改的映像。`sha256sum` 只能證明檔案沒壞，**不能**證明 `SHA256SUMS` 本身來自官方——所以要再驗證校驗檔的 GPG 簽章，才算閉環。

**怎麼做**：先比對 SHA256，再驗證校驗檔的簽章。

⚪ 共通（在任何 Linux/macOS 上）：

```bash
# 校驗 SHA256
sha256sum ubuntu-24.04.4-live-server-amd64.iso
# 與官方的 SHA256SUMS 檔比對
sha256sum -c SHA256SUMS --ignore-missing
```

```bash
# 🟠 Ubuntu：驗證 SHA256SUMS 檔本身的 GPG 簽章
gpg --keyid-format long --keyserver hkp://keyserver.ubuntu.com \
    --recv-keys 0x46181433FBB75451 0xD94AA3F0EFE21092
gpg --verify SHA256SUMS.gpg SHA256SUMS

# 🔵 Fedora：驗證 CHECKSUM 檔簽章
curl -O https://fedoraproject.org/fedora.gpg
gpgv --keyring ./fedora.gpg Fedora-Server-44-1.x-x86_64-CHECKSUM
sha256sum -c Fedora-Server-44-1.x-x86_64-CHECKSUM --ignore-missing
```

**注意**：`gpg --verify` 顯示 `Good signature` 但附帶「not certified with a trusted signature」警告是正常的（你尚未簽署該金鑰）；只要金鑰指紋與官方頁面一致即可。

### 1.3 製作開機 USB

**名詞與關係**：這一節的工具做的都是同一件事——把 ISO 原樣寫進 USB——差別只在有沒有防呆、能不能放多個映像：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **`lsblk`** | 列出所有區塊裝置（整顆磁碟與其分割區）及大小、型號、介面的指令 | `dd` 之前確認「哪一個才是 USB」的唯一依據；`TRAN` 欄顯示 `usb` 的才是目標 |
| **`dd`** | 逐位元組把輸入複製到輸出裝置的工具，不看檔案系統、不問確認 | 兩者的 ISO 都是 hybrid 格式，原樣寫到整顆 USB（`/dev/sdb`，不是 `/dev/sdb1`）就能開機；寫錯裝置立即毀掉資料 |
| **圖形化寫入工具** | 幫你挑裝置、附確認畫面的 `dd` 包裝（🟠 Startup Disk Creator、Rufus、balenaEtcher；🔵 Fedora Media Writer） | 做的事和 `dd` 相同，只多一層防呆；Windows 上沒有 `dd` 就用它們 |
| **Ventoy** | 把 USB 做成「放 ISO 檔的隨身碟」的開機管理器，開機時列出裡面所有 ISO 供選擇 | 與 `dd` 互斥：`dd` 每次覆蓋整支 USB 且只能放一個 ISO，Ventoy 只安裝一次、之後複製 ISO 檔即可 |

**為什麼**：`dd` 沒有任何確認機制，`of=` 寫成工作機的系統碟（例如筆電的 `/dev/sda`）會在幾秒內毀掉整顆硬碟，這是製作開機 USB 最常見、也最不可逆的事故。因此第一步永遠是先用 `lsblk` 確認裝置名稱與容量，再執行 `dd`。

**怎麼做**：

```bash
# ⚪ 共通：dd（⚠️ of= 一定要指向 USB 裝置本身，不是分割區；寫錯會毀掉硬碟）
lsblk -o NAME,SIZE,MODEL,TRAN          # 確認 USB 裝置名稱，例如 /dev/sdb
sudo dd if=./image.iso of=/dev/sdb bs=4M status=progress conv=fsync
```

不想碰 `dd` 或需要在 Windows 上製作，可用圖形化工具；若要同時帶多個 ISO 到機房（Ubuntu、Fedora、救援工具），Ventoy 只需一支 USB。

圖形化工具：
- 🟠 Ubuntu：內建「Startup Disk Creator」（`usb-creator-gtk`），Windows 上用 Rufus / balenaEtcher。
- 🔵 Fedora：官方 **Fedora Media Writer**（跨平台，可直接下載映像）。
- ⚪ 多映像 USB：**Ventoy**（一支 USB 放多個 ISO）。

### 1.4 硬體需求（實務建議）

**名詞與關係**：下表的門檻背後是三個概念——CPU 指令集等級、記憶體耗盡時核心的反應、以及韌體模式：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **x86-64-v1 / v2** | x86-64 的「微架構等級」：v1 是 2003 年的基本指令集，v2 額外要求 SSE4.2、POPCNT、SSSE3 等擴充（約 2009 年 Nehalem 之後的 CPU 都有） | 發行版把整套套件編譯成某個等級，CPU 不到等級就無法執行；🔵 Fedora 44 已編成 v2 |
| **SSE4.2 / POPCNT** | CPU 指令集擴充：向量運算與位元計數指令 | 就是 v2 比 v1 多要求的那幾項；`/proc/cpuinfo` 的 flags 欄有 `sse4_2` 與 `popcnt` 即符合 |
| **OOM killer** | Out-Of-Memory killer，核心在記憶體耗盡時強制砍掉某個行程以自救的機制 | 記憶體太小時安裝程式就是被它砍掉；§2.1 的 swap 是延後它出手的緩衝 |
| **BIOS / UEFI / Secure Boot** | 主機板韌體的兩代標準，以及 UEFI 的開機檔簽章驗證功能 | 表中「韌體」一列只先給結論；完整定義、關係與取捨在 §1.5 |

**為什麼**：Fedora 44 要求 x86-64-v2，在 2009 年前後的舊 CPU（缺 SSE4.2 / POPCNT）上會直接無法開機而非顯示錯誤；記憶體低於 2 GB 時安裝程式本身可能被 OOM 砍掉；磁碟給太小則 §2 的分割規劃根本擺不下。先對照下表確認機器再動手，尤其是回收再利用的舊硬體。

**怎麼做**：

| | 最低 | 建議（伺服器） |
|---|---|---|
| CPU | x86-64-v2 以上（Fedora 44 需 x86-64-v2；Ubuntu 24.04 仍支援 v1 但 26.04 起建議 v2 以上） | 4 核以上 |
| 記憶體 | 2 GB（Server）/ 4 GB（Desktop） | 8 GB 以上 |
| 磁碟 | 25 GB | 50 GB 以上，SSD |
| 韌體 | BIOS 或 UEFI | **UEFI + Secure Boot** |

### 1.5 UEFI、Secure Boot 與 TPM

**名詞與關係**：這一節出現的名詞其實是一條「開機信任鏈」上的環節，先看它們怎麼串起來：

```
主機板韌體 UEFI ──讀取──▶ ESP 分割區內的 shim ──驗證簽章後載入──▶ GRUB ──▶ Linux 核心 ──▶ 核心模組（驅動）
      │                       ▲                                                        ▲
      │ Secure Boot：韌體只執行「有 Microsoft 簽章」的檔案                                │ 核心只載入「有簽章」的模組
      │ shim 是唯一被 Microsoft 簽過的 Linux 檔，之後每一層由發行版簽                       │ 第三方模組（NVIDIA、ZFS）沒有發行版簽章
      │                                                                                │ → 由 DKMS / akmods 自動編譯並用 MOK 簽
      └─ TPM 晶片：每一層被載入時把「hash」記進 TPM；LUKS 可設定「hash 全對才交出金鑰」= 免密碼開機
```

| 名詞 | 一句話定義 | 和其他名詞的關係 |
|---|---|---|
| **UEFI** | 取代舊 BIOS 的主機板韌體標準，負責開機的第一步 | 一切的起點；Secure Boot、ESP、fwupd、systemd-boot 都是 UEFI 才有的功能 |
| **CSM / Legacy Boot** | UEFI 內建的「模擬舊 BIOS」相容模式 | 開著它，安裝媒體可能以 BIOS 模式啟動，裝出來的系統就走舊路線，下面所有東西都用不到 |
| **ESP** | EFI System Partition，一個 FAT32 小分割區（`/boot/efi`），UEFI 只會從這裡找開機檔 | BIOS 模式安裝不會建立 ESP；shim、GRUB、systemd-boot 都放在這裡 |
| **Secure Boot** | UEFI 的簽章驗證功能：只執行被信任金鑰簽過的開機檔 | 主機板出廠只信任 Microsoft 的金鑰，所以 Linux 需要 shim 當跳板 |
| **shim** | 一個極小、由 Microsoft 簽署的第一階段載入器，內嵌發行版（Canonical / Fedora）自己的憑證 | Secure Boot 信任 shim → shim 信任發行版簽的 GRUB 與核心 |
| **MOK** | Machine Owner Key，機器擁有者自己登錄到 shim 的憑證 | 讓「發行版沒簽」的東西（自編模組）也能通過 Secure Boot；登錄在開機時的 MokManager 畫面完成 |
| **DKMS / akmods** | 在每次核心更新後自動重新編譯第三方模組的機制（🟠 DKMS、🔵 akmods） | 編譯出來的模組沒有發行版簽章，必須用 MOK 簽，否則 Secure Boot 下核心拒載 |
| **TPM 2.0** | 主機板上的安全晶片，會記錄開機每一階段的 hash（量測），並能保管金鑰 | 與 Secure Boot 互補：Secure Boot「阻止」未簽章的東西執行，TPM「記錄」實際執行了什麼 |
| **LUKS** | Linux 的全碟加密格式 | 平常開機要輸密碼；綁 TPM 後，只要開機鏈 hash 與登錄時相同就自動解鎖 |
| **fwupd / systemd-boot** | 韌體更新工具 / 極簡開機載入器 | 都依賴 ESP 與 UEFI 變數，BIOS 模式下不存在 |

**為什麼**：理解這條鏈就能預見兩類最常見的災難。第一，Secure Boot 開啟時核心只載入有簽章的模組——裝完 NVIDIA 或 ZFS DKMS 重開機後「驅動不見了 / 沒畫面」，幾乎都是模組沒用 MOK 簽、被核心拒載。第二，CSM 開著時安裝程式可能以 BIOS 模式開機，裝出來的系統沒有 ESP，之後 `fwupd` 韌體更新、`systemd-boot`、TPM 解鎖全都用不了，只能重裝。這兩件事都要在安裝**前**決定，因為事後從 BIOS 模式轉 UEFI 需要重做分割區。

**怎麼做**：

- 兩者都支援 **Secure Boot**（使用 Microsoft 簽署的 shim）。安裝第三方核心模組（NVIDIA、VirtualBox、ZFS DKMS）時：
  - 🟠 Ubuntu：DKMS 會提示設定 **MOK（Machine Owner Key）** 密碼，重開機時在 MokManager 中登錄。
  - 🔵 Fedora：RPM Fusion 的 `akmod-nvidia` 需手動 `mokutil --import` 登錄金鑰（或關閉 Secure Boot）。
- 建議在 UEFI 中關閉 **CSM / Legacy Boot**，使用純 UEFI 模式，之後才能用 `systemd-boot`、`fwupd` 韌體更新等功能。
- 若要 TPM2 自動解鎖 LUKS（免輸密碼開機）：兩者都可用 `systemd-cryptenroll`，見 [08 安全強化](08-安全強化.md)。

**注意**：裝完後用 `[ -d /sys/firmware/efi ]` 與 `mokutil --sb-state` 確認實際的開機模式（見 §8），不要只看 UEFI 設定畫面。完整的信任鏈、金鑰層級與 TPM PCR 細節在 [14 章 UEFI 進階](14-UEFI進階.md)。
---

## 2. 分割區規劃

**名詞與關係**：本節出現的名詞是一個由下而上的「儲存堆疊」，每一層都建在下一層之上，這也是為什麼它們總是一起被討論：

```
實體磁碟 sda
 └─ 分割區（partition）sda1 / sda2 / sda3        ← 用 GPT 分割表切出的固定區段
     └─ （選用）LUKS 加密層                       ← §2.4：整個分割區先加密，上面的層都看不到明文
         └─ LVM：PV → VG → LV                    ← §2.2：把分割區變成可線上伸縮、可快照的「邏輯磁碟」
             └─ 檔案系統 ext4 / XFS / Btrfs      ← §2.3：決定資料怎麼存、能不能縮
                 └─ 掛載點 /、/var、/home …      ← §2.1：檔案系統接到目錄樹的哪個位置
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **分割區 / GPT** | 把一顆磁碟切成數個固定區段；GPT 是 UEFI 時代的分割表格式（取代 MBR，支援 > 2 TB 與超過 4 個主分割區） | 最底層；`/boot/efi`（ESP）與 `/boot` 必須是真正的分割區，其餘可交給 LVM |
| **掛載點（mount point）** | 一個檔案系統被接到目錄樹的位置，例如 `/var`、`/home` | 「獨立切出 `/var`」意思是 `/var` 底下用另一個檔案系統，塞爆它不影響 `/` |
| **檔案系統** | 決定資料如何在區塊裝置上組織的格式（ext4、XFS、Btrfs、ZFS；§2.3） | 分割區或 LV 格式化成某種檔案系統後才能掛載；格式化後要換只能搬資料 |
| **LVM** | Logical Volume Manager，在分割區與檔案系統之間加一層可隨時放大、搬移、快照的邏輯卷（§2.2） | 沒有它，改分割區大小要停機；有它，`/` 與 `/var` 都是可線上調整的 LV |
| **LUKS** | Linux 的區塊裝置加密格式（§1.5、§2.4） | 放在分割區之上、LVM 之下：先解鎖才看得到裡面的 PV |
| **swap** | 記憶體不夠時把不常用的分頁暫放到磁碟的空間，可以是分割區、LV 或一般檔案 | 沒有檔案系統、不掛載到目錄；§2.1 討論大小與 zram |

**為什麼**：分割區是安裝後最難更動的決定：`/` 與 `/var` 混在一起，日誌或容器映像塞滿磁碟時 SSH 都登不進去；XFS 切太大不能縮；沒有 LVM 的機器要加硬碟只能停機搬資料。花五分鐘規劃，換來的是之後幾年都能線上調整。

### 2.1 建議配置（伺服器）

**名詞與關係**：表中每一列「為什麼要獨立」的理由，都牽涉到開機鏈或安全機制裡的某個角色：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **ESP / FAT32** | ESP 是 UEFI 專用的開機分割區（§1.5），必須格式化成 FAT32，因為那是韌體唯一保證能讀的檔案系統 | 掛在 `/boot/efi`；放 shim 與 GRUB 的 `.efi` 檔 |
| **GRUB** | 兩者預設的開機載入器（bootloader）：顯示選單、讀取核心與 initramfs、交棒給核心 | 由 shim 載入（§1.5）；它得自己解析 `/boot` 所在的檔案系統，所以 `/boot` 越單純越可靠 |
| **initramfs** | 隨核心一起載入的小型記憶體檔案系統，內含掛載真正根目錄前需要的驅動與工具（LVM、LUKS、RAID 模組） | 與核心一起放在 `/boot`；核心更新時自動重建，壞掉就開不了機 |
| **tmpfs** | 存在記憶體中的檔案系統，重開機即清空 | `/tmp` 的另一種做法：不佔磁碟，但大檔案會吃 RAM |
| **`noexec` / `nosuid` / `nodev`** | 掛載選項：禁止在該檔案系統執行程式 / 忽略 setuid 位元 / 忽略裝置檔 | 掛載選項只能整個檔案系統一起設，所以 `/tmp` 要獨立才擋得住 |
| **CIS** | Center for Internet Security，發布各作業系統安全設定基準（Benchmark）的組織 | `/tmp` 的掛載選項、獨立 `/var/log` 都是 CIS Benchmark 的建議項 |
| **OOM killer** | 核心在記憶體耗盡時強制砍行程的機制（§1.4） | swap 是它動手前的緩衝；有 swap 系統先「變慢」，沒 swap 直接「掉服務」 |
| **休眠（hibernate）** | 把整個記憶體內容寫進 swap 後關機，下次開機還原 | 因此 swap 必須 ≥ RAM；伺服器不休眠可忽略那一欄 |
| **zram / `zram-generator`** | 在記憶體中劃出一塊壓縮區當 swap 裝置；🔵 由 `zram-generator` 服務在開機時自動建立 | 比磁碟 swap 快，但仍占記憶體，記憶體真正耗盡時幫不上忙，所以伺服器仍建議加磁碟 swap 當最後防線 |
| **swap 檔 / 分割區 / LV** | 放 swap 的三種地方：一般檔案（🟠 `/swap.img`）、獨立分割區、LVM 的 LV | 效果相同；檔案最容易事後調整，LV 可線上放大，分割區最難改 |

**為什麼**：獨立切出 `/var` 與 `/var/log`，是因為真實事故裡塞爆磁碟的幾乎都是日誌、資料庫與容器映像——它們獨立後最多讓該服務出錯，不會把整台系統拖垮到連 root 都寫不了檔案。`/boot` 必須獨立，是因為 GRUB 在 LVM / LUKS 上讀核心不可靠；`/tmp` 獨立則能加 `noexec` 擋掉最常見的「下載到 /tmp 再執行」攻擊路徑。

**怎麼做**：

| 掛載點 | 大小 | 檔案系統 | 說明 |
|---|---|---|---|
| `/boot/efi` | 512 MB～1 GB | FAT32 | UEFI 必要（ESP） |
| `/boot` | 1～2 GB | ext4 / xfs | 放核心與 initramfs；LVM 或加密時必須獨立 |
| `/` | 20～50 GB | ext4（🟠）/ xfs（🔵） | 系統本體 |
| `/var` | 依需求 | 同上 | 資料庫、容器映像、日誌；獨立可防止塞爆 `/` |
| `/var/log` | 5～20 GB | 同上 | 日誌爆量時不影響系統 |
| `/home` | 剩餘 | 同上 | 使用者資料 |
| `/tmp` | 2～10 GB 或 tmpfs | — | 可加 `noexec,nosuid,nodev` 掛載選項（CIS 建議） |
| swap | 依記憶體 | swap | 見下表 |

**為什麼要 swap**：swap 不是「記憶體不夠才用」，而是 OOM killer 動手前的最後緩衝——沒有 swap 的伺服器在記憶體暫時吃緊時會直接砍掉資料庫或 JVM，而非變慢。若要休眠，swap 還必須放得下整個記憶體內容，所以數字更大。

**swap 建議**（Red Hat 準則）：

| 記憶體 | 建議 swap | 若需休眠（hibernate） |
|---|---|---|
| ≤ 2 GB | 2 × RAM | 3 × RAM |
| 2～8 GB | = RAM | 2 × RAM |
| 8～64 GB | 4 GB～0.5 × RAM | 1.5 × RAM |
| > 64 GB | ≥ 4 GB | 不建議休眠 |

**注意**：兩者的預設 swap 做法不同，安裝後用 `free -h` 與 `swapon --show` 確認實際狀態：

> 🔵 Fedora 33+ 預設使用 **zram**（壓縮記憶體交換，`zram-generator`），Workstation 安裝時不再建立磁碟 swap。伺服器若記憶體吃緊仍建議加上磁碟 swap 做最後防線。
> 🟠 Ubuntu Server 預設建立 `/swap.img` 交換檔（非分割區），大小依 RAM 自動計算。

### 2.2 LVM 建議

**名詞與關係**：LVM 只有三層，方向固定由下往上——實體 → 池 → 邏輯卷——所有指令都是在這三層之間搬空間：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **PV（Physical Volume）** | 被 LVM 接管的區塊裝置（分割區、整顆磁碟或解鎖後的 LUKS 裝置），`pvcreate` 建立 | 最底層；加一顆硬碟 = 建一個新 PV |
| **VG（Volume Group）** | 一個或多個 PV 合併成的儲存池，`vgcreate` 建立、`vgextend` 加入新 PV | PV 加進 VG 後空間就變成一個總量，可以切給任何 LV |
| **LV（Logical Volume）** | 從 VG 切出、被當成「分割區」拿去格式化與掛載的邏輯卷，`lvcreate` 建立、`lvextend` 放大 | 最上層；`/`、`/var`、swap 各是一個 LV，可線上放大（縮小要看檔案系統，§2.3） |
| **LVM 快照（snapshot）** | 在 VG 內為某個 LV 建立的即時凍結副本，只記錄之後變動的區塊 | 需要 VG 有未分配空間，所以圖中預留 20%；備份資料庫時先建快照再複製 |
| **`vgextend` / `lvextend`** | 把新 PV 加入 VG / 放大某個 LV（配合 `--resizefs` 同時放大上面的檔案系統） | 擴充磁碟的兩步驟：先 `vgextend` 讓 VG 變大，再 `lvextend` 分給需要的 LV |
| **`gparted`** | 圖形化的分割區編輯工具，直接搬移 / 縮放分割區 | 沒有 LVM 時唯一的擴充手段：必須卸載（通常得用 Live USB 開機）才能操作 |

**為什麼**：沒有 LVM 的伺服器，磁碟滿了只有兩條路：停機用 `gparted` 挪分割區，或重裝搬資料；有 LVM 則是加一顆硬碟、`vgextend` + `lvextend` 幾秒鐘線上完成。LVM 快照也是做資料庫一致性備份最簡單的方式，前提是 VG 有預留空間。

**怎麼做**：

✅ 伺服器**一律使用 LVM**：日後擴充磁碟、線上放大分割區、做快照備份都靠它。

```
sda
├─ sda1  /boot/efi   (ESP, 1 GB)
├─ sda2  /boot       (2 GB, ext4)
└─ sda3  LVM PV
   └─ vg0
      ├─ lv_root   /        (30 GB)
      ├─ lv_var    /var     (40 GB)
      ├─ lv_log    /var/log (10 GB)
      ├─ lv_home   /home    (50 GB)
      ├─ lv_swap   swap     (8 GB)
      └─ (預留 20% 空間給快照與未來擴充)
```

💡 **不要一次把 VG 用完**。保留空間給 LVM 快照（備份前建立）與緊急擴充。

**注意**：兩者安裝程式的 LVM 預設分配方式不同，不看清楚就會裝出「磁碟明明 200 GB，`/` 卻只有 100 GB」的機器：

- 🟠 Ubuntu Server 安裝程式預設「Use an entire disk」+「Set up this disk as an LVM group」勾選，但 **預設只分配約一半空間給 `/`**，安裝後需手動 `lvextend`（見 [04 系統管理](04-系統管理.md)）。
- 🔵 Fedora Server 安裝程式預設「Automatic」會建立 LVM（xfs），`/` 預設最多 70 GB，其餘給 `/home` 或保留。

### 2.3 檔案系統選擇

**名詞與關係**：四種檔案系統分成兩派——傳統日誌式（ext4、XFS）與 Copy-on-Write（Btrfs、ZFS）；後者把快照、壓縮、卷管理都做進檔案系統裡，所以功能多、但寫入模式不同：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **ext4** | Linux 最通用的日誌式檔案系統，工具最齊、可線上放大、離線縮小 | 🟠 預設；「能縮小」是它和 XFS 最大的實務差異 |
| **XFS** | 源自 SGI 的高效能日誌式檔案系統，擅長大檔案與高並行寫入 | 🔵 Server 預設（跟隨 RHEL）；只能放大不能縮小，切錯只能重建 |
| **Btrfs** | 內建快照、壓縮、子卷、多碟 RAID 的 CoW 檔案系統 | 🔵 Workstation 預設；功能像「檔案系統 + 簡化版 LVM」，所以 Workstation 不再用 LVM |
| **ZFS** | 來自 Solaris 的 CoW 檔案系統兼卷管理器，以資料完整性校驗與大容量儲存池著稱 | 授權與 Linux 核心不相容而不在核心樹內：🟠 把模組預先編進核心套件，裝 `zfsutils-linux` 即可；🔵 要靠外部 repo 的模組 |
| **CoW（Copy-on-Write）** | 寫入時不覆蓋原資料、而是寫到新位置再切換指標的策略 | Btrfs / ZFS 的快照與完整性校驗都靠它；代價是頻繁的小寫入（資料庫）會產生碎片、變慢 |
| **子卷（subvolume）** | Btrfs 內部可各自掛載、各自快照的邏輯目錄樹（🔵 預設有 `root` 與 `home` 兩個） | 取代 LVM 中 LV 的角色，但共用同一個空間池，不必預先分大小 |
| **zstd 壓縮** | Btrfs 的透明壓縮演算法之一，`compress=zstd:1` 表示最快的等級 | 🔵 Workstation 預設開啟；省空間並減少寫入量，CPU 負擔很低 |

**為什麼**：檔案系統一旦格式化就只能靠搬資料換掉。最常見的踩雷是 XFS **不能縮小**——切太大的 `/home` 想讓給 `/var` 只能砍掉重建；反之 Btrfs 快照與壓縮對桌面很方便，但在資料庫伺服器上 CoW 可能拖慢寫入。

**怎麼做**：兩者預設不同，反映各自的上游路線——Ubuntu 沿用 Debian 的保守選擇 ext4（成熟、可縮小、工具最齊）；Fedora 跟隨 RHEL 在伺服器上用 XFS（大檔案與高並行表現好），桌面則主推 Btrfs 以取得快照與壓縮。依用途對照下表選擇：

| 檔案系統 | 適用 | 備註 |
|---|---|---|
| ext4 | 通用、穩定、可縮小 | 🟠 Ubuntu 預設 |
| XFS | 大檔案、高並行、企業伺服器 | 🔵 Fedora Server 預設；**無法縮小** |
| Btrfs | 快照、壓縮、子卷、RAID1 | 🔵 Fedora Workstation 預設（zstd:1 壓縮） |
| ZFS | 儲存伺服器、大量資料完整性 | 🟠 Ubuntu 有原生支援（`zfsutils-linux`）；Fedora 需外部 repo |

### 2.4 全碟加密（LUKS）

**名詞與關係**：LUKS（§1.5）不是自己做加密，而是三個元件分工：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **dm-crypt** | Linux 核心的透明區塊加密層（device-mapper 的 crypt 目標），對每個區塊即時加解密 | 真正做加密的元件；LUKS 只是它的「磁頭格式與金鑰槽管理」，解鎖後會多出一個 `crypt` 裝置給 LVM 用 |
| **LUKS1 / LUKS2** | LUKS 的兩代磁頭格式：LUKS2（現行預設）支援多金鑰槽、Argon2 金鑰衍生、TPM 綁定 | 安裝程式建的都是 LUKS2；GRUB 只保證能解 LUKS1，這就是 `/boot` 必須留在加密層外的原因 |
| **`systemd-cryptenroll`** | systemd 提供、把 TPM2 / FIDO2 金鑰登錄進 LUKS2 金鑰槽的工具 | 讓 §1.5 說的「TPM 自動解鎖」成立：開機時 initramfs 向 TPM 要金鑰，而不是向人要密碼 |

**為什麼**：硬碟報廢送修、機器被整台搬走、或雲端供應商回收磁碟時，未加密的資料就是明文外流；筆電與放在非自有機房的伺服器尤其需要。代價是每次開機要有人輸密碼——伺服器若不能接受，就搭配 TPM2 自動解鎖（§1.5）。

**怎麼做**：兩者安裝程式都提供一鍵勾選：
- 🟠 Ubuntu Server：Storage → 「Encrypt the LVM group with LUKS」。
- 🔵 Fedora：Installation Destination → 「Encrypt my data」。

**注意**：安裝後結構：`sda3 → LUKS (crypt) → LVM PV → LV`。`/boot` 必須是未加密的獨立分割區（GRUB 雖能解 LUKS1，但實務上不建議）。

---

## 3. 🟠 Ubuntu Server 24.04 互動式安裝（Subiquity）

**名詞與關係**：安裝流程裡每一個選單都對應裝好之後的某個設定檔或服務，知道對應關係才知道哪些選項事後難改：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Subiquity** | Ubuntu Server 的文字介面安裝程式（§1.1），20.04 起取代 debian-installer，步驟固定為線性流程 | 讀 autoinstall 設定（§5.1）的也是它；本節所有選單都是它的畫面 |
| **DHCP / 靜態 IP** | DHCP 是由伺服器自動配發 IP 的協定；靜態 IP 則是手動寫死 | 安裝程式預設 DHCP；伺服器上線後多半要改靜態，可在 Network 步驟就設好 |
| **netplan** | 🟠 Ubuntu 的網路設定抽象層：讀 `/etc/netplan/*.yaml`，產生底層 systemd-networkd 或 NetworkManager 的設定 | 安裝程式的 Network 步驟就是在寫 netplan YAML；之後改 IP 也是改這裡（05 章） |
| **Proxy / Mirror** | Proxy 是安裝程式對外連線經過的代理伺服器；Mirror 是套件庫的鏡像站 | 兩者都會寫進裝好系統的 `apt` 設定，安裝時填錯之後每次 `apt` 都受影響 |
| **`sudo` 群組** | 🟠 Ubuntu 授權執行 `sudo` 的群組（🔵 對應 `wheel`） | root 鎖定，所以第一位使用者靠這個群組取得管理權 |
| **Ubuntu Pro** | Canonical 的訂閱服務：延長安全更新（ESM）、免重開機修補核心（Livepatch）等，個人 5 台免費 | 安裝時跳過不影響任何功能，之後 `pro attach` 即可 |
| **OpenSSH server** | 提供 SSH 遠端登入的 `sshd` 服務 | 沒勾就沒有遠端管道；從 GitHub / Launchpad 匯入公鑰可一次完成金鑰登入設定 |
| **snap** | Canonical 的自包含套件格式（§1.1） | 「Featured Server Snaps」就是在此預裝 docker、lxd 等 snap 版本 |
| **`apt full-upgrade`** | 更新所有套件，且允許為了解決相依而移除或新增套件（`apt upgrade` 不會） | 新裝系統第一次更新用它，才能把核心與相依一起換到最新 |

**為什麼**：Subiquity 的步驟不多，但有幾個選項一旦按過就很難回頭：語言選中文會讓家目錄出現中文路徑、mirror 沒填台灣站點會讓後續每次 `apt` 都慢、LVM 預設只用一半磁碟、忘了勾 OpenSSH 就得跑機房接螢幕。照下面順序走，並在每個標粗體的步驟停一下確認。

**怎麼做**：

1. **開機**：從 USB 啟動 → 選「Try or Install Ubuntu Server」。
2. **語言 / 鍵盤**：選 English（避免路徑亂碼）；鍵盤依實際。
3. **Installation type**：
   - `Ubuntu Server`：標準。
   - `Ubuntu Server (minimized)`：無 man page、無 vim 等，適合容器 / 雲端。
4. **Network**：預設 DHCP；可在此設定靜態 IP（產生 `/etc/netplan/00-installer-config.yaml`）。
5. **Proxy / Mirror**：企業內網有 proxy 時填 `http://proxy:3128`；Mirror 台灣可填 `http://tw.archive.ubuntu.com/ubuntu`。
6. **Storage**：
   - 「Use an entire disk」+ 勾選 LVM（選擇性勾選 LUKS）。
   - 或「Custom storage layout」手動分割（參考 §2.2）。
   - ⚠️ 確認目標磁碟後才按 Done → Continue（會清除資料）。
7. **Profile**：主機名、使用者、密碼（此使用者自動加入 `sudo` 群組）。
8. **Ubuntu Pro**：可跳過（之後 `sudo pro attach <token>`）。
9. **SSH**：勾選「Install OpenSSH server」，可從 GitHub / Launchpad 匯入公鑰（強烈建議）。
10. **Featured Server Snaps**：一般跳過（之後需要再裝，snap 版本的 docker 等在企業環境常有權限與路徑問題）。
11. 等待安裝完成 → **Reboot Now** → 拔除 USB。

**為什麼要先更新**：ISO 的打包日期到你安裝當下可能相隔數月，中間累積的核心與 OpenSSL 安全更新都還沒套用；趁機器還沒對外提供服務時先更新並重開，比上線後再排維護窗口容易得多。

安裝完成後第一件事：

```bash
sudo apt update && sudo apt full-upgrade -y && sudo reboot
```

### 3.1 🟠 Ubuntu Desktop 安裝重點

**名詞與關係**：桌面版多出來的選項全都圍繞「授權上不能預裝的東西」——專有驅動、有專利的編解碼器——以及一個實驗性的加密選項：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Flutter 安裝程式** | 24.04 起 Ubuntu Desktop 的新安裝程式（以 Flutter 框架撰寫，取代舊的 Ubiquity） | 桌面專用，但底層引擎與 Subiquity 相同；選項比 Server 少，多了驅動與編解碼器 |
| **third-party software** | 安裝時勾選即安裝專有驅動（NVIDIA、部分 Wi-Fi 韌體）與多媒體編解碼器 | 牽涉 §1.5 的 DKMS 與 MOK：Secure Boot 開啟時安裝程式會要你設 MOK 密碼 |
| **編解碼器（codec）/ H.264** | 播放或編碼特定影音格式的程式庫；H.264 是最常見但有專利的影片格式 | 因授權問題不在預設安裝內，靠 third-party 勾選或 `ubuntu-restricted-extras` 補齊 |
| **`ubuntu-restricted-extras`** | 一個 meta 套件，一次裝齊受限元件（編解碼器、Microsoft 核心字型、unrar 等） | 與 third-party 勾選重疊，漏勾時事後補裝用它 |
| **TPM 備援全碟加密** | 用 TPM2 保管 LUKS 金鑰、免密碼開機的桌面版實作（§1.5、§2.4） | 標為實驗：韌體更新或改 Secure Boot 設定會讓 TPM 量測值改變而無法解鎖，必須保管好復原金鑰 |

**為什麼**：桌面安裝與伺服器的差別主要在第三方驅動與編解碼器——沒勾「third-party software」的 NVIDIA 機器開機後可能只有低解析度畫面，沒裝 `ubuntu-restricted-extras` 則瀏覽器放不了 H.264 影片。實驗性的 ZFS / TPM 加密選項不建議用在要長期使用的工作機。

**怎麼做**：

- 24.04 起為新的 Flutter 安裝程式；「Default installation」vs「Extended selection」（含 LibreOffice 等）。
- 「Install third-party software」勾選可安裝 NVIDIA 驅動與媒體編解碼器。
- 磁碟：預設 ext4，可選 **ZFS（實驗）**、LVM、LUKS、**TPM 備援全碟加密（實驗）**。
- 安裝完成建議：`sudo apt install ubuntu-restricted-extras`（編解碼器）。

---

## 4. 🔵 Fedora 44 互動式安裝（Anaconda）

**名詞與關係**：Fedora 的安裝程式與權限預設和 Ubuntu 名稱不同、觀念相同，先對上：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Anaconda** | Fedora / RHEL 的安裝程式（§1.1）：Server 為傳統 GTK 介面，Workstation 42 起改為 Web UI | 讀 Kickstart（§5.2）的也是它；Fedora ISO 內只有它一個安裝程式 |
| **hub 式介面 / Installation Summary** | Anaconda 的中央總覽畫面：每個項目（鍵盤、儲存、網路…）各自進去設定再回到總覽，沒有固定順序 | 與 Subiquity 的線性流程相反：不會強迫你經過每一頁，所以容易漏掉項目 |
| **root 鎖定** | 不設 root 密碼、root 不能直接登入的安全預設 | 需要另一個「管理員」帳號透過 `sudo` 提權，於是需要 `wheel` |
| **`wheel` 群組** | 🔵 Fedora / RHEL 系授權執行 `sudo` 的群組（🟠 對應 `sudo` 群組） | 「Make this user administrator」勾選就是加入這個群組 |

**為什麼**：Anaconda 是「中央總覽 + 分項設定」的 hub 式介面，沒有固定順序，容易漏掉項目——最常見的是**網卡預設關閉**，裝完後才發現機器沒有 IP、SSH 連不上，得回機房接螢幕。root 預設鎖定也與 Ubuntu 一致，管理員走 `wheel` 群組。

### 4.1 Fedora Server

**名詞與關係**：Installation Summary 上的每個方塊都對應一個裝好之後的元件，漏掉哪一個，後果就出在對應的元件上：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Installation Source / Closest mirror** | 安裝時的套件來源；「Closest mirror」由 Fedora 的 mirrorlist 服務依地理位置自動挑鏡像站 | netinst 映像沒帶套件，此項必須有網路；DVD 版預設用光碟本身 |
| **Software Selection / add-on** | 選擇基礎環境（Fedora Server Edition）與附加群組：Guest Agents 是虛擬機代理程式，Headless Management 就是 Cockpit | 對應 Kickstart `%packages` 的環境與群組（§5.2） |
| **Blivet-GUI** | Anaconda 內建的進階儲存編輯器，以樹狀圖直接操作分割區 / LVM / Btrfs | 「Custom」是簡化版；要精確照 §2.2 切 PV / VG / LV 時用它 |
| **Network Time** | 開機後由 `chronyd` 向 NTP 伺服器對時的開關 | 沒開的話憑證驗證與日誌時間戳都會出問題；§8 用 `timedatectl` 驗證 |
| **Cockpit** | Red Hat 主導的網頁式主機管理介面（服務、儲存、網路、終端機），監聽 9090 埠 | Fedora Server 預設安裝並啟用；🟠 Ubuntu 要自行 `apt install cockpit`（§9） |
| **`dnf upgrade`** | 🔵 更新所有套件的指令（等同 🟠 的 `apt full-upgrade`，預設就允許相依變動） | 安裝後第一步；核心更新後要重開機才會跑在新核心上 |

**怎麼做**：

1. 從 USB 啟動 → 「Install Fedora 44」（先做「Test this media」可避免 USB 損壞造成安裝失敗）。
2. 語言：English (United States)。
3. **Installation Summary** 中央畫面依序設定：
   - **Keyboard / Time & Date**：時區選 Asia/Taipei，開啟 Network Time。
   - **Installation Source**：netinst 版需要網路，預設「Closest mirror」。
   - **Software Selection**：Fedora Server Edition；可勾選 Guest Agents、Headless Management（Cockpit）等 add-on。
   - **Installation Destination**：
     - 「Automatic」：LVM + xfs。
     - 「Custom」→ 選 LVM 或 Btrfs 手動配置；「Advanced Custom (Blivet-GUI)」有更完整的 GUI。
     - 勾選「Encrypt my data」啟用 LUKS。
   - **Network & Host Name**：設定主機名（`server01.example.com`），開啟網卡（預設關閉！）並設 IP。
   - **Root Password**：Fedora 預設 **鎖定 root** 且不允許 SSH root 密碼登入；建議保持鎖定並建立管理員。
   - **User Creation**：勾選「Make this user administrator」→ 加入 `wheel` 群組。
4. **Begin Installation** → 完成後 Reboot。
5. Fedora Server 預設啟用 **Cockpit**（`https://<ip>:9090`）網頁管理介面。

**為什麼要先更新**：Fedora 更新頻率高，DVD 映像與 updates repo 之間常差數百個套件（含核心）；netinst 雖然裝的是較新的版本，仍建議在上線前先完整升級並重開一次，確認新核心能正常開機。

安裝完成後：

```bash
sudo dnf upgrade -y && sudo reboot
```

### 4.2 🔵 Fedora Workstation

**名詞與關係**：Workstation 幾乎把每個環節都換成桌面取向的元件，所以名字和 Server 對不上：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Live 映像** | 開機即進入完整 GNOME 桌面、可先試用再安裝的 ISO（§1.1） | Workstation 只提供這種形式，安裝程式是桌面上的一個應用程式 |
| **Anaconda Web UI** | Anaconda 的新前端：用瀏覽器技術（與 Cockpit 同一套元件）實作的精靈式流程 | 取代 Workstation 上的 hub 介面；Server 仍用傳統 GTK 版，兩者選項不同 |
| **GNOME Initial Setup** | 第一次開機時 GNOME 的歡迎精靈，負責建立使用者、時區、線上帳號 | Web UI 安裝程式不建使用者，全交給它；在有人完成它之前機器上沒有一般帳號 |
| **RPM Fusion** | 社群維護的第三方 RPM 套件庫，提供 Fedora 因授權不收錄的軟體（NVIDIA 驅動、編解碼器） | 對應 🟠 的 third-party software（§3.1）；`akmod-nvidia` 就來自這裡（§1.5） |
| **Flatpak / Flathub** | Flatpak 是跨發行版的沙箱式桌面應用格式；Flathub 是最大的 Flatpak 應用庫 | 🔵 對應 🟠 的 snap；Workstation 預設只有 Fedora 自家的 Flatpak 庫，要在初次設定啟用第三方來源或手動加 Flathub |

**為什麼**：Workstation 的安裝流程與 Server 幾乎是兩套程式（Web UI 精靈 vs 傳統 hub），儲存預設也從 LVM + xfs 換成 Btrfs 子卷；知道這點才不會在「找不到 LVM 選項」或「使用者建立在哪裡」上卡住。

**怎麼做**：

- Live 映像：開機進 GNOME 後點「Install to Hard Drive」。
- Fedora 42 起 Workstation 使用**新版 Anaconda Web UI**，流程改為精靈式（語言 → 日期 → 儲存 → 確認）。
- 儲存預設 **Btrfs**（`root` 與 `home` 子卷，`compress=zstd:1`），自動加上 EFI 與 1 GB `/boot`（ext4）。
- 使用者建立改在**第一次開機的 GNOME Initial Setup** 完成（自動加入 `wheel`）。
- 安裝完成建議：啟用 RPM Fusion 與 Flathub（見 [03 套件與軟體安裝](03-套件與軟體安裝.md)）。

---

## 5. 無人值守安裝（Unattended Installation）

**名詞與關係**：無人值守要解決兩個問題——「設定寫成什麼格式」與「怎麼把檔案交到安裝程式手上」——兩個發行版在這兩層都用不同的東西：

```
               設定檔                       傳遞方式（怎麼把檔案交給安裝程式）                 誰執行安裝
🟠 Ubuntu   autoinstall YAML ──┬─▶ seed ISO / USB（NoCloud：user-data + meta-data）
            （#cloud-config）  ├─▶ HTTP（ds=nocloud-net;s=http://…）             ──▶ cloud-init 抓檔 ──▶ Subiquity
                               └─▶ 嵌入安裝 ISO
🔵 Fedora   Kickstart ks.cfg  ──┬─▶ USB（LABEL=OEMDRV，Anaconda 自動搜尋）
                               ├─▶ HTTP（inst.ks=http://…）                      ──▶ Anaconda 直接讀
                               └─▶ 嵌入安裝 ISO
兩邊共通：PXE（§5.3）負責把「安裝程式本身」從網路送進機器；設定檔再走上面的 HTTP 路徑
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **無人值守安裝** | 把安裝程式會問的每個問題預先寫在檔案裡，開機後全自動跑完的安裝方式 | 上位概念；autoinstall 與 Kickstart 是兩家各自的實作 |
| **autoinstall** | 🟠 Subiquity 的無人值守格式：一份 YAML，放在 cloud-init 的 `user-data` 檔內 `autoinstall:` 鍵之下 | 依賴 cloud-init 把檔案送進安裝環境；取代 20.04 以前的 preseed |
| **preseed / debian-installer** | Debian 傳統安裝程式（d-i）與它的問答預填格式 | 🟠 20.04 起 Server 不再使用；看到 preseed 教學可直接略過 |
| **Kickstart** | 🔵 Anaconda 的無人值守格式：自訂指令語法的 `.ks` 純文字檔，附 `%packages` / `%post` 區段 | 由 Anaconda 直接解析、不經 cloud-init；RHEL / CentOS / Rocky 通用 |
| **cloud-init** | 雲端映像第一次開機時讀取「資料源」、套用主機名 / 使用者 / 套件等設定的標準工具 | 🟠 安裝媒體內也跑它，專門負責把 autoinstall 檔抓進來交給 Subiquity；§6 雲端映像的主角 |
| **NoCloud** | cloud-init 的一種資料源：不靠雲平台 API，直接從本機光碟 / 磁碟（`ds=nocloud`）或 HTTP（`ds=nocloud-net`）讀 `user-data` + `meta-data` | 沒有雲平台時餵設定給 cloud-init 的方式；兩個檔缺一不可 |
| **`user-data` / `meta-data`** | NoCloud 的兩個檔：前者是 cloud-config 設定本體，後者放 `instance-id` 等實例識別 | autoinstall 內容寫在 `user-data`；`meta-data` 可近乎空白但必須存在 |
| **YAML** | 以縮排表示階層的設定檔格式 | autoinstall 與 cloud-init 都用它；縮排錯一格整份失效，所以要先驗證 |
| **核心參數 / `inst.ks=`** | 開機時附加給 Linux 核心、由安裝程式讀取的參數；`inst.ks=` 告訴 Anaconda 去哪抓 Kickstart | 🟠 對應的是 `autoinstall ds=nocloud…`；PXE 時寫在開機選單裡 |
| **OEMDRV** | Anaconda 會自動搜尋的磁碟標籤：找到標為 `OEMDRV` 的裝置就讀其根目錄的 `ks.cfg` | 不必改核心參數的傳遞方式，適合單台 |
| **schema 驗證** | 用格式描述檔（JSON schema）或專用工具（`pykickstart` 的 `ksvalidator`）在本機檢查設定檔是否合法 | 兩邊都要做：省下一輪開機才發現打錯字的時間（§5.1、§5.2） |
| **PXE** | 讓機器一開機就從網路取得開機程式與安裝程式的機制（§5.3） | 無人值守的「送安裝程式」那一半；設定檔仍靠 HTTP 傳 |

**為什麼**：手動裝第 3 台機器時就會開始出現「這台忘了勾 SSH」「那台 mirror 沒填」的差異，而這些差異日後都會變成難以重現的問題；無人值守把整份安裝決策寫成可版本控管、可 review 的檔案，裝 1 台與裝 50 台的成本一樣，也是 PXE 與 CI 自動建 VM 的前提。

**怎麼做**：兩者的機制完全不同，源自各自的歷史——Kickstart 是 Red Hat 1990 年代末就有的自家格式，Anaconda 原生支援；Ubuntu 在 20.04 放棄 debian-installer 的 preseed 後，改以 cloud-init 風格的 YAML 讓 Subiquity 讀取，因此格式與傳遞方式都向雲端映像的 NoCloud 看齊。

大量佈署、PXE 開機、CI 建 VM 時必備。兩者機制完全不同：

| | 🟠 Ubuntu **autoinstall** | 🔵 Fedora **Kickstart** |
|---|---|---|
| 格式 | YAML（cloud-init 風格） | 自訂 `.ks` 指令語法 |
| 傳遞方式 | 第二顆 ISO/USB（NoCloud：`user-data` + `meta-data`）、HTTP、或嵌入 ISO | 核心參數 `inst.ks=http://...`、`inst.ks=hd:LABEL=OEMDRV:/ks.cfg`、嵌入 ISO |
| 驗證工具 | `cloud-init schema --config-file` + Subiquity JSON schema | `ksvalidator -v F44`（`pykickstart` 套件） |
| 產生範本 | 安裝完成的機器 `/var/log/installer/autoinstall-user-data` | 安裝完成的機器 `/root/anaconda-ks.cfg` |
| 文件 | <https://canonical-subiquity.readthedocs-hosted.com/en/latest/reference/autoinstall-reference.html> | <https://pykickstart.readthedocs.io/> |

### 5.1 🟠 Ubuntu autoinstall 範例

**名詞與關係**：範例裡的每個關鍵欄位與工具，都對應 §5 那條「設定檔 → 傳遞 → cloud-init → Subiquity」鏈上的一個環節：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **`#cloud-config`** | `user-data` 第一行的魔術標頭，告訴 cloud-init「這是 YAML 設定而不是 shell 腳本」 | 少了它整份檔案被忽略；autoinstall 內容必須在它之下的 `autoinstall:` 鍵內 |
| **`match: name`** | netplan 語法（§3）：用萬用字元比對網卡名稱，而非寫死 | KVM（`ens18`）、VirtualBox（`enp0s3`）、實體機（`eno1`）命名不同，寫死就換平台失敗 |
| **`sizing-policy: all`** | autoinstall `storage.layout` 的選項：把整個 VG 分配給 `/` | 修掉 §2.2「預設只用一半」的行為；要照 §2.1 切多個 LV 則改用 `config:` 手動描述 |
| **SHA-512 密碼雜湊** | 以 `$6$` 開頭的 crypt 格式雜湊，`openssl passwd -6` 或 `mkpasswd -m sha-512` 產生 | 檔案會進 git / HTTP 所以不能放明文；Kickstart 的 `--iscrypted` 用同一格式 |
| **`allow-pw: false`** | 把裝好的 sshd 直接設為拒絕密碼登入 | 配合 `authorized-keys`，從第一次開機起就只接受金鑰 |
| **curtin / `in-target` / `late-commands`** | curtin 是 Subiquity 底層實際做分割與複製系統的引擎；`late-commands` 在安裝完成、重開前執行；`curtin in-target` 讓指令在新系統（`/target`）內執行 | 對應 Kickstart 的 `%post`；沒加 `in-target` 的指令是在安裝環境執行，路徑要自己加 `/target` |
| **`qemu-guest-agent`** | 虛擬機內的代理程式，讓 KVM 主機能查 IP、凍結檔案系統做快照、優雅關機 | 只在 KVM / Proxmox 上有意義；實體機可拿掉 |
| **seed ISO / `cloud-localds`** | 把 `user-data` + `meta-data` 包成 NoCloud 格式小 ISO 的工具（來自 `cloud-image-utils`；`genisoimage` / `xorriso` 也能做） | 當第二顆光碟掛給 VM；§6 用的是同一招 |
| **`ds=nocloud` vs `ds=nocloud-net`** | 核心參數，指定 NoCloud 資料源位置：本機路徑 / 裝置，或 HTTP URL | 單台用 seed ISO 配前者；多台或 PXE 用 HTTP 配後者 |
| **`cloud-init schema` / JSON schema** | 前者檢查 cloud-config 外層格式；後者是 Subiquity 公布的 autoinstall 欄位定義，用 Python `jsonschema` 套件比對 | 兩層各管一半，都通過才代表檔案能被接受 |

**為什麼**：這份範例針對幾個實際會失敗的點做了預防：網卡用 `match: name: "en*"` 而不是寫死 `ens18`，因為同一份設定換到不同虛擬化平台時網卡名會變，寫死就直接卡在網路設定；`sizing-policy: all` 修掉 §2.2 提到的「只用一半磁碟」預設；密碼用 SHA-512 雜湊而非明文，因為 `user-data` 通常會進 git 或放在 HTTP 上；`allow-pw: false` 讓機器從第一次開機起就只接受金鑰登入。

**怎麼做**：`user-data`（**第一行必須是 `#cloud-config`**，Subiquity 靠這行辨識檔案格式，少了就會被當成一般安裝、整份設定被忽略）：

```yaml
#cloud-config
autoinstall:
  version: 1
  locale: en_US.UTF-8
  keyboard:
    layout: us
  timezone: Asia/Taipei
  refresh-installer:
    update: false
  apt:
    primary:
      - arches: [amd64]
        uri: http://tw.archive.ubuntu.com/ubuntu
  network:
    version: 2
    ethernets:
      # 用 match 避免因網卡命名（ens18 / enp0s3）不同而失敗
      primary:
        match:
          name: "en*"
        dhcp4: true
  storage:
    layout:
      name: lvm
      sizing-policy: all          # 把整個 VG 分配給 /（預設只用一半）
      # password: "changeme"      # 加上這行即啟用 LUKS 全碟加密
  identity:
    hostname: ubuntu-auto
    username: admin
    # 用 `openssl passwd -6` 或 `mkpasswd -m sha-512` 產生
    password: "$6$rounds=4096$saltsalt$4gZ3hVdJvJZk9pRQx5Ig7KmzuhBRD0xKmFGm5N1Q3wfZ1qKZ8Ri2CQ7O.pTNvB4E9JuYfN3hVbC5pQeXvpRIb/"
  ssh:
    install-server: true
    allow-pw: false
    authorized-keys:
      - "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIExampleKeyReplaceMe admin@laptop"
  packages:
    - vim
    - htop
    - curl
    - qemu-guest-agent
  updates: security
  late-commands:
    - curtin in-target --target=/target -- systemctl enable qemu-guest-agent
    - echo 'admin ALL=(ALL) NOPASSWD:ALL' > /target/etc/sudoers.d/90-admin
    - chmod 440 /target/etc/sudoers.d/90-admin
  shutdown: reboot
```

`meta-data`（可為空檔，但必須存在——NoCloud 資料源同時找兩個檔案，缺一個就不會被辨識為 NoCloud，安裝程式會退回互動模式）：

```yaml
instance-id: ubuntu-auto-001
```

**為什麼用 seed ISO**：Subiquity 讀取 NoCloud 資料的方式與雲端映像相同，所以最不需要額外基礎設施的做法就是把兩個檔案包成一顆小 ISO 當第二顆光碟；沒有 HTTP 伺服器的隔離環境也能用。

製作 NoCloud 資料 ISO 並開機：

```bash
# 需要 cloud-image-utils（Ubuntu）或 genisoimage/xorriso
sudo apt install -y cloud-image-utils
cloud-localds seed.iso user-data meta-data
# 在 VM 掛載兩顆光碟：安裝 ISO + seed.iso；開機時 GRUB 選單按 e，
# 在 linux 行末尾加上： autoinstall ds=nocloud;s=/cdrom/   (若 seed 在第二顆光碟則用 ds=nocloud\;s=/dev/sr1)
```

要裝多台或走 PXE（§5.3）時，改把檔案放到 HTTP 上，就不必為每台機器製作 seed ISO：

```bash
# 把 user-data / meta-data 放到 http://10.0.0.5/auto/ 下，開機核心參數：
# autoinstall ds=nocloud-net;s=http://10.0.0.5/auto/
```

**為什麼要先驗證**：YAML 縮排錯一格或欄位名打錯，要等到開機進安裝程式才會停在錯誤畫面，一輪就是好幾分鐘；在自己機器上先跑驗證，幾秒鐘就能抓到。`cloud-init schema` 只檢查 cloud-config 外層格式，autoinstall 專屬欄位要靠 Subiquity 的 JSON schema 才驗得到，所以兩步都做。

驗證語法（已在 Ubuntu 24.04 實測）：

```bash
# 1) 基本 YAML / cloud-config 格式檢查（一般系統即可執行）
cloud-init schema --config-file user-data
#    → Valid schema user-data

# 2) 完整 autoinstall 欄位驗證：使用 Subiquity 官方 JSON schema
sudo apt install -y python3-jsonschema python3-yaml
curl -sSLO https://raw.githubusercontent.com/canonical/subiquity/main/autoinstall-schema.json
python3 - <<'PY'
import json, yaml, jsonschema
schema = json.load(open('autoinstall-schema.json'))
doc = yaml.safe_load(open('user-data'))
jsonschema.validate(doc['autoinstall'], schema)
print('autoinstall OK')
PY
```

> 💡 安裝媒體內的 Subiquity 會在開機時再驗證一次；若格式錯誤，安裝程式會停在錯誤畫面並顯示原因（可按 F2 進 shell 看 `/var/log/installer/`）。

### 5.2 🔵 Fedora Kickstart 範例

**名詞與關係**：Kickstart 檔分成「指令」與「區段」兩部分；指令對應 Anaconda 的每個設定方塊，區段則是套件清單與安裝前後要跑的 shell：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **指令 vs 區段** | 檔案前半是一行一個的指令（`lang`、`network`、`part`…）；後半是 `%packages` / `%pre` / `%post` 到 `%end` 的區段 | 指令由 Anaconda 直接解讀；區段內容分別是套件清單與在安裝前 / 後執行的 shell |
| **`ignoredisk` / `clearpart` / `zerombr`** | 限定 Anaconda 只碰哪些磁碟 / 清除分割表 / 對沒有分割表的磁碟直接初始化 | 三者組合決定「哪些磁碟會被清」；沒有 `ignoredisk` 時 `--all` 會清所有看得到的磁碟 |
| **`part` / `volgroup` / `logvol`** | 依序建立分割區（含 `pv.01` 這種 PV）、VG、LV 的 Kickstart 指令 | 就是 §2.2 的 PV → VG → LV 用文字描述；`--grow` 表示拿走剩餘空間 |
| **套件群組 `@^` / `@` / `-`** | `%packages` 內的前綴：`@^` 指定環境（environment）、`@` 加群組、`-` 排除套件 | 對應 Anaconda 的 Software Selection（§4.1）；`@^server-product-environment` 等於選 Fedora Server Edition |
| **序列埠 console / IPMI** | `console=ttyS0` 讓核心把訊息也送到序列埠；IPMI 是伺服器主機板的遠端管理介面，可把序列埠內容轉成網路存取（SOL） | 沒螢幕的機架伺服器唯一能看開機訊息的管道；`bootloader --append` 把它寫進 GRUB 讓每次開機都生效 |
| **`%post` / `--log`** | 安裝完成後在新系統內（chroot）執行的 shell 區段，`--log` 把輸出存到指定檔案 | 對應 autoinstall 的 `late-commands`；失敗時看 `/root/ks-post.log` |
| **Ansible** | 以 SSH 推送設定的自動化工具（13 章） | `%post` 只做到「能 SSH、能 sudo」，其餘設定交給 Ansible，避免把整套組態塞進 Kickstart |
| **`ksvalidator` / `pykickstart`** | pykickstart 是 Anaconda 解析 Kickstart 用的 Python 函式庫，`ksvalidator` 是它附的語法檢查器，`-v F44` 指定版本語法 | 本機先驗證，對應 §5.1 的 schema 驗證 |
| **`/root/anaconda-ks.cfg`** | Anaconda 在每台裝好的機器上留下的「實際套用的 Kickstart」 | 互動安裝一台後拿它當範本，比從零寫快 |

**為什麼**：Kickstart 的幾個指令直接對應真實事故：`ignoredisk --only-use=sda` 避免 `clearpart --all` 把插著的資料碟或 USB 一起清掉；`rootpw --lock` + `wheel` 管理員與互動安裝的安全預設一致；`console=ttyS0` 讓沒有螢幕的機器能從序列埠 / IPMI 看到開機訊息；`%post` 在裝好的系統內做 sudo 免密碼與 guest agent 啟用，是 Ansible 接手前最起碼的自動化。

**怎麼做**：`ks.cfg`：

```
# Fedora 44 Server Kickstart
text
lang en_US.UTF-8
keyboard us
timezone Asia/Taipei --utc
timesource --ntp-server=time.stdtime.gov.tw

# 網路：DHCP + 主機名
network --bootproto=dhcp --device=link --activate --hostname=fedora-auto.example.com

# 安裝來源（netinst 用）；DVD 版可改成 cdrom
url --mirrorlist="https://mirrors.fedoraproject.org/mirrorlist?repo=fedora-44&arch=x86_64"
repo --name=updates --mirrorlist="https://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f44&arch=x86_64"

# 使用者：鎖 root、建立 wheel 管理員（密碼用 openssl passwd -6 產生）
rootpw --lock
user --name=admin --groups=wheel --iscrypted --password='$6$rounds=4096$saltsalt$4gZ3hVdJvJZk9pRQx5Ig7KmzuhBRD0xKmFGm5N1Q3wfZ1qKZ8Ri2CQ7O.pTNvB4E9JuYfN3hVbC5pQeXvpRIb/'
sshkey --username=admin "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIExampleKeyReplaceMe admin@laptop"

# 安全
selinux --enforcing
firewall --enabled --service=ssh
services --enabled=sshd,chronyd,firewalld

# 磁碟：清空 sda，UEFI + LVM + xfs
ignoredisk --only-use=sda
zerombr
clearpart --all --initlabel --disklabel=gpt
bootloader --location=mbr --append="console=tty0 console=ttyS0,115200n8"
part /boot/efi --fstype=efi   --size=1024
part /boot     --fstype=xfs   --size=2048
part pv.01     --fstype=lvmpv --size=1 --grow
volgroup vg0 pv.01
logvol /        --vgname=vg0 --name=lv_root --fstype=xfs --size=30720
logvol /var     --vgname=vg0 --name=lv_var  --fstype=xfs --size=20480
logvol /var/log --vgname=vg0 --name=lv_log  --fstype=xfs --size=8192
logvol swap     --vgname=vg0 --name=lv_swap --fstype=swap --size=4096
logvol /home    --vgname=vg0 --name=lv_home --fstype=xfs --size=1 --grow

%packages
@^server-product-environment
@headless-management
vim-enhanced
htop
curl
qemu-guest-agent
-plymouth*
%end

%post --log=/root/ks-post.log
echo 'admin ALL=(ALL) NOPASSWD:ALL' > /etc/sudoers.d/90-admin
chmod 440 /etc/sudoers.d/90-admin
systemctl enable qemu-guest-agent
%end

reboot
```

**為什麼有兩種傳遞方式**：HTTP 適合 PXE 與大量佈署；`OEMDRV` 標籤的 USB 則是 Anaconda 會自動搜尋的路徑，不必改任何核心參數，適合帶一支 USB 到機房裝單台。與 autoinstall 一樣，先用 `ksvalidator` 在本機驗證，不要用一次開機安裝來當語法檢查。

使用方式：

```bash
# 開機時按 Tab/e 編輯核心參數，加上：
#   inst.ks=http://10.0.0.5/ks.cfg
# 或把 ks.cfg 放在標籤為 OEMDRV 的 USB 根目錄，Anaconda 會自動讀取：
#   inst.ks=hd:LABEL=OEMDRV:/ks.cfg
# 純文字模式 + 序列埠： inst.text console=ttyS0

# 驗證語法（在任何 Fedora 上）
sudo dnf install -y pykickstart
ksvalidator -v F44 ks.cfg
```

**注意**：`ksvalidator` 只檢查語法與版本相容性，不會檢查磁碟名稱是否存在或 mirror 是否可達；裝完後看 `/root/anaconda-ks.cfg` 與 `/root/ks-post.log` 確認實際套用的內容。

### 5.3 PXE / 網路開機重點

**名詞與關係**：PXE 是一條由多個協定接力的鏈，每一段各用不同的協定與檔案，卡住時要先知道是斷在哪一段：

```
機器開機，網卡的 PXE 韌體發出 DHCP 請求
  ──▶ DHCP 伺服器回覆：IP + next-server（TFTP 位址）+ filename（開機檔名）   ← DHCP option 66 / 67
  ──▶ 用 TFTP 抓開機檔：BIOS 機器抓 pxelinux.0；UEFI 機器抓 shimx64.efi → grubx64.efi
  ──▶ GRUB 讀選單，用 TFTP 或 HTTP 抓 vmlinuz + initrd，帶著核心參數啟動
  ──▶ 核心 + initrd 依參數抓安裝來源（ISO / repo）與設定檔（autoinstall / ks.cfg），接回 §5.1 / §5.2

iPXE：把網卡內建的 PXE 換成功能更強的開機程式，一開始就能用 HTTP、還能寫腳本判斷機器
UEFI HTTP Boot：韌體原生支援從 HTTP URL 抓開機檔，整條鏈不再需要 TFTP
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **PXE** | Preboot eXecution Environment，網卡韌體內建的「從網路開機」標準，只會 DHCP + TFTP | 整條鏈的起點；能力很弱（無 HTTPS、TFTP 慢），所以才需要 iPXE / HTTP Boot 補強 |
| **DHCP（option 66 / 67）** | 配發 IP 的協定（§3）；PXE 額外用 option 66（next-server）與 67（filename）告知去哪抓什麼 | 沒有這兩個選項機器拿到 IP 也不知道下一步；UEFI 與 BIOS 機器要回不同的 filename |
| **TFTP** | 極簡的 UDP 檔案傳輸協定，無認證、無目錄列表 | PXE 韌體唯一會的下載方式；只用來抓小的開機檔，大檔（initrd、ISO）改走 HTTP |
| **dnsmasq** | 同時提供 DHCP、DNS、TFTP 的輕量服務，一個設定檔搞定 PXE 所需 | 兩者共用；也可在既有 DHCP 旁只做 proxy-DHCP，不影響現有配發 |
| **`vmlinuz` / `initrd`** | 壓縮後的 Linux 核心 / 安裝環境的 initramfs（§2.1） | 從 ISO 內取出放上 HTTP：🟠 在 `casper/`、🔵 在 `images/pxeboot/`；ISO 或 repo 本體再由核心參數指定 |
| **`shimx64.efi` / `grubx64.efi`** | Secure Boot 下 UEFI 機器 PXE 必須先抓的兩個檔，即 §1.5 的 shim 與 GRUB 的網路開機版本 | 從發行版的 `shim-signed` / `grub2-efi` 套件取出放上 TFTP；用未簽章的 GRUB 會被 Secure Boot 擋下 |
| **iPXE** | 開源、功能完整的網路開機程式：支援 HTTP(S)、iSCSI、腳本 | 由 PXE 鏈式載入（chainload）或燒進網卡取代原韌體；載入後接手整條下載流程 |
| **UEFI HTTP Boot** | UEFI 2.5 起的原生功能：DHCP 直接回 HTTP URL，韌體自己下載開機檔 | 完全跳過 TFTP；需要主機板韌體支援，新機器建議優先 |

**為什麼**：機房裡幾十台機器輪流插 USB 既慢又容易插錯台；PXE 讓機器一開機就從網路拿核心與 autoinstall / Kickstart 檔，整批裝機可以無人在場。UEFI 機器的 PXE 又比 BIOS 麻煩，因為 Secure Boot 只接受簽章過的 `shimx64.efi` 起頭，這是最常卡住的地方。

**怎麼做**：

- 兩者皆可用 **dnsmasq**（DHCP + TFTP）+ HTTP 提供 `vmlinuz` / `initrd`：
  - 🟠 Ubuntu：從 ISO 的 `casper/vmlinuz` 與 `casper/initrd`，核心參數 `ip=dhcp url=http://.../ubuntu.iso autoinstall ds=nocloud-net;s=http://.../`。
  - 🔵 Fedora：`images/pxeboot/vmlinuz` 與 `initrd.img`，核心參數 `inst.repo=http://.../os/ inst.ks=http://.../ks.cfg`。
- UEFI PXE 需提供 `grubx64.efi` / `shimx64.efi`；建議直接用 **iPXE** 或 **HTTP boot** 簡化。

---

## 6. 雲端 / 虛擬機映像

**名詞與關係**：這一節把 §5 的 cloud-init / NoCloud 搬到「系統已經裝好」的映像上，再加上 KVM 這一層虛擬化工具：

```
官方雲端映像（noble-server-cloudimg-amd64.img / Fedora-Cloud-Base-….qcow2）
   已裝好系統 + cloud-init，沒有密碼、沒有安裝程式
   │
   ├─ qemu-img create -b <基底> ──▶ vm01.qcow2（只存與基底的差異；基底唯讀、可多台共用）
   ├─ user-data + meta-data ──cloud-localds──▶ seed.iso（NoCloud，§5.1）
   └─ virt-install --import ──▶ libvirt 在 KVM 上開機，掛 vm01.qcow2 + seed.iso
                                    └─ 第一次開機：cloud-init 讀 seed → 設主機名、使用者、公鑰、裝套件
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **KVM / QEMU / libvirt** | KVM 是 Linux 核心的虛擬化功能；QEMU 是模擬硬體、實際跑 VM 的程式；libvirt 是管理它們的統一 API 與守護程序 | 三層堆疊：`virt-install`、virt-manager、GNOME Boxes 都透過 libvirt 操作 QEMU/KVM |
| **`virt-install`** | libvirt 的命令列建 VM 工具；`--import` 表示「磁碟已有系統，不跑安裝程式」 | 雲端映像一律用 `--import`；`--os-variant` 只影響虛擬硬體的最佳化 |
| **`qemu-img` / backing file** | qcow2（§1.1）映像工具；`-b` 建立以另一個映像為「基底」的差異映像 | 基底被移動或修改，所有差異映像立即報廢；`40G` 是虛擬大小，實際只佔用寫入量 |
| **cloud-init / NoCloud / seed ISO** | §5、§5.1 定義的第一次開機設定機制與其本機資料源 | 這裡的 `user-data` 是一般 cloud-config（不是 autoinstall），因為系統已裝好、沒有 Subiquity |
| **Multipass** | Canonical 的一鍵 Ubuntu VM 工具，內建下載雲端映像與餵 cloud-init 設定 | 把上面整條流程包成一行指令；🔵 沒有對應，改用 virt-install 或 virt-manager / GNOME Boxes |
| **Toolbox / Distrobox** | 用容器（podman）開一個「像完整發行版」的互動環境，共用家目錄與硬體 | 不是 VM：共用主機核心，適合開發與測套件，不適合測開機、核心、防火牆 |
| **virt-manager / GNOME Boxes** | libvirt 的圖形前端：前者面向管理員，後者面向桌面使用者 | 🔵 桌面上取代 Multipass 的角色（§9 對照表） |

**為什麼**：測試環境或短命 VM 跑一次完整安裝程式要十幾分鐘，官方雲端映像已裝好系統並內建 cloud-init，只要餵一份 `user-data` 就能在幾十秒內拿到可 SSH 的機器；`qemu-img -b` 的 backing file 讓十台 VM 共用同一份基底映像，磁碟只存差異。這也是本機重現雲端行為（AWS / GCP 同樣用 cloud-init）最便宜的方式。

**怎麼做**：不必安裝，直接用官方雲端映像 + cloud-init（幾秒鐘開一台 VM）：

```bash
# ⚪ 在 KVM 主機（virt-install）
# 🟠 Ubuntu
wget https://cloud-images.ubuntu.com/noble/current/noble-server-cloudimg-amd64.img
# 🔵 Fedora
wget https://download.fedoraproject.org/pub/fedora/linux/releases/44/Cloud/x86_64/images/Fedora-Cloud-Base-Generic-44-1.x.x86_64.qcow2

# 共同：準備 cloud-init 資料
cat > user-data <<'EOF'
#cloud-config
hostname: vm01
users:
  - name: admin
    groups: [sudo, wheel]           # 兩者群組都寫，不存在的會被忽略
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh_authorized_keys:
      - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIExampleKeyReplaceMe admin@laptop
package_update: true
packages: [vim, htop]
EOF
echo "instance-id: vm01" > meta-data
cloud-localds seed.iso user-data meta-data

qemu-img create -f qcow2 -F qcow2 -b noble-server-cloudimg-amd64.img vm01.qcow2 40G
virt-install --name vm01 --memory 2048 --vcpus 2 \
  --disk vm01.qcow2 --disk seed.iso,device=cdrom \
  --os-variant ubuntu24.04 --import --network network=default --noautoconsole
```

**注意**：雲端映像沒有預設密碼，忘了在 `user-data` 放公鑰就進不去，只能刪掉重開；基底映像檔（`.img` / `.qcow2`）被移動或刪除後，所有依賴它的 VM 磁碟都會壞掉。

- Multipass（Canonical）：`multipass launch 24.04 --name dev` 一行開 Ubuntu VM（Windows/macOS/Linux）。
- 🔵 Fedora 對應：`virt-install --os-variant fedora-rawhide` 或使用 **Toolbox / Distrobox** 容器。

---

## 7. WSL2 安裝（Windows）

**名詞與關係**：WSL2 是「真的 Linux 核心、但不是你的核心」，這一節的所有限制都由此而來：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **WSL2** | Windows Subsystem for Linux 第 2 版：在 Windows 內建的輕量 Hyper-V 虛擬機中跑真正的 Linux 核心 | 與 WSL1（系統呼叫轉譯）不同，是完整核心；但核心由 Microsoft 提供，不是發行版自己的 |
| **`wsl.conf`** | 每個發行版內的 `/etc/wsl.conf`，設定 systemd、掛載、網路等 WSL 專屬行為 | `[boot] systemd=true` 沒開時 `systemctl` 全部無效，許多伺服器套件裝不起來 |
| **systemd** | 兩者共用的 init 系統與服務管理器 | WSL 早期不啟動它，現在預設啟動；但仍由 WSL 自己的 init 先接管，行為與實體機略有差異 |
| **ufw / firewalld** | 🟠 / 🔵 各自預設的防火牆管理工具（05 章） | WSL2 網路由 Windows 的 NAT 代管，規則只在 VM 內生效，對外無意義 |
| **SELinux** | 🔵 Fedora 預設啟用的強制存取控制（MAC）機制（16 章） | WSL 的核心沒有啟用 SELinux，所以 Fedora WSL 映像一律 disabled；測 SELinux 政策要用真機或 VM |

**為什麼**：在 Windows 工作機上要跑 Ansible、測 shell script 或編譯 Linux 工具，WSL2 比開一台 VM 輕得多，而且能直接存取 Windows 檔案；但它不是完整的 Linux 主機，把它當成伺服器測試防火牆或 SELinux 設定會得到錯誤結論。

**怎麼做**：

```powershell
wsl --install                       # 安裝 WSL 與預設 Ubuntu
wsl --list --online                 # 列出可用發行版
wsl --install Ubuntu-24.04          # 🟠
wsl --install FedoraLinux-44        # 🔵（Fedora 42 起為官方 WSL 映像）
wsl --set-default FedoraLinux-44
```

**注意**：WSL2 注意事項：
- 預設啟用 systemd（`/etc/wsl.conf` 內 `[boot] systemd=true`）；舊版本需手動加。
- 🔵 Fedora WSL 映像 SELinux 為 **disabled**、無 firewalld；並非完整 Fedora 環境。
- 網路由 Windows 代管，`ufw` / `firewalld` 規則對外部無效。

---

## 8. 安裝後檢查清單

**名詞與關係**：清單裡的指令分別驗證四件事——系統與硬體、儲存、網路與時間、安全機制——下面是前面沒定義過的幾個：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **`hostnamectl` / `timedatectl`** | systemd 附的查詢工具：前者顯示主機名、核心、虛擬化平台；後者顯示時區與 NTP 同步狀態 | 兩者共用，取代看 `/etc/hostname`、`date` 等零散做法 |
| **NTP** | Network Time Protocol，向時間伺服器對時的協定；兩者以 `chronyd` 或 `systemd-timesyncd` 實作 | 時間偏差超過幾分鐘 TLS 憑證就失效、日誌無法對照，所以列入驗收 |
| **`systemctl --failed`** | 列出開機時啟動失敗的 systemd 服務 | 應為空；有東西代表安裝程式的某個選項沒有真正套用成功 |
| **`mokutil --sb-state`** | 查詢 Secure Boot 是否啟用（§1.5） | 配合 `/sys/firmware/efi` 是否存在，才能同時確認 UEFI 模式與 Secure Boot |
| **AppArmor vs SELinux** | 🟠 / 🔵 各自的強制存取控制系統：AppArmor 依路徑寫規則，SELinux 依標籤 | `aa-status` / `getenforce` 分別確認它們有在跑；伺服器都應為啟用狀態 |
| **ufw vs firewalld** | 🟠 / 🔵 各自的防火牆前端（§7、05 章） | `ufw status` / `firewall-cmd --state` 確認防火牆有起來；🟠 ufw 預設是關的，要自己開 |

**為什麼**：最常在交機後才被發現的問題——裝成 BIOS 模式而非 UEFI、Fedora 網卡沒開、`/` 只拿到一半磁碟、NTP 沒同步導致憑證驗證失敗、某個服務開機就失敗——全都能用下面十幾條指令在一分鐘內抓出來。把它當成每台機器的驗收 SOP，而不是出事後的排錯工具。

**怎麼做**：

```bash
# ⚪ 共通
cat /etc/os-release                      # 版本
hostnamectl                              # 主機名、核心、虛擬化類型
lsblk -f                                 # 分割區與檔案系統
df -h                                    # 掛載與使用率
free -h                                  # 記憶體與 swap
ip -br a                                 # 網卡與 IP
systemctl --failed                       # 失敗的服務
timedatectl                              # 時區、NTP 同步狀態
[ -d /sys/firmware/efi ] && echo UEFI || echo BIOS
mokutil --sb-state 2>/dev/null           # Secure Boot 狀態

# 🟠 Ubuntu 專屬
lsb_release -a
sudo ufw status
sudo aa-status | head -3
apt list --upgradable

# 🔵 Fedora 專屬
cat /etc/fedora-release
sudo firewall-cmd --state && sudo firewall-cmd --list-all
getenforce
dnf check-upgrade
```

**注意**：`systemctl --failed` 應為空、`timedatectl` 要看到 `System clock synchronized: yes`、`lsblk -f` 對照 §2 規劃確認 LVM 與 `/var` 確實獨立；任何一項不符就趁還沒上線時處理。

---

## 9. 本章差異總結

本章兩個發行版的差異幾乎都可以回到同一個根源：Ubuntu 的安裝程式與預設值圍繞「LTS 伺服器 + 雲端」設計（Subiquity 與 cloud-init 共用格式、預設 DHCP 啟用、Multipass），而 Fedora 承接 RHEL 的工具鏈與安全預設（Anaconda / Kickstart、xfs + LVM、Cockpit、網卡預設關閉、SELinux enforcing）。下表把本章出現過的差異集中列出，方便交接與排錯時快速對照。

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 安裝程式 | Subiquity（Server）/ Flutter 安裝程式（Desktop） | Anaconda（Server：傳統 GTK；Workstation：Web UI） |
| 預設檔案系統 | ext4（Server 可勾 LVM） | Server：xfs + LVM；Workstation：Btrfs |
| root 帳號 | 鎖定，第一位使用者在 `sudo` 群組 | 鎖定，管理員在 `wheel` 群組 |
| LVM 預設分配 | 只給 `/` 約一半 VG，需手動擴充 | `/` 最多 70 GB，其餘給 `/home` |
| 無人值守 | autoinstall（YAML） | Kickstart（.ks） |
| 網頁管理 | 無（可裝 Cockpit：`apt install cockpit`） | Server 預設有 Cockpit :9090 |
| swap | `/swap.img` 交換檔 | zram（Workstation）；Server 可建 swap LV |
| 安裝時網路 | 預設 DHCP 啟用 | **網卡預設關閉**，需手動開啟 |
| 官方 VM 工具 | Multipass | 無（用 virt-manager / Boxes） |
