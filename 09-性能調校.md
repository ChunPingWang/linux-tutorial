# 09 - 性能調校

**本章解決什麼問題**：機器「感覺很慢」但不知道慢在哪，是運維最常見的求助；沒有量測就改 sysctl、換排程器、開 HugePages，結果往往是把一個看不見的瓶頸換成另一個，或在不相干的地方引入穩定性風險（overcommit、nobarrier、mitigations=off 都是先例）。本章的取捨原則是：**先量測、再調整、再量測**，沒有數據的調校只是迷信。先介紹觀測工具（兩者共通），再依 CPU / 記憶體 / 儲存 I/O / 網路 / 應用層分述，最後是 tuned 與 cgroup。發行版差異主要在套件名稱與 **tuned 的預設狀態**。

---

## 1. 觀測工具總覽（USE / RED 方法）

**名詞與關係**：這一節的名詞分兩類：一類是「怎麼問問題」的方法論（USE / RED），一類是回答問題時要看的核心指標（load average、run queue、%wa/%st、si/so、await 等）。方法論決定你看哪個指標，指標再對應到表中的指令：

```
USE 方法（看資源）：對每一種資源問 Utilization（使用率）/ Saturation（飽和）/ Errors（錯誤）
   CPU ──── 使用率 %us+%sy ──── 飽和 run queue（vmstat r）、load average ──── 錯誤 MCE
   記憶體 ── 使用率 used / available ── 飽和 swap 進出 si/so → 最後 OOM ──── 錯誤 dmesg oom
   磁碟 ─── 使用率 %util ──────── 飽和 aqu-sz、await（%wa 是 CPU 在等它）── 錯誤 SMART、I/O error
   網路 ─── 使用率 rxkB/s txkB/s ── 飽和 retrans、drops ─────────────── 錯誤 errors、dropped
RED 方法（看服務）：對每一個服務問 Rate（請求率）/ Errors（錯誤率）/ Duration（延遲）
   → 數據來自應用或 Prometheus 指標（12 章），本章只處理 USE 那一半
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **USE 方法** | Brendan Gregg 提出的資源檢查法：對每一種硬體資源都問使用率（Utilization）、飽和度（Saturation）、錯誤（Errors）三個問題 | 下表的三個欄位就是 U / S / E；適合找「機器」的瓶頸 |
| **RED 方法** | 針對服務的檢查法：看請求速率（Rate）、錯誤率（Errors）、延遲（Duration） | 與 USE 互補：USE 看資源、RED 看服務；RED 的數據來自應用層監控，不在本章 |
| **使用率 / 飽和 / 錯誤** | 使用率＝資源忙碌時間的比例；飽和＝排隊等資源的工作量；錯誤＝資源回報的失敗次數 | 使用率 100% 未必是問題，飽和（有東西在排隊）才是；錯誤最常被忽略卻最該先看 |
| **load average** | `uptime` 顯示的 1 / 5 / 15 分鐘平均「可執行 + 不可中斷等待（D 狀態）」程序數 | 是 CPU 飽和與磁碟飽和的總和：load 高但 CPU 閒，代表在等 I/O（§4） |
| **run queue** | 等待 CPU 排程的程序佇列，`vmstat` 的 `r` 欄就是它的長度 | load average 的 CPU 那一半；`r` 持續大於核心數即 CPU 飽和 |
| **%wa / %st** | `vmstat` / `top` 的 CPU 時間分類：`wa`（iowait）是 CPU 閒著等磁碟 I/O 的比例，`st`（steal）是被 hypervisor 拿走的比例 | `wa` 高指向磁碟飽和；`st` 高代表 VM 宿主超賣，在客體內調什麼都沒用 |
| **si / so** | `vmstat` 的 swap in / swap out：每秒從 swap 換入 / 換出的量 | 記憶體飽和的訊號；持續非零通常先於 OOM 出現（§3） |
| **await / aqu-sz / %util** | `iostat -x` 的三個磁碟指標：平均每次 I/O 等待毫秒數、平均佇列長度、裝置忙碌時間比例 | 磁碟的 U（`%util`）與 S（`await`、`aqu-sz`）；`%wa` 高時就來看這三個 |
| **MCE** | Machine Check Exception，CPU 回報的硬體錯誤（記憶體位元錯誤、匯流排錯誤），記在 `dmesg` | CPU 那一列的 E 欄；硬體診斷見 12 章 §9 |
| **OOM** | Out Of Memory：記憶體與 swap 用盡時，核心的 OOM killer 挑一個程序殺掉自救 | 記憶體飽和的終點；`si/so` 持續上升是它的前兆 |
| **sysstat / sar** | 提供 `sar`、`iostat`、`mpstat`、`pidstat` 的套件；`sar` 靠背景收集器每 10 分鐘存一份歷史 | 表中多數指令來自它；沒有事先啟用就沒有「出事前」的對照數據 |

**為什麼**：調校的第一步是找到「哪個資源」在瓶頸，而不是憑感覺。USE 方法（Utilization / Saturation / Errors）強迫你對每一種資源都問三個問題，避免只盯著 CPU 使用率卻漏掉磁碟佇列或網卡丟包；下表就是把這三個問題對應到實際指令。

**怎麼做**：依資源逐列檢查，任一欄異常再往該章節深入。

| 資源 | 使用率（Utilization） | 飽和（Saturation） | 錯誤（Errors） |
|---|---|---|---|
| CPU | `top`、`mpstat -P ALL 1`、`sar -u` | run queue：`vmstat 1` 的 `r`、load average | `dmesg`（MCE） |
| 記憶體 | `free -h`、`vmstat 1`、`sar -r` | swap：`vmstat` 的 `si/so`、`sar -B`、OOM | `dmesg -T \| grep -i oom` |
| 磁碟 | `iostat -xz 1` 的 `%util`、`sar -d` | `iostat` 的 `aqu-sz`、`await` | `smartctl`、`dmesg` I/O error |
| 網路 | `sar -n DEV 1`、`ip -s link`、`nload` | `ss -s`、`netstat -s` 的 retrans / drops | `ip -s link` 的 errors / dropped |
| 檔案系統 | `df -h`、`df -i` | — | `dmesg` fs 錯誤 |

上表多數指令來自 sysstat，且 `sar` 需要背景收集器才有歷史數據可查（出事後才裝就沒有「出事前」的對照）。安裝：

```bash
sudo apt install -y sysstat htop iotop atop nload iftop bpfcc-tools bpftrace linux-tools-generic numactl stress-ng   # 🟠（perf 在 linux-tools-generic）
sudo dnf install -y sysstat htop atop nload iftop bcc-tools bpftrace perf kernel-tools numactl stress-ng            # 🔵（iotop 未在 Fedora 44 套件庫，改用 iotop-c 或 bcc 的 biotop）
sudo systemctl enable --now sysstat        # 啟用 sar 每 10 分鐘收集（🟠 另需 /etc/default/sysstat 設 ENABLED="true"）
```

接到「機器很慢」的求助時，先跑下面這組 60 秒檢查再談其他：它依「load → 錯誤 → CPU → 記憶體 → 磁碟 → 網路」順序在一分鐘內排除掉大部分方向，比直接開 `top` 猜有效得多。

```bash
# 快速健康檢查（60 秒版，Netflix 方法）
uptime                      # load average（1/5/15 分）；> CPU 核數即飽和
dmesg -T | tail             # 硬體 / OOM / I/O 錯誤
vmstat 1 5                  # r（runnable）、b（blocked）、si/so（swap）、us/sy/id/wa/st
mpstat -P ALL 1 3           # 單核心是否打滿（單執行緒瓶頸）
pidstat 1 3                 # 每程序 CPU
iostat -xz 1 3              # 磁碟 r/s w/s await %util
free -m                     # available 才是真正可用
sar -n DEV 1 3              # 網卡吞吐 rxkB/s txkB/s
sar -n TCP,ETCP 1 3         # 連線速率、重傳（retrans/s）
top -o %CPU                 # 誰在吃
```

### 1.1 進階：perf 與 eBPF

**名詞與關係**：這些工具都在回答「程序到底在做什麼」，差別在觀測位置（CPU 硬體計數器 vs 核心事件）與開銷：

```
                  ┌─ perf ──── 讀 CPU 硬體計數器（PMU）定期取樣 ──▶ 哪個函式最熱（on-CPU）──▶ flame graph 視覺化
使用者程序 / 核心 ──┤
                  └─ eBPF ──── 在核心事件（tracepoint / kprobe）掛探針 ──▶ 延遲分布、off-CPU、誰開了什麼檔
                          ├─ BCC：預先寫好的工具集（biolatency、runqlat、cachestat…）
                          └─ bpftrace：一行式腳本語言，臨時追蹤用
strace：用 ptrace 攔截每一個系統呼叫，目標程序被拖慢數十倍 → 被 perf trace / eBPF 取代
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **perf** | 核心內建 perf_events 子系統的命令列工具，用硬體計數器與取樣找出 CPU 熱點 | 套件 🟠 `linux-tools-*` / 🔵 `perf`；結果可畫成 flame graph；`perf trace` 是 strace 的低開銷替代 |
| **硬體計數器（PMU）** | CPU 內建的效能監控單元，能計數 cycles、instructions、cache-misses 等事件 | `perf stat` / `perf record` 讀的就是它；VM 內常被 hypervisor 關閉，只剩軟體事件可用 |
| **on-CPU / off-CPU** | on-CPU 是程序真正在用 CPU 的時間；off-CPU 是它在等（鎖、I/O、排程）的時間 | perf 擅長 on-CPU；off-CPU 要靠 eBPF 的 `offcputime`——「CPU 不高卻很慢」的原因通常在 off-CPU |
| **flame graph** | 把取樣到的呼叫堆疊畫成「寬度＝佔用時間比例」的火焰圖 | 由 `perf script` 輸出經 FlameGraph 腳本產生；是閱讀 perf 結果最快的方式 |
| **eBPF** | 核心內建的沙箱虛擬機，可安全載入小程式掛在核心事件上執行，不必改核心或重開機 | BCC 與 bpftrace 都是它的前端；開銷比 strace 低幾個數量級 |
| **tracepoint / kprobe** | eBPF 程式的兩種掛載點：tracepoint 是核心預先埋好的穩定事件，kprobe 可掛在任意核心函式入口 | bpftrace 一行式裡的 `tracepoint:syscalls:...` 與 `kprobe:vfs_read` 就是這兩種 |
| **BCC** | BPF Compiler Collection：一組現成的 eBPF 觀測工具（Python 封裝） | 🟠 `bpfcc-tools`（指令帶 `-bpfcc` 後綴）、🔵 `bcc-tools`（放在 `/usr/share/bcc/tools/`） |
| **bpftrace** | 類 awk 語法的 eBPF 追蹤語言，適合臨時寫一行程式 | 與 BCC 同底層；BCC 是「現成工具」，bpftrace 是「自己寫」 |
| **strace** | 用 ptrace 攔截程序每個系統呼叫的傳統工具 | 每次系統呼叫都要切到 strace 再回來，目標慢數十倍；只適合短暫除錯 |
| **page cache** | 核心用閒置記憶體快取磁碟內容的機制 | `cachestat` 看命中率；命中率低代表讀取真的在打磁碟，詳見 §3 |

**為什麼**：`top` 只能告訴你「哪個程序」在吃 CPU，答不出「程序裡哪個函式」或「它在等什麼」；perf 用硬體計數器取樣熱點，BCC / bpftrace 則在核心事件上掛探針，能看到磁碟延遲分布、排程延遲、off-CPU 等傳統工具完全看不到的面向，而且開銷遠低於 `strace`（strace 會讓目標慢數十倍）。

**怎麼做**：perf 找 on-CPU 熱點，BCC 工具看 I/O / 網路 / 排程，bpftrace 做臨時的一行式追蹤。

```bash
# perf（🟠 linux-tools-$(uname -r)；🔵 perf）
sudo perf top                                # 即時熱點
sudo perf record -g -p $(pgrep -o nginx) -- sleep 30 && sudo perf report
sudo perf stat -e cycles,instructions,cache-misses -p PID sleep 10
sudo perf trace -p PID                       # 系統呼叫追蹤（比 strace 輕）
# Flame graph：git clone https://github.com/brendangregg/FlameGraph; perf script | stackcollapse-perf.pl | flamegraph.pl > out.svg

# BCC 工具（🟠 bpfcc-tools 指令有 -bpfcc 後綴；🔵 bcc-tools 在 /usr/share/bcc/tools/）
sudo execsnoop-bpfcc                          # 🟠 誰在 exec 新程序   /  🔵 sudo /usr/share/bcc/tools/execsnoop
sudo opensnoop-bpfcc                          # 誰在開檔
sudo biolatency-bpfcc                         # 磁碟延遲直方圖
sudo biotop-bpfcc                             # 依程序看磁碟 I/O
sudo tcplife-bpfcc; sudo tcpretrans-bpfcc     # TCP 連線 / 重傳
sudo runqlat-bpfcc                            # CPU 排程延遲
sudo cachestat-bpfcc                          # page cache 命中率
sudo offcputime-bpfcc -p PID 10               # off-CPU 分析（等鎖 / 等 I/O）
# bpftrace 一行程式
sudo bpftrace -e 'tracepoint:syscalls:sys_enter_openat { printf("%s %s\n", comm, str(args->filename)); }'
sudo bpftrace -e 'kprobe:vfs_read { @bytes = hist(arg2); }'
```

**注意**：🟠 Ubuntu 的 perf 綁核心版本，升級核心後要確認 `linux-tools-$(uname -r)` 有跟著裝；🔵 Fedora 的 BCC 工具不在 PATH，要用完整路徑 `/usr/share/bcc/tools/`。

---

## 2. CPU

**名詞與關係**：CPU 調校牽涉四個彼此獨立的層面——頻率（governor / C-state）、中斷分布（IRQ / irqbalance）、記憶體位置（NUMA）、安全緩解（mitigations）——任一層出問題，都會讓「核心數夠」的機器變慢：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **governor（cpufreq）** | 核心 cpufreq 子系統的頻率調速策略：`performance` 鎖最高頻、`powersave` 偏省電、`schedutil` 依排程器負載動態調整 | 用 `cpupower` 或 sysfs 設定；tuned 的 profile 也會設它（§7） |
| **cpupower** | 操作 cpufreq 的命令列工具（🟠 在 `linux-tools-generic`、🔵 `kernel-tools`） | 查看與切換 governor；設定不持久，重開機後要靠 tuned 或服務重設 |
| **C-state** | CPU 閒置時的省電深度（C0 執行中、C1～C6 越深越省電、喚醒越慢） | 與 governor 互補：governor 管「忙時多快」，C-state 管「閒時睡多深」；tuned `latency-performance` 會限制 C-state 以壓低喚醒延遲 |
| **mitigations** | 核心對 Spectre / Meltdown 等 CPU 推測執行漏洞的軟體緩解措施 | 狀態在 `/sys/devices/system/cpu/vulnerabilities/`；`mitigations=off` 換回 5～30% 效能，代價是重新打開漏洞 |
| **CPU 親和性（affinity）** | 把程序限制在指定核心上執行 | `taskset` 臨時設、systemd `AllowedCPUs` 永久設；與 `isolcpus` 搭配才有意義 |
| **isolcpus / nohz_full / rcu_nocbs** | 三個核心參數，把指定核心從一般排程、週期性時鐘中斷、RCU 回呼中隔離出來 | 隔離後的核心只跑你用親和性綁上去的程序；即時 / 低延遲負載才需要 |
| **IRQ** | 硬體中斷請求：網卡、磁碟每收到資料就發一次，由某顆 CPU 負責處理 | `/proc/interrupts` 看分布；全落在 CPU0 會讓那顆核心成為瓶頸 |
| **irqbalance** | 自動把 IRQ 分散到各核心的 daemon（兩者預設啟用） | 一般情況交給它；需要精確控制時停掉它，手動寫 `smp_affinity_list` |
| **NUMA** | Non-Uniform Memory Access：多路伺服器每顆 CPU 有自己「近」的記憶體，存取別顆 CPU 的記憶體延遲多 30～50% | `numactl` 把程序與記憶體綁在同一 node；單路機器只有一個 node，不必處理 |
| **numactl / numastat** | 查看 NUMA 拓撲與各程序跨 node 存取量、把程序綁到 node 的工具 | 套件 `numactl`（兩者同名） |
| **chrt / nice** | `chrt` 設即時排程策略（FIFO / RR），`nice` 調一般程序的優先權 | 與親和性無關：親和性管「在哪顆核心」，這兩個管「誰先跑」 |

**為什麼**：伺服器 CPU 常見的效能損失不在「核心數不夠」，而在頻率調速器停在 `powersave`（延遲敏感服務的 p99 因此飄高）、IRQ 全落在 CPU0、以及多路機器的程序跨 NUMA node 存取記憶體（延遲多 30~50%）。另一方面，Spectre / Meltdown 緩解措施會吃掉 5~30% 效能，但關掉就是安全風險，不能只看數字。

**怎麼做**：先看 governor 與 vulnerabilities 狀態，再視工作負載決定是否綁核心、隔離核心或綁 NUMA。

```bash
lscpu; nproc; cat /proc/cpuinfo | grep MHz
cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor        # performance / powersave / schedutil
# 頻率調速器
sudo apt install -y linux-tools-generic   # 🟠 cpupower 在此    /  🔵 kernel-tools
sudo cpupower frequency-info
sudo cpupower frequency-set -g performance                        # 伺服器 / 延遲敏感
# 永久：🟠 /etc/default/cpupower（若裝 cpupower 服務）或 tuned；🔵 tuned 的 throughput-performance 會設
cat /sys/devices/system/cpu/vulnerabilities/*                     # 安全緩解措施（mitigations）
# 若在可信環境追求極致效能：核心參數 mitigations=off（⚠️ 有安全風險；可提升 5~30%）

# CPU 親和性 / 隔離
taskset -cp 2-3 PID                                               # 綁定核心
sudo systemctl set-property myapp.service AllowedCPUs=4-7          # systemd cgroup 綁定
# 核心參數 isolcpus=4-7 nohz_full=4-7 rcu_nocbs=4-7（即時 / 低延遲工作負載）
# IRQ 平衡
systemctl status irqbalance                                        # 兩者預設啟用
cat /proc/interrupts | head
echo 2 | sudo tee /proc/irq/45/smp_affinity_list                   # 手動綁 IRQ
# NUMA（多路伺服器）
numactl --hardware; numastat -p PID
numactl --cpunodebind=0 --membind=0 ./app                          # 綁 NUMA node
```

**注意**：`isolcpus` 隔離的核心不再參與一般排程，寫錯範圍會讓其他服務擠在剩下的核心上；`mitigations=off` 只適合不跑不可信程式碼的封閉環境。排程器：`chrt -f -p 50 PID`（即時 FIFO；⚠️ 可能餓死其他程序）、`nice`/`renice`。

---

## 3. 記憶體

**名詞與關係**：記憶體問題的關鍵是分清楚「哪一種頁」：page cache 可以隨時丟掉，匿名頁只能換出到 swap，髒頁必須先寫回磁碟。`vm.*` 參數就是在調這三種頁的比例與回收時機：

```
實體記憶體
 ├─ page cache（檔案內容快取）──┬─ 乾淨頁：可直接丟 ──── vfs_cache_pressure 管其中 dentry / inode 快取的回收積極度
 │                              └─ 髒頁（Dirty）：必須先回寫 ── dirty_ratio / dirty_background_ratio 管回寫時機
 ├─ 匿名頁（AnonPages：程序 heap / stack）── 沒有檔案可對應，只能換出到 swap ── swappiness 管積極度
 ├─ slab（核心物件：dentry / inode / socket…）── slabtop 觀察
 └─ HugePages（靜態預留、不可換出）── nr_hugepages；THP 則是核心在背景自動合併出來的大頁
記憶體不足時的順序：先丟乾淨 page cache → 回寫髒頁 → 換出匿名頁（si/so）→ 都不夠 → OOM killer
overcommit_memory 是另一條獨立的軸：決定「配置時」要不要先檢查夠不夠
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **page cache** | 核心用閒置記憶體快取檔案內容，`free` 的 buff/cache 欄 | 可回收，所以 `available` 才是真實可用；`drop_caches` 清的就是它 |
| **匿名頁（anonymous pages）** | 程序的 heap / stack 等不對應任何檔案的記憶體（`/proc/meminfo` 的 AnonPages） | 記憶體不足時只能換出到 swap；`si/so` 就是它在進出 |
| **髒頁（dirty pages）** | page cache 裡已被修改、尚未寫回磁碟的頁 | 由 `dirty_ratio`（上限）與 `dirty_background_ratio`（開始背景回寫）控制；積太多會造成回寫時的 I/O 風暴 |
| **swap / swappiness** | swap 是匿名頁的溢出空間（磁碟交換檔或 🔵 zram）；`swappiness`（0～200）決定核心多偏好換出匿名頁而非丟 page cache | 伺服器降低以保護熱資料；zram 因為在記憶體內壓縮，反而可以留高 |
| **OOM killer** | 記憶體與 swap 都用盡時，核心依 `oom_score` 挑一個程序殺掉 | 是回收機制失敗的最後手段；cgroup `MemoryMax`（§8）可把它限制在單一服務內 |
| **overcommit** | 核心允許程序「承諾」的虛擬記憶體總量超過實體＋swap | `overcommit_memory` 0 啟發式 / 1 永遠允許 / 2 嚴格；Redis fork 需要 1，否則做 RDB 時被拒絕配置 |
| **RSS / PSS / USS** | 程序記憶體的三種算法：RSS 把共用函式庫重複計入、PSS 把共用部分按比例分攤、USS 只算獨佔 | `ps` 的 RSS 加總會高估；`smem` / `smaps_rollup` 提供 PSS 才能正確加總 |
| **slab** | 核心自己的物件快取（dentry、inode、網路 buffer 等） | `slabtop` 觀察；`vfs_cache_pressure` 調其中 dentry / inode 的回收積極度 |
| **THP（Transparent Huge Pages）** | 核心在背景自動把連續的 4 KB 頁合併成 2 MB 大頁 | 與靜態 HugePages 用途相對：THP 自動但有延遲尖峰，資料庫要求關（`never`）；`madvise` 讓應用自己選 |
| **HugePages（靜態）** | 用 `nr_hugepages` 預留的 2 MB 大頁，不可換出、需應用明確申請 | PostgreSQL `huge_pages=on`、JVM `-XX:+UseLargePages`、DPDK 使用；預留後一般程序拿不到這塊記憶體 |
| **max_map_count** | 單一程序可擁有的記憶體映射區段（mmap）數上限 | Elasticsearch 這類大量 mmap 的應用不夠就拒絕啟動 |
| **min_free_kbytes** | 核心保留給自己緊急使用（如網路封包配置）的最低空閒記憶體 | 網路密集或連線數上萬時要提高，見 §5 注意 |

**為什麼**：Linux 會把閒置記憶體全拿去做 page cache，所以 `free` 的「used」高不代表缺記憶體，看錯指標就會誤判；真正的記憶體問題是 swap 進出（`si/so`）、髒頁積太多導致回寫時 I/O 風暴、以及 OOM killer 殺錯程序。這些行為都由 `vm.*` 參數決定，預設值偏向桌面 / 通用，資料庫與大記憶體伺服器需要調整。

**怎麼做**：先用下面指令確認 `available`、`Dirty`、slab 與各程序 RSS / PSS，確定問題在哪一層。

```bash
free -h                                  # buff/cache 可回收；available 為真實可用
cat /proc/meminfo | grep -E "MemAvailable|Dirty|Writeback|AnonPages|Shmem|Slab"
vmstat -s | head -20
sudo slabtop -o | head                   # 核心 slab（dentry / inode 快取）
ps -eo pid,rss,vsz,comm --sort=-rss | head
sudo smem -tk 2>/dev/null                # PSS / USS 精確計算（apt/dnf install smem）
cat /proc/PID/smaps_rollup
```

參數說明：`swappiness` 決定核心多積極把匿名頁換出（伺服器降低以免熱資料被換走）；`dirty_ratio` 太高會在大記憶體機器上累積數 GB 髒頁，一次回寫就讓磁碟 `await` 飆高；`overcommit_memory=1` 是 Redis fork 做 RDB 時不被拒絕配置的前提；`max_map_count` 不夠 Elasticsearch 直接拒絕啟動。`/etc/sysctl.d/90-memory.conf`：

```ini
vm.swappiness = 10                       # 伺服器 10；資料庫 1；桌面 60（🔵 zram 環境保持預設 60 甚至更高）
vm.vfs_cache_pressure = 50               # 保留更多 dentry/inode 快取（檔案伺服器）
vm.dirty_ratio = 10                      # 髒頁佔 RAM 上限（% ）；預設 20；大記憶體機器降低避免 I/O 風暴
vm.dirty_background_ratio = 5            # 背景開始回寫
vm.dirty_expire_centisecs = 3000
vm.min_free_kbytes = 262144              # 保留給核心的緊急記憶體（網路密集時提高）
vm.overcommit_memory = 0                 # 0 啟發式；1 永遠允許（Redis 建議）；2 嚴格
vm.max_map_count = 262144                # Elasticsearch 需要（🟠 24.04 預設已 1048576，實測 10-map-count.conf）
vm.nr_hugepages = 1024                   # 靜態 HugePages（資料庫 / DPDK / JVM）
```

Transparent Huge Pages 會在背景把小頁合併成 2 MB 大頁，對記憶體存取模式零散的資料庫（MongoDB、Redis）反而造成延遲尖峰與記憶體膨脹，所以這類軟體都要求關掉；一般負載保持 `madvise` 讓應用自己選擇即可。靜態 HugePages 則是給 PostgreSQL / JVM 明確配置、不會被換出的大頁。

```bash
# Transparent Huge Pages（實測兩者預設 madvise）
cat /sys/kernel/mm/transparent_hugepage/enabled            # [madvise]
# 資料庫（MongoDB、Redis、Oracle）建議 never；一般保持 madvise：
echo never | sudo tee /sys/kernel/mm/transparent_hugepage/enabled
# 永久：核心參數 transparent_hugepage=never，或 tuned 設定檔 [vm] transparent_hugepages=never
# 靜態 HugePages 給 PostgreSQL：huge_pages = on + vm.nr_hugepages；驗證 grep Huge /proc/meminfo
# 清 page cache（只在測試效能時）
sync && echo 3 | sudo tee /proc/sys/vm/drop_caches
```

**注意**：`drop_caches` 會讓所有快取重新暖機，正式環境跑了只會變慢，不會「釋放記憶體」解決任何問題。swap 策略：見 [06 檔案系統與儲存 §7](06-檔案系統與儲存.md)。有 zram 的 Fedora 通常不需要磁碟 swap；記憶體密集伺服器建議「小 swap + swappiness 10」當 OOM 緩衝。

---

## 4. 儲存 I/O

**名詞與關係**：一次 I/O 從應用到磁碟要經過檔案系統、page cache、區塊層佇列、排程器，最後才到裝置；每一層有自己的調校點，選錯層下手就沒效果：

```
應用 ──▶ 檔案系統（noatime、barrier、data=、nodatacow）──▶ page cache（readahead 決定預讀多少）
     ──▶ 區塊層 blk-mq 佇列（nr_requests = queue depth）──▶ I/O 排程器（none / mq-deadline / bfq 決定順序）──▶ 裝置
                                                                                                    │
                 iostat 的 await（在佇列＋裝置的總等待）、aqu-sz（佇列長度）、%util（裝置忙碌）量的就是這一段 ─┘
程序處於 D 狀態（不可中斷睡眠）＝正在這條路上等；會被算進 load average
SSD 另有 TRIM（告知裝置哪些區塊已釋放）：fstrim.timer 定期批次做，discard 掛載選項則每次刪除即時做
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **I/O 排程器（scheduler）** | 區塊層決定「佇列中的請求以什麼順序送給裝置」的演算法，每個裝置各自設定 | `none` 不排序（NVMe 自己有多佇列）、`mq-deadline` 保證最長等待時間（SSD / 伺服器 HDD）、`bfq` 依程序公平分配（桌面 HDD） |
| **blk-mq / nr_requests（queue depth）** | 核心的多佇列區塊層；`nr_requests` 是每個佇列可暫存的請求數 | 佇列越深越能合併與排序，但單一請求延遲可能變長；`aqu-sz` 是實際佔用的深度 |
| **await / %util / aqu-sz** | `iostat -x` 的每次 I/O 平均延遲（ms）、裝置忙碌比例、平均佇列長度 | `await` > 10 ms 就算慢碟（NVMe 應 < 1 ms）；三者一起看才分得清「裝置慢」還是「排太多」 |
| **D 狀態** | 程序等待 I/O 完成時的不可中斷睡眠狀態（`ps` 的 STAT 欄 `D`） | 大量 D 狀態＝I/O 在排隊；被算進 load average，所以「load 高 CPU 閒」 |
| **readahead** | 核心偵測到循序讀時預先多讀進 page cache 的量（`read_ahead_kb`） | 大檔循序讀調大有效；隨機讀調大反而浪費頻寬與快取空間 |
| **udev 規則** | 裝置出現時自動執行的規則檔（`/etc/udev/rules.d/`） | 排程器與 readahead 直接寫 sysfs 重開機就沒了，用 udev 規則才是永久設定 |
| **noatime / relatime** | 掛載選項：不更新 / 只在必要時更新檔案的存取時間（atime） | 每次讀檔都寫 atime 會產生額外寫入；`relatime` 是兩者間的預設折衷 |
| **barrier** | 檔案系統為保證日誌順序而下的「寫入屏障」（強制 flush） | `nobarrier` 拿掉它換效能，只有在有 BBU（電池備援快取）的 RAID 卡上才安全 |
| **data=writeback / nodatacow** | ext4 的 `data=writeback` 讓資料不經日誌只記中繼資料；Btrfs 的 `nodatacow` 關掉寫入時複製 | 都是用「斷電後一致性」換效能；`nodatacow` 同時關掉該檔的校驗與壓縮 |
| **TRIM / discard / fstrim** | TRIM 是告訴 SSD「這些區塊已釋放」的指令；`discard` 掛載選項每次刪檔即時發、`fstrim.timer` 每週批次發 | 批次比即時對效能友善，所以兩者預設用 timer 而非 `discard` |
| **fio** | 可指定讀寫比例、區塊大小、佇列深度的 I/O 基準工具 | 調校前後用同一組 fio 工作負載比對才知道有沒有效；見 06 章 §10 |

**為什麼**：磁碟通常是伺服器最慢的元件，`load` 高但 CPU 閒、程序卡在 D 狀態，多半是 I/O 在排隊。I/O 排程器選錯（NVMe 用 bfq 白白多一層排序、HDD 用 none 讓磁頭亂跳）、read-ahead 不符存取模式，都會讓同一顆碟的延遲差好幾倍。

**怎麼做**：先用 `iostat` 確認 `await` 與 `%util`，再依裝置類型設排程器，永久化用 udev 規則（開機時自動套用，不依賴 rc.local）。

```bash
iostat -xz 1                             # await（ms）> 10 慢碟、%util 接近 100 飽和、aqu-sz 佇列
sudo iotop -oPa                          # 🟠；🔵 用 biotop（bcc）
cat /sys/block/sda/queue/scheduler        # 實測 🟠 [bfq]（SATA）；NVMe 通常 [none]
# 排程器選擇：NVMe → none；SSD → mq-deadline / none；HDD → bfq（桌面）/ mq-deadline（伺服器）
echo mq-deadline | sudo tee /sys/block/sda/queue/scheduler
# 永久（udev 規則，兩者共通）：
sudo tee /etc/udev/rules.d/60-ioscheduler.rules <<'EOF'
ACTION=="add|change", KERNEL=="sd[a-z]*", ATTR{queue/rotational}=="0", ATTR{queue/scheduler}="mq-deadline"
ACTION=="add|change", KERNEL=="sd[a-z]*", ATTR{queue/rotational}=="1", ATTR{queue/scheduler}="bfq"
ACTION=="add|change", KERNEL=="nvme[0-9]*", ATTR{queue/scheduler}="none"
EOF
cat /sys/block/sda/queue/read_ahead_kb    # 循序讀（大檔案）可調大到 1024~4096
sudo blockdev --setra 2048 /dev/sda
cat /sys/block/sda/queue/nr_requests      # 佇列深度
```

檔案系統層的取捨：多數選項是在「效能」與「斷電後資料一致性」之間換，`nobarrier`、`data=writeback`、`nodatacow` 都會拿掉某種保護，只能在清楚後果的情況下用。

檔案系統層：
- 掛載 `noatime`（或 `relatime`，預設）；`nobarrier` ⚠️ 只在有 BBU 的 RAID 卡上。
- XFS 大量小檔：`mkfs.xfs -i size=512`；大檔：預設即可。`xfs_io -c "extsize 1m"` 給資料庫。
- ext4：`mkfs.ext4 -E lazy_itable_init=0,lazy_journal_init=0`（避免初次使用背景初始化）；`data=writeback` ⚠️ 犧牲一致性。
- Btrfs：`nodatacow` 給資料庫 / VM 映像；`compress=zstd:1` 對讀多寫少有幫助。
- SSD：確認 `fstrim.timer` 啟用（兩者預設每週）；不要用 `discard` 掛載選項（即時 TRIM 影響效能）。
- LVM：`lvcreate --type raid0 -i 2` 條帶化多顆碟；`lvchange --readahead`。
- 資料庫：資料、WAL / redo log、暫存分開放不同 LV / 磁碟。

**注意**：改排程器或 read-ahead 前後都要用 fio 量同一組工作負載，否則不知道是變好還是變差。fio 基準測試見 [06 章 §10](06-檔案系統與儲存.md)。

---

## 5. 網路

**名詞與關係**：一個封包從網卡進到應用，要依序經過 ring buffer → softirq → 協定堆疊 → socket 緩衝 → accept 佇列，每一段都有自己的容量參數；「頻寬沒滿卻丟包」就是某一段的容量先滿了：

```
網卡 ──DMA──▶ ring buffer（ethtool -G）──IRQ / 中斷合併（ethtool -C）──▶ softirq 處理（netdev_max_backlog 排隊；softnet_stat 第 2 欄＝溢出）
   ──▶ 協定堆疊：conntrack（nf_conntrack_max）、TCP 壅塞控制（BBR / CUBIC）、qdisc（fq / fq_codel 管送出順序）
   ──▶ socket 緩衝（tcp_rmem / tcp_wmem，上限 rmem_max / wmem_max）
   ──▶ SYN backlog（tcp_max_syn_backlog：握手進行中）──▶ accept 佇列（somaxconn：握手完成、等應用 accept）──▶ 應用
出站連線另受 ephemeral port（ip_local_port_range）與 TIME_WAIT（tcp_tw_reuse / tcp_fin_timeout）限制
多佇列網卡把 ring buffer 與 IRQ 分到多顆 CPU；單佇列虛擬網卡用 RPS / RFS 以軟體方式分散
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **ring buffer** | 網卡與核心之間的環狀 DMA 緩衝區，網卡收到封包先放這裡等核心取走 | 太小則 softirq 來不及取時直接丟包（`ethtool -S` 的 rx_missed / drop）；`ethtool -G` 加大 |
| **softirq / netdev_max_backlog** | softirq 是核心處理網卡中斷後半段（拆封包、交協定堆疊）的機制；`netdev_max_backlog` 是每顆 CPU 待處理封包的佇列上限 | `/proc/net/softnet_stat` 第 2 欄非 0 代表佇列溢出，要加大 backlog 或分散到多核心 |
| **中斷合併（coalescing）** | 網卡累積多個封包才發一次中斷，用一點延遲換 CPU | `ethtool -C`；`adaptive-rx` 讓網卡依流量自動調整 |
| **offload（TSO / GRO / LRO）** | 把 TCP 分段 / 合併的工作交給網卡硬體做 | 實體機省 CPU；虛擬化環境有時造成封包異常，需關閉 |
| **TCP buffer（tcp_rmem / tcp_wmem）** | 每條 TCP 連線的接收 / 傳送緩衝（最小 / 預設 / 最大三個值），上限另受 `rmem_max` / `wmem_max` 限制 | 緩衝 ≥ 頻寬 × RTT 才跑得滿長距離 10 Gbps；調大會讓每條連線吃更多核心記憶體 |
| **SYN backlog / somaxconn（listen backlog）** | 兩個等候佇列：`tcp_max_syn_backlog` 放握手進行中的連線，`somaxconn` 放握手完成、等應用 `accept()` 的連線 | 滿了就靜默丟連線，只有 `nstat` 的 ListenDrops 會漲；應用自己的 backlog 參數（nginx、redis）也要跟著調 |
| **ephemeral port** | 出站連線由核心自動分配的來源埠，範圍由 `ip_local_port_range` 決定 | 反向代理 / 壓測機大量出站時用完會 `EADDRNOTAVAIL`；配 `tcp_tw_reuse` 縮短 TIME_WAIT 佔用 |
| **TIME_WAIT / tcp_tw_reuse** | 連線關閉後主動關閉方保留的狀態（預設 60 秒，埠被佔住）；`tcp_tw_reuse` 允許出站連線重用這類埠 | 只對出站有效；舊的 `tcp_tw_recycle` 已從核心移除 |
| **壅塞控制（CUBIC / BBR）** | TCP 決定送多快的演算法：CUBIC 以丟包為訊號，BBR 以量測頻寬與 RTT 為訊號 | BBR 是核心模組 `tcp_bbr`，需先 `modprobe`；搭配 `fq` qdisc 效果最佳 |
| **qdisc（fq / fq_codel）** | 佇列規則：決定封包從核心送出到網卡的排程；`fq` 依流公平排程，`fq_codel` 再加主動丟包抑制 bufferbloat | `net.core.default_qdisc` 設定；兩者預設 `fq_codel`，BBR 建議 `fq` |
| **conntrack** | netfilter 追蹤每條連線狀態的表，NAT 與狀態防火牆都靠它 | `nf_conntrack_max` 是表的上限，滿了新連線直接被丟；只有載入 conntrack 模組的機器才有這個參數 |
| **MTU / Jumbo frame** | MTU 是單一封包最大位元組數（預設 1500）；Jumbo frame 指設到 9000 | 減少每封包開銷，但鏈路上任一端不同就出現「小封包通、大封包卡」 |
| **多佇列 / RPS / RFS** | 多佇列網卡把封包依流分到多個 ring buffer 與 IRQ；RPS / RFS 是在單佇列網卡上用軟體把處理分散到多核心 | `ethtool -l` 確認佇列數；虛擬網卡常只有單佇列，才需要 RPS / RFS |
| **iperf3** | 兩端各跑一份、量純 TCP / UDP 頻寬的工具 | 排除應用因素後量鏈路極限；`-P` 多流才能跑滿高頻寬 |

**為什麼**：核心的網路預設值是為一般桌面 / 1 Gbps 設計的：TCP 緩衝上限太小跑不滿 10 Gbps 長距離鏈路、`somaxconn` 與 SYN backlog 太小在流量尖峰時直接丟連線（應用日誌看不到，只有 `nstat` 的 ListenDrops 在漲）、ephemeral port 不夠讓反向代理出現 `EADDRNOTAVAIL`。另外網卡 ring buffer 太小會在 softirq 來不及處理時丟包，這是「頻寬明明沒滿卻掉封包」的常見原因。

**怎麼做**：先用下面指令找到是哪一層在丟（網卡計數器、softnet、TCP 重傳），再對症調 sysctl 或 ethtool。

```bash
sar -n DEV 1; sar -n EDEV 1              # 吞吐 / 錯誤
ss -s; ss -tin                           # 連線統計 / 每連線 RTT、cwnd、retrans
nstat -az | grep -E "TcpRetransSegs|TcpExtListenDrops|TcpExtTCPBacklogDrop"
ethtool -S eth0 | grep -iE "drop|err|miss"
ethtool -g eth0; sudo ethtool -G eth0 rx 4096 tx 4096      # Ring buffer 加大（高流量丟包時）
ethtool -k eth0 | grep -E "tcp-segmentation|generic-receive|large-receive"   # offload（虛擬化環境有時要關 GRO/LRO）
ethtool -c eth0; sudo ethtool -C eth0 adaptive-rx on         # 中斷合併
cat /proc/net/softnet_stat                                   # 第 2 欄非 0 = softirq 來不及（加 netdev_max_backlog）
```

下面這組值放大緩衝與佇列、縮短 TIME_WAIT 佔用、並改用 BBR 壅塞控制（在有丟包的鏈路上吞吐明顯優於 CUBIC）。`/etc/sysctl.d/90-network.conf`（10 Gbps+ 或高連線數伺服器）：

```ini
net.core.rmem_max = 134217728
net.core.wmem_max = 134217728
net.ipv4.tcp_rmem = 4096 87380 67108864
net.ipv4.tcp_wmem = 4096 65536 67108864
net.core.netdev_max_backlog = 250000
net.core.somaxconn = 65535                 # listen backlog（nginx / redis 也要各自調）
net.ipv4.tcp_max_syn_backlog = 65535
net.ipv4.ip_local_port_range = 1024 65535  # 反向代理 / 壓測機需要更多 ephemeral port
net.ipv4.tcp_tw_reuse = 1
net.ipv4.tcp_fin_timeout = 15
net.ipv4.tcp_keepalive_time = 300
net.ipv4.tcp_slow_start_after_idle = 0
net.ipv4.tcp_mtu_probing = 1
net.ipv4.tcp_congestion_control = bbr      # 需 modprobe tcp_bbr（兩者核心皆內建）
net.core.default_qdisc = fq                # 搭配 BBR；🟠 24.04 預設已 fq_codel（實測 10-bufferbloat.conf）
net.ipv4.tcp_fastopen = 3
net.netfilter.nf_conntrack_max = 1048576   # NAT / 防火牆主機（需 conntrack 模組已載入）
```

BBR 是核心模組，sysctl 套用前必須先載入，否則 `sysctl --system` 會報找不到演算法；Jumbo frame 則是儲存 / 內網 10G 減少每封包開銷的手段，但鏈路上任何一端沒設就會出現「小封包通、大封包卡」的詭異故障。

```bash
sudo modprobe tcp_bbr && echo tcp_bbr | sudo tee /etc/modules-load.d/bbr.conf
sudo sysctl --system && sysctl net.ipv4.tcp_congestion_control
# MTU / Jumbo frame（儲存網路、內部 10G）：兩端與交換器都要設 9000
#   🟠 netplan: mtu: 9000   /  🔵 nmcli con mod eth1 802-3-ethernet.mtu 9000
# 多佇列網卡：確認 ethtool -l eth0 的 Combined 等於核心數；RPS/RFS 給單佇列虛擬網卡
iperf3 -s   /   iperf3 -c server -P 8 -t 30        # 頻寬測試（兩者套件 iperf3）
```

**注意**：緩衝區調大會讓每條連線吃更多核心記憶體，連線數上萬的機器要一併看 `vm.min_free_kbytes`；`tcp_tw_reuse` 只對出站連線有效，不要再去找已移除的 `tcp_tw_recycle`。

---

## 6. 應用層與檔案描述子

**名詞與關係**：檔案描述子的上限有三層，各自獨立，任一層卡住都會出現 "Too many open files"：

```
核心全域上限 fs.file-max（整台機器）──▶ 單程序絕對上限 fs.nr_open ──▶ 每程序限制 nofile（ulimit -n）
                                                                     ├─ systemd 服務：由 unit 的 LimitNOFILE / system.conf 的 DefaultLimitNOFILE 決定（不讀 limits.conf）
                                                                     └─ 互動 shell：由 PAM 讀 /etc/security/limits.d/ 決定
inotify watch、pid_max / threads-max 是同類的「核心配額」，各自獨立、各自撞牆
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **檔案描述子（file descriptor）** | 程序開啟的每個檔案、socket、pipe 都佔一個整數編號 | 高連線服務每條連線一個，所以 nginx / 資料庫最先撞到上限 |
| **nofile / ulimit -n** | 每個程序可開啟的描述子數上限（soft / hard 兩個值） | 預設 1024；由 systemd 或 PAM 在程序啟動時設定，改了要重啟程序才生效 |
| **fs.file-max / fs.nr_open** | 前者是整台機器所有程序合計的上限，後者是單一程序的絕對上限 | `nofile` 不能超過 `nr_open`；兩者預設都很大，通常不是瓶頸 |
| **LimitNOFILE / DefaultLimitNOFILE** | systemd unit 的 `[Service]` 選項 / `system.conf` 的全域預設 | systemd 啟動的程序只認這個，改 `limits.conf` 對服務無效 |
| **PAM limits（limits.d）** | PAM 在使用者登入時套用的資源限制設定檔 | 只影響互動登入的 shell 與其子程序 |
| **inotify watch** | 核心讓程式訂閱「檔案有變動」通知的機制，每個被監看的檔案佔一個 watch | IDE、檔案同步工具大量使用；`max_user_watches` 不夠時它們會靜默漏掉變更 |
| **pid_max / threads-max** | 整台機器可用的 PID 數 / 執行緒數上限 | 大量執行緒的應用（JVM、Go）在容器密集的主機上會用完 |

**為什麼**：核心參數調得再好，應用本身的限制沒放開一樣撞牆：`nofile` 預設 1024 讓 nginx / 資料庫在高連線時報 "Too many open files"，inotify watch 不夠讓 IDE 與檔案同步工具靜默失效。這類限制分散在 systemd、PAM limits 與 sysctl 三處，只改其中一處常常沒效。

**怎麼做**：服務用 unit 的 `LimitNOFILE`（systemd 啟動的程序不讀 `limits.conf`），互動 shell 才用 `limits.d`。

```bash
ulimit -n; cat /proc/sys/fs/file-max; cat /proc/sys/fs/nr_open
# 服務：在 unit 加 LimitNOFILE=1048576（見 04 章）；全域 /etc/systemd/system.conf DefaultLimitNOFILE=1048576
# 使用者 shell：/etc/security/limits.d/90-nofile.conf → * soft nofile 1048576 / * hard nofile 1048576
sysctl fs.inotify.max_user_watches=1048576         # IDE / 檔案監看
sysctl kernel.pid_max=4194304; kernel.threads-max  # 大量執行緒
```

各應用的效能天花板多半由自己的設定決定（緩衝池大小、worker 數、持久化策略），作業系統層只是配合。下表列出最常見的幾個關鍵項：

常見應用快速調校：

| 應用 | 重點 |
|---|---|
| Nginx | `worker_processes auto; worker_rlimit_nofile 65535; events { worker_connections 16384; use epoll; }`；開 `keepalive`、`gzip`/`brotli`、`open_file_cache`；🔵 SELinux 反代開 `httpd_can_network_connect` |
| PostgreSQL | `shared_buffers` = 25% RAM、`effective_cache_size` = 75%、`work_mem` 依連線數、`wal_compression`、`huge_pages=on`、WAL 獨立磁碟；用 pgtune 產生 |
| MariaDB / MySQL | `innodb_buffer_pool_size` = 70% RAM、`innodb_log_file_size` 1~4G、`innodb_flush_method=O_DIRECT`、`innodb_io_capacity` 依 SSD |
| Redis / Valkey | `vm.overcommit_memory=1`、THP never、`net.core.somaxconn`、`maxmemory-policy`、持久化用 AOF everysec |
| Java | `-Xms=-Xmx` 固定、G1/ZGC、`-XX:+UseLargePages`、容器內用 `-XX:MaxRAMPercentage=75` |
| Docker / Podman | storage driver overlay2、日誌上限、`--cpus` / `--memory` 限制、避免 `--privileged` |
| 一般 | 用 `systemd-analyze security` 與 `systemd-cgtop` 找資源使用；`perf` 找熱點 |

---

## 7. tuned（🔵 Fedora 預設；🟠 Ubuntu 可安裝，已實測）

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **tuned** | Red Hat 開發的系統調校 daemon，依 profile 一次套用 sysctl、governor、I/O 排程器、THP 等設定 | 取代手動維護多處設定；套用順序在 `sysctl.d` 之後，所以會覆蓋相同參數 |
| **profile** | 一組調校設定的集合（內建在 `/usr/lib/tuned/<name>/tuned.conf`，自訂放 `/etc/tuned/`），分 `[sysctl]`、`[cpu]`、`[vm]`、`[disk]` 等區段 | 同一時間只能啟用一個；用 `include=` 繼承另一個 profile 並只覆寫差異 |
| **tuned-adm** | 操作 tuned 的命令列工具：列出、切換、驗證、建議 profile | `recommend` 依虛擬化與硬體自動建議；`verify` 檢查目前系統值是否仍等於 profile 設定 |
| **tuned-ppd / power-profiles-daemon** | 桌面「省電 / 平衡 / 效能」電源模式的 D-Bus 介面；`tuned-ppd` 是 tuned 提供的相容實作 | 🔵 Fedora 42+ Workstation 用 tuned-ppd 取代 power-profiles-daemon，桌面切換電源模式底層就是切 tuned profile |
| **C-state / energy_perf_bias / min_perf_pct** | profile `[cpu]` 區段常見的三個旋鈕：限制閒置深度、告訴 CPU 偏效能或省電、Intel P-state 的最低頻率百分比 | `latency-performance` 把三者都推向效能以壓低喚醒延遲，代價是耗電與發熱 |

**為什麼**：前面幾節的 governor、sysctl、I/O 排程器、THP 分散在五六個地方，手動維護容易漏、也難在多台機器間保持一致；tuned 是 Red Hat 的動態調校 daemon，以 profile 一次套用 sysctl、CPU governor、I/O 排程器、THP、能耗設定，換工作負載只要換 profile。

tuned 來自 Red Hat 生態（RHEL 的標準元件），所以 Fedora Server 預設安裝並啟用、Workstation 也已改用它管電源；Ubuntu 只把它當普通套件收在倉庫，預設不裝，且版本與 profile 集合較舊。

**怎麼做**：Fedora 直接用；Ubuntu 安裝後會自動依環境選 profile，再依用途換成對應的 profile。

```bash
# 🔵 Fedora Server 預設安裝並啟用；Fedora 42+ Workstation 改用 tuned + tuned-ppd（取代 power-profiles-daemon）
# 🟠 Ubuntu
sudo apt install -y tuned && sudo systemctl enable --now tuned     # 實測 24.04 套件 2.21；安裝後自動選 virtual-guest（VM 內）
tuned-adm active
tuned-adm list                                  # 實測 Fedora 44 內建 15 個；Ubuntu 24.04 有 38 個（含較舊 profile）
tuned-adm recommend                             # 依硬體建議
sudo tuned-adm profile throughput-performance   # 一般伺服器
sudo tuned-adm profile latency-performance      # 低延遲（關 C-state、governor performance）
sudo tuned-adm profile network-latency          # 網路延遲敏感
sudo tuned-adm profile network-throughput       # 40G+ 網路
sudo tuned-adm profile virtual-guest            # VM 內（預設自動選）
sudo tuned-adm profile virtual-host             # KVM 宿主
sudo tuned-adm profile postgresql               # 🔵 需 tuned-profiles-postgresql；🟠 需自建
sudo tuned-adm profile balanced                 # 桌機
sudo tuned-adm verify                           # 檢查設定是否仍生效
sudo tuned-adm off
```

內建 profile 不會剛好符合你的應用；用 `include=` 繼承一個接近的，只覆寫差異，之後所有機器都用同一份設定檔即可。自訂 profile `/etc/tuned/myapp/tuned.conf`（🟠 Ubuntu 路徑相同）：

```ini
[main]
summary=My app server
include=throughput-performance

[sysctl]
vm.swappiness=5
net.core.somaxconn=65535
net.ipv4.tcp_congestion_control=bbr

[vm]
transparent_hugepages=never

[disk]
readahead=4096

[cpu]
governor=performance
energy_perf_bias=performance
min_perf_pct=100

[script]
script=${i:PROFILE_DIR}/post.sh
```

```bash
sudo tuned-adm profile myapp
```

**注意**：⚠️ tuned 會覆蓋 `/etc/sysctl.d/` 中相同的參數（tuned 後套用）。決定用 tuned 就把調校集中在 profile；不用 tuned 就 `systemctl disable --now tuned`，否則會出現「改了 sysctl.d 卻不生效」的鬼問題。

---

## 8. cgroup v2 資源控制（兩者皆為 cgroup v2，實測 `cgroup2fs`）

**名詞與關係**：cgroup 是核心機制，systemd 是它的管理者；systemd 的每個 unit 對應樹上的一個節點，資源限制就是寫在節點上的屬性：

```
/sys/fs/cgroup（cgroup2fs 掛載點，一棵樹）
 ├─ system.slice ── nginx.service ── MemoryMax / CPUQuota / IOWeight / TasksMax（每個 unit 就是一個 cgroup）
 ├─ user.slice ─── user-1000.slice ── session-*.scope（該登入使用者的所有程序）
 └─ batch.slice（自訂）── 加了 Slice=batch.slice 的服務、systemd-run --slice 放進來的臨時工作
資源控制器：cpu（Quota 絕對上限 / Weight 相對比例）、memory（Max 硬限 / High 節流）、io（Weight 比例 / BandwidthMax 硬限）、pids（TasksMax）
子節點的上限不能超過父 slice 的上限
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **cgroup v2** | 核心的「控制群組」第二版：把程序分組後對每組限制 CPU、記憶體、I/O、PID 數；統一成一棵樹（v1 是每種資源各一棵） | 兩者皆為 v2，掛在 `/sys/fs/cgroup`（`cgroup2fs`）；systemd 是它的主要管理者 |
| **slice / service / scope** | systemd 的三種 cgroup 單位：slice 是分組容器、service 是 systemd 自己啟動的程序、scope 是外部啟動後被納管的程序（如登入 session） | 限制可設在任一層，子單位受父 slice 約束 |
| **CPUQuota / CPUWeight** | Quota 是絕對上限（200% = 兩顆核心），Weight 是與同層其他 cgroup 競爭時的相對比例（預設 100） | Quota 防止吃光、Weight 決定擁擠時誰優先；兩者可並用 |
| **MemoryMax / MemoryHigh** | Max 是硬上限，到頂就對該 cgroup 執行 OOM kill；High 是軟上限，超過就節流並積極回收 | 通常一起設、High 略低於 Max，讓服務先變慢而不是直接被殺 |
| **IOWeight / IOReadBandwidthMax / IOWriteBandwidthMax** | I/O 的相對比例 / 每個裝置的絕對頻寬上限 | 備份工作用 BandwidthMax 限死，一般服務用 Weight 分配 |
| **TasksMax** | 該 cgroup 可建立的程序＋執行緒數上限 | 防止 fork 炸彈或執行緒洩漏拖垮整機 |
| **systemd-cgtop / systemd-cgls** | 依 cgroup 即時顯示資源用量 / 樹狀列出 cgroup 與其程序 | 先用 cgtop 找誰在吃，再用 set-property 限制 |
| **set-property / systemd-run** | `set-property` 對既有 unit 寫入 drop-in 永久設限（`--runtime` 只到重開機）；`systemd-run` 把一次性指令放進指定 slice 執行 | 臨時工作不必寫 unit 檔，直接 `systemd-run --slice=` |

**為什麼**：沒有資源上限時，一個失控的備份工作或記憶體洩漏的服務會把整台機器拖垮，OOM killer 還可能殺錯對象；cgroup 讓你對每個服務 / 使用者設 CPU、記憶體、I/O 上限，把故障範圍限制在單一服務。systemd 的每個 unit 本來就是一個 cgroup，所以不需要額外工具。

**怎麼做**：`systemd-cgtop` 找出誰在吃資源，再用 `set-property` 設限（會寫入 drop-in，重開機仍有效）。

```bash
systemd-cgtop                                       # 依 cgroup 看 CPU / 記憶體 / I/O
systemd-cgls                                        # 樹狀
cat /sys/fs/cgroup/system.slice/nginx.service/memory.current
# 服務限制（unit 檔 [Service] 或 systemctl set-property）
sudo systemctl set-property nginx.service MemoryMax=2G MemoryHigh=1.5G CPUQuota=200% IOWeight=500 TasksMax=4096
sudo systemctl set-property --runtime ...           # 暫時
# 使用者限制（所有登入使用者）
sudo systemctl set-property user-1000.slice MemoryMax=8G CPUWeight=50
sudo mkdir -p /etc/systemd/system/user-.slice.d && sudo tee /etc/systemd/system/user-.slice.d/50-limits.conf <<'EOF'
[Slice]
MemoryMax=8G
CPUQuota=400%
EOF
# 自訂 slice 分組
# /etc/systemd/system/batch.slice：[Slice] CPUWeight=20 MemoryMax=16G
# 服務加 Slice=batch.slice；臨時：systemd-run --slice=batch.slice --nice=19 ./job.sh
# I/O 限制
sudo systemctl set-property backup.service IOReadBandwidthMax="/dev/sda 50M" IOWriteBandwidthMax="/dev/sda 50M"
```

**注意**：`MemoryMax` 到頂會直接 OOM kill 該服務，`MemoryHigh` 只是先節流回收，通常兩個一起設、High 略低於 Max。容器：Docker / Podman 的 `--cpus`、`--memory`、`--device-read-bps` 底層就是這些 cgroup 參數。

---

## 9. 開機與服務啟動時間

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **systemd-analyze** | systemd 附的開機分析工具：`blame` 列各 unit 耗時、`critical-chain` 顯示關鍵路徑、`plot` 畫成 SVG 時間軸 | `blame` 只看單一 unit 慢，`critical-chain` 才看得出「誰在等誰」 |
| **wait-online** | `systemd-networkd-wait-online`（🟠）/ `NetworkManager-wait-online`（🔵）：等網路真正連上才讓 `network-online.target` 完成 | 網卡沒接線就等到 timeout；靠 `After=network-online.target` 的服務全部跟著等 |
| **network-online.target** | systemd 的「網路已可用」里程碑 | 只有真正需要對外連線才能啟動的服務才該依賴它，否則用 `network.target` 即可 |
| **nofail / _netdev** | fstab 選項：`nofail` 讓裝置不存在時不阻塞開機，`_netdev` 標記為網路檔案系統、等網路好了才掛 | 少了它們，掛載失敗會讓 systemd 等 90 秒後進 emergency mode |
| **dracut hostonly** | 🔵 dracut 只打包本機硬體需要的模組進 initramfs 的模式 | initramfs 越小開機越快；Fedora 預設開啟 |

**為什麼**：開機慢通常不是硬體慢，而是某個服務在等一個永遠不會到的條件（沒接線的網卡、fstab 裡不存在的網路碟），一等就是 90 秒 timeout；VM 大量重開或自動擴縮時，這 90 秒直接拉長恢復時間。

**怎麼做**：用 `systemd-analyze` 找出時間花在哪個 unit，再針對元兇處理。

```bash
systemd-analyze                                     # 總時間
systemd-analyze blame | head -20                    # 慢的服務
systemd-analyze critical-chain
systemd-analyze plot > /tmp/boot.svg
# 常見元兇：systemd-networkd-wait-online / NetworkManager-wait-online（網卡沒接線）、
#   snapd（🟠）、plymouth、apt-daily（🟠）、fstab 的網路碟沒加 nofail/_netdev
sudo systemctl disable NetworkManager-wait-online.service    # 🔵 不依賴網路的服務可關
sudo systemctl disable systemd-networkd-wait-online.service  # 🟠
# 🔵 dracut 精簡：/etc/dracut.conf.d/host.conf 加 hostonly="yes"（Fedora 預設）
# 🟠 移除不需要的 snap：snap list; sudo snap remove <name>
```

**注意**：關掉 wait-online 之前先確認沒有服務靠 `After=network-online.target` 等網路，否則會變成服務比網路先起來而啟動失敗。

---

## 10. 壓力測試與基準

**名詞與關係**：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **stress-ng** | 對 CPU、I/O、記憶體等子系統施加人工負載的工具 | 用來驗證 OOM、cgroup 限制、散熱降頻等行為，不是量效能的基準 |
| **sysbench** | CPU / 記憶體 / 資料庫的簡單基準工具 | 給一個可重複的數字比對調校前後 |
| **fio / iperf3** | 磁碟 I/O 基準 / 網路頻寬基準 | 分別見 06 章與 §5 |
| **ab / wrk** | HTTP 壓測工具：`ab` 單執行緒簡單，`wrk` 多執行緒可壓更高併發 | 🟠 `apache2-utils`、🔵 `httpd-tools` 提供 ab |
| **pgbench** | PostgreSQL 內建的 TPC-B 型交易基準 | 資料庫調校（§6 表）前後比對用 |
| **基準（baseline）** | 正常狀態下記錄的一組數據（`sar -A`），之後所有比對的參照 | 沒有 baseline 就分不出「變慢了」還是「本來就這樣」 |

**為什麼**：調校前後沒有可重複的基準，就無法證明改動有效，也無法在上線前知道機器的極限在哪；壓力測試同時能把 OOM、檔案描述子不足、連線佇列溢出這些問題在測試環境先逼出來。

**怎麼做**：依瓶頸類型選工具，測試期間同步用 `sar` 記錄，事後比對。

```bash
stress-ng --cpu 8 --io 4 --vm 2 --vm-bytes 1G --timeout 60s --metrics-brief
sysbench cpu --threads=8 run; sysbench memory run                # sysbench（兩者皆有套件）
fio ...（06 章）；iperf3 ...（§5）
ab -n 10000 -c 100 http://host/   /   wrk -t8 -c200 -d30s http://host/   # HTTP（🟠 apache2-utils、wrk；🔵 httpd-tools；wrk 不在 Fedora 套件庫，可用 COPR 或自行編譯）
pgbench -i -s 50 db && pgbench -c 20 -T 60 db                    # PostgreSQL
# 監看基準期間：sar -A 1 > baseline.txt；perf record 全系統
```

**注意**：不要在正式機器上跑 `stress-ng --vm` 或 `fio` 寫入測試，前者會觸發 OOM，後者會寫壞資料。

---

## 11. 本章差異總結

兩者核心預設（THP、qdisc、I/O 排程器、cgroup v2）幾乎一致，差異集中在工具的套件命名（Ubuntu 把 perf / cpupower 綁在核心工具包、BCC 指令加後綴）與 tuned 的地位：Fedora 承襲 RHEL 把 tuned 當標準元件，Ubuntu 則交給使用者自行決定用 sysctl.d 還是 tuned。

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| tuned | 需安裝（2.21），安裝後自動啟用 | Server 預設；Workstation 42+ 用 tuned + tuned-ppd |
| tuned 內建 profile 數（實測） | 38（含舊版與 SAP 等） | 15（精簡） |
| perf 套件 | `linux-tools-generic` / `linux-tools-$(uname -r)` | `perf` |
| cpupower | `linux-tools-generic` | `kernel-tools` |
| BCC 工具 | `bpfcc-tools`（指令帶 `-bpfcc` 後綴） | `bcc-tools`（`/usr/share/bcc/tools/`） |
| iotop | `iotop` | 無（用 `iotop-c` 或 bcc `biotop`） |
| sysstat 啟用 | 需 `/etc/default/sysstat` 設 `ENABLED="true"` + `sysstat.service` | `systemctl enable --now sysstat` |
| 預設 qdisc | `fq_codel`（10-bufferbloat.conf） | `fq_codel`（核心預設） |
| 預設 `vm.max_map_count` | 1048576（10-map-count.conf） | 1048576（/usr/lib/sysctl.d/10-map-count.conf） |
| 預設 THP | madvise | madvise |
| 預設 I/O 排程器 | 轉速碟 bfq、SSD mq-deadline、NVMe none（udev 60-block-scheduler.rules） | 同左（實測同名 udev 規則） |
| swap 策略 | 磁碟交換檔，swappiness 60 | zram，swappiness 60（可留高） |
| HTTP 壓測工具套件 | `apache2-utils`（ab） | `httpd-tools`（ab） |
| cgroup | v2 | v2 |
