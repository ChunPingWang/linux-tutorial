# 03 - 套件與軟體安裝

本章所有指令皆於 Ubuntu 24.04（apt 2.8）與 Fedora 44（**dnf5** 5.4）實測。Fedora 41 起預設為 dnf5，語法與舊版 dnf4 有差異之處會特別註明。

**本章解決什麼問題**：一台主機從裝好到退役，套件管理員是最常被碰到的工具——補安全性更新、加第三方庫裝 Docker、鎖版本避免自動升級弄壞服務、升級後判斷要不要重開機。真實運維中最痛的幾件事都出在這裡：照舊教學用 `apt-key` 結果 24.04 沒這指令；PPA 蓋掉了系統套件導致升級失敗；`dnf upgrade` 半夜自動跑掉 kernel 導致 NVIDIA 模組失效；`pip install` 動到系統 Python 讓 `dnf` 本身壞掉。本章的取捨原則是：**優先用發行版原生套件、每個第三方來源獨立簽章、能釘版本就釘、能回滾就保留回滾能力**；Ubuntu 與 Fedora 在同一件事上做法常不同，表格與指令都採並列對照。

---

## 1. 套件管理架構

**名詞與關係**：套件管理其實是「一條從遠端套件庫到本機資料庫的管線」，每個名詞各占管線的一段，先看它們怎麼串起來：

```
套件庫（repository）──提供 metadata（索引）──▶ 高階工具（apt / dnf）算相依、下載 ──▶ 低階工具（dpkg / rpm）解包、寫入
  │ 位置由 .sources / .repo 定義                  │ 下載的 .deb / .rpm 先放快取                   │ 結果記在本機資料庫
  └─ 每個來源用自己的 GPG 金鑰簽 metadata，高階工具驗簽通過才信任                                    └─ 執行時持有鎖檔，防止兩個工具同時寫
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **套件（`.deb` / `.rpm`）** | 把程式檔案、設定檔、安裝腳本與「相依清單」打成一個檔案的格式；🟠 `.deb` 依 Debian **政策手冊**（Debian Policy，規定套件怎麼打包、檔案該放哪的規範文件）打包，🔵 `.rpm` 由 Red Hat 定義 | 低階工具只認得這個格式；高階工具負責把它從套件庫抓下來 |
| **相依（dependency）** | 套件在 metadata 裡宣告「我要先有 X 才能跑」的清單 | 高階工具會自動補齊；低階工具不會，所以直接 `dpkg -i` / `rpm -i` 常留下「相依損壞」 |
| **低階工具 `dpkg` / `rpm`** | 對單一套件檔解包、執行安裝腳本、寫入本機資料庫的工具，不上網、不解相依 | 高階工具最後都是呼叫它們做實際安裝；`dpkg -S`、`rpm -qf` 這類查詢也是直接讀本機資料庫 |
| **高階工具 `apt` / `apt-get` / `apt-cache` / `dnf`** | 讀套件庫索引、算相依、下載後交給低階工具的前端；🟠 `apt` 是給人互動用的整合介面，`apt-get`（安裝／升級）與 `apt-cache`（查詢）是輸出格式穩定、給腳本用的原始工具；🔵 `dnf` 在 Fedora 41+ 即 **dnf5**，是 `yum` → `dnf4` → `dnf5` 的第三代重寫 | 依賴套件庫定義與 GPG 金鑰才能運作；日常 90% 的操作都在這一層 |
| **套件庫（repository）** | 一個放有套件檔與索引的 HTTP 目錄，由 🟠 `.sources`、🔵 `.repo` 檔告訴高階工具它在哪裡 | 一台主機可同時有官方庫與多個第三方庫；第 3 節整節都在講怎麼管它們 |
| **metadata（索引）** | 套件庫端產生的「有哪些套件、版本、相依、檔案清單」目錄檔，高階工具先下載它才知道有什麼可裝 | 🟠 `apt update` 就是抓 metadata；🔵 dnf 會自動判斷過期重抓；索引損壞就「找不到套件」 |
| **元件（component）** | 官方套件庫依授權與支援等級再分的子區：🟠 main（Canonical 支援）/ universe（社群維護）/ restricted（非自由驅動）/ multiverse（專利或授權受限）；🔵 fedora（發行時凍結的基礎版）/ updates（正式更新）/ updates-testing（測試中的更新） | Fedora 官方不收非自由軟體，所以沒有 multiverse 的對應物，由第三方 RPM Fusion 補（§3.4） |
| **GPG 金鑰（keyring）** | 套件庫用來簽署 metadata 的公鑰，存在本機的金鑰檔（keyring）裡，高階工具驗簽通過才使用該來源 | 🟠 每個來源在 `.sources` 內用 `Signed-By` 指定自己的金鑰檔；🔵 每個 `.repo` 用 `gpgkey=` 指定；金鑰失效整個來源就不能用 |
| **本機資料庫** | 記錄「這台裝了哪些套件、什麼版本、每個檔案屬於誰」的檔案：🟠 純文字 `/var/lib/dpkg/status`，🔵 sqlite 格式的 **rpmdb** | 低階工具寫入、所有查詢從這裡讀；損毀時的修法見 §10 |
| **快取** | 高階工具下載後尚未刪除的套件檔與索引 | 🟠 舊 `.deb` 還在快取才有機會回滾（§2.1）；塞滿 `/var` 是常見故障（§10） |
| **鎖檔（lock）** | 工具執行時建立的檔案（🟠 `/var/lib/dpkg/lock-frontend`），確保同一時間只有一個程序在改資料庫 | 背景自動更新（`unattended-upgrades`）持鎖時，手動 `apt` 就會「Could not get lock」（§10） |

**為什麼**：出問題時（鎖檔卡住、金鑰失效、快取塞滿 `/var`）你得知道該去哪個路徑找東西，以及「低階工具」與「高階工具」的分工——`dpkg -i` / `rpm -i` 不會解相依，很多「相依損壞」都是直接用低階工具裝東西造成的。兩個發行版的架構不同，來自上游差異：Debian 從 1990 年代起就以 `dpkg` + 政策手冊定義 `.deb`，APT 是後來疊上去的前端；Red Hat 生態則以 `rpm` 資料庫為核心，`yum` → `dnf` → `dnf5` 逐代重寫，repo 定義一直沿用 INI 格式的 `.repo`。

**怎麼做**：先記住這張對照表，後面各節的路徑都從這裡來：

| 層級 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 套件格式 | `.deb` | `.rpm` |
| 低階工具（不解相依） | `dpkg` | `rpm` |
| 高階工具（解相依、下載） | `apt`（給人用）/ `apt-get`、`apt-cache`（給腳本用） | `dnf`（= dnf5） |
| 資料庫 | `/var/lib/dpkg/status` | `/var/lib/rpm/` (sqlite) |
| 快取 | `/var/cache/apt/archives/` | `/var/cache/libdnf5/` |
| 設定 | `/etc/apt/apt.conf.d/` | `/etc/dnf/dnf.conf`、`/etc/dnf/libdnf5.conf.d/` |
| 套件庫定義 | `/etc/apt/sources.list.d/*.sources` | `/etc/yum.repos.d/*.repo` |
| GPG 金鑰 | `/etc/apt/keyrings/`（第三方）、`/usr/share/keyrings/`（官方） | `/etc/pki/rpm-gpg/` |
| 元件分類 | main / restricted / universe / multiverse | fedora / updates / updates-testing + 第三方 RPM Fusion |

**注意**：💡 `apt` 輸出格式會變、不適合寫在腳本中；腳本請用 `apt-get` / `apt-cache`。dnf 沒有這個問題。

---

## 2. 日常操作對照

**名詞與關係**：這一節的指令本身直觀，難的是幾個決定「預設行為」的概念，它們都建立在 §1 的「相依」與「metadata」之上：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **建議套件 / 弱相依** | 套件 metadata 裡「有它更好、沒有也能跑」的軟性相依：🟠 稱 Recommends（`apt` 預設會裝），🔵 稱 weak deps（`install_weak_deps` 預設為 True） | 是相依（§1）的弱化版；伺服器用 `--no-install-recommends` / `install_weak_deps=False` 略過，可少裝幾十個桌面用套件 |
| **保留（kept back）** | 🟠 `apt upgrade` 發現某套件的新版需要「新增或移除其他套件」時，選擇不動它、在輸出裡列為 kept back 的狀態 | 這就是 `apt upgrade` 與 `apt full-upgrade` 的分野；kernel、systemd 常因此沒升到；🔵 `dnf upgrade` 預設允許增刪，所以沒有這個狀態 |
| **remove / purge** | remove 只刪程式檔、保留設定檔；purge（🟠）連 `/etc` 下的設定檔一起刪 | 🔵 dnf 沒有 purge，移除時把被改過的設定檔改名為 `.rpmsave` 留在原位 |
| **`.rpmsave`** | 🔵 rpm 在移除或升級套件、發現設定檔曾被管理員修改時，把舊檔改名保留的副檔名 | 與升級時的 `.rpmnew`（新版設定檔另存）成對，大版本升級後的清理見 §9 |
| **孤兒相依（autoremove）** | 當初只因為別的套件需要而被自動裝入、現在已沒有任何套件需要的套件 | 高階工具會記住哪些套件是「自動安裝」的，`autoremove` 就是清這批；手動裝的不會被清 |
| **索引過期** | 本機 metadata（§1）比套件庫上的舊，導致「找不到套件」或裝到舊版 | 🟠 apt 從不自動更新索引，所以 `apt update` 要放在 `apt install` 前；🔵 dnf 依 `metadata_expire`（預設 48 小時）自動重抓 |

**為什麼**：日常操作 90% 是這幾組指令，但兩邊的「預設行為」不同才是坑：🟠 `apt upgrade` 不會裝新相依、也不會移除套件，所以 kernel 或 systemd 這類有新相依的升級會被「保留（kept back）」而沒裝到；🔵 `dnf upgrade` 則預設允許移除，語意等於 `apt full-upgrade`。另外 apt 不會自動更新索引，忘了 `apt update` 就會裝到舊版或「找不到套件」；dnf 會自動檢查 metadata 過期。伺服器上還常需要「不裝建議套件」——`--no-install-recommends` / `install_weak_deps=False` 能讓 nginx 少拉進幾十個桌面用的相依。

**怎麼做**：依情境對照使用：

```bash
# ── 更新套件索引 ──
sudo apt update                       # 🟠 必須手動
# 🔵 dnf 每次執行會自動檢查 metadata 是否過期（預設 48 小時）；強制更新：
sudo dnf makecache --refresh

# ── 查看可升級 ──
apt list --upgradable                 # 🟠
dnf check-upgrade                     # 🔵

# ── 升級 ──
sudo apt upgrade                      # 🟠 不移除套件、不裝新相依
sudo apt full-upgrade                 # 🟠 允許移除 / 新增相依（等於舊的 dist-upgrade）
sudo dnf upgrade                      # 🔵 同 full-upgrade 的語意
sudo dnf upgrade --security           # 🔵 只裝安全性更新
sudo dnf offline-upgrade download && sudo dnf offline reboot   # 🔵 離線升級（重開機時套用，較安全）

# ── 安裝 / 移除 ──
sudo apt install nginx                # 🟠
sudo apt install --no-install-recommends nginx   # 🟠 不裝建議套件（伺服器常用）
sudo apt remove nginx                 # 🟠 保留設定檔
sudo apt purge nginx                  # 🟠 連設定檔一起刪
sudo apt autoremove --purge           # 🟠 清孤兒相依
sudo dnf install nginx                # 🔵
sudo dnf install --setopt=install_weak_deps=False nginx   # 🔵 不裝弱相依
sudo dnf remove nginx                 # 🔵（設定檔會保留為 .rpmsave）
sudo dnf autoremove                   # 🔵

# ── 重新安裝 ──
sudo apt reinstall nginx              # 🟠
sudo dnf reinstall nginx              # 🔵

# ── 搜尋與資訊 ──
apt search "^nginx"                   # 🟠
apt show nginx                        # 🟠
apt-cache policy nginx                # 🟠 看版本與來源優先權
dnf search nginx                      # 🔵
dnf info nginx                        # 🔵
dnf repoquery --info nginx            # 🔵 更詳細

# ── 檔案 ⇄ 套件 ──
dpkg -S /usr/bin/curl                 # 🟠 已安裝的
apt-file search bin/nmap              # 🟠 未安裝的（需 apt install apt-file && apt-file update）
dpkg -L curl                          # 🟠 套件包含哪些檔
rpm -qf /usr/bin/curl                 # 🔵 已安裝的
dnf provides /usr/bin/nmap            # 🔵 未安裝的也能查（支援 */bin/nmap 萬用字元）
rpm -ql curl                          # 🔵

# ── 相依 ──
apt-cache depends curl                # 🟠
apt-cache rdepends --installed libc6  # 🟠 誰依賴它
dnf repoquery --requires curl         # 🔵
dnf repoquery --whatrequires glibc    # 🔵

# ── 已安裝清單 ──
apt list --installed                  # 🟠
dpkg -l | grep ^ii                    # 🟠
dpkg --get-selections > pkgs.txt      # 🟠 匯出（可用 dpkg --set-selections 還原）
dnf list --installed                  # 🔵（dnf4 為 dnf list installed，dnf5 兩種都吃）
rpm -qa --last | head                 # 🔵 依安裝時間排序
dnf repoquery --userinstalled         # 🔵 只列使用者手動安裝的

# ── 變更紀錄 ──
apt-get changelog curl                # 🟠
rpm -q --changelog curl | head        # 🔵

# ── 清快取 ──
sudo apt clean                        # 🟠
sudo dnf clean all                    # 🔵
```

**注意**：`apt remove` 與 `dnf remove` 都會留下設定檔（前者留在原位、後者改名 `.rpmsave`），重裝時會沿用舊設定；要「乾淨重來」🟠 用 `purge`，🔵 得手動刪 `/etc/` 下的殘檔。`dpkg --get-selections > pkgs.txt` 與 `dnf repoquery --userinstalled` 適合在重灌前匯出「這台裝了什麼」。

### 2.1 交易歷史與回滾（🔵 Fedora 獨有優勢）

**名詞與關係**：「回滾」能不能做，取決於套件管理員有沒有「交易」這個單位：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **交易（transaction）** | 🔵 dnf 把一次指令裡所有的安裝、升級、移除視為一個整體，編號後記在交易歷史（`dnf history`）裡 | 回滾的基本單位；🟠 apt 沒有這個概念，只把每次操作寫成 `/var/log/apt/history.log` 的純文字 |
| **undo** | 把「某一筆」交易反向執行：裝的移除、升的降回 | 只動那一筆，之後的交易不受影響 |
| **rollback** | 把系統退回「某筆交易完成當時」的狀態，中間所有交易一併反向 | 涵蓋範圍比 undo 大；兩者都要舊版套件仍在套件庫或快取（§1）裡才能執行 |
| **檔案系統快照** | 在 LVM / ZFS / 虛擬機層把整個磁碟狀態凍結一份，與套件管理員無關 | 🟠 Ubuntu 沒有交易回滾，「可退的升級」只能靠它；🔵 Fedora 遇到舊版套件已下架時也得靠它 |

**為什麼**：升級後服務起不來，最快的處置是「把剛才那筆升級整包退掉」，而不是逐一猜哪個套件有問題。dnf 把每次安裝 / 升級 / 移除記成一筆交易，可以精準還原某一筆或回到某個時間點；apt 只有純文字日誌，回滾要自己查舊版號再一個個裝回去，而且舊版 `.deb` 可能已不在快取或鏡像上。

**怎麼做**：

```bash
dnf history list                      # 列出所有交易
dnf history info 42                   # 某筆交易詳情（last 代表最新一筆）
sudo dnf history undo 42              # 還原該筆交易
sudo dnf history rollback 40          # 回到第 40 筆之後的狀態
```

**注意**：🟠 Ubuntu 沒有內建 rollback，只能看 `/var/log/apt/history.log` 再手動 `apt install pkg=舊版本`。若 Ubuntu 主機需要「可回滾的升級」，靠的是檔案系統快照（LVM / ZFS / VM 快照），不是套件管理員。

### 2.2 判斷是否需要重開機

**名詞與關係**：「已更新但未生效」的根源是記憶體裡還跑著舊程式碼，兩邊各用不同工具回答「要重開機還是只要重啟服務」：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **kernel / glibc / systemd** | 核心、C 標準函式庫、init 系統——三個「幾乎所有程序都載入或依賴」的元件 | 升級後舊版仍在記憶體中執行；kernel 只有重開機才換，glibc / systemd 至少要重啟所有服務 |
| **`/var/run/reboot-required`** | 🟠 套件的安裝腳本在升級 kernel 等關鍵套件後建立的旗標檔，`.pkgs` 列出是哪些套件觸發 | 存在與否就是「要不要重開機」的答案，適合腳本判斷；但它不管服務層 |
| **needrestart** | 🟠 掃描每個執行中程序載入的函式庫是否已被新版取代、列出需重啟服務的工具，`apt` 升級後會自動呼叫 | 補 `reboot-required` 不管服務的缺口；`-b` 批次模式輸出 `NEEDRESTART-KSTA` 給腳本讀 |
| **`dnf needs-restarting`** | 🔵 dnf 內建的對應功能：不帶參數列程序、`-r` 判斷是否需重開機、`-s` 只列 systemd 服務 | 一個指令涵蓋 🟠 兩個工具的功能，用 exit code 回報 |

**為什麼**：升級 kernel、glibc、systemd 後，舊的程式碼還載在記憶體裡跑——安全性修補要等重開機（或重啟服務）才真正生效，這也是弱點掃描「明明已更新卻仍被標為有漏洞」的常見原因。反過來說，不需要重開機時盲目重開，等於白白多一次服務中斷；所以升級後應該用工具判斷，而不是憑感覺。

**怎麼做**：

```bash
# 🟠
cat /var/run/reboot-required          # 存在即需重開機
cat /var/run/reboot-required.pkgs     # 因哪些套件
sudo needrestart                      # 互動式列出需重啟的服務（升級後 apt 會自動呼叫）
needrestart -b                        # 批次模式（NEEDRESTART-KSTA: 1=OK, 2/3=核心已更新）

# 🔵
sudo dnf needs-restarting             # 列出需重啟的程序
sudo dnf needs-restarting -r          # 是否需要重開機（rc=1 代表需要）
sudo dnf needs-restarting -s          # 只列 systemd 服務
```

**注意**：兩邊的 `-b` / `-r` 都是給腳本用的——依 exit code 或輸出決定要不要排程重開機，適合塞進 Ansible 或 cron 的升級流程。

---

## 3. 套件庫管理

### 3.1 官方套件庫設定

**名詞與關係**：套件庫定義檔（§1）在兩邊格式完全不同，這一節的名詞就是那兩種格式的欄位與周邊機制：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **deb822** | 🟠 Ubuntu 24.04 起的套件庫定義格式：一段多行「欄位: 值」（Types / URIs / Suites / Components / Signed-By），存於 `/etc/apt/sources.list.d/*.sources` | 取代舊的一行式 `sources.list`；`Signed-By` 欄位就是 §1 講的「每個來源綁自己的金鑰」 |
| **Suites / Components** | Suites 是版本代號加通道（`noble` 基礎、`noble-updates` 一般更新、`noble-security` 安全性更新、`noble-backports` 回移的新版）；Components 就是 §1 的元件（main / universe …） | 兩者組合決定「從哪個通道拿哪一類套件」；security 通道與鏡像分開設定 |
| **deb-src** | 套件庫裡「原始碼套件」的通道，`Types: deb deb-src` 才會下載它的索引 | `apt build-dep`（§6）要靠它查建置相依；平常不開可省索引下載時間 |
| **`.repo` INI** | 🔵 `/etc/yum.repos.d/` 下、以 `[repoid]` 開頭再列 `name= / baseurl= / metalink= / enabled= / gpgcheck= / gpgkey=` 的定義檔，一個檔可含多個 repo | 從 yum 時代沿用到 dnf5；第三方庫（§3.2）也是同一格式 |
| **鏡像（mirror）與 metalink** | 鏡像是官方套件庫在各地的同步副本；metalink 是 🔵 Fedora 的鏡像清單服務，dnf 依地理位置自動挑鏡像 | 🟠 要手動改 `URIs` 才會換鏡像；🔵 有 metalink 通常不用改，固定 `baseurl=` 只是為了繞開它 |
| **updates-testing** | 🔵 更新先進入測試通道、集滿回饋後才推進 `updates` 的機制（見 §1 的元件） | 用 `--enablerepo` 單次啟用可拿到未正式釋出的修正；永久開啟等於整台跟著吃測試版 |

**為什麼**：官方庫設定平常不用碰，但三種情境會需要：機房對外頻寬有限或跨海延遲高，要換到本地鏡像；要 `apt build-dep` 得先開 `deb-src`；要試 `updates-testing` 裡的修正版。兩邊格式不同是因為 Ubuntu 24.04 跟隨 Debian 全面轉向 **deb822** 格式（多行、可讀、每段可指定 `Signed-By`），舊的一行式 `sources.list` 已淘汰；Fedora 則維持 Red Hat 生態一貫的 INI `.repo` 檔，並預設用 metalink 讓 mirror 選擇自動化，所以通常不需改。

**怎麼做**：🟠 Ubuntu 24.04 改用 **deb822** 格式（`/etc/apt/sources.list.d/ubuntu.sources`，舊的 `/etc/apt/sources.list` 只剩註解）：

```
Types: deb
URIs: http://archive.ubuntu.com/ubuntu/
Suites: noble noble-updates noble-backports
Components: main universe restricted multiverse
Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg

Types: deb
URIs: http://security.ubuntu.com/ubuntu/
Suites: noble-security
Components: main universe restricted multiverse
Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg
```

換台灣鏡像：把 `URIs` 改成 `http://tw.archive.ubuntu.com/ubuntu/`（security 那段不要改）。
加 `deb-src` 只要 `Types: deb deb-src`。

**注意**：security 那段之所以不改，是因為鏡像同步有延遲；安全性更新直接從 `security.ubuntu.com` 拿才不會晚幾小時到幾天。

🔵 Fedora 的 `/etc/yum.repos.d/fedora.repo`、`fedora-updates.repo` 預設用 metalink 自動選最近鏡像，通常不需修改。要固定鏡像（例如內網有 proxy 白名單、或 metalink 老是挑到慢的節點）：

```ini
[fedora]
name=Fedora $releasever - $basearch
baseurl=https://free.nchc.org.tw/fedora/linux/releases/$releasever/Everything/$basearch/os/
#metalink=https://mirrors.fedoraproject.org/metalink?repo=fedora-$releasever&arch=$basearch
enabled=1
gpgcheck=1
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-fedora-$releasever-$basearch
```

啟停套件庫：`updates-testing` 有時已含尚未正式釋出的 bug 修正，臨時要用時建議「單次啟用」而不是永久開著，否則整台機器都會跟著吃測試版：

```bash
# 🔵 啟停套件庫（dnf5 語法）
dnf repolist --all
sudo dnf config-manager setopt updates-testing.enabled=1     # dnf4 為 --set-enabled updates-testing
sudo dnf config-manager setopt updates-testing.enabled=0
sudo dnf install --enablerepo=updates-testing pkg           # 單次啟用
```

### 3.2 新增第三方套件庫（以 Docker 為例，已實測）

**名詞與關係**：加第三方庫的核心問題是「金鑰信任範圍」，兩邊的名詞都圍繞著 §1 的 GPG 金鑰（keyring）機制：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **`apt-key`** | 🟠 舊的金鑰管理指令，把金鑰加進 `/etc/apt/trusted.gpg`——所有來源共用的「全域信任」keyring | 全域信任＝任何來源都能用這把金鑰簽套件；24.04 已移除，由 `Signed-By` 取代 |
| **`/etc/apt/keyrings/`** | 🟠 24.04 慣例存放第三方金鑰檔（`.asc` / `.gpg`）的目錄，只有被 `.sources` 的 `Signed-By` 指到的才會被用 | 與 §1 的 `/usr/share/keyrings/`（官方金鑰）分開放，方便盤點誰是第三方 |
| **`gpgcheck=1` / `gpgkey=`** | 🔵 `.repo` 的兩個欄位：前者要求驗簽、後者指定該 repo 公鑰的位置（URL 或 `file://`） | 是 Fedora 版的「每個來源一把金鑰」；`rpm --import` 把公鑰存進 rpmdb，讓 `rpm -K`（§5）也能驗本機檔 |
| **`dnf config-manager addrepo`** | 🔵 dnf5 的 repo 管理子命令，`--from-repofile=URL` 直接抓廠商的 `.repo` 檔放進 `/etc/yum.repos.d/` | dnf4 的 `--add-repo` 是同一件事的舊寫法；廠商已寫好 `gpgkey=`，所以不必手動 import |

**為什麼**：Docker、PostgreSQL、Grafana 這類軟體的發行版打包版通常落後好幾個版本，正式環境多半直接用廠商官方庫。危險在於金鑰處理：舊做法 `apt-key add` 會把第三方金鑰加進「全域信任」，等於任何一個第三方庫都能簽發假的 `libc6` 蓋掉系統套件；deb822 的 `Signed-By` 把金鑰綁在單一來源上，第三方庫只能對自己的套件負責。Fedora 的 `.repo` 檔本來就是每個 repo 一把 `gpgkey`，所以沒有這段歷史包袱。

**怎麼做**：

```bash
# 🟠 Ubuntu：金鑰放 /etc/apt/keyrings/，用 deb822 .sources 檔並以 Signed-By 綁定
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc
sudo tee /etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: $(. /etc/os-release && echo "$VERSION_CODENAME")
Components: stable
Signed-By: /etc/apt/keyrings/docker.asc
EOF
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

**注意**：⚠️ 舊教學的 `apt-key add` 已在 22.04 廢棄、24.04 移除，**不要再用**；每個第三方庫都應用獨立的 `Signed-By` 金鑰。

🔵 Fedora 直接匯入廠商提供的 `.repo` 檔即可；沒有現成 `.repo` 的來源就手寫一個，並且 `gpgcheck=1` 不要關：

```bash
# 🔵 Fedora：直接匯入官方 .repo 檔（dnf5 語法）
sudo dnf config-manager addrepo --from-repofile=https://download.docker.com/linux/fedora/docker-ce.repo
# dnf4 舊語法：sudo dnf config-manager --add-repo https://download.docker.com/linux/fedora/docker-ce.repo
sudo dnf install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# 🔵 手動建立 .repo 檔
sudo tee /etc/yum.repos.d/example.repo <<'EOF'
[example]
name=Example Repo
baseurl=https://repo.example.com/fedora/$releasever/$basearch/
enabled=1
gpgcheck=1
gpgkey=https://repo.example.com/RPM-GPG-KEY-example
EOF
sudo rpm --import https://repo.example.com/RPM-GPG-KEY-example
```

### 3.3 社群套件庫：PPA vs COPR

**名詞與關係**：PPA 與 COPR 本質都是「由平台代為建置、附自己金鑰的第三方庫（§3.2）」，差別只在平台與加入方式：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **PPA（Personal Package Archive）** | 🟠 Launchpad 上任何人都能建立的個人套件庫，Canonical 的建置農場代為編譯出各版 Ubuntu 的 `.deb` | 加進來後就是一個普通的 apt 來源，只是被 `add-apt-repository` 包裝成一行指令 |
| **Launchpad** | Canonical 的開發協作平台，PPA、Ubuntu 的 bug 追蹤與原始碼託管都在上面 | `ppa:使用者/庫名` 就是它上面的路徑；Pinning 用的來源名 `LP-PPA-…`（§3.5）也由此而來 |
| **`add-apt-repository`** | 🟠 把 PPA 的 `.sources` 檔與金鑰一次寫好並執行 `apt update` 的工具 | 24.04 產生的 `.sources` 已用 `Signed-By`，不再碰全域 keyring |
| **COPR（Cool Other Package Repo）** | 🔵 Fedora 專案提供的個人建置服務與套件庫，定位對應 PPA | `dnf copr enable` 會在 `/etc/yum.repos.d/` 產生 `_copr_*.repo`，之後就是一般的 `.repo` |

**為什麼**：需要比發行版新的 Python、Neovim、starship 這類工具時，PPA / COPR 是最快的來源——有人已經幫你打包好。代價是這些庫由個人維護：沒有安全審查、維護者可能棄坑、也可能用同名套件蓋掉系統版本（例如 PPA 提供的 `python3` 取代系統的，之後 `apt` 本身就壞了）。PPA 是 Canonical 在 Launchpad 上的建置服務，COPR 是 Fedora 的對應物，兩者定位相同但工具不同。

**怎麼做**：

```bash
# 🟠 Ubuntu PPA（Launchpad）
sudo add-apt-repository ppa:deadsnakes/ppa     # 自動加 .sources 與金鑰，並 apt update
sudo add-apt-repository --remove ppa:deadsnakes/ppa
# 24.04 產生的檔案：/etc/apt/sources.list.d/deadsnakes-ubuntu-ppa-noble.sources

# 🔵 Fedora COPR
sudo dnf copr enable atim/starship
dnf copr list
sudo dnf copr disable atim/starship
sudo dnf copr remove atim/starship
```

**注意**：⚠️ PPA / COPR 都是個人維護、無官方安全審查；正式主機盡量避免，或至少釘住特定套件（見 §3.5）。大版本升級時 PPA 會被停用，且 COPR 不一定已為新版建置，升級前先盤點有哪些社群庫。

### 3.4 🔵 RPM Fusion（Fedora 必裝的第三方庫）

**名詞與關係**：這一節在講「發行版因授權不收的軟體去哪找」，兩邊的答案分別是外部庫與內建元件：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **RPM Fusion** | 🔵 Fedora 社群維護的第三方套件庫，分 `free`（授權自由但有專利疑慮，如完整版 ffmpeg）與 `nonfree`（閉源，如 NVIDIA 驅動）兩個 repo | 補 Fedora 官方庫因法律政策缺的那一塊；安裝方式是裝兩個 `-release` 套件（內含 `.repo` 與金鑰，§3.2） |
| **multiverse / restricted** | 🟠 官方庫內的兩個元件（§1）：restricted 放非自由驅動、multiverse 放專利或授權受限的軟體 | Ubuntu 願意自己收錄，所以不需要外部來源；`ubuntu-restricted-extras` 是把常用編解碼器與字型集合起來的 metapackage（§6） |
| **nouveau** | 社群逆向工程寫的開源 NVIDIA 驅動，核心內建 | 沒裝專有驅動時就是它在跑，效能與功能有限；專有驅動 `akmod-nvidia` 來自 RPM Fusion nonfree |
| **`dnf swap`** | 🔵 在同一筆交易內移除 A、安裝 B 的指令 | 把官方的 `ffmpeg-free` 換成 RPM Fusion 的完整 `ffmpeg` 就靠它；兩者互斥所以要 `--allowerasing` |
| **group（套件群組）** | 🔵 Fedora 定義的一組套件集合（如 `multimedia`、`development-tools`），`dnf group install` 一次裝齊 | 🟠 的對應概念是 metapackage：一個沒有實體檔案、只靠相依把一整組套件拉進來的空套件 |

**為什麼**：Fedora 因授權政策不收錄專利／非自由軟體（ffmpeg 完整版、NVIDIA 驅動、部分編解碼器），所以裝完 Fedora 後瀏覽器放不了 H.264 影片、NVIDIA 顯卡只能跑 nouveau，都是這個原因。Ubuntu 則把這類軟體放在自家的 `multiverse` / `restricted` 元件裡，不需另加來源。RPM Fusion 是 Fedora 社群長期維護的對應解法，幾乎所有桌面機都會裝。

**怎麼做**：

```bash
sudo dnf install -y \
  https://mirrors.rpmfusion.org/free/fedora/rpmfusion-free-release-$(rpm -E %fedora).noarch.rpm \
  https://mirrors.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-$(rpm -E %fedora).noarch.rpm
# 完整多媒體支援
sudo dnf swap ffmpeg-free ffmpeg --allowerasing
sudo dnf group install -y multimedia
```

**注意**：🟠 Ubuntu 對應：`multiverse` 元件（預設已啟用）+ `sudo apt install ubuntu-restricted-extras`。純伺服器（無顯示、不轉檔）不需要 RPM Fusion，少一個第三方來源就少一個風險。

### 3.5 版本釘選與優先權

**名詞與關係**：「釘版本」與「壓低某個來源的優先權」是兩件事，兩邊各有一套工具，對照如下：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **hold（`apt-mark hold`）** | 🟠 把套件標成「升級時跳過」，記在 dpkg 的 selections 狀態裡 | 最簡單的鎖版本方式；只管「不升」，不決定從哪個來源拿 |
| **Pinning / Pin-Priority** | 🟠 `/etc/apt/preferences.d/` 內的規則：對符合條件（套件名、來源 `o=`、版本）的候選版本指定優先權數字，apt 選版本時取最高者 | 同時解決「鎖版本」與「限制來源」；`>1000` 可強制降版、`<100` 表示除非明確指定否則不從該來源裝 |
| **`o=`（Origin）** | Pin 條件裡的來源識別字串，官方為 `Ubuntu`，PPA 為 `LP-PPA-<擁有者>-<庫名>`（§3.3） | 要從 `apt-cache policy` 的輸出抄；寫錯不會報錯，只是規則默默不生效 |
| **versionlock** | 🔵 dnf5 內建的鎖版本清單（`/etc/dnf/versionlock.toml`），列在清單內的套件升級時被排除 | 對應 🟠 hold；同樣會擋掉安全性更新 |
| **`priority=` / `exclude=`** | 🔵 `.repo` 檔內的欄位：priority 數字越小越優先（預設 99），exclude 列出該 repo 不提供的套件 | 對應 🟠 Pinning 裡的來源優先權；指令列上的 `--exclude` 則只有單次生效 |
| **DKMS 模組** | 隨 kernel 升級自動重新編譯的第三方核心模組（NVIDIA、ZFS，見 01 章 §1.5） | kernel 升級而模組重編失敗＝驅動消失，是「釘住 kernel」最常見的動機 |

**為什麼**：典型情境：正式環境的 nginx 已針對某版本調過模組與設定，不想被例行 `apt upgrade` 帶到新版；或 kernel 升級會讓自建的 DKMS / NVIDIA 模組失效，要等驗證過再升。另一個情境是加了 PPA 之後，不希望 PPA 裡「順便」帶的其他套件（例如 libssl）蓋掉系統版本，這時要把 PPA 的優先權壓低、只讓特定套件從它升級。apt 用 Pinning 檔案處理優先權，dnf 則直接在 `.repo` 檔用 `priority=` / `exclude=`。

**怎麼做**：

```bash
# 🟠 鎖定版本（升級時跳過）
sudo apt-mark hold nginx
apt-mark showhold
sudo apt-mark unhold nginx

# 🟠 APT Pinning：/etc/apt/preferences.d/
sudo tee /etc/apt/preferences.d/99-pin-nginx <<'EOF'
Package: nginx*
Pin: release o=Ubuntu
Pin-Priority: 1001          # >1000：即使降版也強制用此來源
EOF
# 讓某 PPA 只用於特定套件（其他套件不從該 PPA 升級）
sudo tee /etc/apt/preferences.d/90-ppa-low <<'EOF'
Package: *
Pin: release o=LP-PPA-deadsnakes
Pin-Priority: 100
EOF
apt-cache policy nginx        # 驗證

# 🔵 versionlock（dnf5 內建）
sudo dnf versionlock add nginx
dnf versionlock list
sudo dnf versionlock delete nginx
sudo dnf versionlock clear

# 🔵 套件庫優先權與排除
# 在 .repo 檔加：priority=10 （數字越小越優先，預設 99）
# 或 exclude=kernel* 排除特定套件
sudo dnf upgrade --exclude=kernel*    # 單次排除
```

**注意**：hold / versionlock 會讓安全性更新也一起被擋掉，釘了就要有人負責定期檢視；`apt-mark showhold` 與 `dnf versionlock list` 應納入巡檢清單。Pinning 改完務必用 `apt-cache policy` 驗證優先權，寫錯 `o=` 值不會報錯、只是默默不生效。

### 3.6 只安裝特定版本

**名詞與關係**：指定版本安裝要先看懂版本字串、再知道去哪列出可用版本：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **版本字串** | 套件的完整版本識別：🟠 `上游版本-打包版本`（如 `1.24.0-2ubuntu7`），🔵 `版本-release.發行版`（如 `1.26.3-1.fc44`）；「打包版本 / release」是發行版自己改打包方式時遞增的號碼 | 指定安裝時整串照抄；同一個上游版本可以有多個打包版本 |
| **`apt-cache madison` / `dnf list --showduplicates`** | 列出套件庫索引（§1 的 metadata）裡該套件所有可安裝版本的查詢指令 | 版本字串從這裡複製；🟠 鏡像通常只留最新版，所以常常只列得出一個 |
| **降版（downgrade）** | 安裝比目前已裝更舊的版本 | 🔵 `dnf downgrade` 直接支援；🟠 用 `apt install pkg=舊版` 即可；兩邊裝完都要釘住（§3.5），否則下次升級就蓋回去 |

**為什麼**：重現 bug、跟其他節點對齊版本、或升級後出問題要退回上一版時，需要指定精確版本號安裝。兩邊的版本字串格式不同（`.deb` 為 `上游版本-打包版本`，`.rpm` 為 `版本-release.發行版`），要先列出可用版本再複製貼上，不要用猜的。

**怎麼做**：

```bash
apt-cache madison nginx               # 🟠 列出可用版本
sudo apt install nginx=1.24.0-2ubuntu7 # 🟠
dnf list --showduplicates nginx       # 🔵
sudo dnf install nginx-1.26.3-1.fc44  # 🔵
sudo dnf downgrade nginx              # 🔵 降版
```

**注意**：🟠 Ubuntu 鏡像上通常只保留最新一版，舊版可能已下架、只能從 <https://launchpad.net> 手動抓；裝完特定版本後記得 `apt-mark hold` / `dnf versionlock add`，否則下次升級就被蓋回去。

---

## 4. 通用套件格式：Snap 與 Flatpak

**名詞與關係**：Snap 與 Flatpak 是「和發行版套件平行的第二套套件系統」，各自有來源、守護程序、runtime 與沙盒，先把三套系統並排：

```
                 發行版套件（.deb / .rpm）      🟠 Snap                              🔵 Flatpak
 來源            套件庫（§3）                   Snap Store（Canonical，唯一來源）       remote（Flathub 或發行版自有，可加多個）
 函式庫          共用系統的                     自帶 runtime（core22 等）               自帶 runtime（org.freedesktop.Platform 等）
 更新            apt / dnf 手動                snapd 自動 refresh（只能延後）           flatpak update 手動
 沙盒            無                            AppArmor + seccomp（--classic 可全關）   bubblewrap + portals（override 可放寬）
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Snap** | 🟠 Canonical 的通用套件格式：一個 `.snap` 是壓縮的唯讀映像，執行時掛成 loop 裝置 | 由 snapd 管理、只能從 Snap Store 取得；Ubuntu 桌面的 Firefox、Chromium 就是它 |
| **snapd** | 🟠 負責下載、掛載、自動更新（refresh）snap 的常駐服務 | 自動更新關不掉，只能 `--hold` 延後或用 `refresh.timer` 限制時段——這是伺服器上最大的顧慮 |
| **Flatpak** | 跨發行版的桌面應用打包格式與執行環境，由社群主導 | 🔵 Fedora Workstation 預設；也能裝到 Ubuntu 上，兩邊指令完全相同 |
| **remote / Flathub** | remote 是 Flatpak 的套件來源（對應 §1 的套件庫），Flathub 是最大的公共 remote | 一台機器可加多個 remote；Fedora 自有 remote 內容較少，多數人會再加 Flathub |
| **runtime** | Snap / Flatpak 各自提供的共用基底函式庫集合（如 `core22`、`org.freedesktop.Platform`），應用宣告自己依賴哪一個 | 應用之間共用 runtime，才不會每個 app 都帶一份函式庫；`flatpak uninstall --unused` 清的就是沒人用的 runtime |
| **沙盒（sandbox）** | 限制應用只能存取被授權的檔案、裝置與網路的隔離機制：Snap 用 AppArmor + seccomp，Flatpak 用 bubblewrap（namespace 隔離）+ portals（透過桌面環境代為開檔、存取裝置） | 「應用看不到你的家目錄」就是它造成的；Snap `--classic` 表示完全不沙盒，Flatpak 用 `override` 或 Flatseal 放寬 |

**為什麼**：發行版套件庫更新有週期，桌面應用（瀏覽器、IDE、通訊軟體）常需要最新版且與系統函式庫解耦，Snap / Flatpak 就是為此設計：自帶 runtime、沙盒隔離、跨發行版。兩邊預設不同源自廠商路線：Snap 由 Canonical 主導、後端 store 不開源，Ubuntu 甚至把 Firefox 改成 snap；Flatpak 由社群（Red Hat 工程師為主）與 Flathub 推動，Fedora 採 Flatpak 是自然結果。伺服器上這兩者都要三思：Snap 會自動更新且關不掉（只能延後），半夜升級 `lxd` 或 `microk8s` 造成服務中斷是實際發生過的事。

**怎麼做**：先看兩者定位差異，再依平台操作：

| | 🟠 Snap | 🔵 Flatpak |
|---|---|---|
| 維護者 | Canonical（後端 store 不開源） | 社群（Flathub） |
| 預設安裝 | Ubuntu 預設，Firefox、Thunderbird、Chromium 皆為 snap | Fedora Workstation 預設（Fedora 自有 remote + Flathub 需啟用） |
| 適合 | 桌面應用、CLI 工具、伺服器軟體（如 `lxd`、`microk8s`） | 桌面應用（GUI），不適合伺服器 / CLI |
| 沙盒 | AppArmor + seccomp | bubblewrap + portals |
| 缺點 | 啟動慢、`/snap` 掛載一堆 loop 裝置、自動更新無法關閉（只能延後） | 體積大（runtime 共用可減輕） |

```bash
# 🟠 Snap
snap find nvim
sudo snap install nvim --classic          # classic = 不沙盒（開發工具常需要）
snap list
sudo snap refresh                         # 手動更新
sudo snap refresh --hold=24h nvim         # 延後自動更新
snap revert nvim                          # 回上一版
sudo snap remove --purge nvim
sudo snap set system refresh.timer=sat,02:00-04:00   # 限制自動更新時段

# 🟠 Ubuntu 上改用 Flatpak（桌面）
sudo apt install -y flatpak gnome-software-plugin-flatpak
flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo

# 🔵 Flatpak
flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo
flatpak remotes                            # 實測：flathub  system
flatpak search vlc
flatpak install -y flathub org.videolan.VLC
flatpak run org.videolan.VLC
flatpak list
flatpak update -y
flatpak uninstall --unused                # 清無用 runtime
flatpak override --filesystem=home org.videolan.VLC   # 調整沙盒權限（或用 Flatseal GUI）

# 🔵 Fedora 上裝 snap（不建議，但可以）
sudo dnf install -y snapd && sudo ln -s /var/lib/snapd/snap /snap
```

**注意**：💡 **伺服器**：能用發行版原生套件就用原生套件；容器化服務用 Docker / Podman；Snap / Flatpak 留給桌面。若伺服器真的裝了 snap（如 `lxd`），至少用 `refresh.timer` 把更新時段限制在維護窗口內。

---

## 5. 安裝本機套件檔與其他格式

**名詞與關係**：這一節的東西依「離套件管理員多遠」排成一列：本機 `.deb` / `.rpm` 仍走套件系統，AppImage 與 tar.gz 則完全在系統之外：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **本機套件檔** | 自己下載到磁碟上的 `.deb` / `.rpm`，不在任何套件庫索引裡 | 相依（§1）仍要從套件庫補，所以要用 `apt install ./x.deb` / `dnf install ./x.rpm` 讓高階工具解相依 |
| **半安裝狀態** | 🟠 dpkg 已解開檔案但安裝腳本失敗或相依未滿足，套件在 `/var/lib/dpkg/status` 停在 `half-installed` / `unpacked` 的狀態 | 直接 `dpkg -i` 最常造成；此後每次 `apt` 都會先要求修復，修法見 §10 |
| **`dpkg-deb` / `rpm -qlp` / `rpm2cpio`** | 不安裝、只讀取或解開套件檔內容的工具：`dpkg-deb -c` 列檔案、`-x` 解到目錄；`rpm -qlp` 列檔案、`rpm2cpio` 轉成 cpio 封存再解開 | 安裝來路不明的檔案前，用它們看看要寫哪些路徑 |
| **`rpm -K`** | 🔵 用 rpmdb 裡已匯入的公鑰驗證 `.rpm` 檔簽章與摘要的指令 | 金鑰得先 `rpm --import`（§3.2）；🟠 `.deb` 本身不帶簽章，信任來自套件庫 metadata 的簽章 |
| **AppImage** | 把應用與所有函式庫打成單一可執行檔的格式，執行時用 FUSE 把自身掛載成檔案系統 | 完全不經套件管理員，所以沒有相依解析也沒有自動更新；需要系統先有 FUSE 函式庫 |
| **FUSE** | Filesystem in Userspace，讓一般程序在使用者空間掛載檔案系統的核心介面與函式庫 | AppImage 依賴它；🟠 24.04 套件名帶 `t64` 後綴是因為 64 位元 time_t 轉換而改名 |
| **`/opt` 與 `/usr/local`** | FHS（檔案系統階層標準）保留給「非套件管理員管理」軟體的目錄：`/opt/<軟體名>` 放整包自含軟體，`/usr/local/bin` 放要進 PATH 的連結或自編程式 | 套件管理員只寫 `/usr`，分開放才不會被升級蓋掉或互相覆蓋（§6 同理） |

**為什麼**：廠商只提供 `.deb` / `.rpm` 下載（Chrome、VS Code、內部工具）而沒有 repo 時，就得裝本機檔。關鍵是**用高階工具裝**：`dpkg -i` / `rpm -ivh` 不解相依，裝到一半失敗會留下「半安裝」狀態，是 §10 故障排除裡最常見的起因。AppImage 與 tar.gz 二進位則完全繞過套件系統——好處是免安裝，壞處是沒人追蹤版本與安全更新，得自己管理。

**怎麼做**：

```bash
# 🟠 .deb（用 apt 而非 dpkg -i，才會自動解相依）
sudo apt install ./google-chrome-stable_current_amd64.deb
sudo dpkg -i pkg.deb && sudo apt install -f     # 舊做法：dpkg 裝完再修相依
apt-get download hello                          # 只下載不安裝
dpkg-deb -c pkg.deb                             # 看內容
dpkg-deb -x pkg.deb ./out                       # 解開不安裝

# 🔵 .rpm
sudo dnf install ./google-chrome-stable_current_x86_64.rpm
dnf download hello                              # 只下載
rpm -qlp pkg.rpm                                # 看內容
rpm2cpio pkg.rpm | cpio -idmv                   # 解開不安裝
sudo rpm -ivh pkg.rpm                           # 低階安裝（不建議，不解相依）
rpm -K pkg.rpm                                  # 驗簽章

# ⚪ AppImage（免安裝，需 FUSE）
sudo apt install -y libfuse2t64        # 🟠 24.04 套件名有 t64 後綴
sudo dnf install -y fuse fuse-libs     # 🔵
chmod +x App.AppImage && ./App.AppImage

# ⚪ tar.gz 二進位：放 /opt/，連結到 /usr/local/bin/
sudo tar -C /opt -xzf app-1.0-linux-amd64.tar.gz
sudo ln -sf /opt/app-1.0/bin/app /usr/local/bin/app
```

**注意**：路徑前的 `./` 不能省——`apt install google-chrome.deb` 會被當成套件名去 repo 找。來路不明的本機檔先用 `dpkg-deb -c` / `rpm -qlp` 看它要寫哪些路徑，再用 `rpm -K` 驗簽章。tar.gz 放 `/opt/` 而非 `/usr/`，是為了不跟套件管理員管的檔案混在一起。

---

## 6. 原始碼編譯

**名詞與關係**：自己編譯牽涉「工具鏈、建置相依、安裝位置、事後打包」四個環節，先看它們的先後順序：

```
建置工具鏈（compiler、make、autotools）─────┐
  🟠 build-essential（metapackage）          │
  🔵 development-tools（group）              ├──▶ ./configure --prefix=/usr/local ──▶ make ──▶ make install ──▶ /usr/local/*
建置相依（-dev / -devel 標頭檔與函式庫）──────┘                                                                │
  🟠 apt build-dep（讀 deb-src 索引）                                             改為打包再部署：checkinstall / dpkg-buildpackage / rpmbuild ──▶ .deb / .rpm
  🔵 dnf builddep（讀 .spec）
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **build-essential / development-tools** | 🟠 `build-essential` 是一個 **metapackage**（本身沒有檔案，只靠相依把 gcc、g++、make、libc6-dev 拉進來的空套件）；🔵 對應的是 group（§3.4）`development-tools` | 編譯任何 C / C++ 軟體的最低需求；兩邊都是「一個名字裝一整組」 |
| **建置相依 / `-dev` 套件** | 編譯時才需要、執行時不需要的東西：標頭檔（`.h`）、靜態庫、`pkg-config` 描述檔；🟠 命名為 `libxxx-dev`、🔵 為 `xxx-devel` | 與執行相依（§1）不同，發行版把它們拆成獨立套件，所以自己編時得另外裝 |
| **`build-dep` / `builddep`** | 讀取「該套件打包時宣告的建置相依」並一次裝齊的子命令 | 🟠 資料來自 deb-src 索引（§3.1），所以要先開；🔵 資料在 `.spec` 裡，dnf5 由 `dnf5-plugins` 提供 |
| **`./configure --prefix` / `make install`** | autotools 的標準三步：`configure` 檢查環境並產生 Makefile，`--prefix` 決定安裝根目錄；`make` 編譯；`make install` 把成品複製到 prefix 下 | 很多專案預設 prefix 是 `/usr`，所以要明講 `/usr/local`；裝進 `/usr/local/lib` 後要 `ldconfig` 更新動態連結器的快取 |
| **checkinstall** | 🟠 攔截 `make install` 寫入的檔案清單，順手打成可用 `apt remove` 移除的簡易 `.deb` | 最省事但沒有正確的相依與 metadata；正規做法是 `dh_make` 產生 `debian/` 目錄後 `dpkg-buildpackage` |
| **`.spec` / rpmbuild** | 🔵 `.spec` 是描述「怎麼取得原始碼、編譯、安裝、列出檔案」的 RPM 打包腳本；`rpmbuild -ba` 依它產出 `.rpm`；`rpmdev-setuptree` 建立 `~/rpmbuild/` 的標準目錄 | Fedora 官方所有套件都是這樣打出來的；`dnf builddep` 也能直接讀 `.spec` |
| **fpm** | 用一行指令把目錄或語言套件轉成 `.deb` / `.rpm` 的跨格式打包工具 | 不走各發行版的正規流程，適合內部工具快速產出 |

**為什麼**：需要套件庫沒有的版本、要開啟特定編譯選項（例如 nginx 加第三方模組）、或軟體根本沒人打包時，只能自己編。最大的坑是 `make install` 直接寫進 `/usr/`，之後跟套件管理員互相覆蓋、想移除時也找不到裝了哪些檔——所以一律用 `/usr/local` 前綴。`build-dep` / `builddep` 能一次裝齊某套件的建置相依，比自己一個個補 `-dev` 套件快得多。

**怎麼做**：

```bash
# 🟠 建置工具鏈
sudo apt install -y build-essential pkg-config cmake git autoconf automake libtool
sudo apt build-dep nginx            # 安裝某套件的建置相依（需 deb-src 啟用）

# 🔵
sudo dnf group install -y development-tools          # 實測：group id 為 development-tools
sudo dnf install -y cmake pkgconf-pkg-config git
sudo dnf builddep nginx             # 需 dnf5-plugins（預設已含）

# ⚪ 標準三步驟
./configure --prefix=/usr/local
make -j"$(nproc)"
sudo make install
# 用 /usr/local 前綴避免與套件管理衝突；/usr/local/lib 需 sudo ldconfig
```

**注意**：💡 正式主機建議把自編軟體**打包成 .deb / .rpm** 再部署，方便版本控管與移除（多台主機時尤其重要，不必每台都裝編譯器）：
- 🟠 `checkinstall`（簡易）或 `dh_make` + `dpkg-buildpackage`（正規）。
- 🔵 `rpmdev-setuptree` + 撰寫 `.spec` + `rpmbuild -ba`（`rpmdevtools` 套件）；或 `fpm`（跨格式）。

---

## 7. 語言生態系套件管理（兩者共通）

**名詞與關係**：語言生態系的工具都在回答同一個問題——「怎麼裝新版又不碰系統用的那一份」，Python 的名詞最多，其他語言各一個：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **PEP 668 / externally-managed-environment** | Python 官方規範：發行版可在系統 Python 放一個 `EXTERNALLY-MANAGED` 標記檔，`pip` 看到就拒絕安裝到系統目錄並印出這個錯誤 | 保護 apt / dnf 等系統工具依賴的 Python 函式庫；`--break-system-packages` 就是強行跳過它 |
| **venv** | Python 內建的虛擬環境：在指定目錄建一份獨立的 `site-packages` 與 `pip`，`activate` 後安裝都進那裡 | 給專案用；不動系統 Python，所以不會觸發 PEP 668 |
| **pipx** | 專給「命令列工具」用的安裝器：每個工具自動建一個 venv，再把執行檔連結到 `~/.local/bin` | ansible、black 這類工具的正確裝法；和 venv 的差別是使用者不必自己 activate |
| **nvm / fnm / NodeSource** | nvm 與 fnm 是在家目錄安裝多版本 Node.js 並切換的工具（分別為 shell 腳本與 Rust 實作）；NodeSource 是提供各版 Node 的第三方套件庫（§3.2） | 三者都為了繞過發行版 Node 版本太舊；nvm 走 `curl | bash`，NodeSource 走套件管理員 |
| **rustup** | Rust 官方的工具鏈管理器，裝在 `~/.cargo`，可切換 stable / nightly | Rust 更新頻繁、發行版套件跟不上，官方推薦它而非 `apt install rustc` |
| **alternatives** | 🟠 `update-alternatives` / 🔵 `alternatives`：以符號連結管理「同一個指令名有多個實作」的機制（`/usr/bin/java` → `/etc/alternatives/java` → 實際版本） | 多版本 JDK 並存時靠它切換預設；兩邊指令名不同、機制相同 |
| **deadsnakes** | 🟠 提供多版本 Python 的知名 PPA（§3.3） | 🔵 Fedora 官方庫本身就有 `python3.12`、`python3.13`，不需要社群庫 |

**為什麼**：`apt`、`dnf`、`firewall-cmd`、Ansible 等系統工具本身就是 Python 寫的，`sudo pip install` 把系統 Python 的函式庫換掉，最壞情況是套件管理員直接壞掉、連修復用的工具都跑不起來——這就是兩個發行版都啟用 PEP 668 保護的原因。Node.js 的情況不同：發行版打包版常落後 LTS 好幾版，前端專案通常直接用 nvm 管多版本。Rust / Go / Java 則各有官方推薦的安裝方式，這裡列的是最省事、日後也最容易升級的做法。

**怎麼做**：

```bash
# Python：不要用 sudo pip 動系統 Python（兩者都有 PEP 668 保護 "externally-managed-environment"）
sudo apt install -y python3-venv python3-pip pipx   # 🟠
sudo dnf install -y python3-pip pipx                # 🔵
python3 -m venv ~/.venvs/proj && source ~/.venvs/proj/bin/activate
pipx install ansible-core                           # CLI 工具用 pipx 裝到獨立環境
# 多版本 Python：🟠 deadsnakes PPA；🔵 直接 dnf install python3.12 python3.13（官方庫有多版本）

# Node.js：發行版版本常太舊，建議 nvm 或官方 NodeSource / fnm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
nvm install --lts
# 🔵 Fedora 官方庫也有多版本：dnf install nodejs22

# Rust / Go
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
sudo apt install -y golang-go   # 🟠     /   sudo dnf install -y golang   # 🔵

# Java
sudo apt install -y openjdk-21-jdk              # 🟠
sudo dnf install -y java-21-openjdk-devel       # 🔵
sudo update-alternatives --config java          # 🟠   / sudo alternatives --config java   # 🔵
```

**注意**：看到 `--break-system-packages` 這個參數就是警訊，不要為了省事加上去。`curl | bash` 這種安裝法等於直接執行網路上的腳本，正式主機上請先下載、看過內容再執行，或改用發行版套件。

---

## 8. 常見軟體安裝速查

**名詞與關係**：表格裡除了套件名，還會碰到 Fedora「裝完還要放行」的幾個機制，以及幾個兩邊命名不同的軟體：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **firewalld** | 🔵 Fedora 預設的防火牆管理服務，以「zone + service」描述放行規則，`firewall-cmd` 是它的指令 | 裝完網頁伺服器還要 `--add-service=http` 才通；🟠 Ubuntu 的 ufw 預設未啟用，所以沒這一步（詳見 05 章） |
| **SELinux boolean / 檔案標籤** | 🔵 SELinux 是核心層的強制存取控制：每個檔案與程序都帶標籤，政策決定誰能碰誰；boolean 是政策內可開關的預設選項（如 `httpd_can_network_connect`） | 反向代理連後端、換資料目錄都會撞到；`setsebool -P` 改開關，`semanage fcontext` + `restorecon` 改標籤（詳見 16 章） |
| **`ausearch -m avc`** | 從 audit 日誌撈出 SELinux 拒絕紀錄（AVC）的指令 | 服務起不來時判斷「是不是 SELinux 擋的」的第一步；`setenforce 0` 是全關，只該用來驗證假設 |
| **`postgresql-setup --initdb`** | 🔵 Fedora 打包的 PostgreSQL 初始化腳本，建立 `/var/lib/pgsql/data` 資料目錄 | 🟠 Debian 打包在安裝腳本裡自動做了；Fedora 交給管理員決定何時、在哪初始化 |
| **Valkey** | Redis 在 2024 年改為非開源授權後，由 Linux 基金會維護的相容分支 | 🔵 Fedora 41+ 因授權政策移除 `redis`，套件與服務名變成 `valkey`；🟠 Ubuntu 24.04 仍打包 `redis-server` |
| **Podman** | Red Hat 主導、指令與 Docker 相容、不需常駐守護程序的容器引擎 | 🔵 Workstation 預設安裝，是 Fedora 上的容器首選；用它就不必加 Docker 官方 repo（§3.2） |
| **`ubuntu-drivers` / `akmod-nvidia`** | 🟠 `ubuntu-drivers` 偵測硬體並從 restricted 元件挑合適的專有驅動；🔵 `akmod-nvidia` 是 RPM Fusion（§3.4）的 NVIDIA 驅動，由 akmods 在每次 kernel 升級後自動重編 | 兩者都是第三方核心模組，Secure Boot 下需 MOK 簽章（01 章 §1.5） |

**為什麼**：同一個軟體在兩邊的套件名、服務名常不一樣（`apache2` vs `httpd`、`redis-server` vs `valkey`），照另一邊的記憶敲指令就會「找不到套件」；更麻煩的是 Fedora 裝完服務通常**還沒完**——firewalld 預設擋住、SELinux 預設不讓 nginx 連後端、PostgreSQL 不會自動初始化資料目錄，這些 Ubuntu 上「裝完就能用」的事在 Fedora 都要多做一步。差異來自兩邊的預設安全姿態：Ubuntu 傾向裝完即啟用、防火牆預設不開；Fedora 繼承 RHEL 的 firewalld + SELinux 全面啟用，服務要明確放行。

**怎麼做**：

| 軟體 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| Nginx | `apt install nginx` | `dnf install nginx` + `firewall-cmd --add-service=http` + `setsebool -P httpd_can_network_connect 1`（反代時） |
| Apache | `apt install apache2`（服務 `apache2`） | `dnf install httpd`（服務 `httpd`） |
| PostgreSQL | `apt install postgresql`（自動初始化並啟動） | `dnf install postgresql-server` → **`sudo postgresql-setup --initdb`** → `systemctl enable --now postgresql` |
| MariaDB | `apt install mariadb-server` | `dnf install mariadb-server` |
| Redis | `apt install redis-server`（服務 `redis-server`） | `dnf install valkey`（Fedora 41+ 已無 `redis` 套件，由相容的 **Valkey** 取代；服務 `valkey`） |
| Docker | 官方 repo（§3.2）或 `apt install docker.io`（Ubuntu 打包版） | 官方 repo（§3.2）；或用 Podman（預設安裝於 Workstation） |
| Podman | `apt install podman` | `dnf install podman`（預設） |
| PHP | `apt install php-fpm php-mysql` | `dnf install php-fpm php-mysqlnd` |
| NVIDIA 驅動 | `sudo ubuntu-drivers install`（自動選版本） | RPM Fusion：`dnf install akmod-nvidia xorg-x11-drv-nvidia-cuda` |
| VirtualBox | `apt install virtualbox` | RPM Fusion：`dnf install VirtualBox` |
| Wireshark | `apt install wireshark` + `usermod -aG wireshark $USER` | `dnf install wireshark` + `usermod -aG wireshark $USER` |
| Cockpit | `apt install cockpit` | 預設（Server） |
| ZFS | `apt install zfsutils-linux`（核心內建） | 非官方：<https://openzfs.github.io/openzfs-docs/Getting%20Started/Fedora/> |

**注意**：🔵 Fedora 上裝資料庫、網頁伺服器後**多了兩件事**：開 firewalld 服務、確認 SELinux boolean / 檔案標籤。例如 PostgreSQL 要換資料目錄時需 `semanage fcontext -a -t postgresql_db_t "/data/pgsql(/.*)?"` + `restorecon -R /data/pgsql`。服務起不來時先看 `journalctl -u 服務名` 與 `ausearch -m avc -ts recent`，不要急著 `setenforce 0`。

---

## 9. 發行版大版本升級

**名詞與關係**：大版本升級兩邊工具不同、機制也不同，先分清「線上」與「離線」：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **`do-release-upgrade`** | 🟠 Ubuntu 的升版工具：把 `.sources` 的 Suites 代號換成新版後，在**執行中的系統**上以 apt 把所有套件換掉 | 線上升級；讀 `/etc/update-manager/release-upgrades` 決定升到下一個 LTS 還是下一個半年版 |
| **`dnf system-upgrade`** | 🔵 dnf 插件：先把新版所有套件下載到本機，重開機進入極簡的離線環境套用，再開回新系統 | 離線升級；與 §2 的 `dnf offline-upgrade` 是同一機制，只是換成跨版本 |
| **線上 / 離線升級** | 線上＝在正常執行的系統上替換套件，服務照跑；離線＝重開進特殊環境、沒有服務在跑時替換 | 兩邊機制的根本差異：線上停機短但怕被執行中的服務干擾或 SSH 斷線；離線反之 |
| **支援週期** | 發行版承諾提供安全性更新的期間：🟠 LTS 5 年、🔵 每版約 13 個月（下下版釋出後一個月） | 過期就沒有更新，是不得不升級的根本原因 |
| **`.1` 點釋出** | 🟠 LTS 首版發布約 3～4 個月後的第一次整合更新（如 24.04.1） | Canonical 要等它出來才對上一個 LTS 開放升級提示，之前得加 `-d` 強升 |

**為什麼**：Ubuntu LTS 有 5 年支援，Fedora 每版只有 13 個月，到期後就沒有安全性更新——弱點掃描會直接把整台主機標紅。重灌成本高（重建設定、資料搬移、服務中斷），原地升級是常態，但它是套件管理最高風險的操作：第三方庫可能還沒有新版套件、設定檔格式可能改變、SSH 中途斷線會讓升級卡在半途。兩邊機制不同：Ubuntu 的 `do-release-upgrade` 是**線上**升級，系統邊跑邊換套件；Fedora 的 `dnf system-upgrade` 是先下載、再重開機進入**離線**環境套用，比較不會被執行中的服務干擾。

### 🟠 Ubuntu：`do-release-upgrade`

**名詞與關係**：指令裡的幾個設定與參數，決定「升到哪一版」與「斷線了怎麼辦」：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **`Prompt=`（`release-upgrades`）** | 🟠 `/etc/update-manager/release-upgrades` 的設定值：`lts` 只提示 LTS→LTS、`normal` 提示每個半年版、`never` 不提示 | `do-release-upgrade` 讀它決定目標版本；伺服器一律 `lts` |
| **`-d`（devel release）** | 允許升到開發中版本、或尚未開放提示的版本 | `.1` 點釋出前（§9）LTS→LTS 就得加它；非 LTS 主機加了會升到未穩定版 |
| **備援 sshd（1022 埠）** | 升級程式在 SSH 環境下額外啟動的第二個 sshd，監聽 1022 | 主 sshd 在升級中重啟失敗時的逃生口；防火牆沒放行等於沒有 |
| **tmux / screen** | 終端多工器：程序掛在它底下，SSH 斷線後仍繼續執行 | 線上升級（§9）最怕中途斷線，所以務必在它裡面跑 |

**怎麼做**：

```bash
sudo apt update && sudo apt full-upgrade -y && sudo reboot   # 先更新到最新
cat /etc/update-manager/release-upgrades      # Prompt=lts 只升 LTS→LTS；normal 升到下一版
sudo do-release-upgrade                       # 互動式（建議在 tmux / screen 內執行，SSH 斷線不中斷）
sudo do-release-upgrade -d                    # 升到開發中版本（.1 發布前 LTS→LTS 需要此參數）
```

**注意**：
- LTS → 下一個 LTS 的升級提示通常在新版 **.1 點釋出後**（約 8 月）才開放。
- 升級會在 SSH 環境額外開 1022 埠的備援 sshd；請先 `ufw allow 1022/tcp`。
- 第三方 PPA 會被停用，升級後手動重新啟用。

### 🔵 Fedora：`dnf system-upgrade`

**名詞與關係**：升級本身只有兩行，指令列後半段是升級「之後」的清理，各自對應一種殘留：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **`$releasever`** | 🔵 dnf 的變數，展開為目前 Fedora 版本號，`.repo` 檔的 URL 用它組路徑 | `system-upgrade --releasever=45` 就是把它暫時設成目標版，所有用此變數的 repo 自動指向新版路徑 |
| **`.rpmnew` / `.rpmsave`** | 升級時若設定檔曾被管理員改過：rpm 通常把新版另存為 `.rpmnew`、保留你的；若套件要求必須用新版，則把你的改名 `.rpmsave` | 大版本升級後 `/etc` 會散落一堆，`rpmconf -a` 逐一互動比對、合併（🟠 apt 是在升級當下就問 keep / install） |
| **退役套件（retired package）** | 上游停止維護、Fedora 從新版套件庫移除的套件 | 升級後仍留在本機但不再有更新，`remove-retired-packages` 幫你列出並移除 |
| **`--allowerasing` / `--disablerepo`** | 前者允許 dnf 移除阻礙交易的套件；後者本次不讀某個 repo | 第三方 repo（§3.2）還沒新版套件時的兩種解法：換掉衝突套件，或先不管那個 repo |

**怎麼做**：

```bash
sudo dnf upgrade --refresh -y && sudo reboot
sudo dnf system-upgrade download --releasever=45     # 下載下一版所有套件（可跨兩版，如 43→45）
sudo dnf system-upgrade reboot                        # 重開機進入離線升級
# 完成後
sudo dnf system-upgrade clean
sudo dnf autoremove -y
sudo rpmconf -a                                       # 處理 .rpmnew / .rpmsave（需 dnf install rpmconf）
sudo dnf install -y remove-retired-packages && remove-retired-packages   # 清除已退役套件
```

**注意**：
- 第三方 repo（RPM Fusion、Docker、COPR）多半用 `$releasever` 變數，會自動跟著切換；若某 repo 尚無新版套件，加 `--allowerasing` 或暫時 `--disablerepo=xxx`。
- Fedora 每版只支援 13 個月，**必須定期升級**；建議每年至少升一次、一次只跨一版。
- 兩邊共通：升級前先做快照或完整備份（見第 11 章），並確認 `/boot` 有足夠空間放新 kernel。

---

## 10. 套件系統故障排除

**名詞與關係**：故障排除的每條指令都對應 §1 管線上的某一段壞掉——鎖檔、資料庫、金鑰、或 repo 之間的衝突：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **`lock-frontend` 與 `unattended-upgrades`** | 🟠 `lock-frontend` 是 apt 系列前端共用的鎖檔（§1）；`unattended-upgrades` 是每日自動安裝安全性更新的服務，由 `apt-daily-upgrade.timer` 觸發 | 「Could not get lock」九成是它正在跑；等它做完而非砍鎖，砍鎖會留下半安裝（§5） |
| **`dpkg --configure -a`** | 🟠 把所有停在「已解開但未設定」的套件重新跑安裝腳本 | 半安裝的第一步；`apt install -f`（= `--fix-broken`）再補相依 |
| **`/var/backups/dpkg.status.*`** | 🟠 每天自動備份的 dpkg 資料庫副本 | 狀態檔損毀時直接拿最近的一份覆蓋回去 |
| **rpmdb / `--rebuilddb`** | 🔵 rpm 的本機資料庫（§1）；`rpm --rebuilddb` 從現有內容重新建立索引 | 磁碟滿或斷電後出現 BDB / sqlite 錯誤時的標準修法 |
| **`--nogpgcheck`** | 🔵 本次交易略過金鑰驗簽 | 只能用來確認「是不是金鑰問題」；正解是重新 `rpm --import`（§3.2） |
| **file conflicts / 被保護的套件** | 前者是兩個套件要寫同一個路徑（多半是不同 repo 的同名軟體）；後者是 dnf 拒絕移除的核心套件（`dnf`、`kernel`、`systemd`，列於 `/etc/dnf/protected.d/`） | `--allowerasing` 讓 dnf 移除衝突方，但碰到被保護的套件仍會拒絕 |
| **`distro-sync`** | 🔵 把所有已裝套件強制對齊到目前啟用的 repo 所提供的版本，包含降版 | 換掉或關掉第三方庫後，用來把「來源已消失」的版本拉回官方版 |
| **`debsums` / `rpm -Va`** | 用套件資料庫記錄的 checksum 與權限，逐檔比對已安裝檔案有沒有被改動 | 入侵或磁碟錯誤的快篩；設定檔被改是正常的，二進位被改才要警覺 |

**為什麼**：套件系統壞掉的典型場景：`apt` 被背景的 `unattended-upgrades` 佔住鎖、`dpkg -i` 裝到一半斷電留下半安裝狀態、`/var` 滿了造成 rpm 資料庫損毀、換了第三方庫後 GPG 驗證失敗、或兩個 repo 提供同名檔案打架。這些狀況都有標準修法，硬砍鎖檔或 `--nogpgcheck` 一路開下去反而會把小問題變成重灌。最後兩條 `debsums` / `rpm -Va` 則是懷疑被入侵或磁碟出錯時，用來確認系統檔案有沒有被動過。

**怎麼做**：依錯誤訊息對號入座：

```bash
# 🟠 「Could not get lock /var/lib/dpkg/lock-frontend」
sudo lsof /var/lib/dpkg/lock-frontend        # 找出誰在用（通常是 unattended-upgrades 或 apt-daily）
systemctl status apt-daily.service apt-daily-upgrade.service
# 等它結束；⚠️ 不要直接 rm lock 檔，除非確認沒有程序在跑

# 🟠 半安裝 / 相依損壞
sudo dpkg --configure -a
sudo apt install -f
sudo apt --fix-broken install
sudo apt clean && sudo apt update             # 索引損壞：清掉重抓

# 🟠 dpkg 狀態檔損壞
sudo cp /var/backups/dpkg.status.0 /var/lib/dpkg/status

# 🔵 rpm 資料庫損壞（"rpmdb: BDB0087" 或 sqlite 錯誤）
sudo rpm --rebuilddb
sudo dnf clean all && sudo dnf makecache

# 🔵 GPG 金鑰問題
sudo rpm --import /etc/pki/rpm-gpg/RPM-GPG-KEY-fedora-$(rpm -E %fedora)-primary
sudo dnf install --nogpgcheck pkg             # ⚠️ 僅測試用

# 🔵 交易衝突（file conflicts / 被保護的套件）
sudo dnf upgrade --allowerasing               # 允許移除衝突套件
sudo dnf upgrade --best --skip-broken         # 跳過壞掉的
sudo dnf distro-sync                          # 強制同步到 repo 版本（升降版都做）

# ⚪ 驗證已安裝檔案是否被改動
sudo debsums -c                               # 🟠（apt install debsums）
sudo rpm -Va                                  # 🔵（S=大小 M=權限 5=checksum 變動）
```

**注意**：`--allowerasing` 會真的移除套件，執行前先看清楚交易摘要列出的「移除」清單；`rpm -Va` 對設定檔（`c` 標記）報 `5` 是正常的（你自己改過），對 `/usr/bin` 下的二進位報 `5` 才需要警覺。

---

## 11. 本章差異總結

**為什麼**：本章差異多半不是「哪邊比較好」，而是兩個上游的路線選擇：Debian / Ubuntu 以政策手冊與 dpkg 為核心，強調來源可信與格式明確（deb822 + `Signed-By`），但回滾與離線升級得靠外部工具；Fedora 承襲 RHEL 生態，dnf 內建交易歷史、versionlock、離線升級，安全姿態（firewalld、SELinux）預設全開，代價是非自由軟體要另找 RPM Fusion、裝完服務還要多做放行。下表把本章各節的關鍵差異收攏成一頁，方便在兩種主機間切換時快速查閱。

**怎麼做**：

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 高階工具 | `apt` / `apt-get` | `dnf`（dnf5） |
| 更新索引 | 手動 `apt update` | 自動 |
| 回滾交易 | 無 | `dnf history undo` |
| 套件庫格式 | deb822 `.sources` + `Signed-By` | `.repo` INI + `gpgkey=` |
| 加第三方 repo | 手寫 `.sources` 檔 | `dnf config-manager addrepo --from-repofile=URL` |
| 社群庫 | PPA（`add-apt-repository`） | COPR（`dnf copr enable`） |
| 非自由軟體 | multiverse / restricted（內建） | RPM Fusion（需手動加） |
| 版本鎖定 | `apt-mark hold` + Pinning | `dnf versionlock` + `priority=` / `exclude=` |
| 群組 | metapackage（`build-essential`） | `dnf group install development-tools` |
| 通用套件 | Snap（預設） | Flatpak（預設） |
| 本機檔安裝 | `apt install ./x.deb` | `dnf install ./x.rpm` |
| 建置相依 | `apt build-dep`（需 deb-src） | `dnf builddep` |
| 需重開機判斷 | `needrestart` / `/var/run/reboot-required` | `dnf needs-restarting -r` |
| 大版本升級 | `do-release-upgrade`（線上） | `dnf system-upgrade`（離線重開機） |
| 資料庫修復 | `dpkg --configure -a` / `apt -f install` | `rpm --rebuilddb` / `dnf distro-sync` |
| 檔案完整性 | `debsums` | `rpm -Va` |
| PostgreSQL 初始化 | 自動 | 需 `postgresql-setup --initdb` |
| Redis | `redis-server` | `valkey`（Fedora 41+） |
