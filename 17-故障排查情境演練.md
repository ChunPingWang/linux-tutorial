# 17 - 故障排查情境演練

前面各章的故障排除表是「症狀 → 處理」的查表；本章反過來，用 **真實的告警 / 使用者回報** 當起點，練習「先假設什麼、依什麼順序驗證、看到什麼輸出就該轉向」的思考過程。每個情境都附 **可在實驗機注入故障的腳本**（`scripts/lab/scenarios/NN-*.sh inject`），你可以先不看解答自己排查，再用 `status` 對照、`solve` 還原。15 個可注入情境已於 Ubuntu 24.04 與 Fedora 44 實驗機實測，文中的輸出即為實測結果；另有 5 個無法在容器模擬的情境（開機、SELinux、硬體、時間）以文字推演。

**名詞與關係**：本章沿用前面章節的名詞，這裡只補排查方法本身的幾個概念：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **症狀（symptom）** | 使用者或監控觀察到的現象（「連不上」「變慢」「磁碟滿」） | 排查的起點；一個症狀通常對應多個可能的根因 |
| **假設（hypothesis）** | 對根因的猜測，必須可被一個指令驗證或排除 | 排查 = 依「機率 × 驗證成本」排序假設，逐一驗證 |
| **根因（root cause）** | 移除後症狀不再出現、且能解釋所有觀察的那個原因 | 修復針對根因；「重啟就好了」通常只是清除症狀 |
| **分層排查** | 由下而上（硬體 → 核心 → 儲存 → 網路 → 服務 → 應用）或由外而內（客戶端 → 網路 → 主機 → 程序）逐層縮小範圍 | 12 章 §2 的方法論；本章每個情境都標出「從哪一層開始、為什麼」 |
| **最近變更** | 上次正常到現在之間發生的部署、升級、設定修改 | 多數故障的直接來源；排查第一步永遠先問這個 |
| **注入（inject）/ 還原（solve）** | 本章腳本把故障做出來 / 拆掉的兩個動作 | `status` 顯示關鍵觀察值，供對照自己的排查結果 |

**為什麼要用情境練習**：查表式的知識在真實事故中最大的問題是「不知道該先看哪裡」——磁碟滿可能是日誌、可能是已刪除但被開啟的檔案、可能是 thin pool，指令都會但順序錯就多花一小時。情境演練訓練的是**排序假設**與**看到輸出後轉向**的能力，這只能靠親手做。

**怎麼做**：

```bash
# 在實驗機（scripts/lab/README.md 的 systemd 容器，或任何測試 VM）
sudo bash scripts/lab/scenarios/01-disk-full.sh inject     # 注入故障，只會印出「使用者回報」等級的提示
#   → 自己排查……
sudo bash scripts/lab/scenarios/01-disk-full.sh status     # 對照：腳本認為的關鍵觀察值
sudo bash scripts/lab/scenarios/01-disk-full.sh solve      # 還原環境
```

⚠️ 情境腳本會真的改設定（sshd、fstab、resolv.conf、防火牆），**只在實驗機執行**。多數情境依賴 04 章部署的 `myapp.service`（`scripts/lab/lab-systemd-test.sh` 會建好）。

## 情境總覽

| # | 回報 / 告警 | 練習重點 | 起手層 | 章節 |
|---|---|---|---|---|
| 01 | 磁碟空間 100% 告警，但找不到大檔 | `df` vs `du` 的差異、已刪除但開啟的檔案 | 儲存 | 06、12 |
| 02 | 部署後服務反覆重啟 | `systemctl status` 讀法、埠衝突 | 服務 | 04 |
| 03 | 新站台上線沒生效 | 設定檔語法檢查、reload 不報錯的陷阱 | 服務 | 03、04 |
| 04 | 強化 SSH 後全員登不進 | 保留 session、`sshd -T`、AllowGroups | 服務 | 02、08 |
| 05 | 套件安裝與 curl 全失敗，但 ping IP 正常 | DNS 分層、`resolv.conf` 來源 | 網路 | 05 |
| 06 | 本機正常、外部連不到 | 監聽位址、防火牆、由外而內 | 網路 | 05 |
| 07 | cron 排程「沒有跑」 | cron 環境差異、輸出去哪 | 排程 | 04 |
| 08 | timer 從未觸發 | `list-timers`、unit 驗證、enable vs start | 排程 | 04 |
| 09 | 服務每隔幾秒被殺 | OOM 判讀、cgroup 限制 | 記憶體 | 04、09 |
| 10 | 主機變慢、load 飆高 | `top` → cgroup → 來源 unit | CPU | 09 |
| 11 | `apt update` / `dnf` 全部失敗 | 套件庫隔離、`skip_if_unavailable` | 套件 | 03 |
| 12 | 寫入報 I/O error，`df` 卻有空間 | thin pool、`lvs` 判讀 | 儲存 | 06、15 |
| 13 | 新同事無法 sudo | 群組、sudoers 檢查 | 帳號 | 02 |
| 14 | journal 暴增、`journalctl` 變慢 | 找最吵的 unit、限流 | 日誌 | 07 |
| 15 | 加新碟到 fstab 後擔心重開機 | 重開機前的驗證 | 儲存 | 06、12 |
| 16 | 重開機後停在 `grub rescue>`（推演） | 開機鏈分層 | 開機 | 12、14 |
| 17 | 🔵 Fedora 上服務讀不到搬過來的檔案（推演） | SELinux 標籤 vs 權限 | MAC | 08、16 |
| 18 | 憑證錯誤 / Kerberos 失敗（推演） | 時間偏差 | 時間 | 02 |
| 19 | 間歇性網路中斷（推演） | bonding / 交換器 / 網卡錯誤 | 網路 | 05 |
| 20 | 磁碟 I/O 延遲飆高、dmesg 有錯誤（推演） | SMART、RAID 降級 | 硬體 | 06、12 |

---

## 情境 01：磁碟空間告警，但 `du` 找不到誰佔了空間

**回報**：監控告警 `/var/log/myapp` 使用率 100%；值班同事說「我 `du` 加起來才幾 MB，刪了幾個舊 log 也沒用」。

**排查思路與順序**：
1. **先確認症狀的層次**：`df` 看的是檔案系統回報的已配置區塊，`du` 是走訪目錄樹加總——兩者不一致只有三種可能：(a) 有檔案被刪除但仍被程序開啟（空間不會釋放）、(b) 有東西掛載在目錄「底下」把舊內容蓋住、(c) 檔案系統保留空間或 metadata。**(a) 機率最高、驗證最便宜**，先查。
2. 找出握著已刪除檔案的程序 → 決定是重啟服務還是清空該 fd。
3. 確認根因是「日誌未輪替 / 程式不重開檔案」，補 logrotate 或修程式，否則下週再發生一次。

**怎麼做（實測輸出）**：

```bash
df -h /var/log/myapp
#   Filesystem      Size  Used Avail Use% Mounted on
#   tmpfs            64M   64M     0 100% /var/log/myapp
sudo du -sh /var/log/myapp            # 幾乎為 0 → df/du 不一致，走假設 (a)
sudo lsof +L1 | grep /var/log/myapp   # +L1：連結數 < 1 = 已刪除但仍開啟
#   bash   44626 root  3w  REG  0,607 67108864  0  2 /var/log/myapp/debug.log (deleted)
#   sleep  44693 root  3w  REG  0,607 67108864  0  2 /var/log/myapp/debug.log (deleted)
#   ↑ PID、fd 編號（3w = fd 3 寫入）、大小 64MB、路徑 (deleted)
# 修復（擇一）
sudo systemctl restart <該服務>                 # 正規：讓程式重新開檔
: | sudo tee /proc/44626/fd/3                   # 緊急：不重啟，直接把該 fd 指向的檔案清空（空間立刻釋放）
df -h /var/log/myapp                            # 回到正常
```

**根因**：程式持續寫入一個已被 `rm` 的日誌檔；`rm` 只移除目錄項，inode 在最後一個 fd 關閉前不會釋放。
**預防**：logrotate 用 `copytruncate` 或 `postrotate` 送 HUP 讓程式重開檔案（07 章 §4）；監控加上 `node_filesystem_avail_bytes` 預測告警（12 章）；不要用 `rm` 清正在寫的日誌，用 `: > file`。
**🟠🔵 差異**：無；`lsof` 兩者都需安裝（`lsof`）。

---

## 情境 02：部署後服務反覆重啟

**回報**：`myapp` 更新版本後 `systemctl status myapp` 一直是 `activating (auto-restart)`，日誌只有「Main process exited」。

**排查思路與順序**：
1. `systemctl status` 的三行關鍵：`Active:` 狀態、`Process:` 的退出碼、最近 10 行日誌 → 先讀程式自己的錯誤訊息，通常已經寫明（`Address already in use`、`Permission denied`、`No such file`）。
2. 依錯誤訊息分流：埠衝突 → `ss -ltnp`；權限 → `ls -l` / SELinux；找不到檔 → `ExecStart` 路徑與 `WorkingDirectory`。
3. 找到佔用者後要問「它為什麼在那」（舊版本沒停乾淨？別人手動起的？）而不是直接殺。

**怎麼做（實測輸出）**：

```bash
systemctl status myapp --no-pager
#   Active: activating (auto-restart) (Result: exit-code) ...
#   ... OSError: [Errno 98] Address already in use
sudo ss -ltnp | grep :8085
#   LISTEN 0 128 127.0.0.1:8085 0.0.0.0:* users:(("python3",pid=44728,fd=3))
sudo systemctl status 44728 2>/dev/null || cat /proc/44728/cgroup     # 這個 PID 屬於哪個 unit？
#   → legacy-listener.service（舊版本用 systemd-run 手動起的臨時服務）
sudo systemctl stop legacy-listener
sudo systemctl reset-failed myapp && sudo systemctl restart myapp && systemctl is-active myapp   # active
```

**根因**：舊的臨時服務沒有隨部署停掉，佔住埠；新服務因 `Restart=on-failure` 每 5 秒重試一次。
**預防**：部署腳本用 `systemctl stop` 舊 unit 而非 `kill`；服務用 `Conflicts=` 宣告互斥；`ss -ltnp` 納入部署後檢查。
**🟠🔵 差異**：無。

---

## 情境 03：新站台上線沒生效，`systemctl reload nginx` 也沒報錯

**回報**：「我把設定放到 `conf.d/`，`reload` 過了，`curl localhost:8090` 還是連不上。」

**排查思路與順序**：
1. **`reload` 不等於成功**：nginx 收到 HUP 後若新設定有誤，會**保留舊設定繼續跑**並只在 error log 留一行——服務看起來 active，改動卻沒生效。所以先 `nginx -t`。
2. `-t` 通過再查監聽（`ss -ltnp | grep 8090`）、再查防火牆。
3. 語法錯誤幾乎都在「最後改的那個檔」。

**怎麼做（實測輸出）**：

```bash
curl -s -m2 localhost:8090 || echo "無回應"
systemctl is-active nginx                      # active ← 誤導
sudo nginx -t
#   nginx: [emerg] unexpected "}" in /etc/nginx/conf.d/zz-newsite.conf:6
#   nginx: configuration file /etc/nginx/nginx.conf test failed
sudo sed -n 1,6p /etc/nginx/conf.d/zz-newsite.conf     # 第 5 行 add_header 少了分號
sudo vim /etc/nginx/conf.d/zz-newsite.conf && sudo nginx -t && sudo systemctl reload nginx
curl -s localhost:8090                          # ok
```

**根因**：設定檔語法錯誤；`reload` 對錯誤設定的行為是「拒絕載入、保留舊的」。
**預防**：部署流程一律 `nginx -t && systemctl reload nginx`（`&&` 讓 `-t` 失敗就不 reload）；Ansible 用 `validate:` 參數。同類工具：`sshd -t`、`apachectl configtest`、`haproxy -c`、`named-checkconf`、`visudo -c`。
**🟠🔵 差異**：🟠 nginx 設定在 `sites-available/` + `sites-enabled/` 符號連結（Debian 慣例），`conf.d/` 亦可；🔵 只有 `conf.d/`。錯誤訊息前綴 🟠 含時間戳、🔵 直接以 `nginx:` 開頭（實測）。

---

## 情境 04：強化 SSH 後所有人都登不進來

**回報**：「照 08 章加了 `AllowGroups`，重啟 sshd 後同事全部 `Permission denied (publickey)`，金鑰明明沒換。」

**排查思路與順序**：
1. **先保住現有 session**（不要關終端），所有修復都從這個 session 做。若已全部斷線 → 主控台 / 雲端 serial console / 12 章救援模式。
2. `Permission denied (publickey)` 是**客戶端看到的統一訊息**，真正原因只在**伺服器端日誌**——先看 `journalctl -u sshd`，不要在客戶端猜。
3. 依日誌分流：`not allowed because not listed in AllowGroups` → 群組；`Authentication refused: bad ownership` → 權限；`no matching key exchange` → 演算法。
4. 用 `sshd -T` 看**實際生效值**（drop-in 合併後），而不是看你以為的設定檔。

**怎麼做（實測輸出）**：

```bash
# 客戶端
ssh -v alice@server 2>&1 | tail -3              # Permission denied (publickey) — 沒有更多資訊
# 伺服器端（現有 session）
sudo journalctl -u ssh -n 20 --no-pager -o cat      # 🟠；🔵 -u sshd
#   User alice from 127.0.0.1 not allowed because not listed in AllowGroups
sudo sshd -T | grep -i allowgroups
#   allowgroups ops-team                            ← 只允許 ops-team，管理員自己不在裡面
grep -rn AllowGroups /etc/ssh/sshd_config /etc/ssh/sshd_config.d/
#   /etc/ssh/sshd_config.d/05-allowgroups.conf:1:AllowGroups ops-team
# 修復：把自己加進去或補群組，並先測再 reload
sudo groupadd -f ops-team && sudo usermod -aG ops-team alice
sudo sshd -t && sudo systemctl reload ssh         # 🟠；🔵 sshd
ssh -o BatchMode=yes alice@server true && echo OK  # 另開終端測，成功才關舊 session
```

**根因**：`AllowGroups` 是白名單，沒列到的群組一律拒絕，包括自己。
**預防**：改 sshd 前先 `sshd -T | grep` 比對；用第二個 session 測試登入後才關舊的；重大變更前 `systemd-run --on-active=10m systemctl restart ssh` 之類的自動回滾保險（05 章 §11 的 nmcli 也用同招）；02 章 §4.2.3 的順序。
**🟠🔵 差異**：服務名 `ssh` / `sshd`；🔵 drop-in 檔名要小於 `40-`（02 章）。

---

## 情境 05：套件安裝與 `curl` 全失敗，但 `ping 8.8.8.8` 正常

**回報**：「`apt update` 卡很久然後 `Temporary failure resolving`，網路明明是通的。」

**排查思路與順序**：
1. 「IP 通、名稱不通」= **DNS 層**問題，不用再查路由與防火牆（05 章 §10 的第 5～6 步）。
2. DNS 分三段：`/etc/resolv.conf` 指向誰 → 那個位址通不通 → 它回不回答。先看指向。
3. `resolv.conf` 被改的來源：手動、cloud-init、NetworkManager / Netplan 重寫、VPN 客戶端、容器引擎。查最近變更。

**怎麼做（實測輸出）**：

```bash
ping -c1 1.1.1.1 >/dev/null && echo "IP 通"
getent hosts fedoraproject.org || echo "解析失敗"          # 用 getent 走 NSS，與應用程式行為一致
cat /etc/resolv.conf
#   nameserver 192.0.2.53                                   ← 192.0.2.0/24 是文件用保留網段，根本不存在
#   options timeout:1 attempts:1
ls -l /etc/resolv.conf                                      # 是 symlink（resolved）還是實體檔？誰擁有？
resolvectl status 2>/dev/null | head                        # 若用 resolved：真正的上游在這裡看
dig @1.1.1.1 fedoraproject.org +short                       # 繞過本機設定直接問：能答 → 只是本機指錯
# 修復：改回正確來源，而不是手改 resolv.conf 等下次再被覆蓋
#   🟠 netplan 的 nameservers / 🔵 nmcli con mod ... ipv4.dns（05 章 §5）；容器內由引擎管理
```

**根因**：`resolv.conf` 被寫入不存在的 DNS 伺服器。
**預防**：DNS 設定只從一個來源管（Netplan / NM / resolved.conf.d）；監控加 blackbox DNS probe；`resolv.conf` 若常被覆蓋可 `chattr +i` 當最後手段（04 章 §2.2）。
**🟠🔵 差異**：兩者預設都是 systemd-resolved 的 symlink，改寫要透過各自的網路工具；🟠 雲端映像多了 cloud-init 這個來源。

---

## 情境 06：本機 `curl` 正常，其他主機連不到

**回報**：「服務在 8085 起來了，我在主機上 `curl localhost:8085` 是 ok，但從我的電腦連 `<主機 IP>:8085` 連線被拒。」

**排查思路與順序**（由外而內）：
1. **監聽位址**：`ss -ltnp` 看它綁 `127.0.0.1` 還是 `0.0.0.0`——綁 loopback 的服務外部永遠連不到，這一步 5 秒就能排除一半案例。
2. **本機防火牆**：ufw / firewalld 有沒有放行。
3. **路徑上的防火牆**：雲端安全群組、NAT、公司防火牆（用 `nc -zv` 從客戶端測、`tcpdump` 在伺服器看封包有沒有到）。
4. 🔵 **SELinux**：非標準埠若被擋會在 `ausearch -m avc` 出現。

**怎麼做（實測輸出）**：

```bash
sudo ss -ltnp | grep :8085
#   LISTEN 0 5 127.0.0.1:8085 0.0.0.0:* users:(("python3",pid=45270,fd=3))   ← 只綁 loopback
# 修復 1：讓服務綁所有介面（本例由環境變數 BIND 控制）
echo "BIND=0.0.0.0" | sudo tee -a /etc/default/myapp && sudo systemctl restart myapp
sudo ss -ltnp | grep :8085                  # 0.0.0.0:8085
# 修復 2：防火牆
sudo ufw status | grep 8085 || sudo ufw allow 8085/tcp                                   # 🟠
sudo firewall-cmd --list-ports | grep -q 8085 || { sudo firewall-cmd --permanent --add-port=8085/tcp && sudo firewall-cmd --reload; }   # 🔵
# 🔵 若仍不通：sudo ausearch -m avc -ts recent | grep 8085 → semanage port -a -t http_port_t -p tcp 8085
# 從客戶端驗證
nc -zv <主機IP> 8085
```

**根因**：兩層——服務只綁 127.0.0.1，且防火牆未放行；只修一層仍不通，這是此情境常被「修好又壞」的原因。
**預防**：服務部署清單固定包含「監聽位址、防火牆、（🔵）SELinux 埠」三項；有反向代理時後端刻意只綁 127.0.0.1 是**正確**設計，不要反射性改成 0.0.0.0。
**🟠🔵 差異**：防火牆指令；🔵 多一層 SELinux。

---

## 情境 07：cron 排程「沒有跑」

**回報**：「我在 crontab 放了每分鐘的報表腳本，一小時了 `/var/log/report.log` 還是不存在。」

**排查思路與順序**：
1. cron 有沒有**執行**（daemon 在跑、有 session 紀錄）→ `journalctl -u cron`。有紀錄 = 有執行，問題在腳本；沒紀錄 = crontab 沒載入或 daemon 沒跑。
2. 執行了但沒結果 → **cron 的環境和你的 shell 不同**：PATH 只有 `/usr/bin:/bin`、沒有 `~/.bashrc`、`%` 是換行。先看 crontab 有沒有把輸出導到檔案，沒有的話輸出去了郵件（沒有 MTA 就消失）。
3. 看導向檔的內容，錯誤訊息通常直接寫明。

**怎麼做（實測輸出）**：

```bash
crontab -l
#   * * * * * report.sh >> /var/log/report-cron.log 2>&1
#   * * * * * /opt/tools/report.sh --date=$(date +%F) >> /var/log/report-cron.log 2>&1
sudo journalctl -u cron -n 5 --no-pager -o cat        # 🟠；🔵 -u crond
#   pam_unix(cron:session): session opened for user root    ← 有執行
cat /var/log/report-cron.log
#   /bin/sh: 1: report.sh: not found                          ← 第 1 條：相對路徑，cron 的 PATH 找不到
#   （第 2 條：% 被當成換行，指令在 %F 處被截斷，date 那段變成 stdin）
# 修復
crontab -e
#   PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
#   * * * * * /opt/tools/report.sh --date=$(date +\%F) >> /var/log/report-cron.log 2>&1
```

**根因**：兩個經典 cron 陷阱同時發生——相對路徑 + 未跳脫的 `%`。
**預防**：crontab 頂端宣告 `PATH=`；指令用絕對路徑；`%` 寫成 `\%`；一律 `>> log 2>&1`；或改用 systemd timer（輸出自動進 journal，04 章 §1.4）。
**🟠🔵 差異**：服務 `cron` / `crond`；🔵 `journalctl -u crond` 的訊息格式為 `(CRON) INFO ...`（實測）；🔵 最小安裝可能沒裝 `cronie`。

---

## 情境 08：systemd timer 從未觸發

**回報**：「我寫了 `cleanup.timer` 每小時清 `/tmp`，`systemctl start` 沒報錯，但從沒執行過；重開機後 timer 還不見了。」

**排查思路與順序**：
1. `systemctl list-timers --all`：timer 有沒有在列表、`NEXT` 是不是 `n/a`。
2. `systemctl status <timer>`：`Loaded:` 若是 `bad-setting` 就是單元檔本身有錯，看 `Triggers:` 指向哪個 service。
3. `systemd-analyze verify` 把所有錯誤一次列出（比一行行看快）。
4. 「重開機後不見」= 只 `start` 沒 `enable`（或沒有 `[Install]` 段）。

**怎麼做（實測輸出）**：

```bash
systemctl list-timers --all --no-pager | grep clean          # 沒有 cleanup.timer
systemctl status cleanup.timer --no-pager
#   Loaded: bad-setting (Reason: Unit cleanup.timer has a bad unit file setting.)
#   Active: inactive (dead)
#   Triggers: ● clean-up.service                              ← 名字打錯（cleanup vs clean-up）
sudo systemd-analyze verify /etc/systemd/system/cleanup.timer
#   cleanup.timer:4: Failed to parse calendar specification, ignoring: *-*-* *:00/60
#   cleanup.timer: Timer unit lacks value setting. Refusing.
systemd-analyze calendar "hourly"                              # 先測 OnCalendar 語法再寫進去
# 修復：OnCalendar=hourly、Unit=cleanup.service、補 [Install] WantedBy=timers.target
sudo systemctl daemon-reload && sudo systemctl enable --now cleanup.timer && systemctl list-timers cleanup.timer
```

**根因**：三個錯誤疊加——`OnCalendar` 語法錯（`/60` 分鐘步進非法）、`Unit=` 指到不存在的 service、沒有 `[Install]` 所以無法 enable。
**預防**：寫完 unit 先 `systemd-analyze verify`；`OnCalendar` 先用 `systemd-analyze calendar` 測；一律 `enable --now`；用 `Persistent=true` 補跑錯過的。
**🟠🔵 差異**：無（systemd 255 vs 259 訊息相同）。

---

## 情境 09：服務每隔幾秒被殺，日誌只有 `signal=KILL`

**回報**：「`memhog.service`（報表 worker）一直重啟，`journalctl -u` 只看到 `Main process exited, code=killed, status=9/KILL`，沒有任何錯誤。」

**排查思路與順序**：
1. **SIGKILL 不會來自程式本身**（程式無法攔截也不會自殺用 9）。來源只有三種：OOM killer、人 / 腳本 `kill -9`、systemd 超時（`TimeoutStopSec` 後）。先查 OOM，因為它會在**核心日誌**留紀錄。
2. `journalctl -k -g oom` 有 → 看是整機 OOM（`constraint=CONSTRAINT_NONE`）還是 cgroup 限制（`CONSTRAINT_MEMCG` + `oom_memcg=` 指向哪個 unit）。
3. 決定是程式吃太多（修程式 / 加機器）還是限制設太小（調 `MemoryMax`）；兩者都可能對。

**怎麼做（實測輸出）**：

```bash
systemctl status memhog --no-pager
#   Active: activating (auto-restart) (Result: signal) since ...; 397ms ago     ← 🟠 systemd 255
#   Active: activating (auto-restart) (Result: oom-kill) since ...              ← 🔵 systemd 259 直接告訴你
#   Main PID: 73081 (code=killed, signal=KILL)
systemctl show memhog -p NRestarts -p MemoryMax -p Result
#   NRestarts=6  MemoryMax=67108864  Result=signal|oom-kill
sudo journalctl -k -g "oom" -n 3 --no-pager -o cat
#   oom-kill:constraint=CONSTRAINT_MEMCG,...,oom_memcg=/system.slice/memhog.service,task=python3,pid=14621
#   Memory cgroup out of memory: Killed process 14621 (python3) total-vm:80776kB, anon-rss:65280kB ...
#   ↑ CONSTRAINT_MEMCG = 是 cgroup 限制（64M）殺的，不是整機沒記憶體；anon-rss 65280kB 剛好撞到上限
# 修復：確認程式合理用量後調整限制（或修程式）
sudo systemctl edit memhog      # [Service] MemoryMax=512M  MemoryHigh=400M
sudo systemctl restart memhog
```

**根因**：`MemoryMax=64M` 低於程式實際需求；cgroup v2 到上限先嘗試回收 / 換頁，`MemorySwapMax=0`（或無 swap）時直接 OOM。
**預防**：設 `MemoryHigh`（軟限制，先節流不殺）搭配 `MemoryMax`；監控 `container_memory_working_set` / `systemd-cgtop`；`OOMScoreAdjust` 保護關鍵服務（04 章 §3.1）。
**🟠🔵 差異**：實測 systemd 255（🟠）把 OOM 記為 `Result=signal`，259（🔵）記為 `Result=oom-kill`——在 🟠 上必須看 `journalctl -k` 才能確認。

---

## 情境 10：主機變慢、load average 飆高

**回報**：「網站變慢，`uptime` 的 load 一直 > 核心數，但沒有人在部署。」

**排查思路與順序**：
1. load 高 ≠ CPU 忙：先 `vmstat 1` 分辨是 `r`（等 CPU）還是 `b`（等 I/O）；`%wa` 高就轉去 I/O（情境 20）。
2. CPU 忙 → `top` 找程序 → **找它屬於哪個 unit / 誰起的**（`systemd-cgtop`、`/proc/PID/cgroup`），因為殺掉單一 PID 可能馬上被 `Restart=` 拉起來，或它只是某個批次的其中一個。
3. 決定：停掉 unit、限制它（`CPUQuota`）、或它其實是正常尖峰（看歷史 `sar -u`）。

**怎麼做（實測輸出）**：

```bash
uptime                          # load average: 2.07, 1.66, 1.34（16 核機器上 4 個忙迴圈，1 分鐘值還在爬升）
vmstat 1 3                      # r 欄 ≥ 4、us 高、wa 低 → CPU 型
top -o %CPU -n1 | head -12      # 四個 sh 各 100%
systemd-cgtop -n1 --order=cpu | head   # system.slice/batch-job-*.service
systemctl list-units 'batch-job-*'
#   batch-job-1.service loaded active running /usr/bin/sh -c "while :; do :; done"  ...
sudo journalctl -u batch-job-1 -n 3    # 誰、什麼時候起的（systemd-run 會留紀錄）
sudo systemctl stop 'batch-job-*'      # 或 systemctl set-property batch-job-1 CPUQuota=25% 先限住
```

**根因**：四個未受限的批次工作同時跑。
**預防**：批次工作放進 `batch.slice` 並設 `CPUWeight` / `CPUQuota`（09 章 §8）；用 `nice`/`ionice`；監控 load 與 per-unit CPU。
**🟠🔵 差異**：無（🔵 的 `list-units` 描述欄多了 `[systemd-run]` 前綴，實測）。

---

## 情境 11：`apt update` / `dnf install` 全部失敗

**回報**：「昨天裝了廠商工具的套件庫，今天什麼都裝不了。」

**排查思路與順序**：
1. 套件管理員的錯誤訊息會**指名哪個 repo**；先看訊息，不要急著 `clean`。
2. 判斷：只是那個 repo 壞（其他仍可用）還是整體壞（DNS / proxy / 磁碟滿）。訊息只提到一個 URL → 隔離它。
3. 停用（不是刪除）壞 repo 讓其他操作恢復，再處理廠商問題。

**怎麼做（實測輸出）**：

```bash
# 🟠
sudo apt update
#   W: Failed to fetch https://repo.invalid.example/ubuntu/dists/noble/InRelease  Temporary failure resolving 'repo.invalid.example'
#   W: Some index files failed to download. They have been ignored, or old ones used instead.
#   ↑ 只是 W（警告）：其他 repo 仍更新，但每次都會等 DNS 逾時；若是 E（錯誤，如簽章失敗）整個 update 失敗
grep -l "repo.invalid.example" /etc/apt/sources.list.d/*     # 找到 vendor-tool.sources
sudo sed -i 's/^Types: deb$/Types: deb\nEnabled: no/' /etc/apt/sources.list.d/vendor-tool.sources   # deb822 可用 Enabled: no 停用
# 🔵
sudo dnf install hello
#   Vendor Tool  ???% | 0.0 B/s | 0.0 B | 00m00s
#   >>> Curl error (6): Could not resolve hostname ... repo.invalid.example ...
#   ↑ 該 repo 設了 skip_if_unavailable=0（很多廠商 repo 這樣設），dnf 直接中止整個交易
dnf repolist | grep -i vendor
sudo dnf config-manager setopt vendor-tool.enabled=0             # 停用；或 --disablerepo=vendor-tool 單次跳過
```

**根因**：無法連線的第三方 repo；🔵 因 `skip_if_unavailable=0` 變成硬錯誤。
**預防**：第三方 repo 加 `skip_if_unavailable=1`（🔵）／ 監控 `apt update` 退出碼；廠商 repo 用內部鏡像；03 章 §3.2 的 `Signed-By` 隔離。
**🟠🔵 差異**：🟠 預設只警告、🔵 依 `skip_if_unavailable` 決定；停用方式 `Enabled: no` vs `setopt X.enabled=0`。

---

## 情境 12：寫入報 `Input/output error`，`df` 卻顯示還有 80% 空間

**回報**：「應用程式寫 `/srv/appdata` 一直 I/O error，`df -h` 說用了 17%，磁碟也沒壞。」

**排查思路與順序**：
1. `df` 看到的是**檔案系統**的空間；I/O error 來自**更底層**（區塊裝置）。先 `dmesg` 看核心怎麼說。
2. `lsblk` 看這個掛載點下面疊了什麼（thin LV？LUKS？RAID？iSCSI？）——每一層都可能是「上面看起來有空間、下面其實沒了」。
3. thin pool：`lvs -a` 的 `Data%` / `Meta%`。

**怎麼做（實測輸出）**：

```bash
echo test > /srv/appdata/probe
#   bash: /srv/appdata/probe: Input/output error
df -h /srv/appdata
#   /dev/mapper/vgapp-data  974M  151M  757M  17% /srv/appdata        ← 檔案系統以為還有 757M
sudo dmesg -T | tail -5                                              # device-mapper: thin: ... no free space / switching pool to out-of-data-space mode
lsblk /dev/mapper/vgapp-data                                         # 看到它是 thin LV
sudo lvs -a -o lv_name,lv_size,data_percent,metadata_percent vgapp
#   data              1.00g  11.72                                    ← 虛擬 1G 只用 11.72%
#   tpool           120.00m 100.00  12.01                             ← 但 pool 只有 120M，已 100%
# 修復：擴 pool（VG 要有空間），再讓檔案系統重試
sudo lvextend -L +200M vgapp/tpool
sudo lvs -a vgapp | grep tpool                                       # Data% 下降
echo test > /srv/appdata/probe && echo OK                            # 若仍錯：umount / mount 或 fsck 讓 ext4 離開錯誤狀態
```

**根因**：thin pool 超額配置（1G 虛擬 / 120M 實體）用滿；`df` 看的是 thin volume 的虛擬大小。
**預防**：thin pool 必設 `thin_pool_autoextend_threshold` 與監控 `Data%`（15 章 §3）；VG 保留空間；不在根目錄用 thin。
**🟠🔵 差異**：無。

---

## 情境 13：新同事說「sudo 說我不在 sudoers」

**回報**：「bob 登入後 `sudo` 顯示 `bob is not in the sudoers file. This incident will be reported.`」

**排查思路與順序**：
1. sudo 授權來自兩處：**群組**（`sudo` / `wheel`）或 **sudoers 明列**。先 `id bob` 看群組——最常見就是建帳號時忘了 `-G`。
2. `sudo -l -U bob` 直接列出他能做什麼，比讀 sudoers 快。
3. 加了群組後要**重新登入**才生效（群組在登入時載入）。

**怎麼做（實測輸出）**：

```bash
id bob
#   uid=1002(bob) gid=1002(bob) groups=1002(bob)                    ← 沒有 sudo / wheel
sudo -l -U bob
#   User bob is not allowed to run sudo on <host>.
grep -E "^%(sudo|wheel)" /etc/sudoers; ls /etc/sudoers.d/         # 確認群組規則存在
sudo usermod -aG sudo bob     # 🟠     /   sudo usermod -aG wheel bob   # 🔵
sudo -l -U bob                # 立即可查；bob 本人要重新登入（或 newgrp）才會帶到新群組
```

**根因**：帳號未加入管理員群組。
**預防**：建帳號走 `bootstrap.sh` / Ansible 的 `user:` 模組固定帶群組；離職流程反向移除。
**🟠🔵 差異**：群組名 `sudo` / `wheel`；🟠 `adduser` 互動式不會自動加群組、🔵 `useradd -G wheel`。

---

## 情境 14：journal 暴增、`journalctl` 變慢

**回報**：「`/var/log/journal` 一天長了 2 GB，`journalctl -u nginx` 要等十幾秒。」

**排查思路與順序**：
1. 先量：`journalctl --disk-usage`。
2. 找**最吵的來源**：用 `-o json` 取 `SYSLOG_IDENTIFIER` / `_SYSTEMD_UNIT` 統計最近一段時間的筆數——比肉眼翻快得多。
3. 處理來源（修程式 / 降 log level / 限流），再清空間（`--vacuum-*`）；順序反了下週又滿。

**怎麼做（實測輸出）**：

```bash
journalctl --disk-usage
sudo journalctl --since -1min -o json --output-fields=SYSLOG_IDENTIFIER | grep -o '"SYSLOG_IDENTIFIER":"[^"]*"' | sort | uniq -c | sort -rn | head -3
#   6472 "SYSLOG_IDENTIFIER":"chatty-agent"                           ← 一分鐘六千多筆
#     35 "SYSLOG_IDENTIFIER":"kernel"
sudo journalctl -t chatty-agent -n 3 -o cat                          # 看內容：heartbeat ok seq=... 無意義的心跳
systemctl status chatty-agent                                        # 屬於哪個 unit、誰起的
# 修復：降低程式 log level 或加限流（journald 端）
sudo systemctl edit chatty-agent      # [Service] LogRateLimitIntervalSec=30s  LogRateLimitBurst=100
sudo journalctl --vacuum-size=500M    # 清舊的；並在 journald.conf.d 設 SystemMaxUse（07 章 §2.1）
```

**根因**：某程式以每秒數百筆寫入無意義日誌。
**預防**：journald 全域 `RateLimitBurst` + 服務級 `LogRateLimit*`；`SystemMaxUse` 上限；監控 journal 大小與每 unit 速率。
**🟠🔵 差異**：無。

---

## 情境 15：加新碟到 fstab，擔心重開機

**回報**：「我照文件在 `/etc/fstab` 加了一顆新碟，還沒重開機，怎麼確認不會開不了機？」

**排查思路與順序**：
1. fstab 錯誤在開機時的後果是 emergency mode（12 章 §3.2 第一列），**不要用重開機來測試**。
2. `findmnt --verify` 靜態檢查每一行（來源存在？類型對？選項合法？）；`mount -a` 實際掛一次。
3. 網路碟 / 外接碟一律 `nofail`（+ `_netdev`），讓它不存在也能開機。

**怎麼做（實測輸出）**：

```bash
sudo findmnt --verify
#   [E] unreachable on boot required source: UUID=0badc0de-0000-4000-8000-000000000000
#   [W] your fstab has been modified, but systemd still uses the old version; use 'systemctl daemon-reload' to reload
sudo blkid | grep -i 0badc0de || echo "UUID 不存在"           # 抄錯 UUID
sudo blkid /dev/sdb1                                          # 取正確 UUID
# 修正該行，並加 nofail
sudo mount -a && sudo systemctl daemon-reload && sudo findmnt --verify
#   Success, no errors or warnings detected
```

**根因**：UUID 抄錯 + 沒有 `nofail`，開機時 systemd 會等該裝置 90 秒後進 emergency。
**預防**：每次改 fstab 後 `findmnt --verify && mount -a`；非系統碟一律 `nofail`；Ansible 用 `ansible.posix.mount`（會驗證）。
**🟠🔵 差異**：無。

---

## 情境 16（推演）：重開機後停在 `grub rescue>`

**回報**：核心升級 + 磁碟擴充後重開機，畫面停在 `error: unknown filesystem. Entering rescue mode... grub rescue>`。

**排查思路與順序**（依 14 章的開機鏈由前往後）：
1. `grub rescue>` 表示韌體已找到 GRUB 的第一階段，但 GRUB **找不到 `/boot`**（模組、`grub.cfg`）——問題在 GRUB 與 `/boot` 之間，不在核心。
2. 在 rescue 提示下 `ls` 列分割區、`ls (hd0,gpt2)/` 找哪個有 `grub/`（🟠）或 `grub2/`（🔵）→ `set root=` / `set prefix=` / `insmod normal` / `normal` 手動開機。
3. 進系統後找**為什麼**：`/boot` 分割區 UUID 變了（重做檔案系統）？擴充磁碟時分割區被移動？GRUB 的 `grub.cfg` 內 `search --fs-uuid` 指向舊 UUID？→ 重跑 🟠 `update-grub` + `grub-install`；🔵 `grub2-mkconfig -o /boot/grub2/grub.cfg`（UEFI 勿 `grub2-install`）。
4. 若 rescue 手動也開不了 → Live 媒體 chroot（12 章 §3.1）。

**根因候選**：`/boot` UUID 變更、分割表被改、GRUB 核心映像與模組版本不一致（BIOS 模式的 `core.img` 舊、模組新）。
**預防**：動分割區前備份 `sfdisk -d`；改 `/boot` 後必跑 GRUB 重建；14 章 §12 的檢查表。

---

## 情境 17（推演，🔵 Fedora）：服務讀不到搬過來的檔案，權限明明是 644

**回報**：「把網站檔案從家目錄 `mv` 到 `/var/www/html`，nginx 回 403；`ls -l` 是 `644 nginx:nginx`，`chmod 777` 也沒用。」

**排查思路與順序**（08 章 §5.2 的三原則）：
1. 「權限對但仍拒絕」在 Fedora 上第一個假設就是 **SELinux**：`ls -Z` 看標籤——`mv` 保留原標籤 `user_home_t`，nginx 的 `httpd_t` 不能讀。
2. 用 `ausearch -m avc -ts recent` 確認有 AVC（若沒有，先 `semodule -DB` 排除 dontaudit），`sealert` 會直接建議 `restorecon`。
3. `restorecon -Rv /var/www/html` → 標籤變 `httpd_sys_content_t` → 403 消失。**不要** `setenforce 0`、不要 `audit2allow` 放行 `user_home_t`。
4. 若目錄在非標準路徑（`/srv/www`）→ `semanage fcontext -a -e /var/www /srv/www` 讓 restorecon 認得（16 章 §9）。

**根因**：`mv` 保留來源標籤；DAC 權限無法覆蓋 MAC。
**預防**：部署用 `cp`/`rsync`（取目的目錄的預設標籤）或部署後固定 `restorecon`；Ansible `sefcontext` + `command: restorecon`。
**🟠 對照**：AppArmor 以路徑判斷，`mv` 到被允許的路徑就能讀；但若 nginx profile 沒列該路徑，症狀類似，用 `aa-logprof` 補。

---

## 情境 18（推演）：憑證錯誤、Kerberos / LDAP 登入失敗、`apt update` 簽章「尚未生效」

**回報**：「早上開始所有 HTTPS 連線 `certificate is not yet valid`，AD 登入失敗，一台機器同時發生。」

**排查思路與順序**：
1. 多個不相關的服務**同時**出現「時間相關」錯誤 → 第一個假設是**系統時鐘**：`timedatectl`（`System clock synchronized: no`？時區對嗎？）、`date` 與手機比。
2. 為什麼跑掉：NTP 服務沒啟動 / 被防火牆擋（123/udp）/ 上游不可達 / VM 剛從快照還原或暫停恢復（時間停在快照那一刻）/ RTC 電池沒電。
   - 🟠 `timedatectl timesync-status`（timesyncd）或 `chronyc tracking`；🔵 `chronyc tracking`、`chronyc sources -v`（`?` 表示不可達）。
3. 修：`chronyc makestep`（強制跳時）→ 服務恢復；再修 NTP 可達性。偏差超過 1000 秒時 chrony 預設拒絕修正，需 `makestep 1.0 3` 或手動 `date -s` 後再讓它同步。

**根因候選**：NTP 未同步 + VM 快照還原；時區設錯（差整數小時）。
**預防**：監控 `node_timex_offset_seconds`；VM 裝 guest agent 讓 hypervisor 同步；`chrony.conf` 的 `makestep 1.0 3`。
**🟠🔵 差異**：預設 NTP 客戶端 timesyncd vs chrony（02 章 §2.1）。

---

## 情境 19（推演）：間歇性網路中斷，幾秒後自己恢復

**回報**：「每隔幾小時 SSH 會卡 5～10 秒，監控偶爾 ping 掉包，但沒有規律。」

**排查思路與順序**（05 章 §10 由下而上，但這次重點是「間歇」）：
1. 間歇 = 抓現行犯很難，先看**歷史**：`journalctl -k -g "link is down|Link is Up|NIC Link"`（網卡 flap）、`ip -s link`（errors / dropped 是否累積）、`ethtool -S eth0 | grep -i err`、交換器端口的 log。
2. 有 bonding：`cat /proc/net/bonding/bond0` 的 `Link Failure Count`、`Currently Active Slave` 是否切換過——LACP 與交換器設定不一致會週期性 renegotiate。
3. 沒有硬體線索 → 看 IP 層：DHCP 續約（`journalctl -u NetworkManager` / `networkctl`）、IP 衝突（`arping -D`）、STP 重新收斂（交換器 topology change）。
4. 都沒有 → 主機自己：`softnet_stat` 第 2 欄（softirq 來不及）、`irqbalance`、電源管理讓網卡休眠（`ethtool --show-eee`、關 `powersave`）。

**根因候選**：網線 / SFP 不良（flap）、bonding 與交換器模式不一致、IP 衝突、DHCP 租約、網卡省電。
**預防**：bonding 用 `mii-monitor-interval` + 交換器 LACP 對齊；監控 `node_network_up` 與 `node_network_receive_errs_total`；伺服器網卡關 EEE / ASPM。

---

## 情境 20（推演）：磁碟 I/O 延遲飆高，`dmesg` 出現 I/O error

**回報**：「資料庫查詢變慢 10 倍，`iostat` 的 `await` 從 2ms 變 200ms，`dmesg` 有 `I/O error, dev sdb`。」

**排查思路與順序**：
1. `dmesg` 有 I/O error = **硬體或路徑層**（不是調校問題）：`smartctl -a /dev/sdb` 看 `Reallocated_Sector_Ct`、`Current_Pending_Sector`、`UDMA_CRC_Error_Count`（CRC 錯 = 線材 / 背板）。
2. 有 RAID：`cat /proc/mdstat`（`[U_]` = 降級，重建中 I/O 會慢）或 `lvs -o +raid_sync_action`、硬體 RAID 卡工具；降級狀態本身就解釋了延遲。
3. 沒有 RAID → 這顆碟正在死：**先備份**（11 章）、`ddrescue` 救資料、換碟。
4. 確認不是其他原因偽裝：`iotop` 看是否有人在做全表掃描 / 備份（負載型）而非硬體型——但 dmesg 的 I/O error 已排除這個。

**根因候選**：磁碟壞軌、線材 / 背板 CRC 錯誤、RAID 降級重建、SSD 壽命耗盡（`Percentage Used`）。
**預防**：`smartd` 定期自我測試 + 告警（06 章 §10）；RAID 有備援碟並監控 `mdmonitor`；資料庫放 RAID10 / 企業級 SSD。

---

## 排查習慣總結

1. **先問「最近改了什麼」**：15 個可注入情境裡有 12 個是變更引起的（部署、設定、安裝套件、加 fstab）。
2. **看伺服器端的日誌，不看客戶端的猜測**：情境 04、02、03 的關鍵都在 `journalctl -u` 或 `-t` 指令輸出。
3. **`status` 顯示 active 不代表正常**：情境 03（reload 保留舊設定）、08（timer 載入失敗）。
4. **分清楚哪一層**：`df` vs `du`（01）、`df` vs thin pool（12）、IP 通 vs DNS（05）、權限 vs 標籤（17）。
5. **修復要針對根因並加預防**，否則只是把告警關掉。
6. **先確認有退路再動手**：保留 SSH session（04）、`findmnt --verify` 而非重開機（15）、快照（11 章）。
