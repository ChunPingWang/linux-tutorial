# 02 - 初始設定

安裝完成後、上線前必做的基礎設定。本章末附一支可同時在 Ubuntu / Fedora 執行的初始化腳本 [`scripts/bootstrap.sh`](scripts/bootstrap.sh)（已於兩個發行版實測）。

網路與防火牆的完整內容在 [05 網路與防火牆](05-網路與防火牆.md)，本章只做最基本的設定。

**本章解決什麼問題**：剛裝好的系統是「能開機」，不是「能上線」——主機名還是 `localhost`、時間可能偏差、只有安裝時那個帳號、SSH 還接受密碼、防火牆沒開。這些東西每一項都不難，但漏掉任何一項的後果都會在幾週後才浮現：日誌時間對不上、離職者帳號還在、22 埠被暴力破解、SELinux 擋住服務卻不知道原因。本章的取捨原則是「先做對、再做少」：只挑上線前一定要處理的項目，每一項都標明 Ubuntu / Fedora 的差異，並把可重複的部分收進 `bootstrap.sh`，讓第二台以後的機器不必再手動走一遍。

---

## 1. 主機名與 hosts

**名詞與關係**：這一節的名詞其實是一條「主機名怎麼被寫進去、又怎麼被查出來」的路徑：`hostnamectl` 把名字寫進 `/etc/hostname` 與核心 → `sudo`、日誌、提示字元讀到這個名字 → 當程式需要把名字換成 IP 時，先查 `/etc/hosts`、查不到才問 DNS。

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **hostnamectl** | systemd 提供的主機名管理指令，一次寫入 `/etc/hostname`、通知核心、經 D-Bus 即時生效，不必重開機 | 取代「手改 `/etc/hostname` 再重開」；它**不會**幫你改 `/etc/hosts`，那一步要自己做 |
| **`/etc/hostname`** | 靜態主機名的存放檔，開機時由 systemd 讀入核心 | hostnamectl 的寫入目標；內容只有一行 |
| **FQDN** | Fully Qualified Domain Name，含網域的完整主機名（`web01.example.com`），對應的短名是 `web01` | 建議直接把 FQDN 設成主機名；`hostname -f` 要能解析出它，靠的是 DNS 或 `/etc/hosts` |
| **DNS** | 把主機名換成 IP 的網路查詢服務 | 查不到自己的名字、或 DNS 根本不通時，需要時間逾時才會放棄，這就是 `sudo` 卡住的來源 |
| **`/etc/hosts`** | 本機的靜態「名稱 → IP」對照表；解析順序（`/etc/nsswitch.conf` 的 `hosts: files dns`）排在 DNS 之前 | 只要這裡有自己的 FQDN，解析主機名就不會走到 DNS，也就不會卡住 |
| **`127.0.1.1`** | Debian 系保留給「本機主機名」用的 loopback 位址，與 `127.0.0.1 localhost` 分開 | 讓主機名不綁死在某張網卡的 IP，換 IP 不必改 hosts；Fedora 的 Anaconda 沒有這個慣例 |

**為什麼**：主機名會出現在提示字元、日誌（`journalctl`、syslog 轉送）、監控與郵件告警裡；十台機器都叫 `localhost` 或 `ubuntu`，收到告警時根本不知道是哪台出事。此外 `sudo` 每次執行都會解析自己的主機名，若 `/etc/hosts` 沒有對應而 DNS 又不通，每個 `sudo` 指令會卡住數秒甚至出現 `unable to resolve host` 警告。

**怎麼做**：用 `hostnamectl` 設定（會同步寫入 `/etc/hostname` 並即時生效）：

```bash
# ⚪ 共通
sudo hostnamectl set-hostname web01.example.com
hostnamectl                                  # 查看（含核心、硬體、虛擬化資訊）
hostnamectl set-hostname --pretty "Web Server 01"   # 顯示用名稱（可含空白）
```

`/etc/hosts` 建議加入自己的 FQDN，避免 `sudo` 因 DNS 解析慢而延遲。兩個發行版的慣例不同：Debian 系安裝程式固定寫 `127.0.1.1`（避免主機名綁死在某張網卡的 IP），Anaconda 則不會自動加這行，慣例上直接寫實際 IP：

```
127.0.0.1   localhost
127.0.1.1   web01.example.com web01      # 🟠 Ubuntu 慣例用 127.0.1.1
192.168.1.10 web01.example.com web01     # 🔵 Fedora 慣例直接寫實際 IP（或同樣用 127.0.1.1）
```

---

## 2. 時區、時間與語系

**名詞與關係**：Linux 的「時間」由三個彼此分工的東西組成——硬體時鐘給開機時的起始值、系統時鐘負責運行中的計時、時區只負責「顯示時怎麼換算」。`timedatectl` 是這三者的統一操作入口。

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **timedatectl** | systemd 的時間 / 時區管理指令，背後是 `systemd-timedated` 服務 | 同時管時區、RTC 模式，並回報 NTP 同步狀態（§2.1）；改時區不需重開機 |
| **時區（tzdata）** | 「UTC 與本地時間的換算規則」資料庫（`/usr/share/zoneinfo`），`/etc/localtime` 指向其中被選用的那一份 | 系統內部一律以 UTC 計時，時區只影響顯示與 cron 的解讀；`set-timezone` 改的就是這個連結 |
| **RTC（硬體時鐘）** | 主機板上靠電池走的時鐘，關機時仍在計時，開機時核心從它讀出初始時間 | 系統時鐘的「起始值」；之後由 NTP（§2.1）持續校正，並定期回寫 RTC |
| **UTC vs local time** | RTC 存的是 UTC 還是本地時間的兩種模式（`timedatectl` 的 `RTC in local TZ`） | Linux 預設 UTC、Windows 預設 local；雙開時兩者會輪流把對方的時鐘「修正」錯，夏令時間切換時也會跳動 |

**為什麼**：時區設錯，日誌、cron、憑證到期時間全部對不上，跨機器排查事件時要在腦中換算 +8；硬體時鐘若設成 local time，每次夏令時間切換或雙系統開機都會讓系統時間跳動。伺服器一律 RTC 用 UTC、系統時區設本地，是最不會出錯的組合。

**怎麼做**：

```bash
# ⚪ 時區
timedatectl list-timezones | grep -i taipei
sudo timedatectl set-timezone Asia/Taipei
timedatectl                                  # 確認 "System clock synchronized: yes"

# ⚪ 硬體時鐘使用 UTC（雙開 Windows 時才設 local）
sudo timedatectl set-local-rtc 0
```

### 2.1 時間同步（NTP）

**名詞與關係**：先看時間怎麼從上游一路流到你的系統時鐘：

```
上游時間源（GPS / 原子鐘）──▶ 公開 NTP 伺服器池（time.stdtime.gov.tw、tw.pool.ntp.org）
                                        │  NTP 協定（UDP 123）
                                        ▼
  本機客戶端 daemon（擇一，不可同時跑）：
      🟠 systemd-timesyncd（SNTP：只會「跟」上游）   或   ⚪ chronyd（完整 NTP：能跟上游、也能當別人的上游）
                                        │  平時用 slew（微調快慢）慢慢追；chrony 可依 makestep 在偏差太大時直接跳
                                        ▼
                                     系統時鐘 ──定期回寫──▶ RTC（§2）
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **NTP** | Network Time Protocol，透過網路對時的協定，會綜合多個來源、估算網路延遲、並持續校正時鐘的漂移 | chrony 與 ntpd 是完整實作；能當伺服器對下游提供時間 |
| **SNTP** | Simple NTP，NTP 的簡化子集：只跟一個來源、不做複雜的漂移估算、不能當伺服器 | `systemd-timesyncd` 實作的是這個；桌機夠用，伺服器有精度與可靠度疑慮 |
| **systemd-timesyncd** | systemd 內建的輕量 SNTP 客戶端，設定檔 `/etc/systemd/timesyncd.conf` | 🟠 Ubuntu 預設；裝 chrony 時套件腳本會把它停用（兩者互斥） |
| **chrony（chronyd / chronyc）** | 完整的 NTP 實作：`chronyd` 是 daemon，`chronyc` 是查詢與操作它的 CLI | 🔵 Fedora 預設；虛擬機暫停後時間大跳、網路斷續等情境比 ntpd 處理得好 |
| **pool / iburst** | `chrony.conf` 的來源設定：`pool` 指向一個會解析出多台伺服器的名稱，`iburst` 讓剛啟動時連發多個封包以加快首次同步 | 一行 `pool` 就能拿到多個來源，是最省事的寫法 |
| **slew vs step（makestep）** | slew 是「讓時鐘走快或走慢」慢慢追上，不會出現時間倒退；step 是直接把時間改掉 | 預設只 slew，偏差半小時要追很久；`makestep 1.0 3` 允許開機前 3 次更新時若偏差超過 1 秒就直接 step |

**為什麼**：時間偏差超過幾分鐘，Kerberos / AD 登入會失敗、TLS 憑證被判定尚未生效、資料庫複寫與分散式系統的一致性判斷會出錯；虛擬機暫停再恢復後時間常常一下子落後好幾分鐘，沒有 NTP 就永遠追不回來。

兩者預設工具不同，是設計取向差異：Ubuntu 認為多數機器只需要「當客戶端對時」，所以內建輕量的 `systemd-timesyncd`；Fedora / RHEL 沿用完整的 `chrony`，能當 NTP 伺服器、也更擅長處理虛擬機那種時間跳動的情境。

| | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 預設 | `systemd-timesyncd`（僅 SNTP 客戶端） | `chrony`（完整 NTP，可當伺服器） |
| 設定檔 | `/etc/systemd/timesyncd.conf` | `/etc/chrony.conf` |
| 查狀態 | `timedatectl timesync-status` | `chronyc tracking` / `chronyc sources -v` |

**怎麼做**：伺服器建議兩邊都統一用 chrony，設定檔與操作方式就一致了；`makestep` 允許開機初期直接跳時間，避免虛擬機開機後慢慢「滑」半小時才追上：

```bash
# 🟠 Ubuntu：改用 chrony（伺服器建議；虛擬機、需要精準時間、要當 NTP server 時）
sudo apt install -y chrony            # 安裝後 timesyncd 會自動停用
sudo vim /etc/chrony/chrony.conf      # 注意路徑在 /etc/chrony/ 目錄下

# 🔵 Fedora
sudo vim /etc/chrony.conf

# ⚪ 兩者的 chrony.conf 台灣常用設定
#   pool time.stdtime.gov.tw iburst
#   pool tw.pool.ntp.org     iburst
#   makestep 1.0 3           # 開機前 3 次更新允許直接跳時間
sudo systemctl restart chronyd && chronyc sources -v
```

**注意**：🟠 Ubuntu 的 chrony 設定檔在 `/etc/chrony/chrony.conf`（多一層目錄），🔵 Fedora 在 `/etc/chrony.conf`；寫 Ansible 或腳本時最常在這裡踩雷。

### 2.2 語系（locale）

**名詞與關係**：語系要能用，必須「先產生、再指定」——locale 資料得先編譯成二進位存在系統裡，環境變數再指向它；兩個發行版差在「產生」這一步由誰做。

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **locale** | glibc 對「語言、字元編碼、排序、日期與數字格式」的一組設定，名稱如 `en_US.UTF-8`（語言_地區.編碼） | 程式啟動時依環境變數載入；被指到的 locale 必須已存在於 `locale -a` 清單，否則就是 `setlocale` 警告 |
| **`LANG` / `LC_*`** | 指定 locale 的環境變數：`LANG` 是所有類別的預設，`LC_TIME`、`LC_COLLATE` 等可個別覆寫，`LC_ALL` 強制全部 | SSH 客戶端預設會把自己的 `LANG` 送到伺服器（`SendEnv`），所以伺服器沒產生對應 locale 時警告會在登入時出現 |
| **locale-gen / update-locale** | 🟠 Debian 系工具：`locale-gen` 依 `/etc/locale.gen` 從 `/usr/share/i18n` 原始資料編譯出 locale；`update-locale` 把 `LANG=` 寫進 `/etc/default/locale` 設定系統預設 | 兩步缺一不可：只 `update-locale` 沒 `locale-gen`，等於指到不存在的 locale |
| **localectl** | systemd 的語系 / 鍵盤配置指令，查看兩邊通用；設定時寫入 `/etc/locale.conf` | 🔵 Fedora 用它設定系統預設；🟠 Ubuntu 慣例仍走 `update-locale` |
| **langpacks-\* / glibc-langpack-\*** | 🔵 Fedora 把各語言預先編譯好的 locale 拆成套件：`glibc-langpack-zh` 提供 locale 本體，`langpacks-zh_TW` 是中介套件，順便拉進字型、輸入法與翻譯 | 裝套件 = 產生 locale，所以 Fedora 沒有 `locale-gen`；沒裝就 `locale -a` 看不到 |

**為什麼**：語系影響訊息語言、排序規則與日期格式。伺服器若設成 `zh_TW`，錯誤訊息會變成中文，拿去搜尋時幾乎找不到對應的討論；反之若完全沒產生語系，SSH 登入時會不斷出現 `setlocale: LC_ALL: cannot change locale` 警告，某些程式（Perl、Python）甚至因此執行失敗。

**怎麼做**：兩者的語系來源不同——Ubuntu 從 `/usr/share/i18n` 的原始資料現場 `locale-gen` 編譯，Fedora 則把各語言拆成 `langpacks-*` / `glibc-langpack-*` 套件預先編譯好，裝套件就有：

```bash
# ⚪ 查看
localectl status
locale -a

# 🟠 Ubuntu：產生語系並設定
sudo locale-gen en_US.UTF-8 zh_TW.UTF-8
sudo update-locale LANG=en_US.UTF-8
# 或互動式：sudo dpkg-reconfigure locales

# 🔵 Fedora：語系由 langpacks 套件提供
sudo dnf install -y langpacks-zh_TW glibc-langpack-zh
sudo localectl set-locale LANG=en_US.UTF-8
```

💡 伺服器建議 `LANG=en_US.UTF-8`（日誌與錯誤訊息用英文較好搜尋），個人桌面再用 `zh_TW.UTF-8`。

---

## 3. 使用者與 sudo

**名詞與關係**：本節的核心是「每個人用自己的帳號，透過 sudo 借用 root 的權限，而每次借用都留下紀錄」。四個名詞的關係：帳號加入管理員群組 → sudoers 規則放行該群組 → `sudo` 才會允許提權 → 提權事件寫入日誌。

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **root** | UID 0 的超級使用者，核心對它不做任何權限檢查 | 兩個發行版預設鎖定它的密碼（§3.3），日常都以 sudo 借用它的身分 |
| **sudo** | 讓一般帳號「以 root（或其他使用者）的身分執行一條指令」的工具，做不做得到由 sudoers 規則決定 | 每次提權都寫日誌；與 `su` 的差別是驗的是**自己的**密碼、而且可以細到只放行特定指令 |
| **sudoers** | sudo 的規則檔 `/etc/sudoers` 與 `/etc/sudoers.d/`（語法見 §3.2） | 發行版預設只放行一個管理員群組：🟠 `sudo`、🔵 `wheel`（§3.1） |
| **`auth.log` / journal** | 認證相關事件的日誌：🟠 Ubuntu 寫 `/var/log/auth.log`，🔵 Fedora 只進 systemd journal（`journalctl _COMM=sudo`） | sudo、sshd、PAM 都寫到這裡，是事後追溯「誰、何時、做了什麼」的唯一依據 |

**為什麼**：所有人共用 root 或同一個帳號，日誌裡只會看到「root 做了什麼」，出事時無法追溯是誰、也無法只撤銷一個人的權限。正確做法是每人一個帳號、用 sudo 提權，`sudo` 會把每一次提權連同指令記進 `auth.log` / journal。

### 3.1 建立管理員帳號

**名詞與關係**：建帳號的指令兩邊名字一樣、行為卻不同，加上管理員群組名稱也不同，是本節要先釐清的：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **useradd** | shadow-utils 的低階建帳號指令，只做參數指定的事，預設**不**建家目錄、不設密碼 | ⚪ 兩邊都有；🔵 Fedora 的 `adduser` 只是它的符號連結，行為完全相同 |
| **adduser** | 🟠 Debian 系的互動式包裝腳本：自動建家目錄、複製 `/etc/skel`、問密碼與姓名 | 底層仍呼叫 useradd；只有在 Ubuntu 上「adduser ≠ useradd」 |
| **`/etc/skel`** | 新帳號家目錄的範本目錄（`.bashrc`、`.profile` 等） | adduser 自動複製；useradd 要加 `-m` 才會建家目錄並順便複製 |
| **管理員群組（`sudo` / `wheel`）** | 被 sudoers 預設規則放行的群組：🟠 Debian 系用 `sudo`，🔵 Red Hat 系沿用 BSD 的 `wheel`（「big wheel」= 大人物） | 加入這個群組 = 取得完整 sudo 權限；群組名不同是跨發行版腳本的分歧點（§10 的 `SUDO_GROUP`） |
| **`usermod -aG`** | 把既有帳號「附加」進群組的指令 | `-a` = append；沒有 `-a` 的 `-G` 是「把群組清單**設成**這些」，會把人從其他群組踢掉 |

**為什麼**：安裝時建立的帳號往往是共用的「第一個帳號」；正式上線前應為每位管理員建立獨立帳號並加入管理員群組，之後 §4.2 關掉 root 登入與密碼登入時才有帳號可用。兩者管理員群組名稱不同（Debian 系用 `sudo`、Red Hat 系沿用 BSD 傳統的 `wheel`），這是跨發行版腳本最常寫錯的地方。

**怎麼做**：

```bash
# 🟠 Ubuntu：adduser 是互動式包裝（建家目錄、複製 skel、問密碼）
sudo adduser alice
sudo usermod -aG sudo alice

# 🔵 Fedora：用 useradd（-m 建家目錄）；adduser 只是 useradd 的別名
sudo useradd -m -s /bin/bash alice
sudo passwd alice
sudo usermod -aG wheel alice

# ⚪ 驗證
id alice
sudo -l -U alice
```

**注意**：🔵 Fedora 的 `useradd` 若忘記 `-m`，帳號建了但沒有家目錄，之後 SSH 金鑰放不進去、shell 啟動也會報錯；`usermod -aG` 的 `-a` 不可省略，否則會把使用者從其他群組移除。

### 3.2 sudo 規則

**名詞與關係**：sudoers 規則的每一行都是「**誰** 在哪台主機=(以誰的身分) 可以跑哪些指令」；下面的名詞就是這一行裡的各個零件，以及安全地寫入它們的工具：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **sudoers 規則語法** | `使用者 主機=(目標使用者:目標群組) 標籤: 指令`，例如 `deploy ALL=(ALL) NOPASSWD:ALL` | `ALL` 是萬用字元；`(ALL:ALL)` 與 `(ALL)` 的差別只是能否用 `-g` 切換群組 |
| **`%群組`** | 前綴 `%` 表示這條規則套用到整個群組的成員 | 發行版預設規則 `%sudo` / `%wheel` 就是這樣寫，§3.1 加入群組才會生效 |
| **NOPASSWD** | 規則標籤，表示執行該規則涵蓋的指令時不問密碼 | 給沒人在旁邊輸密碼的自動化帳號用；對一般管理員等於拿掉最後一道確認 |
| **visudo** | 帶檔案鎖與**語法檢查**的 sudoers 編輯器；`visudo -c` 只檢查不編輯、`-f` 可指定檔案 | 語法錯的 sudoers 會讓 sudo 整個拒絕運作，而修 sudoers 又需要 sudo——這個死結就是 visudo 存在的理由 |
| **`/etc/sudoers.d/`** | 主檔以 `@includedir` 載入的目錄，每個檔案是一段獨立規則；檔名含 `.` 或 `~` 的會被忽略 | 套件升級只碰主檔；檔案權限須 `440`，能被其他人寫入的檔案 sudo 會拒絕載入 |

**為什麼**：自動化帳號（CI、Ansible、部署腳本）沒有人在旁邊輸入密碼，需要 `NOPASSWD`；但一般管理員若也免密碼，只要終端機一時沒鎖、或某個程式被劫持，就等於直接拿到 root。另一個常見需求是讓維運人員只能重啟特定服務、看日誌，而不是整台機器的 root。

⚠️ **永遠用 `visudo` 或放在 `/etc/sudoers.d/`**，直接改 `/etc/sudoers` 打錯字會鎖死 sudo。

**怎麼做**：把規則放在 `/etc/sudoers.d/` 的獨立檔案，套件升級不會覆蓋、也方便用設定管理工具逐檔部署；寫完一定要 `visudo -c` 檢查語法：

```bash
# ⚪ 免密碼（自動化帳號用；一般管理員仍建議要密碼）
echo 'deploy ALL=(ALL) NOPASSWD:ALL' | sudo tee /etc/sudoers.d/90-deploy
sudo chmod 440 /etc/sudoers.d/90-deploy
sudo visudo -c                                # 檢查語法

# ⚪ 只允許特定指令
# %ops ALL=(root) /usr/bin/systemctl restart nginx, /usr/bin/journalctl
```

發行版預設的 sudoers 規則（實測）：

```
%sudo   ALL=(ALL:ALL) ALL      # 🟠 Ubuntu（另有 %admin ALL=(ALL) ALL 舊規則）
%wheel  ALL=(ALL)     ALL      # 🔵 Fedora
```

### 3.3 root 帳號

**為什麼**：root 是所有暴力破解的第一目標，帳號名固定、權限最大；鎖定 root 密碼（`passwd -l`：在 `/etc/shadow` 的密碼雜湊前加 `!`，任何密碼都對不上，但 `sudo -i` 這種驗自己密碼、不驗 root 密碼的路徑不受影響）後，攻擊者必須先猜對一個不知道名字的帳號、再突破 sudo，多了一層。兩個發行版預設都已鎖定，只要別手癢解開。

**怎麼做**：

- 兩者預設都**鎖定 root 密碼**（`passwd -S root` 顯示 `L`）。
- 建議保持鎖定；需要 root shell 時用 `sudo -i`。
- 🔵 Fedora 若要臨時啟用：`sudo passwd root`；再鎖回：`sudo passwd -l root`。

### 3.4 密碼與帳號政策

**名詞與關係**：密碼政策分兩層——「多久要換」由 shadow-utils 的欄位管，「換的時候能不能太簡單」由 PAM 模組在改密碼當下檢查；兩層各有自己的設定檔。

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **PAM** | Pluggable Authentication Modules，Linux 上所有需要「驗身分」的程式（login、sshd、sudo、Cockpit）共用的模組框架，設定在 `/etc/pam.d/` | 密碼複雜度、失敗鎖定等政策都做成 PAM 模組，各程式自動套用，不必逐一設定 |
| **pam_pwquality** | PAM 模組，在使用者**改密碼**時檢查強度，設定檔 `/etc/security/pwquality.conf` | 🟠 套件 `libpam-pwquality`，裝了就自動掛進 `/etc/pam.d/common-password`；🔵 預設已裝並啟用。只管新密碼，不會回頭檢查既有密碼 |
| **`minlen` / `minclass` / `maxrepeat`** | pwquality 三個最常用的參數：最短長度、至少幾種字元類別（大寫 / 小寫 / 數字 / 符號）、同一字元最多連續幾次 | 對 root 執行的 `passwd` 預設只警告不強制（`enforce_for_root` 可改） |
| **`/etc/login.defs`** | shadow-utils 的帳號預設值檔（`PASS_MAX_DAYS`、`PASS_WARN_AGE`、UID 範圍等） | 只在 useradd 建帳號那一刻被讀一次、寫進 `/etc/shadow`；改它不影響既有帳號 |
| **chage** | 直接修改 `/etc/shadow` 內既有帳號的有效期欄位（`-M` 最長天數、`-W` 提前警告、`-l` 列出） | 與 login.defs 互補：一個管「未來建的」、一個管「已經有的」 |
| **Cockpit** | 網頁管理介面（§9.3），用系統帳號密碼經 PAM 登入 | 是「就算 SSH 已關密碼登入，密碼強度仍然重要」的原因之一 |

**為什麼**：只要還有任何路徑需要密碼（console 登入、sudo、Cockpit），密碼強度就有意義；沒有複雜度限制，使用者會設 `123456`，而合規稽核（ISO 27001、PCI）也幾乎都要求有效期與複雜度政策。`login.defs` 只影響之後新建的帳號，既有帳號要用 `chage` 逐一調整，這是常被忽略的細節。

**怎麼做**：

```bash
# ⚪ 密碼有效期（/etc/login.defs 影響新帳號；chage 改既有帳號）
sudo chage -M 90 -W 14 alice        # 90 天到期，前 14 天警告
sudo chage -l alice

# ⚪ 密碼複雜度：pam_pwquality
# 🟠 Ubuntu
sudo apt install -y libpam-pwquality
# 🔵 Fedora：預設已安裝
sudo vim /etc/security/pwquality.conf
#   minlen = 12
#   minclass = 3
#   maxrepeat = 3
```

**注意**：自動化帳號（如 `deploy`）不要設密碼有效期，否則到期那天 CI 會無聲失敗。

---

## 4. SSH 伺服器

**名詞與關係**：SSH 是協定，OpenSSH 是實作，sshd 是伺服器端程式，金鑰是本章要用來取代密碼的憑證；先把這幾個層次分開：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **SSH** | Secure Shell，加密的遠端登入 / 指令執行 / 檔案傳輸協定，預設 TCP 22 | 本章唯一的遠端入口；scp、sftp、rsync、git 的 ssh 傳輸都跑在它上面 |
| **OpenSSH** | 兩個發行版都用的 SSH 實作，拆成伺服器套件 `openssh-server` 與客戶端套件（🟠 `openssh-client`、🔵 `openssh-clients`） | 版本決定支援哪些金鑰與演算法（§4.2.1） |
| **sshd** | OpenSSH 的伺服器 daemon，設定檔 `/etc/ssh/sshd_config` 與 `sshd_config.d/` | 🟠 服務名 `ssh`、🔵 `sshd`（§4.1）；本章所有「伺服器端」設定都是給它的 |
| **ssh（客戶端）** | 連線用的指令，設定在 `~/.ssh/config` 與 `/etc/ssh/ssh_config` | `ssh_config` 與 `sshd_config` 只差一個 d，改錯檔是常見烏龍 |
| **金鑰對（公鑰 / 私鑰）** | 一對數學上配對的檔案：私鑰留在工作機、公鑰放到伺服器的 `~/.ssh/authorized_keys` | 登入時伺服器用公鑰出題、客戶端用私鑰答題，密碼完全不經過網路（§4.2） |

**為什麼**：SSH 是伺服器唯一的遠端入口，也是網路上被掃描最頻繁的服務——機器一拿到公網 IP，幾分鐘內就會開始收到暴力嘗試。本節的目標是把 SSH 從「預設能用」調整成「只有持有金鑰的特定人能進」，而且改壞了還救得回來。

### 4.1 安裝與啟用

**名詞與關係**：兩邊差的不只是服務名，還有「sshd 是開機就常駐，還是有人連才啟動」：

```
🔵 Fedora（傳統常駐）：
   systemctl enable --now sshd.service ──▶ 開機就啟動 sshd，由 sshd 自己 listen 22

🟠 Ubuntu 22.10+ / 24.04（socket activation）：
   ssh.socket（enabled；由 systemd 代為 listen 22）──第一個連線到達──▶ 拉起 ssh.service（disabled，但被 socket 觸發）──▶ sshd 接手
   └─ 監聽的埠寫在 ssh.socket 的 ListenStream=，sshd_config 的 Port 在這種模式下沒有作用（§4.3）
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **unit** | systemd 管理的最小單位，依副檔名分類：`.service`（程序）、`.socket`（監聽點）、`.timer`（排程）等 | 同名的 `.socket` 與 `.service` 是一組：socket 負責聽、service 負責做 |
| **socket activation** | 由 systemd 先佔住埠，有連線進來才啟動對應的 service | 好處是開機更快、沒人用不佔資源；代價是「埠號」的設定搬到了 socket unit |
| **`ssh.socket` / `ssh.service`** | 🟠 Ubuntu 的兩個 unit：前者 enabled、後者 disabled，是 socket activation 的標準組合 | `systemctl list-unit-files` 看到「ssh.socket enabled、ssh.service disabled」是正常狀態，不是壞掉 |
| **`enable --now`** | `enable` 設定開機自動啟動、`--now` 同時立刻啟動，一條指令做兩件事 | 只 `start` 沒 `enable`，重開機就不見；只 `enable` 沒 `start`，現在還連不上 |

**為什麼**：Server 版安裝時通常已含 sshd，但 Workstation / 最小安裝可能沒有；更重要的是兩者的服務名稱不同（`ssh` vs `sshd`），寫錯會得到 `Unit not found`，腳本裡必須分支處理。

**怎麼做**：

```bash
# 🟠 Ubuntu（Server 版安裝時勾選即有）
sudo apt install -y openssh-server
sudo systemctl enable --now ssh          # 服務名是 ssh（不是 sshd）

# 🔵 Fedora（Server 版預設已啟用）
sudo dnf install -y openssh-server
sudo systemctl enable --now sshd         # 服務名是 sshd
```

**注意**：

> 🟠 **Ubuntu 22.10+ / 24.04 使用 socket activation**：`ssh.socket` 啟用、`ssh.service` 停用（實測 `systemctl list-unit-files | grep ^ssh` 顯示 `ssh.socket enabled`、`ssh.service disabled`）。意思是 sshd 在第一個連線進來時才啟動。**改 Port 時只改 `sshd_config` 不夠**，見 §4.3。

### 4.2 金鑰登入與基本強化

**為什麼**：密碼可被猜測、暴力破解、釣魚；金鑰登入是「證明你持有私鑰」的挑戰-回應（伺服器用公鑰出一道只有私鑰能解的題，客戶端答對即通過），私鑰永遠不離開你的電腦，伺服器只存公鑰。搭配 `PasswordAuthentication no`（sshd 選項，關閉密碼驗證；預設 `yes`），網路上的暴力破解流量就完全無效。本節流程已於兩台實驗機實測（金鑰登入成功、密碼登入被拒、authorized_keys 限制選項生效）。

#### 4.2.1 金鑰類型與產生

**名詞與關係**：金鑰的「類型」與登入時的「簽章演算法」是兩回事，`ssh-rsa` 被停用的其實是後者；另外私鑰檔本身還有一層 passphrase 保護：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **ssh-keygen** | OpenSSH 產生、檢視、轉換金鑰與簽發憑證的工具 | 產生的私鑰以 passphrase 加密後存檔；`-l` 看指紋、`-p` 改 passphrase |
| **金鑰類型 vs 簽章演算法** | 類型是數學結構（rsa、ed25519、ecdsa）；簽章演算法是登入時「怎麼用它簽」——RSA 金鑰可用 `ssh-rsa`（SHA-1）或 `rsa-sha2-256/512` 簽 | OpenSSH 8.8+ 預設拒絕的是 `ssh-rsa` 這種**簽法**，不是 RSA 金鑰本身；只有對方是只會 ssh-rsa 的老系統才會登不進去 |
| **passphrase 與 KDF** | passphrase 是加密私鑰檔的口令；KDF（`-a` 回合數）把口令反覆運算拉伸成加密金鑰，回合越多離線暴力破解越慢 | 私鑰檔被偷時的最後防線；配合 ssh-agent（§4.2.5）就不必每次輸入 |
| **指紋（fingerprint）** | 公鑰的 SHA256 雜湊（`SHA256:...`），人可核對的短識別碼 | 伺服器日誌記的是指紋而非整把公鑰；核對「登入的是哪把金鑰」就靠它 |
| **FIDO2 硬體金鑰（`-sk`）** | 私鑰存在 YubiKey 等裝置內，電腦上的 `id_ed25519_sk` 只是「把手」；`-O resident` 把把手也存進裝置、`-O verify-required` 每次登入要輸 PIN | 需要兩端 OpenSSH 8.2+；私鑰物理上偷不走，但裝置遺失就要撤銷 |

**為什麼**：金鑰演算法不是都一樣安全——`ssh-rsa`（SHA-1 簽章）已被新版 OpenSSH 預設拒絕，用舊金鑰會在升級後突然登不進去；ed25519 短、快、沒有參數選擇的陷阱，是目前的首選。私鑰是最終的信任根，所以要在自己的工作機產生、絕不在伺服器上產生再複製回來。

| 類型 | 建議 | 說明 |
|---|---|---|
| **ed25519** | ✅ 首選 | 短、快、安全；OpenSSH 6.5+（2014）皆支援 |
| rsa 4096 | 相容舊設備時 | 較長、較慢；**避免 rsa 2048 以下與 SHA-1 簽章**（`ssh-rsa` 已被新版 OpenSSH 預設停用，`rsa-sha2-256/512` 仍可） |
| ecdsa | 不建議 | NIST 曲線信任疑慮；能用 ed25519 就不用 |
| ed25519-sk / ecdsa-sk | 高安全 | 綁 FIDO2 硬體金鑰（YubiKey）：私鑰在裝置內，每次登入需觸碰 |

**怎麼做**：在**你的工作機**（不是伺服器）產生：

```bash
ssh-keygen -t ed25519 -a 100 -C "alice@laptop-2026"
#   -t 類型；-a 100 私鑰加密的 KDF 回合數（提高離線破解成本）；-C 註解（辨識用，會出現在公鑰尾端）
#   會問存放路徑（預設 ~/.ssh/id_ed25519）與 passphrase：
#   ⚠️ 請設 passphrase；私鑰檔被偷時是最後一道防線。配合 ssh-agent 就不必每次輸入。
ssh-keygen -t ed25519-sk -O resident -O verify-required -C "alice-yubikey"    # FIDO2 硬體金鑰版
ssh-keygen -t rsa -b 4096 -a 100 -C "alice@laptop-rsa"                        # 需相容舊系統時
ls -l ~/.ssh/          # id_ed25519（私鑰，600）與 id_ed25519.pub（公鑰，644）
ssh-keygen -l -f ~/.ssh/id_ed25519.pub        # 指紋（實測輸出 256 SHA256:... alice@laptop (ED25519)）；用來核對伺服器日誌
ssh-keygen -p -f ~/.ssh/id_ed25519            # 更改 / 補設 passphrase
```

**注意**：私鑰檔權限必須 `600`、`~/.ssh` 必須 `700`，否則 ssh 會拒絕使用（`WARNING: UNPROTECTED PRIVATE KEY FILE!`）。

#### 4.2.2 把公鑰放到伺服器

**名詞與關係**：公鑰放上去之後，sshd 讀不讀得到取決於三件事：檔案位置對、權限夠嚴（StrictModes）、🔵 SELinux 標籤正確。

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **`~/.ssh/authorized_keys`** | 伺服器端每個帳號各自的公鑰清單，一行一把 | sshd 登入時逐行比對；位置可由 `AuthorizedKeysFile` 改（§4.2.4） |
| **ssh-copy-id** | 客戶端小腳本：用密碼登入一次，把公鑰附加進 authorized_keys 並把權限設好 | 等於「方法 2」的自動版；前提是伺服器此刻還接受密碼 |
| **StrictModes** | sshd 選項（預設 `yes`）：家目錄、`.ssh`、`authorized_keys` 任一個能被他人寫入，就整份公鑰檔忽略不讀 | 「權限太開 → 金鑰被無聲忽略 → 客戶端只看到 Permission denied」的來源；日誌會有 `Authentication refused: bad ownership or modes` |
| **SELinux 標籤（`ssh_home_t`）** | 🔵 Fedora 每個檔案都帶一個安全標籤（`ls -Z` 可見），sshd 只被政策允許讀標為 `ssh_home_t` 的檔案 | `cp` 會沿用來源的標籤、手動 `mkdir` 依父目錄推導，兩者都可能得到錯的標籤 |
| **restorecon** | 依 SELinux 政策的預設規則，把檔案標籤重設為「應該的值」 | 標籤問題的標準修法；§7 會把整套機制講清楚 |

**為什麼**：公鑰要在「還能用密碼登入」的時候放上去，順序反了就得回 console 救。手動複製時最常出錯的不是內容而是權限——`authorized_keys` 或家目錄權限太開，sshd 會因 `StrictModes` 默默忽略那把金鑰，客戶端只看到 `Permission denied`，不會有任何提示。

**怎麼做**：

```bash
# 方法 1：ssh-copy-id（最簡單；此時伺服器還允許密碼登入）
ssh-copy-id -i ~/.ssh/id_ed25519.pub alice@192.168.1.10
# 方法 2：手動（沒有 ssh-copy-id、或用 console / 雲端 metadata 時）
cat ~/.ssh/id_ed25519.pub | ssh alice@192.168.1.10 \
  'install -d -m 700 ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys'
# 方法 3：安裝時就放（01 章：autoinstall 的 ssh.authorized-keys、Kickstart 的 sshkey、cloud-init 的 ssh_authorized_keys）
# 方法 4：本手冊的 scripts/bootstrap.sh --pubkey "..."
```

伺服器端的結構與權限（sshd 的 `StrictModes yes` 預設會檢查，錯了就默默拒絕金鑰）：

```
/home/alice/                 擁有者 alice，不可群組/其他可寫
└── .ssh/                    700  alice:alice
    └── authorized_keys      600  alice:alice；每行一把公鑰
```

**注意**：🔵 Fedora 多一個 SELinux 標籤的檢查點——用 `cp` 從別處複製或手動 `mkdir` 的 `.ssh` 目錄，標籤可能不是 `ssh_home_t`，權限看起來全對但 sshd 就是讀不到：

```bash
# 伺服器端檢查
ls -ld ~ ~/.ssh ~/.ssh/authorized_keys
# 🔵 Fedora：家目錄若是手動建立或從別處複製，SELinux 標籤可能不對（sshd 讀不到）：
sudo restorecon -Rv ~/.ssh          # 標籤應為 ssh_home_t（ls -Z 查看）
```

#### 4.2.3 測試與切換到「只允許金鑰」

**名詞與關係**：這一節要動 sshd 的設定，先弄清楚設定檔是怎麼被拼起來、怎麼驗證、以及 🔵 Fedora 多出來的那一層：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **drop-in（`sshd_config.d/*.conf`）** | 主檔第一行 `Include /etc/ssh/sshd_config.d/*.conf` 載入的片段檔，兩個發行版都有 | 檔案按檔名排序合併；sshd 對每個選項採「**第一次出現的值**」，所以檔名數字小的贏、主檔內同名設定輸給 drop-in |
| **`sshd -t` / `sshd -T`** | `-t` 只檢查語法（錯誤時非 0 退出）；`-T` 印出所有選項的**最終生效值** | `-T` 是驗證 drop-in 排序結果的唯一可靠方式，別用「看檔案」猜 |
| **reload vs restart** | reload 送 SIGHUP 讓 sshd 重讀設定、既有連線不斷；restart 把程序砍掉重來 | 改設定用 reload 才保得住那條救命的 session |
| **KbdInteractiveAuthentication** | 「鍵盤互動式」驗證：sshd 把問答交給 PAM（§3.4），問的可以是密碼也可以是 OTP | 只關 `PasswordAuthentication` 而留它，PAM 仍會問密碼——所以兩個要一起關；要做 2FA 時再單獨開回來 |
| **crypto-policies** | 🔵 Fedora / RHEL 的全系統加密策略（`update-crypto-policies`），透過 `40-redhat-crypto-policies.conf` 把 Ciphers / MACs / KexAlgorithms 塞進 sshd | 這些選項寫在主檔或數字大於 40 的 drop-in 會被它蓋掉；正確做法是改策略本身 |
| **`50-cloud-init.conf`** | 🟠 雲端映像由 cloud-init 產生的 drop-in，常內含 `PasswordAuthentication yes` | 同樣受排序規則影響：自訂檔用更小的數字才壓得過它 |

**為什麼**：關掉密碼登入是不可逆的動作——如果金鑰其實沒放對，reload 之後就再也進不去，只能靠 console。所以先用「強制純金鑰」的方式測一次，確認日誌出現 `Accepted publickey`，再改設定；改設定的整個過程都保留原本的 SSH session，reload 不會中斷既有連線，改壞了還能在同一個視窗改回來。

⚠️ 順序很重要：**先確認金鑰能登入，再關閉密碼**；並保留目前的 SSH session 不要關（改壞了還能救）。

**怎麼做**：先測試：

```bash
# 在工作機：明確指定金鑰、關閉密碼備援，確認純金鑰能進
ssh -i ~/.ssh/id_ed25519 -o PasswordAuthentication=no -o BatchMode=yes alice@192.168.1.10 'echo OK'
ssh -v alice@192.168.1.10 2>&1 | grep -E "Offering|Authenticated|Permission"   # 除錯：看送了哪把金鑰、哪一步失敗
# 伺服器端看日誌（實測輸出 "Accepted publickey for alice from ... ED25519 SHA256:..."）
journalctl -u ssh -g "Accepted|Failed" -n 20    # 🟠 （🔵 -u sshd）
```

兩者都支援 `Include /etc/ssh/sshd_config.d/*.conf`，**設定請放 drop-in 檔而非直接改主檔**（套件升級不會覆蓋，也方便用 Ansible 管理）。每一行設定的用意寫在區塊內註解；`sshd -t` 先檢查語法再 reload，語法錯時 `&&` 會擋住 reload，避免把 sshd 弄到起不來：

```bash
sudo tee /etc/ssh/sshd_config.d/10-hardening.conf <<'EOF'
PermitRootLogin no                 # root 一律不可直接登入（用一般帳號 + sudo）
PasswordAuthentication no          # 關閉密碼（只留金鑰）
KbdInteractiveAuthentication no    # 關閉鍵盤互動式（PAM 密碼 / OTP 的通道；要用 2FA 時再開，見 08 章 §2.3）
PubkeyAuthentication yes
MaxAuthTries 3                     # 一個連線最多試 3 次
LoginGraceTime 30                  # 30 秒內沒登入就斷線（預設 120，縮短可減少資源耗盡攻擊）
X11Forwarding no                   # 伺服器不需要 GUI 轉發
AllowTcpForwarding no              # 不當跳板（需要用 ssh -L / -R 的管理者可對特定群組 Match 開放）
ClientAliveInterval 300            # 每 5 分鐘探測一次閒置連線
ClientAliveCountMax 2              # 2 次沒回應就斷（10 分鐘）
AllowGroups sshusers sudo wheel    # 只有這些群組能 SSH（兩個發行版的管理員群組都列上，不存在的群組會被忽略）
EOF
sudo sshd -t && sudo systemctl reload ssh     # 🟠  （🔵 改成 sshd）；sshd -t 語法檢查，錯了不會 reload
sudo sshd -T | grep -E "^(permitrootlogin|passwordauthentication|maxauthtries|allowgroups)"   # 看「實際生效值」（實測用法）
```

`sudo tee ... <<'EOF'` 這種寫法的意義見 [附錄 A](A-常用指令與語法說明.md)。

**注意**：drop-in 的「先讀到者優先」規則和一般直覺相反，檔名數字太大會被發行版預設檔蓋掉、設定看似寫了卻沒生效，一定要用 `sshd -T` 驗證實際值：

> 🔵 Fedora 的 `/etc/ssh/sshd_config.d/` 已內建 `40-redhat-crypto-policies.conf` 與 `50-redhat.conf`（實測）。加密演算法由 **crypto-policies** 統一管理，`sshd_config` 內寫 `Ciphers` 會被忽略；要改請用 `update-crypto-policies --set DEFAULT:NO-SHA1` 之類的子策略或自訂策略（08 章 §3.1）。
> drop-in 檔按檔名排序、**先讀到的設定優先**，所以自訂檔要用小於 40 的數字開頭（如 `10-`）才能覆蓋 Red Hat 預設。
> 🟠 Ubuntu 24.04 也有 `/etc/ssh/sshd_config.d/` 但預設為空；雲端映像可能有 `50-cloud-init.conf`（內含 `PasswordAuthentication yes`！用更小的數字覆蓋它）。

#### 4.2.4 authorized_keys 進階：限制每把金鑰能做什麼（已實測）

**名詞與關係**：sshd 有兩個層級可以設限制——寫在 `authorized_keys` 該行前面的「金鑰選項」只管那一把金鑰，`sshd_config` 的全域選項與 `Match` 區塊管一類對象；兩者取交集。

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **金鑰選項（key options）** | 寫在 authorized_keys 公鑰前面、逗號分隔的限制條件，只對那一把金鑰生效 | 只能比全域設定更嚴、不能更寬：全域 `AllowTcpForwarding no` 時，金鑰選項開不回來 |
| **`$SSH_ORIGINAL_COMMAND`** | 被 `command=` 強制執行的腳本內可讀到「客戶端原本要求的指令」的環境變數 | 讓 rsync-only / git-only 腳本做白名單檢查，而不是無條件執行 |
| **`AuthorizedKeysFile` / `AuthorizedKeysCommand`** | sshd 全域選項：前者改公鑰檔的位置（可用 `%u` 代入帳號），後者改成呼叫外部程式取得公鑰 | 把「誰能登入」的控制權從使用者的家目錄收回到管理者或中央系統（LDAP、IAM） |
| **`Match` 區塊** | `sshd_config` 內依 `User` / `Group` / `Address` 條件套用不同設定的段落，必須放在檔案（或 drop-in）末尾 | 金鑰選項管「一把金鑰」，Match 管「一類對象」，互補使用 |

**為什麼**：備份、rsync、git 部署這類自動化用途的金鑰通常沒有 passphrase、又放在會被多人接觸的機器上；一旦外洩，攻擊者拿到的是一個完整 shell。把限制寫在公鑰那一行，就算私鑰被偷，也只能從指定網段連進來、只能執行指定的那支指令，損害被限制在最小範圍。

**怎麼做**：在公鑰前面加選項（逗號分隔、與公鑰之間一個空白）：

```
from="192.168.1.0/24,10.8.0.0/24",no-port-forwarding,no-agent-forwarding,no-X11-forwarding ssh-ed25519 AAAA... alice@laptop
command="/usr/local/bin/backup-receiver.sh",restrict ssh-ed25519 AAAA... backup@nas
```

| 選項 | 意義 |
|---|---|
| `from="pattern"` | 只接受來自這些 IP / 網段 / 主機名的連線（實測：不在名單內的來源直接拒絕） |
| `command="cmd"` | 不管客戶端要求執行什麼，**一律只執行這個指令**（實測 `ssh ... id` 仍回傳 `uptime` 的結果）；原始指令在 `$SSH_ORIGINAL_COMMAND` 可供腳本檢查。用於備份專用帳號、git、rsync-only |
| `restrict` | 一次關閉所有轉發、pty、agent、X11、user-rc（OpenSSH 7.2+；建議搭配 `command=`） |
| `no-pty` | 不給互動終端 |
| `expiry-time="20261231"` | 金鑰到期日（OpenSSH 8.x+） |
| `environment="VAR=val"` | 設定環境變數（需 `PermitUserEnvironment`） |

sshd 端對應的全域限制：`AuthorizedKeysFile`（改公鑰位置，例如集中在 `/etc/ssh/authorized_keys/%u`，使用者就無法自行加金鑰）、`AuthorizedKeysCommand`（從 LDAP / API 動態取得公鑰）、`Match User|Group|Address` 區塊（對特定對象放寬或收緊，例如只讓 `Match Group admins` 用 `AllowTcpForwarding yes`）。

#### 4.2.5 客戶端：ssh-agent、跳板與多把金鑰

**名詞與關係**：這一節全在工作機端。ssh-agent 解決「passphrase 要一直輸」，ProxyJump 解決「經跳板連內網」，`~/.ssh/config` 把這些設定固定下來；`-A` 是要避開的那個選項。

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **ssh-agent** | 在工作機背景保管「已解密的私鑰」的程序，`ssh` 透過一個 socket 請它代為簽名 | 私鑰不離開 agent；`ssh-add` 是把金鑰放進去（並輸入一次 passphrase）的指令 |
| **agent forwarding（`ssh -A`）** | 把本機 agent 的 socket 轉發到遠端主機，讓遠端的 `ssh` 也能請你的 agent 簽名 | 遠端的 root 可以直接用那個 socket 冒充你——跳板機被入侵時，內網全部一起淪陷 |
| **ProxyJump（`ssh -J`）** | 先連跳板、在跳板上開一條到目標的 TCP 通道、再在通道內做一次完整的「本機 ↔ 目標」SSH | 認證始終在本機完成，跳板只看到加密流量；取代 `-A` 與舊式 `ProxyCommand nc` |
| **`~/.ssh/config`** | 客戶端的每主機設定檔：`Host` 別名 → 實際主機名、帳號、金鑰、跳板、埠 | scp / rsync / git 也讀它；§4.4 再舉一個最小範例 |
| **IdentitiesOnly** | 只送 `IdentityFile` 指定的金鑰，不把 agent 裡每一把都輪流試 | 對應伺服器端的 `MaxAuthTries`（§4.2.3）：金鑰多時試到第 4 把就被踢 |

**為什麼**：設了 passphrase 之後若每連一次就要輸入一次，使用者最後一定會把 passphrase 拿掉；ssh-agent 讓你只輸入一次就能重複使用。另一個常見的錯誤是為了經跳板機連內網而用 `ssh -A`，這會讓跳板機在你連線期間能冒用你的金鑰——跳板機被入侵時，內網所有機器一起淪陷；`ProxyJump` 只轉發 TCP 連線，金鑰始終留在本機。

**怎麼做**：

```bash
# ssh-agent：輸入一次 passphrase，之後自動使用
eval "$(ssh-agent -s)"; ssh-add ~/.ssh/id_ed25519          # 桌面環境（GNOME Keyring / KDE Wallet）通常已自動啟動 agent
ssh-add -l                                                  # 已載入的金鑰
ssh-add -t 4h ~/.ssh/id_ed25519                             # 4 小時後自動卸載
# ⚠️ ssh -A（agent forwarding）會讓跳板機能用你的金鑰；改用 ProxyJump
ssh -J bastion.example.com alice@10.0.0.10                  # 經跳板直連（金鑰不離開本機）
```

`~/.ssh/config`（多台主機、多把金鑰時必備；`chmod 600`）。金鑰一多，ssh 預設會把 agent 裡每一把都試一遍，超過伺服器的 `MaxAuthTries 3` 就被踢掉，`IdentitiesOnly yes` 就是解這個問題：

```
Host *
    AddKeysToAgent yes
    IdentitiesOnly yes                 # 只送 IdentityFile 指定的金鑰（金鑰多時避免被伺服器 MaxAuthTries 擋）
    ServerAliveInterval 60
    HashKnownHosts yes
Host bastion
    HostName bastion.example.com
    User alice
    IdentityFile ~/.ssh/id_ed25519
Host web01 web02
    HostName %h.internal.example.com
    User deploy
    IdentityFile ~/.ssh/id_deploy
    ProxyJump bastion
Host github.com
    IdentityFile ~/.ssh/id_github
```

#### 4.2.6 金鑰管理原則

**名詞與關係**：原則清單最後一條提到的「SSH 憑證」是另一套信任模型——伺服器不再逐台存公鑰，改成信任一把 CA：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **SSH 憑證（certificate）** | 由 CA 私鑰對「使用者公鑰 + 身分 + 有效期 + 允許的帳號（principal）」簽出的檔案（`id_ed25519-cert.pub`） | 取代逐台放公鑰：伺服器只需信任 CA，憑證到期自動失效，離職不必去每台機器刪行 |
| **CA（Certificate Authority）** | 一把專門用來簽憑證、平時離線保管的金鑰對 | 它的私鑰外洩 = 所有伺服器淪陷，比任何單一使用者金鑰都重要 |
| **`TrustedUserCAKeys`** | sshd 選項：指向 CA 公鑰檔，凡是它簽出的憑證都接受 | 有它就不再需要 authorized_keys（兩者也可並存過渡） |
| **Teleport / step-ca** | 把「簽發短效憑證 + SSO 登入 + 稽核錄影」包成產品的工具 | 底層就是上述 SSH 憑證機制，只是把 CA 與簽發流程自動化 |

**為什麼**：金鑰不會過期、不會被「改密碼」作廢，離職一年後仍能登入的情況並不罕見。金鑰是否好管理，取決於一開始的規則：一人一把、能對應到具體的人與裝置，撤銷時才知道要刪哪一行。

**怎麼做**：

- **一人一把金鑰、一台裝置一把**：離職 / 遺失只需從 `authorized_keys` 刪除那一行；不要多人共用。
- 私鑰**不備份到伺服器**、不放進 git；passphrase 必設。
- 定期（每年）檢視伺服器上的 `authorized_keys`，刪除不明或離職者的公鑰；用 `ssh-keygen -l -f authorized_keys` 列出指紋逐一核對。
- 大規模環境：改用 **SSH 憑證（CA）**：`ssh-keygen -s ca_key -I alice -n alice,deploy -V +52w id_ed25519.pub` 簽發、sshd 設 `TrustedUserCAKeys`，伺服器不必逐台放公鑰；或用 Teleport / step-ca 等。
- 進一步：08 章 §2.3 的 TOTP 雙因子、§3 完整強化清單、§4 Fail2ban。

### 4.3 更改 SSH Port（含 SELinux / socket 差異）

**名詞與關係**：改埠會牽動三個彼此獨立的系統——🟠 systemd 的 socket、🔵 SELinux 的埠標籤、以及兩邊的防火牆；漏掉任何一個都是「改完連不上」：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Fail2ban** | 讀日誌、把反覆登入失敗的來源 IP 暫時加進防火牆黑名單的工具（08 章 §4） | 22 埠的掃描雜訊越多，它要處理的事件越多；改埠減少的正是這種雜訊 |
| **`systemctl edit` / override** | 為某個 unit 建立 `/etc/systemd/system/<unit>.d/override.conf` 覆寫片段，不動套件原檔 | 改 `ssh.socket`（§4.1）的監聽埠只能這樣做；`ListenStream=` 空行是「先清空清單」的 systemd 慣例，再加新值 |
| **SELinux 埠標籤（`ssh_port_t`）** | 🔵 SELinux 政策不只標檔案，也給每個埠號標類型；sshd 只被允許 bind 標為 `ssh_port_t` 的埠 | 沒標新埠 → sshd 啟動時 bind 被拒 → 服務起不來；`semanage port` 是加標籤的工具（套件 `policycoreutils-python-utils`） |
| **ufw / firewalld** | 兩個發行版的防火牆前端（§6 詳述） | 埠改了但防火牆仍只放 22，等於自己把自己鎖在門外 |

**為什麼**：22 埠每天會收到數千次自動化掃描與暴力嘗試，塞爆 `auth.log`、也讓 Fail2ban 忙不完。改埠**不是安全措施**（掃描器幾分鐘就找得到），但能把日誌雜訊降 90% 以上，讓真正的攻擊比較容易被看見。若已做到金鑰登入 + 防火牆限來源，改不改埠都可以。

**怎麼做**：sshd 的 drop-in 兩邊相同，但各自多一個發行版特有的步驟——🟠 Ubuntu 的 sshd 由 `ssh.socket` 啟動，監聽埠由 socket 決定，只改 `sshd_config` 沒用；🔵 Fedora 的 SELinux 只允許 sshd 綁 22，沒先標記新埠 sshd 會直接起不來。兩邊都記得同步開防火牆，否則改完就把自己鎖在外面：

```bash
# ⚪ 共通：sshd_config drop-in
echo "Port 2222" | sudo tee /etc/ssh/sshd_config.d/05-port.conf

# 🟠 Ubuntu 24.04：因為是 socket activation，還要覆寫 ssh.socket
sudo systemctl edit ssh.socket
#   [Socket]
#   ListenStream=
#   ListenStream=2222
sudo systemctl daemon-reload
sudo systemctl restart ssh.socket
sudo ufw allow 2222/tcp

# 🔵 Fedora：SELinux 只允許 sshd 綁 22，必須先標記新埠
sudo semanage port -a -t ssh_port_t -p tcp 2222     # 需 policycoreutils-python-utils
sudo firewall-cmd --permanent --add-port=2222/tcp && sudo firewall-cmd --reload
sudo systemctl restart sshd
```

**注意**：改完先在另一個視窗用新埠連一次（`ssh -p 2222 ...`）確認能進，再關掉舊的 session；🟠 Ubuntu 的 `ListenStream=` 空行不能省，那是清掉原本 22 的寫法。

### 4.4 客戶端設定（`~/.ssh/config`）

**為什麼**：改了埠、換了帳號與金鑰之後（`~/.ssh/config` 的 `Host` 別名機制見 §4.2.5，這裡是加上 `Port` 後的最小範例），每次都打 `ssh -p 2222 -i ~/.ssh/id_ed25519 alice@192.168.1.10` 既冗長又容易錯；寫進 `~/.ssh/config` 之後只要 `ssh web01`，scp / rsync / git 也會自動套用同樣的設定。

**怎麼做**：

```
Host web01
    HostName 192.168.1.10
    User alice
    Port 2222
    IdentityFile ~/.ssh/id_ed25519
    ServerAliveInterval 60
```

---

## 5. 網路基本設定（詳見第 05 章）

**名詞與關係**：兩個發行版的網路設定分成「描述層」與「執行層」，DNS 則統一交給 systemd-resolved：

```
描述層（你編輯的東西）：
   🟠 Netplan（/etc/netplan/*.yaml）                       🔵 nmcli / keyfile（/etc/NetworkManager/system-connections/）
          │ netplan apply 轉譯成 networkd 設定                     │ 直接就是 NetworkManager 的設定
          ▼                                                      ▼
執行層（真正設定介面的 daemon）：
   systemd-networkd（Ubuntu Server 預設 renderer）          NetworkManager（Fedora 全線；Ubuntu Desktop 也用它）
          │                                                      │
          └──────── 兩者都把 DNS 查詢交給 ───────────────────────┘
                                   ▼
   systemd-resolved（本機 stub resolver 127.0.0.53；/etc/resolv.conf 是指向它的符號連結；resolvectl 查狀態）
                                   ▼
   核心網路堆疊（`ip` 指令看到的是最終結果，不管上面是誰設的）
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **DHCP / 租約** | 由伺服器動態配發 IP 的協定；配發的 IP 有「租約」期限，到期要續租，續不到或伺服器重啟後可能拿到不同 IP | 伺服器要的是固定的 IP，所以改成靜態設定（或在 DHCP 伺服器端做 MAC 綁定） |
| **Netplan** | 🟠 Ubuntu 的網路設定抽象層：用 YAML 描述，`netplan apply` 轉譯給底層的 renderer（networkd 或 NetworkManager）執行 | 它本身不設定網路，只是翻譯；Server 版 renderer 預設是 networkd |
| **systemd-networkd** | systemd 內建的網路設定 daemon，設定檔在 `/run/systemd/network/`（由 Netplan 產生） | 適合伺服器的靜態設定；沒有 Wi-Fi、VPN 等桌面功能 |
| **NetworkManager / nmcli** | 🔵 Fedora 的網路管理 daemon 與其 CLI；設定以「connection」為單位存成 keyfile | 一張網卡可以有多個 connection 設定檔，啟用哪一個由 `nmcli con up` 決定 |
| **connection（nmcli）** | NetworkManager 的一份設定，有自己的名稱，綁定某個介面 | 名稱 ≠ 介面名（雲端映像常是 `cloud-init ens18`），`nmcli con mod` 用的是 connection 名稱 |
| **systemd-resolved / resolvectl** | 兩邊預設都啟用的本機 DNS 快取與轉發 daemon，`resolvectl status` 看每個介面實際用哪些 DNS | Netplan 與 NetworkManager 都把 nameservers 交給它；`/etc/resolv.conf` 只是它的符號連結，直接改會被蓋掉 |
| **`netplan try`** | 套用設定並倒數 120 秒，期間沒按 Enter 確認就自動還原 | 遠端改網路的保命符；`netplan apply` 沒有這個保護 |

**為什麼**：伺服器用 DHCP 拿到的 IP 在租約更新或 DHCP 伺服器重啟後可能改變，DNS 記錄、防火牆規則、監控目標會全部失效；上線前應改成靜態 IP。兩者的網路管理框架不同：Ubuntu Server 用 Netplan（YAML 描述，背後交給 systemd-networkd），Fedora 全線用 NetworkManager（`nmcli` 操作）。

**怎麼做**：先看目前的介面名稱（`ens18` 只是範例，實機請以 `ip -br addr` 為準），再改設定。🟠 Netplan 的 `netplan try` 是遠端改網路的保命符，沒在 120 秒內按確認就自動還原：

```bash
# ⚪ 查看
ip -br addr
ip route
resolvectl status | head -20

# 🟠 Ubuntu Server：Netplan 靜態 IP
sudo tee /etc/netplan/01-static.yaml <<'EOF'
network:
  version: 2
  ethernets:
    ens18:
      dhcp4: false
      addresses: [192.168.1.10/24]
      routes:
        - to: default
          via: 192.168.1.1
      nameservers:
        addresses: [1.1.1.1, 8.8.8.8]
        search: [example.com]
EOF
sudo chmod 600 /etc/netplan/01-static.yaml     # 否則會警告 permissions too open
sudo netplan try                                # 120 秒內沒確認會自動還原（防斷線）
sudo netplan apply

# 🔵 Fedora：NetworkManager 靜態 IP
nmcli con show
sudo nmcli con mod "ens18" ipv4.method manual \
    ipv4.addresses 192.168.1.10/24 ipv4.gateway 192.168.1.1 \
    ipv4.dns "1.1.1.1 8.8.8.8" ipv4.dns-search example.com
sudo nmcli con up "ens18"
```

**注意**：🔵 `nmcli con mod` 的第一個參數是 connection 名稱，不一定等於介面名（雲端映像常叫 `cloud-init ens18` 或 `Wired connection 1`），先用 `nmcli con show` 確認。

---

## 6. 防火牆基本啟用（詳見第 05 章）

**名詞與關係**：ufw 與 firewalld 都不是防火牆本體，而是幫你產生核心 netfilter / nftables 規則的「前端」；差別在抽象方式——ufw 是一條條規則，firewalld 用 zone 分組：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **封包過濾（netfilter / nftables）** | 核心內依規則決定每個封包放行或丟棄的機制；nftables 是現行的規則語言與工具 | ufw 與 firewalld 底層現在都輸出 nftables 規則；同一台機器只能開一個前端，否則規則互相覆蓋 |
| **ufw** | Uncomplicated Firewall，🟠 Ubuntu 的簡易前端：`default` 定預設策略、`allow` / `deny` 加規則、`enable` 啟用 | 規則存在 `/etc/ufw/`；`OpenSSH` 這種名稱來自 `/etc/ufw/applications.d/` 的應用程式設定檔 |
| **firewalld / firewall-cmd** | 🔵 Fedora 的動態防火牆 daemon 與其 CLI，用 zone 組織規則 | 開機即 enforce；「動態」指改規則不必重載整個規則集、不中斷既有連線 |
| **zone** | firewalld 的信任等級群組（`public`、`internal`、`trusted`…），每張網卡屬於一個 zone，規則掛在 zone 上 | 預設所有網卡在 `public`；本章只動 public，多網卡分區見 05 章 |
| **service（firewalld）** | 預先定義的「服務名 → 埠 / 協定」對照（`/usr/lib/firewalld/services/*.xml`），如 `https` = 443/tcp、`cockpit` = 9090/tcp | `--add-service` 比 `--add-port` 可讀；沒有現成定義時才直接用埠號 |
| **runtime vs `--permanent`** | firewalld 有兩份設定：runtime 立即生效但重開消失；permanent 只寫檔，要 `--reload` 才載入成 runtime | 「改了沒生效」與「重開機規則不見」兩個常見抱怨的共同根源 |

**為什麼**：之後裝的資料庫、快取、監控 exporter 很多預設就監聽 `0.0.0.0`，沒有防火牆就等於直接暴露在網路上。上線前先把「預設拒絕、只放行 SSH」立起來，之後每開一個服務才刻意加一條規則。兩者的預設狀態相反：Ubuntu 的 ufw 裝了但沒啟用，Fedora 的 firewalld 開機就在 enforce。

**怎麼做**：一定要先放行 SSH 再 `enable`，順序反了遠端 session 會立刻斷線：

```bash
# 🟠 Ubuntu：ufw（預設安裝但未啟用）
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow OpenSSH          # 或 ufw allow 22/tcp
sudo ufw enable
sudo ufw status verbose

# 🔵 Fedora：firewalld（預設安裝且啟用，預設 zone = public，只放行 ssh/dhcpv6-client/mdns）
sudo firewall-cmd --state
sudo firewall-cmd --list-all
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

**注意**：🔵 firewalld 的 `--permanent` 只寫設定檔不生效，沒 `--permanent` 則重開機就消失；要「現在生效且永久」就是 `--permanent` 加 `--reload`（或同一條規則下兩次）。

---

## 7. 強制存取控制：AppArmor 與 SELinux

**名詞與關係**：MAC 是疊在傳統權限**之上**的第二道關卡；AppArmor 與 SELinux 是同一個概念的兩種實作，差在「規則綁什麼」，而 SELinux 的三個常用操作（標籤、boolean、埠）都是從「標籤」這個核心衍生出來的：

```
每一次存取：  DAC（傳統 rwx 權限）「這個使用者能碰這個檔嗎？」──通過──▶ MAC「這個『程式』被允許碰這類東西嗎？」──▶ 兩關都過才放行
                                                                          │
   🟠 AppArmor：規則綁「程式路徑」 ──▶ profile（/etc/apparmor.d/usr.sbin.nginx）列出 nginx 可讀寫哪些路徑；enforce 擋、complain 只記
   🔵 SELinux ：規則綁「標籤」     ──▶ 程序帶 httpd_t、檔案帶 httpd_sys_content_t、埠帶 http_port_t，政策定義哪種標籤能碰哪種標籤
                                          ├─ 標籤錯了 → restorecon 修檔案標籤 / semanage port 加埠標籤
                                          ├─ 政策太嚴 → boolean（setsebool）打開某類被預設關閉的行為
                                          └─ 被擋時   → 核心寫一筆 AVC 拒絕紀錄到 /var/log/audit/audit.log（ausearch / sealert 讀）
```

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **DAC / MAC** | DAC（Discretionary）是檔案擁有者自己決定的 rwx 權限；MAC（Mandatory）是系統政策強制、擁有者也改不了的規則 | MAC 只會「再多擋」，永遠不會放行 DAC 已拒絕的東西 |
| **AppArmor / profile** | 🟠 以**程式路徑**為單位的 MAC；每個受管程式一個 profile，寫明它能存取哪些路徑、能力 | 沒有 profile 的程式不受限；profile 可設 enforce（擋）或 complain（只記錄） |
| **SELinux / 標籤（label）** | 🔵 以**標籤**為單位的 MAC：每個程序、檔案、埠、socket 都帶一個安全上下文，政策以標籤對標籤定義允許的存取（type enforcement） | 標籤跟著檔案走，`cp` 到新位置可能帶著舊標籤——這是「複製後服務讀不到」的根源 |
| **targeted policy** | Fedora 預設的 SELinux 政策：只把網路服務等「目標」程序關進限制域，一般使用者程序不受限 | 所以桌面日常幾乎感覺不到 SELinux，只有服務會被擋 |
| **enforcing / permissive / disabled** | SELinux 三種模式：擋並記錄 / 只記錄不擋 / 完全關閉 | `setenforce 0/1` 是即時切換（重開恢復）；`disabled` 會讓檔案不再被標籤，切回來要整碟重標（relabel），所以 Fedora 建議永遠不要 disabled |
| **boolean** | 政策內預留的開關（`getsebool -a` 列出），例如 `httpd_can_network_connect` 允許 web 服務主動連外 | 開 boolean 是「政策內合法的放寬」，不必寫自訂政策；`-P` 才會持久 |
| **restorecon / semanage** | `restorecon` 依政策預設把檔案標籤重設；`semanage` 修改政策的持久設定（`port` 加埠標籤、`fcontext` 加自訂路徑的預設標籤） | 先 `semanage fcontext` 定義規則、再 `restorecon` 套用，是處理非標準目錄的正規流程 |
| **AVC / ausearch / sealert** | AVC（Access Vector Cache）拒絕紀錄是 SELinux 擋人時寫進 audit.log 的那一行；`ausearch -m avc` 篩出來，`sealert` 進一步翻譯成「該開哪個 boolean / 該下哪條指令」 | 排障永遠從這裡開始，而不是猜 |

**為什麼**：傳統的檔案權限只管「誰能讀寫什麼」，而 MAC 管的是「這個程式即使被入侵，也只能碰它該碰的東西」——nginx 被打穿了也讀不到 `/etc/shadow`、跑不了 shell。代價是服務偶爾會被莫名擋住（複製檔案後標籤不對、要連外時 boolean 沒開），很多人因此直接關掉，等於把整層防護丟掉。

兩者選擇不同是設計哲學差異：Ubuntu 跟隨 Debian / Novell 傳統採 AppArmor，以檔案路徑寫規則、易讀易寫、逐服務漸進部署；Fedora 是 SELinux 的上游發源地，以標籤做 type enforcement，防護更完整但學習曲線較陡，且預設對所有目標服務 enforcing。

| | 🟠 Ubuntu AppArmor | 🔵 Fedora SELinux |
|---|---|---|
| 模型 | 路徑（path）為基礎的 profile | 標籤（label）為基礎的 type enforcement |
| 預設 | enabled，多數服務 enforce | **enforcing**（targeted policy） |
| 查狀態 | `sudo aa-status` | `getenforce`、`sestatus` |
| 暫時關閉 | `sudo aa-complain /etc/apparmor.d/usr.sbin.nginx`（單一 profile） | `sudo setenforce 0`（全域 permissive，重開機恢復） |
| 永久設定 | `/etc/apparmor.d/`；`systemctl disable apparmor` | `/etc/selinux/config` 的 `SELINUX=enforcing|permissive|disabled`（Fedora 建議永遠不要 disabled） |
| 除錯 | `journalctl -k -g apparmor` / `dmesg` 中 `DENIED` | `sudo ausearch -m avc -ts recent`、`sealert -a /var/log/audit/audit.log` |

**怎麼做**：Fedora 上九成的 SELinux 問題只需要這三個指令之一——檔案從別處複製過來標籤不對就 `restorecon`，服務要連外或存取非標準位置就開對應 boolean，服務要綁非標準埠就查 / 加埠標籤：

```bash
# 🔵 Fedora 三個最常用的 SELinux 操作
sudo restorecon -Rv /var/www/html            # 修正檔案標籤（複製檔案後常需要）
sudo setsebool -P httpd_can_network_connect 1 # 開布林值（-P 永久）
sudo semanage port -l | grep http            # 查埠標籤
```

⚠️ **不要為了「省事」關閉 SELinux 或 AppArmor**。多數問題只要 `restorecon` 或開對 boolean 即可，見 [08 安全強化](08-安全強化.md)。

**注意**：懷疑是 MAC 擋住時，先 `setenforce 0` / `aa-complain` 測試一次，問題消失就確認是 MAC，再回到 enforcing 用 `ausearch` / `journalctl` 找出正確的修法，不要停在 permissive。

---

## 8. 系統更新與基本工具

**名詞與關係**：「更新」在兩邊各有一個指令，但更新完還有「哪些東西要重開才生效」的問題；名詞如下：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **apt / dnf** | 兩個發行版的套件管理器（🟠 `apt`、🔵 `dnf`，Fedora 41+ 為 dnf5），負責從套件庫下載、解相依、安裝與更新 | 本章與 §9.1 的自動更新都建立在它們之上；套件名慣例差異見下表 |
| **`full-upgrade` vs `upgrade`** | 🟠 apt 的 `upgrade` 不會為了升級而移除既有套件，`full-upgrade` 允許（核心版本更換時常需要）；🔵 dnf 的 `upgrade` 本來就允許 | 伺服器第一次更新用 `full-upgrade`，才不會因為相依變動而漏掉核心 |
| **glibc** | GNU C Library，幾乎所有程式都動態連結的基礎函式庫 | 更新它之後，已在執行的程序仍用舊版；與核心一樣要重開（或重啟所有服務）才真的生效 |
| **`reboot-required` / `needs-restarting`** | 🟠 套件安裝後若需要重開機會建立 `/var/run/reboot-required`；🔵 `dnf needs-restarting -r` 檢查核心 / glibc / systemd 是否更新過並回傳退出碼 | 沒有它們，只能靠人記得「剛才更新有沒有含核心」 |
| **qemu-guest-agent** | 虛擬機內與 hypervisor 溝通的 agent（§9.5），基本工具清單先把它裝上 | 實體機上安裝也無害（服務偵測不到虛擬通道就不會啟動） |

**為什麼**：安裝映像檔通常已落後數週到數月，裡面的 openssh、glibc、核心可能帶有已公開的漏洞，上線前第一次完整更新是最便宜的防護。基本工具則是為了之後排障——半夜出事時才發現沒有 `htop`、`lsof`、`dig`，而網路又剛好斷了，就只能乾瞪眼。

**怎麼做**：更新完記得檢查是否需要重開機（核心或 glibc 更新後，不重開等於沒更新）：

```bash
# 🟠 Ubuntu
sudo apt update && sudo apt full-upgrade -y
sudo apt install -y vim curl wget git htop tmux net-tools dnsutils unzip \
    tree jq rsync lsof strace ncdu qemu-guest-agent
[ -f /var/run/reboot-required ] && echo "需要重開機"

# 🔵 Fedora
sudo dnf upgrade -y
sudo dnf install -y vim-enhanced curl wget git htop tmux net-tools bind-utils unzip \
    tree jq rsync lsof strace ncdu qemu-guest-agent
sudo dnf needs-restarting -r
```

套件名稱差異（常踩雷）。差異來自兩個上游的打包慣例：Debian 把開發標頭拆成 `-dev`、Red Hat 用 `-devel`；Debian 傾向以上游專案名或功能命名（`dnsutils`、`apache2`），Fedora 則跟隨上游原始套件名（`bind-utils`、`httpd`）：

| 用途 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| dig / nslookup | `dnsutils` | `bind-utils` |
| vim 完整版 | `vim` | `vim-enhanced` |
| 編譯工具 | `build-essential` | `@development-tools`（`dnf group install development-tools`） |
| Python 開發 | `python3-dev`, `python3-venv` | `python3-devel` |
| SSL 開發 | `libssl-dev` | `openssl-devel` |
| 開發函式庫命名 | `lib<name>-dev` | `<name>-devel` |
| Apache | `apache2` | `httpd` |
| MariaDB 伺服器 | `mariadb-server` | `mariadb-server` |
| 網路工具 | `iproute2` | `iproute` |
| ping | `iputils-ping` | `iputils` |
| ps/top | `procps` | `procps-ng` |
| cron | `cron` | `cronie` |
| Java | `default-jdk` / `openjdk-21-jdk` | `java-21-openjdk-devel` |

---

## 9. 其他常見初始設定

### 9.1 自動安全更新（詳見第 08 章）

**名詞與關係**：自動更新工具靠「套件庫對更新的分類」決定只裝哪些，靠 systemd timer 決定什麼時候跑：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **安全更新（security update）** | 只修漏洞、不加功能的套件版本：🟠 來自 `-security` 套件庫（如 `noble-security`），🔵 由套件庫更新元資料標記 `type=security` | 自動更新工具就是用這個分類做過濾，所以「只裝安全更新」不必人工判斷 |
| **CVE** | Common Vulnerabilities and Exposures，公開漏洞的統一編號 | 安全更新的變更紀錄會列出修了哪些 CVE，掃描器與稽核也用它對帳 |
| **unattended-upgrades** | 🟠 Debian 系的自動更新套件：`/etc/apt/apt.conf.d/20auto-upgrades` 是總開關、`50unattended-upgrades` 定義允許的來源 | Ubuntu Server 預設已裝並啟用，只允許 `-security` 來源 |
| **dnf-automatic（dnf5-plugin-automatic）** | 🔵 dnf 的自動更新外掛，設定在 `/etc/dnf/automatic.conf`：`apply_updates` 決定「只下載」還是「真的安裝」，`upgrade_type` 決定 `default`（全部）還是 `security` | 需自行安裝；`dnf-automatic.timer` 是相容的別名 |
| **systemd timer** | 取代 cron 的排程 unit（§4.1 的 unit 之一），`.timer` 到點就啟動同名 `.service` | 兩邊的自動更新都由 timer 觸發（🟠 `apt-daily-upgrade.timer`、🔵 `dnf5-automatic.timer`）；`systemctl list-timers` 看下次執行時間 |
| **`%ghost` 檔** | RPM 規格中「屬於套件、但不隨套件安裝」的檔案，通常是留給使用者自己建立的設定檔 | 這就是 `automatic.conf` 裝完卻不存在的原因，得先從範本複製 |

**為什麼**：重大漏洞（如 OpenSSH、glibc 的 CVE）公開後幾小時內就會有掃描與攻擊，靠人記得手動更新來不及；只套用安全更新、不動一般功能更新，能在「盡快補洞」與「不要半夜被更新弄壞服務」之間取得平衡。Ubuntu 預設就開著，Fedora 需要自己裝。

**怎麼做**：

```bash
# 🟠 Ubuntu：預設已啟用 unattended-upgrades（僅安全更新）
cat /etc/apt/apt.conf.d/20auto-upgrades
# 🔵 Fedora
sudo dnf install -y dnf5-plugin-automatic
# ⚠️ 實測：dnf5 的 /etc/dnf/automatic.conf 是 %ghost 檔，安裝後不一定存在；
#    預設值在 /usr/share/dnf5/dnf5-plugins/automatic.conf，要改設定先複製過來
[ -f /etc/dnf/automatic.conf ] || sudo cp /usr/share/dnf5/dnf5-plugins/automatic.conf /etc/dnf/automatic.conf
sudo sed -i 's/^apply_updates *=.*/apply_updates = yes/; s/^upgrade_type *=.*/upgrade_type = security/' /etc/dnf/automatic.conf
sudo systemctl enable --now dnf-automatic.timer      # 實為 dnf5-automatic.timer 的別名，每日 06:00 執行
```

**注意**：自動更新不會幫你重開機，核心更新後仍要定期用 §8 的 `reboot-required` / `needs-restarting` 檢查並安排維護窗口。

### 9.2 登入橫幅與 MOTD

**名詞與關係**：橫幅與 MOTD 的差別是「認證前 vs 認證後」——前者誰都看得到，所以只放法律聲明；後者只有登進來的人看得到，可以放系統訊息：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **登入前橫幅（banner）** | 認證**之前**顯示的文字：本機 console 讀 `/etc/issue`，SSH 由 sshd 的 `Banner` 選項送出（慣例指向 `/etc/issue.net`） | 攻擊者不用密碼就看得到，所以內容不能透露任何系統資訊 |
| **MOTD** | Message of the Day，登入**成功之後**由 PAM（§3.4）的 `pam_motd` 模組顯示的訊息 | 已認證的人才看得到，適合放維護公告 |
| **`/etc/update-motd.d/`** | 🟠 Ubuntu 的動態 MOTD：目錄內每個可執行腳本的輸出串接成 MOTD，登入時即時產生 | `chmod -x` 等於停用該片段；`50-motd-news` 會連線 `motd.ubuntu.com` 抓訊息（`motd-news`） |
| **`/etc/motd`、`/etc/motd.d/`** | 靜態 MOTD 檔，內容原樣顯示（⚪ 兩邊都支援；🔵 Fedora 只有這種） | 🟠 Ubuntu 的 `/etc/motd` 也會顯示，但排在動態內容之後 |

**為什麼**：登入前橫幅主要是法律與稽核需求——很多合規框架要求在認證前明示「僅限授權使用、活動受監控」，否則入侵者可主張不知情。MOTD 則相反，Ubuntu 預設的動態 MOTD 會連網抓廣告訊息（`motd-news`），在隔離環境會讓登入變慢，關掉比較乾淨。

**怎麼做**：

```bash
# ⚪ SSH 登入前橫幅
echo "Authorized access only. Activity is monitored." | sudo tee /etc/issue.net
echo "Banner /etc/issue.net" | sudo tee /etc/ssh/sshd_config.d/20-banner.conf

# 🟠 Ubuntu：MOTD 由 /etc/update-motd.d/ 動態產生；可關閉廣告
sudo chmod -x /etc/update-motd.d/50-motd-news /etc/update-motd.d/10-help-text
# 🔵 Fedora：/etc/motd 與 /etc/motd.d/*.（靜態）
```

**注意**：橫幅內容不要透露發行版、版本或用途（如「Production DB」），那是在幫攻擊者做偵察。

### 9.3 Cockpit 網頁管理介面

**名詞與關係**：Cockpit 是「用瀏覽器做 systemctl / journalctl 會做的事」，它的啟動方式與登入方式各借用了本章前面講過的機制：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **Cockpit** | Red Hat 主導的網頁管理介面：核心套件 `cockpit` 加各功能模組（`cockpit-storaged`、`cockpit-podman`…），底層直接呼叫 systemd、journald、NetworkManager 的 API | 🔵 Fedora Server 預設已裝；🟠 Ubuntu 在 universe 套件庫 |
| **`cockpit.socket`** | 用 §4.1 的 socket activation：systemd 代聽 9090，有人連線才啟動 `cockpit.service`（`cockpit-ws`） | 所以 `enable` 的是 `.socket` 而非 `.service`；沒人用時記憶體佔用為零 |
| **9090 / firewalld 的 `cockpit` service** | Cockpit 的預設 HTTPS 埠；firewalld 內建同名 service 定義（§6） | 🟠 ufw 沒有內建定義，直接放 `9090/tcp` |
| **密碼登入（PAM）** | Cockpit 透過 PAM 驗系統帳號密碼，與 SSH 的 `PasswordAuthentication` 設定無關 | 這是 §3.4 密碼政策仍然重要的原因；也可改設定用 SSH 金鑰或 SSO |

**為什麼**：不是每位需要看機器狀態的人都熟 CLI；Cockpit 提供瀏覽器介面看日誌、服務、儲存、網路與更新，也能開 web terminal，對小團隊或交接給非 Linux 背景的同事很實用。它走 socket activation，沒人連線時不佔資源。

**怎麼做**：

```bash
# 🟠 Ubuntu
sudo apt install -y cockpit
# 🔵 Fedora Server 預設已安裝；Workstation 需：
sudo dnf install -y cockpit
# ⚪
sudo systemctl enable --now cockpit.socket
# 🟠 sudo ufw allow 9090/tcp   /   🔵 sudo firewall-cmd --permanent --add-service=cockpit && sudo firewall-cmd --reload
# 瀏覽 https://<ip>:9090
```

**注意**：Cockpit 用系統帳號密碼登入，若 §4.2 已關閉 SSH 密碼登入，9090 就成了唯一還接受密碼的入口；公網機器請只對管理網段開放，或改走 SSH 通道。

### 9.4 Shell 環境

**名詞與關係**：要讓設定「對所有人、兩個發行版都生效」，得先知道 bash 啟動時讀哪些檔、順序如何：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **login shell vs interactive shell** | 登入（console、SSH）時的第一個 shell 是 login shell，讀 `/etc/profile` → `~/.profile`；之後開的每個終端機是 interactive shell，讀全域 bashrc（🟠 `/etc/bash.bashrc`、🔵 `/etc/bashrc`）→ `~/.bashrc` | 兩個發行版的 `~/.profile` 都會 source `~/.bashrc`，所以放在 profile 層的變數兩種 shell 都看得到 |
| **`/etc/profile.d/*.sh`** | `/etc/profile` 末尾會逐一 source 的目錄，兩邊路徑相同 | 放這裡不必碰名稱不同的全域 bashrc，套件更新也不會覆蓋 |
| **`EDITOR` / `VISUAL`** | 需要開編輯器的程式（`visudo`、`systemctl edit`、`crontab -e`、git）讀的環境變數 | 沒設時各程式各有預設（🟠 多為 nano、🔵 為 vi），行為不一致 |
| **`HISTSIZE` / `HISTTIMEFORMAT`** | bash 記憶體內保留的歷史筆數 / 顯示歷史時的時間格式；設了後者，bash 才會把時間戳寫進 `~/.bash_history` | `HISTFILESIZE` 管檔案筆數，需要長期保留時一起設；歷史只是輔助，正式稽核仍要靠 §3 的 sudo 日誌 |

**為什麼**：預設的 bash 歷史只留 500 筆、沒有時間戳，事故後想回查「昨天誰在幾點跑了什麼」查不到；`EDITOR` 沒設則 `visudo`、`systemctl edit`、`crontab -e` 會落到 nano 或提示錯誤。放在 `/etc/profile.d/` 對所有使用者生效，且兩個發行版都會讀，不必碰各自不同的全域 bashrc。

**怎麼做**：

```bash
# ⚪ 系統層級：/etc/profile.d/*.sh（兩者都會讀）
sudo tee /etc/profile.d/custom.sh <<'EOF'
export EDITOR=vim
export HISTSIZE=10000
export HISTTIMEFORMAT="%F %T "
alias ll='ls -alF'
EOF
# 🟠 Ubuntu 的全域 bashrc：/etc/bash.bashrc
# 🔵 Fedora 的全域 bashrc：/etc/bashrc
```

### 9.5 虛擬機 Guest Agent

**名詞與關係**：guest agent 是 hypervisor 伸進 VM 裡的「手」，每個平台一套，功能相同：

| 名詞 | 一句話定義 | 與其他名詞的關係 |
|---|---|---|
| **guest agent** | 跑在 VM 內、透過虛擬化平台的專用通道（不走網路）與 hypervisor 對話的 daemon | 平台端的「查 IP、凍結檔案系統、要求乾淨關機、對時」都靠它在 VM 內執行；沒裝就只剩「拔電源」層級的控制 |
| **qemu-guest-agent** | KVM / QEMU（Proxmox、oVirt、OpenStack、libvirt）的 guest agent，透過 virtio-serial 通道 | 服務名 `qemu-guest-agent`；VM 設定端也要開啟 agent 通道（Proxmox 的「QEMU Guest Agent」選項）才通 |
| **open-vm-tools** | VMware 的開源 guest tools，取代舊的 VMware Tools ISO | 兩邊套件名相同；VMware 官方也建議用它 |
| **`hv_*` 模組 / hyperv-daemons** | Hyper-V 的整合服務：核心內建驅動（`hv_vmbus`、`hv_utils`…）負責通道，🔵 `hyperv-daemons` 補上 KVP / VSS / fcopy 三個使用者空間 daemon | 只有核心模組時基本功能可用；一致性快照（VSS）要 daemon 才完整 |
| **一致性快照（fsfreeze）** | 平台做快照前，由 agent 呼叫 `fsfreeze` 暫停檔案系統寫入並把緩衝 flush 到磁碟，快照完再解凍 | 沒有 agent 的快照 = 相當於當下拔電源的磁碟狀態，資料庫可能不一致 |

**為什麼**：沒有 guest agent，虛擬化平台看不到 VM 的 IP、無法做一致性快照（備份時檔案系統沒 freeze，資料庫可能不一致）、也無法乾淨關機（只能等同於拔電源）。這是虛擬機上線前最常被遺漏、但影響備份可靠性的一步。

**怎麼做**：依平台安裝對應套件（§8 的基本工具清單已含 `qemu-guest-agent`），裝完 `systemctl enable --now` 即可：

| 平台 | 套件（兩者名稱相同） |
|---|---|
| KVM / Proxmox | `qemu-guest-agent` |
| VMware | `open-vm-tools` |
| Hyper-V | 核心內建（`hv_*` 模組）；🔵 Fedora 可裝 `hyperv-daemons` |
| VirtualBox | `virtualbox-guest-additions-iso`（🟠）/ `virtualbox-guest-additions`（🔵，RPM Fusion） |

---

## 10. 一鍵初始化腳本

**為什麼**：本章的步驟手動做一次要半小時，做第五台時一定會漏掉某一步（最常見是忘了開防火牆或忘了 `-aG`）；把流程收成腳本，每台機器的起點就一致，之後排障時不必先懷疑「這台是不是當初少設了什麼」。

**怎麼做**：[`scripts/bootstrap.sh`](scripts/bootstrap.sh) 會自動偵測發行版並完成：更新系統、安裝基本工具、設定時區、建立管理員、SSH 強化、啟用防火牆、自動安全更新。

```bash
curl -fsSL https://<your-repo>/scripts/bootstrap.sh -o bootstrap.sh
sudo bash bootstrap.sh --user alice --pubkey "ssh-ed25519 AAAA..." --timezone Asia/Taipei
```

腳本原始碼見下方連結；重點在於 `ID` 偵測（`/etc/os-release` 是每個發行版都有的識別檔，`ID=` 欄位是發行版代號、`ID_LIKE=` 是它的上游家族，用 `.` source 進來就成為 shell 變數）與雙發行版分支——本章所有「🟠 / 🔵」差異（套件管理器、管理員群組、SSH 服務名）都收斂在這幾個變數裡：

```bash
. /etc/os-release
case "$ID" in
  ubuntu|debian) PKG="apt-get -y -q"; SUDO_GROUP=sudo;  SSH_SVC=ssh  ;;
  fedora|rhel|rocky|almalinux) PKG="dnf -y -q"; SUDO_GROUP=wheel; SSH_SVC=sshd ;;
esac
```

**注意**：執行前先看過腳本內容再 `sudo bash`；`--pubkey` 一定要給，否則腳本關閉密碼登入後會沒有任何方式進得去。

---

## 11. 本章差異總結

以下把本章出現過的發行版差異集中成一張表，方便寫跨發行版腳本或交接時快速對照。大多數差異不是誰對誰錯，而是上游路線不同：Ubuntu 承襲 Debian 的工具與命名、偏向輕量預設（timesyncd、ufw 未啟用）；Fedora 作為 RHEL 上游，預設就啟用完整的企業級元件（chrony、firewalld、SELinux enforcing、Cockpit）。

| 項目 | 🟠 Ubuntu | 🔵 Fedora |
|---|---|---|
| 管理員群組 | `sudo` | `wheel` |
| 建立使用者 | `adduser`（互動式） | `useradd -m` + `passwd` |
| SSH 服務 | `ssh`（`ssh.socket` 啟動，改 Port 要 override socket） | `sshd`（改 Port 要 `semanage port`） |
| SSH 加密設定 | 直接寫 sshd_config | crypto-policies 統一管理 |
| 時間同步 | timesyncd（可換 chrony，設定在 `/etc/chrony/chrony.conf`） | chrony（`/etc/chrony.conf`） |
| 語系 | `locale-gen` + `update-locale` | `langpacks-*` + `localectl` |
| 網路設定 | Netplan YAML | nmcli / NetworkManager keyfile |
| 防火牆 | ufw（需手動 enable） | firewalld（預設啟用） |
| MAC | AppArmor | SELinux（enforcing） |
| 自動更新 | unattended-upgrades（預設啟用） | dnf5-plugin-automatic（需安裝） |
| MOTD | 動態 `/etc/update-motd.d/` | 靜態 `/etc/motd` |
| 全域 bashrc | `/etc/bash.bashrc` | `/etc/bashrc` |
| 網頁管理 | 需安裝 Cockpit | Server 預設 Cockpit |
