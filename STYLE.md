# 編修規範：Why → How

本手冊每一個重點都採「**為什麼（Why）→ 怎麼做（How）**」的順序撰寫。讀者在看到任何指令之前，必須先知道：這個設定解決什麼問題、不做會怎樣、為什麼選這個做法而不是另一個。

## 結構

1. **章節開頭**：一段「本章解決什麼問題」——說明這個主題在真實運維中的痛點、不處理的後果、以及本章的取捨原則。
2. **每個 `##` / `###` 小節**：先寫 **為什麼**（1～3 句：問題、風險、背景），再寫 **怎麼做**（指令、設定），必要時補 **注意**（陷阱、驗證方式）。用粗體標籤 `**為什麼**`、`**怎麼做**`、`**注意**` 開頭，讓讀者一眼看到層次。
3. **對照表**：表格前一句說明「為什麼兩個發行版在這件事上會不同」（歷史 / 設計哲學 / 上游差異）。
4. **指令區塊**：指令本身不改（已實測）；解釋寫在區塊前的文字或區塊內的註解。
5. 避免只有 What 的句子：「設定 X 為 Y」→ 改為「因為 A 會導致 B，所以把 X 設為 Y；設定方法如下」。

## 範例

改寫前（只有 What）：

> ### 4.3 更改 SSH Port
> ```bash
> echo "Port 2222" | sudo tee /etc/ssh/sshd_config.d/05-port.conf
> ```

改寫後（Why → How）：

> ### 4.3 更改 SSH Port
>
> **為什麼**：22 埠每天會收到數千次自動化掃描與暴力嘗試，塞爆 `auth.log`、也讓 Fail2ban 忙不完。改埠**不是安全措施**（掃描器幾分鐘就找得到），但能把日誌雜訊降 90% 以上，讓真正的攻擊比較容易被看見。若已做到金鑰登入 + 防火牆限來源，改不改埠都可以。
>
> **怎麼做**：sshd 讀 `sshd_config.d/` 內的 drop-in，所以只要新增一個檔案：
> ```bash
> echo "Port 2222" | sudo tee /etc/ssh/sshd_config.d/05-port.conf
> ```
>
> **注意**：🟠 Ubuntu 24.04 的 sshd 由 `ssh.socket` 啟動，只改 `sshd_config` 沒用，還要覆寫 socket 的 `ListenStream`；🔵 Fedora 的 SELinux 只允許 sshd 綁 22，要先 `semanage port` 標記新埠，否則 sshd 起不來。

## 不可更動的內容

- 所有 ``` 程式碼區塊內容（已於 Ubuntu 24.04 / Fedora 44 實測）。
- 🟠 / 🔵 / ⚪ / ⚠️ / 💡 標記與章節編號。
- 表格中的指令、路徑、版本號。
